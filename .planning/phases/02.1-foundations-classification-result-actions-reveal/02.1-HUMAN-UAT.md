---
status: resolved
phase: 02.1-foundations-classification-result-actions-reveal
source: [02.1-VERIFICATION.md]
started: 2026-05-15T10:55:00Z
updated: 2026-05-15T13:30:00Z
---

## Current Test

[all tests complete]

## Tests

### 1. E2E completo anónimo → wall → login → vuelta a /planta/:id
expected: Tras completar login desde el wall, la app navega automáticamente a /planta/:id mostrando el PersistentClassificationBanner con el contexto elegido. La fila en DB tiene user_id=user.id, anonymous_id=NULL, context=action.
result: pass — verificado por usuario tras fix LoginForm navigate race. Confirmado con cuenta existente.

### 2. Identificación autenticada → tap card → morph (5s) → banner
expected: Tras tap, ClassificationMorph aparece sobre el accordion (verde primary + ✓ + Deshacer); 5s sin tap colapsa a PersistentClassificationBanner. Durante 5s el row en DB ya está clasificado.
result: pass — requirió fix useCallback en handlers de PlantResultView (timer del morph se reiniciaba en cada re-render).

### 3. Tap Deshacer dentro de 5s revierte a unclassified
expected: El row en DB vuelve a context='unclassified', las 2 cards reaparecen, no se dispara classification_completed event.
result: pass

### 4. Tap Cambiar en banner muestra cards sin DB write
expected: Las 2 cards reaparecen, el row en DB sigue con el context actual hasta el siguiente tap en una card.
result: pass — verificado por re-tap de la card siguiente: vuelve a morph → banner sin error.

### 5. UnclassifiedSection visible en home auth con count>0, ausente con count=0 y para anónimos
expected: Visible con header 'Sin clasificar (N)' + grid de hasta 4 thumbs cuando user existe y count>0; null en los otros 2 casos.
result: pass — incluye fix de route /historial → /mis-plantas?context=unclassified.

### 6. PostHog Live Events: los 10 eventos emiten con propiedades correctas
expected: 10 eventos PostHog del Phase 02.1 con propiedades documentadas.
result: code-verified — call sites confirmados por gsd-code-reviewer agent y validados con grep across src/. Run end-to-end no validable en localhost: src/lib/track.ts:47 setea `opt_out_capturing_by_default: isLocalhost` (decisión pre-existente). Pendiente validación en preview/prod.

### 7. Migración RLS UPDATE policy aplicada en remoto
expected: 'Users can update own searches' presente en pg_policies para plant_searches.
result: pass — verificado por SQL query del usuario: las 5 policies presentes (SELECT, 2× INSERT, UPDATE, DELETE).

### 8. Reduced-motion respetado en los 3 componentes
expected: Con prefers-reduced-motion: reduce, transiciones se reducen a opacity-only con duration ≤ 100ms.
result: skipped — código tiene useReducedMotion() en ClassificationCards, ClassificationMorph, PersistentClassificationBanner. Validación visual diferida a otra sesión.

### 9. Sweep manual del flujo: identificación → cards → morph → banner → Cambiar → cards
expected: Eyebrow cambia atómicamente con el sub-render (UI-SPEC FLAG #5); usuario nunca ve copy desincronizada.
result: pass — con cambio de diseño durante UAT. Decisión de producto: en vez del eyebrow atómico flip, dos secciones permanentes "¿Qué hacemos con esta planta?" + "Más sobre esta planta". Más claro como ficha. Implementado en commit 6d67e30.

## Summary

total: 9
passed: 7
issues: 0
pending: 0
skipped: 1
blocked: 0
code-verified: 1

## Gaps

(none — todos los issues encontrados durante UAT se arreglaron inline)

### Bugs encontrados y corregidos durante UAT

1. **Eyebrow duplicado en cards phase** (ClassificationCards traía su propio eyebrow + Plan 07 añadió otro al padre) → fix: drop internal eyebrow de Cards (commit 5323e08).
2. **Morph nunca llegaba a banner** (timer reiniciado por re-renders por handlers no memoizados) → fix: useCallback en handleClassify/Undo/Commit/Change (commit 28d5607).
3. **Cambio de UX:** dos secciones permanentes en vez de eyebrow atómico (commit 6d67e30).
4. **Auto-navigate post-login no funcionaba** (LoginForm navigate("/mis-plantas") corría síncrono y ganaba la race contra el listener del CustomEvent) → fix: remove premature navigate from LoginForm (commit e36d0c8).
5. **Route /historial → /mis-plantas?context=unclassified** (typo en CONTEXT.md propagado) → fix: surfaced por verifier, corregido (commit e74f5e5).
