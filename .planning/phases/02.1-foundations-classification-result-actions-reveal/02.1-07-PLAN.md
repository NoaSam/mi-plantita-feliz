---
phase: 02.1
plan: 07
type: execute
wave: 3
depends_on: [01, 02, 03, 04, 05, 06]
files_modified:
  - src/pages/PlantDetail.tsx
  - src/components/PlantResultView.tsx
  - src/pages/Index.tsx
  - src/pages/History.tsx
  - src/App.tsx
autonomous: true
requirements: [SPEC-2, SPEC-4, SPEC-5, SPEC-6, SPEC-9, SPEC-10]
tags: [pages, integration, routing, posthog, tracking-sweep]
must_haves:
  truths:
    - "`PlantResultView` deriva eyebrow + sub-render del MISMO `plant.context` en una sola expresión — eyebrow y sección de clasificación cambian atómicamente, sin flash de copy incorrecta (UI-SPEC FLAG #5, SPEC-2)."
    - "`PlantResultView` mantiene state machine local: `phase: 'cards' | 'morph' | 'banner'` y `pendingAction: 'home' | 'wild' | null`; SOLO controla qué se renderiza, NO duplica el `context` de la fila DB (D-08)."
    - "Cuando `auth.user === null` y se tap una card, `PlantResultView` NO llama `useClassifyPlant.classify` — abre `<AnonClassificationWall>` en su lugar (SPEC-7, D-13)."
    - "Cuando `auth.user` existe y `useClassifyPlant.classify(id, action)` resuelve `{ ok: false }`, `PlantResultView` llama `toast.error('No se pudo guardar. Reintenta.')` y NO transiciona a morph — las cards permanecen visibles (D-09)."
    - "El accordion (Qué es / Cómo cuidarla / Qué le pasa) renderiza SIEMPRE, independiente de `context` o `phase` — la sección de clasificación va ENCIMA del accordion sin reemplazarlo (SPEC-2 AC)."
    - "La nueva página `src/pages/PlantDetail.tsx` monta `usePlantById(id)` (Plan 02); cuando `notFound === true`, ejecuta `toast.error('Esta planta no existe o ya no está disponible')` y `navigate('/', { replace: true })` (D-06)."
    - "`PlantDetail` renderiza `<PlantResultView plant={plant} />` SIN pasar `onReset` — el botón \"Volver a empezar\" es propiedad de `Index.tsx` (post-identificación), no de detail. `PlantResultView` debe aceptar `onReset?` opcional (D-05)."
    - "`Index.tsx` monta `<UnclassifiedSection />` (Plan 05) DESPUÉS de `<PhotoCapture>` ÚNICAMENTE cuando `auth.user !== null`. La sección maneja internamente el caso `count === 0 → null` (SPEC-5)."
    - "`History.tsx` ELIMINA la inline expansion (líneas ~134-140 + 177-201 del archivo original) y reemplaza el handler de la fila por `() => { track('history_item_clicked', { context: item.context, position: index }); navigate('/planta/' + item.id); }`. Las propiedades del event coinciden con el contrato `docs/posthog-events.md` § Phase 02.1 (`{ context, position }`). Edit mode con checkbox sigue igual (D-07, SPEC-9, SPEC-10)."
    - "`History.tsx` monta `<HistorySummary />` (Plan 05) encima de la lista con counts calculados via `useMemo` sobre `history` (SPEC-6)."
    - "`History.tsx` renderiza `<ContextChip context={item.context} />` (Plan 05) por fila a la derecha del bloque de info (SPEC-6)."
    - "`History.tsx` lee `useSearchParams()` para honrar el deep link `?context=unclassified` (de \"Ver todas\" en UnclassifiedSection — Plan 05). Si el param es `'unclassified'`, filtra `filtered` para mostrar solo filas con `context === 'unclassified'`. NO se renderiza UI de filtros (SPEC-6 AC, D-07)."
    - "`App.tsx` registra `<Route path=\"/planta/:id\" element={<AppLayout><PlantDetail /></AppLayout>} />` junto a las rutas existentes; añade `/signup` como `Navigate to=\"/mis-plantas\" replace` (mismo patrón que `/login` existente) (D-05)."
    - "`App.tsx` registra un `useEffect` con `window.addEventListener('mp:pending-classification-resolved', handler)` que llama `navigate('/planta/' + e.detail.plant_search_id, { replace: true })`. Cleanup en el return del effect quita el listener (D-13, contrato de Plan 03)."
    - "El `<Toaster />` montado en Plan 01 (App.tsx) NO se duplica; verificar presencia con grep antes de añadir nada."
    - "Sweep de tracking: los 10 events PostHog del Phase 02.1 (`classification_action_clicked`, `classification_undo_clicked`, `classification_completed`, `classification_change_clicked`, `unclassified_section_shown`, `unclassified_section_clicked`, `history_item_clicked`, `anon_classification_wall_shown`, `anon_classification_wall_action`, `anon_searches_claimed`) emiten desde sus call sites correctos. `history_item_clicked` se cablea AQUÍ (los demás los cablean Planes 03-06) (SPEC-10)."
  artifacts:
    - path: "src/pages/PlantDetail.tsx"
      provides: "Page that renders /planta/:id by mounting usePlantById + PlantResultView"
      exports: ["default PlantDetail"]
    - path: "src/components/PlantResultView.tsx"
      provides: "Result + detail view extended with classification section state machine"
      exports: ["default PlantResultView", "PlantResultViewProps"]
    - path: "src/pages/Index.tsx"
      provides: "Home with conditional UnclassifiedSection"
      exports: ["default Index"]
    - path: "src/pages/History.tsx"
      provides: "History list with HistorySummary header + ContextChip per row + navigation to detail"
      exports: ["default HistoryPage"]
    - path: "src/App.tsx"
      provides: "Router with /planta/:id, /signup redirect, and pending-classification-resolved listener"
      exports: ["default App"]
  key_links:
    - from: "src/pages/PlantDetail.tsx"
      to: "src/hooks/use-plant-by-id.ts"
      via: "const { plant, isLoading, notFound } = usePlantById(id)"
      pattern: "usePlantById"
    - from: "src/pages/PlantDetail.tsx"
      to: "src/components/PlantResultView.tsx"
      via: "<PlantResultView plant={plant} />"
      pattern: "PlantResultView"
    - from: "src/components/PlantResultView.tsx"
      to: "src/hooks/use-classify-plant.ts"
      via: "const { classify } = useClassifyPlant()"
      pattern: "useClassifyPlant"
    - from: "src/components/PlantResultView.tsx"
      to: "src/components/ClassificationCards.tsx"
      via: "import + render when phase === 'cards'"
      pattern: "ClassificationCards"
    - from: "src/components/PlantResultView.tsx"
      to: "src/components/ClassificationMorph.tsx"
      via: "import + render when phase === 'morph'"
      pattern: "ClassificationMorph"
    - from: "src/components/PlantResultView.tsx"
      to: "src/components/PersistentClassificationBanner.tsx"
      via: "import + render when phase === 'banner'"
      pattern: "PersistentClassificationBanner"
    - from: "src/components/PlantResultView.tsx"
      to: "src/components/AnonClassificationWall.tsx"
      via: "import + render when !auth.user (controlled by wallOpen state)"
      pattern: "AnonClassificationWall"
    - from: "src/pages/Index.tsx"
      to: "src/components/UnclassifiedSection.tsx"
      via: "import + render when auth.user !== null"
      pattern: "UnclassifiedSection"
    - from: "src/pages/History.tsx"
      to: "src/components/HistorySummary.tsx"
      via: "<HistorySummary counts={...} />"
      pattern: "HistorySummary"
    - from: "src/pages/History.tsx"
      to: "src/components/ContextChip.tsx"
      via: "<ContextChip context={item.context} />"
      pattern: "ContextChip"
    - from: "src/App.tsx"
      to: "src/pages/PlantDetail.tsx"
      via: "<Route path=\"/planta/:id\" element={<AppLayout><PlantDetail /></AppLayout>} />"
      pattern: "/planta/:id"
    - from: "src/App.tsx"
      to: "window CustomEvent listener"
      via: "addEventListener('mp:pending-classification-resolved', ...)"
      pattern: "mp:pending-classification-resolved"
---

<objective>
Integrar los building blocks de los Planes 01-06 en las páginas y rutas de la app. Esta es la única fase del Phase 02.1 que toca páginas existentes y registra rutas — los Planes 04-06 escribieron componentes aislados, este Plan los conecta.

Purpose: convertir el conjunto de hooks + componentes en un flujo end-to-end funcional: (1) la pantalla de resultado deja clasificar plantas, (2) la home revela "Sin clasificar (N)" cuando aplica, (3) la página de historial enseña el resumen + chips y navega al detalle, (4) el detalle de una planta es deep-linkable, (5) tras login desde el wall, el cliente vuelve automáticamente al detalle ya clasificado.

Output: 1 página nueva (`PlantDetail.tsx`), 4 archivos modificados (`PlantResultView`, `Index`, `History`, `App`), y un sweep de verificación de los 10 events PostHog.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

<task id="T07-01" type="execute">
<title>Crear página PlantDetail.tsx para la ruta /planta/:id</title>

<read_first>
- src/pages/PlantDetail.tsx (NO EXISTE — verificar con `ls`; este task lo crea)
- src/hooks/use-plant-by-id.ts (creado por Plan 02 — confirmar shape `{ plant, isLoading, notFound }`)
- src/components/PlantResultView.tsx (será modificado por T07-02 para aceptar `plant: PlantResult` con `context`)
- src/pages/History.tsx:114-117 (analog del skeleton de carga: `<Leaf className="size-10 text-primary animate-pulse-slow" strokeWidth={1.2} />` — reusar exactamente)
- src/components/auth/RequireAuth.tsx (la ruta NO debe estar gateada por RequireAuth — D-06: si la fila existe pero RLS la deniega, `usePlantById` devuelve `notFound = true` igual que si no existe; no leak de existencia)
- .planning/phases/02.1-foundations-classification-result-actions-reveal/02.1-PATTERNS.md § PlantDetail (analog de líneas)
</read_first>

<action>
Crear `src/pages/PlantDetail.tsx` con este shape exacto:

```tsx
import { useEffect } from "react";
import { useParams, useNavigate } from "react-router-dom";
import { Leaf } from "lucide-react";
import { toast } from "sonner";
import PlantResultView from "@/components/PlantResultView";
import { usePlantById } from "@/hooks/use-plant-by-id";

export default function PlantDetail() {
  const { id } = useParams<{ id: string }>();
  const navigate = useNavigate();
  const { plant, isLoading, notFound } = usePlantById(id);

  useEffect(() => {
    if (notFound) {
      toast.error("Esta planta no existe o ya no está disponible");
      navigate("/", { replace: true });
    }
  }, [notFound, navigate]);

  if (isLoading) {
    return (
      <div className="px-6 py-8 flex justify-center">
        <Leaf className="size-10 text-primary animate-pulse-slow" strokeWidth={1.2} />
      </div>
    );
  }

  if (!plant) return null;

  return (
    <div className="px-6 py-8">
      <PlantResultView plant={plant} />
    </div>
  );
}
```

Notas:
- NO se pasa `onReset` — `PlantResultView` lo declara opcional (T07-02). Cuando `onReset` es undefined, el componente NO renderiza el botón "Volver a empezar".
- El `useEffect` en `notFound` evita un render flash con plant=null y luego el navigate.
- El skeleton (Leaf pulsante) reusa exactamente el patrón de `History.tsx:115-117`.
- `useParams<{ id: string }>()` puede devolver `id` undefined; `usePlantById(undefined)` debe tratar undefined → `notFound = true` (Plan 02 task verificará esta entrada).
</action>

<acceptance_criteria>
- `test -f src/pages/PlantDetail.tsx`
- `grep -q "usePlantById" src/pages/PlantDetail.tsx`
- `grep -q 'useParams<{ id: string }>' src/pages/PlantDetail.tsx`
- `grep -q '"Esta planta no existe o ya no está disponible"' src/pages/PlantDetail.tsx`
- `grep -q "navigate('/', { replace: true })" src/pages/PlantDetail.tsx` (o `"/", { replace: true })`)
- `grep -q "<PlantResultView plant={plant} />" src/pages/PlantDetail.tsx`
- NO hay `onReset` en PlantDetail.tsx: `! grep -q "onReset" src/pages/PlantDetail.tsx`
- TypeScript build pasa: `npx tsc --noEmit` sale 0
</acceptance_criteria>

<done>PlantDetail mounted. T07-05 registrará la ruta en App.tsx para que sea alcanzable.</done>
</task>

<task id="T07-02" type="execute">
<title>Modificar PlantResultView.tsx — integrar sección de clasificación atómica con eyebrow</title>

<read_first>
- src/components/PlantResultView.tsx (1-99 — archivo completo; lee el original antes de editar)
- src/hooks/use-plant-identifier.ts (definir `PlantResult.context?: 'home' | 'wild' | 'unclassified'` — Plan 01 ya lo amplió)
- src/components/ClassificationCards.tsx (Plan 04 — props: `{ onClassify: (action) => void; pendingAction: 'home' | 'wild' | null; plantSearchId: string }`)
- src/components/ClassificationMorph.tsx (Plan 04 — props: `{ action: 'home' | 'wild'; onUndo: () => void; onCommit: () => void; plantSearchId: string }`)
- src/components/PersistentClassificationBanner.tsx (Plan 04 — props: `{ context: 'home' | 'wild'; onChange: () => void; plantSearchId: string }`)
- src/components/AnonClassificationWall.tsx (Plan 06 — props: `{ open: boolean; onOpenChange: (open: boolean) => void; plantSearchId: string; intendedAction: 'home' | 'wild' | null }`)
- src/hooks/use-classify-plant.ts (Plan 02 — `const { classify, revert } = useClassifyPlant()`; `classify(id, action) → Promise<{ ok: boolean }>`)
- src/contexts/AuthContext.tsx (`useAuth()` — `{ user, ... }`)
- src/lib/track.ts:56-59 (firma `track(event, properties?)`)
</read_first>

<action>
Reescribir `src/components/PlantResultView.tsx` con esta estructura. Mantén el accordion existente intacto.

```tsx
import { useState } from "react";
import { motion, AnimatePresence } from "framer-motion";
import { RotateCcw } from "lucide-react";
import { toast } from "sonner";
import { Button } from "@/components/ui/button";
import {
  Accordion,
  AccordionItem,
  AccordionTrigger,
  AccordionContent,
} from "@/components/ui/accordion";
import type { PlantResult } from "@/hooks/use-plant-identifier";
import ReactMarkdown from "react-markdown";
import { track } from "@/lib/track";
import { useAuth } from "@/contexts/AuthContext";
import { useClassifyPlant } from "@/hooks/use-classify-plant";
import { getAnonymousId } from "@/lib/anonymous-id";
import ClassificationCards from "@/components/ClassificationCards";
import ClassificationMorph from "@/components/ClassificationMorph";
import PersistentClassificationBanner from "@/components/PersistentClassificationBanner";
import AnonClassificationWall from "@/components/AnonClassificationWall";

interface PlantResultViewProps {
  plant: PlantResult;
  onReset?: () => void;
}

const sections = [
  { value: "description", emoji: "\u{1F331}", label: "Qué es" },
  { value: "care", emoji: "\u{1F4A7}", label: "Cómo cuidarla" },
  { value: "diagnosis", emoji: "\u{1F50D}", label: "Qué le pasa" },
] as const;

type Phase = "cards" | "morph" | "banner";

function derivePhase(context: PlantResult["context"]): Phase {
  if (context === "home" || context === "wild") return "banner";
  return "cards";
}

export default function PlantResultView({ plant, onReset }: PlantResultViewProps) {
  const { user } = useAuth();
  const { classify, revert } = useClassifyPlant();

  // State machine: "cards" → "morph" → "banner" o reverse via Cambiar
  const [phase, setPhase] = useState<Phase>(() => derivePhase(plant.context));
  const [pendingAction, setPendingAction] = useState<"home" | "wild" | null>(null);
  const [committedContext, setCommittedContext] = useState<"home" | "wild" | null>(
    plant.context === "home" || plant.context === "wild" ? plant.context : null
  );
  const [wallOpen, setWallOpen] = useState(false);
  const [intendedAction, setIntendedAction] = useState<"home" | "wild" | null>(null);

  const contentMap: Record<string, string> = {
    description: plant.description,
    care: plant.care,
    diagnosis: plant.diagnosis,
  };

  // Eyebrow + sub-render derivados de phase en una sola expresión (FLAG #5):
  const showsCardsOrMorph = phase === "cards" || phase === "morph";
  const eyebrow = showsCardsOrMorph
    ? "¿Qué hacemos con esta planta?"
    : "Más sobre esta planta";

  const handleClassify = async (action: "home" | "wild") => {
    if (!user) {
      // Anónimo: abrir wall, NO tocar DB
      setIntendedAction(action);
      setWallOpen(true);
      return;
    }
    // Autenticado: clasificación inmediata
    setPendingAction(action);
    const { ok } = await classify(plant.id, action);
    if (!ok) {
      toast.error("No se pudo guardar. Reintenta.");
      setPendingAction(null);
      return;
    }
    setPhase("morph");
  };

  const handleUndo = async () => {
    if (!pendingAction) return;
    const { ok } = await revert(plant.id);
    if (!ok) {
      toast.error("No se pudo guardar. Reintenta.");
      return;
    }
    setPendingAction(null);
    setPhase("cards");
  };

  const handleCommit = () => {
    if (!pendingAction) return;
    setCommittedContext(pendingAction);
    setPendingAction(null);
    setPhase("banner");
  };

  const handleChange = () => {
    // No toca DB hasta el siguiente tap (D-08)
    setPhase("cards");
  };

  const handleSectionClick = (value: string, label: string) => {
    track("result_section_click", { section: value, section_label: label, plant_name: plant.name });
  };

  return (
    <motion.div
      initial={{ opacity: 0, y: 20 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ duration: 0.3, ease: [0.16, 1, 0.3, 1] }}
      className="flex flex-col gap-6"
    >
      {/* Photo */}
      <div className="rounded-2xl overflow-hidden border-2 border-foreground">
        <img
          src={plant.imageUrl}
          alt={plant.name}
          className="w-full h-64 object-cover"
        />
      </div>

      {/* Plant name */}
      <h1 className="font-display text-3xl font-bold text-foreground">
        {plant.name}
      </h1>

      {/* Classification section — eyebrow + body atomic per phase */}
      <section aria-labelledby="classification-eyebrow" className="flex flex-col gap-4">
        <p id="classification-eyebrow" className="font-display text-base text-muted-foreground">
          {eyebrow}
        </p>
        <AnimatePresence mode="wait">
          {phase === "cards" && (
            <ClassificationCards
              key="cards"
              plantSearchId={plant.id}
              pendingAction={pendingAction}
              onClassify={handleClassify}
            />
          )}
          {phase === "morph" && pendingAction && (
            <ClassificationMorph
              key="morph"
              plantSearchId={plant.id}
              action={pendingAction}
              onUndo={handleUndo}
              onCommit={handleCommit}
            />
          )}
          {phase === "banner" && committedContext && (
            <PersistentClassificationBanner
              key="banner"
              plantSearchId={plant.id}
              context={committedContext}
              onChange={handleChange}
            />
          )}
        </AnimatePresence>
      </section>

      {/* Anon wall — controlled by wallOpen state. Only render when intendedAction is set
          to satisfy AnonClassificationWall's non-null `intendedAction: 'home' | 'wild'` prop. */}
      {intendedAction && (
        <AnonClassificationWall
          open={wallOpen}
          onOpenChange={setWallOpen}
          plantSearchId={plant.id}
          intendedAction={intendedAction}
          anonymousId={getAnonymousId()}
        />
      )}

      {/* Accordion sections — UNCHANGED, renders always */}
      <Accordion
        type="multiple"
        defaultValue={["diagnosis"]}
        className="flex flex-col gap-4"
      >
        {sections.map(({ value, emoji, label }) => (
          <AccordionItem
            key={value}
            value={value}
            className="border-2 border-foreground rounded-2xl bg-secondary/50 overflow-hidden"
            style={{ boxShadow: "var(--shadow-press)" }}
          >
            <AccordionTrigger
              className="px-6 py-4 hover:no-underline"
              onClick={() => handleSectionClick(value, label)}
            >
              <span className="font-display text-xl font-semibold text-foreground">
                {emoji} {label}
              </span>
            </AccordionTrigger>
            <AccordionContent className="px-6 pb-5">
              <div className="text-lg leading-relaxed text-foreground/80 prose prose-lg max-w-none">
                <ReactMarkdown>{contentMap[value]}</ReactMarkdown>
              </div>
            </AccordionContent>
          </AccordionItem>
        ))}
      </Accordion>

      {/* Reset button — only when onReset is provided (post-identification flow) */}
      {onReset && (
        <Button
          variant="hero"
          size="xl"
          onClick={onReset}
          style={{ boxShadow: "var(--shadow-press)" }}
        >
          <RotateCcw className="!size-8" />
          Volver a empezar
        </Button>
      )}
    </motion.div>
  );
}
```

Notas críticas:
- La prop pública cambia de `result` a `plant` para reflejar que ahora es la fila (con `context`) y no sólo el resultado de identificación. Verificar que `Index.tsx` (T07-03) y `PlantDetail.tsx` (T07-01) usen `plant={...}`.
- `onReset?` es opcional: PlantDetail no lo pasa (no tiene sentido "Volver a empezar" desde detail).
- El eyebrow y el AnimatePresence comparten el mismo render — cuando `phase` cambia, ambos cambian en el mismo commit React (FLAG #5 resuelto).
- El estado inicial deriva de `plant.context`: si llega `'home'` o `'wild'`, arranca en banner; si `'unclassified'`, arranca en cards.
- `handleClassify` para anónimo NO toca DB — abre el wall. Plan 06 (`AnonClassificationWall`) persiste la intención y navega a `/signup`/`/login`. Tras auth, el listener en App.tsx (T07-05) traerá al usuario de vuelta a `/planta/:id` con la fila ya actualizada.
- El tracking `classification_action_clicked` lo dispara `ClassificationCards` (Plan 04) ANTES de invocar `onClassify` — no se duplica aquí.
- El tracking `classification_completed` lo dispara `ClassificationMorph` (Plan 04) en el setTimeout antes del `onCommit` — no se duplica aquí.
</action>

<acceptance_criteria>
- `grep -q "interface PlantResultViewProps" src/components/PlantResultView.tsx`
- `grep -q "plant: PlantResult" src/components/PlantResultView.tsx`
- `grep -q "onReset?" src/components/PlantResultView.tsx`
- `grep -q "useClassifyPlant" src/components/PlantResultView.tsx`
- `grep -q "ClassificationCards" src/components/PlantResultView.tsx`
- `grep -q "ClassificationMorph" src/components/PlantResultView.tsx`
- `grep -q "PersistentClassificationBanner" src/components/PlantResultView.tsx`
- `grep -q "AnonClassificationWall" src/components/PlantResultView.tsx`
- `grep -q '"¿Qué hacemos con esta planta?"' src/components/PlantResultView.tsx`
- `grep -q '"Más sobre esta planta"' src/components/PlantResultView.tsx`
- `grep -q '"No se pudo guardar. Reintenta."' src/components/PlantResultView.tsx`
- El accordion sigue presente y NO depende de `phase`: `grep -q "type=\"multiple\"" src/components/PlantResultView.tsx`
- Botón "Volver a empezar" condicional: `grep -q "{onReset && (" src/components/PlantResultView.tsx`
- TypeScript build pasa: `npx tsc --noEmit` sale 0
- Visual smoke: `npm run dev` y abrir una identificación post-cámara → ver cards encima del accordion para context='unclassified', banner para 'home'/'wild'.
</acceptance_criteria>

<done>PlantResultView ahora hostea la state machine completa de clasificación. T07-03 actualiza Index.tsx para pasar `plant=` en lugar de `result=`.</done>
</task>

<task id="T07-03" type="execute">
<title>Modificar Index.tsx — montar UnclassifiedSection condicional + actualizar prop a `plant`</title>

<read_first>
- src/pages/Index.tsx (1-53 — archivo completo)
- src/components/UnclassifiedSection.tsx (Plan 05 — componente sin props o con props mínimas; lee el contrato del Plan 05)
- src/contexts/AuthContext.tsx (`useAuth()` shape)
- src/components/PlantResultView.tsx (después de T07-02 — la prop ahora es `plant`)
- src/hooks/use-plant-identifier.ts (`PlantResult` shape — debe incluir `context` tras Plan 01)
</read_first>

<action>
Modificar `src/pages/Index.tsx` con tres cambios puntuales:

1. Importar `useAuth` y `UnclassifiedSection`:
```tsx
import { useAuth } from "@/contexts/AuthContext";
import UnclassifiedSection from "@/components/UnclassifiedSection";
```

2. Renombrar la variable destructurada `result` → mantenerla, pero pasar la prop `plant={result}` al `<PlantResultView>`:
```tsx
// Antes (línea 27 original):
//   <PlantResultView result={result} onReset={handleReset} />
// Después:
<PlantResultView plant={result} onReset={handleReset} />
```

3. Después del `<PhotoCapture ... />` (línea 49 original), añadir UnclassifiedSection condicional al usuario auth:
```tsx
const { user } = useAuth();
// ... en el JSX, dentro del wrapper principal del fallback (cuando no hay result ni isLoading):
<div className="w-full">
  <PhotoCapture onCapture={(file, coords) => identify(file, coords)} isLoading={isLoading} />
</div>
{user && <UnclassifiedSection />}
```

El bloque final del fallback queda:
```tsx
return (
  <div className="flex flex-col items-center justify-center min-h-[calc(100vh-5rem)] px-6 gap-8">
    <Leaf className="size-24 text-primary" strokeWidth={1.2} />
    <h1 className="font-display text-3xl font-bold text-foreground text-center leading-tight">
      Hola, ¿qué planta quieres ver hoy?
    </h1>
    <p className="text-lg text-muted-foreground text-center">
      Haz una foto y te diremos qué planta es, cómo cuidarla y si le pasa algo.
    </p>

    {error && (
      <div className="w-full p-4 bg-accent/10 border-2 border-accent rounded-2xl text-accent text-lg text-center">
        {error}
      </div>
    )}

    <div className="w-full">
      <PhotoCapture onCapture={(file, coords) => identify(file, coords)} isLoading={isLoading} />
    </div>

    {user && <UnclassifiedSection />}
  </div>
);
```

Notas:
- `UnclassifiedSection` (Plan 05) maneja internamente el caso `count === 0 → return null`, por eso el guard aquí es solo `user` (autenticado) — no se duplica la lógica de count.
- `useAuth()` SOLO devuelve `null` para `user` cuando es anónimo; si está autenticado, `user` es un objeto. Por eso `{user && ...}` es suficiente sin `!== null`.
- La transición a `result` (post-identificación) sigue intacta: las líneas 24-30 originales se mantienen, sólo se cambia `result={result}` → `plant={result}`.
</action>

<acceptance_criteria>
- `grep -q "import UnclassifiedSection" src/pages/Index.tsx`
- `grep -q "import { useAuth }" src/pages/Index.tsx`
- `grep -q "useAuth()" src/pages/Index.tsx`
- `grep -q "<PlantResultView plant={result}" src/pages/Index.tsx`
- `grep -q "{user && <UnclassifiedSection />}" src/pages/Index.tsx` (o equivalente con espaciado)
- NO hay `<PlantResultView result={result}` en Index.tsx (la prop antigua): `! grep -q '<PlantResultView result={result}' src/pages/Index.tsx`
- TypeScript build pasa: `npx tsc --noEmit` sale 0
- Smoke: home anónimo NO renderiza la sección; home auth con `unclassified_count === 0` NO renderiza; home auth con count > 0 SÍ renderiza.
</acceptance_criteria>

<done>Home muestra UnclassifiedSection cuando aplica. PlantResultView recibe la prop nueva.</done>
</task>

<task id="T07-04" type="execute">
<title>Modificar History.tsx — montar HistorySummary + ContextChip por fila + navegar a detalle (eliminar inline expand)</title>

<read_first>
- src/pages/History.tsx (1-256 — archivo completo)
- src/components/HistorySummary.tsx (Plan 05 — props: `{ counts: { total, home, wild, unclassified } }`)
- src/components/ContextChip.tsx (Plan 05 — props: `{ context: 'home' | 'wild' | 'unclassified' }`)
- src/hooks/use-plant-history.ts (Plan 01 amplió cada fila con `context: 'home' | 'wild' | 'unclassified'`)
- src/lib/track.ts:56-59
</read_first>

<action>
Modificar `src/pages/History.tsx` con cambios quirúrgicos:

1. Imports adicionales:
```tsx
import { useEffect } from "react";
import { useNavigate, useSearchParams, useLocation } from "react-router-dom";
import HistorySummary from "@/components/HistorySummary";
import ContextChip from "@/components/ContextChip";
```

2. Eliminar `import ReactMarkdown from "react-markdown";` (ya no se usa tras quitar inline expand) y eliminar `const [expanded, setExpanded] = useState<string | null>(null);` (línea 36 original).

3. Dentro del componente, añadir hooks de navegación y filtro por query param:
```tsx
const navigate = useNavigate();
const [searchParams] = useSearchParams();
const contextParam = searchParams.get("context");
```

4. Ampliar `filtered` para que respete `?context=unclassified`:
```tsx
const filtered = useMemo(() => {
  return history.filter((item: PlantResult) => {
    const matchesSearch = !search || item.name.toLowerCase().includes(search.toLowerCase());
    const matchesMonth = monthFilter === 0 || new Date(item.date).getMonth() + 1 === monthFilter;
    const matchesContext = contextParam !== "unclassified" || item.context === "unclassified";
    return matchesSearch && matchesMonth && matchesContext;
  });
}, [history, search, monthFilter, contextParam]);
```

5. Calcular counts para HistorySummary con useMemo:
```tsx
const counts = useMemo(() => ({
  total: history.length,
  home: history.filter((p) => p.context === "home").length,
  wild: history.filter((p) => p.context === "wild").length,
  unclassified: history.filter((p) => p.context === "unclassified").length,
}), [history]);
```

6. Reemplazar el bloque "Card row" para navegar al detalle en vez de expandir; eliminar el bloque "Expanded detail" completo (líneas 177-201 originales). **Importante:** el `.map` debe pasar el index para el tracking del contrato `history_item_clicked`. El nuevo `.map` y handler:
```tsx
{filtered.map((item: PlantResult, index: number) => (
  <motion.div ...>
    <div
      className="flex items-center gap-4 p-4 cursor-pointer active:bg-secondary/80 transition-colors"
      onClick={() => {
        if (editMode) {
          toggleSelect(item.id);
        } else {
          // Contrato docs/posthog-events.md § Phase 02.1: { context, position }
          track("history_item_clicked", { context: item.context, position: index });
          navigate(`/planta/${item.id}`);
        }
      }}
    >
      ...
    </div>
  </motion.div>
))}
```

NOTA: el contrato exacto de propiedades es `{ context, position }` (cf. `docs/posthog-events.md` § Phase 02.1). NO añadir `plant_search_id` (no contratado) ni omitir `position` (sí contratado).

7a. Consumir `refetch` de `usePlantHistory` (Plan 01 lo expuso) y refrescar al volver a `/mis-plantas` o cuando se dispara el evento de clasificación resuelta (D-12 — manual refetch tras mutación):
```tsx
const { history, isLoading, deletePlants, refetch } = usePlantHistory();
const location = useLocation(); // import añadido arriba

// Refetch on mount + on every navigation back to /mis-plantas
useEffect(() => {
  refetch();
}, [location.pathname, refetch]);

// Refetch when an anon→auth flow auto-classifies a plant elsewhere
useEffect(() => {
  const handler = () => refetch();
  window.addEventListener("mp:pending-classification-resolved", handler);
  return () => window.removeEventListener("mp:pending-classification-resolved", handler);
}, [refetch]);
```

(El primer useEffect garantiza que regresar al historial via back button muestra contexts actualizados; el segundo cubre el caso edge de que el listener de App.tsx navegue a `/planta/:id` y el usuario después vuelva con back, encontrando el contexto ya actualizado.)

7b. Insertar HistorySummary encima de la lista (antes del bloque `{isLoading ? ... : ...}`):
```tsx
{!isLoading && history.length > 0 && (
  <div className="mb-6">
    <HistorySummary counts={counts} />
  </div>
)}
```

8. Insertar ContextChip dentro del row, después del bloque `<div className="flex-1 min-w-0">...</div>` y antes del cierre del `<div>` de la card row:
```tsx
{!editMode && <ContextChip context={item.context} />}
```

(En edit mode no se muestra el chip para no competir con el checkbox; ajustar layout responsive si hace falta.)

9. Eliminar el bloque completo que renderiza `{!editMode && expanded === item.id && (... <motion.div initial={{ opacity: 0, height: 0 }} ...)}` — todo el JSX entre líneas 177-201 originales se borra. NO se mantiene como fallback.

10. Eliminar la importación residual de `AnimatePresence` si ya no se usa en otra parte del archivo (verificar — sí se usa en la sticky delete bar, así que se mantiene).

El componente queda con: header, search, month filter, summary nuevo, lista que navega al detalle (sin expand), edit mode + delete bar idénticos a antes.
</action>

<acceptance_criteria>
- `grep -q "import HistorySummary" src/pages/History.tsx`
- `grep -q "import ContextChip" src/pages/History.tsx`
- `grep -q "useNavigate" src/pages/History.tsx`
- `grep -q "useSearchParams" src/pages/History.tsx`
- `grep -q "history_item_clicked" src/pages/History.tsx`
- `grep -q 'navigate(\`/planta/${item.id}\`)' src/pages/History.tsx` (o `navigate("/planta/" + item.id)`)
- `<HistorySummary` aparece exactamente una vez: `[ "$(grep -c '<HistorySummary' src/pages/History.tsx)" -eq 1 ]`
- `<ContextChip` aparece al menos una vez por fila: `grep -q "<ContextChip context={item.context}" src/pages/History.tsx`
- NO existe state `expanded`: `! grep -q "useState<string | null>(null)" src/pages/History.tsx` (verificar que el único useState que queda es para search/monthFilter/editMode/selected/confirmOpen)
- NO existe el bloque "Expanded detail": `! grep -q "Expanded detail" src/pages/History.tsx` (si el comentario ya se quitó)
- NO se renderiza `<ReactMarkdown>` dentro del file: `! grep -q "ReactMarkdown" src/pages/History.tsx` (a menos que se use en otro sitio del file que se decida mantener — confirmar y si lo confirma el contrato, mantener este criterio en negativo)
- NO hay UI de filtros nuevos (solo chips visuales y el buscador/month filter ya existentes): el archivo NO debe ganar `<select>` ni `<button>` nuevos para filtrar por context. Solo el deep-link `?context=unclassified` filtra.
- TypeScript build pasa: `npx tsc --noEmit` sale 0
- Smoke: tap en una fila navega a `/planta/:id` (URL cambia, página de detalle carga); deep link `/mis-plantas?context=unclassified` filtra a solo unclassified.
</acceptance_criteria>

<done>History es deep-linkable, navega al detalle, muestra summary + chips. T07-05 registra la ruta para que la navegación funcione end-to-end.</done>
</task>

<task id="T07-05" type="execute">
<title>Modificar App.tsx — registrar /planta/:id, /signup, y listener mp:pending-classification-resolved</title>

<read_first>
- src/App.tsx (1-46 — archivo completo)
- src/pages/PlantDetail.tsx (creado en T07-01)
- src/contexts/AuthContext.tsx (verificar que dispatch `mp:pending-classification-resolved` ocurre desde Plan 03 — leer el plan de Plan 03 para confirmar el detail shape)
- src/lib/pending-classification.ts (Plan 02 — confirma `EVENT_NAME` exportado o el string literal a usar)
</read_first>

<action>
Modificar `src/App.tsx` con tres cambios:

1. Importar `PlantDetail`:
```tsx
import PlantDetail from "./pages/PlantDetail";
```

2. Refactorizar el componente `App` para que use `useNavigate` (necesita el listener). Como el listener vive dentro del Router, hay que mover la lógica a un componente interno:

```tsx
import { useEffect } from "react";
import { BrowserRouter, Navigate, Route, Routes, useLocation, useNavigate } from "react-router-dom";

function ScrollToTop() {
  const { pathname } = useLocation();
  useEffect(() => { window.scrollTo(0, 0); }, [pathname]);
  return null;
}

function PendingClassificationListener() {
  const navigate = useNavigate();
  useEffect(() => {
    const handler = (e: Event) => {
      const detail = (e as CustomEvent<{ plant_search_id: string; action: string }>).detail;
      if (detail?.plant_search_id) {
        navigate(`/planta/${detail.plant_search_id}`, { replace: true });
      }
    };
    window.addEventListener("mp:pending-classification-resolved", handler);
    return () => window.removeEventListener("mp:pending-classification-resolved", handler);
  }, [navigate]);
  return null;
}
```

3. Actualizar el bloque de rutas:
```tsx
const App = () => (
  <AuthProvider>
    <TooltipProvider>
      <BrowserRouter>
        <ScrollToTop />
        <PendingClassificationListener />
        <Routes>
          <Route path="/" element={<AppLayout><Index /></AppLayout>} />
          <Route path="/mis-plantas" element={<AppLayout><History /></AppLayout>} />
          <Route path="/planta/:id" element={<AppLayout><PlantDetail /></AppLayout>} />
          <Route path="/ajustes" element={<AppLayout><SettingsPage /></AppLayout>} />
          <Route path="/login" element={<Navigate to="/mis-plantas" replace />} />
          <Route path="/signup" element={<Navigate to="/mis-plantas" replace />} />
          {/* Legal pages — accessible without login */}
          <Route path="/privacidad" element={<PrivacyPolicy />} />
          <Route path="/cookies" element={<CookiePolicy />} />
          <Route path="/aviso-legal" element={<LegalNotice />} />
          <Route path="/terminos" element={<TermsOfService />} />
          <Route path="*" element={<NotFound />} />
        </Routes>
        <CookieConsentBanner />
      </BrowserRouter>
    </TooltipProvider>
  </AuthProvider>
);
```

Notas:
- `<PlantDetail />` NO va dentro de `RequireAuth` — la página debe ser accesible para todos. RLS + `notFound` (D-06) gestionan acceso. Si la fila pertenece al user actual, se renderiza; si no (RLS deny o id inexistente), `usePlantById` mete `notFound = true` y la página redirige a `/`.
- `/signup` es un Navigate idéntico a `/login` — el switch de modo dentro del AuthForms es responsabilidad de Phase 03 (FLAG documentado abajo). Lo importante de Phase 02.1 es que la URL no rompa cuando AnonClassificationWall navegue ahí.
- `PendingClassificationListener` NO renderiza nada; sólo monta/desmonta el listener junto al Router. Cleanup correcto en el return del effect.
- El nombre del CustomEvent es **exactamente** `mp:pending-classification-resolved` (cita de Plan 03). Verifica el detail shape antes de hacer release.
- El `<Toaster />` NO se duplica aquí — Plan 01 ya lo montó. Verifica con grep antes de añadir nada.
</action>

<acceptance_criteria>
- `grep -q "import PlantDetail" src/App.tsx`
- `grep -q "/planta/:id" src/App.tsx`
- `grep -q "/signup" src/App.tsx`
- `grep -q "PendingClassificationListener" src/App.tsx`
- `grep -q "mp:pending-classification-resolved" src/App.tsx`
- `grep -q "useNavigate" src/App.tsx`
- El listener tiene cleanup: `grep -q "removeEventListener" src/App.tsx`
- El Toaster NO se duplica: `[ "$(grep -c "<Toaster" src/App.tsx)" -le 1 ]`
- `<PlantDetail />` NO está envuelto por `RequireAuth`: `! grep "RequireAuth" src/App.tsx | grep -i "PlantDetail"`
- TypeScript build pasa: `npx tsc --noEmit` sale 0
- Smoke E2E: anónimo identifica → tap card → wall → "Crear cuenta" → completa signup → app navega automáticamente a `/planta/:id` con la fila ya clasificada (banner visible, no morph).
</acceptance_criteria>

<done>Routing y wiring del CustomEvent listo. T07-06 verifica que los 10 events PostHog emiten desde sus call sites correctos.</done>
</task>

<task id="T07-06" type="verify">
<title>Sweep de tracking — verificar los 10 eventos PostHog desde sus call sites correctos</title>

<read_first>
- docs/posthog-events.md § Phase 02.1 (10 events con propiedades exactas)
- src/lib/track.ts:56-59 (firma de `track(event, properties?)`)
- src/components/ClassificationCards.tsx (Plan 04)
- src/components/ClassificationMorph.tsx (Plan 04)
- src/components/PersistentClassificationBanner.tsx (Plan 04)
- src/components/UnclassifiedSection.tsx (Plan 05)
- src/components/AnonClassificationWall.tsx (Plan 06)
- src/services/auth.service.ts (Plan 03)
- src/pages/History.tsx (modificado en T07-04)
</read_first>

<action>
Esta es una task de **verificación cruzada**, no de escritura de código. Para cada uno de los 10 eventos PostHog del Phase 02.1, ejecuta el grep correspondiente, abre el call site, y confirma que las propiedades documentadas en `docs/posthog-events.md` § Phase 02.1 se pasan correctamente.

Tabla de verificación (ejecutar en orden):

| # | Event | grep esperado | Plan que lo cablea | Verificación adicional |
|---|-------|----------------|--------------------|-----------------------|
| 1 | `classification_action_clicked` | `grep -rn "classification_action_clicked" src/components/ClassificationCards.tsx` | Plan 04 | propiedades: `action`, `source`, `plant_search_id`, `was_unclassified` |
| 2 | `classification_undo_clicked` | `grep -rn "classification_undo_clicked" src/components/ClassificationMorph.tsx` | Plan 04 | propiedades: `action`, `plant_search_id` |
| 3 | `classification_completed` | `grep -rn "classification_completed" src/components/ClassificationMorph.tsx` | Plan 04 | propiedades: `action`, `source`, `plant_search_id` |
| 4 | `classification_change_clicked` | `grep -rn "classification_change_clicked" src/components/PersistentClassificationBanner.tsx` | Plan 04 | propiedades: `current_context`, `plant_search_id` |
| 5 | `unclassified_section_shown` | `grep -rn "unclassified_section_shown" src/components/UnclassifiedSection.tsx` | Plan 05 | dedupe via module-level flag — verificar `let _shownThisSession = false;` |
| 6 | `unclassified_section_clicked` | `grep -rn "unclassified_section_clicked" src/components/UnclassifiedSection.tsx` | Plan 05 | propiedades: `plant_search_id` o `target` (link \"Ver todas\" vs thumbnail) |
| 7 | `history_item_clicked` | `grep -rn "history_item_clicked" src/pages/History.tsx` | **Plan 07 (este task)** | propiedades: `context`, `position` (índice 0-based en lista visible) |
| 8 | `anon_classification_wall_shown` | `grep -rn "anon_classification_wall_shown" src/components/AnonClassificationWall.tsx` | Plan 06 | propiedades: `intended_action` |
| 9 | `anon_classification_wall_action` | `grep -rn "anon_classification_wall_action" src/components/AnonClassificationWall.tsx` | Plan 06 | propiedades: `action` (signup/login/dismiss), `intended_action` |
| 10 | `anon_searches_claimed` | `grep -rn "anon_searches_claimed" src/services/auth.service.ts` | Plan 03 | propiedades: `count` (>0 only) |

Sweep completo:
```bash
for ev in classification_action_clicked classification_undo_clicked classification_completed classification_change_clicked unclassified_section_shown unclassified_section_clicked history_item_clicked anon_classification_wall_shown anon_classification_wall_action anon_searches_claimed; do
  count=$(grep -r "\"$ev\"\|'$ev'" src/ | wc -l)
  echo "$ev: $count call site(s)"
done
```

Cada evento debe aparecer al menos 1 vez. Si alguno aparece 0 veces, abrir el plan correspondiente y verificar; si aparece más de 2 veces (excepto si son sites legítimamente distintos), revisar duplicación.

QA manual con PostHog Live Events:
1. Abrir PostHog Live Events en otra ventana.
2. Como anónimo: identificar planta → tap card → ver `anon_classification_wall_shown` + `anon_classification_wall_action` con action=`signup`.
3. Completar signup → ver `anon_searches_claimed` con count>0 → app navega a /planta/:id → ver banner.
4. Como auth: identificar planta nueva → tap card → ver `classification_action_clicked` → esperar 5s → ver `classification_completed`.
5. Tap "Cambiar" en banner → ver `classification_change_clicked`.
6. En Index con count>0: ver `unclassified_section_shown` (sólo una vez por sesión, validar haciendo unmount/remount manual).
7. Tap thumbnail en UnclassifiedSection → ver `unclassified_section_clicked`.
8. Navegar a /mis-plantas → tap fila → ver `history_item_clicked`.
9. En morph, tap "Deshacer" antes de 5s → ver `classification_undo_clicked`.

Resultado: lista de los 10 eventos con `✓` (visto en Live Events) o `✗` (no aparece). Si algún `✗`, regresar al plan correspondiente para corregir.
</action>

<acceptance_criteria>
- Los 10 events tienen al menos un call site en `src/`: `for ev in classification_action_clicked classification_undo_clicked classification_completed classification_change_clicked unclassified_section_shown unclassified_section_clicked history_item_clicked anon_classification_wall_shown anon_classification_wall_action anon_searches_claimed; do grep -rq "\"$ev\"\|'$ev'" src/ || echo "MISSING: $ev"; done` (no debe imprimir ningún MISSING)
- `unclassified_section_shown` deduplica una sola vez por sesión: confirmar con QA manual, montando/desmontando UnclassifiedSection y observando el evento en Live Events
- `anon_searches_claimed` solo emite cuando `count > 0`: revisar `src/services/auth.service.ts` y confirmar el guard explícito
- TypeScript build pasa: `npx tsc --noEmit` sale 0
- QA manual: capturar screenshot de PostHog Live Events durante el flujo E2E y adjuntarlo al SUMMARY.md
</acceptance_criteria>

<done>Tracking completo verificado. Phase 02.1 listo para verify-phase.</done>
</task>

</tasks>

<threat_model>

| ID | Threat type | Surface | Disposition | Mitigation |
|----|-------------|---------|-------------|------------|
| T-02.1-30 | Information disclosure | `usePlantById` deep link `/planta/:id` | mitigate | RLS sobre `plant_searches` ya filtra por `user_id = auth.uid()`. Plan 02 mapea RLS-deny y "not found" al mismo `notFound = true` — `PlantDetail` no distingue uno del otro al usuario, evitando enumeration de IDs ajenos. El toast es ambiguo: "Esta planta no existe o ya no está disponible". |
| T-02.1-31 | Spoofing | Custom DOM event `mp:pending-classification-resolved` desde script en la misma origin | accept | Mismo origen ya puede ejecutar cualquier acción del usuario. El navigate va a `/planta/:id` que pasa por RLS → `notFound` redirige a `/`. Sin escalación de privilegios ni leak. Threat ya documentado en Plan 03 (T-02.1-12) — esta entrada lo cierra desde el lado del receptor (App.tsx). |
| T-02.1-32 | Cross-site scripting (XSS) | `useParams` `id` interpolado en el template `/planta/${id}` y consumido por React/Supabase | mitigate | React escapa el id automáticamente al renderizar. Supabase pasa el id como bound parameter en `.eq('id', id)` (Plan 02). NO se interpola en HTML directo ni en una query SQL string. |
| T-02.1-33 | Tampering / open redirect | `navigate(\`/planta/${plant_search_id}\`)` con `plant_search_id` viniendo del CustomEvent detail | mitigate | El path es relativo y forzado: el template hardcodea `/planta/` como prefijo. Aunque `plant_search_id` sea malicioso (`../foo` o `https://evil.com`), `react-router` trata todo como un segmento de path tras el slash inicial — no permite escape a otra origin. Si el id contiene `/`, la ruta `/planta/:id` no matchea y NotFound se muestra. |
| T-02.1-34 | Repudiation | Event tracking sweep — `history_item_clicked` puede ser disparado sin click real desde devtools | accept | Eventos de PostHog son señal de producto, no de auth. No hay decisión de seguridad que dependa del telemetry; falsificar eventos solo distorsiona métricas internas. Sin impact en autorización. |
| T-02.1-35 | Race condition | `PlantResultView` state machine: usuario tap card → red lenta → `classify` resuelve después de unmount (e.g. user navega rápidamente a otra ruta) | mitigate | El `useEffect` cleanup en `PlantResultView` no aborta la promesa, pero el `setPendingAction` y `setPhase` después del await no se ejecutan en componente desmontado (React 18 ya gestiona esto silenciosamente con un warning en dev). El UPDATE en DB sí persiste — al volver al detalle, `usePlantById` lee el contexto correcto. Acepable. |
| T-02.1-36 | UX regression / data loss | Eliminación de inline expand en History.tsx — usuario que hacía bookmark mental del state expanded pierde la affordance | accept | El nuevo flujo (navigate a /planta/:id) es estrictamente mejor: deep-linkable, back button funciona, contenido idéntico. Documentar en CHANGELOG. NO bloquea release. |

**Risk summary:** 0 HIGH, 0 MEDIUM blockers. Todas las threats están aceptadas o mitigadas con controles existentes (RLS, React escaping, react-router path matching). El threat más interesante (T-02.1-30) reusa la mitigation de Plan 02 — el merge en `usePlantById` es la primary defense.

</threat_model>

<success_criteria>
- [ ] PlantDetail.tsx creado y monta usePlantById + PlantResultView
- [ ] PlantResultView.tsx acepta `plant: PlantResult` con `onReset?` opcional
- [ ] Eyebrow + sub-render derivan del mismo `phase` (UI-SPEC FLAG #5 resuelto)
- [ ] Cards/morph/banner state machine funciona en autenticado
- [ ] Anon abre wall en lugar de tocar DB
- [ ] Error path muestra toast en español
- [ ] Index.tsx monta UnclassifiedSection condicional al user
- [ ] Index.tsx pasa `plant={result}` (no `result={result}`)
- [ ] History.tsx elimina inline expand, navega a /planta/:id, muestra HistorySummary + ContextChip
- [ ] History.tsx soporta deep link ?context=unclassified
- [ ] App.tsx registra /planta/:id, /signup, y listener mp:pending-classification-resolved
- [ ] Listener tiene cleanup correcto en useEffect return
- [ ] Toaster NO se duplica (Plan 01 lo montó)
- [ ] Los 10 events PostHog emiten desde sus call sites correctos (sweep T07-06)
- [ ] TypeScript build pasa (`npx tsc --noEmit`)
- [ ] Smoke E2E completo: identificación anon → wall → signup → vuelta automática a /planta/:id con banner
- [ ] Smoke E2E completo: identificación auth → tap card → morph → 5s → banner
- [ ] Smoke E2E completo: tap "Cambiar" en banner → cards → tap → nuevo morph
- [ ] Smoke E2E completo: home con count>0 → tap thumbnail → /planta/:id correcto
- [ ] Smoke E2E completo: history → tap fila → /planta/:id correcto
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-foundations-classification-result-actions-reveal/02.1-07-SUMMARY.md` per the standard summary template. Document:
1. La decisión de hacer eyebrow + sub-render atómicos via state único `phase` (resuelve FLAG #5).
2. La decisión de NO gateado `/planta/:id` con RequireAuth — RLS + notFound ya gestionan acceso (D-06).
3. El FLAG abierto: `/signup` y `/login` redirigen al mismo `/mis-plantas` — el modo de AuthForms (signup vs login) NO se pre-selecciona en Phase 02.1. Documentar como hardening para Phase 03 si los datos de funnel muestran fricción.
4. El FLAG abierto: tracking de los 10 events depende de QA manual con PostHog Live Events; no hay test E2E automatizado.
</output>

<flags_for_checker>
1. **Pre-selección de modo signup/login en /mis-plantas** — `/signup` y `/login` redirigen ambos a `/mis-plantas`. RequireAuth muestra AuthForms con un default de "login". Cuando AnonClassificationWall navega a `/signup`, el usuario tiene que pulsar "crear cuenta" otra vez en el AuthForms. Aceptable para v0; flag para Phase 03+ medir fricción.
2. **State machine de PlantResultView depende del orden Plan 04 → Plan 07** — si Plan 04 cambia las props de los 3 componentes (Cards/Morph/Banner), Plan 07 debe actualizarse. Verificar al integrar.
3. **`prefers-reduced-motion` en PlantResultView** — el wrapper `<motion.div>` (líneas 37-41 originales) tiene una transición fija. Plan 04 declara que sus componentes respetan `useReducedMotion()`; verificar que el wrapper exterior también lo haga (UI-SPEC § Motion summary). Si no, añadir un `useReducedMotion()` y short-circuit a `transition={{ duration: 0 }}`.
4. **No tests automatizados E2E** — el flujo anon→signup→vuelta a /planta/:id depende de QA manual. Phase 03 podría introducir Playwright para automatizar.
</flags_for_checker>
