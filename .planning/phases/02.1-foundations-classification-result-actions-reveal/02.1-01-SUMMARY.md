---
phase: 02.1-foundations-classification-result-actions-reveal
plan: 01
subsystem: database
tags: [supabase, migration, schema, postgres, plant_searches, context]

# Dependency graph
requires:
  - phase: 02-prompt-optimization
    provides: plant_searches base shape (already exists in repo)
provides:
  - "Schema migration: plant_searches.context column ('home','wild','unclassified')"
  - "Composite partial index plant_searches_user_context_date(user_id, context, created_at desc)"
  - "Foundation for Wave 2 hooks (use-classify-plant, use-unclassified-count, use-plant-by-id)"
affects: [02.1-02, 02.1-03, 02.1-04, 02.1-05, 02.1-06, 02.1-07, 03-calendar-v0, 03.1-plant-map-v0]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Postgres ≥11 fast-path ADD COLUMN ... NOT NULL DEFAULT (metadata-only, no row rewrite)"
    - "Partial composite index with WHERE user_id is not null to skip anon rows in surface queries"
    - "Idempotent migrations (add column if not exists, create index if not exists)"

key-files:
  created:
    - "supabase/migrations/20260506000000_add_context_to_plant_searches.sql"
  modified: []

key-decisions:
  - "CHECK constraint restricted to 3 values ('home','wild','unclassified') — 'unsaved' from initial SCHEMA-AUDIT dropped per SPEC-1"
  - "Default 'unclassified' handles backfill via Postgres fast-path; no explicit UPDATE statement"
  - "Partial index filters out anonymous rows (user_id IS NULL) — they never surface in user UIs"

patterns-established:
  - "Migration pattern: alter table ... add column if not exists ... check (in (...)) followed by create index if not exists ... where ..."
  - "Naming: YYYYMMDDHHMMSS_<verb>_<noun>.sql with timestamp from filename matching analog 20260413000000_add_consensus_match_level.sql"

requirements-completed: []  # SPEC-1 and SPEC-9 are NOT yet fully complete — Task 2 (db push) + Tasks 3-4 (types + Toaster) still pending. Plan was halted at checkpoint:human-action.

# Metrics
duration: ~1min (Task 1 only — halted at Task 2 checkpoint)
completed: 2026-05-13
---

# Phase 02.1 Plan 01: Foundations — Schema Migration + Types + Toaster Summary

**Migration SQL file created for adding `context` column to `plant_searches`; execution HALTED at blocking checkpoint (Task 2) awaiting user/orchestrator to run `supabase db push` + types regeneration.**

## Performance

- **Duration:** ~1 min (Task 1 only)
- **Started:** 2026-05-13T07:56:29Z
- **Halted at checkpoint:** 2026-05-13T07:57:16Z
- **Tasks completed:** 1 of 4 (Task 1)
- **Tasks blocked:** Task 2 (checkpoint:human-action) → blocks Tasks 3, 4
- **Files created:** 1

## Accomplishments

- Created `supabase/migrations/20260506000000_add_context_to_plant_searches.sql` with the exact contents specified in PLAN.md:
  - `add column if not exists context text not null default 'unclassified' check (context in ('home', 'wild', 'unclassified'))`
  - `create index if not exists plant_searches_user_context_date on plant_searches (user_id, context, created_at desc) where user_id is not null`
  - No manual backfill statement — Postgres ≥11 fast-path DEFAULT handles pre-existing rows
- Verified migration file matches SPEC-1 (3-value CHECK, not the 4-value set from earlier SCHEMA-AUDIT draft).
- Verified no `'unsaved'` value present, no `update plant_searches set context` statement.

## Task Commits

1. **Task 1: Create schema migration for context column** — `66dfe7e` (feat)

**Task 2** — HALTED. `checkpoint:human-action` (gate=blocking). No commit yet.
**Task 3, Task 4** — NOT STARTED. Blocked behind Task 2.
**Plan metadata commit** — pending (this SUMMARY.md will be committed alongside, but the plan is INCOMPLETE).

## Files Created/Modified

- `supabase/migrations/20260506000000_add_context_to_plant_searches.sql` — Schema migration: adds `context` column with CHECK constraint + composite partial index.

## Decisions Made

- None beyond plan-as-written. The plan locks the migration SQL verbatim; Task 1 wrote it exactly as specified.

## Deviations from Plan

None — Task 1 executed exactly as written. Tasks 2-4 not executed (halted at blocking checkpoint per plan design).

## Issues Encountered

- **Worktree base drift:** Initial verification showed worktree HEAD at `e8d09f74` but required base `0605871b`. Resolved via `git reset --hard 0605871b9ff000fb6440be38363e74d4c7c4d504` per worktree_branch_check protocol. No data lost (the worktree had not yet committed any of this plan's work).

## Checkpoint: HUMAN-ACTION REQUIRED (BLOCKING)

**Type:** checkpoint:human-action
**Status:** Plan execution halted; no further auto-execution possible.

### What was built
Migration file `supabase/migrations/20260506000000_add_context_to_plant_searches.sql` exists locally on this worktree branch (commit `66dfe7e`). The remote Supabase schema is **NOT yet modified**; `src/integrations/supabase/types.ts` is **NOT yet regenerated**.

### What the user/orchestrator must do
Per Plan Task 2 acceptance criteria (run in repo root, not in the worktree — the migration file will be merged back to main):

1. Push the migration to the linked Supabase project:
   ```bash
   supabase db push
   ```
   If interactive auth is required, set `SUPABASE_ACCESS_TOKEN` first.

2. Regenerate the TypeScript types:
   ```bash
   supabase gen types typescript --project-id sdxfxkqzgnonxfshbjfc > src/integrations/supabase/types.ts
   ```
   Confirm `project-id` matches `supabase/config.toml` before running; if it differs, use the value from `config.toml`.

3. Verify types include `context: string`:
   ```bash
   grep "context: string" src/integrations/supabase/types.ts
   ```
   Expected: 3 hits (Row, Insert, Update).

4. Verify remote schema (Supabase dashboard SQL editor):
   ```sql
   select column_name, data_type, column_default
   from information_schema.columns
   where table_name = 'plant_searches' and column_name = 'context';
   -- expected: 1 row, text, '''unclassified''::text'

   select indexname from pg_indexes
   where tablename = 'plant_searches' and indexname = 'plant_searches_user_context_date';
   -- expected: 1 row
   ```

### What remains for Plan 02.1-01 after checkpoint approval
Tasks 3 and 4 (executed in a follow-up agent after Task 2 is approved):

- **Task 3:** Extend `PlantResult` interface in `src/hooks/use-plant-identifier.ts` with `context?: 'home' | 'wild' | 'unclassified'`. Refactor `usePlantHistory` to share a `fetchHistory` `useCallback`, map `context` per row, and expose a public `refetch`.
- **Task 4:** Mount `<Toaster richColors closeButton position="top-center" />` from `@/components/ui/sonner` inside `<TooltipProvider>` in `src/App.tsx`.

## Next Phase Readiness

Plan 02.1-01 is **NOT complete**. Downstream Wave 2 plans (02.1-02 onwards) cannot start until:
1. `supabase db push` succeeds (Task 2 — blocking checkpoint).
2. Types regenerated (Task 2 — blocking checkpoint).
3. Task 3 + Task 4 executed (depend on types being regenerated).

The orchestrator must surface the Task 2 checkpoint to the user and re-spawn this plan executor after approval to complete Tasks 3 and 4.

## Self-Check

- File created: `supabase/migrations/20260506000000_add_context_to_plant_searches.sql` — FOUND.
- Task 1 commit `66dfe7e` — present on worktree branch.
- Migration content matches plan: `context text not null default 'unclassified'` + 3-value CHECK + composite partial index `plant_searches_user_context_date` with `WHERE user_id is not null` — VERIFIED.
- No `'unsaved'` value — VERIFIED (0 occurrences).
- No manual backfill UPDATE — VERIFIED (0 occurrences).

## Self-Check: PASSED (for Task 1 only; Tasks 2-4 not yet executed by design)

---
*Phase: 02.1-foundations-classification-result-actions-reveal*
*Plan: 01*
*Status: PARTIAL — halted at Task 2 checkpoint (human-action, blocking)*
*Halted: 2026-05-13*
