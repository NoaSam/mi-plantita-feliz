---
phase: 02.1-foundations-classification-result-actions-reveal
verified: 2026-05-15T10:44:24Z
status: human_needed
score: 10/10 SPEC requirements verified in code
overrides_applied: 0
gaps:
  - truth: "UnclassifiedSection's 'Ver todas' link must navigate to a working history filter route"
    status: partial
    reason: "El botón navega a `/historial?context=unclassified` pero la ruta registrada en App.tsx es `/mis-plantas`. El usuario aterriza en NotFound. El filtro `?context=unclassified` está implementado correctamente en History.tsx, sólo la URL del botón es incorrecta."
    artifacts:
      - path: "src/components/UnclassifiedSection.tsx"
        issue: "Línea 44: `navigate(\"/historial?context=unclassified\")` — ruta inexistente"
      - path: "src/App.tsx"
        issue: "Sólo `/mis-plantas` está registrado; no hay alias `/historial`"
    missing:
      - "Cambiar `navigate(\"/historial?context=unclassified\")` por `navigate(\"/mis-plantas?context=unclassified\")` en UnclassifiedSection.tsx:44, O añadir un Route alias `/historial` que redirija a `/mis-plantas` preservando query params"
human_verification:
  - test: "E2E completo anónimo → wall → signup → vuelta a /planta/:id"
    expected: "Tras completar signup desde el wall, la app navega automáticamente a /planta/:id mostrando el PersistentClassificationBanner (no morph) con el contexto elegido. La fila en DB tiene user_id=new_user.id, anonymous_id=NULL, context=action."
    why_human: "Requiere PostHog Live Events, conexión a Supabase real, y crear cuenta real para verificar el funnel end-to-end."
  - test: "Identificación autenticada → tap card → morph (5s) → banner"
    expected: "Tras tap, ClassificationMorph aparece sobre el accordion (verde primary + ✓ + Deshacer); 5s sin tap colapsa a PersistentClassificationBanner. Durante 5s el row en DB ya está clasificado."
    why_human: "Requiere ejecutar el flujo con timing visual; el código tiene las wirings correctas (timer + tracking) pero no hay E2E automatizado."
  - test: "Tap Deshacer dentro de 5s revierte a unclassified"
    expected: "El row en DB vuelve a context='unclassified', las 2 cards reaparecen, no se dispara classification_completed event."
    why_human: "Visual + temporal; requiere ojos humanos verificando estado intermedio."
  - test: "Tap Cambiar en banner muestra cards sin DB write"
    expected: "Las 2 cards reaparecen, el row en DB sigue con el context actual hasta el siguiente tap en una card."
    why_human: "Requiere inspector de Network/DB para confirmar que Cambiar NO dispara UPDATE."
  - test: "UnclassifiedSection visible en home auth con count>0, ausente con count=0 y para anónimos"
    expected: "Visible con header 'Sin clasificar (N)' + grid de hasta 4 thumbs cuando user existe y count>0; null en los otros 2 casos."
    why_human: "Requiere usuarios reales con diferentes estados (anon, auth con 0 unclassified, auth con N>0)."
  - test: "PostHog Live Events: los 10 eventos emiten con propiedades correctas"
    expected: "classification_action_clicked {action, source, plant_search_id, was_unclassified=true}; classification_undo_clicked {action, plant_search_id}; classification_completed {action, source, plant_search_id}; classification_change_clicked {current_context, plant_search_id}; unclassified_section_shown {count} (1 por sesión); unclassified_section_clicked {target, position?, plant_search_id?}; history_item_clicked {context, position}; anon_classification_wall_shown {intended_action}; anon_classification_wall_action {action, intended_action}; anon_searches_claimed {count}."
    why_human: "Requiere abrir PostHog Live Events durante QA manual del flujo E2E. Todos los call sites están en el código pero no hay test automatizado."
  - test: "Migración RLS UPDATE policy aplicada en remoto"
    expected: "`supabase/migrations/20260515000000_add_plant_searches_update_policy.sql` ha sido empujado al proyecto remoto (`supabase db push`). Verificable en Supabase dashboard SQL editor: `select policyname from pg_policies where tablename = 'plant_searches'` retorna 4 policies incluyendo 'Users can update own searches'."
    why_human: "El archivo existe en el repo pero verify-phase no puede confirmar el estado remoto. Sin la policy aplicada en remoto, todos los UPDATEs de context (classify/revert/processPendingClassification) son silenciosamente denegados por RLS — bloqueando la feature core en producción."
  - test: "Reduced-motion respetado en los 3 componentes"
    expected: "Con `prefers-reduced-motion: reduce` en el OS, las transiciones de cards/morph/banner se reducen a opacity-only sin scale, con duration ≤ 100ms."
    why_human: "Requiere alternar la setting del SO y observar visualmente."
  - test: "Sweep manual del flujo: identificación → cards → morph → banner → Cambiar → cards"
    expected: "Eyebrow cambia atómicamente con el sub-render (UI-SPEC FLAG #5); usuario nunca ve 'Más sobre esta planta' encima de las cards o viceversa."
    why_human: "Visual; el código wiring es correcto pero el commit-atomic visible sólo se confirma con ojos."
---

# Phase 02.1: Foundations: Classification + Result Actions + Reveal — Verification Report

**Phase Goal:** Las identificaciones de plantas se pueden etiquetar como `home` (calendar v0) o `wild` (map v0) desde el resultado, el detalle accedido vía historial y desde la sección "Sin clasificar" en home; los usuarios anónimos ven la opción pero la acción está protegida por un wall de login que preserva su intención y la ejecuta automáticamente tras el alta.

**Verified:** 2026-05-15T10:44:24Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

Mapeo a SPEC-1..SPEC-10 (locked en `02.1-SPEC.md`) — derivado de Goal + 22 Acceptance Criteria.

| # | Truth (SPEC ID + descripción)                                                                              | Status        | Evidence                                                                                                                                          |
|---|-------------------------------------------------------------------------------------------------------------|---------------|---------------------------------------------------------------------------------------------------------------------------------------------------|
| 1 | SPEC-1: Columna `context` + CHECK + índice compuesto parcial añadidos en `plant_searches`                  | ✓ VERIFIED    | `supabase/migrations/20260506000000_add_context_to_plant_searches.sql` líneas 4-12 con CHECK exacto y partial index correcto                       |
| 2 | SPEC-2: `ClassificationCards` renderiza encima del accordion cuando `context = 'unclassified'`             | ✓ VERIFIED    | `PlantResultView.tsx:159-167` renderiza Cards dentro de AnimatePresence por encima del Accordion (líneas 202-229); accordion intacto              |
| 3 | SPEC-3: Tap → ClassificationMorph; 5s sin Deshacer → banner; Deshacer revierte                              | ✓ VERIFIED    | `ClassificationMorph.tsx:77-82` arm timer en mount + clear en unmount; `PlantResultView.tsx:99-114` handleUndo + handleCommit                      |
| 4 | SPEC-4: `PersistentClassificationBanner` para 'home'/'wild'; Cambiar revela cards SIN tocar DB              | ✓ VERIFIED    | `PersistentClassificationBanner.tsx:24-30` (track + onChange, no Supabase import); `PlantResultView.tsx:123-126` handleChange sólo setPhase       |
| 5 | SPEC-5: `UnclassifiedSection` condicional en home (user && count>0), grid de 4 thumbs, link "Ver todas"    | ⚠️ PARTIAL    | Component logic correcto, count anon=0 (`use-unclassified-count.ts:36-42`), Index.tsx:55 `{user && <UnclassifiedSection />}`, **pero "Ver todas" navega a ruta inexistente — ver gap** |
| 6 | SPEC-6: `HistorySummary` + `ContextChip` por fila en History; sin filtros UI                                | ✓ VERIFIED    | `History.tsx:140-146` mounts HistorySummary; `:213-214` renders ContextChip per row; no `<select>`/`<button>` extra de filtros (solo deep link)  |
| 7 | SPEC-7: Anon tap → abre `AnonClassificationWall`; row NO se modifica                                        | ✓ VERIFIED    | `PlantResultView.tsx:81-87` handleClassify checks `!user` antes de classify; abre wall via setWallOpen(true); writePendingClassification en wall  |
| 8 | SPEC-8: Tras signup/login en wall → claim → UPDATE context → navigate a /planta/:id → banner               | ✓ VERIFIED    | `AuthContext.tsx:47-63` ordena claim→processPending→dispatch event; `App.tsx:30-43` PendingClassificationListener navega a /planta/:id           |
| 9 | SPEC-9: `PlantResultView` es canónico — post-id, /planta/:id, y desde History                              | ✓ VERIFIED    | `PlantDetail.tsx:30-34` monta PlantResultView con plant de usePlantById; `Index.tsx:30` lo monta con plant=result; `History.tsx:175` navigate    |
| 10 | SPEC-10: Los 10 events PostHog emiten desde call sites correctos; `unclassified_section_shown` deduplicado | ✓ VERIFIED    | Todos los 10 events tienen call site en `src/` (sweep abajo); dedupe via `_unclassifiedSectionShownThisSession` flag en UnclassifiedSection      |

**Score:** 9/10 fully VERIFIED, 1/10 PARTIAL (SPEC-5 con sub-bug en "Ver todas"). El goal end-to-end del fase está implementado; el bug del enlace "Ver todas" es una regresión UX que afecta a uno de los 3 puntos de entrada al detalle desde la home.

### Required Artifacts

| Artifact                                                                          | Expected                                                                | Status      | Details                                                                          |
|-----------------------------------------------------------------------------------|-------------------------------------------------------------------------|-------------|----------------------------------------------------------------------------------|
| `supabase/migrations/20260506000000_add_context_to_plant_searches.sql`            | Migration: column + CHECK + index                                       | ✓ VERIFIED  | 12 líneas, CHECK exacto `('home', 'wild', 'unclassified')`, partial index correcto |
| `supabase/migrations/20260515000000_add_plant_searches_update_policy.sql`         | RLS UPDATE policy fix CR-01                                             | ✓ VERIFIED  | Policy exacta: `auth.uid() = user_id` en using + with check                       |
| `src/integrations/supabase/types.ts`                                              | Regenerated con `context: string` x3                                    | ✓ VERIFIED  | 3 occurrences confirmados (Row, Insert, Update)                                  |
| `src/hooks/use-plant-identifier.ts`                                               | PlantResult extends con `context?`                                      | ✓ VERIFIED  | Línea 17 `context?: 'home' | 'wild' | 'unclassified'`                              |
| `src/hooks/use-plant-history.ts`                                                  | fetchHistory useCallback + refetch + context per row                    | ✓ VERIFIED  | Líneas 22-56 fetchHistory shared, línea 52 `context: row.context as ...`, línea 64 refetch |
| `src/hooks/use-classify-plant.ts`                                                 | classify+revert returning `{ok}`, UNDO_WINDOW_MS export                  | ✓ VERIFIED  | Línea 14 `UNDO_WINDOW_MS = 5000`; ambas mutaciones devuelven `{ok}` (no throws)   |
| `src/hooks/use-unclassified-count.ts`                                             | count + recent + refetch; mountedRef guard (WR-02)                      | ✓ VERIFIED  | mountedRef declarado, guards en cada setState (líneas 37, 52, 56)                |
| `src/hooks/use-plant-by-id.ts`                                                    | `usePlantById(string \| undefined)` (WR-03)                              | ✓ VERIFIED  | Línea 16 firma con union; `if (!id)` short-circuit                                |
| `src/lib/pending-classification.ts`                                               | write/read/clear/process; CR-04 belt-suspenders `.eq("user_id", ...)`   | ✓ VERIFIED  | Líneas 82-88: `.eq("id", ...).eq("user_id", user.id).select("id").maybeSingle()` |
| `src/services/auth.service.ts`                                                    | claimAnonymousSearches con count + tracking; CR-04 falsy id check        | ✓ VERIFIED  | Línea 31 `if (!anonymousId) return { count: 0 }`; línea 55 dispara solo count>0   |
| `src/contexts/AuthContext.tsx`                                                    | Chain: claim → processPending → dispatch CustomEvent                    | ✓ VERIFIED  | Líneas 52-62; orden correcto; cleanup en línea 73                                |
| `src/components/ClassificationCards.tsx`                                          | 2 cards verticales, copy locked, tracking ANTES de onClassify           | ✓ VERIFIED  | Copy verbatim verificado; `handleClick` línea 46-54 fires track antes              |
| `src/components/ClassificationMorph.tsx`                                          | Timer + pause/resume + tracking + a11y                                  | ✓ VERIFIED  | Líneas 53-82 timer con clear en unmount; pause/resume on mouse/focus; role=status |
| `src/components/PersistentClassificationBanner.tsx`                               | Banner cream + Cambiar tracking; sin DB write                            | ✓ VERIFIED  | No import de supabase; track antes de onChange                                    |
| `src/components/UnclassifiedSection.tsx`                                          | Condicional count>0, grid de 4, dedupe per session, navigate            | ⚠️ PARTIAL  | Component sí, **pero navigate "Ver todas" usa /historial (no existe)** — ver gap  |
| `src/components/HistorySummary.tsx`                                               | Aggregate copy verbatim                                                 | ✓ VERIFIED  | Aside con counts interpolados; copy correcto                                      |
| `src/components/ContextChip.tsx`                                                  | 3 cva variants; wild font-bold (FLAG #1)                                 | ✓ VERIFIED  | cva con 3 variants; wild=font-bold; aria-label dinámico                          |
| `src/components/AnonClassificationWall.tsx`                                       | shadcn Sheet side=bottom; write/clear pending; 2 tracking events         | ✓ VERIFIED  | useEffect on open escribe + tracking; handle dismiss limpia; signup/login no limpian |
| `src/components/PlantResultView.tsx`                                              | State machine + eyebrow atómico + CR-02 resync + CR-03 undo recovery     | ✓ VERIFIED  | Líneas 59-67 useEffect resync; líneas 102-110 undo failure promueve a banner      |
| `src/pages/PlantDetail.tsx`                                                       | Mount usePlantById + PlantResultView; notFound → toast + navigate('/')  | ✓ VERIFIED  | Líneas 11-18 correcto                                                            |
| `src/pages/Index.tsx`                                                             | `{user && <UnclassifiedSection />}` condicional                          | ✓ VERIFIED  | Línea 55 condicional correcta; prop `plant={result}` línea 30                     |
| `src/pages/History.tsx`                                                           | Summary + chips + navigate; inline expand eliminado                      | ✓ VERIFIED  | HistorySummary línea 140; ContextChip línea 214; sin ReactMarkdown; sin `expanded` state |
| `src/App.tsx`                                                                     | /planta/:id + /signup + PendingClassificationListener + Toaster sin dup  | ✓ VERIFIED  | Routes líneas 55, 58; listener líneas 30-43; Toaster solo 1 vez                  |

### Key Link Verification

| From                              | To                                | Via                                                       | Status      | Details                                                                       |
|-----------------------------------|-----------------------------------|-----------------------------------------------------------|-------------|-------------------------------------------------------------------------------|
| PlantResultView                   | useClassifyPlant                   | `const { classify, revert } = useClassifyPlant()`        | ✓ WIRED     | Línea 43, llamado en handleClassify y handleUndo                              |
| PlantResultView                   | ClassificationCards/Morph/Banner  | imports + render condicional por phase                    | ✓ WIRED     | 3 imports líneas 18-20; render con AnimatePresence líneas 158-186              |
| PlantResultView                   | AnonClassificationWall            | render condicional cuando intendedAction set              | ✓ WIRED     | Línea 191-199                                                                  |
| ClassificationMorph               | UNDO_WINDOW_MS                    | `import { UNDO_WINDOW_MS } from "@/hooks/use-classify-plant"` | ✓ WIRED | Línea 4                                                                       |
| AuthContext                       | processPendingClassification      | `await processPendingClassification()` after claim       | ✓ WIRED     | Línea 53; orden correcto post-claim                                            |
| AuthContext                       | window CustomEvent                | `window.dispatchEvent(new CustomEvent('mp:...', detail))` | ✓ WIRED     | Líneas 57-61                                                                   |
| App.tsx                           | window CustomEvent listener       | `window.addEventListener('mp:pending-classification-resolved', handler)` | ✓ WIRED | Líneas 30-43; cleanup en return                                  |
| PlantDetail                       | usePlantById + PlantResultView    | hook + render                                             | ✓ WIRED     | Líneas 11, 32                                                                  |
| Index                             | UnclassifiedSection               | `{user && <UnclassifiedSection />}`                       | ✓ WIRED     | Línea 55                                                                       |
| History                           | HistorySummary + ContextChip      | mount + per-row                                           | ✓ WIRED     | Líneas 140, 214                                                                |
| History                           | navigate(`/planta/${id}`)         | onClick item                                              | ✓ WIRED     | Línea 175                                                                      |
| UnclassifiedSection (thumbnail)   | navigate(`/planta/${id}`)         | handleThumbClick                                          | ✓ WIRED     | Línea 39                                                                       |
| UnclassifiedSection ("Ver todas") | History page con filter           | `navigate("/historial?context=unclassified")`             | ✗ NOT_WIRED | **Ruta `/historial` no existe en App.tsx → aterriza en NotFound** (ver gap)   |
| AnonClassificationWall            | sessionStorage                    | writePendingClassification on open                        | ✓ WIRED     | useEffect líneas 36-44                                                         |
| pending-classification            | UPDATE plant_searches             | `.eq("id", ...).eq("user_id", user.id)`                  | ✓ WIRED     | Líneas 82-88 (CR-04 fix)                                                       |

### Data-Flow Trace (Level 4)

| Artifact                       | Data Variable     | Source                                              | Produces Real Data | Status       |
|-------------------------------|-------------------|------------------------------------------------------|--------------------|--------------|
| UnclassifiedSection            | count, recent     | useUnclassifiedCount → supabase.from(plant_searches).select(count exact) | Yes (real query con .eq(context,unclassified)) | ✓ FLOWING |
| PlantDetail (via PlantResultView) | plant          | usePlantById → supabase.from(plant_searches).select('*').eq('id',id).maybeSingle() | Yes (real query) | ✓ FLOWING |
| History list                   | history           | usePlantHistory → supabase.from(plant_searches).select('*').eq('user_id',user.id) | Yes (real query)   | ✓ FLOWING |
| ClassificationCards "pendingAction" | pendingAction | PlantResultView state set en handleClassify         | Yes (auth) / opens wall (anon) | ✓ FLOWING |
| ClassificationMorph timer      | UNDO_WINDOW_MS    | Constante 5000 importada del hook                   | Yes (locked)       | ✓ FLOWING    |
| PersistentClassificationBanner context | committedContext | PlantResultView state; initial desde plant.context | Yes (DB row context) | ✓ FLOWING |
| AnonClassificationWall pending | sessionStorage    | writePendingClassification escribe payload          | Yes                | ✓ FLOWING    |
| processPendingClassification action | sessionStorage | readPendingClassification con TTL + shape validation | Yes                | ✓ FLOWING    |

### Behavioral Spot-Checks

| Behavior                                                                    | Command                                                         | Result          | Status |
|------------------------------------------------------------------------------|------------------------------------------------------------------|------------------|--------|
| TypeScript compila sin errores                                              | `npx tsc --noEmit`                                              | Exit 0           | ✓ PASS |
| Build de producción exitoso                                                  | `npm run build`                                                  | `built in 1.27s`; sin errores | ✓ PASS |
| Test suite pasa                                                              | `npm test`                                                       | 92 tests passed / 8 files | ✓ PASS |
| Los 10 events PostHog tienen call site en `src/`                            | grep para cada nombre de evento                                  | Todos ≥1 site, 11 total | ✓ PASS |
| Migration SQL file exists with correct CHECK and index                       | grep en `20260506000000_*.sql`                                  | Verbatim verificado | ✓ PASS |
| RLS UPDATE policy migration exists (CR-01 fix)                              | grep en `20260515000000_*.sql`                                  | Policy correcta  | ✓ PASS |
| `<Toaster />` montado exactamente 1 vez en App.tsx                          | `grep -c "<Toaster" src/App.tsx`                                | 1 hit            | ✓ PASS |
| `useState<string \| null>(null)` ya no existe en History.tsx (expanded removed) | grep                                                          | 0 hits           | ✓ PASS |
| Index.tsx pasa `plant={result}` (no `result={result}`)                      | grep                                                            | `plant={result}` | ✓ PASS |
| History.tsx pasa `<ContextChip context={item.context...` por fila          | grep                                                            | 1 hit            | ✓ PASS |
| `unclassified_section_shown` dedupe flag a nivel módulo                     | grep `_unclassifiedSectionShownThisSession`                     | Declared + check + set | ✓ PASS |
| PendingClassificationListener tiene cleanup                                  | grep `removeEventListener`                                       | Cleanup en return | ✓ PASS |
| Migration RLS aplicada en remoto                                             | requiere Supabase dashboard SQL editor                          | N/A              | ? SKIP — humano |
| E2E anonymous → wall → signup → /planta/:id con banner                      | requiere browser interaction + servidor                         | N/A              | ? SKIP — humano |
| PostHog Live Events confirma 10 eventos durante flujo manual                | requiere PostHog dashboard                                       | N/A              | ? SKIP — humano |

### Requirements Coverage

REQUIREMENTS.md no contiene IDs `SPEC-1..SPEC-10` (Phase 02.1 fue insertada después de la creación inicial del archivo de trazabilidad; los SPEC IDs están locked únicamente en `02.1-SPEC.md`). El cross-reference cubre las 10 specs locales:

| Requirement | Source Plan(s)                | Description                                                | Status      | Evidence                                                  |
|-------------|-------------------------------|-------------------------------------------------------------|-------------|-----------------------------------------------------------|
| SPEC-1      | 02.1-01                       | Migration `context` column + CHECK + composite index       | ✓ SATISFIED | Migration file, types regen, 3 hits `context: string`     |
| SPEC-2      | 02.1-04, 02.1-07              | ClassificationCards renders for context=unclassified        | ✓ SATISFIED | PlantResultView phase=cards renders cards above accordion |
| SPEC-3      | 02.1-02, 02.1-04, 02.1-07     | Tap → Morph → 5s → Banner; Deshacer reverts                 | ✓ SATISFIED | useClassifyPlant + Morph timer + handleUndo path          |
| SPEC-4      | 02.1-04, 02.1-07              | PersistentClassificationBanner; Cambiar reveals cards w/o DB | ✓ SATISFIED | Banner has no supabase import; handleChange only setPhase |
| SPEC-5      | 02.1-02, 02.1-05, 02.1-07     | UnclassifiedSection condicional + grid + "Ver todas" link   | ⚠️ PARTIAL  | Component + thumbnails OK; "Ver todas" link broken (gap)  |
| SPEC-6      | 02.1-05, 02.1-07              | HistorySummary + ContextChip per row; sin filtros UI        | ✓ SATISFIED | Mounted + rendered; deep link `?context=unclassified` honra filtro |
| SPEC-7      | 02.1-06, 02.1-07              | AnonClassificationWall on anon tap; no DB modification      | ✓ SATISFIED | PlantResultView opens wall on `!user`; row no se modifica |
| SPEC-8      | 02.1-02, 02.1-03, 02.1-07     | claim → UPDATE context → navigate; banner post-auth         | ✓ SATISFIED | AuthContext chain + dispatch + listener + PlantDetail     |
| SPEC-9      | 02.1-01, 02.1-02, 02.1-07     | PlantResultView canonical for result + detail + history     | ✓ SATISFIED | Index, PlantDetail, History (via navigate) usan PlantResultView |
| SPEC-10     | 02.1-03, 02.1-04, 02.1-05, 02.1-06, 02.1-07 | 10 PostHog events from correct call sites + dedupe | ✓ SATISFIED | Sweep verifica todos los 10 nombres; dedupe flag présent  |

No hay orphan requirements: todos los SPEC IDs declarados en frontmatters de plans (SPEC-1..SPEC-10) están cubiertos.

### Code Review Fixes Verification

Las 4 BLOCKERs y 3 WARNINGs del REVIEW.md están RESOLVED en código:

| Finding | Fix expectation                                                                                          | Verified Location                                          | Status      |
|---------|-----------------------------------------------------------------------------------------------------------|-------------------------------------------------------------|-------------|
| CR-01   | `supabase/migrations/20260515000000_add_plant_searches_update_policy.sql` con UPDATE policy correcta     | File exists, content matches                               | ✓ RESOLVED  |
| CR-02   | `PlantResultView` useEffect resync on `[plant.id, plant.context]` resets phase/pending/committed/wallOpen | Líneas 59-67                                               | ✓ RESOLVED  |
| CR-03   | `handleUndo` failure path promueve a banner + toast "No se pudo deshacer..."                              | Líneas 102-110                                             | ✓ RESOLVED  |
| CR-04   | `processPendingClassification` con `.eq("user_id", user.id).select("id").maybeSingle()` + `claimAnonymousSearches` early-return en falsy anon id | pending-classification.ts:82-88 + auth.service.ts:31 | ✓ RESOLVED  |
| WR-01   | 5 console.log en use-plant-identifier.ts gated por `import.meta.env.DEV`                                 | Líneas 62, 71, 83, 101, 139                                | ✓ RESOLVED  |
| WR-02   | use-unclassified-count.ts uses `mountedRef` para guard de setState                                        | Líneas 27-33, 37, 52                                       | ✓ RESOLVED  |
| WR-03   | usePlantById signature widened a `string \| undefined`                                                    | use-plant-by-id.ts línea 16                                | ✓ RESOLVED  |

### Anti-Patterns Found

| File             | Line | Pattern                          | Severity | Impact                                                    |
|------------------|------|------------------------------------|----------|-----------------------------------------------------------|
| UnclassifiedSection.tsx | 44   | `navigate("/historial?context=unclassified")` — ruta no registrada | ⚠️ Warning | Tap en "Ver todas" aterriza en NotFound; documentado en gap. |

No otros TODOs, FIXMEs, ni stubs detectados. No console.log fuera de paths gated. No empty implementations.

### Human Verification Required

Ver `human_verification` en frontmatter. Resumen:

1. **E2E flujo completo anónimo → wall → signup → /planta/:id con banner** — requiere browser + Supabase real + PostHog.
2. **Migración RLS UPDATE policy aplicada en remoto** — `20260515000000_add_plant_searches_update_policy.sql` existe localmente; el usuario indicó que ya hizo `supabase db push`, pero la verificación remota requiere Supabase dashboard.
3. **PostHog Live Events confirma 10 eventos con propiedades correctas durante el flujo manual** — todos los call sites están presentes en código, pero la confirmación de emisión + payload requires PostHog UI.
4. **Identificación auth → tap → morph (5s) → banner** — visual + temporal.
5. **Tap Deshacer dentro de 5s revierte a unclassified** — visual + DB inspector.
6. **Tap Cambiar en banner muestra cards sin DB write** — Network/DB inspector.
7. **UnclassifiedSection: 3 escenarios (anon, auth count=0, auth count>0)** — usuarios reales.
8. **Reduced-motion respetado** — toggling OS setting.
9. **Eyebrow + sub-render atómico (UI-SPEC FLAG #5)** — visual confirmation.

### Gaps Summary

**1 gap detectado, severidad WARNING:**

El botón "Ver todas ›" de `UnclassifiedSection` navega a `/historial?context=unclassified`, una ruta inexistente en `App.tsx`. La ruta correcta es `/mis-plantas?context=unclassified`. La consecuencia es que el usuario que toque "Ver todas" aterrizará en la página `NotFound` (catch-all `*`) en lugar de en `History.tsx` filtrado por `context = unclassified`.

**Cobertura del fix-target:**
- El handler en `UnclassifiedSection.tsx:44` dispara correctamente el evento PostHog `unclassified_section_clicked` con `target: 'view_all'` ANTES de la navegación rota — el tracking sigue siendo correcto.
- La lógica del filtro en `History.tsx:39, 62-65` lee correctamente `searchParams.get("context")` y filtra cuando `=== "unclassified"` — sólo está esperando el path correcto.
- Origen del bug: `02.1-CONTEXT.md` línea 212 dice "el link 'Ver todas' lleva a `/historial?context=unclassified`" — la URL en CONTEXT.md fue inconsistente con `02.1-SPEC.md`/`ROADMAP.md` que usan `/mis-plantas` en todas las otras referencias. Plan 05 ejecutó verbatim contra CONTEXT.md sin reconciliar.

**Fix sugerido:** una línea — cambiar `/historial` por `/mis-plantas` en `src/components/UnclassifiedSection.tsx:44`. Alternativa: añadir un Route alias en `App.tsx` (`<Route path="/historial" element={<Navigate to={...} replace />}`) que preserve los query params. La primera opción es más directa.

**Status decision:** Aunque hay 1 gap, todos los SPEC requirements están implementados a nivel de código con las wirings correctas. El gap es una regresión UX puntual, no un bloqueador del core funnel (el flujo de clasificación principal — cards, morph, banner, wall, post-auth redirect — funciona; sólo se rompe la conveniencia de "Ver todas las plantas sin clasificar"). Por eso clasifico como **WARNING** y dejo la decisión final al humano (status `human_needed`):

- Si el humano decide que el gap es aceptable como tech-debt → cerrar Phase 02.1.
- Si el humano decide que el gap debe fijarse antes de release → 1-line fix en UnclassifiedSection.tsx:44 cubre el caso.

Adicionalmente, el flujo end-to-end completo requiere verificación manual con browser + Supabase real + PostHog Live Events (ver `human_verification` arriba) — no existe un test E2E automatizado en este phase (flag conocido en 07-PLAN `<flags_for_checker>` #4).

---

_Verified: 2026-05-15T10:44:24Z_
_Verifier: Claude (gsd-verifier)_
