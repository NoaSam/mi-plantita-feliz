---
phase: 05-plantnet-fourth-provider
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260818000000_extend_model_evaluations_for_plantnet.sql
  - src/integrations/supabase/types.ts
autonomous: false
requirements: [PLANT-01]
user_setup:
  - service: plantnet
    why: "4º proveedor de identificación (Phase 5 D-01) — la key ya fue generada durante el benchmark 2026-08-18"
    env_vars:
      - name: PLANTNET_API_KEY
        source: "PlantNet console — https://my.plantnet.org/account/api (CPO ya tiene la key generada)"
    dashboard_config:
      - task: "Añadir PLANTNET_API_KEY como Supabase Function Secret"
        location: "Supabase Dashboard → Project Settings → Edge Functions → Secrets"
  - service: supabase
    why: "supabase db push requiere access token en shell no interactivo"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard → Account → Access Tokens (si no está ya en el entorno)"

must_haves:
  truths:
    - "La tabla model_evaluations acepta 'plantnet' como valor válido en la columna model (CHECK constraint widened de 3 a 4 valores)"
    - "La tabla model_evaluations tiene la columna raw_response jsonb (nullable, sin default)"
    - "La tabla plant_searches tiene la columna plantnet_diverged bool NOT NULL DEFAULT false"
    - "Las 3 columnas nuevas son visibles desde src/integrations/supabase/types.ts tras regenerar types"
    - "RLS + GRANTs de las tablas existentes se preservan (ALTER TABLE sobre tablas ya protegidas por 'existing tables keep their grants')"
    - "La CPO tiene PLANTNET_API_KEY registrada como Supabase Function Secret antes de que Plan 03 despliegue el edge function"
  artifacts:
    - path: "supabase/migrations/20260818000000_extend_model_evaluations_for_plantnet.sql"
      provides: "ALTER model_evaluations (widen CHECK + add raw_response jsonb) + ALTER plant_searches (add plantnet_diverged bool)"
      contains: "check (model in ('claude', 'gemini', 'gpt4o', 'plantnet'))"
    - path: "src/integrations/supabase/types.ts"
      provides: "Types regenerados con las 3 columnas nuevas visibles"
      contains: "raw_response"
  key_links:
    - from: "supabase/migrations/20260818000000_extend_model_evaluations_for_plantnet.sql"
      to: "producción (Supabase project sdxrgxfmurshrnrnvhda)"
      via: "supabase db push"
      pattern: "supabase db push"
    - from: "supabase db push (aplicada)"
      to: "src/integrations/supabase/types.ts"
      via: "supabase gen types typescript --project-id sdxrgxfmurshrnrnvhda"
      pattern: "raw_response.*Json.*null"
---

<objective>
Extender el schema de la DB con las 3 columnas nuevas que necesita PlantNet como 4º proveedor (per D-03, D-04, D-12) y regenerar los types de Supabase. Registrar PLANTNET_API_KEY como Function Secret.

**Purpose:** Este plan desbloquea el edge function (Plan 03) y las queries analíticas (Plan 04). Sin la migración aplicada primero, cualquier `INSERT ... model='plantnet'` fallaría por CHECK constraint violation (ver 05-RESEARCH.md Pitfall #3).

**Output:**
- 1 migración SQL idempotente aplicada en producción
- src/integrations/supabase/types.ts regenerado con `raw_response: Json | null` y `plantnet_diverged: boolean` visibles
- PLANTNET_API_KEY registrada en Supabase Function Secrets (CPO manual)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-plantnet-fourth-provider/05-CONTEXT.md
@.planning/phases/05-plantnet-fourth-provider/05-RESEARCH.md
@.planning/phases/05-plantnet-fourth-provider/05-PATTERNS.md
@supabase/MIGRATION_CONVENTIONS.md
@supabase/migrations/20260413000000_add_consensus_match_level.sql
@supabase/migrations/20260407000000_create_model_evaluations.sql

<interfaces>
<!-- Estado actual del schema relevante (extraído del código, para que el executor no explore) -->

Migración original que define la tabla model_evaluations (20260407000000):
```sql
create table model_evaluations (
  id                uuid        primary key default gen_random_uuid(),
  plant_search_id   uuid        not null references plant_searches(id) on delete cascade,
  model             text        not null check (model in ('claude', 'gemini', 'gpt4o')),
  raw_name          text,
  scientific_name   text,
  description       text,
  care              text,
  diagnosis         text,
  response_ms       integer,
  success           boolean     not null default false,
  error_message     text,
  is_winner         boolean     not null default false,
  consensus_group   text        check (consensus_group in ('correct', 'no_consensus')),
  created_at        timestamptz not null default now()
);
```

Nombre del CHECK constraint que hay que dropear (convención Postgres auto-named):
- `model_evaluations_model_check`

Analog directo (patrón a copiar) para ALTER + add column idempotente:
```sql
-- supabase/migrations/20260413000000_add_consensus_match_level.sql
alter table model_evaluations
  add column if not exists consensus_match_level text
    check (consensus_match_level in ('exact', 'normalized', 'genus'));
create index if not exists model_evaluations_match_level
  on model_evaluations (consensus_match_level)
  where consensus_match_level is not null;
```

Convención de nombre de archivo de migración:
- Formato: `YYYYMMDDHHMMSS_<snake_case>.sql`
- Última migración existente: `20260518000000_add_last_watered_at_to_plant_searches.sql`
- Nueva migración: `20260818000000_extend_model_evaluations_for_plantnet.sql`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear migración SQL extend_model_evaluations_for_plantnet</name>
  <read_first>
    - supabase/migrations/20260407000000_create_model_evaluations.sql (constraint name origen)
    - supabase/migrations/20260413000000_add_consensus_match_level.sql (patrón idempotente)
    - supabase/MIGRATION_CONVENTIONS.md § "Para migraciones que NO crean tablas" (confirma: no GRANTs)
    - .planning/phases/05-plantnet-fourth-provider/05-RESEARCH.md líneas 423-446 (SQL verbatim)
  </read_first>
  <files>supabase/migrations/20260818000000_extend_model_evaluations_for_plantnet.sql</files>
  <action>
Crear el archivo `supabase/migrations/20260818000000_extend_model_evaluations_for_plantnet.sql` con este contenido exacto (implementa D-04 + D-03 + D-12; sin GRANTs por convención "existing tables keep their grants"):

```sql
-- ═══════════════════════════════════════════════════════════════════════════════
-- Phase 5: extender schema para PlantNet como 4º proveedor (votante cross-validated).
-- Ver .planning/phases/05-plantnet-fourth-provider/05-CONTEXT.md D-01..D-04, D-12.
--
-- NO GRANTs: model_evaluations y plant_searches son tablas existentes protegidas
-- por la cláusula "existing tables keep their current grants" de Supabase.
-- Ver supabase/MIGRATION_CONVENTIONS.md § "Para migraciones que NO crean tablas".
-- ═══════════════════════════════════════════════════════════════════════════════

-- ─── model_evaluations: widen CHECK constraint (3 → 4 valores) ──────────────
-- PlantNet vota activamente vía cross-validation (D-01). Su fila usa model='plantnet'
-- + raw_response JSON completo. is_winner puede ser false (rama D-01 branch 1 o 3
-- donde el LLM alineado gana) o true (nunca — PlantNet nunca aporta el resultado
-- final al cliente; el LLM alineado siempre es el 'is_winner'). consensus_group
-- y consensus_match_level quedan NULL para plantnet (D-01: no participa en el
-- pickWinner/computeConsensus de LLMs).

alter table public.model_evaluations
  drop constraint if exists model_evaluations_model_check;

alter table public.model_evaluations
  add constraint model_evaluations_model_check
  check (model in ('claude', 'gemini', 'gpt4o', 'plantnet'));

-- ─── model_evaluations: raw_response jsonb (D-03) ───────────────────────────
-- Guarda el JSON completo devuelto por PlantNet (~2-5KB con nb-results=5 sin
-- include-related-images). Nullable: filas LLM históricas y nuevas NUNCA lo
-- pueblan; filas plantnet fallidas (D-09) también lo dejan NULL.

alter table public.model_evaluations
  add column if not exists raw_response jsonb;

-- ─── plant_searches: plantnet_diverged bool (D-12) ──────────────────────────
-- Marca las identificaciones donde PlantNet devolvió score ≥ 0.8 pero ningún
-- LLM coincidió con su científico (D-01 branch 2). Consultable vía la query
-- nueva de docs/model-evaluation-queries.sql (Plan 04). NOT NULL DEFAULT false
-- para que las filas existentes queden marcadas como no-divergentes.

alter table public.plant_searches
  add column if not exists plantnet_diverged boolean not null default false;
```

Después de crear el archivo, verifica sintaxis con:
```bash
node -e "const fs = require('fs'); const sql = fs.readFileSync('supabase/migrations/20260818000000_extend_model_evaluations_for_plantnet.sql', 'utf8'); if (!/alter table public\.model_evaluations/.test(sql) || !/plantnet_diverged/.test(sql) || !/raw_response jsonb/.test(sql)) throw new Error('SQL missing required statements'); console.log('SQL structure OK');"
```
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260818000000_extend_model_evaluations_for_plantnet.sql &amp;&amp; grep -qE "check \(model in \('claude', 'gemini', 'gpt4o', 'plantnet'\)\)" supabase/migrations/20260818000000_extend_model_evaluations_for_plantnet.sql &amp;&amp; grep -qE "add column if not exists raw_response jsonb" supabase/migrations/20260818000000_extend_model_evaluations_for_plantnet.sql &amp;&amp; grep -qE "add column if not exists plantnet_diverged boolean not null default false" supabase/migrations/20260818000000_extend_model_evaluations_for_plantnet.sql &amp;&amp; echo "OK: migración creada con los 3 cambios esperados"</automated>
  </verify>
  <acceptance_criteria>
    - Archivo existe en `supabase/migrations/20260818000000_extend_model_evaluations_for_plantnet.sql`
    - Contiene DROP + ADD constraint model_evaluations_model_check con 4 valores incluyendo 'plantnet'
    - Contiene `add column if not exists raw_response jsonb`
    - Contiene `add column if not exists plantnet_diverged boolean not null default false`
    - NO contiene GRANTs (verifica: `! grep -q "^grant " supabase/migrations/20260818000000_extend_model_evaluations_for_plantnet.sql`)
    - Comentarios en español referenciando D-XX de CONTEXT.md
  </acceptance_criteria>
  <done>Migración creada, idempotente, sin GRANTs, con referencias a decisiones D-01..D-04, D-12.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] CPO registra PLANTNET_API_KEY como Supabase Function Secret</name>
  <files>N/A — acción externa en Supabase Dashboard (no toca archivos del repo)</files>
  <action>
Este paso NO puede ejecutar Claude — requiere que la CPO abra el Dashboard de Supabase y pegue la API key que ya generó durante el benchmark del 2026-08-18 (mencionada en 05-BENCHMARK.md y confirmada en 05-CONTEXT.md § External APIs). Sin este secret, `Deno.env.get("PLANTNET_API_KEY")` devolverá `undefined` en producción y todas las llamadas PlantNet fallarán silenciosamente (D-09) — el sistema seguirá funcionando con 3 LLMs pero PlantNet no votará nunca.

**Pasos que debe ejecutar la CPO:**

1. Abre https://supabase.com/dashboard/project/sdxrgxfmurshrnrnvhda/functions
2. Ve a **Secrets** (menú lateral: "Edge Functions" → "Secrets" o "Manage secrets")
3. Pulsa **New secret** (o "Add new secret")
4. **Name:** `PLANTNET_API_KEY` (exactamente, en UPPER_SNAKE_CASE — mismo patrón que `ANTHROPIC_API_KEY`)
5. **Value:** pega la API key que generaste en https://my.plantnet.org/account/api el 2026-08-18 durante el benchmark
6. Guarda
7. Verifica que aparece en la lista de secrets (Supabase oculta el valor pero muestra el nombre)

Si la key se ha perdido: regenera en https://my.plantnet.org/account/api (revoca la vieja, genera nueva, pégala en el secret).
  </action>
  <what-built>
Un Function Secret llamado `PLANTNET_API_KEY` visible en el listado de secrets del proyecto Supabase (con el valor enmascarado). Consumible desde el edge function vía `Deno.env.get("PLANTNET_API_KEY")` en el próximo deploy (Plan 03 Task 4).
  </what-built>
  <how-to-verify>
Ir a https://supabase.com/dashboard/project/sdxrgxfmurshrnrnvhda/functions → Secrets: `PLANTNET_API_KEY` debe aparecer en la lista. Alternativamente, verificable indirectamente cuando se despliegue el edge function y las identificaciones empiecen a rellenar filas `plantnet` en `model_evaluations` (Plan 03 Task 5).
  </how-to-verify>
  <verify>Confirmación manual de la CPO: el secret aparece en el listado del Dashboard.</verify>
  <resume-signal>Escribe "listo" o "done" cuando la secret esté registrada. Si prefieres regenerar la key, escribe "regenerada" y pega la nueva confirmación.</resume-signal>
  <done>PLANTNET_API_KEY presente en Supabase Function Secrets del proyecto sdxrgxfmurshrnrnvhda.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Aplicar migración con supabase db push + regenerar types</name>
  <read_first>
    - supabase/migrations/20260818000000_extend_model_evaluations_for_plantnet.sql (creada en Task 1)
    - src/integrations/supabase/types.ts (estado previo, para diff en verify)
    - .planning/phases/05-plantnet-fourth-provider/05-RESEARCH.md § "Regenerar types" (líneas 453-465)
  </read_first>
  <files>src/integrations/supabase/types.ts</files>
  <action>
Aplicar la migración a producción (proyecto `sdxrgxfmurshrnrnvhda`) y regenerar los TypeScript types.

**Paso 1 — Verificar el token de acceso a Supabase (no interactivo):**

```bash
if [ -z "$SUPABASE_ACCESS_TOKEN" ]; then
  echo "SUPABASE_ACCESS_TOKEN no está en el entorno. La CPO debe:"
  echo "  1. Ir a https://supabase.com/dashboard/account/tokens"
  echo "  2. Crear un token (o usar uno existente)"
  echo "  3. Exportar: export SUPABASE_ACCESS_TOKEN='sbp_...'"
  echo "  4. Volver a lanzar el ejecutor con el token en el entorno"
  exit 1
fi
```

Si el token no está: convertir la tarea en checkpoint informativo y esperar a la CPO.

**Paso 2 — Ejecutar `supabase db push`:**

```bash
npx supabase db push --linked
```

Salida esperada: `Applying migration 20260818000000_extend_model_evaluations_for_plantnet.sql...` + `Finished supabase db push.`

Si aparece prompt interactivo pidiendo confirmación → el shell del ejecutor no es TTY → convertir en `checkpoint:human-action` y pedir a la CPO que ejecute manualmente el mismo comando en su terminal.

**Paso 3 — Verificar que la migración se aplicó (schema live):**

```bash
# Comprobación indirecta vía regenerar types (siguiente paso). Si los types incluyen
# raw_response y plantnet_diverged, la migración se aplicó.
```

**Paso 4 — Regenerar types:**

```bash
npx supabase gen types typescript --project-id sdxrgxfmurshrnrnvhda > src/integrations/supabase/types.ts
```

**Paso 5 — Verificar que los types contienen las 3 columnas nuevas:**

```bash
grep -q "raw_response" src/integrations/supabase/types.ts || { echo "FALLO: raw_response no aparece en types.ts — migración no aplicada o project-id incorrecto"; exit 1; }
grep -q "plantnet_diverged" src/integrations/supabase/types.ts || { echo "FALLO: plantnet_diverged no aparece"; exit 1; }
```

**Paso 6 — Verificar que el proyecto compila:**

```bash
npm run build
```

Debe terminar sin errores TypeScript relacionados con `model_evaluations` o `plant_searches`. Los types añaden columnas nullable/con default → ningún consumer actual las usa → no debe romper.
  </action>
  <verify>
    <automated>grep -q "raw_response" src/integrations/supabase/types.ts &amp;&amp; grep -q "plantnet_diverged" src/integrations/supabase/types.ts &amp;&amp; npm run build 2>&amp;1 | tail -5 | grep -qE "built in|✓" &amp;&amp; echo "OK: types regenerados con las columnas nuevas y build pasa"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` reporta migración aplicada sin errores
    - `src/integrations/supabase/types.ts` contiene la string `raw_response`
    - `src/integrations/supabase/types.ts` contiene la string `plantnet_diverged`
    - `npm run build` compila sin errores TS
    - La cadena `'plantnet'` aparece en el union type de `model` en types.ts (verifica: `grep -c "'plantnet'" src/integrations/supabase/types.ts` ≥ 1)
  </acceptance_criteria>
  <done>Migración aplicada en producción, types regenerados y committeados, build verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| supabase-cli → producción DB | ALTER TABLE toca schema de producción; una migración mal formada puede dejar CHECK constraint inconsistente |
| CLI de la CPO → Supabase Dashboard | El PLANTNET_API_KEY se pega manualmente en Web UI; riesgo bajo (pantalla enmascarada) pero fuera del control automatizado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | Migración SQL (constraint widening) | mitigate | Uso `drop constraint if exists` + `add constraint` en la misma transacción implícita de la migración. Idempotente: correr dos veces produce el mismo estado. `if not exists` en add column. Ver Pitfall #3 de RESEARCH.md — CHECK constraint debe estar widened ANTES que el edge function inserte 'plantnet'; garantizado por dependencia `Plan 03 depends_on: [01]`. |
| T-05-01-02 | Denial of Service | plant_searches (add column NOT NULL DEFAULT) | mitigate | `add column ... not null default false` en Postgres 12+ NO reescribe la tabla (metadata-only); zero downtime. Verificable porque plant_searches tiene <200 filas hoy — si acaso, milisegundos de bloqueo. |
| T-05-01-03 | Information Disclosure | PLANTNET_API_KEY como Function Secret | mitigate | Se registra vía Dashboard (fuera de código, fuera de git). Mismo patrón que ANTHROPIC_API_KEY / OPENAI_API_KEY / GEMINI_API_KEY. Nunca aparece en logs (edge fn solo hace `Deno.env.get`). |
| T-05-01-04 | Elevation of Privilege | GRANTs sobre model_evaluations / plant_searches | accept | Tablas existentes protegidas por "existing tables keep their grants" (MIGRATION_CONVENTIONS.md). ALTER TABLE sin GRANTs no cambia el permission model. Verificable ejecutando queries post-migración con role authenticated. |
| T-05-01-05 | Repudiation | Migración aplicada sin logs | accept | Supabase graba el hash del archivo aplicado en `supabase_migrations.schema_migrations`. Trazable. |
</threat_model>

<verification>
- Migración creada con estructura y contenido esperados (Task 1)
- CPO confirma que PLANTNET_API_KEY está registrado como Function Secret (Task 2, checkpoint)
- `supabase db push` aplica sin errores (Task 3)
- `src/integrations/supabase/types.ts` regenerado contiene `raw_response`, `plantnet_diverged`, y `'plantnet'` en el union de `model` (Task 3)
- `npm run build` verde (Task 3)
</verification>

<success_criteria>
1. `supabase/migrations/20260818000000_extend_model_evaluations_for_plantnet.sql` existe, es idempotente, sin GRANTs innecesarios
2. La migración se aplica a producción sin errores
3. Los types regenerados incluyen las 3 columnas/valores nuevos
4. `PLANTNET_API_KEY` está registrada en Supabase Function Secrets (confirmado por la CPO)
5. `npm run build` compila
6. Ningún test existente se rompe: `npm run test -- --run` sigue pasando (los tests no consumen las columnas nuevas)
</success_criteria>

<output>
Al terminar, crea `.planning/phases/05-plantnet-fourth-provider/05-01-SUMMARY.md` con:
- Migración aplicada (hash + timestamp)
- Diff resumido de `src/integrations/supabase/types.ts` (líneas añadidas)
- Confirmación de secret PLANTNET_API_KEY registrado
- Cualquier problema encontrado en `supabase db push` (auth, prompts, etc.)
</output>
