---
phase: 05-plantnet-fourth-provider
plan: 05
type: execute
wave: 3
depends_on: ["01", "02", "03", "04"]
files_modified:
  - .planning/phases/05-plantnet-fourth-provider/VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [PLANT-01, PLANT-02, PLANT-03]

must_haves:
  truths:
    - "Existe `.planning/phases/05-plantnet-fourth-provider/VERIFICATION.md` con checklist en español cubriendo los 3 requirements + las decisiones D-01/D-10/D-11/D-12/D-13"
    - "La CPO ha ejecutado el checklist con datos reales (mínimo 24h de tráfico post-deploy) y ha firmado go / no-go"
    - "ROADMAP.md marca Phase 5 como completed y actualiza el contador de progreso"
    - "REQUIREMENTS.md marca PLANT-01, PLANT-02, PLANT-03 como completos con referencia a Plan 05 en la tabla de trazabilidad"
  artifacts:
    - path: ".planning/phases/05-plantnet-fourth-provider/VERIFICATION.md"
      provides: "Checklist de aceptación + firma CPO"
      min_lines: 60
    - path: ".planning/ROADMAP.md"
      provides: "Phase 5 marcada como completa"
      contains: "[x] **Phase 5:"
    - path: ".planning/REQUIREMENTS.md"
      provides: "PLANT-01/02/03 marcados como completos"
      contains: "[x] **PLANT-01"
  key_links:
    - from: "VERIFICATION.md checklist"
      to: "docs/model-evaluation-queries.sql query 13"
      via: "referencia + copy de la query para ejecutar en Supabase"
      pattern: "13\\. DIVERGENCIAS"
    - from: "VERIFICATION.md checklist"
      to: "supabase/functions/identify-plant/index.ts (deploy live)"
      via: "smoke test manual + queries de comprobación"
      pattern: "identify-plant"
---

<objective>
Cerrar Phase 5 con un checklist de verificación exhaustivo que la CPO ejecuta sobre datos reales de producción, seguido de la actualización de ROADMAP y REQUIREMENTS.

**Purpose:** Toda phase se cierra con un go/no-go explícito de la CPO. VERIFICATION.md formaliza qué evidencia (queries, logs, DevTools) demuestra que las decisiones D-01..D-13 (excepto removed) están vivas en producción y que los 3 requirements se cumplen.

**Output:**
- VERIFICATION.md: 6 bloques (A: cobertura de datos; B: cross-validation funciona; C: divergencia registrada; D: UX invisible; E: latencia OK; F: regresión) con criterios pasa/no-pasa
- ROADMAP.md: Phase 5 marcada `[x]` con fecha de completion
- REQUIREMENTS.md: PLANT-01/02/03 marcados `[x]`
- Sign-off explícito de la CPO
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-plantnet-fourth-provider/05-CONTEXT.md
@.planning/phases/05-plantnet-fourth-provider/05-BENCHMARK.md
@.planning/phases/05-plantnet-fourth-provider/05-01-SUMMARY.md
@.planning/phases/05-plantnet-fourth-provider/05-02-SUMMARY.md
@.planning/phases/05-plantnet-fourth-provider/05-03-SUMMARY.md
@.planning/phases/05-plantnet-fourth-provider/05-04-SUMMARY.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@docs/model-evaluation-queries.sql

<interfaces>
<!-- Formato de VERIFICATION.md previo en Phase 04.1 (referencia de estilo) -->

Phase 04.1 usó VERIFICATION.md con 4 bloques (A/B/C/D) en español. Extracto del patrón:

```markdown
# Verificación Phase 5 — PlantNet as Fourth Voter

**Fecha ejecución:** _(rellena al correrlo)_
**CPO signoff:** ⏳ pendiente / ✅ aprobado / ❌ bloqueado

## Bloque A — <título>
### A.1 <subquery>
```sql
SELECT ...
```
**Criterio pasa:** <>
**Resultado:** _(rellenar)_
**Notas:** _(opcional)_
```

Requirements a marcar como completos (formato en REQUIREMENTS.md):
- `- [ ] **PLANT-01**: ...` → `- [x] **PLANT-01**: ...`
- Tabla de trazabilidad al final del archivo: cambiar `Pendiente` → `Completo`

ROADMAP a marcar como completo:
- Línea `- [ ] **Phase 5: PlantNet as Fourth Identification Provider**` → `- [x] **Phase 5: PlantNet as Fourth Identification Provider — Cross-Validated Voter** (completed 2026-08-XX)`
- Sección `### Phase 5:` cambiar Status a "Completa"
- Tabla `## Progress`: cambiar fila `5. PlantNet as Fourth Provider | 0/? | Ready for planning | -` a `5. PlantNet as Fourth Voter | 5/5 | Complete | 2026-08-XX`
- `progress:` en frontmatter de STATE.md: actualizar completed_phases, completed_plans, percent
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear VERIFICATION.md con checklist de 6 bloques</name>
  <read_first>
    - .planning/phases/04.1-mobile-load-time-optimization/VERIFICATION.md (si existe — patrón de estilo)
    - .planning/phases/05-plantnet-fourth-provider/05-CONTEXT.md § success criteria
    - docs/model-evaluation-queries.sql (query 13 para embeber en el checklist)
  </read_first>
  <files>.planning/phases/05-plantnet-fourth-provider/VERIFICATION.md</files>
  <action>
Crear `.planning/phases/05-plantnet-fourth-provider/VERIFICATION.md` con el siguiente contenido:

```markdown
# Verificación Phase 5 — PlantNet as Fourth Voter (Cross-Validated)

**Deploy edge function:** _(fecha del deploy real de Plan 03)_
**Ventana de datos:** mínimo 24h desde el deploy antes de ejecutar el checklist
**Fecha ejecución checklist:** _(rellenar al ejecutar)_
**CPO signoff:** ⏳ pendiente / ✅ aprobado / ❌ bloqueado



## Contexto rápido

Este phase añade PlantNet como 4º proveedor votante con regla de cross-validation. Los 3 LLMs siguen siendo los que aportan care/diagnosis/watering/description; PlantNet solo puede corregir el nombre científico cuando su score ≥ 0.8 Y algún LLM ya estaba alineado con él. UX visible al usuario: cero cambios.

Decisiones cubiertas: D-01 (cross-validation), D-02 (UX invisible), D-03 (raw_response completo), D-04 (schema extendido), D-07 (query SQL de divergencia), D-09 (fallo silencioso), D-10 (threshold 0.8), D-11 (match exact/normalized, NO genus), D-12 (registro dual DB + PostHog), D-13 (latencia validada por benchmark).

Requirements: PLANT-01, PLANT-02, PLANT-03.



## Bloque A — Cobertura de datos (PLANT-01)

### A.1 — ≥95% de identificaciones nuevas tienen 4 filas en model_evaluations

Ejecutar en Supabase SQL Editor:

```sql
WITH ventana AS (
  SELECT id, name, created_at
  FROM plant_searches
  WHERE created_at >= NOW() - INTERVAL '24 hours'
),
conteos AS (
  SELECT
    ps.id,
    COUNT(me.id) FILTER (WHERE me.model IN ('claude','gemini','gpt4o')) AS filas_llm,
    COUNT(me.id) FILTER (WHERE me.model = 'plantnet') AS filas_plantnet
  FROM ventana ps
  LEFT JOIN model_evaluations me ON me.plant_search_id = ps.id
  GROUP BY ps.id
)
SELECT
  COUNT(*)                                                                AS total_busquedas,
  COUNT(*) FILTER (WHERE filas_llm = 3)                                   AS con_3_llms,
  COUNT(*) FILTER (WHERE filas_plantnet = 1)                              AS con_plantnet,
  ROUND(100.0 * COUNT(*) FILTER (WHERE filas_plantnet = 1) / NULLIF(COUNT(*), 0), 1)
                                                                          AS pct_con_plantnet
FROM conteos;
```

**Criterio pasa:** `pct_con_plantnet >= 95` (D-09 permite algún fallo, pero no debería ser >5%).
**Resultado:** _(rellenar)_
**Notas:** _(si <95%, revisar logs edge fn por errores plantnet — ver query A.2)_

### A.2 — Breakdown de errores plantnet (si A.1 sale bajo)

```sql
SELECT
  error_message,
  COUNT(*) AS ocurrencias
FROM model_evaluations
WHERE model = 'plantnet'
  AND success = false
  AND created_at >= NOW() - INTERVAL '24 hours'
GROUP BY error_message
ORDER BY ocurrencias DESC;
```

**Criterio pasa:** Los errores son mayoritariamente `NO_RESULTS` (foto que PlantNet no reconoce — esperado) o esporádicos `TIMEOUT` / `RATE_LIMIT`. Cero `PLANTNET_API_KEY not configured` (indicaría secret faltante).
**Resultado:** _(rellenar)_



## Bloque B — Cross-validation funciona (D-01, D-10, D-11)

### B.1 — Muestreo de overrides aplicados (últimas 24h)

Los overrides no se marcan en DB — son transparentes. Se verifican comparando el `scientific_name` del LLM winner (fila con is_winner=true) contra el `scientific_name` de PlantNet en las mismas búsquedas:

```sql
SELECT
  ps.id,
  ps.name AS nombre_mostrado,
  me_llm.model AS llm_winner_model,
  me_llm.scientific_name AS llm_winner_scientific,
  me_pn.scientific_name AS plantnet_scientific,
  (me_pn.raw_response->'results'->0->>'score')::float AS plantnet_score,
  ps.plantnet_diverged,
  CASE
    WHEN me_pn.scientific_name IS NULL THEN 'plantnet_failed'
    WHEN (me_pn.raw_response->'results'->0->>'score')::float < 0.8 THEN 'plantnet_below_threshold'
    WHEN ps.plantnet_diverged THEN 'diverged_no_llm_matched'
    WHEN LOWER(me_llm.scientific_name) = LOWER(me_pn.scientific_name) THEN 'aligned_exact'
    ELSE 'aligned_normalized_or_other_llm_matched'
  END AS estado
FROM plant_searches ps
LEFT JOIN model_evaluations me_llm ON me_llm.plant_search_id = ps.id AND me_llm.is_winner = true
LEFT JOIN model_evaluations me_pn  ON me_pn.plant_search_id  = ps.id AND me_pn.model = 'plantnet'
WHERE ps.created_at >= NOW() - INTERVAL '24 hours'
ORDER BY ps.created_at DESC
LIMIT 30;
```

**Criterio pasa:** Se ven las 5 categorías esperadas distribuidas de forma coherente. En particular:
- `plantnet_failed` <5% (D-09 no debería ser habitual)
- `plantnet_below_threshold` es normal (fotos ambiguas) — sin límite
- `diverged_no_llm_matched` marca correctamente `plantnet_diverged=true`
- `aligned_*` son la mayoría (ecosistemas típicos de plantas de casa)

**Resultado:** _(rellenar con la distribución observada)_

### B.2 — Genus-only matches NO se cuentan como override (D-11)

Buscar casos con mismo género pero especies distintas en los últimos 30 días:

```sql
SELECT
  ps.id,
  ps.plantnet_diverged,
  me_llm.scientific_name AS llm,
  me_pn.scientific_name  AS plantnet,
  LOWER(SPLIT_PART(TRIM(me_llm.scientific_name), ' ', 1)) AS genero_llm,
  LOWER(SPLIT_PART(TRIM(me_pn.scientific_name), ' ', 1))  AS genero_pn
FROM plant_searches ps
JOIN model_evaluations me_llm ON me_llm.plant_search_id = ps.id AND me_llm.is_winner = true
JOIN model_evaluations me_pn  ON me_pn.plant_search_id  = ps.id AND me_pn.model = 'plantnet' AND me_pn.success = true
WHERE ps.created_at >= NOW() - INTERVAL '30 days'
  AND LOWER(SPLIT_PART(TRIM(me_llm.scientific_name), ' ', 1)) = LOWER(SPLIT_PART(TRIM(me_pn.scientific_name), ' ', 1))
  AND LOWER(me_llm.scientific_name) != LOWER(me_pn.scientific_name)
  AND (me_pn.raw_response->'results'->0->>'score')::float >= 0.8
LIMIT 20;
```

**Criterio pasa:** Cada fila devuelta debe tener `plantnet_diverged = true` (D-11: mismo género con especies distintas = divergencia, no override). Si aparece alguna con `plantnet_diverged = false`, hay un bug en applyPlantnetOverride.
**Resultado:** _(rellenar)_



## Bloque C — Divergencia registrada (D-12)

### C.1 — Query 13 de model-evaluation-queries.sql devuelve resultados coherentes

Copiar y ejecutar la query 13 completa de `docs/model-evaluation-queries.sql`.

**Criterio pasa:**
- La query ejecuta sin errores
- Las filas devueltas tienen todos los campos poblados (excepto `plantnet_top3_snippet` que puede ser `[]` si el JSON está mal formado)
- Los tipos de divergencia (`mismo género, especies distintas` vs `géneros distintos`) tienen sentido intuitivo

**Resultado:** _(pegar screenshot o output de las 5-10 primeras filas)_

### C.2 — Evento PostHog `plantnet_divergence` visible en dashboard

Ir a https://eu.posthog.com/events → filtrar `event = 'plantnet_divergence'` en las últimas 24h.

**Criterio pasa:**
- El número de eventos coincide (±10%) con el número de filas devueltas por la query C.1 en la misma ventana
- Cada evento tiene las 5 propiedades documentadas en `docs/posthog-events.md` (plantnet_scientific, plantnet_score, llm_winner_scientific, llm_winner_model, plant_search_id)

**Resultado:** _(rellenar)_
**Nota:** Si POSTHOG_PROJECT_API_KEY no se registró (Plan 03 Task 3 opcional), C.2 es N/A — verificar en Supabase Functions logs que aparece `POSTHOG_PROJECT_API_KEY not set — divergence event skipped` en las divergencias.



## Bloque D — UX invisible al usuario (D-02)

### D.1 — Response JSON al cliente NO expone plantnet

1. Abre https://mi-plantita-feliz.vercel.app (o la app Android)
2. Hazte una identificación con cualquier planta
3. DevTools → Network → localiza la request a `identify-plant`
4. Response tab → JSON

**Criterio pasa:**
- El JSON tiene: `name`, `description`, `care`, `diagnosis`, `watering_interval_days`, `model`, `plant_search_id`, `created_at`, `models`, `consensus_reached`
- `models` es un array de **exactamente 3 items** (claude, gemini, gpt4o)
- **NO** aparece ningún campo `plantnet_*` en top-level ni dentro de `models`
- El campo `model` (winner) es uno de `claude` / `gemini` / `gpt4o` (nunca `plantnet`)

**Resultado:** _(pegar el JSON o screenshot)_

### D.2 — UX visual sin cambios

Hazte 3 identificaciones en la app (potus, monstera, planta rara). Compara con recuerdos de pre-Phase-5:
- Sin badges nuevos
- Sin loading states nuevos
- Nombre común aparece como antes ("Potus (Epipremnum aureum)")
- Cuidados, diagnóstico, riego se ven idénticos

**Criterio pasa:** ninguna diferencia visual perceptible.
**Resultado:** _(rellenar)_



## Bloque E — Latencia (PLANT-03)

### E.1 — Latencia p50/p95 pre vs post-deploy

```sql
SELECT
  DATE_TRUNC('day', created_at) AS dia,
  COUNT(*) AS busquedas,
  PERCENTILE_DISC(0.5)  WITHIN GROUP (ORDER BY response_ms) AS p50_ms,
  PERCENTILE_DISC(0.95) WITHIN GROUP (ORDER BY response_ms) AS p95_ms
FROM model_evaluations
WHERE model = 'plantnet'
  AND success = true
  AND created_at >= NOW() - INTERVAL '7 days'
GROUP BY dia
ORDER BY dia DESC;
```

**Criterio pasa:** p95 PlantNet < 2000ms (el benchmark del 2026-08-18 midió p95=695ms). Cualquier valor >5000ms indica cambio de comportamiento de la API.

**Complementaria — latencia percibida al usuario (end-to-end):**

La latencia percibida sigue midiéndose vía `winning_model.responseMs` (ya trackeado en `plant_identified` event). Comparar el p95 de `response_ms` de LLM winners antes/después del deploy:

```sql
SELECT
  CASE WHEN created_at < '_(fecha deploy Plan 03)_' THEN 'pre_phase_5' ELSE 'post_phase_5' END AS periodo,
  PERCENTILE_DISC(0.5)  WITHIN GROUP (ORDER BY response_ms) AS p50_ms,
  PERCENTILE_DISC(0.95) WITHIN GROUP (ORDER BY response_ms) AS p95_ms,
  COUNT(*) AS n
FROM model_evaluations
WHERE is_winner = true
  AND success = true
  AND created_at >= NOW() - INTERVAL '14 days'
GROUP BY periodo;
```

**Criterio pasa:** p95 post_phase_5 no supera p95 pre_phase_5 en más de 200ms (margen para ruido — PlantNet no debería afectar el path del winner LLM).

**Resultado:** _(rellenar)_



## Bloque F — Regresión (tests + smoke funcional)

### F.1 — Suite de tests verde

```bash
npm run test -- --run
```

**Criterio pasa:** Test Files X passed, Tests Y passed. Cero fallos.
**Resultado:** _(rellenar con el resumen)_

### F.2 — Build verde

```bash
npm run build
```

**Criterio pasa:** ✓ built in Nms sin errores TypeScript.
**Resultado:** _(rellenar)_

### F.3 — Smoke Android (si hay Android disponible)

Instalar el APK de dev o la última versión de Play Store → hacer una identificación completa. Verificar que el resultado se muestra correctamente y aparece en el historial.

**Criterio pasa:** identificación funcional, historial correcto, sin crash.
**Resultado:** _(rellenar o N/A si no se testa)_



## Sign-off

Marcar aquí cuando todos los bloques anteriores estén en verde:

- [ ] Bloque A pasa (PLANT-01)
- [ ] Bloque B pasa (D-01, D-10, D-11)
- [ ] Bloque C pasa (D-12, PLANT-02)
- [ ] Bloque D pasa (D-02)
- [ ] Bloque E pasa (PLANT-03)
- [ ] Bloque F pasa (regresión)

**Decisión CPO:** ⏳ pendiente / ✅ **APROBADO — Phase 5 puede cerrarse** / ❌ **BLOQUEADO — describir aquí qué falla y siguiente acción:**

_(espacio para notas de la CPO)_



## Anexo: Rollback plan

Si algo va mal después del deploy:

1. **Revertir edge function:** `git revert <commit-plan-03>` + `npx supabase functions deploy identify-plant` — vuelve al comportamiento 3-LLM
2. **Migración NO se revierte** — las columnas nuevas (`raw_response`, `plantnet_diverged`) quedan pero no molestan (nullable / default false). Si insister, `ALTER TABLE ... DROP COLUMN ...` como migración manual.
3. **Function Secret PLANTNET_API_KEY:** puede quedarse o eliminarse (si no se usa, no cuesta nada).

Impacto de rollback en usuarios: **cero** — la UX ya era invisible respecto a PlantNet (D-02).
```

Ahora crear el archivo:
```bash
# Se crea vía la tool Write
```
  </action>
  <verify>
    <automated>test -f .planning/phases/05-plantnet-fourth-provider/VERIFICATION.md &amp;&amp; grep -q "Bloque A" .planning/phases/05-plantnet-fourth-provider/VERIFICATION.md &amp;&amp; grep -q "Bloque F" .planning/phases/05-plantnet-fourth-provider/VERIFICATION.md &amp;&amp; grep -q "Sign-off" .planning/phases/05-plantnet-fourth-provider/VERIFICATION.md &amp;&amp; grep -q "PLANT-01" .planning/phases/05-plantnet-fourth-provider/VERIFICATION.md &amp;&amp; grep -q "PLANT-02" .planning/phases/05-plantnet-fourth-provider/VERIFICATION.md &amp;&amp; grep -q "PLANT-03" .planning/phases/05-plantnet-fourth-provider/VERIFICATION.md &amp;&amp; wc -l .planning/phases/05-plantnet-fourth-provider/VERIFICATION.md | awk '$1 >= 60 {print "OK: VERIFICATION.md con 60+ lineas"}'</automated>
  </verify>
  <acceptance_criteria>
    - Archivo `.planning/phases/05-plantnet-fourth-provider/VERIFICATION.md` creado
    - 6 bloques A/B/C/D/E/F con criterios pasa/no-pasa explícitos
    - Cada bloque tiene ≥1 query o comando ejecutable
    - Sección Sign-off con checkboxes por bloque + decisión CPO
    - Anexo de rollback
    - Todos los requirements (PLANT-01, PLANT-02, PLANT-03) referenciados explícitamente
    - Todas las decisiones no-removed (D-01, D-02, D-03, D-04, D-07, D-09, D-10, D-11, D-12, D-13) cubiertas
    - Estilo consistente con VERIFICATION.md de fases previas (español, secciones con headers, bloques SQL fenced)
  </acceptance_criteria>
  <done>VERIFICATION.md creado y committeable, listo para que la CPO ejecute checklist.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [CHECKPOINT] CPO ejecuta VERIFICATION.md y decide go/no-go</name>
  <files>.planning/phases/05-plantnet-fourth-provider/VERIFICATION.md (la CPO rellena el archivo con resultados y sign-off)</files>
  <action>
Ejecutar el checklist completo del archivo VERIFICATION.md creado en Task 1: correr las queries SQL en Supabase, verificar UX en DevTools, revisar PostHog dashboard, tests + build locales. Rellenar cada campo "Resultado" del checklist y firmar la decisión final (approved / blocked / partial) en la sección Sign-off.
  </action>
  <verify>Sección "Sign-off" del VERIFICATION.md rellena con "✅ APROBADO" y los 6 checkboxes marcados.</verify>
  <done>La CPO ha ejecutado los 6 bloques y firmado approved (o ha documentado bloqueo / partial con siguiente acción).</done>
  <what-built>
El checklist VERIFICATION.md está creado. Ahora la CPO debe:
1. Esperar mínimo 24h desde el deploy del edge function (Plan 03) para tener volumen de datos
2. Ejecutar cada bloque del checklist en Supabase SQL Editor + DevTools + PostHog
3. Marcar los criterios en el archivo
4. Firmar go / no-go

**Tiempo estimado ejecución: 30-45 min** (queries + navegación + notas).

Los planes 01/02/03/04 quedan committeados independientemente. Este paso solo pausa el marcado de "Phase 5 completa" en ROADMAP hasta que la CPO firme.
  </what-built>
  <how-to-verify>
Abrir `.planning/phases/05-plantnet-fourth-provider/VERIFICATION.md` y trabajar los 6 bloques:

**Bloque A — Cobertura de datos (5 min):**
1. Copiar la query A.1 a Supabase SQL Editor
2. Ejecutar
3. Anotar `pct_con_plantnet` en el archivo
4. Si <95%, ejecutar A.2 y anotar breakdown de errores

**Bloque B — Cross-validation funciona (10 min):**
1. Ejecutar B.1 (muestreo de 30 identificaciones recientes)
2. Anotar distribución de las 5 categorías
3. Ejecutar B.2 (verificar que genus-only NO cuenta como override)

**Bloque C — Divergencia registrada (5 min):**
1. Ejecutar query 13 de `docs/model-evaluation-queries.sql` (copy-paste)
2. Pegar output de primeras 5-10 filas
3. Abrir PostHog dashboard, filtrar `plantnet_divergence` de últimas 24h, contar eventos

**Bloque D — UX invisible (5 min):**
1. Ir a mi-plantita-feliz.vercel.app
2. Hacer 1 identificación
3. DevTools → Network → copiar el JSON response
4. Verificar `models.length === 3` y sin plantnet
5. Comparar UX visual con recuerdo pre-Phase-5

**Bloque E — Latencia (5 min):**
1. Ejecutar query E.1 (latencia PlantNet)
2. Ejecutar query E.1 complementaria (winner latency antes/después)
3. Anotar p95

**Bloque F — Regresión (5 min):**
1. Localmente: `npm run test -- --run` — anotar resumen
2. Localmente: `npm run build` — verifica que compila

**Sign-off (2 min):**
Marcar los 6 checkboxes + escribir la decisión final.

**Si algo bloquea:** describir en la sección "espacio para notas" y comunicar. Los issues comunes:
- `pct_con_plantnet` bajo → revisar secret PLANTNET_API_KEY, revisar logs edge fn
- Divergencia = 0 con >50 búsquedas → probablemente el threshold 0.8 esté muy alto o los LLMs y PlantNet están demasiado alineados (revisar en Phase 5.1 candidate)
- Response JSON expone plantnet → bug en Plan 03 STEP 6 (revisar modelsSummary)
  </how-to-verify>
  <resume-signal>"approved" (firma go, avanzar a Task 3) / "blocked: <descripción>" (parar y arreglar) / "partial: N bloques ok, M pendientes de datos" (esperar más volumen).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Actualizar ROADMAP.md + REQUIREMENTS.md + STATE.md tras sign-off</name>
  <read_first>
    - .planning/ROADMAP.md (localizar Phase 5 y tabla de progreso)
    - .planning/REQUIREMENTS.md (localizar PLANT-01/02/03 y tabla trazabilidad)
    - .planning/STATE.md (localizar frontmatter progress + Current Position)
    - .planning/phases/05-plantnet-fourth-provider/VERIFICATION.md (para timestamp de sign-off)
  </read_first>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <action>
Esta tarea solo se ejecuta si Task 2 devolvió "approved". Si devolvió "blocked" o "partial", saltar y esperar.

**1) `.planning/REQUIREMENTS.md`:**

Reemplazar en la sección "Identification Providers":
```
- [ ] **PLANT-01**: ...
- [ ] **PLANT-02**: ...
- [ ] **PLANT-03**: ...
```
Por:
```
- [x] **PLANT-01**: ...
- [x] **PLANT-02**: ...
- [x] **PLANT-03**: ...
```

Y en la tabla de trazabilidad al final, cambiar:
```
| PLANT-01 | Fase 5 | Pendiente |
| PLANT-02 | Fase 5 | Pendiente |
| PLANT-03 | Fase 5 | Pendiente |
```
Por:
```
| PLANT-01 | Fase 5 (Plan 03) | Completo |
| PLANT-02 | Fase 5 (Plan 04) | Completo |
| PLANT-03 | Fase 5 (Plan 03 + benchmark 05-BENCHMARK.md) | Completo |
```

**2) `.planning/ROADMAP.md`:**

En la lista de phases al principio, cambiar:
```
- [ ] **Phase 5: PlantNet as Fourth Identification Provider** — ...
```
Por:
```
- [x] **Phase 5: PlantNet as Fourth Identification Provider — Cross-Validated Voter** — Añadido PlantNet como 4º proveedor votante con cross-validation. UX invisible al usuario, coherencia nombre-cuidados preservada, divergencias registradas en DB + PostHog. Completado 2026-08-XX.
```
(sustituir 2026-08-XX por la fecha real del sign-off en VERIFICATION.md).

En la sección `### Phase 5:`, cambiar el `**Status:**` a `**Status:** Complete (2026-08-XX)`.

En la lista de plans (dentro de Phase 5), cambiar cada `- [ ]` por `- [x]`.

En la tabla `## Progress` al final, cambiar la fila:
```
| 5. PlantNet as Fourth Provider | 0/? | Ready for planning | - |
```
Por:
```
| 5. PlantNet as Fourth Voter | 5/5 | Complete | 2026-08-XX |
```

**3) `.planning/STATE.md`:**

En el frontmatter, actualizar:
```yaml
progress:
  total_phases: 10
  completed_phases: 8    # ← +1 (era 7)
  total_plans: 44        # ← +5 nuevos plans (era 39)
  completed_plans: 39    # ← +5 (era 34)
  percent: 89            # ← recalcular
```

En "Current Position", cambiar:
```
**Status:** Ready to execute
**Last closed:** Phase 04.1 ...
**In progress:** Phase 5 (PlantNet as Fourth Provider) — CONTEXT.md ready, siguiente = `/gsd-plan-phase 5`
```
Por:
```
**Status:** Ready to execute
**Last closed:** Phase 5 (PlantNet as Fourth Voter — Cross-Validated) — ✅ APPROVED por CPO 2026-08-XX (VERIFICATION.md sign-off)
**In progress:** ninguna — próxima phase pendiente a decidir. Candidatos activos: Phase 2 (Prompt Optimization), Phase 5.1 (Eliminar Claude — indicado por benchmark D-13)
```

Y en "Session Continuity":
```
Última sesión: 2026-08-XX — Phase 5 cerrada. PlantNet vive como 4º votante con cross-validation. UX invisible confirmado por CPO. Divergencias visibles vía query 13 + PostHog. Deferred: Phase 5.1 candidate (eliminar Claude — benchmark mostró 0/10 wins) revisar tras 2-4 semanas de datos de divergencia.
Siguiente acción: `/gsd-discuss-phase 2` (Prompt Optimization) o `/gsd-discuss-phase 5.1` según prioridad CPO.
```

Ejecutar los cambios y verificar con git status/diff.
  </action>
  <verify>
    <automated>grep -q "\[x\] \*\*Phase 5:" .planning/ROADMAP.md &amp;&amp; grep -q "\[x\] \*\*PLANT-01\*\*" .planning/REQUIREMENTS.md &amp;&amp; grep -q "\[x\] \*\*PLANT-02\*\*" .planning/REQUIREMENTS.md &amp;&amp; grep -q "\[x\] \*\*PLANT-03\*\*" .planning/REQUIREMENTS.md &amp;&amp; grep -q "completed_phases: 8" .planning/STATE.md &amp;&amp; grep -q "PLANT-01 | Fase 5" .planning/REQUIREMENTS.md &amp;&amp; echo "OK: ROADMAP + REQUIREMENTS + STATE actualizados"</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md: PLANT-01/02/03 con `[x]`
    - REQUIREMENTS.md: tabla trazabilidad con "Completo" para los 3
    - ROADMAP.md: Phase 5 con `[x]` en la lista principal
    - ROADMAP.md: sección Phase 5 con Status "Complete"
    - ROADMAP.md: tabla Progress actualizada a "5/5 Complete"
    - STATE.md: frontmatter progress con completed_phases=8, percent actualizado
    - STATE.md: Current Position + Session Continuity reflejan Phase 5 cerrada
  </acceptance_criteria>
  <done>ROADMAP + REQUIREMENTS + STATE reflejan el cierre de Phase 5. Todos los cambios committeados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VERIFICATION.md + docs de proyecto | Documentación interna. Riesgo bajo (no expone secretos). |
| CPO ejecutando queries SQL como owner | Acceso completo a DB — mismo perfil que hoy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Repudiation | Sign-off sin trazabilidad | mitigate | VERIFICATION.md se committea en git con timestamp. La firma de la CPO + fecha quedan en el archivo. |
| T-05-05-02 | Tampering | ROADMAP marcado como completo sin verificación real | mitigate | Task 3 depende de "approved" de Task 2. Si la CPO no aprueba, Task 3 no se ejecuta. Auditable en el commit history. |
| T-05-05-03 | Information Disclosure | Query B.1 devuelve UUIDs de plant_search — no PII | accept | UUID no es PII sensible. Ejecutable solo desde SQL Editor por owner. |
</threat_model>

<verification>
- VERIFICATION.md creado, con 6 bloques + sign-off + anexo rollback (Task 1)
- CPO ha ejecutado el checklist y firmado approved (Task 2)
- ROADMAP.md, REQUIREMENTS.md, STATE.md reflejan cierre (Task 3)
</verification>

<success_criteria>
1. VERIFICATION.md existe, cubre los 3 requirements y las 10 decisiones activas (D-01..D-04, D-07, D-09..D-13)
2. CPO ha ejecutado el checklist con datos reales y ha firmado el sign-off
3. ROADMAP marca Phase 5 como completa con fecha
4. REQUIREMENTS marca PLANT-01/02/03 como completos
5. STATE refleja el nuevo current position (Phase 5 cerrada, próxima phase pendiente)
6. Los planes 01-05 aparecen todos marcados como completos en ROADMAP § Phase 5 (Plans)
</success_criteria>

<output>
Al terminar, crea `.planning/phases/05-plantnet-fourth-provider/05-05-SUMMARY.md` con:
- Fecha de sign-off
- Resumen del resultado de cada bloque (A/B/C/D/E/F)
- Observaciones destacables (tasa de divergencia observada, latencia real vs benchmark, cualquier issue menor)
- Referencias cruzadas a los 4 SUMMARYs previos
- Recomendaciones para Phase 5.1 candidate (eliminar Claude) o Phase 5.2 candidate (reemplazo total) según datos observados
</output>
