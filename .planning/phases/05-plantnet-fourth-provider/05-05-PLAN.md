---
phase: 05-plantnet-fourth-provider
plan: 05
type: execute
wave: 5
depends_on: [05-03, 05-04]
files_modified:
  - .planning/phases/05-plantnet-fourth-provider/05-VERIFICATION.md
autonomous: false
requirements: [PLANT-01, PLANT-02, PLANT-03]
tags: [verification, smoke-test, sql, cpo-checkpoint, phase-gate]

must_haves:
  truths:
    - "≥5 identificaciones post-deploy generan 4 filas cada una en model_evaluations (PLANT-01 ≥95%)"
    - "PlantNet rows tienen raw_response NOT NULL en success cases (jsonb válido)"
    - "Query A/C/D/D-BIS de docs/model-evaluation-queries.sql corren sin error sintáctico"
    - "PlantNet P95 latency ≤ min(claude P95, gemini P95, gpt4o P95) — PLANT-03 verificado"
    - "El cliente sigue recibiendo response idéntica (D-02) — smoke test en app producción"
    - "Ninguna regresión en tests existentes (npm run test verde)"
    - "VERIFICATION.md documenta resultados de cada check + CPO decision go/no-go"
  artifacts:
    - path: ".planning/phases/05-plantnet-fourth-provider/05-VERIFICATION.md"
      provides: "Checklist ejecutado + resultados de queries SQL + CPO sign-off"
      contains: "PHASE 5 VERIFICATION"
  key_links:
    - from: "05-VERIFICATION.md § SQL check A"
      to: "docs/model-evaluation-queries.sql Query A"
      via: "CPO copy-paste + snapshot result"
      pattern: "Query A"
    - from: "05-VERIFICATION.md § CPO sign-off"
      to: "STATE.md next milestone update"
      via: "manual state update"
      pattern: "Phase 5 completed"
---

<objective>
Cerrar Phase 5 con una verificación end-to-end que confirma los 3 requirements (PLANT-01, PLANT-02, PLANT-03) contra datos reales de producción. Ejecutar smoke tests SQL, comprobar que el cliente NO cambió su experiencia (D-02), y obtener sign-off de la CPO antes de marcar la phase completa en `STATE.md`.

Este plan también captura la **primera pasada de análisis** (Success Criterion #5 del ROADMAP): la CPO corre las 4 queries del `docs/model-evaluation-queries.sql § PHASE 5` contra ≥5 identificaciones reales y anota conclusiones preliminares sobre si Phase 5.1 (dar voto real a PlantNet) tiene sentido con los datos actuales.

Purpose: Cerrar la phase con evidencia. Requirement PLANT-03 (latencia P95 no empeora) SOLO es verificable post-deploy con datos reales — no hay forma de probarlo a priori.

Output:
- `.planning/phases/05-plantnet-fourth-provider/05-VERIFICATION.md` con:
  - Resultado de smoke test SQL (Query A: 4 filas, plantnet presente)
  - Resultado de latencia SQL (Query C: PlantNet P95 ≤ mín LLM P95)
  - Resultado de divergencia SQL (Query D-BIS: %)
  - Snapshot cliente-side: 1 identificación en app producción, verificar UI idéntica + PostHog event `plant_identified` sin `model:plantnet` en `models`
  - Conclusiones preliminares Phase 5.1 (¿trigger de >10 puntos ya visible?)
  - CPO sign-off go/no-go
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-plantnet-fourth-provider/05-CONTEXT.md
@.planning/phases/05-plantnet-fourth-provider/05-RESEARCH.md
@docs/model-evaluation-queries.sql
@supabase/functions/identify-plant/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escribir esqueleto de VERIFICATION.md + esperar ≥5 identificaciones reales post-deploy</name>
  <read_first>
    - .planning/phases/05-plantnet-fourth-provider/05-CONTEXT.md § Success Criteria (referencia para checklist)
    - docs/model-evaluation-queries.sql § PHASE 5 (para saber qué queries se van a ejecutar)
    - .planning/ROADMAP.md § Phase 5 Success Criteria (líneas 169-174)
  </read_first>
  <files>.planning/phases/05-plantnet-fourth-provider/05-VERIFICATION.md</files>
  <action>
    Crear archivo `.planning/phases/05-plantnet-fourth-provider/05-VERIFICATION.md` con el esqueleto siguiente (en español, coherente con VERIFICATION.md de Phase 04.1 según STATE.md):

    ```markdown
    # Phase 5 — VERIFICATION

    **Fecha inicio:** 2026-08-15
    **Deploy edge function:** [fecha del deploy real, plan 05-03 Task 2]
    **Verificado por:** Claude Code + CPO
    **Estado:** In progress → CPO sign-off pending

    ---

    ## Checks

    ### Bloque A — Datos crudos en DB (PLANT-01 ≥95% persistencia)

    **Query:** copy-paste `docs/model-evaluation-queries.sql § PHASE 5 § A` en Supabase SQL Editor.

    Esperado:
    - 4 filas devueltas (una por modelo: claude, gemini, gpt4o, plantnet)
    - `total_llamadas` similar entre las 4 filas (~mismo N si volumen ha sido continuo)
    - `tasa_exito_pct` de plantnet debe ser ≥95% (PLANT-01). Si <95%, investigar:
      - Verificar que `PLANTNET_API_KEY` está en Function Secrets
      - Verificar que no se está excediendo free tier (500/día — con 15-30/día, imposible)
      - Verificar en logs de Supabase Functions si hay `plantnet API error:` recurrente

    Resultado real (rellenar tras ejecutar):
    ```
    [pegar output de Query A aquí, formato tabla]
    ```

    Status: [ ] PASS / [ ] FAIL — [comentario]

    ---

    ### Bloque B — Latencia comparada (PLANT-03 no regresión P95)

    **Query:** copy-paste `docs/model-evaluation-queries.sql § PHASE 5 § C` en Supabase SQL Editor.

    Esperado:
    - PlantNet `p95_ms` ≤ 10_000 (respeta el timeout de 10s configurado)
    - PlantNet `p95_ms` ≤ mín(claude p95_ms, gemini p95_ms, gpt4o p95_ms) — deseable pero no requirement duro
    - PlantNet `p95_ms` ≤ 15_000 (LLM timeout) — REQUIRED para no ser cuello de botella (PLANT-03)
    - LLMs P95 no ha aumentado vs baseline pre-Phase-5 (comparar con snapshot en 04.1 VERIFICATION si disponible)

    Resultado real:
    ```
    [pegar output de Query C aquí]
    ```

    Status: [ ] PASS / [ ] FAIL — [comentario]

    ---

    ### Bloque C — Divergencia PlantNet vs winner-LLM (métrica honesta para Phase 5.1 signal)

    **Query:** copy-paste `docs/model-evaluation-queries.sql § PHASE 5 § D-BIS` en Supabase SQL Editor.

    Esperado (informational, no gating):
    - `busquedas_comparables` ≥ 5 (mínimo para signal preliminar)
    - `divergen_pct` es el número clave. Trigger del ROADMAP para Phase 5.1: >10 puntos de diferencia sostenida.
    - Interpretación:
      - Divergencia baja (<10%): PlantNet y LLMs coinciden mayormente — poca ganancia de voto real
      - Divergencia media (10-30%): merece Phase 5.1 análisis con más volumen
      - Divergencia alta (>30%): PlantNet aporta perspectiva claramente distinta, prioridad Phase 5.1

    Resultado real:
    ```
    [pegar output de Query D-BIS aquí]
    ```

    Casos concretos (Query D, LIMIT 50):
    ```
    [pegar top 10 filas de Query D aquí para spot-check visual]
    ```

    Interpretación preliminar de la CPO (2-3 líneas): [rellenar]

    Status: [ ] SEÑAL INSUFICIENTE (<5 busquedas) / [ ] BAJA / [ ] MEDIA / [ ] ALTA

    ---

    ### Bloque D — Query B (top-1 accuracy vs golden set) — respeta D-10/D-11

    Si D-10 = A2 (skipped): marcar `N/A per D-10 = A2` y saltar a Bloque E.

    Si D-10 = A1/A3/A4 y D-11 = B1: la tabla `public.golden_set_ground_truth` debe estar poblada (ver 05-CONTEXT.md § Addendum). Ejecutar Query B tras poblarla.

    Esperado (con caveat D-10 documentado):
    - 4 filas devueltas si tabla poblada con las 31 fotos re-corridas
    - Accuracy plantnet ≈100% si el golden set sigue contaminado (D-10 = A1)
    - Accuracy LLMs es el número honesto — comparar en absoluto, no relativo a plantnet

    Resultado real:
    ```
    [pegar output de Query B aquí, o "SKIPPED per D-10=A2"]
    ```

    Status: [ ] PASS / [ ] SKIPPED / [ ] FAIL

    ---

    ### Bloque E — Cliente-side: D-02 preservado (invisible al usuario)

    **Smoke test manual:**
    1. Abrir app en producción (https://mi-plantita-feliz.vercel.app o Android v1.3.0)
    2. Hacer 1 identificación real (foto o carga)
    3. Verificar UI:
       - [ ] Result screen idéntica a pre-Phase-5 (sin badges, sin señales de plantnet)
       - [ ] `PlantResultView` renderiza los mismos campos
       - [ ] Loading state igual (sin nuevo skeleton para plantnet)

    4. Verificar Network tab (DevTools navegador):
       - [ ] Response del edge function tiene shape `{ name, care, diagnosis, watering_interval_days, model, models: [...] }`
       - [ ] Array `models` tiene 3 items (claude, gemini, gpt4o) — NO 4
       - [ ] Ningún item de `models` tiene `model: "plantnet"`

    5. Verificar PostHog event `plant_identified`:
       - [ ] Event dispara con property `models` de 3 items
       - [ ] Property `models` NO incluye "plantnet"
       - [ ] Property `winner` es uno de claude/gemini/gpt4o

    Status: [ ] PASS / [ ] FAIL — [comentario, screenshot si fail]

    ---

    ### Bloque F — Regresión: tests existentes verdes

    ```bash
    npm run test
    ```

    Esperado: todos los tests pasan sin modificación. En particular:
    - `src/test/consensus.test.ts` — verifica que consensus.ts no cambió
    - `src/hooks/use-plant-identifier.test.ts` — verifica que el client mock no rompe

    Resultado:
    ```
    [pegar tail de npm test output aquí]
    ```

    Status: [ ] PASS / [ ] FAIL

    ---

    ## Conclusiones preliminares Phase 5.1

    (Rellenar tras ejecutar los bloques A-E con ≥5 identificaciones reales)

    - Divergencia observada: __%
    - Trigger ROADMAP Phase 5.1 (>10 pts) alcanzado: [ ] Sí / [ ] No / [ ] Datos insuficientes
    - Recomendación CPO: [ ] Abrir Phase 5.1 candidate / [ ] Esperar más data (target N) / [ ] Descartar

    ---

    ## CPO Sign-off

    **Fecha:** [rellenar]
    **CPO:** Noemi
    **Decisión:** [ ] APPROVED / [ ] REJECTED / [ ] PARTIAL (con notas)
    **Notas:**
    - [comentarios de la CPO]

    Si APPROVED → actualizar `.planning/STATE.md`: Phase 5 marked complete, planned_phase → siguiente (Phase 2 o Phase 5.1 según decisión).
    Si REJECTED → gap-closure plan mediante `/gsd-plan-phase 5 --gaps`.

    ---

    *VERIFICATION file para Phase 5 — plantnet-fourth-provider*
    ```

    **Preparación pre-verificación:** el plan requiere ≥5 identificaciones reales post-deploy. Si el volumen actual (~15-30/día) las genera naturalmente, esperar ~4-8 horas post-deploy antes de rellenar los checks. Alternativamente, la CPO puede hacer 5 identificaciones manuales rápidas en la app para acelerar (5 fotos × 20s = ~2 min).
  </action>
  <verify>
    <automated>test -f .planning/phases/05-plantnet-fourth-provider/05-VERIFICATION.md && grep -q "PHASE 5" .planning/phases/05-plantnet-fourth-provider/05-VERIFICATION.md && grep -q "Bloque A" .planning/phases/05-plantnet-fourth-provider/05-VERIFICATION.md && grep -q "Bloque E" .planning/phases/05-plantnet-fourth-provider/05-VERIFICATION.md && grep -q "CPO Sign-off" .planning/phases/05-plantnet-fourth-provider/05-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Archivo `05-VERIFICATION.md` creado con esqueleto en español
    - 6 bloques (A-F) presentes: PLANT-01 datos crudos, PLANT-03 latencia, divergencia, query B, cliente D-02, regresión tests
    - Sección "Conclusiones preliminares Phase 5.1" presente
    - Sección "CPO Sign-off" presente con checkboxes go/no-go
  </acceptance_criteria>
  <done>
    Esqueleto de verification listo. Task 2 lo rellena con datos reales tras el deploy + esperar volumen mínimo.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] CPO ejecuta queries + smoke test app + sign-off en VERIFICATION.md</name>
  <what-built>
    Todo el pipeline de Phase 5 está desplegado:
    - Edge function `identify-plant` con 4 promesas paralelas (3 LLMs + plantnet)
    - Migración aplicada (`raw_response jsonb` + widened CHECK constraint)
    - Docs SQL con 4 queries nuevas (A/B/C/D + D-BIS)
    - (Opcional) Tabla auxiliar `golden_set_ground_truth` si D-11 = B1

    Falta que la CPO valide con datos reales. Este checkpoint es LEGITIMO porque:
    - PLANT-03 (latencia P95) SOLO se verifica con datos reales post-deploy
    - PLANT-01 (≥95% persistencia) requiere volumen para ser señal
    - D-02 (invisible al usuario) requiere smoke test manual en la app real
    - PostHog event verification requiere sesión real de la CPO en la app
  </what-built>
  <how-to-verify>
    Pasos concretos para la CPO:

    **PASO 1 — Verificar volumen mínimo (≥5 identificaciones nuevas post-deploy):**
    Abrir Supabase SQL Editor, ejecutar:
    ```sql
    SELECT COUNT(*) AS busquedas_post_deploy
    FROM plant_searches
    WHERE created_at >= '[fecha del deploy plan 05-03]';
    ```
    Si <5, hacer 5 identificaciones rápidas en la app (foto+enter+esperar resultado) hasta llegar a ≥5.

    **PASO 2 — Ejecutar y rellenar bloques A, B, C del VERIFICATION.md:**
    - Bloque A: Query A → pegar resultado tabla, marcar PASS si plantnet aparece con tasa ≥95%
    - Bloque B: Query C → pegar resultado, marcar PASS si plantnet p95 ≤ 10_000
    - Bloque C: Query D-BIS → pegar resultado + top 10 de Query D, interpretar divergencia

    **PASO 3 — Bloque D (query B) según D-10/D-11:**
    - Si D-10 = A2: marcar SKIPPED
    - Si D-10 = A1/A3/A4 + D-11 = B1: poblar `golden_set_ground_truth` con las 31 fotos del set (script ad-hoc o INSERT manual), luego ejecutar query B y pegar resultado
    - Si D-11 = B2/B3/B4: seguir el path acordado en 05-CONTEXT.md addendum

    **PASO 4 — Bloque E (smoke test app):**
    - Abrir https://mi-plantita-feliz.vercel.app (o Android v1.3.0) en móvil
    - Identificar 1 planta con foto real
    - Con DevTools abierto (F12), verificar:
      - Network tab → response del edge function → `models` array tiene 3 items, ninguno con model="plantnet"
    - Con PostHog dashboard abierto (paralelo): buscar evento `plant_identified` de la identificación → verificar property `models` sin "plantnet"

    **PASO 5 — Bloque F (regresión tests):**
    ```bash
    cd ~/mi-plantita-feliz
    npm run test
    ```
    Pegar el tail del output.

    **PASO 6 — Rellenar Conclusiones preliminares Phase 5.1:**
    Basado en divergen_pct de Bloque C:
    - <10%: recomendación "esperar más data"
    - 10-30%: recomendación "considerar Phase 5.1 pero necesita más volumen"
    - >30%: recomendación "abrir Phase 5.1 candidate"

    **PASO 7 — Sign-off:**
    - Rellenar fecha, marcar APPROVED / REJECTED / PARTIAL con notas
    - Commit `.planning/phases/05-plantnet-fourth-provider/05-VERIFICATION.md`

    Tiempo estimado: 30-45 min (mayoría tiempo esperando volumen si no hay 5 identificaciones ya).
  </how-to-verify>
  <resume-signal>Escribe "aprobado" con las conclusiones clave (divergencia %, decisión Phase 5.1) o "rechazado" con el gap detectado — se abrirá plan de gap-closure</resume-signal>
  <files>.planning/phases/05-plantnet-fourth-provider/05-VERIFICATION.md</files>
  <action>
    PAUSAR EJECUCIÓN tras Task 1 (skeleton creado). Presentar a la CPO los 7 pasos concretos del bloque `<how-to-verify>` arriba: (1) verificar ≥5 identificaciones post-deploy, (2) ejecutar bloques A/B/C, (3) query B según D-10/D-11, (4) smoke test app + DevTools + PostHog, (5) npm run test, (6) rellenar conclusiones Phase 5.1, (7) sign-off.

    La CPO ejecuta y rellena `.planning/phases/05-plantnet-fourth-provider/05-VERIFICATION.md` con datos reales. Este task NO escribe el archivo (la CPO lo hace directamente en su editor con los resultados) — es checkpoint puramente humano de ejecución + captura de datos.

    Cuando la CPO responda "aprobado" o "rechazado", el bucle se cierra: aprobado → actualizar STATE.md marking Phase 5 complete; rechazado → recomendar `/gsd-plan-phase 5 --gaps`.
  </action>
  <verify>
    <automated>test -s .planning/phases/05-plantnet-fourth-provider/05-VERIFICATION.md && grep -q "APPROVED\|REJECTED\|PARTIAL" .planning/phases/05-plantnet-fourth-provider/05-VERIFICATION.md</automated>
  </verify>
  <done>VERIFICATION.md rellenado con datos reales de los 6 bloques + CPO sign-off (APPROVED / REJECTED / PARTIAL) committed en el archivo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Esta phase es puramente verificación post-deploy. Las trust boundaries son las heredadas de plans 05-02, 05-03, 05-04.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Information Disclosure | Snapshot de queries en VERIFICATION.md commit | mitigate | Los resultados no contienen PII (nombres científicos, latencias, image_urls públicas). Si aparece image_url de plant_searches en el commit, verificar que corresponde al bucket público y no leak de RLS. |
| T-05-05-02 | Repudiation | CPO sign-off sin auditoría | accept | El sign-off queda committed en git con timestamp + firma en el archivo. Audit trail suficiente para proyecto solo-CPO. |
</threat_model>

<verification>
- VERIFICATION.md creado con esqueleto (Task 1 automated)
- CPO ha ejecutado los 6 bloques y rellenado con datos reales (Task 2 human checkpoint)
- Sign-off APPROVED/REJECTED committed en el archivo
</verification>

<success_criteria>
- ROADMAP Phase 5 Success Criterion #1 verificado: ≥95% de identificaciones nuevas tienen fila plantnet en model_evaluations
- ROADMAP Phase 5 Success Criterion #2 verificado: queries SQL corren sin error, resultados interpretables
- ROADMAP Phase 5 Success Criterion #3 verificado: flujo visible al usuario idéntico (Bloque E)
- ROADMAP Phase 5 Success Criterion #4 verificado: latencia P95 plantnet ≤ 10s (Bloque B)
- ROADMAP Phase 5 Success Criterion #5 verificado: primera pasada de análisis + conclusión Phase 5.1 documentada
- CPO ha decidido go/no-go sobre Phase 5 y (según divergen_pct) sobre Phase 5.1 candidate
</success_criteria>

<output>
After completion, create `.planning/phases/05-plantnet-fourth-provider/05-05-SUMMARY.md`
</output>
