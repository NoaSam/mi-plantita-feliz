---
phase: 05-plantnet-fourth-provider
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - supabase/functions/identify-plant/index.ts
autonomous: false
requirements: [PLANT-01, PLANT-03]

must_haves:
  truths:
    - "PlantNet se llama en paralelo con los 3 LLMs vía Promise.allSettled (4 promesas totales) por identificación"
    - "El SSE / respuesta al cliente sigue exponiendo SOLO los 3 LLMs en `models` — plantnet se filtra explícitamente (D-02)"
    - "Se inserta 1 fila plantnet en `model_evaluations` por identificación (raw_response = JSON completo per D-03; success=false + raw_response=null si falló per D-09)"
    - "Cuando applyPlantnetOverride devuelve `diverged=true`, se marca `plant_searches.plantnet_diverged=true` en el mismo INSERT (o UPDATE) y se dispara evento PostHog `plantnet_divergence`"
    - "Cuando applyPlantnetOverride devuelve un winner con scientificName sustituido, el JSON de respuesta al cliente lleva el científico de PlantNet (con el nombre común del LLM alineado) — SIN badges, SIN señales visibles (D-02)"
    - "Si PlantNet falla (timeout, quota, 4xx/5xx), el flujo sigue exacto que hoy: 3 LLMs deciden, respuesta idéntica al cliente, fila plantnet con success=false (D-09)"
    - "La latencia percibida por el usuario no empeora: PlantNet corre en paralelo con timeout ≤ 10s (< LLM_TIMEOUT_MS=15s); D-13 confirma p50 PlantNet=307ms << p50 LLM más rápido=917ms (PLANT-03)"
  artifacts:
    - path: "supabase/functions/identify-plant/index.ts"
      provides: "Edge function con callPlantnet + integración cross-validation + persistencia 4ª fila + flag divergencia + PostHog event"
      contains: "callPlantnetTimed"
  key_links:
    - from: "supabase/functions/identify-plant/index.ts"
      to: "PlantNet API (https://my-api.plantnet.org/v2/identify/all)"
      via: "fetch multipart con base64 blob"
      pattern: "my-api\\.plantnet\\.org/v2/identify"
    - from: "supabase/functions/identify-plant/index.ts"
      to: "consensus.ts (applyPlantnetOverride, matchScientific)"
      via: "import"
      pattern: "import.*applyPlantnetOverride.*from.*consensus"
    - from: "supabase/functions/identify-plant/index.ts"
      to: "model_evaluations (INSERT 4ª fila)"
      via: "supabaseAdmin.from('model_evaluations').insert(...)"
      pattern: "model:\\s*['\"]plantnet['\"]"
    - from: "supabase/functions/identify-plant/index.ts"
      to: "plant_searches.plantnet_diverged (SET true en divergencia)"
      via: "INSERT con plantnet_diverged o UPDATE post-insert"
      pattern: "plantnet_diverged"
    - from: "supabase/functions/identify-plant/index.ts"
      to: "PostHog (evento plantnet_divergence)"
      via: "fetch POST a eu.i.posthog.com desde el edge fn (no hay SDK server-side)"
      pattern: "plantnet_divergence"
---

<objective>
Extender `supabase/functions/identify-plant/index.ts` con la 4ª llamada a PlantNet + la lógica de cross-validation (aplicando `applyPlantnetOverride` de Plan 02) + persistencia de la 4ª fila + flag de divergencia + evento PostHog. Preservar el shape del response al cliente (D-02 invisible).

**Purpose:** Este es el corazón funcional de Phase 5. Aquí se conectan la migración (Plan 01) y la lógica pura (Plan 02) al flujo real de identificación. Depende de ambos porque:
- Necesita `raw_response` (columna nueva) y `plantnet_diverged` (columna nueva) en el schema
- Necesita `applyPlantnetOverride` importable desde `consensus.ts`

**Output:**
- `index.ts` con `callPlantnetTimed` nueva + Promise.allSettled ampliado a 4 promesas + split llmResults/plantnetResult + integración con applyPlantnetOverride + 4ª fila en model_evaluations con raw_response + flag plant_searches.plantnet_diverged en divergencia + dispatch PostHog event server-side
- Deploy del edge function a producción
- Verificación con 3-5 identificaciones reales (checkpoint humano)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-plantnet-fourth-provider/05-CONTEXT.md
@.planning/phases/05-plantnet-fourth-provider/05-RESEARCH.md
@.planning/phases/05-plantnet-fourth-provider/05-PATTERNS.md
@.planning/phases/05-plantnet-fourth-provider/05-BENCHMARK.md
@.planning/phases/05-plantnet-fourth-provider/05-01-SUMMARY.md
@.planning/phases/05-plantnet-fourth-provider/05-02-SUMMARY.md
@supabase/functions/identify-plant/index.ts
@supabase/functions/identify-plant/consensus.ts

<interfaces>
<!-- Estado actual del edge function (extraído verbatim del código) -->

Imports actuales (líneas 1-6):
```typescript
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";
import {
  type ModelName,
  extractScientificName,
  computeConsensus,
} from "./consensus.ts";
```

Constante de timeout LLM (línea 95):
```typescript
const LLM_TIMEOUT_MS = 15_000;
```

Helper isAbortError (líneas 99-101):
```typescript
function isAbortError(e: unknown): boolean {
  return e instanceof Error && (e.name === "AbortError" || e.name === "TimeoutError");
}
```

Shape actual del Promise.allSettled (líneas 389-397):
```typescript
const settled = await Promise.allSettled([
  callModelTimed("claude", () => callClaude(base64Data, mediaType)),
  callModelTimed("gemini", () => callGemini(base64Data, mediaType)),
  callModelTimed("gpt4o",  () => callOpenAI(base64Data, mediaType)),
]);
```

Inserción model_evaluations actual (líneas 484-511) — columnas por fila:
```
plant_search_id, model, raw_name, scientific_name, description, care, diagnosis,
response_ms, success, error_message, is_winner, consensus_group, consensus_match_level
```

Shape del response actual al cliente (líneas 528-541):
```typescript
{
  name, description, care, diagnosis, watering_interval_days,
  model,                          // ← el winner LLM ('claude' | 'gemini' | 'gpt4o')
  plant_search_id, created_at,
  models: modelsSummary,           // ← array de 3 items (LLMs)
  consensus_reached,
}
```

Función callPlantnetTimed a copiar VERBATIM (05-RESEARCH.md líneas 238-308):
Ver el bloque "Pattern 2: PlantNet caller with timeout, silent failure" en 05-RESEARCH.md. Copiar sin modificaciones, respetando la interfaz `PlantnetEvaluationResult` que incluye además el campo `score`.

**IMPORTANTE — ajuste vs RESEARCH:** La versión de RESEARCH.md L229-236 define `PlantnetEvaluationResult` SIN `score`. Este plan NECESITA `score` (D-10). Añadir el campo:

```typescript
interface PlantnetEvaluationResult {
  success: boolean;
  rawName: string | null;
  scientificName: string | null;
  score: number | null;          // ← AÑADIR: results[0].score, needed for D-10 threshold
  rawResponse: unknown | null;
  responseMs: number;
  errorMessage: string | null;
}
```

Y en la extracción del top-1 (RESEARCH L285-289), extraer también:
```typescript
const top = json?.results?.[0];
const rawName = top?.species?.scientificNameWithoutAuthor ?? null;
const score = typeof top?.score === "number" ? top.score : null;
const scientificName = typeof rawName === "string" ? rawName.toLowerCase() : null;
```

Constante nueva (junto a LLM_TIMEOUT_MS):
```typescript
const PLANTNET_TIMEOUT_MS = 10_000;   // < LLM_TIMEOUT_MS por diseño (D-13)
```

PostHog dispatch desde Deno edge function:
- No hay SDK server-side de PostHog para Deno
- Usar `fetch` directo a `https://eu.i.posthog.com/capture/`
- Payload: `{ api_key, event, distinct_id, properties }`
- `POSTHOG_API_KEY` (PROJECT API key, NO personal) debe estar en Function Secrets — CPO debe registrarla si no está (checkpoint informativo en Task 3)

Convención de PostHog en el repo (docs/posthog-events.md):
- Client-side helper `track()` — no reutilizable en Deno
- Este evento se dispara server-side (edge function tiene contexto de divergencia; el cliente NO tiene visibilidad de si hubo divergencia porque D-02)
- `distinct_id`: usar `user_id` si está presente, si no `anonymous_id`, si no un fallback `'edge-fn-anon'`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Añadir callPlantnetTimed + extender Promise.allSettled a 4 promesas</name>
  <read_first>
    - supabase/functions/identify-plant/index.ts (completo, para localizar puntos de inserción)
    - supabase/functions/identify-plant/consensus.ts (post-Plan-02, con applyPlantnetOverride)
    - .planning/phases/05-plantnet-fourth-provider/05-RESEARCH.md líneas 213-373 (Pattern 2 verbatim)
    - .planning/phases/05-plantnet-fourth-provider/05-PATTERNS.md § "Provider caller pattern" (D)
  </read_first>
  <files>supabase/functions/identify-plant/index.ts</files>
  <action>
Modificar `supabase/functions/identify-plant/index.ts` en varios puntos. Todos los cambios en este archivo.

**1) Actualizar imports (línea 2-6):**

```typescript
import {
  type ModelName,
  extractScientificName,
  computeConsensus,
  applyPlantnetOverride,
  type PlantnetOverrideInput,
} from "./consensus.ts";
```

**2) Añadir constante después de `LLM_TIMEOUT_MS` (línea ~95):**

```typescript
// PlantNet timeout — deliberately shorter than LLM_TIMEOUT_MS so PlantNet
// never gates the SSE first-winner. D-13 benchmark 2026-08-18 measured
// PlantNet p95 = 695ms, so 10s is ~14× headroom.
const PLANTNET_TIMEOUT_MS = 10_000;
```

**3) Añadir interfaz + función `callPlantnetTimed` después de `callOpenAI` (~línea 252, antes de `// ─── Parsing and extraction`):**

Copiar el bloque de 05-RESEARCH.md líneas 229-308 (Pattern 2) con la modificación descrita en `<interfaces>` (añadir campo `score: number | null`):

```typescript
// ─── PlantNet caller (Phase 5 — silent failure per D-09) ──────────────────────

interface PlantnetEvaluationResult {
  success: boolean;
  rawName: string | null;         // top-1 scientificNameWithoutAuthor
  scientificName: string | null;  // lowercased (matches LLMs' extractScientificName output)
  score: number | null;           // results[0].score (0..1), needed for D-10 threshold
  rawResponse: unknown | null;    // full JSON payload (D-03)
  responseMs: number;
  errorMessage: string | null;
}

async function callPlantnetTimed(
  base64Data: string,
  mediaType: string,
): Promise<PlantnetEvaluationResult> {
  const start = Date.now();
  const PLANTNET_API_KEY = Deno.env.get("PLANTNET_API_KEY");
  if (!PLANTNET_API_KEY) {
    return {
      success: false, rawName: null, scientificName: null, score: null, rawResponse: null,
      responseMs: Date.now() - start,
      errorMessage: "PLANTNET_API_KEY not configured",
    };
  }

  try {
    const binaryStr = atob(base64Data);
    const bytes = new Uint8Array(binaryStr.length);
    for (let i = 0; i < binaryStr.length; i++) bytes[i] = binaryStr.charCodeAt(i);

    const form = new FormData();
    form.append("images", new Blob([bytes], { type: mediaType }), "plant.jpg");
    form.append("organs", "auto");

    // nb-results=5, NO include-related-images (evita inflación storage — Pitfall #6)
    const url = `https://my-api.plantnet.org/v2/identify/all?api-key=${PLANTNET_API_KEY}&nb-results=5&lang=es`;
    const res = await fetch(url, {
      method: "POST",
      body: form,
      signal: AbortSignal.timeout(PLANTNET_TIMEOUT_MS),
    });

    if (!res.ok) {
      const text = await res.text();
      console.error("plantnet API error:", res.status, text.substring(0, 200));
      return {
        success: false, rawName: null, scientificName: null, score: null, rawResponse: null,
        responseMs: Date.now() - start,
        errorMessage: res.status === 429
          ? "RATE_LIMIT:plantnet:429"
          : `API_ERROR:plantnet:${res.status}`,
      };
    }

    const json = await res.json();
    const top = json?.results?.[0];
    const rawName = top?.species?.scientificNameWithoutAuthor ?? null;
    const score  = typeof top?.score === "number" ? top.score : null;
    const scientificName = typeof rawName === "string" ? rawName.toLowerCase() : null;

    return {
      success: rawName !== null,
      rawName,
      scientificName,
      score,
      rawResponse: json,
      responseMs: Date.now() - start,
      errorMessage: rawName === null ? "NO_RESULTS" : null,
    };
  } catch (e) {
    return {
      success: false, rawName: null, scientificName: null, score: null, rawResponse: null,
      responseMs: Date.now() - start,
      errorMessage: isAbortError(e)
        ? `TIMEOUT:plantnet:${PLANTNET_TIMEOUT_MS}ms`
        : (e instanceof Error ? e.message : "Unknown error"),
    };
  }
}
```

**4) Extender Promise.allSettled (líneas 389-397) a 4 promesas:**

```typescript
const step1Start = Date.now();
const settled = await Promise.allSettled([
  callModelTimed("claude", () => callClaude(base64Data, mediaType)),
  callModelTimed("gemini", () => callGemini(base64Data, mediaType)),
  callModelTimed("gpt4o",  () => callOpenAI(base64Data, mediaType)),
  callPlantnetTimed(base64Data, mediaType),   // ← 4ª promesa, shape diferente
]);
const step1Ms = Date.now() - step1Start;
```

**5) Split del resultado post-allSettled (justo después del bloque anterior, reemplaza la extracción actual de `allResults` en línea 411-413):**

```typescript
// SPLIT: los 3 primeros son LLMs (ModelResult), el 4º es PlantNet (PlantnetEvaluationResult).
// pickWinner y computeConsensus reciben SOLO llmResults (D-01: consensus.ts intacto).
const llmSettled = settled.slice(0, 3) as PromiseSettledResult<ModelResult>[];
const plantnetSettled = settled[3] as PromiseSettledResult<PlantnetEvaluationResult>;

const llmResults = llmSettled
  .filter((s): s is PromiseFulfilledResult<ModelResult> => s.status === "fulfilled")
  .map((s) => s.value);

const plantnetResult: PlantnetEvaluationResult | null =
  plantnetSettled.status === "fulfilled" ? plantnetSettled.value : null;
```

**6) Actualizar el `perModelSummary` log (líneas 401-409):**

Añadir el 4º elemento al log:

```typescript
const perModelSummary = (() => {
  const parts: string[] = [];
  const llmNames = ["claude", "gemini", "gpt4o"] as const;
  llmSettled.forEach((s, i) => {
    if (s.status === "fulfilled") {
      const r = s.value;
      parts.push(`${llmNames[i]}=${r.success ? "ok" : (r.errorMessage ?? "fail")}(${r.responseMs}ms)`);
    } else {
      parts.push(`${llmNames[i]}=REJECTED`);
    }
  });
  if (plantnetSettled.status === "fulfilled") {
    const p = plantnetSettled.value;
    const scoreStr = p.score !== null ? ` score=${p.score.toFixed(2)}` : "";
    parts.push(`plantnet=${p.success ? "ok" : (p.errorMessage ?? "fail")}(${p.responseMs}ms)${scoreStr}`);
  } else {
    parts.push(`plantnet=REJECTED`);
  }
  return parts.join(" ");
})();
console.log(`[identify] STEP 1 done in ${step1Ms}ms — ${perModelSummary}`);
```

**7) IMPORTANTE — pickWinner sigue recibiendo SOLO llmResults (línea 416):**

Verifica que NO se pasa `plantnetResult` a `pickWinner`. Este linaje debe permanecer intacto:

```typescript
// STEP 2: Pick winner via LLM consensus (D-01: PlantNet NO participa aquí)
const winner = pickWinner(llmResults);
```

Reemplaza cualquier referencia a `allResults` en las líneas 411-500 por `llmResults` (STEP 2, STEP 4, STEP 5, STEP 6). El nombre `allResults` desaparece del edge fn — hay `llmResults` (3 items) + `plantnetResult` (1 objeto, no array).

Ejecutar deno type-check (si está disponible localmente):
```bash
npx supabase functions serve identify-plant --no-verify-jwt 2>&1 | head -30 &
SERVE_PID=$!
sleep 3
kill $SERVE_PID 2>/dev/null
# Si no hay errores de tipo en las primeras líneas, sintaxis OK
```

Si no está disponible `supabase functions serve` local, saltar al deploy en Task 4 y verificar allí.
  </action>
  <verify>
    <automated>grep -q "callPlantnetTimed" supabase/functions/identify-plant/index.ts &amp;&amp; grep -q "PLANTNET_TIMEOUT_MS" supabase/functions/identify-plant/index.ts &amp;&amp; grep -q "applyPlantnetOverride" supabase/functions/identify-plant/index.ts &amp;&amp; grep -qE "callPlantnetTimed\(base64Data" supabase/functions/identify-plant/index.ts &amp;&amp; grep -qE "pickWinner\(llmResults\)" supabase/functions/identify-plant/index.ts &amp;&amp; echo "OK: callPlantnetTimed añadido, 4ª promesa integrada, pickWinner intacto"</automated>
  </verify>
  <acceptance_criteria>
    - `callPlantnetTimed` definida con firma que incluye `score: number | null` en `PlantnetEvaluationResult`
    - Promise.allSettled tiene 4 elementos (grep: `Promise.allSettled` seguido de 4 líneas con `callModelTimed` o `callPlantnetTimed`)
    - Split explícito en `llmResults` (3) + `plantnetResult` (1) tras allSettled
    - `pickWinner(llmResults)` — NO `pickWinner(allResults)` con plantnet incluido
    - `PLANTNET_TIMEOUT_MS` definido y usado (< 15_000)
    - Log `[identify] STEP 1` incluye info de plantnet + score
    - Imports actualizados con `applyPlantnetOverride` desde `./consensus.ts`
  </acceptance_criteria>
  <done>Edge function tiene la 4ª llamada + split correcto + logging; el flujo LLM permanece intacto.</done>
</task>

<task type="auto">
  <name>Task 2: Integrar applyPlantnetOverride + persistir 4ª fila + flag divergencia + PostHog event</name>
  <read_first>
    - supabase/functions/identify-plant/index.ts (post-Task-1)
    - .planning/phases/05-plantnet-fourth-provider/05-RESEARCH.md líneas 311-373 (Pattern 3 — insert 4ª fila)
    - .planning/phases/05-plantnet-fourth-provider/05-CONTEXT.md § "Métricas y análisis" (D-12 payload)
    - docs/posthog-events.md (patrón de payload)
  </read_first>
  <files>supabase/functions/identify-plant/index.ts</files>
  <action>
Cablear la lógica de override + persistencia + PostHog. Todo en `index.ts` (mismo archivo que Task 1).

**1) Después de `const winner = pickWinner(llmResults);` (STEP 2, ~línea 416) — antes de la comprobación de `if (!winner)`:**

Aplicar cross-validation. `applyPlantnetOverride` es tolerante a `winner === null` no directamente — solo llamar cuando hay winner:

```typescript
// STEP 2b: Cross-validation PlantNet override (Phase 5 D-01)
// Solo aplicable si hay un LLM winner. Si no hay (todos los LLMs fallaron),
// el early return de más abajo devuelve error al usuario y no llegamos aquí.
type PlantnetOverrideOutcome = {
  winner: ModelResult;
  diverged: boolean;
  matchedLlm: ModelName | null;
};

let overrideOutcome: PlantnetOverrideOutcome = winner
  ? { winner, diverged: false, matchedLlm: null }
  : (null as unknown as PlantnetOverrideOutcome);

if (winner) {
  const plantnetInput: PlantnetOverrideInput | null = plantnetResult
    ? {
        success: plantnetResult.success,
        scientificName: plantnetResult.scientificName,
        score: plantnetResult.score,
      }
    : null;

  const outcome = applyPlantnetOverride(winner, llmResults, plantnetInput);
  overrideOutcome = {
    winner: outcome.winner as ModelResult,   // preserves plantInfo + all LLM fields
    diverged: outcome.diverged,
    matchedLlm: outcome.matchedLlm,
  };

  // Log override effect for observability
  if (overrideOutcome.matchedLlm) {
    console.log(
      `[identify] PlantNet override: LLM winner was ${winner.model} (${winner.scientificName}), ` +
      `PlantNet=${plantnetResult?.scientificName} (score=${plantnetResult?.score?.toFixed(2)}), ` +
      `→ using ${overrideOutcome.matchedLlm} row with PlantNet's scientific name.`
    );
  }
  if (overrideOutcome.diverged) {
    console.log(
      `[identify] PlantNet DIVERGENCE: LLM winner=${winner.scientificName}, ` +
      `PlantNet=${plantnetResult?.scientificName} (score=${plantnetResult?.score?.toFixed(2)}), ` +
      `no LLM matched → keeping LLM winner, flagging plantnet_diverged=true.`
    );
  }
}
```

**IMPORTANTE:** Todas las líneas siguientes que usaban `winner` (STEP 3 storage, STEP 4 insert plant_searches, STEP 5 model_evaluations, STEP 6 response) deben usar `overrideOutcome.winner` para el JSON expuesto al cliente y para el `is_winner` en model_evaluations. `winner` original solo sigue siendo relevante para el LOG anterior y para `consensusGroups` (que se calcula sobre llmResults).

Aplicar reemplazo en STEP 4 (línea ~460):

```typescript
// STEP 4: DB insert — plant_searches
const finalWinner = overrideOutcome.winner;   // ← alias claro
const { data: searchRow, error: searchError } = await supabaseAdmin
  .from("plant_searches")
  .insert({
    name:                   finalWinner.plantInfo!.name,
    description:            finalWinner.plantInfo!.description,
    care:                   finalWinner.plantInfo!.care,
    diagnosis:              finalWinner.plantInfo!.diagnosis,
    watering_interval_days: finalWinner.plantInfo!.watering_interval_days,
    image_url:              imageUrl,
    model:                  finalWinner.model,
    plantnet_diverged:      overrideOutcome.diverged,   // ← nueva columna (D-12)
    ...(user_id ? { user_id } : { user_id: null, anonymous_id: anonymous_id ?? null }),
    ...(typeof lat === "number" && isFinite(lat) && lat >= -90 && lat <= 90 &&
       typeof lng === "number" && isFinite(lng) && lng >= -180 && lng <= 180
      ? { lat, lng } : {}),
  })
  .select("id, created_at")
  .single();
```

**Cuidado:** si la sustitución de `scientificName` de PlantNet debe reflejarse también en el `name` mostrado al usuario ("Nombre común (Scientific)"), hay que reconstruir el string. El LLM alineado ya devuelve algo como `"Potus (Epipremnum aureum)"`; si PlantNet dijo `"epipremnum aureum"` (misma cosa normalizada), no hay cambio visual. Si PlantNet corrigió (`"epipremnum aureum 'golden'"` → `"epipremnum aureum"`), la línea del `name` original del LLM podría no reflejar la corrección.

**Decisión operativa (documentar en comentario):** para esta fase, `finalWinner.plantInfo.name` mantiene el string original del LLM alineado (que ya contiene el científico dentro de paréntesis, tras el paso de LLM). No re-escribir el `name` — la sustitución de PlantNet es interna (el `scientific_name` que se persiste en model_evaluations refleja la corrección; el string visible al usuario mantiene el LLM). Esta es la interpretación conservadora de D-02 ("100% invisible al usuario") — el nombre común español + formato lo aporta el LLM. En futuras iteraciones se puede reconstruir el `name` con `${extractCommon(llm.name)} (${plantnet.scientific})`.

**2) STEP 5 — insertar 4ª fila plantnet en model_evaluations (~línea 484-511):**

Reemplaza el bloque completo (map + push + insert) por:

```typescript
// STEP 5: Analytics — insert model_evaluations (fire-and-forget)
const consensusGroups = computeConsensus(llmResults);
if (searchRow && (llmResults.length > 0 || plantnetResult)) {
  const llmRows = llmResults.map((r) => {
    const consensus = r.success ? (consensusGroups.get(r.model) ?? null) : null;
    return {
      plant_search_id:         searchRow.id,
      model:                   r.model,
      raw_name:                r.rawName,
      scientific_name:         r.scientificName,
      description:             r.plantInfo?.description ?? null,
      care:                    r.plantInfo?.care ?? null,
      diagnosis:               r.plantInfo?.diagnosis ?? null,
      response_ms:             r.responseMs,
      success:                 r.success,
      error_message:           r.errorMessage,
      is_winner:               r.model === finalWinner.model,
      consensus_group:         consensus?.verdict ?? null,
      consensus_match_level:   consensus?.matchLevel ?? null,
      raw_response:            null,   // LLMs no pueblan raw_response
    };
  });

  // 4ª fila: plantnet (siempre — incluso si falló, per D-09 registro completo)
  const plantnetRow = plantnetResult ? {
    plant_search_id:         searchRow.id,
    model:                   "plantnet" as const,
    raw_name:                plantnetResult.rawName,
    scientific_name:         plantnetResult.scientificName,
    description:             null,
    care:                    null,
    diagnosis:               null,
    response_ms:             plantnetResult.responseMs,
    success:                 plantnetResult.success,
    error_message:           plantnetResult.errorMessage,
    is_winner:               false,   // D-01: PlantNet nunca es row "is_winner"; el LLM alineado siempre lo es
    consensus_group:         null,
    consensus_match_level:   null,
    raw_response:            plantnetResult.rawResponse,   // JSON completo (D-03)
  } : null;

  const evaluationRows = plantnetRow ? [...llmRows, plantnetRow] : llmRows;

  // Fire-and-forget
  supabaseAdmin
    .from("model_evaluations")
    .insert(evaluationRows)
    .then(({ error: evalError }) => {
      if (evalError) console.error("model_evaluations insert error:", evalError);
    });
}
```

**3) Dispatch PostHog event `plantnet_divergence` (fire-and-forget, después del insert de STEP 5):**

```typescript
// STEP 5b: PostHog event on divergence (D-12)
if (overrideOutcome.diverged && plantnetResult && winner) {
  const POSTHOG_API_KEY = Deno.env.get("POSTHOG_PROJECT_API_KEY");
  if (POSTHOG_API_KEY) {
    const distinctId = user_id ?? anonymous_id ?? "edge-fn-anon";
    const payload = {
      api_key: POSTHOG_API_KEY,
      event: "plantnet_divergence",
      distinct_id: distinctId,
      properties: {
        plantnet_scientific: plantnetResult.scientificName,
        plantnet_score:      plantnetResult.score,
        llm_winner_scientific: winner.scientificName,
        llm_winner_model:      winner.model,
        plant_search_id:       searchRow?.id ?? null,
        $lib: "edge-function",
      },
    };
    // Fire-and-forget, timeout corto — un fallo en analytics NO debe afectar al usuario
    fetch("https://eu.i.posthog.com/capture/", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(payload),
      signal: AbortSignal.timeout(2_000),
    }).catch((e) => console.error("posthog capture error:", e));
  } else {
    console.warn("[identify] POSTHOG_PROJECT_API_KEY not set — divergence event skipped");
  }
}
```

**4) STEP 6 — response al cliente (líneas 513-541), usar `finalWinner`:**

```typescript
// STEP 6: Return JSON — SOLO exponer LLMs en `models` (D-02: PlantNet invisible)
const modelsSummary = llmResults.map((r) => {
  const consensus = r.success ? (consensusGroups.get(r.model) ?? null) : null;
  return {
    model: r.model,
    success: r.success,
    scientific_name: r.scientificName,
    response_ms: r.responseMs,
    is_winner: r.model === finalWinner.model,
    consensus_verdict: consensus?.verdict ?? null,
  };
});
const consensusReached =
  consensusGroups.get(finalWinner.model)?.verdict === "correct";

return new Response(
  JSON.stringify({
    name:                   finalWinner.plantInfo!.name,
    description:            finalWinner.plantInfo!.description,
    care:                   finalWinner.plantInfo!.care,
    diagnosis:              finalWinner.plantInfo!.diagnosis,
    watering_interval_days: finalWinner.plantInfo!.watering_interval_days,
    model:                  finalWinner.model,
    plant_search_id:        searchRow?.id ?? null,
    created_at:             searchRow?.created_at ?? null,
    models:                 modelsSummary,   // 3 items — plantnet NO expuesto
    consensus_reached:      consensusReached,
  }),
  { headers: { ...corsHeaders, "Content-Type": "application/json" } }
);
```

**5) Verificar sintaxis y tests cliente (mock fetch no debe romperse):**

```bash
npm run test -- --run
```

Los tests del cliente mockean `globalThis.fetch` con un JSON de 3 items en `models` — no rompen porque el response sigue exponiendo solo 3 LLMs.
  </action>
  <verify>
    <automated>grep -q "applyPlantnetOverride(winner" supabase/functions/identify-plant/index.ts &amp;&amp; grep -q "overrideOutcome.diverged" supabase/functions/identify-plant/index.ts &amp;&amp; grep -q "plantnet_diverged:" supabase/functions/identify-plant/index.ts &amp;&amp; grep -q "\"plantnet_divergence\"" supabase/functions/identify-plant/index.ts &amp;&amp; grep -q "raw_response:.*plantnetResult.rawResponse" supabase/functions/identify-plant/index.ts &amp;&amp; grep -qE "is_winner:\s*false" supabase/functions/identify-plant/index.ts &amp;&amp; grep -q "modelsSummary = llmResults" supabase/functions/identify-plant/index.ts &amp;&amp; npm run test -- --run 2>&amp;1 | tail -10 | grep -qE "Test Files.*passed|✓" &amp;&amp; echo "OK: override + insert 4ª fila + PostHog + response invisible + tests cliente pasan"</automated>
  </verify>
  <acceptance_criteria>
    - `applyPlantnetOverride(winner, llmResults, plantnetInput)` invocado con el input construido desde `plantnetResult`
    - `overrideOutcome.diverged` usado en el INSERT de `plant_searches.plantnet_diverged`
    - 4ª fila plantnet en `evaluationRows` con `raw_response = plantnetResult.rawResponse` y `is_winner: false`
    - `modelsSummary` construido desde `llmResults` (NO desde `[...llmResults, plantnetRow]`)
    - Dispatch a `https://eu.i.posthog.com/capture/` con `event: "plantnet_divergence"` cuando `diverged=true`
    - `POSTHOG_PROJECT_API_KEY` leído de Deno.env con warn si no está (no throw)
    - Los tests cliente `npm run test -- --run` siguen verdes (fetch mock produce el mismo shape de response)
    - `winner` original de pickWinner NO se pasa a modelsSummary (usa `finalWinner`)
  </acceptance_criteria>
  <done>Cross-validation cableada, persistencia completa, PostHog event dispatched en divergencia, cliente sigue viendo respuesta idéntica en shape.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] CPO registra POSTHOG_PROJECT_API_KEY (si no está ya) como Function Secret</name>
  <files>N/A — acción externa en Supabase Dashboard (no toca archivos del repo)</files>
  <action>
El evento `plantnet_divergence` se dispara server-side desde el edge function. Necesita una PROJECT API key de PostHog (NO la personal API key) como Function Secret. Si ya está registrada de fases anteriores, este paso es un no-op y la CPO puede escribir "ya está" para continuar.

**Pasos que debe ejecutar la CPO:**

1. Abre https://supabase.com/dashboard/project/sdxrgxfmurshrnrnvhda/settings/functions
2. Ve a **Edge Functions → Secrets**
3. Comprueba si `POSTHOG_PROJECT_API_KEY` ya está en la lista:
   - **Si sí:** listo, escribe "ya está" y continúa.
   - **Si no:**
     a. Ve a https://eu.posthog.com/project/settings
     b. En "Project ID / Project API Key" copia la **Project API Key** (la que empieza por `phc_...`, NO la personal `phx_...`)
     c. Vuelve al Dashboard Supabase — añadir secret `POSTHOG_PROJECT_API_KEY` con ese valor
4. Confirma que aparece en la lista de secrets

**Alternativa:** si prefieres NO trackear divergencias en PostHog (solo DB flag), puedes saltar este paso — el edge function loguea un warn y continúa. La query SQL de divergencia (Plan 04) sigue funcionando sin PostHog.
  </action>
  <what-built>
Function Secret `POSTHOG_PROJECT_API_KEY` (opcional) que el edge function usa para hacer POST a eu.i.posthog.com/capture/ cuando detecta una divergencia. Si no se registra, las divergencias siguen guardándose en `plant_searches.plantnet_diverged` (visible vía query 13 de docs/model-evaluation-queries.sql).
  </what-built>
  <how-to-verify>
Ir a https://supabase.com/dashboard/project/sdxrgxfmurshrnrnvhda/functions → Secrets: comprobar que `POSTHOG_PROJECT_API_KEY` aparece (o que la CPO ha respondido "skip").
  </how-to-verify>
  <verify>Confirmación manual de la CPO: secret listado o decisión explícita de saltar.</verify>
  <resume-signal>"ya está" / "registrada" / "skip" (si eliges no usar PostHog para este evento).</resume-signal>
  <done>POSTHOG_PROJECT_API_KEY registrada en Supabase Function Secrets, o la CPO ha optado explícitamente por skip.</done>
</task>

<task type="auto">
  <name>Task 4: Deploy edge function a producción + verificar deploy limpio</name>
  <read_first>
    - supabase/functions/identify-plant/index.ts (versión final)
  </read_first>
  <files>supabase/functions/identify-plant/index.ts</files>
  <action>
Desplegar el edge function a producción. La migración de Plan 01 debe estar aplicada (verificar STATE.md o el summary de Plan 01).

**Paso 1 — verificar precondición (migración Plan 01 aplicada):**

```bash
grep -q "raw_response" src/integrations/supabase/types.ts && grep -q "plantnet_diverged" src/integrations/supabase/types.ts || {
  echo "FALLO: migración Plan 01 no aplicada. Aborto."
  exit 1
}
```

**Paso 2 — deploy:**

```bash
npx supabase functions deploy identify-plant --project-ref sdxrgxfmurshrnrnvhda
```

Salida esperada: `Deployed Function identify-plant on project sdxrgxfmurshrnrnvhda`

Si aparece prompt interactivo (token, confirmación) → convertir en checkpoint:human-action.

**Paso 3 — smoke test con curl (sin foto real — solo verifica que responde 400 por payload inválido, NO 500 por syntax error):**

```bash
curl -s -o /dev/null -w "%{http_code}\n" \
  -X POST https://sdxrgxfmurshrnrnvhda.supabase.co/functions/v1/identify-plant \
  -H "Content-Type: application/json" \
  -H "apikey: $(grep VITE_SUPABASE_ANON_KEY .env | cut -d= -f2)" \
  -d '{}'
```

Esperado: `500` con body `{"error":"Invalid image format"}` (el handler valida el shape del image). NO debe ser 502/504/timeout — eso indicaría syntax error o crash al cargar el módulo Deno.

**Paso 4 — verificar logs del deploy (ver que el módulo carga sin errores de import):**

```bash
npx supabase functions logs identify-plant --project-ref sdxrgxfmurshrnrnvhda | tail -30
```

Buscar líneas `booted` sin `error` cercano. Si hay error de import (típico: falla al resolver `./consensus.ts` porque exportó mal `applyPlantnetOverride`), volver a Task 1/Plan 02 y arreglar.
  </action>
  <verify>
    <automated>curl -s -o /tmp/plantnet-smoke.json -w "%{http_code}\n" -X POST https://sdxrgxfmurshrnrnvhda.supabase.co/functions/v1/identify-plant -H "Content-Type: application/json" -H "apikey: $(grep VITE_SUPABASE_ANON_KEY .env 2>/dev/null | cut -d= -f2)" -d '{}' | grep -qE "^(400|500)$" &amp;&amp; grep -q "Invalid image format\|error" /tmp/plantnet-smoke.json &amp;&amp; echo "OK: edge function desplegada y responde"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase functions deploy` reporta éxito
    - Smoke test curl devuelve 400/500 con `{"error":"Invalid image format"}` (NO 502/504/timeout)
    - Logs no muestran errores de import/carga del módulo
    - El deploy nuevo aparece con timestamp reciente en Supabase Dashboard → Functions → identify-plant → Deployments
  </acceptance_criteria>
  <done>Edge function con PlantNet integrado desplegado en producción, responde a requests sin crashear.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: [CHECKPOINT] Smoke test manual — 3-5 identificaciones reales</name>
  <files>N/A — verificación en producción vía app + Supabase SQL Editor + DevTools + PostHog</files>
  <action>
Ejecutar 3-5 identificaciones reales en producción y validar con las queries + verificación DevTools descritas en `<how-to-verify>` que la integración PlantNet funciona: 4 filas por búsqueda, raw_response guardado, UX cliente sin cambios, divergencias (si ocurren) marcadas y con evento PostHog.
  </action>
  <verify>La CPO confirma manualmente todos los criterios listados en la sección "Criterios de sign-off" al final del bloque how-to-verify.</verify>
  <done>3-5 identificaciones exitosas con 4 filas por búsqueda, raw_response poblado, response cliente idéntico al pre-Phase-5, sin errores en logs edge fn.</done>
  <what-built>
El edge function con PlantNet como 4º votante está desplegado. Esta verificación humana confirma que:
1. UX visible al usuario NO cambió (D-02)
2. 4 filas se insertan en `model_evaluations` por identificación
3. `raw_response` contiene JSON de PlantNet
4. Al menos una divergencia (probable con plantas ambiguas) marca `plant_searches.plantnet_diverged=true` y dispara evento PostHog

**IMPORTANTE:** para forzar una divergencia, prueba con una foto donde LLMs y PlantNet suelan diferir (helechos, cactus jóvenes, suculentas parecidas). Si no ocurre en las 5 pruebas, no es un fallo — la divergencia depende del ecosistema real.
  </what-built>
  <how-to-verify>
**Paso 1 — 3-5 identificaciones desde la app en producción (mi-plantita-feliz.vercel.app o Android):**
- 2 plantas conocidas y "fáciles" (potus, sansevieria) → esperado: LLMs y PlantNet coinciden, override transparente
- 2 plantas ambiguas o poco comunes → esperado: posibles divergencias
- 1 foto mala (borrosa, sin planta) → esperado: LLMs fallan, PlantNet falla o devuelve nada → error al usuario igual que hoy

**Paso 2 — Verificar en Supabase SQL Editor (https://supabase.com/dashboard/project/sdxrgxfmurshrnrnvhda/sql/new):**

```sql
-- ¿4 filas por búsqueda?
SELECT
  ps.id,
  ps.name AS displayed_name,
  ps.plantnet_diverged,
  COUNT(me.id) AS filas_evaluacion,
  BOOL_OR(me.model = 'plantnet') AS tiene_fila_plantnet
FROM plant_searches ps
LEFT JOIN model_evaluations me ON me.plant_search_id = ps.id
WHERE ps.created_at >= NOW() - INTERVAL '30 minutes'
GROUP BY ps.id, ps.name, ps.plantnet_diverged
ORDER BY ps.created_at DESC;
```

Esperado: `filas_evaluacion = 4` y `tiene_fila_plantnet = true` para cada búsqueda (o 3 con `tiene_fila_plantnet = false` si PlantNet falló silenciosamente).

**Paso 3 — Verificar que raw_response se guarda:**

```sql
SELECT
  ps.name,
  me.success,
  me.scientific_name,
  me.raw_response IS NOT NULL AS tiene_json,
  jsonb_typeof(me.raw_response) AS tipo_json,
  me.raw_response->'results'->0->'score' AS score_top1,
  me.raw_response->'results'->0->'species'->>'scientificNameWithoutAuthor' AS sci_top1
FROM model_evaluations me
JOIN plant_searches ps ON ps.id = me.plant_search_id
WHERE me.model = 'plantnet'
  AND me.created_at >= NOW() - INTERVAL '30 minutes'
ORDER BY me.created_at DESC;
```

Esperado: `tiene_json = true` y `tipo_json = 'object'` para las plantnet rows exitosas. `score_top1` numérico entre 0 y 1.

**Paso 4 — En el navegador, DevTools → Network → request a `identify-plant`:**

Buscar en la response JSON:
- `models` array con 3 items (claude, gemini, gpt4o) — SIN plantnet ✓
- Cualquier campo `plantnet_*` visible → FALLO (D-02 violado, reportar)
- El shape idéntico al de antes de Phase 5

**Paso 5 — PostHog (si POSTHOG_PROJECT_API_KEY registrado):**

Ir a https://eu.posthog.com/events → filtrar por `event = 'plantnet_divergence'` en los últimos 30 minutos. Si hubo divergencias, deben aparecer eventos con payload `{plantnet_scientific, plantnet_score, llm_winner_scientific, llm_winner_model, plant_search_id}`.

**Criterios de sign-off:**
- ✅ 3-5 identificaciones exitosas con UX idéntica a antes
- ✅ 4 filas en model_evaluations por búsqueda (o 3 si PlantNet falló silenciosamente en alguna)
- ✅ raw_response con JSON válido para las plantnet rows exitosas
- ✅ Ninguna referencia a `plantnet_*` en el response JSON al cliente
- ✅ Al menos observar el flujo end-to-end sin errores en logs del edge fn
  </how-to-verify>
  <resume-signal>"approved" si todo OK. Si hay problemas, describe qué falla (ej: "no se guarda raw_response" / "response al cliente incluye plantnet_score" / "solo 3 filas en model_evaluations").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cliente → Edge Function | Base64 de imagen + user_id/anonymous_id + lat/lng — ya validados en el código existente |
| Edge Function → PlantNet API | Envío de foto a 3rd party (T&C revisadas en 05-RESEARCH.md); API key va en query string |
| Edge Function → PostHog EU | Dispatch server-side de evento `plantnet_divergence` con metadata de identificación |
| Edge Function → Supabase DB (service_role) | INSERT en model_evaluations + plant_searches |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Information Disclosure | PLANTNET_API_KEY en query string de la URL | mitigate | HTTPS end-to-end (TLS oculta la URL a intermediarios). Convención del propio API de PlantNet (v2/identify usa api-key como query param). Alternativa header no soportada por su endpoint. Riesgo residual: logs internos de PlantNet la ven, pero es *nuestra* key limitada a 500/día — impacto = quota abuse. |
| T-05-03-02 | Information Disclosure | Foto compartida con PlantNet (3rd party) | accept | Revisado en 05-RESEARCH.md § External APIs / T&C. La foto ya se envía a Anthropic/Google/OpenAI hoy — añadir PlantNet no cambia la clase de exposición. |
| T-05-03-03 | Denial of Service | Quota PlantNet 500/día exhaustion | mitigate | Volumen actual ~15-30/día → 3-6% del techo. D-09 fallo silencioso: si se agota → 3 LLMs siguen (comportamiento indistinguible del baseline). Monitorizable vía query de tasa error_message LIKE 'RATE_LIMIT:plantnet%' en model-evaluation-queries.sql (Plan 04). |
| T-05-03-04 | Tampering | Cross-validation logic incoherencia nombre-cuidados | mitigate | applyPlantnetOverride solo sustituye scientificName cuando LLM matchea al mismo epíteto específico (exact/normalized). Rechaza genus (D-11) — Ficus lyrata + PlantNet=Ficus benjamina NO override, marca diverged. Tests exhaustivos en Plan 02. |
| T-05-03-05 | Information Disclosure | PostHog event con PII (user_id como distinct_id) | mitigate | user_id es el UUID de Supabase Auth, ya es el distinct_id habitual en PostHog (mismo patrón que `plant_identified`). No incluimos email/PII. plant_search_id incluido para debug pero es un UUID interno. |
| T-05-03-06 | Denial of Service | PostHog fetch cuelga y demora el response al usuario | mitigate | fetch a PostHog es fire-and-forget con AbortSignal.timeout(2_000). NO await — no bloquea. `.catch` explícito para tragarse errores. |
| T-05-03-07 | Tampering | Edge fn expone plantnet en `models` por error → cliente lo pinta | mitigate | Test cliente `use-plant-identifier.test.ts` mockea response con 3 items. Verificación manual en Task 5 revisa el response JSON en DevTools. Si algún día se añade plantnet al modelsSummary, se filtra explícitamente con `.filter(r => r.model !== 'plantnet')` (documentado en comentario). |
| T-05-03-08 | Tampering | INSERT plant_searches falla con plantnet_diverged (columna inexistente en producción) | mitigate | Plan 01 aplica la migración ANTES de este plan (waves: 1 → 2 → 3). Task 4 tiene un precondición grep en types.ts que aborta si la migración no está. |
| T-05-03-09 | Elevation of Privilege | Edge fn escribe en plant_searches sin RLS | accept | Uso service_role (SUPABASE_SERVICE_ROLE_KEY) — bypass RLS por diseño. Mismo patrón que hoy. La responsabilidad de vincular al user correcto está en el propio código (user_id / anonymous_id). |
| T-05-03-10 | Repudiation | No hay trazabilidad de cuándo aplicó cross-validation | mitigate | Console.log explícito en cada override + divergencia (visibles en Supabase Functions logs). model_evaluations guarda toda la evidencia (raw_response de PlantNet, is_winner del LLM). PostHog event ofrece dashboard time-series. |
</threat_model>

<verification>
- callPlantnetTimed + PLANTNET_TIMEOUT_MS presentes en index.ts (Task 1)
- Promise.allSettled con 4 promesas (Task 1)
- pickWinner recibe solo llmResults (Task 1)
- applyPlantnetOverride invocado + overrideOutcome usado en STEP 4/5/6 (Task 2)
- 4ª fila plantnet insertada con raw_response (Task 2)
- Dispatch PostHog en divergencia (Task 2)
- Response al cliente sigue con 3 items en `models` (Task 2)
- `npm run test -- --run` verde (Task 2)
- Deploy limpio, smoke test curl responde (Task 4)
- CPO confirma con 3-5 identificaciones reales que UX invisible + 4 filas + raw_response OK (Task 5)
</verification>

<success_criteria>
1. **PLANT-01:** PlantNet corre en paralelo con 3 LLMs, JSON completo se persiste en `model_evaluations.raw_response` para ≥95% de identificaciones (verificable en Task 5 SQL query)
2. **PLANT-03:** Latencia P95 no empeora — D-13 confirma que PlantNet p95=695ms mientras cualquier LLM más rápido tarda p50=917ms; PlantNet no gate el consenso
3. **D-01/D-10/D-11:** Cross-validation aplica correctamente (tests unitarios en Plan 02; smoke test en Task 5)
4. **D-02:** El response JSON al cliente NO expone plantnet en ningún campo (verificable en DevTools en Task 5)
5. **D-09:** Fallo silencioso — si PlantNet cae, la respuesta al usuario es idéntica a hoy
6. **D-12:** Divergencias marcan `plant_searches.plantnet_diverged=true` + disparan evento PostHog `plantnet_divergence`
7. Ningún test existente se rompe
</success_criteria>

<output>
Al terminar, crea `.planning/phases/05-plantnet-fourth-provider/05-03-SUMMARY.md` con:
- Resumen de cambios en index.ts (líneas antes/después)
- Confirmación del deploy en producción (URL Dashboard + timestamp)
- Resultados del smoke test de Task 5 (número de búsquedas, cuántas con 4 filas, cuántas divergencias)
- Cualquier ajuste de umbral / edge case descubierto en pruebas reales
</output>
