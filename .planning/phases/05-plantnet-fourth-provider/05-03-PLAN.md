---
phase: 05-plantnet-fourth-provider
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - supabase/functions/identify-plant/index.ts
autonomous: true
requirements: [PLANT-01, PLANT-03]
tags: [edge-function, plantnet, observer-silent, promise-allsettled]

must_haves:
  truths:
    - "PlantNet se llama en paralelo con los 3 LLMs vía Promise.allSettled (4 promesas)"
    - "PlantNet corre en modo observador silencioso — NO participa en pickWinner/computeConsensus (D-01)"
    - "PlantNet NO se expone en el JSON response al cliente — modelsSummary sigue con 3 items LLM (D-02)"
    - "Si PlantNet falla (timeout, 4xx/5xx, sin key), la fila plantnet guarda NULL y el usuario recibe su identificación normal (D-09)"
    - "consensus.ts NO se modifica — ModelName sigue siendo union de 3"
    - "La 4ª fila de model_evaluations se persiste con model='plantnet', raw_response=JSON completo, is_winner=false, consensus_group=NULL"
    - "PlantNet timeout es 10s (< 15s LLM) para no ser cuello de botella (D-09 + latencia P95)"
  artifacts:
    - path: "supabase/functions/identify-plant/index.ts"
      provides: "Función callPlantnetTimed + 4ª promesa en Promise.allSettled + 4ª fila en evaluationRows insert"
      contains: "callPlantnetTimed"
    - path: "supabase/functions/identify-plant/consensus.ts"
      provides: "SIN CAMBIOS — verificable por git diff vacío"
      contains: "export type ModelName = \"claude\" | \"gemini\" | \"gpt4o\""
  key_links:
    - from: "identify-plant/index.ts:Promise.allSettled"
      to: "PlantNet API v2 (https://my-api.plantnet.org/v2/identify/all)"
      via: "callPlantnetTimed multipart POST con FormData"
      pattern: "my-api\\.plantnet\\.org"
    - from: "identify-plant/index.ts:evaluationRows insert"
      to: "model_evaluations (4ª fila)"
      via: "supabaseAdmin.from('model_evaluations').insert(evaluationRows)"
      pattern: "model:\\s*['\"]plantnet['\"]"
    - from: "PlantNet failure (timeout/4xx/5xx)"
      to: "Fila con raw_response=NULL, success=false, error_message=TIMEOUT/RATE_LIMIT/API_ERROR"
      via: "Silent failure per D-09"
      pattern: "(TIMEOUT|RATE_LIMIT|API_ERROR):plantnet"
---

<objective>
Extender el edge function `supabase/functions/identify-plant/index.ts` para llamar a PlantNet API v2 en paralelo con los 3 LLMs existentes (Claude, Gemini, GPT-4o), añadiendo una 4ª promesa a `Promise.allSettled`, split post-settle en `llmResults` (para consensus/pickWinner intactos) + `plantnetResult` (para persistencia solo), y una 4ª fila en el INSERT a `model_evaluations` con `raw_response` = JSON completo de PlantNet (D-03).

Puntos críticos NO negociables (per CONTEXT.md):
- **D-01 (observador silencioso):** PlantNet NO participa en `pickWinner`/`computeConsensus`. `consensus.ts` NO se modifica. `is_winner` siempre `false`, `consensus_group` siempre `NULL`.
- **D-02 (invisible al cliente):** `modelsSummary` del JSON response al cliente se filtra para excluir la fila plantnet — sigue siendo 3 items LLM.
- **D-09 (failure silencioso):** Timeout 10s (< 15s LLMs para no ser cuello de botella), errores 4xx/5xx / sin key devuelven `{success: false, rawResponse: NULL, errorMessage: taxonomy}` sin bloquear al usuario.

Purpose: Requirement PLANT-01 (persistir JSON completo de PlantNet en `model_evaluations` para ≥95% de identificaciones nuevas) + PLANT-03 (latencia P95 no empeora — 10s timeout PlantNet < 15s LLMs, y `Promise.allSettled` no bloquea al ser fire-and-forget el insert).

Output:
- `supabase/functions/identify-plant/index.ts` extendido con `PLANTNET_TIMEOUT_MS`, `PlantnetEvaluationResult` interface, `callPlantnetTimed` función completa, 4ª promesa en Promise.allSettled, split llmResults+plantnetResult, 4ª fila en evaluationRows insert, `modelsSummary` sigue con 3 items LLM (filter explícito).
- `consensus.ts` sin tocar (`git diff --exit-code supabase/functions/identify-plant/consensus.ts` debe devolver 0).
- Edge function deployed a producción (`supabase functions deploy identify-plant`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-plantnet-fourth-provider/05-CONTEXT.md
@.planning/phases/05-plantnet-fourth-provider/05-RESEARCH.md
@.planning/phases/05-plantnet-fourth-provider/05-PATTERNS.md
@supabase/functions/identify-plant/index.ts
@supabase/functions/identify-plant/consensus.ts

<interfaces>
<!-- Tipos y funciones existentes del edge function que este plan consume o extiende.  -->
<!-- Extraído de supabase/functions/identify-plant/index.ts y consensus.ts.  -->

Desde `./consensus.ts` (líneas 1-6 index.ts, IMPORTAR — NO modificar):
```typescript
export type ModelName = "claude" | "gemini" | "gpt4o";
export function extractScientificName(text: string): string | null;
export function computeConsensus(...): {...};
```

Desde `index.ts` (líneas 95-101, ya existentes):
```typescript
const LLM_TIMEOUT_MS = 15_000;
function isAbortError(e: unknown): boolean {
  return e instanceof Error && (e.name === "AbortError" || e.name === "TimeoutError");
}
```

Patrón existente `callClaude` (líneas 103-152, ES ANALOG para copiar shape):
```typescript
async function callClaude(base64Data: string, mediaType: string): Promise<string> {
  const ANTHROPIC_API_KEY = Deno.env.get("ANTHROPIC_API_KEY");
  if (!ANTHROPIC_API_KEY) throw new Error("ANTHROPIC_API_KEY is not configured");
  // ... fetch with AbortSignal.timeout(LLM_TIMEOUT_MS)
  // ... error mapping TIMEOUT / RATE_LIMIT / API_ERROR
}
```

Patrón existente `callModelTimed` (líneas 307-340, NO reutilizable directamente — asume `caller: () => Promise<string>` con parseo markdown LLM).

Patrón existente `Promise.allSettled` (líneas 389-397):
```typescript
const settled = await Promise.allSettled([
  callModelTimed("claude", () => callClaude(base64Data, mediaType)),
  callModelTimed("gemini", () => callGemini(base64Data, mediaType)),
  callModelTimed("gpt4o",  () => callOpenAI(base64Data, mediaType)),
]);
```

Patrón existente insert (líneas 484-511, fire-and-forget):
```typescript
supabaseAdmin.from("model_evaluations").insert(evaluationRows).then(...)
```

Nueva interface a añadir en `index.ts` (LOCAL — NO en consensus.ts):
```typescript
interface PlantnetEvaluationResult {
  success: boolean;
  rawName: string | null;
  scientificName: string | null;
  rawResponse: unknown | null;
  responseMs: number;
  errorMessage: string | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Añadir constants, interface, y función callPlantnetTimed en identify-plant/index.ts</name>
  <read_first>
    - supabase/functions/identify-plant/index.ts (COMPLETO — es un solo archivo, ~550 líneas; leer para conocer contexto de líneas 95-101, 103-152, 307-340, 389-397, 484-511)
    - supabase/functions/identify-plant/consensus.ts (para confirmar que ModelName es union de 3 y NO tocar)
    - .planning/phases/05-plantnet-fourth-provider/05-RESEARCH.md líneas 219-308 (código VERBATIM a copiar de callPlantnetTimed)
    - .planning/phases/05-plantnet-fourth-provider/05-PATTERNS.md § 1.A y 1.B (imports + provider caller pattern)
    - .planning/phases/05-plantnet-fourth-provider/05-RESEARCH.md § Common Pitfalls #4 (líneas 525-533 — NO añadir "plantnet" a ModelName en consensus.ts)
    - .planning/phases/05-plantnet-fourth-provider/05-RESEARCH.md § Common Pitfalls #5 (líneas 535-545 — extraer scientificNameWithoutAuthor, no scientificName)
    - .planning/phases/05-plantnet-fourth-provider/05-RESEARCH.md § Common Pitfalls #6 (líneas 547-555 — NO include-related-images)
  </read_first>
  <files>supabase/functions/identify-plant/index.ts</files>
  <action>
    Editar `supabase/functions/identify-plant/index.ts` para añadir 3 bloques nuevos (SIN modificar código existente en este task — la orquestación se hace en Task 2):

    **BLOQUE A — Constante de timeout** (añadir INMEDIATAMENTE después de la línea `const LLM_TIMEOUT_MS = 15_000;`, aprox. línea 95):

    ```typescript
    const PLANTNET_TIMEOUT_MS = 10_000;  // < 15s LLM para no ser cuello de botella (D-09)
    ```

    **BLOQUE B — Interface local** (añadir cerca de las otras type declarations, antes de la función `callClaude`, aprox. línea 100-103):

    ```typescript
    interface PlantnetEvaluationResult {
      success: boolean;
      rawName: string | null;         // top-1 scientificNameWithoutAuthor
      scientificName: string | null;  // lowercased para join con extractScientificName output
      rawResponse: unknown | null;    // JSON completo (jsonb)
      responseMs: number;
      errorMessage: string | null;
    }
    ```

    **BLOQUE C — Función `callPlantnetTimed`** (añadir DESPUÉS de la última función provider — `callOpenAI` o similar, alrededor de línea 340). COPIAR VERBATIM de RESEARCH.md líneas 238-308:

    ```typescript
    async function callPlantnetTimed(
      base64Data: string,
      mediaType: string,
    ): Promise<PlantnetEvaluationResult> {
      const start = Date.now();
      const PLANTNET_API_KEY = Deno.env.get("PLANTNET_API_KEY");
      if (!PLANTNET_API_KEY) {
        return {
          success: false, rawName: null, scientificName: null, rawResponse: null,
          responseMs: Date.now() - start,
          errorMessage: "PLANTNET_API_KEY not configured",
        };
      }

      try {
        // Multipart POST — coherente con bootstrap-golden-set.ts, evita depender de
        // que la imagen esté ya en storage público al momento de invocar.
        // Alternativa GET-con-URL requeriría STEP 3 (upload) ANTES de STEP 1,
        // reordenando el flow.
        const binaryStr = atob(base64Data);
        const bytes = new Uint8Array(binaryStr.length);
        for (let i = 0; i < binaryStr.length; i++) bytes[i] = binaryStr.charCodeAt(i);

        const form = new FormData();
        form.append("images", new Blob([bytes], { type: mediaType }), "plant.jpg");
        form.append("organs", "auto");

        const url = `https://my-api.plantnet.org/v2/identify/all?api-key=${PLANTNET_API_KEY}&nb-results=5&lang=es`;
        const res = await fetch(url, {
          method: "POST",
          body: form,
          signal: AbortSignal.timeout(PLANTNET_TIMEOUT_MS),
        });

        if (!res.ok) {
          const text = await res.text();
          console.error("plantnet API error:", res.status, text.substring(0, 200));
          return {
            success: false, rawName: null, scientificName: null, rawResponse: null,
            responseMs: Date.now() - start,
            errorMessage: res.status === 429
              ? "RATE_LIMIT:plantnet:429"
              : `API_ERROR:plantnet:${res.status}`,
          };
        }

        const json = await res.json();
        const top = json?.results?.[0];
        const rawName = top?.species?.scientificNameWithoutAuthor ?? null;
        // scientificName lowercased para consistencia con extractScientificName() de LLMs
        // que hace .toLowerCase() (consensus.ts líneas 45-49)
        const scientificName = typeof rawName === "string" ? rawName.toLowerCase() : null;

        return {
          success: rawName !== null,
          rawName,
          scientificName,
          rawResponse: json,
          responseMs: Date.now() - start,
          errorMessage: rawName === null ? "NO_RESULTS" : null,
        };
      } catch (e) {
        return {
          success: false, rawName: null, scientificName: null, rawResponse: null,
          responseMs: Date.now() - start,
          errorMessage: isAbortError(e)
            ? `TIMEOUT:plantnet:${PLANTNET_TIMEOUT_MS}ms`
            : (e instanceof Error ? e.message : "Unknown error"),
        };
      }
    }
    ```

    **NO tocar `consensus.ts` en este task** — verificable con `git diff --exit-code supabase/functions/identify-plant/consensus.ts` al final.

    **NO exponer la URL completa en console.log** — el patrón usa `console.error("plantnet API error:", res.status, text.substring(0, 200))` para evitar leak de la API key en logs (STRIDE: Information Disclosure).

    **Ejecutar `npm run build`** al finalizar para verificar type-check (el `Deno.env` funciona igual bajo Vite build; TS reconoce Deno namespace en edge functions vía JSR types).
  </action>
  <verify>
    <automated>grep -c "callPlantnetTimed" supabase/functions/identify-plant/index.ts | awk '$1 >= 1' && grep -q "PLANTNET_TIMEOUT_MS = 10_000" supabase/functions/identify-plant/index.ts && grep -q "interface PlantnetEvaluationResult" supabase/functions/identify-plant/index.ts && grep -q "scientificNameWithoutAuthor" supabase/functions/identify-plant/index.ts && grep -q "my-api.plantnet.org/v2/identify/all" supabase/functions/identify-plant/index.ts && ! grep -q "include-related-images" supabase/functions/identify-plant/index.ts && git diff --exit-code supabase/functions/identify-plant/consensus.ts</automated>
  </verify>
  <acceptance_criteria>
    - `index.ts` contiene `const PLANTNET_TIMEOUT_MS = 10_000` (con comentario D-09)
    - `index.ts` contiene `interface PlantnetEvaluationResult` con los 6 campos (success, rawName, scientificName, rawResponse, responseMs, errorMessage)
    - `index.ts` contiene `async function callPlantnetTimed(base64Data, mediaType): Promise<PlantnetEvaluationResult>` — copiada verbatim de RESEARCH.md L238-308
    - `index.ts` usa `scientificNameWithoutAuthor` (NO `scientificName` — evita autoría, Pitfall #5)
    - `index.ts` usa `nb-results=5` sin `include-related-images` (Pitfall #6)
    - `index.ts` usa `AbortSignal.timeout(PLANTNET_TIMEOUT_MS)` (patrón consistente con callClaude)
    - `index.ts` error taxonomy: `TIMEOUT:plantnet:${ms}ms`, `RATE_LIMIT:plantnet:429`, `API_ERROR:plantnet:${status}`, `NO_RESULTS`
    - `consensus.ts` NO ha sido modificado (`git diff --exit-code` devuelve 0)
    - `npm run build` pasa sin errores TypeScript
  </acceptance_criteria>
  <done>
    Función `callPlantnetTimed` disponible en `index.ts` con todas las guardias (timeout, error taxonomy, no-key fallback, JSON parse safety). Lista para ser invocada en Task 2.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extender Promise.allSettled a 4 promesas + split + persistir 4ª fila</name>
  <read_first>
    - supabase/functions/identify-plant/index.ts (secciones STEP 1 líneas 389-410, STEP 5 líneas 484-511, STEP 6 líneas 513-524 — post-Task-1)
    - .planning/phases/05-plantnet-fourth-provider/05-RESEARCH.md § Pattern 1 (líneas 178-211 — Promise.allSettled extension + split)
    - .planning/phases/05-plantnet-fourth-provider/05-RESEARCH.md § Pattern 3 (líneas 311-372 — 4ª fila insert)
    - .planning/phases/05-plantnet-fourth-provider/05-PATTERNS.md § 1.D y 1.F y 1.G (fan-out + insert + response filter)
    - .planning/phases/05-plantnet-fourth-provider/05-RESEARCH.md § Open Question #2 (líneas 766-769 — NO exponer plantnet al cliente)
  </read_first>
  <files>supabase/functions/identify-plant/index.ts</files>
  <action>
    Modificar tres secciones de `index.ts` (STEP 1, STEP 5, STEP 6). Los diffs son puntuales — no reescribir el archivo.

    **DIFF 1 — STEP 1: Añadir 4ª promesa + split** (aprox. líneas 389-410):

    Cambiar el `Promise.allSettled` de 3 items a 4 items. Después, separar en `llmResults` (para pickWinner/consensus, intactos) y `plantnetResult` (solo para persistir).

    ANTES:
    ```typescript
    const settled = await Promise.allSettled([
      callModelTimed("claude", () => callClaude(base64Data, mediaType)),
      callModelTimed("gemini", () => callGemini(base64Data, mediaType)),
      callModelTimed("gpt4o",  () => callOpenAI(base64Data, mediaType)),
    ]);
    ```

    DESPUÉS (copiar del Pattern 1 RESEARCH.md L192-210):
    ```typescript
    const settled = await Promise.allSettled([
      callModelTimed("claude", () => callClaude(base64Data, mediaType)),
      callModelTimed("gemini", () => callGemini(base64Data, mediaType)),
      callModelTimed("gpt4o",  () => callOpenAI(base64Data, mediaType)),
      callPlantnetTimed(base64Data, mediaType),  // ← 4ª promesa (D-01 observer silencioso)
    ]);

    // Split: LLMs (3) para consensus/pickWinner intactos; PlantNet (1) solo para persistir.
    // PlantNet queda EXCLUIDO de pickWinner y computeConsensus (D-01).
    const llmSettled = settled.slice(0, 3);
    const plantnetSettled = settled[3];
    const plantnetResult = plantnetSettled.status === "fulfilled"
      ? plantnetSettled.value
      : null;
    ```

    Renombrar cualquier referencia posterior a `settled` que aplique solo a LLMs (`pickWinner`, `computeConsensus`, cálculo de `allResults` de LLMs) para usar `llmSettled` en su lugar. Ejemplo (STEP 1 logging líneas 401-409):

    ANTES:
    ```typescript
    const perModelSummary = settled.map((s, i) => {
      const modelName = (["claude", "gemini", "gpt4o"] as const)[i];
      // ...
    }).join(" ");
    ```

    DESPUÉS:
    ```typescript
    const perModelSummary = llmSettled.map((s, i) => {
      const modelName = (["claude", "gemini", "gpt4o"] as const)[i];
      // ...
    }).join(" ");

    // Log adicional para plantnet (fuera del summary de consenso)
    const plantnetLog = plantnetResult
      ? `plantnet=${plantnetResult.success ? "ok" : (plantnetResult.errorMessage ?? "fail")}(${plantnetResult.responseMs}ms)`
      : "plantnet=REJECTED";
    console.log(`[identify] STEP 1 done in ${step1Ms}ms — ${perModelSummary} | ${plantnetLog}`);
    ```

    **DIFF 2 — STEP 5: Añadir 4ª fila con raw_response** (aprox. líneas 484-511):

    Después de construir `evaluationRows` para las 3 filas LLM (código existente), hacer push de la 4ª fila plantnet SI `plantnetResult` no es null. Copiar de RESEARCH.md L322-372:

    ```typescript
    // 3 filas LLM (código existente). Añadir raw_response: null a cada una:
    const evaluationRows = allResults.map((r) => {
      const consensus = r.success ? (consensusGroups.get(r.model) ?? null) : null;
      return {
        plant_search_id:         searchRow.id,
        model:                   r.model,
        raw_name:                r.rawName,
        scientific_name:         r.scientificName,
        description:             r.plantInfo?.description ?? null,
        care:                    r.plantInfo?.care ?? null,
        diagnosis:               r.plantInfo?.diagnosis ?? null,
        response_ms:             r.responseMs,
        success:                 r.success,
        error_message:           r.errorMessage,
        is_winner:               r.model === winner.model,
        consensus_group:         consensus?.verdict ?? null,
        consensus_match_level:   consensus?.matchLevel ?? null,
        raw_response:            null,  // ← nueva columna, LLMs no la usan (D-03)
      };
    });

    // 4ª fila plantnet (solo si al menos intentamos llamar — plantnetResult existe)
    if (plantnetResult) {
      evaluationRows.push({
        plant_search_id:         searchRow.id,
        model:                   "plantnet",
        raw_name:                plantnetResult.rawName,
        scientific_name:         plantnetResult.scientificName,
        description:             null,  // PlantNet no devuelve markdown de cuidados
        care:                    null,
        diagnosis:               null,
        response_ms:             plantnetResult.responseMs,
        success:                 plantnetResult.success,
        error_message:           plantnetResult.errorMessage,
        is_winner:               false,  // D-01: PlantNet nunca gana
        consensus_group:         null,   // D-01: NO participa en consenso
        consensus_match_level:   null,
        raw_response:            plantnetResult.rawResponse,  // ← JSON completo, D-03
      });
    }
    ```

    Nota TypeScript: el type del array ahora es union — puede requerir un cast local si TS estricto se queja. Usar `as const` o cast explícito al type del INSERT. Alternativa: definir `type EvaluationRow = { model: 'claude' | 'gemini' | 'gpt4o' | 'plantnet'; ... }` local a esta sección.

    **DIFF 3 — STEP 6: NO exponer plantnet en modelsSummary** (aprox. líneas 513-524):

    El `modelsSummary` que se serializa al cliente debe SEGUIR siendo 3 items (D-02 invisible al usuario). El código actual usa `allResults` que ya es LLM-only tras el split. Si por alguna razón se usara `evaluationRows` para construir `modelsSummary`, añadir filter explícito:

    ```typescript
    // modelsSummary: SOLO LLMs — PlantNet NO se expone al cliente (D-02).
    // Si el futuro añade plantnet a allResults, este filter previene regresión.
    const modelsSummary = allResults
      .filter((r) => r.model !== "plantnet")  // defensa en profundidad
      .map((r) => ({ /* shape existente */ }));
    ```

    Añadir comentario justo antes: `// D-02: PlantNet excluido del response al cliente. Solo en DB.`

    **Al terminar los 3 diffs:**
    - Verificar que `consensus.ts` sigue sin cambios: `git diff --exit-code supabase/functions/identify-plant/consensus.ts` → 0
    - Ejecutar `npm run test` — los tests existentes (consensus.test.ts, use-plant-identifier.test.ts) deben pasar sin modificación (Pitfall #7 verifica esto)
    - Ejecutar `npm run build` para type-check
    - Deploy: `supabase functions deploy identify-plant`
    - Smoke test: hacer 1 identificación desde la app en producción y verificar via SQL:
      ```sql
      SELECT model, success, response_ms, raw_response IS NOT NULL AS has_json
      FROM model_evaluations
      WHERE plant_search_id = (SELECT id FROM plant_searches ORDER BY created_at DESC LIMIT 1)
      ORDER BY model;
      ```
      Debe devolver 4 filas: claude, gemini, gpt4o, plantnet. Fila plantnet debe tener `has_json = true` si PlantNet respondió, `false` si falló (D-09).
  </action>
  <verify>
    <automated>grep -q "callPlantnetTimed(base64Data, mediaType)" supabase/functions/identify-plant/index.ts && grep -q "plantnetResult" supabase/functions/identify-plant/index.ts && grep -q "model:\s*\"plantnet\"" supabase/functions/identify-plant/index.ts && grep -q "raw_response:\s*plantnetResult.rawResponse" supabase/functions/identify-plant/index.ts && grep -q "D-01" supabase/functions/identify-plant/index.ts && grep -q "D-02" supabase/functions/identify-plant/index.ts && git diff --exit-code supabase/functions/identify-plant/consensus.ts && npm run build 2>&1 | tail -5 | grep -qE "(built in|✓)" && npm run test 2>&1 | tail -10 | grep -qE "(passed|✓)"</automated>
  </verify>
  <acceptance_criteria>
    - `Promise.allSettled` tiene 4 items (línea con `callPlantnetTimed(base64Data, mediaType)` presente)
    - Split `llmSettled = settled.slice(0, 3)` + `plantnetResult = ...` presente
    - `pickWinner` recibe solo llmResults (no cambio de firma; `plantnet` no aparece en su input)
    - `evaluationRows` incluye 4ª fila con `model: "plantnet"`, `is_winner: false`, `consensus_group: null`, `raw_response: plantnetResult.rawResponse`
    - Las 3 filas LLM incluyen `raw_response: null` (nueva columna, no la usan)
    - `modelsSummary` NO incluye plantnet (verificable por comentario `D-02` + filter o por diseño del código — `allResults` ya excluye plantnet tras el split)
    - Comentarios `D-01` y `D-02` presentes en las secciones críticas para audit trail
    - `consensus.ts` sin cambios (git diff exit 0)
    - `npm run build` pasa
    - `npm run test` pasa (regresión de consensus.test.ts + use-plant-identifier.test.ts)
    - Smoke SQL post-deploy: 4 filas por identificación, plantnet row con raw_response NOT NULL cuando success
  </acceptance_criteria>
  <done>
    Edge function deployed y funcionando. Cada nueva identificación genera 4 filas en `model_evaluations`. Cliente recibe response idéntica a antes (D-02). PlantNet observa en silencio (D-01). Failures silenciosos (D-09). Latencia P95 no empeora (PLANT-03 verificable en plan 05-05 con query C).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Edge function (Deno runtime) → PlantNet API v2 (my-api.plantnet.org) | Foto del usuario (base64) enviada a 3rd party via multipart POST HTTPS. |
| Edge function → Supabase Postgres (INSERT) | 4ª fila con `raw_response jsonb` — JSON de PlantNet sin sanitizar (jsonb valida sintaxis). |
| PLANTNET_API_KEY (Function Secret) → outbound fetch URL | Key va en query string (`?api-key=...`), potencial leak en logs si console.log(url). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Information Disclosure | PLANTNET_API_KEY en URL de fetch | mitigate | NO loggear la URL completa. Solo `console.error("plantnet API error:", res.status, text.substring(0, 200))` — patrón idéntico a `callClaude` líneas 141-142. Verificable: `grep -c "console.log.*url.*plantnet" index.ts` debe ser 0. |
| T-05-03-02 | Information Disclosure | Foto del usuario compartida con PlantNet (3rd party) | accept | Los 3 LLMs actuales (Anthropic, OpenAI, Google) YA reciben la foto — no es escalada nueva del perfil de privacidad. CPO ya asumió esta consecuencia al aprobar la phase (D-01 explícito). Nota flagged en RESEARCH.md § Security Domain "Nota de privacidad" — subir a CPO si política de privacidad menciona explícitamente processors data GDPR. |
| T-05-03-03 | Server-Side Request Forgery | Response de PlantNet contiene URLs (related-images, GBIF) | mitigate | NO usar `include-related-images=true` (Pitfall #6). El JSON se guarda en `raw_response jsonb` pero NO se hace fetch a URLs contenidas. Verificable: `grep -c "include-related-images" index.ts` debe ser 0. |
| T-05-03-04 | Denial of Service | PlantNet quota exhausted (500/día free) | accept | Failure silencioso (D-09): una identificación con plantnet fallado sigue devolviendo resultado LLM al usuario. Volumen actual 15-30/día × 16 = 500. Buffer ~16x. |
| T-05-03-05 | Denial of Service | PlantNet lento bloquea edge function | mitigate | `AbortSignal.timeout(10_000)` fuerza abort antes que el timeout del edge function (60s Supabase default). PLANTNET_TIMEOUT_MS (10s) < LLM_TIMEOUT_MS (15s) — PlantNet no puede ser cuello de botella. |
| T-05-03-06 | Tampering | JSON malicioso de PlantNet en raw_response | accept | jsonb en Postgres valida sintaxis JSON automáticamente. La columna es opaque storage — no se ejecuta ni se hace fetch de URLs contenidas. Consumers (queries SQL de plan 05-04) hacen SELECT solo, no execution. |
| T-05-03-07 | Elevation of Privilege | Widen del CHECK constraint permite `model='plantnet'` en INSERT | accept | Widening es strictly more permissive. Cualquier código futuro que inserte `plantnet` es intencional. No hay nueva superficie de RLS. |
| T-05-03-08 | Information Disclosure | plantnet en `modelsSummary` filtrado al cliente | mitigate | D-02: modelsSummary excluye plantnet. Comentario explícito `D-02: PlantNet excluido del response al cliente` + filter defensivo en Diff 3. Verificable con test: response JSON del edge function NO debe contener `"model":"plantnet"` en `models[]`. |
</threat_model>

<verification>
- `Promise.allSettled` tiene 4 promesas (verifiable con grep)
- Split limpio: llmResults (3) alimenta consensus, plantnetResult (1) solo persiste
- 4ª fila en INSERT con `raw_response = JSON completo`, is_winner=false, consensus_group=null
- `consensus.ts` sin cambios (git diff exit 0) — D-01 preservado
- `modelsSummary` sin plantnet — D-02 preservado
- Tests existentes pasan sin modificación (`npm run test`)
- Deploy edge function: `supabase functions deploy identify-plant` sin errores
- Smoke SQL post-deploy: identificación nueva genera 4 filas correctas
</verification>

<success_criteria>
- PLANT-01: ≥95% de identificaciones nuevas post-deploy tienen 4 filas en `model_evaluations` (verificable con SQL en plan 05-05)
- PLANT-03: PLANTNET_TIMEOUT_MS (10s) < LLM_TIMEOUT_MS (15s) garantiza que PlantNet no es cuello de botella. `Promise.allSettled` no cambia el shape del response — el cliente recibe JSON idéntico a antes.
- D-01/D-02/D-09 preservados (audit trail via comentarios + git diff empty en consensus.ts)
- No regresión en tests existentes
</success_criteria>

<output>
After completion, create `.planning/phases/05-plantnet-fourth-provider/05-03-SUMMARY.md`
</output>
