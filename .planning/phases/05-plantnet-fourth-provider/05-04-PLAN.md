---
phase: 05-plantnet-fourth-provider
plan: 04
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - docs/model-evaluation-queries.sql
  - docs/posthog-events.md
autonomous: true
requirements: [PLANT-02]

must_haves:
  truths:
    - "`docs/model-evaluation-queries.sql` incluye 1 query nueva de divergencia PlantNet vs LLM winner (D-07)"
    - "La query es copy-pasteable en Supabase SQL Editor sin ediciones, agrupa por familia si posible, filtra últimos 30 días"
    - "El comentario del header general de model-evaluation-queries.sql refleja que ahora son 4 filas por búsqueda (no 3)"
    - "`docs/posthog-events.md` documenta el evento nuevo `plantnet_divergence` con payload keys (D-12)"
    - "Estilo consistente con las 12 queries y ~15 eventos existentes (español, separadores, tablas markdown)"
  artifacts:
    - path: "docs/model-evaluation-queries.sql"
      provides: "Sección PHASE 5 con query de divergencia"
      contains: "PHASE 5"
    - path: "docs/posthog-events.md"
      provides: "Documentación del evento plantnet_divergence"
      contains: "plantnet_divergence"
  key_links:
    - from: "docs/model-evaluation-queries.sql (query nueva)"
      to: "plant_searches.plantnet_diverged + model_evaluations.model='plantnet'"
      via: "JOIN + WHERE plantnet_diverged = true"
      pattern: "plantnet_diverged\\s*=\\s*true"
    - from: "docs/posthog-events.md"
      to: "supabase/functions/identify-plant/index.ts (dispatcher)"
      via: "referencia en tabla de eventos"
      pattern: "plantnet_divergence"
---

<objective>
Documentar las herramientas de análisis que producirá la fase: la query SQL para investigar divergencias en Supabase SQL Editor + el evento PostHog para consumo en dashboard.

**Purpose:** Cerrar el ciclo de PLANT-02 (analytics). Después de que Plan 03 despliegue el edge function, empezarán a acumularse datos. La CPO necesita las herramientas listas y documentadas para revisarlos cuando quiera (sin depender de que Claude escriba la query ad-hoc cada vez).

**Output:**
- `docs/model-evaluation-queries.sql`: header actualizado (3→4 filas por búsqueda) + sección nueva `PHASE 5 — PLANTNET COMO 4º VOTANTE` con 1 query de divergencia comentada en español y copy-pasteable
- `docs/posthog-events.md`: entrada nueva para `plantnet_divergence` en la tabla de eventos custom + detalle de propiedades del payload

Este plan corre en Wave 2 en paralelo con Plan 03 (edge function) — no colisiona en archivos. Solo depende de Plan 01 para que la columna `plantnet_diverged` exista cuando la query se ejecute.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-plantnet-fourth-provider/05-CONTEXT.md
@.planning/phases/05-plantnet-fourth-provider/05-PATTERNS.md
@docs/model-evaluation-queries.sql
@docs/posthog-events.md

<interfaces>
<!-- Estilo actual de model-evaluation-queries.sql (extraído verbatim del archivo) -->

Header actual (líneas 1-4):
```sql
-- ═══════════════════════════════════════════════════════════════════════════════
-- Queries de evaluación de modelos — Supabase SQL Editor
-- Tabla: model_evaluations (3 filas por búsqueda: claude, gemini, gpt4o)
-- ═══════════════════════════════════════════════════════════════════════════════
```
Este comentario hay que actualizarlo a "4 filas por búsqueda: claude, gemini, gpt4o, plantnet".

Patrón de sección numerada (existente para queries 1-12):
```sql
-- ─────────────────────────────────────────────────────────────────────────────
-- N. TÍTULO EN MAYÚSCULAS
-- Comentario en español (2-6 líneas) explicando qué mide + notas.
-- ─────────────────────────────────────────────────────────────────────────────

SELECT ...
```

Al final del archivo (línea 337) va la query 12 sobre `consensus_match_level`. La nueva sección se APPENDA al final, sin tocar las 12 existentes.

Patrón de "aggregate section header" (existente en línea 243-247):
```sql
-- ═══════════════════════════════════════════════════════════════════════════════
-- SECTION TITLE
-- Comentario general del bloque.
-- ═══════════════════════════════════════════════════════════════════════════════
```

Payload del evento PostHog `plantnet_divergence` (definido en Plan 03 Task 2):
```typescript
{
  plantnet_scientific: string,    // e.g. "Ficus benjamina"
  plantnet_score: number,          // 0..1, e.g. 0.87
  llm_winner_scientific: string,   // e.g. "Ficus lyrata"
  llm_winner_model: "claude" | "gemini" | "gpt4o",
  plant_search_id: string (uuid),
}
```

Patrón de tabla de eventos en `docs/posthog-events.md`:
```markdown
| Evento | Propiedades | Archivo | Cuándo se dispara |
|---|---|---|---|
| `event_name` | `prop_a`, `prop_b` | `path/to/file.tsx` | Descripción cuándo se dispara |
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extender docs/model-evaluation-queries.sql con la query de divergencia</name>
  <read_first>
    - docs/model-evaluation-queries.sql (completo, para conocer el estilo de queries 1-12 y localizar el EOF)
    - .planning/phases/05-plantnet-fourth-provider/05-CONTEXT.md § D-07, D-12 (definiciones)
  </read_first>
  <files>docs/model-evaluation-queries.sql</files>
  <action>
Modificar `docs/model-evaluation-queries.sql` en 2 puntos:

**1) Actualizar el header general (línea 3):**

Cambiar:
```sql
-- Tabla: model_evaluations (3 filas por búsqueda: claude, gemini, gpt4o)
```
Por:
```sql
-- Tabla: model_evaluations (4 filas por búsqueda desde Phase 5 deploy: claude, gemini, gpt4o, plantnet)
```

**2) APPENDAR al final del archivo (después de la query 12, sin tocar nada existente):**

```sql


-- ═══════════════════════════════════════════════════════════════════════════════
-- PHASE 5 — PLANTNET COMO 4º VOTANTE (cross-validated override)
--
-- Desde el deploy de Phase 5, cada identificación tiene 4 filas en model_evaluations
-- (una por proveedor: claude, gemini, gpt4o, plantnet). PlantNet vota activamente
-- vía cross-validation (ver .planning/phases/05-plantnet-fourth-provider/05-CONTEXT.md
-- D-01): cuando PlantNet devuelve score ≥ 0.8 y algún LLM coincide con su
-- científico, PlantNet "manda" el nombre; cuando no coincide ningún LLM,
-- se marca plant_searches.plantnet_diverged = true (divergencia registrada
-- para análisis, pero LLMs siguen decidiendo).
-- ═══════════════════════════════════════════════════════════════════════════════


-- ─────────────────────────────────────────────────────────────────────────────
-- 13. DIVERGENCIAS PLANTNET VS LLM WINNER (últimos 30 días)
--
-- Responde: ¿en qué identificaciones PlantNet difirió del LLM ganador,
-- con qué frecuencia, y en qué géneros/familias ocurre más?
--
-- Se considera divergencia: PlantNet devolvió score ≥ 0.8 pero ningún LLM
-- coincidió con su científico a nivel exact/normalized (per D-11 no cuenta
-- el match de género — Ficus lyrata vs Ficus benjamina es divergencia,
-- no coincidencia). Ver Phase 5 D-01, D-10, D-11, D-12.
--
-- Cuando plantnet_diverged = true, el edge function además dispara el evento
-- PostHog `plantnet_divergence` (ver docs/posthog-events.md § Phase 5).
--
-- Interpretación:
--   - pocas divergencias (<5% de identificaciones) → PlantNet y LLMs concuerdan
--     la mayoría del tiempo; el sistema es coherente
--   - muchas divergencias (>20%) → posible señal de recalibración del umbral
--     0.8 (D-10) o de que PlantNet detecta especies que los LLMs no distinguen
--   - divergencias concentradas en un género (columna genero_llm) → puede
--     merecer atención por si los cuidados del LLM no son fiables para esa
--     familia
-- ─────────────────────────────────────────────────────────────────────────────

WITH divergencias AS (
  SELECT
    ps.id                                             AS plant_search_id,
    ps.created_at                                     AS fecha,
    ps.name                                           AS nombre_mostrado_al_usuario,
    ps.model                                          AS llm_winner_model,
    -- Fila del LLM winner (buscada en model_evaluations por is_winner=true)
    me_llm.scientific_name                            AS llm_winner_scientific,
    -- Fila de PlantNet (siempre presente si intentó llamar; puede ser success=false)
    me_pn.scientific_name                             AS plantnet_scientific,
    (me_pn.raw_response->'results'->0->>'score')::float
                                                      AS plantnet_score,
    -- Género extraído del científico del LLM (primera palabra, minúsculas)
    LOWER(SPLIT_PART(TRIM(me_llm.scientific_name), ' ', 1))
                                                      AS genero_llm,
    LOWER(SPLIT_PART(TRIM(me_pn.scientific_name), ' ', 1))
                                                      AS genero_plantnet,
    -- Payload compacto para inspección rápida (top-3 de PlantNet)
    jsonb_path_query_array(
      me_pn.raw_response,
      '$.results[0 to 2] ? (@.score >= 0.5)'
    )                                                 AS plantnet_top3_snippet
  FROM plant_searches ps
  LEFT JOIN model_evaluations me_llm
    ON me_llm.plant_search_id = ps.id
   AND me_llm.is_winner = true
  LEFT JOIN model_evaluations me_pn
    ON me_pn.plant_search_id = ps.id
   AND me_pn.model = 'plantnet'
  WHERE ps.plantnet_diverged = true
    AND ps.created_at >= NOW() - INTERVAL '30 days'
)
SELECT
  fecha,
  nombre_mostrado_al_usuario,
  llm_winner_model,
  llm_winner_scientific,
  plantnet_scientific,
  ROUND(plantnet_score::numeric, 2)                   AS plantnet_score,
  CASE
    WHEN genero_llm = genero_plantnet THEN 'mismo género, especies distintas'
    ELSE 'géneros distintos'
  END                                                 AS tipo_divergencia,
  genero_llm,
  genero_plantnet,
  plant_search_id
FROM divergencias
ORDER BY fecha DESC;


-- ─────────────────────────────────────────────────────────────────────────────
-- 13-BIS. RESUMEN AGREGADO DE DIVERGENCIAS (últimos 30 días)
--
-- Snapshot rápido para chequeo mensual: cuántas búsquedas totales, cuántas
-- con PlantNet exitoso, cuántas con override aplicado, cuántas divergentes.
-- ─────────────────────────────────────────────────────────────────────────────

WITH stats AS (
  SELECT
    COUNT(DISTINCT ps.id)                                              AS total_busquedas,
    COUNT(DISTINCT ps.id) FILTER (WHERE ps.plantnet_diverged = true)   AS busquedas_divergentes,
    COUNT(DISTINCT me.plant_search_id) FILTER (
      WHERE me.model = 'plantnet' AND me.success = true
    )                                                                  AS plantnet_exitoso,
    COUNT(DISTINCT me.plant_search_id) FILTER (
      WHERE me.model = 'plantnet' AND me.success = false
    )                                                                  AS plantnet_fallido
  FROM plant_searches ps
  LEFT JOIN model_evaluations me ON me.plant_search_id = ps.id
  WHERE ps.created_at >= NOW() - INTERVAL '30 days'
)
SELECT
  total_busquedas,
  plantnet_exitoso,
  plantnet_fallido,
  busquedas_divergentes,
  ROUND(100.0 * plantnet_exitoso / NULLIF(total_busquedas, 0), 1)
                                                                      AS pct_plantnet_exitoso,
  ROUND(100.0 * busquedas_divergentes / NULLIF(plantnet_exitoso, 0), 1)
                                                                      AS pct_divergencia_sobre_exitosos
FROM stats;
```

**Nota sobre "1 query" del quality_gate:** el spec pide 1 query nueva de divergencia. He añadido 13 (detalle fila-a-fila) + 13-BIS (snapshot agregado). Ambas son inseparables en la práctica — sin la agregada, la CPO tendría que contar manualmente. La agregada es un ~15% de código extra. **Si el checker considera que 13-BIS sobra, eliminar ese bloque y dejar solo la 13.**

Verificar sintaxis:
```bash
# Sanity check: contamos que el archivo termina bien y que no se rompen las queries existentes
python3 -c "
with open('docs/model-evaluation-queries.sql') as f: c = f.read()
assert 'PHASE 5' in c, 'no PHASE 5 section'
assert 'plantnet_diverged = true' in c, 'no divergence WHERE'
assert 'plantnet_score' in c, 'no plantnet_score field'
assert c.count('SELECT') >= 13, f'expected >=13 SELECTs, got {c.count(\"SELECT\")}'
print('OK')
"
```
  </action>
  <verify>
    <automated>grep -q "PHASE 5" docs/model-evaluation-queries.sql &amp;&amp; grep -q "plantnet_diverged = true" docs/model-evaluation-queries.sql &amp;&amp; grep -q "13\. DIVERGENCIAS" docs/model-evaluation-queries.sql &amp;&amp; grep -q "4 filas por búsqueda desde Phase 5" docs/model-evaluation-queries.sql &amp;&amp; python3 -c "c = open('docs/model-evaluation-queries.sql').read(); assert c.count('SELECT') >= 13" &amp;&amp; echo "OK: sección Phase 5 añadida, header actualizado"</automated>
  </verify>
  <acceptance_criteria>
    - Header general actualizado a "4 filas por búsqueda desde Phase 5 deploy"
    - Sección `PHASE 5 — PLANTNET COMO 4º VOTANTE` presente al final del archivo
    - Query 13 (divergencias detalle) presente, con JOIN sobre plant_searches.plantnet_diverged
    - Comentarios en español, ≥5 líneas de contexto explicando la query
    - Estilo consistente: separador `─` de 77 chars, título en mayúsculas, `WITH ... AS (...)` para CTEs
    - Ninguna query existente (1-12) tocada — `git diff docs/model-evaluation-queries.sql` solo debe mostrar el cambio del header + append al final
  </acceptance_criteria>
  <done>Query de divergencia documentada, copy-pasteable, estilo consistente con las 12 queries previas.</done>
</task>

<task type="auto">
  <name>Task 2: Documentar evento plantnet_divergence en docs/posthog-events.md</name>
  <read_first>
    - docs/posthog-events.md (completo, para localizar la sección apropiada)
    - .planning/phases/05-plantnet-fourth-provider/05-CONTEXT.md § D-12 (payload)
  </read_first>
  <files>docs/posthog-events.md</files>
  <action>
Añadir a `docs/posthog-events.md` una sección nueva al final del archivo (después de la última sección existente, sea Phase 03.1 tracking o similar).

**Contenido a añadir:**

```markdown


## Phase 5 — PlantNet cross-validation (server-side event)

**Status:** Live tras el deploy de Phase 5 (2026-08-XX — actualizar con fecha real).

Único evento server-side de la app hasta la fecha. Se dispara desde `supabase/functions/identify-plant/index.ts` cuando PlantNet difiere del LLM winner (ver `.planning/phases/05-plantnet-fourth-provider/05-CONTEXT.md` D-01 branch 4 + D-12).

### Evento

| Evento | Propiedades | Archivo | Cuándo se dispara |
|---|---|---|---|
| `plantnet_divergence` | `plantnet_scientific`, `plantnet_score`, `llm_winner_scientific`, `llm_winner_model`, `plant_search_id` | `supabase/functions/identify-plant/index.ts` | En cada identificación donde PlantNet devuelve `score ≥ 0.8` pero ningún LLM coincide con su científico a nivel exact/normalized (D-11: match de género NO cuenta). |

### Detalle de propiedades

- **`plantnet_scientific`** *(string)* — científico devuelto por PlantNet, lowercased (`scientificNameWithoutAuthor` del top-1 de `results[]`). Ej: `"ficus benjamina"`.
- **`plantnet_score`** *(number, 0..1)* — score de confianza de PlantNet en el top-1. Umbral de override = 0.8 (D-10). En divergencias siempre será ≥ 0.8.
- **`llm_winner_scientific`** *(string)* — científico del LLM que ganó el pickWinner (extractScientificName sobre el `name` del LLM). Ej: `"ficus lyrata"`.
- **`llm_winner_model`** *(`"claude"` | `"gemini"` | `"gpt4o"`)* — modelo del LLM winner.
- **`plant_search_id`** *(uuid)* — id de la fila en `plant_searches` (permite JOIN a la búsqueda completa para debug).

### Distinct ID

Usa `user_id` si está presente, si no `anonymous_id`, si no el fallback `"edge-fn-anon"`. Consistente con la vinculación de identidad del resto de la app (`plant_identified` sigue el mismo criterio).

### Cómo consumirlo

- **PostHog Dashboard:** filtra `event = 'plantnet_divergence'` en Insights o Trends. Útil para ver picos por día/semana o segmentar por `llm_winner_model` para detectar qué LLM difiere más.
- **SQL alternativo:** la misma información es consultable en Supabase con las queries 13 y 13-BIS de `docs/model-evaluation-queries.sql`. Los dos canales son intencionalmente redundantes: PostHog para dashboards y alertas casi-real-time, SQL para retrospectiva completa cuando se necesita cruzar con otras columnas de plant_searches (contexto, familia, etc.).

### Requisito de despliegue

El edge function necesita `POSTHOG_PROJECT_API_KEY` (no la personal key) como Supabase Function Secret. Si no está registrada, el evento se skipea silenciosamente con un `console.warn` — la funcionalidad de override + flag DB sigue funcionando (los datos SQL no dependen de PostHog).
```

Verificar sintaxis Markdown básica:
```bash
grep -q "plantnet_divergence" docs/posthog-events.md
grep -q "## Phase 5" docs/posthog-events.md
# Sanity check tablas: contar "|" en las líneas de tabla del bloque nuevo
```
  </action>
  <verify>
    <automated>grep -q "## Phase 5 — PlantNet" docs/posthog-events.md &amp;&amp; grep -q "plantnet_divergence" docs/posthog-events.md &amp;&amp; grep -q "plantnet_score" docs/posthog-events.md &amp;&amp; grep -q "llm_winner_model" docs/posthog-events.md &amp;&amp; grep -q "plant_search_id" docs/posthog-events.md &amp;&amp; grep -q "POSTHOG_PROJECT_API_KEY" docs/posthog-events.md &amp;&amp; echo "OK: evento documentado con las 5 propiedades + nota deployment"</automated>
  </verify>
  <acceptance_criteria>
    - Nueva sección `## Phase 5` presente al final del archivo
    - Tabla con evento `plantnet_divergence` en el formato existente (4 columnas: Evento, Propiedades, Archivo, Cuándo se dispara)
    - Detalle de las 5 propiedades del payload con tipo y ejemplo
    - Nota sobre distinct_id (user_id > anonymous_id > fallback)
    - Referencia a las queries 13/13-BIS de model-evaluation-queries.sql
    - Nota sobre requisito de `POSTHOG_PROJECT_API_KEY`
    - Ninguna sección existente tocada — `git diff docs/posthog-events.md` solo debe mostrar append al final
  </acceptance_criteria>
  <done>Evento server-side documentado con payload completo, integrado en el estilo del documento existente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → CPO consumer | La query se ejecutará en Supabase SQL Editor con role authenticated (o service_role si CPO usa el Dashboard). Su output puede incluir user_id / anonymous_id de las búsquedas. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Information Disclosure | Query 13 devuelve `plant_search_id` que podría permitir join a datos de usuario | accept | Ejecutable solo desde Supabase SQL Editor por la CPO (owner). No hay endpoint expuesto al cliente. Mismo perfil de riesgo que las 12 queries existentes. |
| T-05-04-02 | Tampering | La query 13 se copia y edita mal, filtra por ventanas incorrectas | mitigate | Documentado en comentario "últimos 30 días" con `INTERVAL '30 days'` explícito. La CPO puede editar la ventana localmente. |
| T-05-04-03 | Elevation of Privilege | jsonb_path_query_array podría filtrar contenido no autorizado | accept | raw_response solo contiene datos de PlantNet (URLs de fotos genéricas de la especie + nombres científicos). No hay PII. |
| T-05-04-04 | Information Disclosure | PostHog event documenta payload con datos que podrían ser PII si mal interpretados | mitigate | Payload NO incluye email/nombre/etc. Solo IDs internos y strings botánicos. Documentado explícitamente. |
</threat_model>

<verification>
- Header de model-evaluation-queries.sql actualizado
- Sección PHASE 5 añadida con query 13 (+ 13-BIS opcional)
- Query 13 sintácticamente válida y ejecutable (con permisos + datos post-deploy)
- posthog-events.md tiene sección `## Phase 5` con evento documentado
- Estilo consistente con el resto del archivo
</verification>

<success_criteria>
1. **PLANT-02:** `docs/model-evaluation-queries.sql` tiene query de divergencia PlantNet vs LLM winner copy-pasteable
2. **PLANT-02:** `docs/posthog-events.md` documenta el evento `plantnet_divergence` con payload keys
3. Estilo consistente con el corpus documental existente
4. Documentos NO rompen ningún tooling (no hay linters activos, pero estructura Markdown válida y SQL parseable)
</success_criteria>

<output>
Al terminar, crea `.planning/phases/05-plantnet-fourth-provider/05-04-SUMMARY.md` con:
- Diff resumido de cada archivo (líneas añadidas)
- Confirmación de que las 12 queries existentes en SQL y las secciones anteriores del posthog-events.md quedan intactas
- Comentario sobre si 13-BIS se mantuvo o eliminó según feedback del checker
</output>
