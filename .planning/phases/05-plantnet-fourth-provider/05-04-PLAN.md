---
phase: 05-plantnet-fourth-provider
plan: 04
type: execute
wave: 4
depends_on: [05-01, 05-02]
files_modified:
  - docs/model-evaluation-queries.sql
  - supabase/migrations/20260815010000_create_golden_set_ground_truth.sql
autonomous: true
requirements: [PLANT-02]
tags: [analytics, sql, docs, golden-set, optional-migration]

must_haves:
  truths:
    - "docs/model-evaluation-queries.sql tiene sección nueva PHASE 5 con 4 queries (A, B, C, D + D-BIS)"
    - "Query A (success rate) devuelve 4 filas incluyendo plantnet"
    - "Query B (top-1 accuracy) respeta la decisión D-10/D-11 tomada en plan 05-01 (con caveat comment si aplica)"
    - "Query C (latencia comparada) permite verificar PLANT-03 (P95 plantnet no empeora)"
    - "Query D + D-BIS (divergencia PlantNet vs winner-LLM) proveen métrica honesta sin depender del golden set"
    - "Header de docs/model-evaluation-queries.sql actualizado de '3 filas' a '4 filas' por búsqueda"
    - "Las queries son copy-pasteables sin edición en Supabase SQL Editor"
    - "Si D-11 = B1: existe tabla auxiliar `public.golden_set_ground_truth` con GRANT explícitos y RLS"
  artifacts:
    - path: "docs/model-evaluation-queries.sql"
      provides: "Sección PHASE 5 con 4 queries analíticas comparativas PlantNet vs LLMs"
      contains: "PHASE 5 — PLANTNET"
    - path: "supabase/migrations/20260815010000_create_golden_set_ground_truth.sql (opcional, solo si D-11 = B1)"
      provides: "Tabla auxiliar para modelar ground truth con GRANT explícitos"
      contains: "create table public.golden_set_ground_truth"
  key_links:
    - from: "docs/model-evaluation-queries.sql § PHASE 5"
      to: "Supabase SQL Editor (workflow CPO)"
      via: "copy-paste directo"
      pattern: "Query [A-D]"
    - from: "05-CONTEXT.md § Addendum D-10"
      to: "docs/model-evaluation-queries.sql query B caveat comment"
      via: "referencia obligatoria per plan 05-01 addendum"
      pattern: "Ver 05-CONTEXT.md.*Addendum D-10"
---

<objective>
Extender `docs/model-evaluation-queries.sql` con una nueva sección "PHASE 5 — PLANTNET COMO 4º PROVEEDOR" que contiene las 4 queries analíticas comparativas (A: success rate, B: top-1 accuracy vs golden set, C: latencia comparada, D + D-BIS: divergencia PlantNet vs winner-LLM). Estas queries son el deliverable de análisis que la CPO ejecutará manualmente en Supabase SQL Editor para decidir si Phase 5.1 (dar voto real a PlantNet) tiene sentido.

Ajuste crítico per plan 05-01 addendum: la query B se escribe respetando la decisión D-10/D-11 (contaminación del golden set + modeling de ground truth). Si D-11 = B1 (tabla auxiliar), este plan crea la migración `20260815010000_create_golden_set_ground_truth.sql` con GRANT explícitos (per convenciones MIGRATION_CONVENTIONS.md — tabla NUEVA en public post-oct-2026).

Purpose: Requirement PLANT-02 (queries copy-pasteables en Supabase SQL Editor). Success Criterion #2 del ROADMAP.

Output:
- `docs/model-evaluation-queries.sql` con:
  - Header actualizado: "4 filas por búsqueda: claude, gemini, gpt4o, plantnet"
  - Nueva sección PHASE 5 al final con las 4 queries (A, B, C, D + D-BIS)
  - Query B con caveat comment referenciando D-10 addendum
- (Solo si D-11 = B1) `supabase/migrations/20260815010000_create_golden_set_ground_truth.sql` con:
  - `CREATE TABLE public.golden_set_ground_truth (...)`
  - `GRANT SELECT, INSERT, UPDATE, DELETE ... TO authenticated`
  - `GRANT ... TO service_role`
  - `ALTER TABLE ... ENABLE ROW LEVEL SECURITY`
  - Policy RLS mínima
  - `supabase db push` aplicada
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-plantnet-fourth-provider/05-CONTEXT.md
@.planning/phases/05-plantnet-fourth-provider/05-RESEARCH.md
@.planning/phases/05-plantnet-fourth-provider/05-PATTERNS.md
@docs/model-evaluation-queries.sql
@supabase/MIGRATION_CONVENTIONS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: [Conditional] Crear tabla auxiliar golden_set_ground_truth (solo si D-11 = B1)</name>
  <read_first>
    - .planning/phases/05-plantnet-fourth-provider/05-CONTEXT.md § Addendum (D-11 elegida en plan 05-01)
    - supabase/MIGRATION_CONVENTIONS.md § "Boilerplate para nuevas migraciones que crean tablas" (líneas 11-51)
    - supabase/migrations/20260407000000_create_model_evaluations.sql (tabla existente similar — referencia para plant_search_id FK)
  </read_first>
  <files>supabase/migrations/20260815010000_create_golden_set_ground_truth.sql</files>
  <action>
    **Precondición:** Leer `.planning/phases/05-plantnet-fourth-provider/05-CONTEXT.md` § Addendum y verificar la decisión D-11.

    - Si D-11 = **B1 (tabla auxiliar)**: EJECUTAR este task.
    - Si D-11 = B2/B3/B4 o D-10 = A2 (no requiere ground truth externo): SKIP este task (crear archivo vacío con comentario `-- SKIPPED per D-11 decision in 05-CONTEXT.md addendum`).

    Si SKIPPED, saltar a Task 2 y ajustar la query B en Task 2 según la decisión (VALUES inline si B2, JOIN a plant_searches con flag si B4, etc.).

    Si EJECUTAR (B1), crear archivo `supabase/migrations/20260815010000_create_golden_set_ground_truth.sql` con contenido:

    ```sql
    -- Auxiliary table for Phase 5 query B (top-1 accuracy PlantNet vs golden set).
    -- Maps a plant_search_id (real run of edge function) to a scientific_name ground truth.
    -- Populated manually by CPO: re-run edge function with the 31 golden set photos,
    -- then INSERT plant_search_id + scientific_name (from scripts/.benchmark-golden-set.json).
    --
    -- Caveat (D-10): the golden set was bootstrapped with PlantNet as oracle on 2026-05-17.
    -- See .planning/phases/05-plantnet-fourth-provider/05-CONTEXT.md § Addendum for
    -- interpretation guidance when reading query B results.

    -- 1) Tabla
    create table public.golden_set_ground_truth (
      id                uuid        primary key default gen_random_uuid(),
      plant_search_id   uuid        not null unique references public.plant_searches(id) on delete cascade,
      image_url         text,       -- opcional, para audit y verificación visual
      scientific_name   text        not null,
      notes             text,       -- p.ej. "spot-checked by CPO 2026-08-16"
      created_at        timestamptz not null default now()
    );

    -- 2) GRANTs explícitos (REQUIRED per MIGRATION_CONVENTIONS.md — tabla nueva en public, post oct-2026)
    grant select, insert, update, delete on public.golden_set_ground_truth to authenticated;
    grant select, insert, update, delete on public.golden_set_ground_truth to service_role;

    -- 3) RLS (obligatorio en todo el proyecto)
    alter table public.golden_set_ground_truth enable row level security;

    -- 4) Política RLS — datos internos, solo authenticated puede leer/escribir.
    -- No es data de usuario individual (no hay user_id) — es ground truth compartido.
    -- Se permite a cualquier authenticated user leer/escribir (útil para CPO desde app admin).
    create policy "authenticated users can read golden set ground truth"
      on public.golden_set_ground_truth
      for select
      to authenticated
      using (true);

    create policy "authenticated users can write golden set ground truth"
      on public.golden_set_ground_truth
      for all
      to authenticated
      using (true)
      with check (true);
    ```

    Aplicar la migración:
    ```bash
    supabase db push
    ```

    Regenerar types (para tipar la tabla nueva si algún consumer TS la usa):
    ```bash
    npx supabase gen types typescript --linked > src/integrations/supabase/types.ts
    ```

    Verificar:
    ```bash
    supabase migration list --linked | grep 20260815010000
    supabase db execute --linked "SELECT column_name FROM information_schema.columns WHERE table_name = 'golden_set_ground_truth'"
    ```
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260815010000_create_golden_set_ground_truth.sql && (grep -q "SKIPPED per D-11" supabase/migrations/20260815010000_create_golden_set_ground_truth.sql || (grep -q "create table public.golden_set_ground_truth" supabase/migrations/20260815010000_create_golden_set_ground_truth.sql && grep -q "grant select, insert, update, delete on public.golden_set_ground_truth to authenticated" supabase/migrations/20260815010000_create_golden_set_ground_truth.sql && grep -q "grant select, insert, update, delete on public.golden_set_ground_truth to service_role" supabase/migrations/20260815010000_create_golden_set_ground_truth.sql && grep -q "enable row level security" supabase/migrations/20260815010000_create_golden_set_ground_truth.sql))</automated>
  </verify>
  <acceptance_criteria>
    - Si D-11 = B1: archivo existe con `CREATE TABLE`, GRANTs para authenticated + service_role, RLS enabled, mínimo 1 policy. Migración aplicada en remoto.
    - Si D-11 ≠ B1: archivo existe con comentario `-- SKIPPED per D-11 decision`. Sin CREATE TABLE.
    - Cualquiera de los dos casos: `supabase db push` no falla.
  </acceptance_criteria>
  <done>
    Decisión sobre tabla auxiliar cerrada. Task 2 (queries SQL) sabe si escribir query B con JOIN a la tabla real, o con VALUES inline, o con divergencia vs winner-LLM (según D-10 = A2).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extender docs/model-evaluation-queries.sql con sección PHASE 5</name>
  <read_first>
    - docs/model-evaluation-queries.sql (leer estilo actual, en particular header líneas 1-4 y las 12 queries existentes)
    - .planning/phases/05-plantnet-fourth-provider/05-RESEARCH.md § Code Examples (líneas 567-736 — 4 queries VERBATIM a copiar, con D-BIS)
    - .planning/phases/05-plantnet-fourth-provider/05-PATTERNS.md § 3 (líneas 242-315 — style guide de sección + separadores)
    - .planning/phases/05-plantnet-fourth-provider/05-CONTEXT.md § Addendum D-10/D-11 (para saber cómo escribir query B exactamente)
  </read_first>
  <files>docs/model-evaluation-queries.sql</files>
  <action>
    Modificar `docs/model-evaluation-queries.sql` en dos partes:

    **PARTE A — Actualizar header** (líneas 1-4):

    ANTES:
    ```sql
    -- ═══════════════════════════════════════════════════════════════════════════════
    -- Queries de evaluación de modelos — Supabase SQL Editor
    -- Tabla: model_evaluations (3 filas por búsqueda: claude, gemini, gpt4o)
    -- ═══════════════════════════════════════════════════════════════════════════════
    ```

    DESPUÉS:
    ```sql
    -- ═══════════════════════════════════════════════════════════════════════════════
    -- Queries de evaluación de modelos — Supabase SQL Editor
    -- Tabla: model_evaluations (4 filas por búsqueda desde Phase 5 2026-08-15:
    --                            claude, gemini, gpt4o, plantnet — el 4º es observador
    --                            silencioso, NO participa en consenso)
    -- ═══════════════════════════════════════════════════════════════════════════════
    ```

    **PARTE B — Añadir sección PHASE 5 AL FINAL** (después de la última query 12, sin tocar nada más):

    Añadir el bloque completo — COPIAR VERBATIM de RESEARCH.md líneas 574-723, con ajustes específicos en query B según D-10/D-11.

    ```sql


    -- ═══════════════════════════════════════════════════════════════════════════════
    -- PHASE 5 — PLANTNET COMO 4º PROVEEDOR (observador silencioso)
    -- Queries comparativas. Ejecutar contra datos generados desde 2026-08-15
    -- (fecha estimada de deploy — actualizar en el WHERE clause de cada query).
    -- ═══════════════════════════════════════════════════════════════════════════════


    -- ─────────────────────────────────────────────────────────────────────────────
    -- A. TASA DE ÉXITO POR MODELO (4-way, incluye plantnet)
    -- Mismo patrón que query 1 pero ahora con el 4º proveedor.
    -- ─────────────────────────────────────────────────────────────────────────────

    SELECT
      model,
      COUNT(*)                                             AS total_llamadas,
      COUNT(*) FILTER (WHERE success = true)               AS exitosas,
      COUNT(*) FILTER (WHERE success = false)              AS fallidas,
      ROUND(
        100.0 * COUNT(*) FILTER (WHERE success = true) / COUNT(*),
        1
      )                                                    AS tasa_exito_pct
    FROM model_evaluations
    WHERE created_at >= '2026-08-15'  -- ← desde deploy de Phase 5 (evitar comparación desigual)
    GROUP BY model
    ORDER BY model = 'plantnet' DESC, tasa_exito_pct DESC;


    -- ─────────────────────────────────────────────────────────────────────────────
    -- B. TOP-1 ACCURACY: PLANTNET VS LLMS CONTRA EL GOLDEN SET
    -- ⚠️  ADVERTENCIA (D-10): el golden set actual fue bootstrappeado con PlantNet
    -- (scripts/bootstrap-golden-set.ts, 2026-05-17). Ver
    -- .planning/phases/05-plantnet-fourth-provider/05-CONTEXT.md § Addendum D-10
    -- para caveat de interpretación. Esta query mostrará PlantNet ≈100% por
    -- construcción; el número relativo LLMs vs PlantNet solo es "coherencia
    -- PlantNet con su criterio original". Para métrica honesta, ver query D-BIS
    -- (divergencia PlantNet vs winner-LLM).
    -- ─────────────────────────────────────────────────────────────────────────────

    -- [AJUSTAR PER D-11 en 05-CONTEXT.md addendum]
    -- Si D-11 = B1 (tabla auxiliar public.golden_set_ground_truth): usar la query
    -- de abajo tal cual.
    -- Si D-11 = B2 (VALUES inline): reemplazar la CTE con VALUES.
    -- Si D-11 = B3 (JSON local): esta query queda como TODO — CPO corre script
    -- ad-hoc que genera el CSV, no SQL.
    -- Si D-10 = A2 (cambio a divergencia): OMITIR esta query, usar solo query D + D-BIS.

    WITH ground_truth AS (
      SELECT plant_search_id, scientific_name AS truth
      FROM public.golden_set_ground_truth  -- ← creada en plan 05-04-01 si D-11 = B1
    )
    SELECT
      me.model,
      COUNT(*)                                                             AS evaluaciones,
      COUNT(*) FILTER (WHERE LOWER(TRIM(me.scientific_name)) = LOWER(TRIM(gt.truth)))
                                                                           AS aciertos_exact,
      ROUND(
        100.0 * COUNT(*) FILTER (WHERE LOWER(TRIM(me.scientific_name)) = LOWER(TRIM(gt.truth)))
          / NULLIF(COUNT(*), 0),
        1
      )                                                                    AS accuracy_pct
    FROM model_evaluations me
    JOIN ground_truth gt ON gt.plant_search_id = me.plant_search_id
    WHERE me.success = true
    GROUP BY me.model
    ORDER BY me.model = 'plantnet' DESC, accuracy_pct DESC;


    -- ─────────────────────────────────────────────────────────────────────────────
    -- C. LATENCIA COMPARADA — PLANTNET VS LLMS
    -- Extiende query 2 (latencia) al 4º modelo. p50 y p90 sobre respuestas exitosas.
    -- Objetivo: verificar que PlantNet no es más lento que los LLMs (10s timeout).
    -- Verifica también PLANT-03 (latencia P95 no empeora).
    -- ─────────────────────────────────────────────────────────────────────────────

    SELECT
      model,
      COUNT(*)                                                          AS muestras,
      ROUND(AVG(response_ms))                                           AS media_ms,
      ROUND(PERCENTILE_CONT(0.50) WITHIN GROUP (ORDER BY response_ms))  AS p50_ms,
      ROUND(PERCENTILE_CONT(0.90) WITHIN GROUP (ORDER BY response_ms))  AS p90_ms,
      ROUND(PERCENTILE_CONT(0.95) WITHIN GROUP (ORDER BY response_ms))  AS p95_ms,
      MIN(response_ms)                                                  AS min_ms,
      MAX(response_ms)                                                  AS max_ms
    FROM model_evaluations
    WHERE success = true
      AND response_ms IS NOT NULL
      AND created_at >= '2026-08-15'
    GROUP BY model
    ORDER BY p50_ms ASC;


    -- ─────────────────────────────────────────────────────────────────────────────
    -- D. CASOS DE DIVERGENCIA — PLANTNET VS WINNER-LLM (detalle por búsqueda)
    -- Métrica más honesta que B (no depende del golden set contaminado).
    -- Muestra las búsquedas donde PlantNet dio un nombre distinto al del ganador
    -- LLM en la misma foto. Estos casos son candidatos a revisión manual — el
    -- disagreement es dato bruto para decidir Phase 5.1.
    -- ─────────────────────────────────────────────────────────────────────────────

    WITH winners AS (
      SELECT plant_search_id, scientific_name AS winner_name
      FROM model_evaluations
      WHERE is_winner = true AND success = true
    ),
    plantnet AS (
      SELECT plant_search_id, scientific_name AS plantnet_name, raw_name AS plantnet_raw
      FROM model_evaluations
      WHERE model = 'plantnet' AND success = true
    )
    SELECT
      w.plant_search_id,
      ps.image_url,
      w.winner_name                                          AS winner_llm_name,
      p.plantnet_name                                        AS plantnet_top1,
      p.plantnet_raw                                         AS plantnet_top1_raw,
      ps.created_at
    FROM winners w
    JOIN plantnet p ON p.plant_search_id = w.plant_search_id
    JOIN plant_searches ps ON ps.id = w.plant_search_id
    WHERE LOWER(TRIM(w.winner_name)) <> LOWER(TRIM(p.plantnet_name))
    ORDER BY ps.created_at DESC
    LIMIT 50;


    -- ─────────────────────────────────────────────────────────────────────────────
    -- D-BIS. RESUMEN DE DIVERGENCIA (agregado)
    -- % de búsquedas donde PlantNet y winner-LLM discrepan (ambos exitosos).
    -- El % es la señal directa que dispararía la evaluación de Phase 5.1 candidate.
    -- Trigger sugerido en ROADMAP: >10 puntos de diferencia sostenida.
    -- ─────────────────────────────────────────────────────────────────────────────

    WITH pairs AS (
      SELECT
        w.plant_search_id,
        LOWER(TRIM(w.scientific_name))  AS winner_name,
        LOWER(TRIM(p.scientific_name))  AS plantnet_name
      FROM model_evaluations w
      JOIN model_evaluations p ON p.plant_search_id = w.plant_search_id
      WHERE w.is_winner = true AND w.success = true
        AND p.model = 'plantnet' AND p.success = true
    )
    SELECT
      COUNT(*)                                                       AS busquedas_comparables,
      COUNT(*) FILTER (WHERE winner_name = plantnet_name)            AS coinciden,
      COUNT(*) FILTER (WHERE winner_name <> plantnet_name)           AS divergen,
      ROUND(
        100.0 * COUNT(*) FILTER (WHERE winner_name <> plantnet_name) / NULLIF(COUNT(*), 0),
        1
      )                                                              AS divergen_pct
    FROM pairs;
    ```

    **Si D-10 = A2 (cambio a divergencia)** — Skip query B, dejar comentario en su lugar:
    ```sql
    -- ─────────────────────────────────────────────────────────────────────────────
    -- B. TOP-1 ACCURACY — SKIPPED per D-10 = A2
    -- La CPO decidió cambiar la métrica de accuracy vs golden set (contaminado)
    -- a divergencia PlantNet vs winner-LLM. Ver query D + D-BIS abajo.
    -- ─────────────────────────────────────────────────────────────────────────────
    ```

    **Verificación manual post-edit:** Pegar cada query en Supabase SQL Editor y ejecutarla (opcional smoke test). Con ≥1 identificación post-deploy, Query A debe devolver 4 filas. Con 0 identificaciones aún, Query A devuelve tabla vacía sin error de sintaxis.

    **NO tocar** ninguna de las 12 queries existentes (líneas ~1-450 del archivo original). Solo update del header + append al final.
  </action>
  <verify>
    <automated>grep -q "PHASE 5 — PLANTNET COMO 4º PROVEEDOR" docs/model-evaluation-queries.sql && grep -q "A. TASA DE ÉXITO POR MODELO (4-way" docs/model-evaluation-queries.sql && grep -q "C. LATENCIA COMPARADA" docs/model-evaluation-queries.sql && grep -q "D-BIS" docs/model-evaluation-queries.sql && grep -q "4 filas por búsqueda" docs/model-evaluation-queries.sql && grep -q "05-CONTEXT.md.*Addendum" docs/model-evaluation-queries.sql</automated>
  </verify>
  <acceptance_criteria>
    - Header de `docs/model-evaluation-queries.sql` actualizado a "4 filas por búsqueda"
    - Sección `-- PHASE 5 — PLANTNET COMO 4º PROVEEDOR` presente al final
    - Query A (tasa de éxito 4-way) presente y sintácticamente válida
    - Query B presente (con caveat comment D-10 + ajuste per D-11) O reemplazada con comentario SKIPPED si D-10 = A2
    - Query C (latencia comparada) presente con PERCENTILE_CONT para p50/p90/p95
    - Query D (casos divergencia detalle) presente con LIMIT 50
    - Query D-BIS (resumen % divergencia) presente
    - Todas las queries son copy-pasteables (delimitadas por separadores `─`, sin dependencias externas más allá de la tabla auxiliar si D-11 = B1)
    - Comentarios explicativos en español para cada sección (patrón del archivo)
    - NO se modificaron las 12 queries existentes (verificable con `git diff docs/model-evaluation-queries.sql` — solo diff en header + append al final)
  </acceptance_criteria>
  <done>
    Sección PHASE 5 documentada y lista. La CPO abre Supabase SQL Editor, copia cualquiera de las queries A/B/C/D/D-BIS, obtiene números. Deliverable de análisis (Success Criterion #2 ROADMAP) completo.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CPO manual → Supabase SQL Editor | Queries read-only sobre `model_evaluations`. |
| (Opcional) Migration new table `golden_set_ground_truth` | Nueva superficie DB — cubierta por GRANTs explícitos + RLS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Elevation of Privilege | Nueva tabla `golden_set_ground_truth` (si D-11 = B1) | mitigate | GRANT explícitos a `authenticated` + `service_role` (per MIGRATION_CONVENTIONS.md — obligatorio post oct-2026). RLS enabled con policies mínimas (authenticated read/write). No PII: contiene solo IDs de fotos y nombres científicos. |
| T-05-04-02 | Information Disclosure | Query D expone `image_url` de plant_searches en el resultado | accept | La CPO es la única con acceso a Supabase SQL Editor (dashboard behind auth). `image_url` ya es URL pública (bucket público) — no filtra secrets. |
| T-05-04-03 | Tampering | Manipulación de `golden_set_ground_truth` cambia baseline de accuracy | mitigate | Cualquier authenticated user puede escribir. Riesgo bajo: solo la CPO tiene cuenta admin en producción. RLS + audit trail via `created_at` column. Alternativa más estricta: policy `to service_role` only, pero limita workflow CPO. |
| T-05-04-04 | Information Disclosure | Queries expuestas en docs/ committed en repo público | accept | El repo es privado (mi-plantita-feliz). Las queries no contienen secrets — solo shape de queries. Si el repo se abriera, no hay leak. |
</threat_model>

<verification>
- Header updated a "4 filas por búsqueda"
- 4 queries (A, B, C, D + D-BIS) presentes en sección PHASE 5
- Query B respeta decisión D-10/D-11 (caveat comment + tabla auxiliar si B1, o SKIPPED si A2)
- Si D-11 = B1: migración auxiliar aplicada con GRANTs + RLS (via `supabase db push`)
- 12 queries existentes intactas (git diff solo en header + append)
</verification>

<success_criteria>
- PLANT-02: `docs/model-evaluation-queries.sql` tiene sección PHASE 5 completa con 4 queries copy-pasteables
- Query B (o su equivalente per D-10 = A2) es interpretable sin ambigüedad
- CPO puede abrir Supabase SQL Editor tras el deploy de plan 05-03 y obtener números útiles (después de ≥5 identificaciones nuevas para tener signal)
</success_criteria>

<output>
After completion, create `.planning/phases/05-plantnet-fourth-provider/05-04-SUMMARY.md`
</output>
