---
phase: 05-plantnet-fourth-provider
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/identify-plant/consensus.ts
  - src/test/consensus.test.ts
autonomous: true
requirements: [PLANT-01]

must_haves:
  truths:
    - "Existe la función pura `matchScientific(a, b): 'exact' | 'normalized' | 'genus' | null` reutilizable por computeConsensus y por applyPlantnetOverride"
    - "computeConsensus sigue produciendo exactamente el mismo resultado que antes de este refactor (0 regresión en los tests actuales)"
    - "Existe la función pura `applyPlantnetOverride(llmWinner, plantnetResult): { winner, diverged, matchedLlm }` que implementa D-01 branches 1/2/3/4 y D-10 (threshold 0.8) y D-11 (solo exact/normalized cuentan; genus NO)"
    - "Los 4 branches de applyPlantnetOverride tienen tests unitarios que pasan"
    - "pickWinner NO se toca (los LLMs siguen decidiendo entre sí exactamente como hoy)"
  artifacts:
    - path: "supabase/functions/identify-plant/consensus.ts"
      provides: "matchScientific extraída como función pura + applyPlantnetOverride nueva"
      contains: "export function applyPlantnetOverride"
    - path: "src/test/consensus.test.ts"
      provides: "Tests unitarios para matchScientific + applyPlantnetOverride (mínimo 4 casos)"
      contains: "describe(\"applyPlantnetOverride\""
  key_links:
    - from: "supabase/functions/identify-plant/consensus.ts"
      to: "supabase/functions/identify-plant/index.ts (consumidor en Plan 03)"
      via: "import { applyPlantnetOverride } from './consensus.ts'"
      pattern: "export function applyPlantnetOverride"
    - from: "src/test/consensus.test.ts"
      to: "consensus.ts"
      via: "vitest suite"
      pattern: "npm run test.*consensus"
---

<objective>
Añadir la lógica de cross-validation de PlantNet a `consensus.ts` como funciones puras testables, sin tocar la mecánica de consenso de los 3 LLMs.

**Purpose:** Aísla la nueva regla D-01 (cross-validation) en funciones puras que:
1. Pueden testearse con vitest en Node (sin bootstrapear un Deno runtime),
2. No modifican el path de decisión LLM existente (pickWinner + computeConsensus intactos),
3. Se aplican como CAPA POSTERIOR al pickWinner (el edge function en Plan 03 hará `applyPlantnetOverride(llmWinner, plantnetResult)`).

Este plan corre en Wave 1 en paralelo con Plan 01 (migración) porque `consensus.ts` es lógica pura Deno/TS — no depende del schema.

**Output:**
- `consensus.ts` con función pública `matchScientific` extraída del matching inline de `computeConsensus`, y función pública `applyPlantnetOverride`
- `consensus.test.ts` con tests para ambas funciones nuevas + los tests existentes siguen pasando
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-plantnet-fourth-provider/05-CONTEXT.md
@.planning/phases/05-plantnet-fourth-provider/05-RESEARCH.md
@supabase/functions/identify-plant/consensus.ts
@src/test/consensus.test.ts

<interfaces>
<!-- Estado actual de consensus.ts (extraído verbatim) — el executor NO debe re-leer para explorar -->

Types actuales:
```typescript
export type ModelName = "claude" | "gemini" | "gpt4o";
export type ConsensusVerdict = "correct" | "no_consensus";
export type ConsensusMatchLevel = "exact" | "normalized" | "genus";

export interface ModelInput {
  model: ModelName;
  success: boolean;
  scientificName: string | null;
}

export interface ConsensusResult {
  verdict: ConsensusVerdict;
  matchLevel: ConsensusMatchLevel | null;
}
```

Funciones exportadas actuales (SIN MODIFICAR SU FIRMA):
- `extractScientificName(name: string): string | null` — parsea "Nombre común (Scientific name)"
- `normalizeScientificName(name: string): string` — strips cultivars/hybrids/infraspecific
- `extractGenus(normalized: string): string | null` — first word
- `computeConsensus(results: ModelInput[]): Map<ModelName, ConsensusResult>` — matching inline con 3 niveles

Lógica inline actual dentro de computeConsensus (líneas 122-143) que hay que EXTRAER como `matchScientific`:
```typescript
// Given two scientific names (lowercased), returns the best match tier
// or null if no match.
// Current inline logic:
//   if (a.raw === b.raw) → "exact"
//   else if (a.normalized === b.normalized && a.normalized !== "") → "normalized"
//   else if (a.genus !== null && a.genus === b.genus) → "genus"
//   else → null
```

Contrato de la función nueva `applyPlantnetOverride` (D-01):
```typescript
// Result que devuelve callPlantnetTimed (definido en Plan 03; aquí solo el tipo mínimo)
export interface PlantnetOverrideInput {
  success: boolean;                     // true si results[] no vacío
  scientificName: string | null;        // lowercased scientificNameWithoutAuthor de results[0]
  score: number | null;                 // results[0].score (0..1); null si failed
}

export interface PlantnetOverrideResult {
  winner: ModelInput & { scientificName: string | null };  // LLM winner (posiblemente con científico "corregido" por PlantNet)
  diverged: boolean;                    // true SOLO si score >= 0.8 y ningún LLM coincidió
  matchedLlm: ModelName | null;         // el LLM al que se alineó PlantNet, si aplica
}

export function applyPlantnetOverride(
  llmWinner: LlmWinnerInput,            // shape mínimo: { model, scientificName, ...opcional }
  llmResults: LlmWinnerInput[],         // los 3 LLMs para buscar match
  plantnetResult: PlantnetOverrideInput | null,
): PlantnetOverrideResult
```

Comportamiento esperado (D-01 branches):
1. **PlantNet null / failed / no score:** devolver `{ winner: llmWinner, diverged: false, matchedLlm: null }` (D-09)
2. **score < 0.8:** devolver `{ winner: llmWinner, diverged: false, matchedLlm: null }` (per redefinición en CONTEXT D-01 branch 3 tras el reshape — PlantNet solo cuenta como voto pasivo, no pisa; el consenso 3-LLM ya lo hizo pickWinner)
3. **score >= 0.8 Y algún LLM coincide con PlantNet vía `matchScientific = 'exact' | 'normalized'` (NO genus, D-11):** devolver `{ winner: {...llm_alineado, scientificName: plantnet.scientificName}, diverged: false, matchedLlm: <ese_model> }` — El LLM alineado aporta care/diagnosis/etc.; PlantNet impone su científico. Preferir el LLM que era originalmente `llmWinner` si coincide él mismo; si no, cualquier LLM con match (primero que aparezca en `llmResults`).
4. **score >= 0.8 Y ningún LLM coincide (exact ni normalized):** devolver `{ winner: llmWinner, diverged: true, matchedLlm: null }` — mandan los LLMs (coherentes consigo mismos), pero se marca la divergencia para análisis futuro (D-12).

Match `genus` NO cuenta para override (D-11): dentro de un género los cuidados pueden variar (Ficus lyrata vs Ficus benjamina).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extraer matchScientific como función pública en consensus.ts</name>
  <read_first>
    - supabase/functions/identify-plant/consensus.ts (líneas 94-161, computeConsensus completo)
    - src/test/consensus.test.ts (patrón de tests existentes con vitest)
  </read_first>
  <behavior>
    - `matchScientific('epipremnum aureum', 'epipremnum aureum')` → `'exact'`
    - `matchScientific("epipremnum aureum 'golden'", 'epipremnum aureum')` → `'normalized'` (normalización strips cultivar)
    - `matchScientific('ficus lyrata', 'ficus benjamina')` → `'genus'` (mismo primer word)
    - `matchScientific('epipremnum aureum', 'monstera deliciosa')` → `null`
    - `matchScientific(null, 'epipremnum aureum')` → `null` (input null)
    - `matchScientific('', 'epipremnum aureum')` → `null` (input empty)
    - `computeConsensus` sigue devolviendo EXACTAMENTE lo mismo antes/después del refactor (los 40+ tests existentes en consensus.test.ts siguen verdes)
  </behavior>
  <files>supabase/functions/identify-plant/consensus.ts, src/test/consensus.test.ts</files>
  <action>
**Refactor:** extraer la lógica de matching inline de `computeConsensus` (líneas 122-143) a una función pura pública. Preserva el comportamiento observable de `computeConsensus` bit-a-bit.

**Paso 1 — Añadir a `consensus.ts` justo después de `extractGenus` (aprox. línea 82), antes de `computeConsensus`:**

```typescript
// ─── Scientific name matching (D-11) ──────────────────────────────────────────

/**
 * Compares two lowercased scientific names and returns the best match tier
 * (exact > normalized > genus), or null if no match at any tier.
 *
 * - "exact": string equality after lowercase (input assumed already lowercased)
 * - "normalized": equality after stripping cultivars, hybrid markers,
 *   infraspecific ranks (spp/var/subsp/f/cv). Uses normalizeScientificName().
 * - "genus": same first word (genus) but different species/lower ranks.
 *
 * Returns null when either input is null/empty or when normalized/genus
 * comparison would compare against empty strings.
 *
 * Used by:
 *  - computeConsensus (LLM ↔ LLM matching, all 3 tiers count)
 *  - applyPlantnetOverride (PlantNet ↔ LLM matching, only exact + normalized
 *    count per Phase 5 D-11; genus is intentionally rejected because within
 *    the same genus care instructions can differ meaningfully — e.g.
 *    Ficus lyrata vs Ficus benjamina).
 */
export function matchScientific(
  a: string | null,
  b: string | null,
): ConsensusMatchLevel | null {
  if (!a || !b) return null;

  if (a === b) return "exact";

  const na = normalizeScientificName(a);
  const nb = normalizeScientificName(b);
  if (na !== "" && na === nb) return "normalized";

  const ga = extractGenus(na);
  const gb = extractGenus(nb);
  if (ga !== null && ga === gb) return "genus";

  return null;
}
```

**Paso 2 — Refactorizar `computeConsensus` para usar `matchScientific`:**

Reemplaza el bloque de matching inline (líneas 122-143 aprox., el bucle interior `for (const b of enriched)` que hace `a.raw === b.raw` / `a.normalized === b.normalized` / `a.genus === b.genus`) por una llamada a `matchScientific`. La estructura de "best tier so far, break si exact" se preserva:

```typescript
// Reemplazo del bloque interior (mantener la variable bestTier y la lógica "break on exact"):
for (const a of enriched) {
  let bestTier: ConsensusMatchLevel | null = null;

  for (const b of enriched) {
    if (a.model === b.model) continue;

    const tier = matchScientific(a.raw, b.raw);
    if (tier === null) continue;

    // Rank: exact > normalized > genus
    if (tier === "exact") {
      bestTier = "exact";
      break;
    }
    if (tier === "normalized" && bestTier !== "exact") {
      bestTier = "normalized";
    } else if (tier === "genus" && bestTier === null) {
      bestTier = "genus";
    }
  }

  matchResults.set(a.model, bestTier);
}
```

**IMPORTANTE:** Preservar la estructura de `enriched` con `raw`, `normalized`, `genus` NO es necesaria si `matchScientific` normaliza internamente — pero por eficiencia (evitar 2× normalizar en un loop de 3×3) puedes mantener el enriched como está y llamar `matchScientific(a.raw, b.raw)` que sí duplica trabajo. Con 3 modelos son 9 comparaciones + 6 llamadas efectivas × 2 normalizaciones = 12 llamadas a `normalizeScientificName` (regex sobre strings ~30 chars). Aceptable — la simplicidad de código gana. NO optimizar prematuramente.

**Paso 3 — Añadir tests en `src/test/consensus.test.ts`:**

Añade después del describe de `extractGenus` (aprox. línea 100 tras leer el archivo completo — el executor debe leer el archivo entero para localizar el punto de inserción exacto):

```typescript
// ─── matchScientific ──────────────────────────────────────────────────────────

describe("matchScientific", () => {
  it("returns 'exact' when strings are identical", () => {
    expect(matchScientific("epipremnum aureum", "epipremnum aureum")).toBe("exact");
  });

  it("returns 'normalized' when only differ by cultivar", () => {
    expect(matchScientific("epipremnum aureum 'golden'", "epipremnum aureum")).toBe("normalized");
  });

  it("returns 'normalized' when only differ by infraspecific rank", () => {
    expect(matchScientific("hedera helix var. hibernica", "hedera helix")).toBe("normalized");
  });

  it("returns 'genus' when same first word but different species", () => {
    expect(matchScientific("ficus lyrata", "ficus benjamina")).toBe("genus");
  });

  it("returns null when different genus", () => {
    expect(matchScientific("epipremnum aureum", "monstera deliciosa")).toBeNull();
  });

  it("returns null when either input is null", () => {
    expect(matchScientific(null, "epipremnum aureum")).toBeNull();
    expect(matchScientific("epipremnum aureum", null)).toBeNull();
  });

  it("returns null when either input is empty string", () => {
    expect(matchScientific("", "epipremnum aureum")).toBeNull();
  });
});
```

Actualiza el import al principio del archivo para incluir `matchScientific`:
```typescript
import {
  extractScientificName,
  normalizeScientificName,
  extractGenus,
  computeConsensus,
  matchScientific,  // ← nuevo
  applyPlantnetOverride,  // ← nuevo (Task 2 lo usará)
} from "../../supabase/functions/identify-plant/consensus.ts";
```

**Paso 4 — Verificar que los tests existentes siguen verdes:**

```bash
npm run test -- --run src/test/consensus.test.ts
```

Debe pasar TODOS los tests (los 40+ existentes + los 7 nuevos de matchScientific). Si algún test de `computeConsensus` falla, la extracción cambió el comportamiento observable — revisar el refactor.
  </action>
  <verify>
    <automated>grep -q "export function matchScientific" supabase/functions/identify-plant/consensus.ts &amp;&amp; grep -q "describe(\"matchScientific\"" src/test/consensus.test.ts &amp;&amp; npm run test -- --run src/test/consensus.test.ts 2>&amp;1 | tail -20 | grep -qE "Test Files.*passed|✓" &amp;&amp; echo "OK: matchScientific extraída, tests verdes"</automated>
  </verify>
  <acceptance_criteria>
    - `matchScientific` está exportada en `consensus.ts`
    - `computeConsensus` usa `matchScientific` internamente (verifica: la lógica inline `a.raw === b.raw` ya no aparece bruta en el bloque interior — buscar `grep -c "a\.raw === b\.raw" supabase/functions/identify-plant/consensus.ts` debe ser 0)
    - `src/test/consensus.test.ts` tiene un describe nuevo para `matchScientific` con ≥7 casos
    - `npm run test -- --run src/test/consensus.test.ts` verde (todos, existentes + nuevos)
  </acceptance_criteria>
  <done>matchScientific extraída como función pura pública, computeConsensus refactorizado sin regresión, tests unitarios pasan.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Añadir applyPlantnetOverride en consensus.ts (D-01, D-10, D-11)</name>
  <read_first>
    - supabase/functions/identify-plant/consensus.ts (post-Task-1, para conocer los exports)
    - .planning/phases/05-plantnet-fourth-provider/05-CONTEXT.md § "Rol de PlantNet" (D-01 branches, D-10, D-11)
  </read_first>
  <behavior>
    - **Branch 1 (PlantNet failed/null):** `applyPlantnetOverride(winner, results, null)` → `{ winner, diverged: false, matchedLlm: null }`
    - **Branch 1 bis (PlantNet success=false):** input con `success: false` → mismo comportamiento que null
    - **Branch 2 (score < 0.8):** input score=0.7 → `{ winner (original), diverged: false, matchedLlm: null }`
    - **Branch 3 (score ≥ 0.8, match exact con winner mismo):** winner llm ya alineado con PlantNet → `{ winner: {...winner, scientificName: plantnet.scientificName}, diverged: false, matchedLlm: winner.model }`
    - **Branch 3 bis (score ≥ 0.8, match normalized con otro LLM ≠ winner):** ningún LLM winner original coincide pero otro LLM sí → `{ winner: <ese_llm_alineado con scientificName=plantnet.scientificName>, diverged: false, matchedLlm: <ese_llm.model> }`
    - **Branch 3 ter (score ≥ 0.8, match genus solo):** ningún LLM matchea exact/normalized pero uno matchea genus → NO se considera match (D-11 rechaza genus) → cae al branch 4
    - **Branch 4 (score ≥ 0.8, ningún match exact/normalized):** → `{ winner (original), diverged: true, matchedLlm: null }` — se preserva LLM winner + se marca divergencia
    - **Preferencia:** si múltiples LLMs matchean, preferir el que ya era `llmWinner`; si el winner no matchea pero otros sí, tomar el primer LLM del array que matchee
    - **Edge case (score null / undefined con success=true):** tratar como score < 0.8 (branch 2) — nunca override sin score confiable
  </behavior>
  <files>supabase/functions/identify-plant/consensus.ts, src/test/consensus.test.ts</files>
  <action>
**Añadir al final de `consensus.ts` (después de `computeConsensus`):**

```typescript
// ─── PlantNet cross-validation override (Phase 5 D-01) ────────────────────────

export const PLANTNET_OVERRIDE_SCORE_THRESHOLD = 0.8;  // D-10

export interface PlantnetOverrideInput {
  success: boolean;
  scientificName: string | null;   // lowercased scientificNameWithoutAuthor
  score: number | null;            // 0..1
}

/**
 * Minimal shape needed from an LLM result for the override to work.
 * We deliberately DO NOT depend on the edge function's ModelResult type
 * (which lives in index.ts and has non-portable fields like plantInfo).
 * The edge function passes objects that satisfy this shape.
 */
export interface LlmWinnerInput {
  model: ModelName;
  scientificName: string | null;
}

export interface PlantnetOverrideResult<T extends LlmWinnerInput = LlmWinnerInput> {
  /** The final winner. Same identity as `llmWinner` OR a different LLM's row
   *  (from `llmResults`), with `scientificName` potentially replaced by
   *  PlantNet's canonical name in override cases. */
  winner: T;
  /** True only when score >= threshold AND no LLM matched (branch 4). */
  diverged: boolean;
  /** The model name of the LLM PlantNet aligned with, if any (branch 3). */
  matchedLlm: ModelName | null;
}

/**
 * Applies PlantNet's vote as an override layer on top of the LLM consensus.
 * See Phase 5 CONTEXT.md D-01 for the full decision tree.
 *
 * Rules (D-01 branches):
 *   1. PlantNet failed / null / no score → return llmWinner untouched (D-09).
 *   2. score < 0.8 → return llmWinner untouched (below the override threshold).
 *   3. score >= 0.8 AND at least one LLM matches (via matchScientific at
 *      'exact' or 'normalized' tier — genus NOT allowed per D-11) → return
 *      the matched LLM with its scientificName replaced by PlantNet's.
 *      If multiple LLMs match, prefer llmWinner itself; otherwise the first
 *      matching LLM in llmResults.
 *   4. score >= 0.8 AND no LLM matches → return llmWinner untouched but
 *      mark diverged=true so the caller can flag plant_searches.plantnet_diverged
 *      and dispatch the PostHog event (D-12).
 *
 * Pure function — no side effects. Testable in isolation.
 *
 * NOTE on D-11: only 'exact' and 'normalized' tiers count as a match here.
 * The 'genus' tier is intentionally rejected because within the same genus
 * care instructions can differ meaningfully (e.g. Ficus lyrata vs Ficus benjamina).
 * Accepting genus match would break the coherence guarantee between the
 * displayed scientific name and the LLM-provided care fields.
 */
export function applyPlantnetOverride<T extends LlmWinnerInput>(
  llmWinner: T,
  llmResults: T[],
  plantnetResult: PlantnetOverrideInput | null,
): PlantnetOverrideResult<T> {
  // Branch 1: PlantNet unavailable
  if (
    plantnetResult === null ||
    !plantnetResult.success ||
    plantnetResult.scientificName === null ||
    plantnetResult.score === null ||
    !Number.isFinite(plantnetResult.score)
  ) {
    return { winner: llmWinner, diverged: false, matchedLlm: null };
  }

  // Branch 2: below threshold
  if (plantnetResult.score < PLANTNET_OVERRIDE_SCORE_THRESHOLD) {
    return { winner: llmWinner, diverged: false, matchedLlm: null };
  }

  // Branches 3 / 3-bis / 4: threshold met, look for a match
  const plantnetSci = plantnetResult.scientificName;

  const isValidMatch = (llmSci: string | null): boolean => {
    const tier = matchScientific(llmSci, plantnetSci);
    // D-11: only exact or normalized count; genus does not.
    return tier === "exact" || tier === "normalized";
  };

  // Prefer llmWinner if it matches
  if (isValidMatch(llmWinner.scientificName)) {
    return {
      winner: { ...llmWinner, scientificName: plantnetSci },
      diverged: false,
      matchedLlm: llmWinner.model,
    };
  }

  // Otherwise pick the first LLM (in input order) that matches
  const alignedLlm = llmResults.find((r) => r !== llmWinner && isValidMatch(r.scientificName));
  if (alignedLlm) {
    return {
      winner: { ...alignedLlm, scientificName: plantnetSci },
      diverged: false,
      matchedLlm: alignedLlm.model,
    };
  }

  // Branch 4: no match → preserve LLM winner, mark divergence
  return { winner: llmWinner, diverged: true, matchedLlm: null };
}
```

**Añadir tests en `src/test/consensus.test.ts` (al final del archivo):**

```typescript
// ─── applyPlantnetOverride (Phase 5 D-01) ─────────────────────────────────────

describe("applyPlantnetOverride", () => {
  const llmA = { model: "claude" as const, scientificName: "monstera deliciosa" };
  const llmB = { model: "gemini" as const, scientificName: "epipremnum aureum" };
  const llmC = { model: "gpt4o" as const,  scientificName: "ficus lyrata" };
  const llms = [llmA, llmB, llmC];

  it("returns winner untouched when plantnet is null (D-09)", () => {
    const r = applyPlantnetOverride(llmA, llms, null);
    expect(r.winner).toBe(llmA);
    expect(r.diverged).toBe(false);
    expect(r.matchedLlm).toBeNull();
  });

  it("returns winner untouched when plantnet.success is false", () => {
    const r = applyPlantnetOverride(llmA, llms, {
      success: false, scientificName: null, score: null,
    });
    expect(r.winner).toBe(llmA);
    expect(r.diverged).toBe(false);
  });

  it("returns winner untouched when score below 0.8 threshold (branch 2)", () => {
    const r = applyPlantnetOverride(llmA, llms, {
      success: true, scientificName: "monstera deliciosa", score: 0.7,
    });
    expect(r.winner).toBe(llmA);
    expect(r.diverged).toBe(false);
    expect(r.matchedLlm).toBeNull();
  });

  it("overrides scientific name when score >= 0.8 and winner matches exact (branch 3)", () => {
    const r = applyPlantnetOverride(llmA, llms, {
      success: true, scientificName: "monstera deliciosa", score: 0.95,
    });
    expect(r.winner.model).toBe("claude");
    expect(r.winner.scientificName).toBe("monstera deliciosa");
    expect(r.diverged).toBe(false);
    expect(r.matchedLlm).toBe("claude");
  });

  it("picks aligned LLM (not winner) when winner does not match but another does (branch 3-bis)", () => {
    // winner is llmC (ficus lyrata), plantnet says monstera → llmA matches
    const r = applyPlantnetOverride(llmC, llms, {
      success: true, scientificName: "monstera deliciosa", score: 0.92,
    });
    expect(r.winner.model).toBe("claude");
    expect(r.winner.scientificName).toBe("monstera deliciosa");
    expect(r.diverged).toBe(false);
    expect(r.matchedLlm).toBe("claude");
  });

  it("accepts normalized match (cultivar stripped)", () => {
    const winnerWithCultivar = { model: "gemini" as const, scientificName: "epipremnum aureum 'golden'" };
    const set = [llmA, winnerWithCultivar, llmC];
    const r = applyPlantnetOverride(winnerWithCultivar, set, {
      success: true, scientificName: "epipremnum aureum", score: 0.9,
    });
    expect(r.winner.model).toBe("gemini");
    expect(r.winner.scientificName).toBe("epipremnum aureum");
    expect(r.matchedLlm).toBe("gemini");
  });

  it("REJECTS genus-only match — falls through to divergence (D-11)", () => {
    // llmC = ficus lyrata; plantnet says ficus benjamina (same genus, different species)
    const r = applyPlantnetOverride(llmC, llms, {
      success: true, scientificName: "ficus benjamina", score: 0.9,
    });
    expect(r.winner).toBe(llmC);           // preserved
    expect(r.diverged).toBe(true);          // divergence recorded
    expect(r.matchedLlm).toBeNull();        // no valid match
  });

  it("marks diverged=true when score >= 0.8 and no LLM matches (branch 4)", () => {
    const r = applyPlantnetOverride(llmA, llms, {
      success: true, scientificName: "sansevieria trifasciata", score: 0.88,
    });
    expect(r.winner).toBe(llmA);
    expect(r.diverged).toBe(true);
    expect(r.matchedLlm).toBeNull();
  });

  it("uses threshold 0.8 exactly (score = 0.8 counts as >= threshold)", () => {
    const r = applyPlantnetOverride(llmA, llms, {
      success: true, scientificName: "monstera deliciosa", score: 0.8,
    });
    expect(r.matchedLlm).toBe("claude");    // 0.8 is inclusive
  });
});
```

Ejecutar tests para verificar:
```bash
npm run test -- --run src/test/consensus.test.ts
```
  </action>
  <verify>
    <automated>grep -q "export function applyPlantnetOverride" supabase/functions/identify-plant/consensus.ts &amp;&amp; grep -q "PLANTNET_OVERRIDE_SCORE_THRESHOLD" supabase/functions/identify-plant/consensus.ts &amp;&amp; grep -q "describe(\"applyPlantnetOverride\"" src/test/consensus.test.ts &amp;&amp; npm run test -- --run src/test/consensus.test.ts 2>&amp;1 | tail -20 | grep -qE "Test Files.*passed|✓" &amp;&amp; echo "OK: applyPlantnetOverride + tests verdes"</automated>
  </verify>
  <acceptance_criteria>
    - `applyPlantnetOverride` exportada en `consensus.ts`
    - Constante `PLANTNET_OVERRIDE_SCORE_THRESHOLD` = 0.8 exportada
    - Interfaces `PlantnetOverrideInput`, `LlmWinnerInput`, `PlantnetOverrideResult` exportadas
    - Uso de `matchScientific` internamente (D-11: filtra a exact + normalized; rechaza genus)
    - `describe("applyPlantnetOverride"...)` en tests con mínimo 8 casos (los 8 listados: null, success=false, score bajo, exact/winner, normalized/otro, cultivar, genus rechazado, no match, threshold inclusive)
    - `npm run test -- --run src/test/consensus.test.ts` verde
    - `pickWinner` en `supabase/functions/identify-plant/index.ts` NO se ha tocado (verifica: `grep -c "function pickWinner" supabase/functions/identify-plant/index.ts` sigue siendo 1 y `git diff supabase/functions/identify-plant/index.ts` está vacío)
  </acceptance_criteria>
  <done>applyPlantnetOverride implementada y testeada exhaustivamente; el edge function (Plan 03) puede importarla y usarla.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consensus.ts (Deno + vitest Node) | Función pura consumida por edge function; sus outputs afectan qué nombre científico y qué cuidados ve el usuario |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering | applyPlantnetOverride devuelve winner con scientificName "corregido" incoherente con care/diagnosis | mitigate | Los 4 branches se cubren por tests. La regla D-11 (rechazar genus) es el freno principal: solo se sustituye el científico si LLM matched at exact/normalized (mismo epíteto específico, no solo mismo género). El test "REJECTS genus-only match" verifica que Ficus lyrata + PlantNet=Ficus benjamina NO override. |
| T-05-02-02 | Information Disclosure | Test cases exponen datos reales de usuarios | accept | Los mock inputs son nombres científicos genéricos (monstera deliciosa, etc.), no PII. |
| T-05-02-03 | Tampering | Refactor de computeConsensus rompe el consenso LLM sin que nos demos cuenta | mitigate | Los 40+ tests existentes de consensus.test.ts se ejecutan sin cambios y deben seguir verdes. Cualquier regresión rompe el CI. |
| T-05-02-04 | Denial of Service | Bug en applyPlantnetOverride que lanza exception con inputs edge case (score NaN, scientificName undefined con success=true) | mitigate | Función maneja explícitamente `!Number.isFinite(score)` y `scientificName === null`. Test explícito de score=0.8 exacto (inclusive). El caller (Plan 03) además está envuelto en try/catch general del handler; una exception aquí caería en el catch → fallo silencioso, no crash del edge fn. |
| T-05-02-05 | Repudiation | Cambios de override no dejan traza para debugging | accept | El caller (Plan 03) es responsable de loggear + flag DB + PostHog event. Esta función es pura, sin side effects. |
</threat_model>

<verification>
- `consensus.ts` compila (implícito en `npm run test` que usa el mismo transpiler)
- Los tests existentes de consensus siguen todos verdes tras el refactor (`npm run test -- --run src/test/consensus.test.ts`)
- Los tests nuevos de `matchScientific` (≥7) pasan
- Los tests nuevos de `applyPlantnetOverride` (≥8) pasan
- `pickWinner` y el resto de `index.ts` NO se han modificado en este plan
</verification>

<success_criteria>
1. `matchScientific` extraída como función pública sin regresión en `computeConsensus`
2. `applyPlantnetOverride` implementa D-01 branches 1/2/3/3-bis/4 correctamente con threshold D-10 = 0.8 y regla D-11 (rechaza genus)
3. Tests unitarios ≥15 nuevos casos verdes
4. `pickWinner` y la mecánica de consenso LLM permanecen intactos
5. `npm run test -- --run` (todo el suite) pasa
</success_criteria>

<output>
Al terminar, crea `.planning/phases/05-plantnet-fourth-provider/05-02-SUMMARY.md` con:
- Firma exacta de `matchScientific` y `applyPlantnetOverride`
- Número de tests nuevos añadidos y todos pasan
- Confirmación de que `pickWinner` no se tocó
</output>
