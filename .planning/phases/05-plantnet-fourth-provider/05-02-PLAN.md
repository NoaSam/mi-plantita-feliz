---
phase: 05-plantnet-fourth-provider
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - supabase/migrations/20260815000000_extend_model_evaluations_for_plantnet.sql
  - src/integrations/supabase/types.ts
autonomous: false
requirements: [PLANT-01]
tags: [migration, schema, blocking, supabase-cli, types-regen]

user_setup:
  - service: plantnet
    why: "PlantNet API v2 requiere API key en Function Secret para que el edge function pueda autenticarse (Plan 05-03 lo consume)"
    env_vars:
      - name: PLANTNET_API_KEY
        source: "PlantNet Dashboard -> https://my.plantnet.org/account/api (crear cuenta si no existe, generar key)"
    dashboard_config:
      - task: "Añadir PLANTNET_API_KEY a Supabase Function Secrets"
        location: "Supabase Dashboard -> Project Settings -> Edge Functions -> Secrets"

must_haves:
  truths:
    - "La tabla `model_evaluations` acepta `model = 'plantnet'` sin violar CHECK constraint"
    - "La tabla `model_evaluations` tiene columna `raw_response jsonb` nullable"
    - "Los TypeScript types en `src/integrations/supabase/types.ts` reflejan el schema nuevo (raw_response + plantnet en union)"
    - "El secret PLANTNET_API_KEY está disponible en Supabase Function Secrets para runtime en plan 05-03"
  artifacts:
    - path: "supabase/migrations/20260815000000_extend_model_evaluations_for_plantnet.sql"
      provides: "Migración ALTER TABLE que widen CHECK constraint + add raw_response column"
      contains: "add column if not exists raw_response jsonb"
    - path: "src/integrations/supabase/types.ts"
      provides: "TypeScript types actualizados"
      contains: "raw_response"
  key_links:
    - from: "supabase db push"
      to: "supabase.model_evaluations.model CHECK constraint"
      via: "widening constraint permite INSERT con model='plantnet' en plan 05-03"
      pattern: "model in \\('claude', 'gemini', 'gpt4o', 'plantnet'\\)"
    - from: "PLANTNET_API_KEY en Function Secrets"
      to: "Deno.env.get('PLANTNET_API_KEY') en edge function"
      via: "Supabase Functions runtime inyecta secrets como env vars"
      pattern: "PLANTNET_API_KEY"
---

<objective>
Extender el schema de `model_evaluations` para soportar PlantNet como 4º proveedor: (1) widen del CHECK constraint sobre `model` de 3 a 4 valores, (2) añadir columna `raw_response jsonb` para el JSON completo (D-03). Aplicar la migración a la DB remota vía `supabase db push` (BLOCKING — sin esto, el edge function de plan 05-03 fallaría con violación de constraint), y regenerar TypeScript types. En paralelo, la CPO añade `PLANTNET_API_KEY` a Function Secrets.

Purpose: Foundation schema-level. Sin este plan, plan 05-03 (edge function) no puede persistir la fila de PlantNet — el INSERT fallaría con `model_evaluations_model_check` violation.

Output:
- Nueva migración `20260815000000_extend_model_evaluations_for_plantnet.sql`
- `supabase/migrations/*.sql` aplicadas en DB remota vía `supabase db push`
- `src/integrations/supabase/types.ts` regenerado con `raw_response: Json | null` + `model: 'claude' | 'gemini' | 'gpt4o' | 'plantnet'`
- `PLANTNET_API_KEY` configurado en Supabase Function Secrets
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-plantnet-fourth-provider/05-CONTEXT.md
@.planning/phases/05-plantnet-fourth-provider/05-RESEARCH.md
@.planning/phases/05-plantnet-fourth-provider/05-PATTERNS.md
@supabase/MIGRATION_CONVENTIONS.md
@supabase/migrations/20260413000000_add_consensus_match_level.sql
@supabase/migrations/20260407000000_create_model_evaluations.sql

<interfaces>
<!-- Schema actual de model_evaluations (verificado en migración 20260407000000) -->
<!-- Estas son las columnas que el edge function de plan 05-03 va a poblar en la 4ª fila (plantnet) -->

Columnas actuales de `model_evaluations`:
- `id uuid PK`
- `plant_search_id uuid FK -> plant_searches(id)`
- `model text NOT NULL CHECK (model in ('claude', 'gemini', 'gpt4o'))` ← esta phase widen a 4 valores
- `raw_name text`
- `scientific_name text`
- `description text`, `care text`, `diagnosis text` ← NULL para plantnet
- `response_ms integer`
- `success boolean NOT NULL DEFAULT false`
- `error_message text`
- `is_winner boolean NOT NULL DEFAULT false` ← siempre false para plantnet (D-01)
- `consensus_group text CHECK (in ('correct', 'no_consensus'))` ← NULL para plantnet (D-01)
- `consensus_match_level text CHECK (in ('exact', 'normalized', 'genus'))` ← añadido en 20260413, NULL para plantnet
- `created_at timestamptz NOT NULL DEFAULT now()`

Nueva columna añadida por esta phase:
- `raw_response jsonb` ← nullable; NULL para LLMs y para plantnet failures; JSON completo para plantnet success
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear migración SQL que widen CHECK constraint + add raw_response column</name>
  <read_first>
    - supabase/MIGRATION_CONVENTIONS.md § "Para migraciones que NO crean tablas" (líneas 54-55)
    - supabase/migrations/20260413000000_add_consensus_match_level.sql (analog exacto — ALTER TABLE + add column + `if not exists`)
    - supabase/migrations/20260407000000_create_model_evaluations.sql (línea 8 — CHECK constraint actual)
    - .planning/phases/05-plantnet-fourth-provider/05-RESEARCH.md líneas 423-446 (SQL verbatim a copiar)
    - .planning/phases/05-plantnet-fourth-provider/05-PATTERNS.md líneas 196-238 (migration pattern + naming timestamp)
  </read_first>
  <files>supabase/migrations/20260815000000_extend_model_evaluations_for_plantnet.sql</files>
  <action>
    Crear archivo `supabase/migrations/20260815000000_extend_model_evaluations_for_plantnet.sql` con contenido EXACTO (copiado verbatim de RESEARCH.md líneas 423-446):

    ```sql
    -- Widen the model column CHECK constraint to accept 'plantnet' as a 4th value.
    -- PlantNet runs as a silent observer alongside the 3 LLMs (Phase 5 D-01).
    -- It does NOT vote in consensus; its rows always have consensus_group = NULL
    -- and is_winner = false.
    alter table public.model_evaluations
      drop constraint if exists model_evaluations_model_check;

    alter table public.model_evaluations
      add constraint model_evaluations_model_check
      check (model in ('claude', 'gemini', 'gpt4o', 'plantnet'));

    -- Add raw_response column for PlantNet's full JSON payload (Phase 5 D-03).
    -- Nullable because existing rows (claude/gemini/gpt4o) don't populate it,
    -- and PlantNet rows have NULL when the API call failed (silent-failure mode, D-09).
    alter table public.model_evaluations
      add column if not exists raw_response jsonb;

    -- Optional index for future JSON introspection queries (e.g., top-3 accuracy).
    -- Skip if analytics queries only ever touch raw_name / scientific_name — that
    -- covers 100% of the D-07 queries. Add later if needed.
    -- create index if not exists model_evaluations_raw_response_gin
    --   on public.model_evaluations using gin (raw_response)
    --   where model = 'plantnet';
    ```

    NO añadir GRANT statements — `model_evaluations` es tabla existente (creada 2026-04-07) protegida por la cláusula "existing tables keep their grants" per `supabase/MIGRATION_CONVENTIONS.md` líneas 75-83. Sólo `CREATE TABLE` en `public` requiere los GRANT nuevos post-oct-2026.

    NO habilitar RLS — ya está habilitado en la migración original de 2026-04-07.

    Timestamp `20260815000000` es consistente: la última migración es `20260518000000`; hoy es 2026-08-15.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260815000000_extend_model_evaluations_for_plantnet.sql && grep -q "add column if not exists raw_response jsonb" supabase/migrations/20260815000000_extend_model_evaluations_for_plantnet.sql && grep -q "check (model in ('claude', 'gemini', 'gpt4o', 'plantnet'))" supabase/migrations/20260815000000_extend_model_evaluations_for_plantnet.sql && ! grep -q "^grant " supabase/migrations/20260815000000_extend_model_evaluations_for_plantnet.sql</automated>
  </verify>
  <acceptance_criteria>
    - Archivo existe en la ruta exacta
    - Contiene `drop constraint if exists model_evaluations_model_check`
    - Contiene `add constraint model_evaluations_model_check check (model in ('claude', 'gemini', 'gpt4o', 'plantnet'))`
    - Contiene `add column if not exists raw_response jsonb`
    - NO contiene `grant ` (statements — es ALTER TABLE, no CREATE TABLE)
    - NO contiene `enable row level security` (RLS ya activo)
    - Los comentarios `--` explican qué + por qué (D-01, D-03, D-09)
  </acceptance_criteria>
  <done>
    Migración SQL creada, sintaxis Postgres válida, idempotente (drop constraint if exists, add column if not exists), sin GRANT statements innecesarios, comentarios en inglés (código) referencian decisiones D-01/D-03/D-09.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] CPO añade PLANTNET_API_KEY a Supabase Function Secrets</name>
  <what-built>
    Plan 05-03 (edge function extension) va a leer `Deno.env.get("PLANTNET_API_KEY")`. Sin este secret configurado, cada llamada a PlantNet devolverá `{success: false, errorMessage: "PLANTNET_API_KEY not configured"}` — coherente con failure silencioso (D-09), pero waste (todas las filas plantnet quedarían con `raw_response = NULL`).

    Este es checkpoint `human-action` legítimo: crear cuenta PlantNet + generar API key requiere interacción humana con dashboards externos que no tienen CLI/API públicos.
  </what-built>
  <how-to-verify>
    Pasos a ejecutar por la CPO:

    1. Si no tienes cuenta PlantNet, crear una en https://my.plantnet.org/register (email + password, ~2 min)
    2. Ir a https://my.plantnet.org/account/api
    3. Copiar la API key mostrada (o generarla si no existe)
    4. Abrir Supabase Dashboard del proyecto (https://supabase.com/dashboard/project/{project_id}/settings/functions)
    5. En "Edge Functions" -> "Secrets" -> "Add new secret":
       - Name: `PLANTNET_API_KEY`
       - Value: la key copiada
    6. Guardar. Verificar que aparece en la lista de secrets (Supabase enmascara el valor tras guardar)

    Verificación post-setup (opcional, desde local CLI):
    ```bash
    supabase secrets list
    # Debe aparecer PLANTNET_API_KEY (valor enmascarado como '****')
    ```

    Free tier PlantNet = 500 requests/día, más que suficiente para el volumen actual (15-30/día, D-08).
  </how-to-verify>
  <resume-signal>Escribe "listo" cuando el secret esté configurado, o "skip" si prefieres desplegar sin él (todas las filas plantnet quedarán con raw_response=NULL hasta añadirlo)</resume-signal>
  <files>N/A — checkpoint puramente humano (config en Supabase Dashboard externo).</files>
  <action>
    PAUSAR EJECUCIÓN. Presentar a la CPO los pasos concretos de configuración (ver bloque `<how-to-verify>` arriba) para añadir `PLANTNET_API_KEY` a Supabase Function Secrets. NO proceder a Task 3 (supabase db push) hasta que la CPO confirme "listo" o "skip".

    Si "skip": documentar en el SUMMARY que el edge function del plan 05-03 devolverá siempre `PLANTNET_API_KEY not configured` hasta que se añada el secret; el resto del plan procede pero las filas plantnet quedarán con raw_response=NULL (coherente con D-09 pero waste).
  </action>
  <verify>
    <automated>echo "checkpoint task — verificación manual: CPO ha confirmado listo/skip. Post-setup opcional: 'supabase secrets list | grep PLANTNET_API_KEY'"</automated>
  </verify>
  <done>La CPO ha confirmado "listo" (secret añadido) o "skip" (proceder sin secret, con la implicación documentada).</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Push migración a la DB remota (supabase db push)</name>
  <read_first>
    - supabase/migrations/20260815000000_extend_model_evaluations_for_plantnet.sql (creada en Task 1)
    - .planning/phases/05-plantnet-fourth-provider/05-RESEARCH.md § Common Pitfalls #3 (líneas 515-523 — deploy order matters)
  </read_first>
  <files>supabase/migrations/20260815000000_extend_model_evaluations_for_plantnet.sql</files>
  <action>
    Ejecutar `supabase db push` para aplicar la migración a la DB remota. Este paso es BLOCKING per instrucciones del planner: sin el push, el edge function de plan 05-03 (que va a hacer INSERT con `model = 'plantnet'`) fallaría con violación de `model_evaluations_model_check` constraint.

    Comando exacto:
    ```bash
    supabase db push
    ```

    Si el CLI pide confirmación (`Do you want to push these migrations to the remote database? [Y/n]`), responder `y`.

    Si el CLI falla con "not linked to a project", ejecutar antes:
    ```bash
    supabase link --project-ref <project_id>
    ```

    Verificar post-push que la migración se aplicó consultando la DB remota:
    ```bash
    supabase db execute --linked "SELECT column_name FROM information_schema.columns WHERE table_name = 'model_evaluations' AND column_name = 'raw_response'"
    ```

    Debe devolver 1 fila. Si devuelve 0, el push no aplicó — investigar (probablemente `SUPABASE_ACCESS_TOKEN` no configurado o el proyecto no está linked).

    Non-TTY note: si el push requiere prompts interactivos, verificar que `SUPABASE_ACCESS_TOKEN` env var está seteado.
  </action>
  <verify>
    <automated>supabase migration list --linked 2>/dev/null | grep -q "20260815000000" || (echo "MIGRATION NOT APPLIED — check supabase link and access token"; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `supabase migration list --linked` incluye `20260815000000` como aplicada
    - Query directa a la DB remota confirma que la columna `raw_response jsonb` existe en `model_evaluations`
    - Query directa confirma que `INSERT INTO model_evaluations (model) VALUES ('plantnet')` no viola constraint (test opcional, se puede diferir a plan 05-03)
  </acceptance_criteria>
  <done>
    Migración aplicada en DB remota. Constraint widened, columna añadida. Ahora `INSERT` con `model='plantnet'` es válido. Plan 05-03 puede proceder sin riesgo de constraint violation.
  </done>
</task>

<task type="auto">
  <name>Task 4: Regenerar TypeScript types desde el schema remoto</name>
  <read_first>
    - .planning/phases/05-plantnet-fourth-provider/05-RESEARCH.md § Persistence Schema (líneas 453-465 — comando exacto)
    - .planning/phases/05-plantnet-fourth-provider/05-PATTERNS.md § "REGENERATE" (líneas 318-334)
    - src/integrations/supabase/types.ts (para conocer formato actual antes de regenerar)
  </read_first>
  <files>src/integrations/supabase/types.ts</files>
  <action>
    Regenerar `src/integrations/supabase/types.ts` desde el schema remoto. La migración de Task 3 debe estar aplicada antes; si no lo está, los nuevos types NO incluirán `raw_response` (falso positivo silencioso).

    Comando (Supabase CLI 2.67.1 verificada disponible):
    ```bash
    npx supabase gen types typescript --linked > src/integrations/supabase/types.ts
    ```

    Si `--linked` falla, usar `--project-id <PROJECT_ID>`:
    ```bash
    npx supabase gen types typescript --project-id <PROJECT_ID> > src/integrations/supabase/types.ts
    ```

    (El PROJECT_ID se ve en Supabase Dashboard -> Project Settings, o en `supabase/config.toml`.)

    Post-regen, verificar que los cambios esperados aparecen:
    ```bash
    grep -n "raw_response" src/integrations/supabase/types.ts
    grep -n "plantnet" src/integrations/supabase/types.ts
    ```

    Si no aparecen: la migración no está aplicada en el remoto, o `--project-id` apunta al equivocado. Fix Task 3 primero.

    Ejecutar `npm run build` para verificar que no se rompió el type-check:
    ```bash
    npm run build
    ```

    Debe pasar sin errores TypeScript.
  </action>
  <verify>
    <automated>grep -q "raw_response" src/integrations/supabase/types.ts && grep -q "plantnet" src/integrations/supabase/types.ts && npm run build 2>&1 | tail -20 | grep -qE "(built in|✓)"</automated>
  </verify>
  <acceptance_criteria>
    - `src/integrations/supabase/types.ts` contiene `raw_response` (nueva columna aparece en el schema type)
    - `src/integrations/supabase/types.ts` contiene `plantnet` (nuevo valor en el union de `model`)
    - `npm run build` pasa sin errores TypeScript
    - No hay diff ruidoso fuera de las líneas de `model_evaluations` — si Supabase CLI reordenó otras tablas, usar `git add -p src/integrations/supabase/types.ts` para aislar el diff (nota en PATTERNS.md línea 333)
  </acceptance_criteria>
  <done>
    TypeScript types reflejan schema DB actualizado. `raw_response: Json | null` está tipado. Plan 05-03 puede tipar la 4ª fila del INSERT sin `any` cast.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev → Supabase remote DB | `supabase db push` cruza red (TLS via CLI). Migraciones DDL sensibles. |
| CPO → Supabase Dashboard (Functions Secrets) | Secret material (`PLANTNET_API_KEY`) se introduce vía UI del dashboard. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering | supabase/migrations/*.sql | mitigate | Migración committed a git ANTES de push (audit trail); usar `if not exists` / `drop if exists` para idempotencia (re-run safe). |
| T-05-02-02 | Information Disclosure | PLANTNET_API_KEY en Function Secrets | mitigate | Secret vive SOLO en Supabase Function Secrets (server-side, encrypted at rest). NUNCA en `.env` committed, NUNCA en client bundle. Coherente con patrón ANTHROPIC_API_KEY / OPENAI_API_KEY / GEMINI_API_KEY existentes. |
| T-05-02-03 | Denial of Service | model_evaluations_model_check widening | accept | Ampliar CHECK constraint es strictly more permissive — no rompe INSERTs existentes. Si por bug el edge function insertase `model='xxx'` (invalid), el constraint aún lo rechaza. |
| T-05-02-04 | Elevation of Privilege | Nueva columna raw_response jsonb | accept | RLS de `model_evaluations` sigue igual — solo service_role escribe, client no lee esta tabla (analytics-only). No hay ampliación de superficie de ataque. |
| T-05-02-05 | Information Disclosure | Regenerated types en git diff | accept | `src/integrations/supabase/types.ts` NO contiene secrets (solo shapes). Public info. |
</threat_model>

<verification>
- Migración SQL sintácticamente válida y aplicada en remoto (Task 3 automated check)
- CPO ha añadido PLANTNET_API_KEY (Task 2 human checkpoint)
- TypeScript types reflejan schema actualizado (Task 4 grep + build check)
- `npm run build` pasa sin errores
- `supabase migration list --linked` incluye la nueva migración
</verification>

<success_criteria>
- Tabla `model_evaluations` en DB remota acepta `model = 'plantnet'` y tiene columna `raw_response jsonb`
- `src/integrations/supabase/types.ts` refleja el schema (union de 4 valores + `raw_response: Json | null`)
- `PLANTNET_API_KEY` configurado en Function Secrets (verificable con `supabase secrets list`)
- Plan 05-03 puede empezar sin schema blockers
</success_criteria>

<output>
After completion, create `.planning/phases/05-plantnet-fourth-provider/05-02-SUMMARY.md`
</output>
