---
phase: 02-prompt-optimization
plan: 01
subsystem: ai
tags: [prompts, ai, supabase, migration, edge-function, claude, gemini, gpt4o]

requires:
  - phase: 01-foundation
    provides: plant_searches table + identify-plant edge function with 3-model consensus
provides:
  - watering_interval_days as integer field in identify-plant JSON response
  - plant_searches.watering_interval_days column (nullable integer, check 1-60)
  - Regenerated Supabase types reflecting the new column
  - SYSTEM_PROMPT with 10-plant few-shot for canonical Spanish↔scientific names
  - Explicit uncertainty handling (low-confidence note in description, null watering allowed)
affects: [03-calendar-v0, 02-02-benchmark, 02-03-hook-passthrough]

tech-stack:
  added: []
  patterns:
    - "SYSTEM_PROMPT lockstep convention: same text duplicated in scripts/benchmark-prompt.ts; both files carry a // IMPORTANT sync comment"
    - "toIntOrNull coercion helper for AI-returned numerics (out-of-range or non-finite → null, never throws)"

key-files:
  created:
    - supabase/migrations/20260517000000_add_watering_interval_days_to_plant_searches.sql
  modified:
    - supabase/functions/identify-plant/index.ts
    - src/integrations/supabase/types.ts

key-decisions:
  - "Did NOT extend model_evaluations schema — coherence measured at runtime via Plan 02-02 benchmark (CONTEXT.md Claude's Discretion)"
  - "Did NOT bump Claude model version — out of scope per planner discretion"
  - "Migration uses ALTER (no GRANT block needed per MIGRATION_CONVENTIONS.md § non-table migrations)"
  - "isFallbackResult NOT extended — null watering is legitimate and must not contribute to fallback signal"

patterns-established:
  - "Prompt-script lockstep: any change to SYSTEM_PROMPT in identify-plant/index.ts MUST be mirrored in scripts/benchmark-prompt.ts (no shared module by design; comment in both files)"
  - "Numeric AI-output coercion: prefer per-field toIntOrNull / toStr helpers inside parseAIResponse over global validation layer"

requirements-completed: [PROM-01, PROM-02]

duration: ~25min (live, including subagent stall recovery)
completed: 2026-05-17
---

# Phase 02 Plan 01: Edge function extension + few-shot SYSTEM_PROMPT Summary

**identify-plant edge function now returns `watering_interval_days` as a structured integer (or null) and SYSTEM_PROMPT teaches 10 canonical Spanish↔scientific plant names with explicit uncertainty handling**

## Performance

- **Duration:** ~25 min (subagent stall in Task 2 → orchestrator finished inline)
- **Completed:** 2026-05-17
- **Tasks:** 2 / 2
- **Files modified:** 3

## Accomplishments
- Schema migration `20260517000000_add_watering_interval_days_to_plant_searches.sql` created with idempotent `ALTER plant_searches ADD COLUMN IF NOT EXISTS watering_interval_days integer CHECK (... 1-60)`
- Migration pushed to linked Supabase project (`sdxfxkqzgnonxfshbjfc`) — column live in production
- `src/integrations/supabase/types.ts` regenerated via `supabase gen types typescript --linked` — Row/Insert/Update all carry `watering_interval_days: number | null`
- SYSTEM_PROMPT rewritten with 10-plant few-shot block (Potus → Epipremnum aureum, Monstera, Sansevieria, Ficus lyrata, Chamaedorea, Calathea, Dracaena, Spathiphyllum, Echeveria, Cactaceae) + explicit uncertainty instructions
- `PlantInfo`, `parseAIResponse` (with new `toIntOrNull` helper), the `plant_searches` INSERT and the final Response JSON all carry the new field
- `consensus.ts`, `isFallbackResult`, and the 3 model callers (`callClaude`, `callGemini`, `callOpenAI`) untouched as required by D-03/D-05
- Lockstep drift comment added above `const SYSTEM_PROMPT` (mirrors `scripts/benchmark-prompt.ts` from Plan 02-02)

## Task Commits

1. **Task 1: Migration + Supabase push + regenerate types** — `7dd9562` (feat) — created migration, ran `supabase db push` (live), ran `supabase gen types --linked`
2. **Task 2: SYSTEM_PROMPT rewrite + PlantInfo / parseAIResponse / INSERT / Response edits** — `8777c84` (feat)

## Files Created/Modified
- `supabase/migrations/20260517000000_add_watering_interval_days_to_plant_searches.sql` — schema migration (created)
- `src/integrations/supabase/types.ts` — regenerated by Supabase CLI to reflect new column
- `supabase/functions/identify-plant/index.ts` — 5 surgical edits: SYSTEM_PROMPT (lines 14-67), drift comment, `PlantInfo` interface, `parseAIResponse` (added `toIntOrNull`), `plant_searches` INSERT, Response JSON

## Decisions Made
- Followed plan as specified. No new dependencies. `consensus.ts` and `model_evaluations` schema untouched (per D-03 and Claude's Discretion in CONTEXT.md).

## Deviations from Plan

### 1. [Runtime — Stalled subagent] Wave 1 executor agents stalled at 600s

- **Found during:** Task 2 execution (subagent stream watchdog timeout)
- **Issue:** Both Wave 1 gsd-executor subagents (Plan 02-01 and Plan 02-02) stalled at the 600s mark with `Agent stalled: no progress for 600s (stream watchdog did not recover)`. Plan 02-01's subagent had completed Task 1 (committing `7dd9562` directly onto `feat/phase-2-prompt-optimization` rather than its worktree branch — `supabase db push` and `supabase gen types --linked` require running from the project root for the Supabase CLI to find its config). It then stalled while preparing to commit Task 2's edits to `index.ts`.
- **Fix:** Orchestrator switched to inline (no-subagent) execution. Read the current `index.ts`, applied the 5 surgical edits exactly as specified in the plan, ran `npx tsc --noEmit` (clean), and committed Task 2 as `8777c84`. No re-do of Task 1 needed — its work was already on the feature branch.
- **Verification:** All acceptance criteria except the line-count grep pass (see deviation #2). `npx tsc --noEmit` exits 0. `consensus.ts` diff vs HEAD is empty.
- **Committed in:** `8777c84` (Task 2)

### 2. [Plan acceptance-check formula] `grep -c "watering_interval_days" >= 7` returns 6

- **Found during:** Task 2 verification
- **Issue:** The plan's automated check counts **lines** (`grep -c`) and expects `>= 7`. The current file has the string on 6 distinct lines (line 27, 64, 75, 265, 418, 485), with multiple occurrences on lines 265/418/485 — total **9 occurrences** across 6 lines. Functional intent (every required call site carries the field) is met.
- **Fix:** None. The discrepancy is in the acceptance formula, not the implementation. All 7 places listed in the plan's prose breakdown (JSON example, field instructions, PlantInfo, parseAIResponse return, INSERT, Response JSON, semantic explanation) are present.
- **Verification:** `grep -o "watering_interval_days" supabase/functions/identify-plant/index.ts | wc -l` = 9.

---

**Total deviations:** 2 (1 runtime recovery, 1 acceptance-formula nit).
**Impact on plan:** Plan executed to completion. The subagent stall was a Claude Code harness behavior, not a plan or code defect.

## Issues Encountered
- See deviations above. No code-level issues.

## User Setup Required
None — migration is already live in production; edge function deploys on next `supabase functions deploy identify-plant` (not part of this plan; CI/manual deploy handled separately).

## Next Phase Readiness
- **Plan 02-02 (benchmark CLI):** SYSTEM_PROMPT now carries the canonical text; the benchmark script's `SYSTEM_PROMPT` constant must be populated with this same text in lockstep.
- **Plan 02-03 (hook passthrough):** `PlantResult` extension can now mirror the edge-function Response shape — field is already in the JSON wire format.
- **Phase 03 (Calendar v0):** Can consume `watering_interval_days` directly from `plant_searches` rows or fresh identify responses.

---
*Phase: 02-prompt-optimization*
*Completed: 2026-05-17*
