---
phase: 02-prompt-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/identify-plant/index.ts
  - supabase/migrations/20260517000000_add_watering_interval_days_to_plant_searches.sql
  - src/integrations/supabase/types.ts
autonomous: true
requirements: [PROM-01, PROM-02]
tags: [prompts, ai, supabase, migration, edge-function]

must_haves:
  truths:
    - "D-01: El edge function devuelve watering_interval_days como número entero o null en cada respuesta JSON; el resto del contrato (name, description, care, diagnosis) queda intacto (campo solitario, sin estructurar más campos del care)"
    - "D-02: Tipo del campo número | null en la respuesta del edge function (PlantInfo extiende con watering_interval_days: number | null) y en la columna plant_searches.watering_interval_days (integer nullable con check 1-60); toIntOrNull coerce out-of-range a null"
    - "D-03: consensus.ts NO se modifica — pickWinner existente selecciona el winner; winner.plantInfo!.watering_interval_days es el valor que llega al cliente cuando los 3 modelos disienten en frecuencia"
    - "D-04: El SYSTEM_PROMPT instruye explícitamente la semántica del número: frecuencia promedio anual de riego en días para planta de interior con luz indirecta y 20-22°C"
    - "D-05: Estrategia única — solo iterar el system prompt; los 3 modelos (claude-sonnet-4 / gemini-2.5-flash-lite / gpt-4o), su selección via pickWinner, y el flujo SSE de Phase 4 no se tocan; no se añade paso de validación cruzada ni re-prompt"
    - "D-06: Prompt único compartido — un solo SYSTEM_PROMPT inyectado a callClaude, callGemini, callOpenAI (los 3 callers no se modifican; el patrón existente se mantiene)"
    - "D-07: El SYSTEM_PROMPT contiene un few-shot de 5-10 plantas comunes — par nombre común español ↔ nombre científico — para anclar nomenclatura canónica entre los 3 modelos; NO se incluyen valores de watering_interval_days en los examples (no sesgar el riego)"
    - "D-08: El SYSTEM_PROMPT instruye al modelo a manejar incertidumbre — si no está seguro, devolver la planta más probable + nota explícita de confianza baja en description; watering_interval_days puede ser null o el de la mejor opción"
    - "La tabla plant_searches tiene la columna watering_interval_days integer nullable con check 1-60 (cumple persistencia bajo Claude's Discretion del CONTEXT.md)"
    - "El INSERT a plant_searches y la response JSON propagan winner.plantInfo.watering_interval_days"
    - "model_evaluations sigue funcionando sin modificación (NO se extiende el schema)"
    - "isFallbackResult NO compara watering_interval_days (null legítimo no es señal de fallback)"
  artifacts:
    - path: "supabase/migrations/20260517000000_add_watering_interval_days_to_plant_searches.sql"
      provides: "Schema migration ALTER plant_searches add column watering_interval_days"
      contains: "watering_interval_days integer"
    - path: "supabase/functions/identify-plant/index.ts"
      provides: "Edge function with extended SYSTEM_PROMPT, PlantInfo, parseAIResponse, INSERT, response"
      contains: "watering_interval_days"
    - path: "src/integrations/supabase/types.ts"
      provides: "Regenerated types reflecting new plant_searches.watering_interval_days column"
      contains: "watering_interval_days: number | null"
  key_links:
    - from: "SYSTEM_PROMPT (JSON example)"
      to: "client UI Phase 3 Calendar v0"
      via: "JSON response field watering_interval_days"
      pattern: "watering_interval_days"
    - from: "parseAIResponse"
      to: "plant_searches INSERT + Response JSON"
      via: "winner.plantInfo!.watering_interval_days"
      pattern: "winner\\.plantInfo!\\.watering_interval_days"
    - from: "plant_searches column"
      to: "src/integrations/supabase/types.ts Row/Insert/Update"
      via: "supabase gen types regeneration"
      pattern: "watering_interval_days.*number.*null"
---

<objective>
Hacer que el edge function `identify-plant` devuelva `watering_interval_days` como campo numérico estructurado (PROM-01) y mejorar la precisión de identificación (PROM-02) reescribiendo el SYSTEM_PROMPT con few-shot de nombres canónicos y manejo explícito de incertidumbre. Persistir el nuevo campo en `plant_searches` para que Phase 3 Calendar v0 pueda consumirlo.

Propósito: Desbloquea Calendar v0 (Phase 3) entregando un número que la UI puede mostrar sin parsear markdown, y reduce drift de nomenclatura entre los 3 modelos (Claude/Gemini/GPT-4o) para mejorar el match del consensus.

Salida: Edge function actualizado, migración aplicada en producción, tipos regenerados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-prompt-optimization/02-CONTEXT.md
@.planning/phases/02-prompt-optimization/02-PATTERNS.md
@supabase/MIGRATION_CONVENTIONS.md
@supabase/functions/identify-plant/index.ts
@supabase/migrations/20260506000000_add_context_to_plant_searches.sql

<interfaces>
<!-- Tipos y contratos clave que el ejecutor necesita. Extraídos de supabase/functions/identify-plant/index.ts (líneas 54-69) y consensus.ts. -->

Desde supabase/functions/identify-plant/index.ts:
```typescript
interface PlantInfo {
  name: string;
  description: string;
  care: string;
  diagnosis: string;
}

interface ModelResult {
  model: ModelName;
  success: boolean;
  plantInfo: PlantInfo | null;
  rawName: string | null;
  scientificName: string | null;
  responseMs: number;
  errorMessage: string | null;
}

const FALLBACK_NAME        = "Planta no identificada";
const FALLBACK_DESCRIPTION = "No se pudo identificar la planta. Intenta con otra foto más clara.";
const FALLBACK_CARE        = "Asegúrate de que la foto muestre bien la planta.";
const FALLBACK_DIAGNOSIS   = "No hay suficiente información para un diagnóstico.";
```

Desde consensus.ts (NO MODIFICAR en esta fase):
```typescript
export type ModelName = "claude" | "gemini" | "gpt4o";
export function extractScientificName(name: string): string | null;
export function computeConsensus(results: ModelInput[]): Map<ModelName, ConsensusResult>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Crear migración + push a Supabase + regenerar tipos</name>
  <files>supabase/migrations/20260517000000_add_watering_interval_days_to_plant_searches.sql, src/integrations/supabase/types.ts</files>
  <read_first>
    - supabase/migrations/20260506000000_add_context_to_plant_searches.sql (analog — el ALTER más reciente sobre plant_searches con default + check; per PATTERNS.md líneas 207-220)
    - supabase/MIGRATION_CONVENTIONS.md (SIN bloque GRANT para ALTER per § "Para migraciones que NO crean tablas" líneas 53-55)
    - src/integrations/supabase/types.ts (shape actual de plant_searches Row; NO editar a mano — regenerar via CLI)
    - .planning/phases/02-prompt-optimization/02-PATTERNS.md § Migration (líneas 201-246)
  </read_first>
  <action>
Paso A — Crear el archivo de migración en `supabase/migrations/20260517000000_add_watering_interval_days_to_plant_searches.sql` con EXACTAMENTE este contenido (sigue la convención del analog: ALTER idempotente, comentario de cabecera, SIN bloque GRANT per `MIGRATION_CONVENTIONS.md` § ALTER):

```sql
-- Add watering_interval_days to plant_searches: structured frequency used by Calendar v0 (Phase 3).
-- Nullable: model returns null when it can't determine the value with confidence (D-02).
-- Range 1-60 days enforced at the edge function via toIntOrNull; check constraint is belt-and-suspenders.
-- Historical rows stay NULL; backfill deferred (see CONTEXT.md § Claude's Discretion).
-- See: .planning/phases/02-prompt-optimization/02-CONTEXT.md
alter table plant_searches
  add column if not exists watering_interval_days integer
    check (watering_interval_days is null or (watering_interval_days >= 1 and watering_interval_days <= 60));
```

NO añadir bloque GRANT (per `supabase/MIGRATION_CONVENTIONS.md` § "Para migraciones que NO crean tablas (alter, indexes, data) — Sin cambios"). NO añadir índice — ninguna query filtra/ordena por esta columna.

Paso B [BLOCKING] — Pushear el schema al proyecto Supabase vinculado. Desde la raíz del repo:

```bash
supabase db push
```

Si hay prompts interactivos que no se pueden suprimir y el comando necesita `SUPABASE_ACCESS_TOKEN`, exportalo desde tu shell antes de correr. Si el push falla por cualquier motivo distinto a "already applied", PARA y reporta el error — no procedas al Paso C.

Paso C — Regenerar los tipos TypeScript desde el schema (ya migrado). Desde la raíz del repo:

```bash
npx supabase gen types typescript --linked > src/integrations/supabase/types.ts
```

Diff esperado en `src/integrations/supabase/types.ts`: 3 líneas añadidas dentro de la tabla `plant_searches` — una en `Row`, una en `Insert`, una en `Update`, cada una declarando `watering_interval_days: number | null` (los shapes `Insert` y `Update` probablemente añadan `?` para opcional). Si el diff tiene más de ~5 líneas añadidas, otra columna upstream se ha movido — PARA y muestra el diff para revisión antes de seguir.

NO editar a mano `src/integrations/supabase/types.ts` — el archivo es generado (comentario de cabecera "This file was automatically generated…").
  </action>
  <verify>
    <automated>grep -c "watering_interval_days" supabase/migrations/20260517000000_add_watering_interval_days_to_plant_searches.sql | grep -vq "^0$" && grep -q "watering_interval_days" src/integrations/supabase/types.ts && grep -c "watering_interval_days.*number" src/integrations/supabase/types.ts | grep -vq "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - Existe el archivo `supabase/migrations/20260517000000_add_watering_interval_days_to_plant_searches.sql`
    - El archivo de migración contiene el string `alter table plant_searches`
    - El archivo de migración contiene el string `add column if not exists watering_interval_days integer`
    - El archivo de migración contiene el string `check (watering_interval_days is null or (watering_interval_days >= 1 and watering_interval_days <= 60))`
    - El archivo de migración NO contiene el string `grant` (case-insensitive) — `grep -i grant supabase/migrations/20260517000000_*.sql` sale con código no-cero (no match)
    - `supabase db push` sale con código 0 (o reporta "no changes to push" en reintento)
    - `src/integrations/supabase/types.ts` contiene el substring `watering_interval_days: number | null`
    - `npx tsc --noEmit` sale con código 0 (los tipos regenerados compilan limpio)
  </acceptance_criteria>
  <done>Migración committeada, schema pusheado a producción, tipos regenerados, TypeScript compila limpio.</done>
</task>

<task type="auto">
  <name>Tarea 2: Reescribir SYSTEM_PROMPT + extender PlantInfo, parseAIResponse, INSERT y Response</name>
  <files>supabase/functions/identify-plant/index.ts</files>
  <read_first>
    - supabase/functions/identify-plant/index.ts (SYSTEM_PROMPT actual líneas 14-50, PlantInfo líneas 54-59, constantes FALLBACK líneas 204-216, parseAIResponse líneas 218-241, INSERT a plant_searches líneas 385-400, Response JSON líneas 452-465)
    - supabase/functions/identify-plant/consensus.ts (read-only — NO MODIFICAR; solo confirmar que queda intacto per D-03 + PATTERNS.md "Out of Scope")
    - .planning/phases/02-prompt-optimization/02-CONTEXT.md § decisions (D-01 a D-08 + Claude's Discretion)
    - .planning/phases/02-prompt-optimization/02-PATTERNS.md § `index.ts` (líneas 23-199)
  </read_first>
  <action>
Esta tarea hace 5 ediciones quirúrgicas dentro de `supabase/functions/identify-plant/index.ts`. NO se tocan otros archivos. NO refactor de código no relacionado.

**Edit 1 — Reemplazar SYSTEM_PROMPT (líneas 14-48)** con el prompt nuevo de abajo. Mantén el comentario de cabecera de sección `// ─── Prompt ───…` y mantén `USER_MESSAGE` intacto en línea 50.

**ADEMÁS añade este comentario de aviso de drift inmediatamente encima de la línea `const SYSTEM_PROMPT = \`...\`;`** (dos líneas, texto exacto):
```typescript
// IMPORTANT: keep this prompt in sync with scripts/benchmark-prompt.ts SYSTEM_PROMPT.
//            If you edit one, you MUST edit the other — there is no shared module.
```
Este comentario lockstep es obligatorio porque Plan 02-02 duplica este prompt en el script de benchmark (Wave 1 paralelo; sin módulo compartido por diseño). Sin este comentario un futuro mantenedor dejará silenciosamente que los dos diverjan, y el benchmark medirá comportamiento stale del prompt. Plan 02-02 lleva el comentario recíproco.

El prompt nuevo DEBE:
- Ser un único `const SYSTEM_PROMPT = \`...\`;` template literal (D-06: un prompt, los 3 modelos).
- Estar en español (CLAUDE.md: UI/contenido en español, código en inglés; la salida del prompt ES contenido user-facing).
- Añadir `watering_interval_days` DENTRO del bloque de ejemplo JSON con un entero representativo (usa `7`, NO `null` — el modelo necesita ver el shape concreto per PATTERNS.md línea 53).
- Documentar la semántica del número en prosa: "frecuencia promedio anual de riego en días, asumiendo planta de interior con luz indirecta y 20-22°C" (D-04).
- Permitir `null` explícitamente cuando no esté seguro (D-02, D-08).
- Incluir un few-shot de 5-10 plantas comunes — par `nombre común español ↔ nombre científico` — para anclar nomenclatura canónica (D-07). Lista recomendada per CONTEXT.md `<specifics>`: Potus / Epipremnum aureum, Monstera deliciosa, Sansevieria / Sansevieria trifasciata, Ficus / Ficus lyrata, Palmera de salón / Chamaedorea elegans, Calatea / Calathea, Dracena / Dracaena marginata, Espatifilo / Spathiphyllum, Suculenta / Echeveria, Cactus / Cactaceae. NO incluir valores de `watering_interval_days` en los examples (D-07).
- Añadir manejo explícito de incertidumbre: si no estás seguro, devuelve la planta más probable y di en `description` por qué la confianza es baja (D-08).

Usa este body de prompt exacto (reemplaza líneas 16-48):

```typescript
const SYSTEM_PROMPT = `Eres un botánico experto que ayuda a personas con plantas en casa. Responde SIEMPRE en español, con tono cercano y claro. Evita jerga botánica innecesaria — si usas un término técnico, explícalo brevemente.

El usuario te envía una foto de una planta. Analízala y responde SOLO con un JSON válido (sin texto antes ni después) con esta estructura:

{
  "name": "Nombre común (Nombre científico)",
  "description": "Markdown con descripción de la planta",
  "care": "Markdown con guía de cuidados",
  "diagnosis": "Markdown con diagnóstico visual",
  "watering_interval_days": 7
}

Instrucciones para cada campo:

**name**: Nombre común seguido del nombre científico entre paréntesis. Ej: "Potus (Epipremnum aureum)". Usa nombres canónicos en español — evita variantes ortográficas. Plantas comunes y su nomenclatura de referencia:
- Potus → Epipremnum aureum
- Monstera → Monstera deliciosa
- Sansevieria / Lengua de suegra → Sansevieria trifasciata
- Ficus / Ficus lira → Ficus lyrata
- Palmera de salón → Chamaedorea elegans
- Calatea → Calathea
- Dracena → Dracaena marginata
- Espatifilo / Lirio de la paz → Spathiphyllum
- Suculenta → Echeveria
- Cactus → Cactaceae

**description**: Usa Markdown. Incluye:
- Qué planta es y a qué familia pertenece
- De dónde es originaria
- Características visuales principales (hojas, flores, tamaño típico)
- Algún dato curioso o útil si lo hay
- Si NO estás del todo seguro de la identificación, dilo claramente al principio: indica qué planta crees que es y por qué tu confianza es baja (e.g. "podría ser X, pero la foto no muestra bien Y").

**care**: Usa Markdown con una lista clara. Incluye estas categorías con indicaciones concretas y prácticas:
- **Riego** — frecuencia y cantidad (ej: "cada 3-4 días en verano, cada semana en invierno")
- **Luz** — tipo e intensidad (ej: "luz indirecta brillante, evitar sol directo")
- **Temperatura** — rango ideal
- **Sustrato** — tipo recomendado
- **Abono** — frecuencia y época
- **Consejo extra** — un tip práctico que marque la diferencia

**diagnosis**: Usa Markdown. Analiza lo que ves en la foto:
- Si se ve **sana**: dilo claramente y menciona qué señales positivas observas
- Si tiene **problemas**: describe los síntomas que ves, la causa más probable, y qué hacer paso a paso para solucionarlo
- Si la foto no permite un diagnóstico claro, dilo honestamente

**watering_interval_days**: Número entero entre 1 y 60 que representa la **frecuencia promedio anual de riego en días** para una planta de interior en condiciones típicas (luz indirecta, 20-22°C). Ej: 7 significa "regar cada 7 días en promedio a lo largo del año". Si no puedes determinar la frecuencia con confianza (planta desconocida, identificación incierta, o sin datos suficientes), devuelve \`null\` — es preferible no inventar un número.`;
```

**Edit 2 — Extender la interfaz PlantInfo** (línea 54-59) con el campo nuevo:

```typescript
interface PlantInfo {
  name: string;
  description: string;
  care: string;
  diagnosis: string;
  watering_interval_days: number | null;
}
```

**Edit 3 — Extender parseAIResponse** (líneas 218-241). Añade un helper `toIntOrNull` dentro de la función (siguiendo el estilo de `toStr`) e incluye el campo nuevo en el objeto devuelto. NO modificar `isFallbackResult` — `null` es un valor legítimo y no debe contribuir a la señal de fallback (per PATTERNS.md línea 142).

```typescript
function parseAIResponse(text: string): PlantInfo | null {
  let parsed: Record<string, unknown> | null = null;

  try {
    parsed = JSON.parse(text);
  } catch {
    const jsonMatch = text.match(/\{[\s\S]*\}/);
    if (jsonMatch) {
      try { parsed = JSON.parse(jsonMatch[0]); } catch { /* invalid JSON */ }
    }
  }

  if (!parsed) return null;

  const toStr = (v: unknown, fb: string) =>
    typeof v === "string" && v.trim() ? v : fb;

  const toIntOrNull = (v: unknown): number | null => {
    const n = typeof v === "number" ? v : typeof v === "string" ? Number(v) : NaN;
    if (!Number.isFinite(n)) return null;
    const int = Math.round(n);
    if (int < 1 || int > 60) return null;  // out-of-range → likely hallucination
    return int;
  };

  return {
    name: toStr(parsed.name, FALLBACK_NAME),
    description: toStr(parsed.description, FALLBACK_DESCRIPTION),
    care: toStr(parsed.care, FALLBACK_CARE),
    diagnosis: toStr(parsed.diagnosis, FALLBACK_DIAGNOSIS),
    watering_interval_days: toIntOrNull(parsed.watering_interval_days),
  };
}
```

**Edit 4 — Extender el INSERT a plant_searches** (líneas 385-400). Añade `watering_interval_days` junto a `diagnosis`, ANTES de `image_url`:

```typescript
const { data: searchRow, error: searchError } = await supabaseAdmin
  .from("plant_searches")
  .insert({
    name:                   winner.plantInfo!.name,
    description:            winner.plantInfo!.description,
    care:                   winner.plantInfo!.care,
    diagnosis:              winner.plantInfo!.diagnosis,
    watering_interval_days: winner.plantInfo!.watering_interval_days,
    image_url:              imageUrl,
    model:                  winner.model,
    ...(user_id ? { user_id } : { user_id: null, anonymous_id: anonymous_id ?? null }),
    ...(typeof lat === "number" && isFinite(lat) && lat >= -90 && lat <= 90 &&
       typeof lng === "number" && isFinite(lng) && lng >= -180 && lng <= 180
      ? { lat, lng } : {}),
  })
  .select("id, created_at")
  .single();
```

**Edit 5 — Extender el Response JSON final** (líneas 452-465). Añade `watering_interval_days` junto a `diagnosis`:

```typescript
return new Response(
  JSON.stringify({
    name:                   winner.plantInfo!.name,
    description:            winner.plantInfo!.description,
    care:                   winner.plantInfo!.care,
    diagnosis:              winner.plantInfo!.diagnosis,
    watering_interval_days: winner.plantInfo!.watering_interval_days,
    model:                  winner.model,
    plant_search_id:        searchRow?.id ?? null,
    created_at:             searchRow?.created_at ?? null,
    models:                 modelsSummary,
    consensus_reached:      consensusReached,
  }),
  { headers: { ...corsHeaders, "Content-Type": "application/json" } }
);
```

**Guardrails fuera de scope:**
- NO modificar `consensus.ts` (D-03 explícito reusa el `pickWinner` existente).
- NO modificar los 3 model callers (`callClaude`, `callGemini`, `callOpenAI`) — inyectan SYSTEM_PROMPT via la constante; el campo nuevo fluye automáticamente (per PATTERNS.md línea 195).
- NO modificar `isFallbackResult` — `null` es legítimo y no debe convertirse en señal de fallback.
- NO extender el INSERT a `model_evaluations` (líneas 408-426) — per discreción del planner, esta fase no extiende ese schema; la coherencia se mide en runtime con el benchmark del Plan 02.
- NO actualizar la versión del modelo de Claude (fuera de scope per discreción del planner).
- NO añadir eventos PostHog nuevos (CONTEXT.md `<canonical_refs>` explícito).
  </action>
  <verify>
    <automated>cd /Users/noemisantos/mi-plantita-feliz && grep -c "watering_interval_days" supabase/functions/identify-plant/index.ts | awk '{ if ($1 >= 7) exit 0; else exit 1 }' && grep -q "toIntOrNull" supabase/functions/identify-plant/index.ts && grep -q "frecuencia promedio anual de riego" supabase/functions/identify-plant/index.ts && grep -q "Epipremnum aureum" supabase/functions/identify-plant/index.ts && grep -q "Monstera deliciosa" supabase/functions/identify-plant/index.ts && ! git diff supabase/functions/identify-plant/consensus.ts | grep -q "^[+-]"</automated>
  </verify>
  <acceptance_criteria>
    - El archivo `supabase/functions/identify-plant/index.ts` contiene el string `watering_interval_days` al menos 7 veces (ejemplo JSON del SYSTEM_PROMPT, instrucciones del campo en el SYSTEM_PROMPT, interfaz PlantInfo, return de parseAIResponse, INSERT, Response JSON, y sección de instrucciones del campo — 7+ ocurrencias)
    - El archivo contiene el nombre del helper `toIntOrNull`
    - El archivo contiene el string semántico `frecuencia promedio anual de riego`
    - El archivo contiene `Epipremnum aureum` y `Monstera deliciosa` (anchors de muestra del few-shot)
    - El archivo `supabase/functions/identify-plant/consensus.ts` tiene cero diff vs git HEAD (intacto)
    - El archivo sigue conteniendo el body original de `isFallbackResult` sin cambios — `grep -A 7 "function isFallbackResult" supabase/functions/identify-plant/index.ts | grep -q "watering_interval_days"` NO devuelve match (isFallbackResult NO referencia el campo nuevo)
    - `npx tsc --noEmit` sale con código 0 (TypeScript compila — los tipos de la Tarea 1 ya deben estar regenerados)
    - El archivo sigue conteniendo los 3 model callers (`callClaude`, `callGemini`, `callOpenAI`) — `grep -c "^async function call" supabase/functions/identify-plant/index.ts` devuelve `3`
  </acceptance_criteria>
  <done>Edge function reescrito y compilando; SYSTEM_PROMPT enseña el campo nuevo con semántica y anchors del few-shot; PlantInfo, parseAIResponse, INSERT y Response llevan watering_interval_days; consensus.ts e isFallbackResult intactos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Límite | Descripción |
|--------|-------------|
| cliente → edge function (HTTP) | Cruza imagen base64 untrusted subida por el usuario + user_id/anonymous_id/lat/lng opcional |
| edge function → 3 APIs de IA externas | Request server-side con secretos `*_API_KEY` a Anthropic / Google / OpenAI |
| edge function → Supabase Storage + DB | INSERT con service-role en `plant_searches` + `model_evaluations` + upload al bucket `plant-images` |
| Migración → DB en vivo | Cambio DDL aplicado via `supabase db push` desde la máquina del developer a producción |

## STRIDE Threat Register

| Threat ID | Categoría | Componente | Disposición | Plan de mitigación |
|-----------|-----------|------------|-------------|--------------------|
| T-02-01 | T (Tampering) | parseAIResponse — campo nuevo `watering_interval_days` desde respuesta de IA | mitigate | `toIntOrNull` coerce non-finite/non-integer/out-of-range (< 1 o > 60) a `null` antes de que el valor llegue a DB o cliente. El check SQL de la columna es belt-and-suspenders. |
| T-02-02 | I (Info disclosure) | Logs del edge function (`console.error` en errores de API líneas 105, 143, 188, 378, 403, 433, 467) | accept | Ya en producción. Los logs incluyen status codes de API y errores de Supabase pero NO loguean valores raw de `*_API_KEY` ni imágenes completas del usuario. No se añade logging nuevo en esta fase. |
| T-02-03 | D (DoS) | 3 llamadas a APIs externas por request (líneas 330-334 Promise.allSettled) | accept | El handling existente de 429 → `RATE_LIMIT:*` no cambia. El edge function tiene caps de concurrencia a nivel Supabase. El prompt nuevo es ~600 tokens más largo (few-shot list) pero bien dentro del budget de 2048 max_tokens. |
| T-02-04 | E (Elevation) | `supabase db push` desde máquina del developer a la DB de producción | mitigate | La migración es un puro `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` con columna nullable y un check constraint. Cero backfill de datos. Reversible via `ALTER TABLE plant_searches DROP COLUMN watering_interval_days` si hace falta. El cambio de schema no puede afectar reads/writes existentes (columna nullable, defaultea a NULL). |
| T-02-05 | I (Info disclosure) | Filas de `model_evaluations` y `plant_searches` | accept | Patrón RLS + service-role existente sin cambios. La columna nueva lleva un entero (sin PII). |
| T-02-06 | T (Tampering) | Prompt injection via contenido de la imagen (el modelo podría ser ingeniería socialmente vía texto visible en imagen) | accept | Fuera de scope para esta fase per CONTEXT.md `<deferred>` ("Paso de validación cruzada — descartado en D-05"). El contrato estricto JSON existente + fallback de `parseAIResponse` significa que un "texto en imagen" maliciosamente craftado puede a lo sumo producir un `PlantInfo` fallback o un JSON inválido, ambos fallan closed (`success: false`). |
| T-02-07 | S (Spoofing) | Campo `anonymous_id` del request del cliente (sin cambios) | accept | Concern pre-existente, sin cambios en esta fase. No se introduce aquí. |

Sin amenazas de severidad `high` — todas las mitigaciones están o implementadas en este plan (T-02-01, T-02-04) o ya en código de producción (T-02-02, T-02-03, T-02-05, T-02-06, T-02-07).
</threat_model>

<verification>
Tras todas las tareas:

1. **TypeScript compila:** `npx tsc --noEmit` sale con código 0.
2. **Migración aplicada:** `supabase db push` sale con código 0 o reporta que no hay nada nuevo que pushear.
3. **Sin drift en consensus.ts:** `git diff supabase/functions/identify-plant/consensus.ts` está vacío.
4. **Sin dependencias nuevas:** `git diff package.json` no muestra cambios (este plan no añade paquetes npm).
5. **Shape del edge function:** la respuesta JSON de cualquier futura llamada a `identify-plant` contendrá `watering_interval_days: <int | null>` — verificable post-deploy via `curl` contra el edge function en vivo con una imagen de prueba.
</verification>

<success_criteria>
- La migración `20260517000000_add_watering_interval_days_to_plant_searches.sql` existe, se aplicó en producción, y `plant_searches` tiene la columna `watering_interval_days integer` nullable con el check constraint.
- `src/integrations/supabase/types.ts` refleja la columna nueva en `Row`, `Insert`, `Update`.
- El SYSTEM_PROMPT de `supabase/functions/identify-plant/index.ts` enseña el campo nuevo con semántica, few-shot de 10 nombres canónicos español↔científico, y manejo explícito de incertidumbre.
- `PlantInfo`, `parseAIResponse` (con `toIntOrNull`), el INSERT a plant_searches, y el Response JSON final llevan `watering_interval_days`.
- `consensus.ts` e `isFallbackResult` intactos.
- TypeScript compila limpio.
</success_criteria>

<output>
Tras completar, crear `.planning/phases/02-prompt-optimization/02-01-SUMMARY.md` resumiendo:
- El nombre exacto del archivo de migración + el resultado del `supabase db push` en vivo
- El shape del delta del SYSTEM_PROMPT (line count antes/después, lista de anchors del few-shot)
- Las 5 ediciones quirúrgicas en `index.ts` con número de línea
- Confirmación de que `consensus.ts`, el schema de `model_evaluations`, los model callers, e `isFallbackResult` están intactos
- Confirmación de que `npx tsc --noEmit` sale con código 0
- Nota de que Plan 03 (passthrough en cliente) ya puede consumir el campo nuevo
</output>
