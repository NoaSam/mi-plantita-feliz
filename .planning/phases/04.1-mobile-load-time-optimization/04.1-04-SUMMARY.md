---
phase: 04.1-mobile-load-time-optimization
plan: "04"
subsystem: verification-and-closure-docs
tags: [verification, roadmap, docs, phase-closure, cpo-checkpoint]
dependency_graph:
  requires:
    - plan-04.1-01-complete   # backfill executed
    - plan-04.1-02-complete   # thumbnail helper + lazy attrs + preconnect
    - plan-04.1-03-complete   # perf_screen_loaded hook + event wiring
  provides:
    - phase-04.1-verification-checklist   # 04.1-VERIFICATION.md ready for CPO
    - phase-04.1-marked-complete-in-roadmap
    - phase-6-folded-into-04.1-in-roadmap
  affects:
    - .planning/phases/04.1-mobile-load-time-optimization/04.1-VERIFICATION.md
    - .planning/ROADMAP.md
tech_stack:
  added: []
  patterns: [docs-only-phase-closure, human-verify-checkpoint-blocking, cross-reference-cross-linking-in-roadmap]
key_files:
  created:
    - .planning/phases/04.1-mobile-load-time-optimization/04.1-VERIFICATION.md
  modified:
    - .planning/ROADMAP.md
decisions:
  - "VERIFICATION.md written in Spanish (as required by CLAUDE.md UI copy rule + memory language rule), 4 blocks (A: SQL sanity, B: device throttled test, C: PostHog event, D: post-deploy trends), thresholds explicit."
  - "Bloque D (post-deploy 24-48h) marked NOT blocking for the merge — merge decision is on Blocks A+B+C only. Bloque D is confirmatory in real users after deploy."
  - "ROADMAP edits scoped to 5 targeted changes: 2 in top phase list, 1 in Phase 04.1 detail (plans list), 2 in Phase 6 detail (Status + Absorbed by). Progress table row also updated. No other phases touched."
  - "Phase 6 kept as historical section (not deleted) — preserves the rationale, tradeoffs, and OOM-risk analysis for future reference. Status changed to 'FOLDED INTO Phase 04.1' with cross-reference to plan 04.1-01."
metrics:
  duration: "~10 min (2 doc edits + 2 commits)"
  completed: "2026-08-12"
  tasks_completed: 2
  tasks_pending: 1
  files_created: 1
  files_modified: 1
---

# Plan 04.1-04 — Verification checklist + ROADMAP closure — SUMMARY

**Status:** Docs complete · **CPO verification pending** (checkpoint gate blocking)
**Executed:** 2026-08-12
**Branch:** `feat/phase-3-calendar-v0`

Close-out plan for Phase 04.1: (a) produced `04.1-VERIFICATION.md` for the CPO to run manually against her iPhone before merging, (b) updated `ROADMAP.md` to mark Phase 04.1 as complete and fold Phase 6 (candidate backfill) into it.

Zero production code touched. Docs-only.

## Tasks

| # | Task | Commit | Files |
|---|---|---|---|
| 1 | Create 04.1-VERIFICATION.md (Bloques A/B/C/D + Decisión final + Post-merge) | `d9ba77f` | `.planning/phases/04.1-mobile-load-time-optimization/04.1-VERIFICATION.md` |
| 2 | Update ROADMAP.md — mark Phase 04.1 [x], fold Phase 6 into 04.1, update progress table | `d0e6a92` | `.planning/ROADMAP.md` |
| 3 | **CHECKPOINT — CPO manual verification against iPhone** | pending | — |

## Verification gates

- VERIFICATION.md grep-check: **6/6 patterns present** (`perf_screen_loaded`, `1500ms`, `2000ms`, `plant_searches`, `app_platform`, file exists) ✓
- ROADMAP.md grep-check: **7/7 patterns present** (`Folded into Phase 04.1`, all 4 plan filenames, `[x] **Phase 04.1`, `**FOLDED INTO Phase 04.1**`) ✓
- Git diff review: 5 targeted edits in ROADMAP.md, no other phase entries touched ✓
- No production code changed — this plan is docs-only per plan-04 objective ✓

## What was built

### `.planning/phases/04.1-mobile-load-time-optimization/04.1-VERIFICATION.md`

Manual verification checklist, ~20 min run time, Spanish content with English event/prop identifiers. Four blocks:

**Bloque A — Sanity check post-backfill (2 min):** 3 SQL queries in Supabase SQL Editor to confirm `legacy_rows = 0`, `storage_rows >= 98`, and `bucket_objects >= 98`.

**Bloque B — Verificación visual en dispositivo real (10 min):** iPhone throttled to Slow 3G in Safari Web Inspector, walkthrough of `/mis-plantas` (D-10: TTFC < 1.5s), `/regar` (D-10: TTFC < 1.5s), `/mapa` (D-10: pins < 2s), plus scroll 60fps check. Blanks provided for CPO to write observed TTFC values.

**Bloque C — PostHog event verification (5 min):** Filter Live events by `perf_screen_loaded`, verify 3 events arrive with correct schema (`screen`, `ttfc_ms`, `plants_count`, `app_platform: "web"` from iOS PWA, `app_version` matching package.json). Optional Android Capacitor verification (C.2) checks `app_platform: "android"`.

**Bloque D — Post-deploy trends (24-48h post-merge):** PostHog Trends insight, median(ttfc_ms) broken down by screen, comparison table vs D-10 targets with D-11 tolerance column. Explicitly marked as NOT blocking for merge — confirmatory in real user data.

**Decisión final:** APPROVED (mergear) / NEEDS ADJUSTMENT (paths A/B/reevaluate). Space for CPO signature + date + notes.

### `.planning/ROADMAP.md`

Five targeted edits:

1. **Line 18 (top phase list):** Phase 04.1 → `[x]` + `(completed 2026-08-12)`
2. **Line 20 (top phase list):** Phase 6 → `[x]` + `**Folded into Phase 04.1**` note with backfill date
3. **Line 141 (Phase 04.1 detail):** `**Plans:** 3/4 plans executed` → `4/4 plans executed`, plan 04 → `[x]`
4. **Line 186 (Phase 6 detail):** Status changed from `Candidate — NOT scheduled` to `**FOLDED INTO Phase 04.1** (executed 2026-08-12 via npm run backfill:images, plan 04.1-01; ~98 rows migrated, 0 failed)` + new `**Absorbed by:**` link
5. **Line 243 (Progress table):** Phase 04.1 row → `4/4 · Complete · 2026-08-12`, Phase 6 row → `— · Folded into 04.1 · 2026-08-12`

Historical sections (Phase 6 rationale, tradeoffs, OOM-risk analysis) preserved as-is — deleting them would lose useful context; the `FOLDED INTO` status makes clear the execution is done.

## Checkpoint gate (pending)

Task 3 is `type="checkpoint:human-verify" gate="blocking"`. The CPO must:

1. Read `.planning/phases/04.1-mobile-load-time-optimization/04.1-VERIFICATION.md`
2. Execute Bloques A + B + C (~17 min)
3. Mark Decisión final: **APPROVED** or **NEEDS ADJUSTMENT**
4. If APPROVED → merge `feat/phase-3-calendar-v0` to `main`
5. Post-merge: run Bloque D in 24-48h to confirm at scale

**This plan cannot be marked fully complete until the CPO records her decision.** The auto-mode default (auto-approve human-verify checkpoints) is NOT active here per user's `<critical_constraints>` — the executor was instructed to write the docs, commit, and return `## CHECKPOINT REQUIRED`. The decision goes into a later revision of this SUMMARY (or a follow-up commit that appends the CPO's decision + measured TTFC values).

## Follow-up items (post-checkpoint)

Once the CPO decides:

- **If APPROVED:**
  - Append actual measured TTFC values from Bloque B to this SUMMARY under a new `## CPO Verification Results` section
  - Confirm Bloque C: `perf_screen_loaded` seen from `app_platform: web` (and optionally `android`)
  - Merge to `main`
  - Schedule Bloque D check for 2026-08-14 (24-48h after merge)
  - If Bloque D confirms → phase 04.1 fully closed; consider Android v1.1.1 release

- **If NEEDS ADJUSTMENT:**
  - Document which criterion missed
  - Path A: Supabase Pro upgrade (activates the render/image transformations that thumbnail-url.ts is already coded to auto-detect and use)
  - Path B: New plan to generate thumbnails during backfill (produce N sizes per foto and store the derived URLs)
  - Return to `discuss-phase` if scope re-evaluation is needed

## Deviations from Plan

None — plan executed exactly as written for Tasks 1 and 2. Task 3 is the human checkpoint gate and is not "executed" by the agent by design.

Minor note on `<action>` guidance: Task 1's action referenced "check 04.1-01-SUMMARY.md for the actual backfill date". That was confirmed — the SUMMARY says `2026-08-12`, so both VERIFICATION.md and ROADMAP.md use `2026-08-12` consistently (no placeholder-date follow-up needed).

## Threat surface scan

No new threat flags. This plan's threat model (T-04.1.04-01: "ROADMAP claims work is done when it isn't"; T-04.1.04-02: "merge conflict garbles ROADMAP") is mitigated by:

- T-04.1.04-01 mitigation: Task 3 checkpoint is BLOCKING — the CPO explicitly approves before merge. The docs commit doesn't mean the phase is verified; only the human check does.
- T-04.1.04-02 mitigation: Small targeted edits (5 changes), reviewable via `git diff`. No wholesale rewrites.

## Self-Check: PASSED

- FOUND: `.planning/phases/04.1-mobile-load-time-optimization/04.1-VERIFICATION.md` (192 lines, contains all 4 blocks + Decisión final + Post-merge sections)
- FOUND: `.planning/ROADMAP.md` updated (5 edits reviewed via `git diff`, matches spec)
- FOUND: commit `d9ba77f` (docs: verification checklist)
- FOUND: commit `d0e6a92` (docs: ROADMAP updates)
- Grep-checks: VERIFICATION.md 6/6 patterns present, ROADMAP.md 7/7 patterns present
- No production code touched (grep `src/` in commits d9ba77f + d0e6a92 → 0 matches, both docs-only)
