# Phase 04.1: Mobile Load Time Optimization — Verification Checklist

**Status:** Ready for CPO verification
**Estimated time:** ~20 minutos
**Requiere:** iPhone real (o Android nativo) conectado a la misma red que el dev laptop, o build de producción desplegado en `mi-plantita-feliz.vercel.app`

---

## Pre-requisitos

- [ ] Plan 04.1-01 completado (backfill ejecutado por la CPO — 98 filas migradas 2026-08-12)
- [ ] Plan 04.1-02 completado (thumbnail helper + `<img>` attrs + preconnect landed, tests 170/170)
- [ ] Plan 04.1-03 completado (hook + `perf_screen_loaded` event wireado en 3 pages, tests 179/179)
- [ ] `npm test` pasa localmente sin fallos
- [ ] `npm run lint` devuelve 0 errores
- [ ] La rama `feat/phase-3-calendar-v0` (o la que contenga esta sub-phase) está pusheada a remote y hay Vercel preview URL disponible — OR — ya hay merge a `main` y usas `mi-plantita-feliz.vercel.app`

---

## Bloque A — Sanity check post-backfill (2 min)

**Objetivo:** Verificar que el backfill dejó 0 filas legacy en la tabla `plant_searches`.

1. **[ ]** Ir a Supabase Dashboard → SQL Editor
2. **[ ]** Ejecutar:
   ```sql
   SELECT COUNT(*) AS legacy_rows
   FROM plant_searches
   WHERE image_url LIKE 'data:%';
   ```
   **Esperado:** `legacy_rows = 0`
3. **[ ]** Ejecutar:
   ```sql
   SELECT COUNT(*) AS storage_rows
   FROM plant_searches
   WHERE image_url LIKE 'https://%storage%';
   ```
   **Esperado:** `storage_rows >= 98` (todas las legacy migradas + cualquier upload nuevo desde 2026-08-12)
4. **[ ]** Opcional — comprobar que el bucket existe y tiene los blobs:
   ```sql
   SELECT COUNT(*) AS bucket_objects
   FROM storage.objects
   WHERE bucket_id = 'plant-images';
   ```
   **Esperado:** `bucket_objects >= 98`

---

## Bloque B — Verificación visual en dispositivo real (10 min)

**Setup:**
- **Opción 1 (recomendada — production):** Abre `https://mi-plantita-feliz.vercel.app` en Safari en tu iPhone. Login con tu cuenta habitual.
- **Opción 2 (LAN dev server):** `npm run dev` en tu Mac → conecta iPhone a la misma WiFi → abre Safari → `http://<ip-del-mac>:8080` (comprueba la IP con `ipconfig getifaddr en0`)
- **Opción 3 (preview Vercel):** si aún no hay merge, usa la Vercel preview URL del branch.

**Habilita Web Inspector para medir throttling y ver eventos:**
- En iPhone: Ajustes → Safari → Avanzado → Web Inspector = **ON**
- Conecta iPhone al Mac por USB
- En Mac: Safari → Desarrollo → tu iPhone → tu app (aparece cuando la app está en foreground)
- Abre Network tab

### B.1 — /mis-plantas (~3 min)

1. **[ ]** Abre la app, login si es necesario
2. **[ ]** En Safari (Mac) → Desarrollo → iPhone → tu app → Network tab
3. **[ ]** Cambia Network → Throttling → **"3G"** o **"Slow 3G"** (proxy para 4G malo — más agresivo que un test real)
4. **[ ]** Vacía cache (Comando+R con Cache tab open, o Settings → Clear website data para el dominio)
5. **[ ]** Navega a `/mis-plantas` (Ajustes → Mis Plantas, o directamente `/mis-plantas`)
6. **[ ]** Observa:
   - **Sensación:** ¿La primera card aparece "rápido" (<2s)? ¿O "lento" (>5s)?
   - **Network:** ¿Cuántas imágenes descargan inicialmente vs cuántas al hacer scroll? (Con lazy funcionando, deberían ser ~3-4 arriba, resto on-scroll)
   - **Layout:** ¿Se saltan las cards al cargar las imágenes? (Con `width`/`height` explícitos, NO debería haber layout shift)
7. **[ ]** Anota `ttfc_ms` observado si tienes DevTools open — para comparar con PostHog después.

**Criterio D-10 `/mis-plantas`:** TTFC < **1500ms** p50 (aceptable hasta ~**1875ms** con tolerancia D-11 ±25%).

**TTFC observado:** ___________ ms

### B.2 — /regar (~2 min)

1. **[ ]** Navega a `/regar` (bottom tab)
2. **[ ]** Observa:
   - **Sensación:** ¿Rápido / lento?
   - **Fotos:** ¿Se ven al cargar? ¿Layout estable?
   - **Interacción:** Tap el botón "Regar" en una card → toast aparece → animación flash verde 1s (no debe estar rota por los cambios de `<img>`)
3. **[ ]** Anota `ttfc_ms` observado.

**Criterio D-10 `/regar`:** TTFC < **1500ms** p50 (~**1875ms** con tolerancia).

**TTFC observado:** ___________ ms

### B.3 — /mapa (~2 min)

1. **[ ]** Navega a `/mapa` (bottom tab, solo aparece si tienes ≥1 planta wild con coords)
2. **[ ]** Observa:
   - **Tiles OSM:** ¿Aparecen rápido? (con preconnect debería estar ~200ms más rápido que sin él en la primera visita)
   - **Pins:** ¿Aparecen tras ~2s? Antes eran síncronos con el fetch; ahora sigue igual pero los preconnect deberían ayudar
   - Tap un pin → sheet abre → foto del PlantMapSheet se ve
3. **[ ]** Anota tiempo hasta ver los pins.

**Criterio D-10 `/mapa`:** Pins render < **2000ms** p50 (~**2500ms** con tolerancia).

**Tiempo hasta pins observado:** ___________ ms

### B.4 — Scroll test (~2 min)

En cada una de `/mis-plantas` y `/regar` (si tienes >20 items):
1. **[ ]** Scroll rápido arriba/abajo repetido varias veces
2. **[ ]** ¿60fps? Fluido = sí. Judder / stutter = no.

**Criterio D-10 scroll:** 60fps con 50+ items. Con <20 items en `/regar` esto es cheap; en `/mis-plantas` (>50 items posible) es la prueba real.

**Resultado:** ☐ Fluido (60fps) · ☐ Aceptable (algún micro-judder) · ☐ Judder / stutter visible

---

## Bloque C — Verificación del evento perf_screen_loaded (5 min)

**Objetivo:** Confirmar que el evento llega a PostHog con el shape correcto y desde AMBOS shells (web + Capacitor Android).

### C.1 — Desde iPhone PWA / web

1. **[ ]** Con la app abierta en iPhone/prod, navega por `/mis-plantas` → `/regar` → `/mapa` una vez cada una (asegúrate de haber aceptado el consent de analytics — si no, el evento no se dispara)
2. **[ ]** Abre PostHog EU (https://eu.i.posthog.com) → tu project → Activity → Live events
3. **[ ]** Filtra por event = `perf_screen_loaded`
4. **[ ]** Verifica que aparecen 3 eventos en los últimos 5 min con:
   - `screen`: uno con `"mis-plantas"`, uno con `"regar"`, uno con `"mapa"`
   - `ttfc_ms`: número entero > 0
   - `plants_count`: número entero > 0
   - `app_platform`: `"web"` (si estás en Safari iOS PWA) o `"android"` (si estás en la app Capacitor)
   - `app_version`: string (debe matchear la version de `package.json`, p.ej. `"1.2.0"`)

**Eventos recibidos:** ☐ mis-plantas · ☐ regar · ☐ mapa
**app_platform correcto (`web` desde iOS PWA):** ☐ sí · ☐ no
**app_version presente (matchea package.json):** ☐ sí · ☐ no

### C.2 — Desde Android nativo (Capacitor) — opcional pero recomendado

1. **[ ]** Instala la app nativa (build reciente con los cambios de esta phase). Ver `docs/play-store-publish-guide.md` si necesitas rebuild.
2. **[ ]** Repite C.1 pasos 1-4
3. **[ ]** Verifica que ahora `app_platform` = `"android"` en los eventos

**app_platform desde Capacitor:** ☐ `android` · ☐ otro (anotar): _____

---

## Bloque D — Comparativa post-deploy (post-merge, 2 min setup + 24-48h de espera)

**Objetivo:** Una vez la sub-phase esté en producción y con ≥24h de datos reales de usuarios.

1. **[ ]** En PostHog → Insights → nueva insight de tipo "Trends"
2. **[ ]** Filtra por event = `perf_screen_loaded`
3. **[ ]** Break down por `screen`
4. **[ ]** Métrica: `median(ttfc_ms)`
5. **[ ]** Rango: últimos 7 días
6. **[ ]** Anota los 3 medianas (una por screen) y compáralas con los criterios D-10:

| Screen | median(ttfc_ms) observado | D-10 target | D-11 tolerancia | Cumple |
|--------|---------------------------|-------------|-----------------|--------|
| `mis-plantas` | ___ ms | 1500 ms | 1875 ms | ☐ |
| `regar` | ___ ms | 1500 ms | 1875 ms | ☐ |
| `mapa` | ___ ms | 2000 ms | 2500 ms | ☐ |

**Nota:** Este bloque D se hace DESPUÉS de mergear a `main` — no es bloqueante para el merge en sí. Sirve para validar en usuarios reales que los fixes funcionan a escala.

---

## Decisión final

- [ ] **APPROVED** — Los 3 criterios D-10 se cumplen (con tolerancia D-11 ±25% aceptable). Mergear PR a `main`.
- [ ] **NEEDS ADJUSTMENT** — Al menos uno de los criterios está claramente peor de lo esperado. Considerar:
  - Path A (RESEARCH § Critical Blocker): upgrade a Supabase Pro para activar las transformations reales (thumbnails al vuelo)
  - Path B: generar thumbnails en el backfill (nuevo plan que produce N tamaños por foto y guarda las URLs derivadas)
  - Volver a discuss-phase para reevaluar scope

**Decisión CPO:** ☐ APPROVED · ☐ NEEDS ADJUSTMENT
**Fecha de la decisión:** ___________
**Notas:**

---

## Post-merge (recordatorio)

Después del merge:

1. **[ ]** Verificar Bloque D en 24-48h con datos reales de usuarios
2. **[ ]** Si Bloque D confirma los umbrales, cerrar Phase 04.1 en RETROSPECTIVE.md
3. **[ ]** Considerar liberar Android v1.1.1 con los cambios (si compensa vs esperar a agrupar más features)

---

*Verification checklist created: 2026-08-12*
