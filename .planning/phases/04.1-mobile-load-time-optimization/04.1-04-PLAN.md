---
phase: 04.1-mobile-load-time-optimization
plan: 04
type: execute
wave: 4
depends_on: [04.1-01, 04.1-02, 04.1-03]
files_modified:
  - .planning/phases/04.1-mobile-load-time-optimization/04.1-VERIFICATION.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [PERF-04, PERF-05, PERF-06, PERF-07, PERF-08, STOR-01]
tags: [verification, roadmap, docs]

must_haves:
  truths:
    - "Existe un archivo VERIFICATION.md en el directorio de la phase con checklist manual completo para la CPO"
    - "El checklist cubre las 3 pantallas: `/mis-plantas`, `/regar`, `/mapa`"
    - "El checklist incluye pasos para verificar en dispositivo real (iPhone via LAN) con network throttled a 4G"
    - "El checklist incluye pasos para verificar el evento `perf_screen_loaded` en PostHog UI"
    - "El checklist incluye pasos de sanity check post-backfill: 0 filas con `image_url LIKE 'data:%'` en Supabase"
    - "Los umbrales D-10 están en el checklist explícitamente (1.5s / 1.5s / 2s) con la nota de ±25% (D-11)"
    - "ROADMAP.md tiene Phase 04.1 marcada como `[x]` (completada) con fecha"
    - "ROADMAP.md tiene Phase 6 marcada con nota `**Folded into Phase 04.1** (backfill executed YYYY-MM-DD via npm run backfill:images). Kept as reference only.`"
    - "ROADMAP.md lista los 4 plans de Phase 04.1 con checkmarks `[x]`"
  artifacts:
    - path: ".planning/phases/04.1-mobile-load-time-optimization/04.1-VERIFICATION.md"
      provides: "Manual verification checklist for CPO to run pre-merge"
      min_lines: 100
    - path: ".planning/ROADMAP.md"
      provides: "Updated to reflect Phase 04.1 completion + Phase 6 fold-in"
      contains: "Folded into Phase 04.1"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: "Phase 6 (candidate)"
      via: "'Folded into' cross-reference"
      pattern: "Folded into Phase 04.1"
    - from: ".planning/phases/04.1-mobile-load-time-optimization/04.1-VERIFICATION.md"
      to: "PostHog event perf_screen_loaded"
      via: "verification checklist references the event by name"
      pattern: "perf_screen_loaded"
---

<objective>
Cerrar Phase 04.1 con: (a) un checklist manual VERIFICATION.md para que la CPO valide en dispositivo real que los umbrales D-10 se cumplen antes de mergear a `main`, y (b) actualización del ROADMAP.md marcando Phase 04.1 como completada y absorbiendo Phase 6 (backfill) como "folded in".

Purpose: Cierre limpio de la sub-phase. Sin este plan, ni la CPO tiene una lista concreta de qué probar en su iPhone, ni el ROADMAP refleja el trabajo hecho — quedaría deuda de coordinación para futuras phases.

Output:
- `04.1-VERIFICATION.md` con checklist ejecutable en ~20 min por la CPO
- `ROADMAP.md` actualizado: Phase 04.1 = ✅, Phase 6 = "folded into 04.1"
- Este plan NO toca código productivo. Solo docs + verificación humana.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-mobile-load-time-optimization/04.1-CONTEXT.md
@.planning/phases/04.1-mobile-load-time-optimization/04.1-RESEARCH.md
@.planning/ROADMAP.md
@CLAUDE.md
@.planning/phases/04.1-mobile-load-time-optimization/04.1-01-SUMMARY.md
@.planning/phases/04.1-mobile-load-time-optimization/04.1-02-SUMMARY.md
@.planning/phases/04.1-mobile-load-time-optimization/04.1-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 04.1-VERIFICATION.md with CPO manual verification checklist</name>
  <files>
    .planning/phases/04.1-mobile-load-time-optimization/04.1-VERIFICATION.md
  </files>
  <action>
    Create the file with the following structure (Spanish content, English identifiers):

    ```markdown
    # Phase 04.1: Mobile Load Time Optimization — Verification Checklist

    **Status:** Ready for CPO verification
    **Estimated time:** ~20 minutos
    **Requiere:** iPhone real (o Android nativo) conectado a la misma red que el dev laptop, o build de producción desplegado

    ---

    ## Pre-requisitos

    - [ ] Plan 04.1-01 completado (backfill ejecutado por la CPO)
    - [ ] Plan 04.1-02 completado (thumbnail helper + <img> attrs + preconnect)
    - [ ] Plan 04.1-03 completado (perf_screen_loaded event wireado)
    - [ ] `npm test` pasa localmente sin fallos
    - [ ] `npm run lint` devuelve 0 errores

    ---

    ## Bloque A — Sanity check post-backfill (2 min)

    **Objetivo:** Verificar que el backfill dejó 0 filas legacy.

    1. **[ ]** Ir a Supabase Dashboard → SQL Editor
    2. **[ ]** Ejecutar:
       ```sql
       SELECT COUNT(*) AS legacy_rows
       FROM plant_searches
       WHERE image_url LIKE 'data:%';
       ```
       **Esperado:** `legacy_rows = 0`
    3. **[ ]** Ejecutar:
       ```sql
       SELECT COUNT(*) AS storage_rows
       FROM plant_searches
       WHERE image_url LIKE 'https://%storage%';
       ```
       **Esperado:** `storage_rows >= 124` (todas las legacy migradas + cualquier upload nuevo)

    ---

    ## Bloque B — Verificación visual en dispositivo real (10 min)

    **Setup:**
    - Opción 1 (LAN dev server): `npm run dev` en tu Mac → conecta iPhone a la misma WiFi → abre Safari → `http://<ip-del-mac>:8080` (comprueba la IP con `ipconfig getifaddr en0`)
    - Opción 2 (build prod): desplegar a Vercel preview → abre en iPhone
    - En Safari: Ajustes → Safari → Avanzado → Web Inspector = ON → conectar iPhone al Mac por USB → Safari en Mac → Desarrollo → tu iPhone

    ### B.1 — /mis-plantas (~3 min)

    1. **[ ]** Abre la app, login si es necesario
    2. **[ ]** En Safari (Mac) → Desarrollo → iPhone → tu app → Network tab
    3. **[ ]** Cambia Network → Throttling → **"3G"** o **"Slow 3G"** (proxy para 4G malo — más agresivo que un test real)
    4. **[ ]** Vacía cache (Comando+R con Cache tab open, o Settings → Clear website data para el dominio)
    5. **[ ]** Navega a `/mis-plantas` (Ajustes → Mis Plantas)
    6. **[ ]** Observa:
       - **Sensación:** ¿La primera card aparece "rápido" (<2s)? ¿O "lento" (>5s)?
       - **Network:** ¿Cuántas imágenes descargan inicialmente vs cuántas al hacer scroll? (Con lazy funcionando, deberían ser ~3-4 arriba, resto on-scroll)
       - **Layout:** ¿Se saltan las cards al cargar las imágenes? (Con width/height explícitos, NO debería haber layout shift)
    7. **[ ]** Anota `ttfc_ms` observado si tienes DevTools open — para comparar con PostHog después.

    **Criterio D-10 `/mis-plantas`:** TTFC < 1500ms p50 (aceptable hasta ~1875ms con tolerancia D-11 ±25%).

    ### B.2 — /regar (~2 min)

    1. **[ ]** Navega a `/regar` (bottom tab)
    2. **[ ]** Observa:
       - **Sensación:** ¿Rápido / lento?
       - **Fotos:** ¿Se ven al cargar? ¿Layout estable?
       - **Interacción:** Tap el botón "Regar" en una card → toast aparece → animación flash verde 1s (no debe estar rota por los cambios de <img>)

    **Criterio D-10 `/regar`:** TTFC < 1500ms p50 (~1875ms con tolerancia).

    ### B.3 — /mapa (~2 min)

    1. **[ ]** Navega a `/mapa` (bottom tab, solo aparece si tienes ≥1 planta wild con coords)
    2. **[ ]** Observa:
       - **Tiles OSM:** ¿Aparecen rápido? (con preconnect debería estar ~200ms más rápido que sin él en la primera visita)
       - **Pins:** ¿Aparecen tras ~2s? Antes eran síncronos con el fetch; ahora sigue igual pero los preconnect deberían ayudar
       - Tap un pin → sheet abre → foto del PlantMapSheet se ve

    **Criterio D-10 `/mapa`:** Pins render < 2000ms p50 (~2500ms con tolerancia).

    ### B.4 — Scroll test (~2 min)

    En cada una de `/mis-plantas` y `/regar` (si tienes >20 items):
    1. **[ ]** Scroll rápido arriba/abajo repetido varias veces
    2. **[ ]** ¿60fps? Fluido = sí. Judder / stutter = no.

    **Criterio D-10 scroll:** 60fps con 50+ items. Con <20 items en `/regar` esto es cheap; en `/mis-plantas` (>50 items posible) es la prueba real.

    ---

    ## Bloque C — Verificación del evento perf_screen_loaded (5 min)

    **Objetivo:** Confirmar que el evento llega a PostHog con el shape correcto y desde AMBOS shells (web + Capacitor Android).

    ### C.1 — Desde iPhone PWA / web

    1. **[ ]** Con la app abierta en iPhone/prod, navega por /mis-plantas → /regar → /mapa una vez cada una
    2. **[ ]** Abre PostHog EU (eu.i.posthog.com) → tu project → Events (live view)
    3. **[ ]** Filtra por event = `perf_screen_loaded`
    4. **[ ]** Verifica que aparecen 3 eventos en los últimos 5 min con:
       - `screen`: uno con `"mis-plantas"`, uno con `"regar"`, uno con `"mapa"`
       - `ttfc_ms`: número > 0
       - `plants_count`: número > 0
       - `app_platform`: `"web"` (si estás en Safari) o `"android"` (si estás en la app Capacitor)
       - `app_version`: string (debe matchear la version de package.json, p.ej. "1.2.0")

    ### C.2 — Desde Android nativo (Capacitor) — opcional pero recomendado

    1. **[ ]** Instala la app nativa (build reciente con los cambios de esta phase). Ver `docs/play-store-publish-guide.md` si necesitas rebuild.
    2. **[ ]** Repite C.1 pasos 1-4
    3. **[ ]** Verifica que ahora `app_platform` = `"android"` en los eventos

    ---

    ## Bloque D — Comparativa post-deploy (2 min)

    **Objetivo:** Una vez la sub-phase esté en producción y con ≥24h de datos reales de usuarios.

    1. **[ ]** En PostHog → Insights → nueva insight de tipo "Trends"
    2. **[ ]** Filtra por event = `perf_screen_loaded`
    3. **[ ]** Break down por `screen`
    4. **[ ]** Métrica: `median(ttfc_ms)`
    5. **[ ]** Rango: últimos 7 días
    6. **[ ]** Anota los 3 medianas (una por screen) y compáralas con los criterios D-10

    **Nota:** Este bloque D se hace DESPUÉS de mergear a `main` — no es bloqueante para el merge en sí. Sirve para validar en usuarios reales que los fixes funcionan a escala.

    ---

    ## Decisión final

    - [ ] **APPROVED** — Los 3 criterios D-10 se cumplen (con tolerancia D-11 ±25% aceptable). Mergear PR a `main`.
    - [ ] **NEEDS ADJUSTMENT** — Al menos uno de los criterios está claramente peor de lo esperado. Considerar:
      - Path A (RESEARCH § Critical Blocker): upgrade a Supabase Pro para activar las transformations reales
      - Path B: generar thumbnails en el backfill (nuevo plan)
      - Volver a discuss-phase para reevaluar scope

    ---

    ## Post-merge (recordatorio)

    Después del merge:

    1. **[ ]** Verificar Bloque D en 24-48h con datos reales
    2. **[ ]** Si Bloque D confirma los umbrales, cerrar Phase 04.1 en RETROSPECTIVE.md
    3. **[ ]** Considerar liberar Android v1.1.1 con los cambios (si compensa vs esperar a agrupar más features)

    ---

    *Verification checklist created: {DATE_TODAY}*
    ```

    **Note on {DATE_TODAY}:** replace with today's date at file creation time (2026-08-12).
  </action>
  <verify>
    <automated>test -f .planning/phases/04.1-mobile-load-time-optimization/04.1-VERIFICATION.md && grep -q 'perf_screen_loaded' .planning/phases/04.1-mobile-load-time-optimization/04.1-VERIFICATION.md && grep -q '1500ms' .planning/phases/04.1-mobile-load-time-optimization/04.1-VERIFICATION.md && grep -q '2000ms' .planning/phases/04.1-mobile-load-time-optimization/04.1-VERIFICATION.md && grep -q 'plant_searches' .planning/phases/04.1-mobile-load-time-optimization/04.1-VERIFICATION.md && grep -q 'app_platform' .planning/phases/04.1-mobile-load-time-optimization/04.1-VERIFICATION.md && echo OK</automated>
  </verify>
  <done>
    - `.planning/phases/04.1-mobile-load-time-optimization/04.1-VERIFICATION.md` exists
    - Contains 4 blocks (A/B/C/D) + Pre-requisitos + Decisión final + Post-merge
    - All D-10 thresholds explicit (1.5s / 1.5s / 2s)
    - Documents both web + Android verification paths
    - Documents SQL check for legacy rows == 0
    - Documents PostHog event schema verification
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md — mark Phase 04.1 complete + fold Phase 6 in</name>
  <files>
    .planning/ROADMAP.md
  </files>
  <action>
    Edit `.planning/ROADMAP.md` (only 2 sections):

    **A. Update Phase 04.1 status:**

    1. Change line 18 from:
       ```
       - [ ] **Phase 04.1: Mobile Load Time Optimization** — Acelerar el tiempo de carga de las 3 pantallas de listado: `/mis-plantas`, `/regar`, `/mapa`
       ```
       To:
       ```
       - [x] **Phase 04.1: Mobile Load Time Optimization** — Acelerar el tiempo de carga de las 3 pantallas de listado: `/mis-plantas`, `/regar`, `/mapa` (completed YYYY-MM-DD — sustituir con fecha real al ejecutar)
       ```
       (Replace `YYYY-MM-DD` with today's date at execution — the executor of this plan will fill it in.)

    2. Update the "Plans" section for Phase 04.1 (around lines 141-144). Change from:
       ```
       **Plans:** 0 plans

       Plans:
       - [ ] TBD (run /gsd-plan-phase 04.1 to break down)
       ```
       To:
       ```
       **Plans:** 4 plans

       Plans:
       - [x] 04.1-01-PLAN.md — Backfill legacy base64 images to Supabase Storage (CPO manual execution per D-05)
       - [x] 04.1-02-PLAN.md — Thumbnail URL helper + <img> lazy/decoding/width/height + preconnect hints
       - [x] 04.1-03-PLAN.md — perf_screen_loaded PostHog event + usePerfScreenLoaded hook wired into 3 pages
       - [x] 04.1-04-PLAN.md — VERIFICATION.md checklist + ROADMAP updates (this plan)
       ```

    **B. Update Phase 6 to note it was absorbed:**

    Find the Phase 6 entry (around line 20 in the "Phases" list AND around line 181+ in "Phase Details"). Update BOTH:

    1. Line 20 (top-level phase list) — change from:
       ```
       - [ ] **Phase 6: Backfill imágenes históricas base64 → Storage** *(candidate)* — Migrar las ~124 filas legacy de `plant_searches.image_url` (data:image/jpeg;base64,...) a URLs HTTPS en Supabase Storage; reducir tamaño DB y eliminar riesgo OOM en Capacitor
       ```
       To:
       ```
       - [x] **Phase 6: Backfill imágenes históricas base64 → Storage** — **Folded into Phase 04.1** (backfill executed YYYY-MM-DD via `npm run backfill:images`). Kept as historical reference; the goal was fulfilled by Plan 04.1-01.
       ```

    2. Section header around line 181, "### Phase 6: Backfill imágenes históricas base64 → Storage (CANDIDATE)". Change the `**Status:**` line from:
       ```
       **Status:** Candidate — NOT scheduled. Independiente de Phase 5; las dos pueden hacerse en cualquier orden.
       ```
       To:
       ```
       **Status:** **FOLDED INTO Phase 04.1** (executed YYYY-MM-DD via `npm run backfill:images`, plan 04.1-01). This section is preserved as historical context (rationale, tradeoffs, findings) that motivated the work — the actual execution and success criteria were absorbed into Phase 04.1's plan 01.
       ```

    3. Just below that status line, add a new sub-section:
       ```
       **Absorbed by:** [.planning/phases/04.1-mobile-load-time-optimization/04.1-01-PLAN.md](phases/04.1-mobile-load-time-optimization/04.1-01-PLAN.md)

       ```
       (with the blank line after so markdown renders correctly)

    **Note on YYYY-MM-DD:** replace both occurrences with the actual date the backfill was run — you can find this in `04.1-01-SUMMARY.md` (per Plan 01 output requirement). If SUMMARY says "2026-08-12", use "2026-08-12".

    **Read the SUMMARY FIRST:**
    ```bash
    cat .planning/phases/04.1-mobile-load-time-optimization/04.1-01-SUMMARY.md 2>/dev/null | grep -i 'date\|executed\|ran'
    ```
    If SUMMARY doesn't yet exist (e.g., running Plan 04 speculatively before Plan 01 checkpoint completes), use today's date as placeholder and note in this plan's own SUMMARY that a follow-up edit will be needed.
  </action>
  <verify>
    <automated>grep -q 'Folded into Phase 04.1' .planning/ROADMAP.md && grep -q '04.1-01-PLAN.md' .planning/ROADMAP.md && grep -q '04.1-02-PLAN.md' .planning/ROADMAP.md && grep -q '04.1-03-PLAN.md' .planning/ROADMAP.md && grep -q '04.1-04-PLAN.md' .planning/ROADMAP.md && grep -q '\[x\] \*\*Phase 04.1' .planning/ROADMAP.md && grep -q '\*\*FOLDED INTO Phase 04.1\*\*' .planning/ROADMAP.md && echo OK</automated>
  </verify>
  <done>
    - Phase 04.1 marked `[x]` with completion date in the top-level list AND the Plans section
    - All 4 sub-plans listed with `[x]` checkmarks and brief descriptions
    - Phase 6 marked `[x]` with "Folded into Phase 04.1" note in top-level list
    - Phase 6 detail section has updated Status + "Absorbed by:" cross-reference
    - No other Phase entries touched (Phases 1-5 untouched)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    VERIFICATION.md checklist + ROADMAP.md updates. Sub-phase 04.1 documentation is now consistent (Phase 04.1 = done, Phase 6 = folded in). The VERIFICATION checklist is ready for CPO to execute pre-merge.
  </what-built>
  <how-to-verify>
    **This is the last checkpoint before merge. CPO should run the manual verification checklist:**

    1. **[ ]** Open `.planning/phases/04.1-mobile-load-time-optimization/04.1-VERIFICATION.md`
    2. **[ ]** Run Bloque A (SQL check) — ~2 min
    3. **[ ]** Run Bloque B (dispositivo real, throttled network) — ~10 min
    4. **[ ]** Run Bloque C (PostHog event verification) — ~5 min
    5. **[ ]** Mark Decisión final: APPROVED or NEEDS ADJUSTMENT

    **If APPROVED:** merge `feat/phase-04.1-mobile-load-time-optimization` (o el branch en el que trabajaste) → `main`. Después ejecutar Bloque D en 24-48h con datos reales.

    **If NEEDS ADJUSTMENT:** no mergear. Volver a plan-phase con `--gaps` para crear plans que cierren la brecha. Considerar path A (Pro upgrade) o path B (backfill genera thumbnails) del RESEARCH.md § Critical Blocker.

    **Ojo — no bloqueante para este checkpoint:** Bloque D es post-merge; el checkpoint se cierra con APPROVED/NEEDS ADJUSTMENT de Bloques A+B+C.
  </how-to-verify>
  <resume-signal>Type "approved" para cerrar la sub-phase, o "needs adjustment: <descripción>" si hay gaps que cerrar.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs authoring → repo | Markdown files committed to git; no runtime code changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1.04-01 | Repudiation | ROADMAP claims work is done when it isn't | mitigate | Task 3 (checkpoint:human-verify) is BLOCKING. CPO explicitly approves before merge, closing the gap between "plan says done" and "reality is done". |
| T-04.1.04-02 | Tampering | Merge-conflict garbles the ROADMAP or introduces incorrect status | mitigate | Small, targeted edits (only 2 sections). Git diff on the PR is trivial to review. |

**Notes:** No code, no data, no secrets touched. This plan is docs + human verification only. STRIDE surface is minimal.
</threat_model>

<verification>
- Automated grep confirms VERIFICATION.md contains all required blocks + thresholds
- Automated grep confirms ROADMAP.md has both "Phase 04.1 done" + "Phase 6 folded in" markers
- CPO checkpoint: APPROVED / NEEDS ADJUSTMENT decision recorded
</verification>

<success_criteria>
- VERIFICATION.md checklist executable in ~20 min covers all 4 blocks
- ROADMAP.md accurately reflects Phase 04.1 completion + Phase 6 absorption
- CPO has explicit go/no-go for merge based on real device measurements
- Post-merge: Bloque D provides ongoing validation from real users
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-mobile-load-time-optimization/04.1-04-SUMMARY.md`.

The SUMMARY MUST record:
- CPO's Decisión final (APPROVED or NEEDS ADJUSTMENT)
- Actual measured TTFC values from Bloque B (if CPO recorded them)
- PostHog event confirmation from Bloque C (yes/no per platform)
- Any deviations from D-10 that CPO accepted per D-11 tolerance
- Follow-up items for the post-merge window (Bloque D window)
- Note that Phase 6 is now closed as "folded in" — no separate execution needed
</output>
</content>
</invoke>