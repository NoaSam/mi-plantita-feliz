# Phase 04.1: Mobile Load Time Optimization - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-12
**Phase:** 04.1-mobile-load-time-optimization
**Areas discussed:** Alcance y prioridad, Fotos legacy, Loading state, Umbrales éxito

---

## A. Alcance y prioridad entre pantallas

| Option | Description | Selected |
|--------|-------------|----------|
| Atacar las 3 en paralelo, releases parciales | Ship de cada pantalla cuando esté lista | |
| Orden estricto (una primero) | La que más duele primero, luego las siguientes | |
| Las 3 juntas en un solo release final | Trabajar todo internamente, ship único al final | ✓ |

**User's choice:** Un solo release final con las 3 pantallas listas.
**Notes:** Consistente con estilo previo (Phase 3 hizo múltiples fixes en un solo PR de merge). Reduce ceremonia de releases intermedios.

---

## B. Fotos legacy base64 — backfill o parche

| Option | Description | Selected |
|--------|-------------|----------|
| Backfill (fix raíz) | Script migra las ~124 filas base64 → HTTPS Storage | ✓ |
| Parche (lazy loading + thumbnails CSS) | Rápido pero deuda técnica sigue | |

**User's choice:** Backfill (raíz).
**Notes:** Absorbe el trabajo de la Phase 6 (CANDIDATE) previamente separada. Al ejecutar, marcamos Phase 6 como "folded into 04.1" en el ROADMAP.

---

## C. Estado de carga visible

| Option | Description | Selected |
|--------|-------------|----------|
| Skeleton (cards fantasma) | Percepción de rapidez, más trabajo | |
| Spinner grande centrado (actual) | Honesto, minimalista, "esperando" | ✓ |
| Nada + progressive rendering | Header + poblar según llega | |

**User's choice:** Mantener spinner Leaf actual.
**Notes:** Skeletons no se implementan en esta phase. Diferido si post-medición se ve que la percepción sigue siendo un problema.

---

## D. Cuándo consideramos que "va rápido"

| Option | Description | Selected |
|--------|-------------|----------|
| A — Métrica objetiva | Umbrales concretos (TTFC, fps) | ✓ |
| B — Subjetivo tuyo | Shipear, probar en iPhone, decidir | |
| C — Feedback de tester | Ship beta, esperar semana | |

**User's choice:** A — Métrica objetiva.
**Notes:** CPO delegó los números concretos a Claude ("no me molestes"). Umbrales propuestos y locked en CONTEXT.md D-10: TTFC < 1.5s, scroll 60fps con 50 items, pins mapa < 2s.

---

## Claude's Discretion

CPO explicitó: **"Empieza a trabajar pero no me molestes ni preguntes nada que no sea estrictamente necesario"**. Áreas donde Claude decide sin re-consultar:

- Números concretos de umbrales (D-10, D-11)
- Uso de Supabase Storage on-the-fly image transformations (D-08)
- Atributos HTML nativos para lazy loading (D-09)
- Nombre y props del evento PostHog `perf_screen_loaded` (D-12)
- Orden interno de trabajo dentro del release único (D-02)
- Idempotencia y ejecución manual del script backfill (D-04, D-05)
- Paginación, virtualization, React Query migration — evaluación técnica sin consultar
- Backup strategy antes del backfill (pg_dump / supabase db dump)

## Deferred Ideas

- Skeleton screens (diferido, phase futura si perception sigue lenta)
- Virtualization de listas (defer salvo que 60fps no se cumpla)
- Búsqueda/filtros server-side en `/mis-plantas` (nueva capability, su propia phase)
- Preload/preconnect a Supabase + PostHog (posible optimización menor dentro del plan)
- Skeleton para `/mapa` tiles OSM loading
