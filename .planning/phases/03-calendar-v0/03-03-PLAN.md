---
phase: 03-calendar-v0
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - supabase/migrations/20260518000000_add_last_watered_at_to_plant_searches.sql
  - src/integrations/supabase/types.ts
  - src/lib/watering-countdown.ts
  - src/lib/watering-countdown.test.ts
  - src/hooks/use-home-plants.ts
  - src/hooks/use-log-watering.ts
  - src/hooks/use-log-watering.test.ts
  - src/components/PlantWateringCard.tsx
  - src/pages/RegarPage.tsx
  - tailwind.config.ts
autonomous: true
requirements: [RIEG-02]
requirements_addressed: [RIEG-02]
tags: [phase-03, migration, watering-countdown, optimistic-update, sonner-action]

must_haves:
  truths:
    - "Migración SQL `20260518000000_add_last_watered_at_to_plant_searches.sql` añade la columna `last_watered_at timestamptz null` a `plant_searches`. Sin GRANT (tabla existente, cláusula MIGRATION_CONVENTIONS.md). La RLS UPDATE policy (20260515000000) cubre la columna automáticamente."
    - "`src/integrations/supabase/types.ts` regenerado: `plant_searches.Row/Insert/Update` incluye `last_watered_at: string | null`."
    - "Existe la pura función `computeDaysRemaining(lastWateredAt, intervalDays)` en `src/lib/watering-countdown.ts` que retorna `number | null`. Null cuando `lastWateredAt === null` OR `intervalDays === null`."
    - "Existe el unit test `src/lib/watering-countdown.test.ts` cubriendo: null branches (both args), X>0, X=0, X<0, edge cases (exact midnight transitions, leap-year days, timezone-agnostic UTC ms math)."
    - "`useHomePlants` extendido: SELECT incluye `last_watered_at`; `HomePlant` type añade `lastWateredAt: string | null`."
    - "`useHomePlants` listens también a `mp:plant-watered` (NUEVO evento de esta sub-phase) además de los listeners ya cableados en 3-02."
    - "Existe el hook `useLogWatering` con función `logWatering(plantId)` modelado verbatim sobre `useClassifyPlant`: hace UPDATE de `last_watered_at = new Date().toISOString()`, dispatcha `mp:plant-watered`, retorna `{ok: boolean}`."
    - "Existe la función `revertWatering(plantId, previousLastWateredAt)` en el mismo hook que revierte la columna al valor anterior. Retorna `{ok: boolean}` y dispatcha también `mp:plant-watered`."
    - "Existe el unit test `use-log-watering.test.ts` cubriendo: success path, error path (RLS denied), revert path. Patrón `makeChain` de los otros tests."
    - "`PlantWateringCard` ahora consume `computeDaysRemaining` para derivar `status` (normal/urgent/overdue/pending-first) y el badge/buttonLabel/statusText cambian per D-08, D-10, D-15."
    - "El botón onclick wired: `handleWater` llama a `onWater(plant.id)` (que el padre wirea a `logWatering`). Optimistic update visible (badge cambia, status text cambia) ANTES del UPDATE a DB. Si DB falla, toast.error + rollback."
    - "Sonner toast con action button `Deshacer`: `toast(\"✓ Regada · Siguiente riego en N días\", { action: { label: \"Deshacer\", onClick: revertHandler }, duration: 4000 })`. El undo invoca `revertWatering`."
    - "Flash green animation 1s: nueva keyframe `flash-success` en `tailwind.config.ts`, aplicada al card via `animate-flash-success` activado por state local en `RegarPage` cuando una planta acaba de ser regada."
    - "`RegarPage` ahora aplica sort cliente-side por urgencia (D-09): X<0 primero, X=0 después, X>0 ascendente, pending-first al final; empates por nombre común alfabético."
    - "Si la UPDATE DB falla, el optimistic se revierte automáticamente y muestra `toast.error('No se pudo guardar el riego. Inténtalo de nuevo.')`."
  artifacts:
    - path: "supabase/migrations/20260518000000_add_last_watered_at_to_plant_searches.sql"
      provides: "ALTER TABLE plant_searches ADD COLUMN IF NOT EXISTS last_watered_at timestamptz null"
      contains: "last_watered_at"
    - path: "src/lib/watering-countdown.ts"
      provides: "Pure function computeDaysRemaining + helper computeStatus"
      exports: ["computeDaysRemaining", "computeStatus", "type WateringStatus"]
      min_lines: 50
    - path: "src/lib/watering-countdown.test.ts"
      provides: "Unit tests covering all 4 status branches + edge cases"
      min_lines: 90
    - path: "src/hooks/use-log-watering.ts"
      provides: "Optimistic UPDATE hook with logWatering + revertWatering"
      exports: ["useLogWatering", "UseLogWateringReturn"]
      min_lines: 70
    - path: "src/hooks/use-log-watering.test.ts"
      provides: "Unit tests for log + revert + error paths"
      min_lines: 100
    - path: "src/integrations/supabase/types.ts"
      provides: "Regenerated types include last_watered_at"
      contains: "last_watered_at"
    - path: "tailwind.config.ts"
      provides: "Adds flash-success keyframe + animate-flash-success animation"
      contains: "flash-success"
  key_links:
    - from: "src/components/PlantWateringCard.tsx"
      to: "src/lib/watering-countdown.ts"
      via: "computeStatus({lastWateredAt, intervalDays, now}) drives status branching"
      pattern: "computeStatus"
    - from: "src/components/PlantWateringCard.tsx"
      to: "Sonner toast"
      via: "toast(..., { action: { label: 'Deshacer', onClick: handleUndo }, duration: 4000 })"
      pattern: "action: \\{ label: \"Deshacer\""
    - from: "src/pages/RegarPage.tsx"
      to: "src/hooks/use-log-watering.ts"
      via: "logWatering(plantId) + onWater prop passed to PlantWateringCard"
      pattern: "useLogWatering"
    - from: "src/hooks/use-log-watering.ts"
      to: "window CustomEvent mp:plant-watered"
      via: "window.dispatchEvent in log + revert"
      pattern: "mp:plant-watered"
    - from: "src/hooks/use-home-plants.ts"
      to: "window listener mp:plant-watered"
      via: "addEventListener triggers refetch"
      pattern: "mp:plant-watered"
---

<objective>
Hacer el calendario REALMENTE funcional:

1. **Migración SQL**: añadir `last_watered_at timestamptz` a `plant_searches` (D-06). Sin GRANT (tabla existente). La RLS UPDATE policy existente cubre la columna por row-level (no column-level) protection — verificado en research.
2. **Pure function**: `computeDaysRemaining(lastWateredAt, intervalDays)` + `computeStatus({...})` en `src/lib/watering-countdown.ts`. Estos son los building blocks puros del countdown D-08. Unit tested exhaustivamente.
3. **Hook `useLogWatering`**: modelado verbatim sobre `useClassifyPlant`. Funciones `logWatering(plantId)` y `revertWatering(plantId, previousLastWateredAt)`. Dispatchan `mp:plant-watered` para que `useHomePlants` refetche.
4. **Extender `useHomePlants`**: añadir `lastWateredAt` al type + SELECT + listener `mp:plant-watered`.
5. **Wire `PlantWateringCard`**: ahora consume `computeStatus()` para derivar el estado. El botón "Regada"/"Regar" llama optimisticamente a `onWater` (recibido del padre) y dispara Sonner toast con `Deshacer` action.
6. **`RegarPage`**: pasa `onWater` cableado a `useLogWatering.logWatering`; gestiona el estado optimista local; aplica sort por urgencia (D-09); recibe el undo de Sonner action y llama `revertWatering`.
7. **Flash animation**: nueva keyframe `flash-success` en Tailwind config, aplicada al card recién regado durante 1s.
8. **Error handling**: si DB falla, rollback optimista + `toast.error`.

Purpose: Wave 3 entrega el corazón del valor de la phase — el usuario PUEDE marcar una planta como regada, ver el countdown actualizarse, y deshacer en 4s. Sin esta sub-phase, la pantalla `/regar` es un visor estático.

Output: 1 migración SQL + 1 archivo nuevo en lib + 2 archivos nuevos en hooks (con tests) + 4 archivos modificados (types regenerado, useHomePlants extendido, PlantWateringCard wired, RegarPage cablea hook + sort + optimistic). ~400 LOC nuevas.

**EXECUTOR / HUMAN STEP REQUIRED:** Tras crear el archivo SQL en Task 1, el ejecutor debe correr `supabase db push` (o solicitar al usuario que lo haga si está en CI/local sin auth). Documentado en `<user_setup>` abajo.
</objective>

<user_setup>
- service: supabase-cli
  why: "Aplicar migración SQL y regenerar TypeScript types"
  env_vars: []
  manual_steps:
    - task: "Run `supabase db push` to apply migration 20260518000000"
      location: "Local CLI (assumes `supabase link` already done — confirmed via prior phases)"
    - task: "Run `supabase gen types typescript --linked > src/integrations/supabase/types.ts` to regenerate types"
      location: "Local CLI"
    - task: "Commit both files: migration SQL + regenerated types.ts"
      location: "git"
  fallback: "If user doesn't have Supabase CLI auth locally, Claude can present the SQL and let user paste into Supabase Dashboard SQL Editor (Project → SQL → New query); then user can run `supabase gen types typescript --project-id sdxfxkqzgnonxfshbjfc > src/integrations/supabase/types.ts` (project ID hardcoded in `supabase/config.toml`)."
</user_setup>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-calendar-v0/03-CONTEXT.md
@.planning/phases/03-calendar-v0/03-RESEARCH.md
@CLAUDE.md
@supabase/MIGRATION_CONVENTIONS.md
@supabase/migrations/20260517000000_add_watering_interval_days_to_plant_searches.sql
@src/hooks/use-classify-plant.ts
@src/hooks/use-home-plants.ts
@src/components/PlantWateringCard.tsx
@src/pages/RegarPage.tsx
@src/components/ui/sonner.tsx
@src/integrations/supabase/types.ts
@tailwind.config.ts

<interfaces>
DEPENDS_ON (Wave 2 — sub-phase 3-02):
- `src/hooks/use-home-plants.ts` con `HomePlant` type (sin lastWateredAt todavía)
- `src/components/PlantWateringCard.tsx` con `PlantWateringCardProps` que ya tiene `onWater?` opcional
- `src/pages/RegarPage.tsx` renderiza la lista pero sin sort por urgencia
- Tokens `--soft-warn` y `--soft-warn-bg` en index.css

CONSUME (ya existentes):
```typescript
// src/hooks/use-classify-plant.ts — PATTERN VERBATIM
export const UNDO_WINDOW_MS = 5000;
export function useClassifyPlant(): { classify: ...; revert: ... };

// Sonner — verified by researcher
import { toast } from "sonner";
toast("text", {
  action: { label: "Deshacer", onClick: () => {...} },
  duration: 4000,
});
toast.error("text");
```

CREATES:
```typescript
// src/lib/watering-countdown.ts
export type WateringStatus = "normal" | "urgent" | "overdue" | "pending-first";
export function computeDaysRemaining(
  lastWateredAt: string | null,
  intervalDays: number | null,
  now?: Date,
): number | null;
export function computeStatus(args: {
  lastWateredAt: string | null;
  intervalDays: number | null;
  now?: Date;
}): { status: WateringStatus; daysRemaining: number | null };

// src/hooks/use-log-watering.ts
export interface UseLogWateringReturn {
  logWatering: (plantId: string) => Promise<{ ok: boolean; previousLastWateredAt: string | null }>;
  revertWatering: (plantId: string, previousLastWateredAt: string | null) => Promise<{ ok: boolean }>;
}
export function useLogWatering(): UseLogWateringReturn;

// EXTENDED in this sub-phase:
// src/hooks/use-home-plants.ts → HomePlant gains lastWateredAt: string | null
//                              → SELECT adds last_watered_at
//                              → listener adds mp:plant-watered
```

D-08 FORMULA (locked):
```
X = floor((last_watered_at_ms + interval_days * 86_400_000 - now_ms) / 86_400_000)

- lastWateredAt === null OR intervalDays === null → status="pending-first", daysRemaining=null
- X > 0  → status="normal",  badge gray, button "Regada"
- X === 0 → status="urgent",  badge soft-warn, button "Regar"
- X < 0  → status="overdue", badge soft-warn, button "Regar"
```

D-09 SORT ORDER (locked):
```
1. overdue (X<0), more negative first (regla: ascending por X — más atrasada arriba)
2. urgent (X=0)
3. normal (X>0), ascending X (próxima a regar arriba)
4. pending-first (lastWateredAt=null OR intervalDays=null)
Ties: alphabetical by commonName.
```

D-12 FLOW (locked):
1. Tap → setea optimisticamente `lastWateredAt = now()` LOCAL
2. Card flash green 1s
3. Toast "✓ Regada · Siguiente riego en N días" duration 4s con action Deshacer
4. UPDATE a DB sucede INMEDIATAMENTE (researcher recommendation per RESEARCH.md row #50)
5. Si UPDATE falla → toast.error + rollback optimista
6. Tap Deshacer → revertWatering (segunda UPDATE) + dispatch mp:plant-watered + cancelar el flash

LOCKED CONSTRAINTS:
- Event name: `mp:plant-watered` (per researcher Q#7, recomendado en risk #2)
- Toast duration: 4000ms (D-12)
- Toast copy: "✓ Regada · Siguiente riego en N días" (N = computed from new lastWateredAt + intervalDays)
  - Si la planta era pending-first y se acaba de regar pero interval_days es null, la N no existe — el flujo de pedir frecuencia primero está en sub-phase 3-04. En 3-03, si interval_days es null, el botón "Regar" funciona pero el toast muestra "✓ Regada · Configura la frecuencia para ver el próximo riego" (copy de fallback hasta que 3-04 cierre el flujo).
- Toast error copy: "No se pudo guardar el riego. Inténtalo de nuevo."
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Crear migración SQL `last_watered_at` + regenerar types</name>
  <files>supabase/migrations/20260518000000_add_last_watered_at_to_plant_searches.sql, src/integrations/supabase/types.ts</files>
  <read_first>
    - supabase/MIGRATION_CONVENTIONS.md líneas 53-83 (tablas existentes — sin GRANT necesario)
    - supabase/migrations/20260517000000_add_watering_interval_days_to_plant_searches.sql (analog shape exacto)
    - .planning/phases/03-calendar-v0/03-CONTEXT.md D-06
    - .planning/phases/03-calendar-v0/03-RESEARCH.md § Migration SQL exact shape (líneas 131-163)
  </read_first>
  <action>
    **Sub-task 1.1 — Crear archivo SQL:**

    Crear `supabase/migrations/20260518000000_add_last_watered_at_to_plant_searches.sql` con el contenido EXACTO (copiado verbatim de RESEARCH.md líneas 139-149, con timestamp lockeado a `20260518000000` para ir después de Phase 2):

    ```sql
    -- Add last_watered_at to plant_searches: last time the user logged a watering
    -- via Phase 3 Calendar v0. Nullable: rows existing before Phase 3 (and any
    -- newly-created row) have no logged watering until the user taps Regada/Regar.
    -- Countdown formula uses (last_watered_at + watering_interval_days days) - now().
    -- RLS UPDATE policy from 20260515000000_add_plant_searches_update_policy.sql
    -- already gates writes by auth.uid() = user_id; no per-column policy needed.
    -- See: .planning/phases/03-calendar-v0/03-CONTEXT.md D-06
    alter table plant_searches
      add column if not exists last_watered_at timestamptz;

    -- ROLLBACK (manual, if needed):
    --   alter table plant_searches drop column if exists last_watered_at;
    -- VERIFICATION ajuste #3: documented for transparency. The column is
    -- nullable and free of downstream dependencies — drop is safe at any time
    -- before the feature ships to users (after ship, dropping loses data).
    ```

    **Sub-task 1.2 — Aplicar y regenerar types:**

    Después de crear el archivo:

    1. Correr `supabase db push` (o pedir al usuario que pegue el SQL en el Dashboard Editor — ver `<user_setup>` arriba).
    2. Verificar que el push fue exitoso checando la columna en el DB.
    3. Correr `supabase gen types typescript --linked > src/integrations/supabase/types.ts` para regenerar.
    4. El nuevo `src/integrations/supabase/types.ts` debe contener `last_watered_at: string | null` en `plant_searches.Row`, `?: string | null` en `Insert` y `Update`.

    **Verificación SQL post-migración** (correr en SQL Editor o psql):

    ```sql
    select column_name, data_type, is_nullable
    from information_schema.columns
    where table_schema = 'public'
      and table_name = 'plant_searches'
      and column_name = 'last_watered_at';
    -- Expected output: ('last_watered_at', 'timestamp with time zone', 'YES')
    ```

    **NO MODIFICAR:**
    - Migraciones anteriores (todas las del proyecto)
    - El config.toml de Supabase
    - Las RLS policies existentes (la de UPDATE `20260515000000` ya cubre la nueva columna por row-level protection)
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const path='supabase/migrations/20260518000000_add_last_watered_at_to_plant_searches.sql';if(!fs.existsSync(path)){console.error('Migration file missing');process.exit(1);}const s=fs.readFileSync(path,'utf8');const checks={hasAlterTable:/alter\s+table\s+plant_searches/i.test(s),hasAddColumn:/add\s+column\s+if\s+not\s+exists\s+last_watered_at\s+timestamptz/i.test(s),hasNoGrant:!/grant\s+/i.test(s),hasComment:s.includes('Phase 3')};const fails=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);if(fails.length){console.error('SQL FAIL:',fails);process.exit(1);}const types=fs.readFileSync('src/integrations/supabase/types.ts','utf8');if(!types.includes('last_watered_at')){console.error('types.ts does NOT include last_watered_at — did you run supabase gen types?');process.exit(2);}console.log('OK migration + types regen');"</automated>
  </verify>
  <acceptance_criteria>
    - El archivo SQL existe en `supabase/migrations/20260518000000_add_last_watered_at_to_plant_searches.sql`
    - Contiene `alter table plant_searches add column if not exists last_watered_at timestamptz;` (idempotent shape)
    - SIN ningún `grant` (tabla existente — researcher lo confirma)
    - Comentario superior referencia Phase 3 y D-06
    - `supabase db push` aplica la migración sin error
    - `src/integrations/supabase/types.ts` regenerado contiene `last_watered_at: string | null` en `plant_searches.Row`
    - `src/integrations/supabase/types.ts` regenerado contiene `last_watered_at?: string | null` en `plant_searches.Insert` y `Update`
    - `npx tsc --noEmit` exit 0 (los nuevos types compilan)
  </acceptance_criteria>
  <done>Migración aplicada en DB, types regenerados, columna last_watered_at disponible cliente y server.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Crear pure function `computeDaysRemaining` + `computeStatus` con tests exhaustivos</name>
  <files>src/lib/watering-countdown.ts, src/lib/watering-countdown.test.ts</files>
  <read_first>
    - .planning/phases/03-calendar-v0/03-CONTEXT.md D-08 (fórmula countdown)
    - .planning/phases/03-calendar-v0/03-RESEARCH.md Risk #13 (timezone: floor de ms / 86400000)
    - src/lib/anonymous-id.test.ts (analog: pure function tests con vitest)
  </read_first>
  <behavior>
    `computeDaysRemaining(lastWateredAt, intervalDays, now?)`:
    - Test 1: lastWateredAt=null, intervalDays=5 → null
    - Test 2: lastWateredAt="2026-05-17T00:00:00Z", intervalDays=null → null
    - Test 3: both null → null
    - Test 4: lastWateredAt=now-3days, intervalDays=7, now → 4 (X>0 normal)
    - Test 5: lastWateredAt=now-7days, intervalDays=7, now → 0 (X=0 urgent)
    - Test 6: lastWateredAt=now-10days, intervalDays=7, now → -3 (X<0 overdue)
    - Test 7: midnight transition — lastWateredAt=23:59 yesterday, intervalDays=1, now=00:01 today → expected 1 (less than 24h elapsed → not yet 0)
    - Test 8: ms math: 86400000 ms = 1 day; verify floor behavior

    `computeStatus({lastWateredAt, intervalDays, now?})`:
    - Test A: null branch → status='pending-first', daysRemaining=null
    - Test B: X>0 → status='normal', daysRemaining=X
    - Test C: X=0 → status='urgent', daysRemaining=0
    - Test D: X<0 → status='overdue', daysRemaining=X (negative)
  </behavior>
  <action>
    **TDD cycle: RED → GREEN → REFACTOR.**

    **Step RED — Crear `src/lib/watering-countdown.test.ts` PRIMERO con tests que fallan:**

    ```typescript
    import { describe, it, expect } from "vitest";
    import {
      computeDaysRemaining,
      computeStatus,
      type WateringStatus,
    } from "./watering-countdown";

    const NOW = new Date("2026-05-17T12:00:00Z");

    function daysAgo(d: number): string {
      return new Date(NOW.getTime() - d * 86_400_000).toISOString();
    }

    describe("computeDaysRemaining", () => {
      it("returns null when lastWateredAt is null", () => {
        expect(computeDaysRemaining(null, 5, NOW)).toBeNull();
      });

      it("returns null when intervalDays is null", () => {
        expect(computeDaysRemaining(daysAgo(3), null, NOW)).toBeNull();
      });

      it("returns null when both are null", () => {
        expect(computeDaysRemaining(null, null, NOW)).toBeNull();
      });

      it("returns positive when next watering is in the future (X > 0)", () => {
        // watered 3 days ago, interval 7 → next in 4 days
        expect(computeDaysRemaining(daysAgo(3), 7, NOW)).toBe(4);
      });

      it("returns 0 when due today (X === 0)", () => {
        // watered 7 days ago, interval 7 → due now
        expect(computeDaysRemaining(daysAgo(7), 7, NOW)).toBe(0);
      });

      it("returns negative when overdue (X < 0)", () => {
        // watered 10 days ago, interval 7 → 3 days overdue
        expect(computeDaysRemaining(daysAgo(10), 7, NOW)).toBe(-3);
      });

      it("uses floor of day diff (sub-day elapsed time)", () => {
        // watered 23:59 ago (just under 24h), interval 1 → X should be 0 (less than 1 full day until target)
        const lastWatered = new Date(NOW.getTime() - 23 * 3_600_000 - 59 * 60_000).toISOString();
        // target = lastWatered + 1day; remaining ms ~ 60s + 0; floor → 0
        expect(computeDaysRemaining(lastWatered, 1, NOW)).toBe(0);
      });

      it("respects exact midnight transitions (full days only)", () => {
        // watered exactly now, interval 5 → 5 days remaining
        const lastWatered = NOW.toISOString();
        expect(computeDaysRemaining(lastWatered, 5, NOW)).toBe(5);
      });

      it("defaults `now` to current date when omitted (smoke test)", () => {
        // VERIFICATION ajuste #1: lock the clock so the smoke test doesn't
        // depend on the wall clock at all. Result is deterministic.
        vi.useFakeTimers();
        vi.setSystemTime(new Date("2026-05-17T12:00:00Z"));
        try {
          const lastWatered = new Date("2026-05-14T12:00:00Z").toISOString();  // exactly 3 days ago
          const result = computeDaysRemaining(lastWatered, 7);
          // 3 days elapsed, interval 7 → 4 days remaining. Exact.
          expect(result).toBe(4);
        } finally {
          vi.useRealTimers();
        }
      });
    });

    describe("computeStatus", () => {
      it("returns pending-first when lastWateredAt is null", () => {
        const r = computeStatus({ lastWateredAt: null, intervalDays: 5, now: NOW });
        expect(r.status).toBe("pending-first");
        expect(r.daysRemaining).toBeNull();
      });

      it("returns pending-first when intervalDays is null", () => {
        const r = computeStatus({
          lastWateredAt: daysAgo(3),
          intervalDays: null,
          now: NOW,
        });
        expect(r.status).toBe("pending-first");
        expect(r.daysRemaining).toBeNull();
      });

      it("returns normal when X > 0", () => {
        const r = computeStatus({
          lastWateredAt: daysAgo(3),
          intervalDays: 7,
          now: NOW,
        });
        expect(r.status).toBe("normal");
        expect(r.daysRemaining).toBe(4);
      });

      it("returns urgent when X === 0", () => {
        const r = computeStatus({
          lastWateredAt: daysAgo(7),
          intervalDays: 7,
          now: NOW,
        });
        expect(r.status).toBe("urgent");
        expect(r.daysRemaining).toBe(0);
      });

      it("returns overdue when X < 0", () => {
        const r = computeStatus({
          lastWateredAt: daysAgo(10),
          intervalDays: 7,
          now: NOW,
        });
        expect(r.status).toBe("overdue");
        expect(r.daysRemaining).toBe(-3);
      });
    });
    ```

    **Step GREEN — Implementar `src/lib/watering-countdown.ts`:**

    ```typescript
    /**
     * Pure-function helpers for the Phase 3 calendar countdown.
     *
     * D-08 formula:
     *   X = floor((last_watered_at_ms + interval_days * 86400000 - now_ms) / 86400000)
     *
     * - X > 0  → normal       (Regada button, gray badge)
     * - X === 0 → urgent      (Regar button, soft-warn badge)
     * - X < 0  → overdue      (Regar button, soft-warn badge)
     * - lastWateredAt OR intervalDays null → pending-first (no countdown)
     *
     * Timezone-agnostic: uses UTC ms math. Sub-day elapsed time gets floored
     * — see Risk #13 in 03-RESEARCH.md.
     */

    export type WateringStatus = "normal" | "urgent" | "overdue" | "pending-first";

    const DAY_MS = 86_400_000;

    /**
     * Returns the days remaining until next watering.
     * Null when either input is null (pending-first state).
     *
     * Positive = future, 0 = today, negative = overdue.
     */
    export function computeDaysRemaining(
      lastWateredAt: string | null,
      intervalDays: number | null,
      now: Date = new Date(),
    ): number | null {
      if (lastWateredAt === null || intervalDays === null) return null;
      const lastMs = new Date(lastWateredAt).getTime();
      const targetMs = lastMs + intervalDays * DAY_MS;
      const remainingMs = targetMs - now.getTime();
      return Math.floor(remainingMs / DAY_MS);
    }

    /**
     * Wraps computeDaysRemaining with status derivation per D-08.
     */
    export function computeStatus(args: {
      lastWateredAt: string | null;
      intervalDays: number | null;
      now?: Date;
    }): { status: WateringStatus; daysRemaining: number | null } {
      const daysRemaining = computeDaysRemaining(
        args.lastWateredAt,
        args.intervalDays,
        args.now,
      );
      if (daysRemaining === null) {
        return { status: "pending-first", daysRemaining: null };
      }
      if (daysRemaining > 0) return { status: "normal", daysRemaining };
      if (daysRemaining === 0) return { status: "urgent", daysRemaining: 0 };
      return { status: "overdue", daysRemaining };
    }
    ```

    **Step REFACTOR — solo si la suite pasa:** verificar nombres, JSDoc, sin código muerto.

    Correr: `npm test -- --run watering-countdown` debe pasar 100%.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/lib/watering-countdown.ts','utf8');const t=fs.readFileSync('src/lib/watering-countdown.test.ts','utf8');const checks={exportsComputeDaysRemaining:s.includes('export function computeDaysRemaining'),exportsComputeStatus:s.includes('export function computeStatus'),exportsWateringStatus:s.includes('export type WateringStatus'),hasDayMs:s.includes('86_400_000')||s.includes('86400000'),hasFloor:s.includes('Math.floor'),hasNullBranch:s.includes('=== null || intervalDays === null'),testCoversNull:t.includes('returns null when'),testCoversPositive:t.includes('X > 0')||t.includes('positive when'),testCoversZero:t.includes('X === 0')||t.includes('due today'),testCoversNegative:t.includes('X < 0')||t.includes('overdue'),testCoversStatus:t.includes('returns urgent')&&t.includes('returns overdue')};const fails=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);if(fails.length){console.error('FAIL:',fails);process.exit(1);}console.log('OK watering-countdown');"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/watering-countdown.ts` exporta `computeDaysRemaining`, `computeStatus`, `type WateringStatus`
    - Función pura sin side effects (no Date.now mutación; acepta `now` opcional)
    - `computeDaysRemaining` retorna null si CUALQUIER input es null
    - `computeDaysRemaining` usa `Math.floor((target_ms - now_ms) / 86_400_000)`
    - `computeStatus` retorna las 4 status correctas + daysRemaining
    - El test file cubre AL MENOS: null-null, null-int, int-null, X>0, X=0, X<0, midnight edge, default-now smoke, 4 status branches en `computeStatus`
    - `npm test -- --run watering-countdown` exit 0
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>computeDaysRemaining + computeStatus implementados via TDD, suite pasa 100%.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extender `useHomePlants` con `lastWateredAt` + listener `mp:plant-watered`</name>
  <files>src/hooks/use-home-plants.ts</files>
  <read_first>
    - src/hooks/use-home-plants.ts (CURRENT — desde 3-02; modificar SELECT + type + listener)
    - .planning/phases/03-calendar-v0/03-RESEARCH.md Risk #2 (justificación del nuevo event channel mp:plant-watered)
  </read_first>
  <action>
    Modificar `src/hooks/use-home-plants.ts`:

    **Cambio 1 — extender el type `HomePlant`** (líneas 16-22 actuales):

    ```typescript
    export interface HomePlant {
      id: string;
      name: string;
      imageUrl: string;
      createdAt: string;
      wateringIntervalDays: number | null;
      lastWateredAt: string | null;
    }
    ```

    **Cambio 2 — extender el SELECT** (línea de `supabase.from(...).select(...)`):

    Estado actual:
    ```typescript
    .select("id, name, image_url, watering_interval_days, created_at")
    ```

    Estado final:
    ```typescript
    .select("id, name, image_url, watering_interval_days, last_watered_at, created_at")
    ```

    **Cambio 3 — extender el mapping** (dentro del `setPlants`):

    Estado actual:
    ```typescript
    setPlants(
      (data ?? []).map((row) => ({
        id: row.id,
        name: row.name,
        imageUrl: row.image_url,
        createdAt: row.created_at,
        wateringIntervalDays: row.watering_interval_days,
      })),
    );
    ```

    Estado final:
    ```typescript
    setPlants(
      (data ?? []).map((row) => ({
        id: row.id,
        name: row.name,
        imageUrl: row.image_url,
        createdAt: row.created_at,
        wateringIntervalDays: row.watering_interval_days,
        lastWateredAt: row.last_watered_at,
      })),
    );
    ```

    **Cambio 4 — añadir listener `mp:plant-watered`** al bloque useEffect existente (líneas 122-132 originales del archivo, adaptado de 3-02):

    Estado actual:
    ```typescript
    useEffect(() => {
      const handler = () => {
        load();
      };
      window.addEventListener("mp:plant-context-updated", handler);
      window.addEventListener("mp:pending-classification-resolved", handler);
      return () => {
        window.removeEventListener("mp:plant-context-updated", handler);
        window.removeEventListener("mp:pending-classification-resolved", handler);
      };
    }, [load]);
    ```

    Estado final (añadir `mp:plant-watered` al add/remove):

    ```typescript
    useEffect(() => {
      const handler = () => {
        load();
      };
      window.addEventListener("mp:plant-context-updated", handler);
      window.addEventListener("mp:pending-classification-resolved", handler);
      // Phase 3 sub-phase 3-03: dispatched by useLogWatering tras log/revert.
      window.addEventListener("mp:plant-watered", handler);
      return () => {
        window.removeEventListener("mp:plant-context-updated", handler);
        window.removeEventListener("mp:pending-classification-resolved", handler);
        window.removeEventListener("mp:plant-watered", handler);
      };
    }, [load]);
    ```

    **NO MODIFICAR:**
    - El mountedRef pattern
    - El early-return-anon
    - Las dependencies del useEffect
    - El error handling
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/hooks/use-home-plants.ts','utf8');const checks={hasLastWateredAtInType:/lastWateredAt:\s*string\s*\|\s*null/.test(s),hasLastWateredAtInSelect:s.includes('last_watered_at, created_at')||s.includes('last_watered_at,\\n')||/select\([^)]*last_watered_at/.test(s),hasMappingLastWatered:s.includes('lastWateredAt: row.last_watered_at'),hasListenerWatered:s.includes('mp:plant-watered'),hasAddListenerWatered:s.includes('window.addEventListener(\"mp:plant-watered\"'),hasRemoveListenerWatered:s.includes('window.removeEventListener(\"mp:plant-watered\"')};const fails=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);if(fails.length){console.error('FAIL:',fails);process.exit(1);}console.log('OK useHomePlants extended');"</automated>
  </verify>
  <acceptance_criteria>
    - `HomePlant` type incluye `lastWateredAt: string | null`
    - SELECT incluye `last_watered_at` además de `watering_interval_days`
    - Mapping incluye `lastWateredAt: row.last_watered_at`
    - Listener añadido para `mp:plant-watered` (con add + remove simétricos)
    - Listeners pre-existentes (`mp:plant-context-updated`, `mp:pending-classification-resolved`) intactos
    - Test `npm test -- --run use-home-plants` sigue pasando (puede requerir actualizar el test para cubrir el nuevo campo en el mock data)
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>useHomePlants extendido con lastWateredAt + listener mp:plant-watered.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Crear hook `useLogWatering` + test unit</name>
  <files>src/hooks/use-log-watering.ts, src/hooks/use-log-watering.test.ts</files>
  <read_first>
    - src/hooks/use-classify-plant.ts (analog VERBATIM — copy shape; cambiar UPDATE field)
    - src/hooks/use-home-plants.test.ts (test pattern de esta sub-phase 3-02)
    - .planning/phases/03-calendar-v0/03-CONTEXT.md D-12 (flujo + error handling)
  </read_first>
  <action>
    **Sub-task 4.1 — Crear `src/hooks/use-log-watering.ts`:**

    ```typescript
    import { useCallback } from "react";
    import { supabase } from "@/integrations/supabase/client";

    /**
     * Hook que escribe `last_watered_at` en una fila de plant_searches.
     *
     * Patrón verbatim de `useClassifyPlant`: SYNCHRONOUS UPDATE, retorna
     * `{ ok }` para que el caller toste/revierta. Dispatch CustomEvent
     * `mp:plant-watered` para que `useHomePlants` (y futuros listeners)
     * refetcheen.
     *
     * D-12 flow:
     *   1. Caller setea optimistic state local
     *   2. Caller llama logWatering(plantId) — devuelve previousLastWateredAt
     *      para que el caller pueda guardar el undo target.
     *   3. Si ok=true → toast con action Deshacer (4s); tap Deshacer
     *      llama revertWatering(plantId, previousLastWateredAt).
     *   4. Si ok=false → caller rollback + toast.error.
     *
     * IMPORTANTE: el hook NO conoce el previousLastWateredAt — el caller
     * lo obtiene de la planta antes del tap (e.g., desde useHomePlants).
     * La función `logWatering` retorna `previousLastWateredAt: null` siempre
     * porque al UPDATE pierde el valor anterior. El caller debe extraerlo
     * del state local ANTES de invocar.
     *
     * Sub-phase 3-04 añadirá `useEditWateringInterval` similar para D-13.
     */
    export interface UseLogWateringReturn {
      logWatering: (plantId: string) => Promise<{ ok: boolean }>;
      revertWatering: (
        plantId: string,
        previousLastWateredAt: string | null,
      ) => Promise<{ ok: boolean }>;
    }

    export function useLogWatering(): UseLogWateringReturn {
      const logWatering = useCallback(async (plantId: string) => {
        const now = new Date().toISOString();
        const { error } = await supabase
          .from("plant_searches")
          .update({ last_watered_at: now })
          .eq("id", plantId);

        if (error) {
          console.error("[useLogWatering] update failed:", error.message);
          return { ok: false as const };
        }
        if (typeof window !== "undefined") {
          window.dispatchEvent(
            new CustomEvent("mp:plant-watered", {
              detail: { plant_search_id: plantId, action: "log" },
            }),
          );
        }
        return { ok: true as const };
      }, []);

      const revertWatering = useCallback(
        async (plantId: string, previousLastWateredAt: string | null) => {
          const { error } = await supabase
            .from("plant_searches")
            .update({ last_watered_at: previousLastWateredAt })
            .eq("id", plantId);

          if (error) {
            console.error("[useLogWatering] revert failed:", error.message);
            return { ok: false as const };
          }
          if (typeof window !== "undefined") {
            window.dispatchEvent(
              new CustomEvent("mp:plant-watered", {
                detail: { plant_search_id: plantId, action: "revert" },
              }),
            );
          }
          return { ok: true as const };
        },
        [],
      );

      return { logWatering, revertWatering };
    }
    ```

    **Sub-task 4.2 — Crear `src/hooks/use-log-watering.test.ts`:**

    Copia adaptada del patrón `useClassifyPlant.ts` analog (verbatim mock chain):

    ```typescript
    import { renderHook, act } from "@testing-library/react";
    import { describe, it, expect, vi, beforeEach } from "vitest";
    import { useLogWatering } from "./use-log-watering";

    const makeChain = (result: { error: unknown }) => {
      const chain = {
        update: vi.fn(() => chain),
        eq: vi.fn(() => Promise.resolve(result)),
      };
      return chain;
    };

    let mockSupabaseFrom: ReturnType<typeof vi.fn>;
    vi.mock("@/integrations/supabase/client", () => ({
      supabase: {
        from: (...args: unknown[]) => mockSupabaseFrom(...args),
      },
    }));

    describe("useLogWatering", () => {
      beforeEach(() => {
        mockSupabaseFrom = vi.fn();
      });

      it("logWatering: success path returns ok=true and dispatches mp:plant-watered", async () => {
        const chain = makeChain({ error: null });
        mockSupabaseFrom.mockReturnValue(chain);

        const dispatchSpy = vi.spyOn(window, "dispatchEvent");
        const { result } = renderHook(() => useLogWatering());

        let res: { ok: boolean } | undefined;
        await act(async () => {
          res = await result.current.logWatering("plant-001");
        });

        expect(res?.ok).toBe(true);
        expect(mockSupabaseFrom).toHaveBeenCalledWith("plant_searches");
        expect(chain.update).toHaveBeenCalledWith(
          expect.objectContaining({ last_watered_at: expect.any(String) }),
        );
        expect(chain.eq).toHaveBeenCalledWith("id", "plant-001");
        expect(dispatchSpy).toHaveBeenCalled();
        const dispatched = dispatchSpy.mock.calls.find(
          ([e]) => (e as CustomEvent).type === "mp:plant-watered",
        );
        expect(dispatched).toBeDefined();
      });

      it("logWatering: error path returns ok=false and does NOT dispatch", async () => {
        const consoleSpy = vi.spyOn(console, "error").mockImplementation(() => {});
        const chain = makeChain({ error: { message: "RLS denied" } });
        mockSupabaseFrom.mockReturnValue(chain);
        const dispatchSpy = vi.spyOn(window, "dispatchEvent");
        dispatchSpy.mockClear();

        const { result } = renderHook(() => useLogWatering());

        let res: { ok: boolean } | undefined;
        await act(async () => {
          res = await result.current.logWatering("plant-001");
        });

        expect(res?.ok).toBe(false);
        const dispatched = dispatchSpy.mock.calls.find(
          ([e]) => (e as CustomEvent).type === "mp:plant-watered",
        );
        expect(dispatched).toBeUndefined();
        expect(consoleSpy).toHaveBeenCalled();
        consoleSpy.mockRestore();
      });

      it("revertWatering: success sends UPDATE with previous timestamp", async () => {
        const chain = makeChain({ error: null });
        mockSupabaseFrom.mockReturnValue(chain);

        const { result } = renderHook(() => useLogWatering());
        const previous = "2026-05-10T10:00:00Z";

        let res: { ok: boolean } | undefined;
        await act(async () => {
          res = await result.current.revertWatering("plant-001", previous);
        });

        expect(res?.ok).toBe(true);
        expect(chain.update).toHaveBeenCalledWith({ last_watered_at: previous });
        expect(chain.eq).toHaveBeenCalledWith("id", "plant-001");
      });

      it("revertWatering: previous=null persists null (for plants that were pending-first)", async () => {
        const chain = makeChain({ error: null });
        mockSupabaseFrom.mockReturnValue(chain);

        const { result } = renderHook(() => useLogWatering());

        let res: { ok: boolean } | undefined;
        await act(async () => {
          res = await result.current.revertWatering("plant-001", null);
        });

        expect(res?.ok).toBe(true);
        expect(chain.update).toHaveBeenCalledWith({ last_watered_at: null });
      });

      it("revertWatering: error path returns ok=false", async () => {
        const consoleSpy = vi.spyOn(console, "error").mockImplementation(() => {});
        const chain = makeChain({ error: { message: "fail" } });
        mockSupabaseFrom.mockReturnValue(chain);

        const { result } = renderHook(() => useLogWatering());

        let res: { ok: boolean } | undefined;
        await act(async () => {
          res = await result.current.revertWatering("plant-001", "2026-05-10T10:00:00Z");
        });

        expect(res?.ok).toBe(false);
        consoleSpy.mockRestore();
      });
    });
    ```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('src/hooks/use-log-watering.ts','utf8');const t=fs.readFileSync('src/hooks/use-log-watering.test.ts','utf8');const checks={hookExports:h.includes('export function useLogWatering'),hookExportsReturn:h.includes('export interface UseLogWateringReturn'),hookHasLog:h.includes('const logWatering = useCallback'),hookHasRevert:h.includes('const revertWatering = useCallback'),hookUpdatesLastWateredAt:h.includes('update({ last_watered_at: now })'),hookDispatchesEvent:h.includes('new CustomEvent(\"mp:plant-watered\"'),hookSSRGuard:h.includes('typeof window !== \"undefined\"'),testCoversLogSuccess:t.includes('success path returns ok=true'),testCoversLogError:t.includes('error path returns ok=false'),testCoversRevertSuccess:t.includes('revertWatering: success'),testCoversRevertNull:t.includes('previous=null persists null')};const fails=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);if(fails.length){console.error('FAIL:',fails);process.exit(1);}console.log('OK useLogWatering + tests');"</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/use-log-watering.ts` exporta `useLogWatering` y `UseLogWateringReturn`
    - `logWatering(plantId)`: UPDATE `last_watered_at = ISO now` + dispatch `mp:plant-watered` + return `{ok}`
    - `revertWatering(plantId, previousLastWateredAt)`: UPDATE con el valor previo (puede ser `null`) + dispatch + return `{ok}`
    - Console.error en error paths
    - SSR guard `typeof window !== "undefined"` antes de dispatch
    - 5 unit tests cubren: log-success, log-error (no dispatch), revert-success, revert-null-previous, revert-error
    - `npm test -- --run use-log-watering` exit 0
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>useLogWatering creado con log + revert simétricos, tests cubren happy + error paths.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Añadir keyframe `flash-success` a `tailwind.config.ts`</name>
  <files>tailwind.config.ts</files>
  <read_first>
    - tailwind.config.ts (CURRENT — bloque keyframes lines 82-95)
    - .planning/phases/03-calendar-v0/03-RESEARCH.md Risk #6 (recomendación añadir keyframe a Tailwind, no inline CSS)
  </read_first>
  <action>
    Modificar `tailwind.config.ts` para añadir una keyframe `flash-success` que aplica un pulse verde 1s al card recién regado.

    En el bloque `theme.extend.keyframes` (líneas 82-95), añadir DESPUÉS de `pulse-slow`:

    ```typescript
    keyframes: {
      "accordion-down": { ... },
      "accordion-up": { ... },
      "pulse-slow": {
        "0%, 100%": { opacity: "1" },
        "50%": { opacity: "0.6" },
      },
      // Phase 3 sub-phase 3-03 (D-12): flash green 1s tras log de riego.
      // No usar bg-primary directamente (cambio brusco) — un fade suave 30%→0%
      // con el color primario via box-shadow inset evita repaint completo del card.
      "flash-success": {
        "0%": { boxShadow: "inset 0 0 0 9999px hsl(var(--primary) / 0.2), 4px 4px 0 0 hsl(var(--foreground))" },
        "100%": { boxShadow: "inset 0 0 0 9999px hsl(var(--primary) / 0), 4px 4px 0 0 hsl(var(--foreground))" },
      },
    },
    ```

    En el bloque `theme.extend.animation` (líneas 96-100), añadir DESPUÉS de `pulse-slow`:

    ```typescript
    animation: {
      "accordion-down": "accordion-down 0.2s ease-out",
      "accordion-up": "accordion-up 0.2s ease-out",
      "pulse-slow": "pulse-slow 2s ease-in-out infinite",
      // 1s ease-out, runs once (no infinite). RegarPage aplica `animate-flash-success`
      // via React state local 1s tras el log; quita la clase tras el animation end.
      "flash-success": "flash-success 1s ease-out forwards",
    },
    ```

    **NO MODIFICAR:**
    - Cualquier otro keyframe / animation
    - Los colors / fontFamily / borderRadius
    - El safeArea plugin
    - Las soft-warn colors añadidas en 3-02
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('tailwind.config.ts','utf8');const checks={hasFlashKeyframe:/['\"]flash-success['\"]:\s*\{/.test(s),hasFlashAnimation:s.includes('flash-success 1s ease-out forwards'),preservesPulseSlow:s.includes('pulse-slow'),preservesAccordion:s.includes('accordion-down'),preservesSoftWarn:s.includes('softWarn:')};const fails=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);if(fails.length){console.error('FAIL:',fails);process.exit(1);}console.log('OK flash-success keyframe');"</automated>
  </verify>
  <acceptance_criteria>
    - `tailwind.config.ts` contiene la keyframe `"flash-success"` en `theme.extend.keyframes`
    - La keyframe usa `box-shadow inset` para evitar repaint completo (perf)
    - `theme.extend.animation` contiene `"flash-success": "flash-success 1s ease-out forwards"`
    - Keyframes/animations existentes intactos (`pulse-slow`, `accordion-down`, `accordion-up`)
    - `softWarn` y `softWarnBg` colors intactas (de 3-02)
    - `npm run build` exit 0 (Tailwind procesa el config sin error)
  </acceptance_criteria>
  <done>Keyframe + animation flash-success añadidas; utility `animate-flash-success` disponible.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Wire `PlantWateringCard.tsx` con `computeStatus` + Sonner toast + flash animation</name>
  <files>src/components/PlantWateringCard.tsx</files>
  <read_first>
    - src/components/PlantWateringCard.tsx (CURRENT desde 3-02 — TODO se reemplaza)
    - src/lib/watering-countdown.ts (recién creado en Task 2)
    - src/components/ui/sonner.tsx (verificar import `toast` desde "sonner")
    - .planning/phases/03-calendar-v0/03-CONTEXT.md D-08, D-10, D-12, D-15
    - .planning/phases/03-calendar-v0/03-RESEARCH.md Risk #4 (Sonner action API verbatim)
  </read_first>
  <action>
    Reemplazar el contenido COMPLETO de `src/components/PlantWateringCard.tsx`. Mantiene la estructura visual de 3-02 pero ahora deriva los 4 estados D-08 vía `computeStatus()` y wirea el botón.

    Contenido exacto:

    ```tsx
    import { useState } from "react";
    import { toast } from "sonner";
    import type { HomePlant } from "@/hooks/use-home-plants";
    import { computeStatus, type WateringStatus } from "@/lib/watering-countdown";

    /**
     * Splittea "Common (Scientific)" en sus dos partes.
     * Patrón copiado verbatim de PlantMapSheet (no importado para evitar coupling).
     */
    function splitNameField(name: string): {
      commonName: string;
      scientificName: string | null;
    } {
      const match = name.match(/^(.+?)\s*\(([^)]+)\)$/);
      if (match) {
        return { commonName: match[1].trim(), scientificName: match[2].trim() };
      }
      return { commonName: name, scientificName: null };
    }

    export interface PlantWateringCardProps {
      plant: HomePlant;
      /**
       * Caller (RegarPage) wirea esto a `useLogWatering.logWatering`.
       * Opcional para permitir storybook/preview sin DB.
       */
      onWater?: (plant: HomePlant) => Promise<{ ok: boolean }>;
      /**
       * Caller (RegarPage) wirea esto a `useLogWatering.revertWatering`.
       * Si no se pasa, el toast no muestra Deshacer.
       */
      onUndo?: (plant: HomePlant, previousLastWateredAt: string | null) => Promise<{ ok: boolean }>;
      /**
       * Sub-phase 3-04: tap on "Cada N días" abrirá un picker.
       * En 3-03 no se renderiza interactivo todavía.
       */
      onEditFrequency?: (plantId: string) => void;
    }

    /**
     * Card del listado /regar — representa una planta casa con su estado de riego.
     *
     * Phase 3 sub-phase 3-03: countdown real vía computeStatus(); botón wired
     * a onWater (optimistic UPDATE + Sonner toast con Deshacer); flash green
     * animation 1s tras log.
     *
     * Estados D-08:
     * - normal (X>0):    botón "Regada", badge gris, status "Próximo riego en X días"
     * - urgent (X=0):    botón "Regar", badge soft-warn, status "Toca regar hoy"
     * - overdue (X<0):   botón "Regar", badge soft-warn, status "Lleva N días esperándote"
     * - pending-first:   botón "Regar", badge "—", status "Pendiente primera vez · Toca regar para empezar"
     *
     * Sub-phase 3-04 cableará el flujo "pending-first sin intervalo" (D-14):
     * primer tap → picker frecuencia → setea intervalo → log.
     */
    export function PlantWateringCard({
      plant,
      onWater,
      onUndo,
      onEditFrequency,
    }: PlantWateringCardProps) {
      const { commonName } = splitNameField(plant.name);
      const [optimisticLastWatered, setOptimisticLastWatered] = useState<string | null | undefined>(
        undefined,
      );
      const [flashing, setFlashing] = useState(false);

      // Si hay optimistic update, usar ese valor; si no, el de la DB.
      const effectiveLastWateredAt =
        optimisticLastWatered === undefined ? plant.lastWateredAt : optimisticLastWatered;

      const { status, daysRemaining } = computeStatus({
        lastWateredAt: effectiveLastWateredAt,
        intervalDays: plant.wateringIntervalDays,
      });

      const intervalLabel =
        plant.wateringIntervalDays !== null
          ? `Cada ${plant.wateringIntervalDays} días`
          : "Sin frecuencia";

      // D-15 status copy + D-10 button label + badge styling per status.
      let statusText: string;
      let badgeText: string;
      let badgeIsWarn: boolean;
      let buttonLabel: string;

      switch (status) {
        case "normal":
          statusText = `Próximo riego en ${daysRemaining} días`;
          badgeText = `${daysRemaining} d`;
          badgeIsWarn = false;
          buttonLabel = "Regada";
          break;
        case "urgent":
          statusText = "Toca regar hoy";
          badgeText = "0 d";
          badgeIsWarn = true;
          buttonLabel = "Regar";
          break;
        case "overdue":
          statusText = `Lleva ${Math.abs(daysRemaining ?? 0)} días esperándote`;
          badgeText = `${daysRemaining} d`;
          badgeIsWarn = true;
          buttonLabel = "Regar";
          break;
        case "pending-first":
        default:
          statusText = "Pendiente primera vez · Toca regar para empezar";
          badgeText = "—";
          badgeIsWarn = false;
          buttonLabel = "Regar";
          break;
      }

      const handleWater = async () => {
        if (!onWater) {
          console.log("[PlantWateringCard] water tap (no onWater wired):", plant.id);
          return;
        }

        // VERIFICATION ajuste #2: prevent double-tap during the 1s flash window.
        // Without this guard, a quick second tap fires 2 UPDATE + 2 track + 2 toast.
        if (flashing) return;

        // Capture previous BEFORE optimistic mutation (for undo target).
        const previousLastWateredAt = plant.lastWateredAt;

        // 1. Optimistic update visible
        const newTimestamp = new Date().toISOString();
        setOptimisticLastWatered(newTimestamp);
        // 2. Flash animation 1s
        setFlashing(true);
        window.setTimeout(() => setFlashing(false), 1000);

        // 3. Persist immediately (researcher recommendation per RESEARCH.md)
        const result = await onWater(plant);

        if (!result.ok) {
          // Rollback optimistic + toast error
          setOptimisticLastWatered(undefined);
          setFlashing(false);
          toast.error("No se pudo guardar el riego. Inténtalo de nuevo.");
          return;
        }

        // 4. Build the toast copy. Si la planta no tiene intervalDays todavía,
        // el flujo correcto vive en sub-phase 3-04 — aquí mostramos un fallback.
        const newStatus = computeStatus({
          lastWateredAt: newTimestamp,
          intervalDays: plant.wateringIntervalDays,
        });
        const toastMessage =
          newStatus.daysRemaining !== null
            ? `✓ Regada · Siguiente riego en ${newStatus.daysRemaining} días`
            : "✓ Regada · Configura la frecuencia para ver el próximo riego";

        // 5. Sonner toast with Deshacer action.
        toast(toastMessage, {
          duration: 4000,
          ...(onUndo
            ? {
                action: {
                  label: "Deshacer",
                  onClick: async () => {
                    setOptimisticLastWatered(previousLastWateredAt);
                    setFlashing(false);
                    const undoResult = await onUndo(plant, previousLastWateredAt);
                    if (!undoResult.ok) {
                      // If revert fails, keep optimistic-reverted state but toast error.
                      toast.error("No se pudo deshacer el riego. Refresca para sincronizar.");
                    } else {
                      // After successful revert, clear optimistic so the DB refetch is the source of truth.
                      setOptimisticLastWatered(undefined);
                    }
                  },
                },
              }
            : {}),
        });
      };

      const handleEditFrequency = () => {
        if (onEditFrequency) {
          onEditFrequency(plant.id);
        }
        // Sub-phase 3-04 wirea esto. En 3-03 sin handler, no hace nada.
      };

      return (
        <article
          className={`flex flex-col gap-3 p-4 bg-card border-2 border-foreground rounded-2xl ${flashing ? "animate-flash-success" : ""}`}
          style={{ boxShadow: "var(--shadow-press)" }}
          aria-label={`Planta: ${commonName}`}
          data-watering-status={status}
        >
          <div className="flex items-start gap-3">
            <img
              src={plant.imageUrl}
              alt={commonName}
              loading="lazy"
              className="size-16 shrink-0 object-cover rounded-xl border-2 border-foreground"
            />
            <div className="flex-1 min-w-0 flex flex-col gap-1">
              <h2 className="font-display text-lg font-semibold text-foreground leading-tight">
                {commonName}
              </h2>
              <button
                type="button"
                onClick={handleEditFrequency}
                className="font-body text-sm text-muted-foreground text-left underline-offset-2 hover:underline focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring rounded"
                aria-label={`Frecuencia de riego: ${intervalLabel}. Pulsa para editar.`}
              >
                {intervalLabel}
              </button>
              <p className="font-body text-xs text-muted-foreground italic">
                {statusText}
              </p>
            </div>
            <span
              className={
                badgeIsWarn
                  ? "shrink-0 px-2 py-1 rounded-lg bg-soft-warn-bg border-2 border-foreground font-display text-sm font-bold text-foreground"
                  : "shrink-0 px-2 py-1 rounded-lg bg-muted border-2 border-foreground/30 font-display text-sm font-bold text-muted-foreground"
              }
              aria-label={`Estado: ${badgeText}`}
            >
              {badgeText}
            </span>
          </div>
          <button
            type="button"
            onClick={handleWater}
            className="w-full min-h-11 px-4 py-2.5 bg-primary text-primary-foreground border-2 border-foreground rounded-xl font-display text-base font-bold active:translate-x-[2px] active:translate-y-[2px] active:shadow-none focus-visible:outline-none focus-visible:ring-4 focus-visible:ring-ring focus-visible:ring-offset-2 transition-shadow"
            style={{ boxShadow: "var(--shadow-press)" }}
            aria-label={`${buttonLabel} ${commonName}`}
          >
            {buttonLabel}
          </button>
        </article>
      );
    }
    ```

    Decisiones implementadas:
    - **D-08**: 4 estados derivados de `computeStatus()`
    - **D-10**: botón "Regada" (X>0) vs "Regar" (X≤0 o pending-first)
    - **D-12**: flujo optimistic → flash 1s → toast 4s → DB UPDATE → undo restora
    - **D-15**: copy verbatim por estado, tono suave, badge `bg-soft-warn-bg` para urgent/overdue (no rojo)
    - **D-16**: frecuencia sin atribución
    - **A11y**: data-watering-status para CSS hooks futuros; aria-labels en todo
    - **Sonner action**: usa el formato `{ action: { label, onClick } }` verificado en `node_modules/sonner` per researcher

    Edge case manejado:
    - Si `onUndo` no se pasa (storybook/preview), el toast no muestra Deshacer.
    - Si la planta era pending-first sin intervalDays, el toast muestra fallback copy (3-04 cierra ese flujo correctamente).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/components/PlantWateringCard.tsx','utf8');const checks={importsToast:s.includes('import { toast } from \"sonner\"'),importsComputeStatus:s.includes('import { computeStatus'),hasStatusSwitchNormal:s.includes('case \"normal\":'),hasStatusSwitchUrgent:s.includes('case \"urgent\":'),hasStatusSwitchOverdue:s.includes('case \"overdue\":'),hasStatusSwitchPending:s.includes('case \"pending-first\":'),hasCopyNormal:s.includes('Próximo riego en'),hasCopyUrgent:s.includes('Toca regar hoy'),hasCopyOverdue:s.includes('Lleva')&&s.includes('días esperándote'),hasCopyPending:s.includes('Pendiente primera vez'),hasButtonRegada:s.includes('buttonLabel = \"Regada\"'),hasButtonRegar:s.includes('buttonLabel = \"Regar\"'),hasOptimisticState:s.includes('optimisticLastWatered'),hasFlashState:s.includes('flashing'),hasAnimateFlash:s.includes('animate-flash-success'),hasToastDeshacer:s.includes('label: \"Deshacer\"'),hasToastDuration:s.includes('duration: 4000'),hasToastError:s.includes('toast.error(\"No se pudo guardar')};const fails=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);if(fails.length){console.error('FAIL:',fails);process.exit(1);}console.log('OK PlantWateringCard wired');"</automated>
  </verify>
  <acceptance_criteria>
    - Importa `toast` de `sonner` y `computeStatus` de `@/lib/watering-countdown`
    - Switch case completo cubriendo los 4 status (normal/urgent/overdue/pending-first)
    - Copy verbatim D-15: "Próximo riego en X días", "Toca regar hoy", "Lleva N días esperándote", "Pendiente primera vez · Toca regar para empezar"
    - Botón label D-10: "Regada" si normal, "Regar" para los otros 3 estados
    - State `optimisticLastWatered` controla el countdown durante optimistic update
    - State `flashing` controla la animación `animate-flash-success` (1s)
    - `handleWater` flow: optimistic → flash → onWater → si ok: toast con action Deshacer (4s); si fail: rollback + toast.error
    - Toast copy: `"✓ Regada · Siguiente riego en N días"` (o fallback "Configura la frecuencia..." si intervalDays=null)
    - Toast error copy: `"No se pudo guardar el riego. Inténtalo de nuevo."`
    - Toast undo action `label: "Deshacer"`, `onClick` invoca `onUndo(plant, previousLastWateredAt)`
    - data-watering-status atributo en el `<article>` (para CSS/test hooks)
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>PlantWateringCard wired: countdown real, botón funcional, toast con undo, flash animation, error handling completo.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 7: Wire `RegarPage.tsx` con `useLogWatering` + sort por urgencia (D-09)</name>
  <files>src/pages/RegarPage.tsx</files>
  <read_first>
    - src/pages/RegarPage.tsx (CURRENT desde 3-02)
    - src/lib/watering-countdown.ts (computeStatus + WateringStatus)
    - src/hooks/use-log-watering.ts (recién creado en Task 4)
    - .planning/phases/03-calendar-v0/03-CONTEXT.md D-09 (sort order)
  </read_first>
  <action>
    Modificar `src/pages/RegarPage.tsx` para cablear el hook `useLogWatering` y aplicar el sort por urgencia (D-09).

    Contenido final exacto:

    ```tsx
    import { useEffect, useMemo } from "react";
    import { Navigate } from "react-router-dom";
    import { Leaf } from "lucide-react";
    import { useAuth } from "@/hooks/use-auth";
    import { useHomePlants, type HomePlant } from "@/hooks/use-home-plants";
    import { useLogWatering } from "@/hooks/use-log-watering";
    import { PlantWateringCard } from "@/components/PlantWateringCard";
    import { computeStatus } from "@/lib/watering-countdown";

    /**
     * /regar — ¿Toca regar? — Calendario de riego.
     *
     * Phase 3 sub-phase 3-03: lista completa con countdown real + botón funcional.
     *
     * D-09 sort order:
     *   1. overdue (X<0), more negative first
     *   2. urgent (X=0)
     *   3. normal (X>0) ascending X
     *   4. pending-first at the end
     *   Ties: alphabetical by common name.
     *
     * D-15 tono suave: header "Tus plantas casa" empático.
     */

    function splitCommonName(name: string): string {
      const match = name.match(/^(.+?)\s*\(([^)]+)\)$/);
      return match ? match[1].trim() : name;
    }

    /**
     * D-09 sort key: lower = renders earlier.
     *   - overdue (X<0): negative integers (more negative = more overdue = renders first)
     *   - urgent (X=0): 0
     *   - normal (X>0): positive integers (smaller X = renders earlier)
     *   - pending-first: Number.POSITIVE_INFINITY (always last)
     * Returns a tuple so ties break by commonName alpha (handled in sort cb).
     */
    function urgencyKey(plant: HomePlant): number {
      const { status, daysRemaining } = computeStatus({
        lastWateredAt: plant.lastWateredAt,
        intervalDays: plant.wateringIntervalDays,
      });
      if (status === "pending-first") return Number.POSITIVE_INFINITY;
      // daysRemaining is non-null when status !== 'pending-first'
      return daysRemaining ?? 0;
    }

    export default function RegarPage() {
      const { user, isLoading: authLoading } = useAuth();
      const { plants, isLoading: plantsLoading } = useHomePlants();
      const { logWatering, revertWatering } = useLogWatering();

      const isLoading = authLoading || (!!user && plantsLoading);

      useEffect(() => {
        document.title = "¿Toca regar? · Mi Plantita Feliz";
      }, []);

      // D-09 sort por urgencia.
      const sortedPlants = useMemo(() => {
        return [...plants].sort((a, b) => {
          const ka = urgencyKey(a);
          const kb = urgencyKey(b);
          if (ka !== kb) return ka - kb;
          // Tie-break: alphabetical by common name.
          return splitCommonName(a.name).localeCompare(splitCommonName(b.name), "es");
        });
      }, [plants]);

      if (isLoading) {
        return (
          <div
            className="flex items-center justify-center py-20"
            aria-busy="true"
            aria-label="Cargando tus plantas"
          >
            <Leaf className="size-12 text-primary animate-pulse-slow" strokeWidth={1.2} />
          </div>
        );
      }

      if (plants.length === 0) {
        return <Navigate to="/" replace />;
      }

      const handleWater = async (plant: HomePlant) => {
        return logWatering(plant.id);
      };

      const handleUndo = async (plant: HomePlant, previousLastWateredAt: string | null) => {
        return revertWatering(plant.id, previousLastWateredAt);
      };

      return (
        <div className="px-6 py-8 pb-24">
          <header className="mb-6 flex flex-col gap-1">
            <h1 className="font-display text-2xl font-bold text-foreground">
              ¿Toca regar?
            </h1>
            <p className="font-body text-base text-muted-foreground">
              Tus plantas casa
            </p>
          </header>
          <ul className="flex flex-col gap-3" aria-label="Lista de plantas de casa">
            {sortedPlants.map((plant) => (
              <li key={plant.id}>
                <PlantWateringCard
                  plant={plant}
                  onWater={handleWater}
                  onUndo={handleUndo}
                />
              </li>
            ))}
          </ul>
        </div>
      );
    }
    ```

    Decisiones implementadas:
    - **D-09**: sort por urgencia con tie-break alfabético (locale 'es' para acentos correctos)
    - **D-12**: `handleWater` y `handleUndo` puentean al hook — el resto del flujo vive en `PlantWateringCard`
    - **memo del sort**: useMemo evita re-sortear en cada render si plants no cambia
    - **Inferred contract**: `PlantWateringCard.onWater` retorna `{ok}` y el card maneja toast/rollback internamente. RegarPage solo provee el "verbo" del hook.

    NO MODIFICAR:
    - El header
    - El loading spinner
    - El redirect-on-empty
    - El document.title
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/pages/RegarPage.tsx','utf8');const checks={importsUseLogWatering:s.includes('import { useLogWatering }'),importsComputeStatus:s.includes('import { computeStatus }'),hasUrgencyKey:s.includes('function urgencyKey'),hasSortedMemo:s.includes('const sortedPlants = useMemo'),hasLocaleCompare:s.includes('localeCompare')&&s.includes('\"es\"'),hasOnWater:s.includes('onWater={handleWater}'),hasOnUndo:s.includes('onUndo={handleUndo}'),hasHandleWater:s.includes('const handleWater'),hasHandleUndo:s.includes('const handleUndo'),hasInfinityForPending:s.includes('POSITIVE_INFINITY'),preservesNavigate:s.includes('<Navigate to=\"/\"'),preservesDocTitle:s.includes('document.title')};const fails=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);if(fails.length){console.error('FAIL:',fails);process.exit(1);}console.log('OK RegarPage wired');"</automated>
  </verify>
  <acceptance_criteria>
    - Importa `useLogWatering`, `computeStatus`, `HomePlant` type
    - `urgencyKey(plant)` retorna número para sort: X (overdue/urgent/normal) o `POSITIVE_INFINITY` (pending-first)
    - `sortedPlants` está envuelto en `useMemo` con dep `[plants]`
    - Tie-break usa `localeCompare(..., "es")` (no `<` directo — para acentos)
    - `handleWater` invoca `logWatering(plant.id)` y retorna el resultado
    - `handleUndo` invoca `revertWatering(plant.id, previousLastWateredAt)` y retorna el resultado
    - `<PlantWateringCard>` recibe `onWater={handleWater}` y `onUndo={handleUndo}`
    - Iteración usa `sortedPlants` (NO `plants`) en el render
    - Header y loading state intactos
    - `npx tsc --noEmit` exit 0
    - `npm run build` exit 0
  </acceptance_criteria>
  <done>RegarPage cablea hook + sort por urgencia; PlantWateringCard recibe onWater/onUndo cableados a useLogWatering.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Supabase REST (UPDATE) | `useLogWatering` escribe `last_watered_at`. RLS UPDATE policy `auth.uid() = user_id` (Phase 02.1) gates writes. |
| client-internal | CustomEvent `mp:plant-watered` same-origin. |
| migration → DB | ALTER TABLE añade columna nullable. Sin impacto en data existente. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Tampering | Un usuario rogue intenta UPDATE de plantas de otro user | mitigate | RLS policy `20260515000000_add_plant_searches_update_policy.sql` ya gate-a — operación row-level por auth.uid(). |
| T-03-11 | Information disclosure | `last_watered_at` añade un dato temporal del usuario | accept | Timestamp de actividad del propio user. No es PII sensible. Visible solo para el dueño vía RLS SELECT (cubre Phase 02.1). |
| T-03-12 | DoS | Spam de Regar/Deshacer (4s undo window + flash 1s) | accept | Cada tap = 1 UPDATE Supabase (cheap). Sin razón para rate-limit. Si emerge en producción, considerar debounce. |
| T-03-13 | Integrity | Si UPDATE de log falla pero el optimistic update se queda visible | mitigate | El `handleWater` revierte `optimisticLastWatered` a `undefined` (delegando al DB value) y muestra toast.error. |
| T-03-14 | Integrity | Si UPDATE de revert falla, la planta queda en estado optimistic-revertido pero la DB tiene `last_watered_at = now()` | accept | Edge case raro. El componente muestra "No se pudo deshacer el riego. Refresca para sincronizar." Aceptable trade-off vs reintento automático complejo. |
| T-03-15 | Tampering | Migration SQL: `add column if not exists` es idempotente y seguro | mitigate | Si la columna ya existe, el ALTER es no-op. Si la migración corre 2x, no rompe. |
| T-03-16 | XSS | Toast copy contiene `${daysRemaining}` interpolado | mitigate | Number primitive — sin riesgo. Sonner escapa text content por defecto. |
</threat_model>

<verification>
- Migration `20260518000000_add_last_watered_at_to_plant_searches.sql` existe; `supabase db push` aplica; verification SQL muestra la columna.
- `src/integrations/supabase/types.ts` contiene `last_watered_at`.
- `src/lib/watering-countdown.ts` y test pasan 100% (cubre 4 status branches + edge cases).
- `src/hooks/use-log-watering.ts` y test pasan (log + revert + error paths).
- `src/hooks/use-home-plants.ts` extendido con `lastWateredAt` + listener `mp:plant-watered`.
- `src/components/PlantWateringCard.tsx` consume `computeStatus`, render varía por status, toast con Deshacer cableado.
- `src/pages/RegarPage.tsx` sortea por urgencia y wirea hook.
- `tailwind.config.ts` keyframe `flash-success` añadida.
- `npx tsc --noEmit` exit 0.
- `npm run build` exit 0.
- `npm test -- --run` sin regresiones (los 3 tests nuevos + use-home-plants test existente pasan).
- Smoke manual (con planta home con `last_watered_at != null`): la card muestra "Próximo riego en X días" + badge "X d" + botón "Regada". Tap → flash green 1s → toast "✓ Regada · Siguiente riego en N días" con Deshacer. Esperar 4s → toast desaparece. Tap "Deshacer" en los 4s → la planta vuelve al countdown previo.
- Smoke manual (con planta home con `last_watered_at = null`, después de migración): card muestra "Pendiente primera vez · Toca regar para empezar" + badge "—" + botón "Regar". Tap → optimistic set + UPDATE → toast "✓ Regada · Configura la frecuencia..." (fallback — sub-phase 3-04 cierra). UPDATE persiste. Refresh → la planta ahora muestra countdown real.
- Error path manual: tirar la red en DevTools, tap "Regar" → toast.error + rollback visible.
</verification>

<success_criteria>
- Columna `last_watered_at` en producción.
- Pure function `computeDaysRemaining` testeada para todos los branches.
- Hook `useLogWatering` con log + revert + dispatch + tests.
- Card derive correctamente el estado y el copy de cada planta.
- Optimistic update funciona; rollback en error; undo en 4s vía Sonner action.
- Flash animation 1s aplica al card.
- Sort por urgencia (D-09) en `RegarPage`.
- TypeScript compila, build pasa, tests pasan.
</success_criteria>

<output>
After completion, create `.planning/phases/03-calendar-v0/03-03-SUMMARY.md` documenting:
- Migración aplicada y types regenerados (commit ambos)
- Pure function + hook + card + page funcionan con plantas que tengan `last_watered_at`
- Plantas con `last_watered_at = null` siguen en "Pendiente primera vez" — el flujo completo (D-14: pedir frecuencia si null) se cierra en sub-phase 3-04
- Toast con action prop de Sonner verificado en el código (verbatim del API que researcher confirmó)
- Sort por urgencia + tie-break alfabético locale 'es' para acentos
- Event channel `mp:plant-watered` para refetch reactivo de `useHomePlants`
</output>
