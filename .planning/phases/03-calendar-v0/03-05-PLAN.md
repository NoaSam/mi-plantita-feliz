---
phase: 03-calendar-v0
plan: 05
type: execute
wave: 5
depends_on: [02, 03, 04]
files_modified:
  - src/pages/RegarPage.tsx
  - src/hooks/use-log-watering.ts
  - src/hooks/use-edit-watering-interval.ts
  - src/components/PlantWateringCard.tsx
  - docs/posthog-events.md
  - e2e/fixtures.ts
  - e2e/regar.spec.ts
autonomous: true
requirements: [RIEG-01, RIEG-02]
requirements_addressed: [RIEG-01, RIEG-02]
tags: [phase-03, posthog-tracking, e2e-playwright, microcopy-review]

must_haves:
  truths:
    - "Los 5 eventos PostHog de D-17 están cableados en los 5 sitios correctos:"
    - "  - `calendar_opened` se dispara una vez por mount de `RegarPage`, gated por flag local `trackedOpen` (patrón verbatim MapPage:45,51-55). Props: `home_count`, `overdue_count`, `pending_first_time_count`."
    - "  - `watering_logged` se dispara en `useLogWatering.logWatering` ANTES del dispatch del CustomEvent (tras DB UPDATE confirmado ok=true). Props: `plant_search_id`, `days_remaining_before` (signed int), `interval_days`, `was_first_time` (boolean)."
    - "  - `watering_undone` se dispara en `useLogWatering.revertWatering` (igual, antes del dispatch). Props: `plant_search_id`."
    - "  - `watering_frequency_edited` se dispara en `useEditWateringInterval.editInterval`. Props: `plant_search_id`, `prev_interval`, `new_interval`, `source` ('ia_initial' | 'user_override' | 'null_filled_in_by_user')."
    - "  - `calendar_card_navigated_to_detail` se dispara en `PlantWateringCard` cuando el usuario tapea la card (NO el botón Regar ni el texto frecuencia — el área principal de la card lleva a `/planta/:id`). Props: `plant_search_id`, `position` (índice 0-based en la lista ordenada)."
    - "Existe nuevo handler de tap en el área de la card (no el botón ni la frecuencia) que dispara el track + navega — añade props `position` a `PlantWateringCardProps`."
    - "`docs/posthog-events.md` tiene una nueva sección `## Phase 3 — Calendar v0` siguiendo verbatim la estructura de `## Phase 03.1 — Plant Map v0` (líneas 155-203): tabla de eventos, detalle de propiedades, funnel previsto."
    - "Existe el E2E test `e2e/regar.spec.ts` con dos test cases:"
    - "  - Happy path: anon identify → wall → signup → classify home → tab Regar visible → navigate → card visible → tap Regar → toast con Deshacer → contador reset visual."
    - "  - Defensive: navigate to `/regar` cuando user no tiene plantas casa → redirect a `/`."
    - "El fixture `e2e/fixtures.ts` exporta `MOCK_HOME_PLANTS` con 3+ plantas variando `last_watered_at` y `watering_interval_days` para cubrir los 4 estados D-08 + el caso D-14 (interval=null)."
    - "Microcopy final pass per D-15/D-16: verificar que ninguna copy contiene atribución IA/usuario (e.g., NO '(según IA)', NO '(tú decidiste)') y que el tono es suave en todos los strings nuevos."
    - "Microcopy final pass D-15: verificar uso de `bg-soft-warn-bg` (no `bg-destructive`) en badges urgent/overdue."
  artifacts:
    - path: "e2e/regar.spec.ts"
      provides: "Playwright E2E tests for /regar happy path + defensive empty redirect"
      min_lines: 100
    - path: "e2e/fixtures.ts"
      provides: "MOCK_HOME_PLANTS export added"
      contains: "MOCK_HOME_PLANTS"
    - path: "docs/posthog-events.md"
      provides: "Phase 3 — Calendar v0 section with 5 events documented + funnel"
      contains: "Phase 3 — Calendar v0"
  key_links:
    - from: "src/pages/RegarPage.tsx"
      to: "track('calendar_opened', {...})"
      via: "useEffect with trackedOpen flag"
      pattern: "calendar_opened"
    - from: "src/hooks/use-log-watering.ts"
      to: "track('watering_logged' | 'watering_undone', {...})"
      via: "BEFORE window.dispatchEvent in log + revert"
      pattern: "watering_logged"
    - from: "src/hooks/use-edit-watering-interval.ts"
      to: "track('watering_frequency_edited', {...})"
      via: "BEFORE window.dispatchEvent"
      pattern: "watering_frequency_edited"
    - from: "src/components/PlantWateringCard.tsx"
      to: "track('calendar_card_navigated_to_detail', {...})"
      via: "BEFORE navigate(`/planta/${id}`) en card body click handler"
      pattern: "calendar_card_navigated_to_detail"
    - from: "e2e/regar.spec.ts"
      to: "/regar route + plant_searches REST mock"
      via: "page.route + page.goto"
      pattern: "/regar"
---

<objective>
Cerrar Phase 3 con tracking, tests E2E, docs y microcopy review final:

1. **Wire 5 PostHog eventos** (D-17) en los 5 sitios correctos siguiendo el patrón "track BEFORE side-effect" del codebase (ver `PlantMapSheet.tsx:67-74`, `ClassificationMorph.tsx:42-50`).
2. **Card tap navigation**: añadir handler para que el área principal de la card (foto + nombre — NO el botón ni la frecuencia) navegue a `/planta/:id`. El tracking event correspondiente (`calendar_card_navigated_to_detail`) se dispara antes del navigate.
3. **Actualizar `docs/posthog-events.md`** con sección Phase 3 documentando los 5 eventos + funnel.
4. **E2E Playwright `e2e/regar.spec.ts`**:
   - Happy path: navigate `/regar` con auth + home plants → tap Regar → toast con Deshacer → counter reset visual.
   - Defensive: navigate sin home plants → redirect a `/`.
5. **`MOCK_HOME_PLANTS` fixture** con plantas variando los 4 estados D-08.
6. **Microcopy review final** (D-15/D-16): grep para asegurar que no hay copy regresiva (rojo intenso, atribución IA/usuario, tono alarmista).

Purpose: Wave 5 cierra el ciclo de release. Sin tracking + tests E2E + docs, Phase 3 no es "production ready" según los estándares de las 2 phases anteriores. Esta sub-phase no añade features visibles al usuario — añade observability y safety net.

Output: 4 archivos modificados con track calls + 1 archivo modificado en docs + 2 archivos nuevos/extendidos en e2e. ~250 LOC nuevas, principalmente tests E2E.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-calendar-v0/03-CONTEXT.md
@.planning/phases/03-calendar-v0/03-RESEARCH.md
@CLAUDE.md
@docs/posthog-events.md
@src/lib/track.ts
@src/pages/MapPage.tsx
@src/components/PlantMapSheet.tsx
@src/components/ClassificationMorph.tsx
@src/pages/RegarPage.tsx
@src/hooks/use-log-watering.ts
@src/hooks/use-edit-watering-interval.ts
@src/components/PlantWateringCard.tsx
@e2e/fixtures.ts
@e2e/map.spec.ts

<interfaces>
DEPENDS_ON (Waves 2 + 3 + 4):
- `RegarPage` con `useHomePlants`, `useLogWatering`, `useEditWateringInterval`, `WateringFrequencyPicker` cableados.
- `PlantWateringCard` con 4 callbacks (onWater, onUndo, onEditFrequency, onWaterRequiringFrequency).
- `useLogWatering` con `logWatering` + `revertWatering`.
- `useEditWateringInterval` con `editInterval`.
- Migración `last_watered_at` ya aplicada.

CONSUME (ya existentes):
```typescript
// src/lib/track.ts
import { track } from "@/lib/track";
track(event: string, properties?: Record<string, unknown>): void;
// No-op si consent denied.

// useNavigate
import { useNavigate } from "react-router-dom";
const navigate = useNavigate();
navigate(`/planta/${id}`);
```

D-17 EVENTS (locked, copy verbatim from CONTEXT.md):

```typescript
// 1. calendar_opened
track("calendar_opened", {
  home_count: number,            // = plants.length
  overdue_count: number,         // count of X < 0
  pending_first_time_count: number, // count of status === 'pending-first'
});
// Dispara una sola vez por mount, gated por flag local trackedOpen.

// 2. watering_logged
track("watering_logged", {
  plant_search_id: string,
  days_remaining_before: number, // signed int (negativo si atrasada, 0 si hoy, positivo si anticipada)
  interval_days: number | null,   // frecuencia configurada al momento del log
  was_first_time: boolean,        // true si era pending-first
});
// Dispara DESPUÉS del UPDATE exitoso, ANTES del dispatch del CustomEvent.

// 3. watering_undone
track("watering_undone", { plant_search_id: string });
// Dispara DESPUÉS del UPDATE de revert exitoso, ANTES del dispatch.

// 4. watering_frequency_edited
track("watering_frequency_edited", {
  plant_search_id: string,
  prev_interval: number | null,
  new_interval: number,
  source: 'ia_initial' | 'user_override' | 'null_filled_in_by_user',
});
// `source` deriving:
//   - 'null_filled_in_by_user': prev_interval === null (IA no dio frecuencia, usuario rellenó)
//   - 'ia_initial': prev_interval !== null AND new_interval === prev_interval (no-op edit)
//   - 'user_override': prev_interval !== null AND new_interval !== prev_interval (usuario sobreescribió)
// NOTA: el hook `useEditWateringInterval.editInterval(plantId, newDays)` actualmente solo
// recibe plantId y newDays. Para track con source y prev_interval, hay 2 opciones:
//   A) Cambiar la signature a editInterval(plantId, newDays, prev_interval) — minimo refactor.
//   B) Que el caller (RegarPage) haga el track BEFORE invocar editInterval — separa concerns.
// **DECISION**: Opción B. RegarPage tiene acceso al `pickerState.plant.wateringIntervalDays`
// para calcular prev_interval y source. El hook stays pure de tracking.

// 5. calendar_card_navigated_to_detail
track("calendar_card_navigated_to_detail", {
  plant_search_id: string,
  position: number,  // índice 0-based en la lista ordenada visible
});
// Dispara ANTES del navigate(`/planta/${id}`).
```

CARD TAP-TO-DETAIL CONTRACT:
- `PlantWateringCard` recibe NEW prop `onNavigateToDetail?: (plant: HomePlant, position: number) => void` (opcional).
- El handler se invoca en `onClick` del bloque `<div className="flex items-start gap-3">` (área foto+nombre+frecuencia+badge).
- Los elementos interactivos internos (botón frecuencia, botón Regar/Regada) hacen `e.stopPropagation()` en sus handlers para evitar doble-trigger.
- RegarPage pasa la position from `sortedPlants.indexOf(plant)`.

TOAST ACTION UNDO CONTRACT:
- El undo en `PlantWateringCard.tsx` (3-03) llama `onUndo(plant, previousLastWateredAt)` — el hook hace el track. NO duplicar track en el componente.

E2E TEST STRATEGY (per researcher analog `e2e/map.spec.ts`):
- `e2e/regar.spec.ts` mockea `**/rest/v1/plant_searches*` con `MOCK_HOME_PLANTS`.
- Para el happy path tap-and-undo, el mock debe responder GET (devuelve plants) y PATCH (UPDATE retorna 200).
- Bloquear PostHog (ya hecho via `blockAnalytics`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire `calendar_opened` en `RegarPage.tsx`</name>
  <files>src/pages/RegarPage.tsx</files>
  <read_first>
    - src/pages/RegarPage.tsx (CURRENT desde 3-04)
    - src/pages/MapPage.tsx líneas 45, 51-55 (analog verbatim del trackedOpen pattern)
    - src/lib/watering-countdown.ts (computeStatus para derivar overdue_count y pending_first_time_count)
    - .planning/phases/03-calendar-v0/03-CONTEXT.md D-17 evento 1
  </read_first>
  <action>
    Modificar `src/pages/RegarPage.tsx` para añadir el track call `calendar_opened`:

    **Cambio 1 — añadir import de `track`:**

    Junto a los demás imports al top:
    ```tsx
    import { track } from "@/lib/track";
    ```

    **Cambio 2 — añadir state `trackedOpen`** junto a `pickerState`:

    ```tsx
    const [trackedOpen, setTrackedOpen] = useState(false);
    ```

    **Cambio 3 — añadir `useEffect` que dispara el track una sola vez** tras `isLoading` se resuelve. Insertar DESPUÉS del `useEffect` que setea `document.title` y ANTES del `sortedPlants = useMemo(...)`:

    ```tsx
    useEffect(() => {
      if (isLoading || trackedOpen) return;
      // Derive counts using computeStatus over the current plants list.
      let overdueCount = 0;
      let pendingFirstTimeCount = 0;
      for (const p of plants) {
        const { status } = computeStatus({
          lastWateredAt: p.lastWateredAt,
          intervalDays: p.wateringIntervalDays,
        });
        if (status === "overdue") overdueCount += 1;
        if (status === "pending-first") pendingFirstTimeCount += 1;
      }
      track("calendar_opened", {
        home_count: plants.length,
        overdue_count: overdueCount,
        pending_first_time_count: pendingFirstTimeCount,
      });
      setTrackedOpen(true);
    }, [isLoading, trackedOpen, plants]);
    ```

    **NO MODIFICAR:**
    - El `useEffect` del document.title
    - El `sortedPlants = useMemo(...)`
    - Los handlers
    - El JSX

    Patrón verbatim de `MapPage.tsx:51-55` — solo añade el cálculo de overdue + pending_first counts (que MapPage no necesita).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/pages/RegarPage.tsx','utf8');const checks={hasTrackImport:s.includes('import { track }'),hasTrackedOpenState:s.includes('const [trackedOpen, setTrackedOpen]'),hasCalendarOpenedCall:s.includes('track(\"calendar_opened\"'),hasHomeCountProp:s.includes('home_count: plants.length'),hasOverdueCount:s.includes('overdue_count: overdueCount'),hasPendingCount:s.includes('pending_first_time_count: pendingFirstTimeCount'),hasGatedFlag:s.includes('isLoading || trackedOpen')};const fails=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);if(fails.length){console.error('FAIL:',fails);process.exit(1);}console.log('OK calendar_opened');"</automated>
  </verify>
  <acceptance_criteria>
    - Import de `track` añadido
    - State `trackedOpen` inicializado a `false`
    - useEffect dispara `track("calendar_opened", {...})` una sola vez tras `isLoading` se resuelve
    - Gating: `if (isLoading || trackedOpen) return;`
    - Props del event: `home_count` (= plants.length), `overdue_count`, `pending_first_time_count`
    - `setTrackedOpen(true)` tras dispatch
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>calendar_opened cableado en RegarPage con dedup local.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire `watering_logged` + `watering_undone` en `useLogWatering`</name>
  <files>src/hooks/use-log-watering.ts</files>
  <read_first>
    - src/hooks/use-log-watering.ts (CURRENT desde 3-03)
    - src/lib/watering-countdown.ts (computeDaysRemaining para `days_remaining_before`)
    - .planning/phases/03-calendar-v0/03-CONTEXT.md D-17 eventos 2 y 3
  </read_first>
  <action>
    Modificar `src/hooks/use-log-watering.ts` para añadir track calls en `logWatering` y `revertWatering`. Los track son disparados DESPUÉS del UPDATE confirmado ok=true y ANTES del dispatch del CustomEvent.

    **Problema de signature**: el track de `watering_logged` necesita `days_remaining_before`, `interval_days`, `was_first_time`. Esto requiere que el caller pase esta info al hook. Hay 2 opciones:

    - **Opción A**: cambiar la signature de `logWatering(plantId)` → `logWatering(plant: { id, lastWateredAt, wateringIntervalDays })` y el hook calcula los tracking props.
    - **Opción B**: añadir un parámetro opcional `tracking?: { ... }` con los props ya calculados.

    **DECISION**: Opción A — pasar `HomePlant` al hook. Esto centraliza la lógica de track (DRY), evita duplicate computation en el caller, y el hook ya estaba diseñado para tomar `plant` en futuras phases.

    **Cambio 1 — actualizar `UseLogWateringReturn`:**

    Estado actual (post-3-03):
    ```typescript
    export interface UseLogWateringReturn {
      logWatering: (plantId: string) => Promise<{ ok: boolean }>;
      revertWatering: (plantId: string, previousLastWateredAt: string | null) => Promise<{ ok: boolean }>;
    }
    ```

    Estado final:
    ```typescript
    import type { HomePlant } from "@/hooks/use-home-plants";

    export interface UseLogWateringReturn {
      /**
       * Logs a watering for the given plant. Returns `{ok}`.
       * The caller (PlantWateringCard) is responsible for the optimistic UI
       * + toast + undo orchestration. This hook handles persistence, tracking,
       * and event dispatch only.
       */
      logWatering: (plant: HomePlant) => Promise<{ ok: boolean }>;
      revertWatering: (
        plant: HomePlant,
        previousLastWateredAt: string | null,
      ) => Promise<{ ok: boolean }>;
    }
    ```

    **Cambio 2 — actualizar implementación con tracking y `import { computeDaysRemaining }`**:

    Contenido COMPLETO final:

    ```typescript
    import { useCallback } from "react";
    import { supabase } from "@/integrations/supabase/client";
    import { track } from "@/lib/track";
    import { computeDaysRemaining } from "@/lib/watering-countdown";
    import type { HomePlant } from "@/hooks/use-home-plants";

    /**
     * Hook que escribe `last_watered_at` en una fila de plant_searches.
     *
     * Phase 3 sub-phase 3-05: tracking de PostHog cableado.
     *
     * D-12 flow: caller setea optimistic; este hook persiste + trackea +
     * dispatcha CustomEvent. Tracking fires BEFORE dispatch — orden importa
     * para que el siguiente refetch (triggered by dispatch) no preceda al
     * evento conceptualmente.
     */
    export interface UseLogWateringReturn {
      logWatering: (plant: HomePlant) => Promise<{ ok: boolean }>;
      revertWatering: (
        plant: HomePlant,
        previousLastWateredAt: string | null,
      ) => Promise<{ ok: boolean }>;
    }

    export function useLogWatering(): UseLogWateringReturn {
      const logWatering = useCallback(async (plant: HomePlant) => {
        const now = new Date().toISOString();
        const { error } = await supabase
          .from("plant_searches")
          .update({ last_watered_at: now })
          .eq("id", plant.id);

        if (error) {
          console.error("[useLogWatering] update failed:", error.message);
          return { ok: false as const };
        }

        // D-17 evento `watering_logged`: track BEFORE dispatch.
        // days_remaining_before: usando el estado PRE-LOG (i.e., antes de
        // que el optimistic update mutara el state local del card).
        const daysRemainingBefore = computeDaysRemaining(
          plant.lastWateredAt,
          plant.wateringIntervalDays,
        );
        // was_first_time: true si la planta no tenía lastWateredAt ANTES del log.
        // Esto incluye plants pending-first por null-lastWatered (intervalDays
        // puede o no estar set; el card-side maneja el flujo D-14 case 2).
        const wasFirstTime = plant.lastWateredAt === null;

        track("watering_logged", {
          plant_search_id: plant.id,
          days_remaining_before: daysRemainingBefore,
          interval_days: plant.wateringIntervalDays,
          was_first_time: wasFirstTime,
        });

        if (typeof window !== "undefined") {
          window.dispatchEvent(
            new CustomEvent("mp:plant-watered", {
              detail: { plant_search_id: plant.id, action: "log" },
            }),
          );
        }
        return { ok: true as const };
      }, []);

      const revertWatering = useCallback(
        async (plant: HomePlant, previousLastWateredAt: string | null) => {
          const { error } = await supabase
            .from("plant_searches")
            .update({ last_watered_at: previousLastWateredAt })
            .eq("id", plant.id);

          if (error) {
            console.error("[useLogWatering] revert failed:", error.message);
            return { ok: false as const };
          }

          // D-17 evento `watering_undone`: track BEFORE dispatch.
          track("watering_undone", { plant_search_id: plant.id });

          if (typeof window !== "undefined") {
            window.dispatchEvent(
              new CustomEvent("mp:plant-watered", {
                detail: { plant_search_id: plant.id, action: "revert" },
              }),
            );
          }
          return { ok: true as const };
        },
        [],
      );

      return { logWatering, revertWatering };
    }
    ```

    **NOTA — actualizar callers**: `PlantWateringCard.handleWater` ya pasaba `plant` a `onWater`; `handleUndo` ya pasaba `plant` a `onUndo`. RegarPage los wirea a `logWatering(plant)` y `revertWatering(plant, prev)`. Verificar que tras este cambio, RegarPage's `handleWater = async (plant) => logWatering(plant)` y `handleUndo = async (plant, prev) => revertWatering(plant, prev)` siguen pasando los args correctos. NO se requiere cambio en RegarPage (Task 5 de 3-03 ya lo cableó así).

    **Actualizar test**: `src/hooks/use-log-watering.test.ts` debe actualizarse para pasar un `HomePlant` mock en vez de `string`. Cambio mínimo: definir `const mockPlant: HomePlant = { id: "plant-001", name: "Test", imageUrl: "img", createdAt: "...", wateringIntervalDays: 7, lastWateredAt: "2026-05-10T10:00:00Z" }` y pasar `mockPlant` en lugar de `"plant-001"`. Verificar que los expects del `chain.eq("id", "plant-001")` siguen pasando.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/hooks/use-log-watering.ts','utf8');const checks={importsTrack:s.includes('import { track }'),importsComputeDaysRemaining:s.includes('import { computeDaysRemaining }'),importsHomePlant:s.includes('import type { HomePlant }'),hasWateringLoggedCall:s.includes('track(\"watering_logged\"'),hasWateringUndoneCall:s.includes('track(\"watering_undone\"'),hasDaysRemainingBefore:s.includes('days_remaining_before:'),hasIntervalDaysProp:s.includes('interval_days: plant.wateringIntervalDays'),hasWasFirstTime:s.includes('was_first_time: wasFirstTime'),hasNewSignatureLog:s.includes('logWatering: (plant: HomePlant)'),hasNewSignatureRevert:s.includes('revertWatering: ('),trackBeforeDispatch:s.indexOf('track(\"watering_logged\"')<s.indexOf('mp:plant-watered',s.indexOf('track(\"watering_logged\"'))};const fails=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);if(fails.length){console.error('FAIL:',fails);process.exit(1);}console.log('OK useLogWatering tracking');"</automated>
  </verify>
  <acceptance_criteria>
    - Imports añadidos: `track`, `computeDaysRemaining`, type `HomePlant`
    - Signature de `logWatering` cambia a `(plant: HomePlant)` — antes era `(plantId: string)`
    - Signature de `revertWatering` cambia a `(plant: HomePlant, previousLastWateredAt: string | null)` — antes era `(plantId: string, ...)`
    - `logWatering`: track `watering_logged` con `plant_search_id`, `days_remaining_before`, `interval_days`, `was_first_time` — DISPARA tras UPDATE ok=true y ANTES de dispatchEvent
    - `revertWatering`: track `watering_undone` con `plant_search_id` — DISPARA tras UPDATE ok=true y ANTES de dispatchEvent
    - `days_remaining_before` usa `computeDaysRemaining(plant.lastWateredAt, plant.wateringIntervalDays)` (state PRE-log)
    - `was_first_time` = `plant.lastWateredAt === null`
    - Test actualizado para pasar HomePlant — `npm test -- --run use-log-watering` exit 0
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>useLogWatering trackea log + undo; signature actualizada a HomePlant; tests pasan.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire `watering_frequency_edited` en `useEditWateringInterval` + actualizar caller</name>
  <files>src/hooks/use-edit-watering-interval.ts, src/pages/RegarPage.tsx</files>
  <read_first>
    - src/hooks/use-edit-watering-interval.ts (CURRENT desde 3-04)
    - src/pages/RegarPage.tsx (CURRENT — `handlePickerSave`)
    - .planning/phases/03-calendar-v0/03-CONTEXT.md D-17 evento 4 (source enum logic)
  </read_first>
  <action>
    Per la decisión en el `<interfaces>` (Opción B): el caller (RegarPage) calcula `prev_interval` y `source` y los pasa al hook. Esto evita que el hook conozca el prev value (que no tiene acceso sin un round-trip extra a DB).

    **Cambio 1 — extender signature de `editInterval`:**

    Estado actual (post-3-04):
    ```typescript
    editInterval: (plantId: string, newDays: number) => Promise<{ ok: boolean }>;
    ```

    Estado final:
    ```typescript
    editInterval: (
      plantId: string,
      newDays: number,
      prevDays: number | null,
      source: "ia_initial" | "user_override" | "null_filled_in_by_user",
    ) => Promise<{ ok: boolean }>;
    ```

    **Cambio 2 — actualizar implementación con track:**

    Contenido COMPLETO final de `src/hooks/use-edit-watering-interval.ts`:

    ```typescript
    import { useCallback } from "react";
    import { supabase } from "@/integrations/supabase/client";
    import { track } from "@/lib/track";

    export type FrequencyEditSource =
      | "ia_initial"
      | "user_override"
      | "null_filled_in_by_user";

    export interface UseEditWateringIntervalReturn {
      /**
       * Updates the watering interval for the given plant.
       *
       * Phase 3 sub-phase 3-05: tracking de PostHog cableado.
       * The caller provides `prevDays` and `source` because the hook does not
       * have access to the plant's current state without an extra round-trip.
       */
      editInterval: (
        plantId: string,
        newDays: number,
        prevDays: number | null,
        source: FrequencyEditSource,
      ) => Promise<{ ok: boolean }>;
    }

    export function useEditWateringInterval(): UseEditWateringIntervalReturn {
      const editInterval = useCallback(
        async (
          plantId: string,
          newDays: number,
          prevDays: number | null,
          source: FrequencyEditSource,
        ) => {
          const { error } = await supabase
            .from("plant_searches")
            .update({ watering_interval_days: newDays })
            .eq("id", plantId);

          if (error) {
            console.error("[useEditWateringInterval] update failed:", error.message);
            return { ok: false as const };
          }

          // D-17 evento `watering_frequency_edited`: track BEFORE dispatch.
          track("watering_frequency_edited", {
            plant_search_id: plantId,
            prev_interval: prevDays,
            new_interval: newDays,
            source,
          });

          if (typeof window !== "undefined") {
            window.dispatchEvent(
              new CustomEvent("mp:plant-frequency-updated", {
                detail: { plant_search_id: plantId, new_interval_days: newDays },
              }),
            );
          }
          return { ok: true as const };
        },
        [],
      );

      return { editInterval };
    }
    ```

    **Cambio 3 — actualizar test `use-edit-watering-interval.test.ts`:**

    Los tests existentes deben pasar `prevDays` y `source`. Actualizar las 3 invocaciones:

    - Test "success path": `result.current.editInterval("plant-001", 5, 7, "user_override")`
    - Test "error path": `result.current.editInterval("plant-001", 99, 7, "user_override")`
    - Test "boundary values": `result.current.editInterval("plant-001", value, null, "null_filled_in_by_user")`

    Y añadir UN nuevo test que verifica el track call:

    ```typescript
    it("fires watering_frequency_edited track with prev/new/source", async () => {
      const chain = makeChain({ error: null });
      mockSupabaseFrom.mockReturnValue(chain);

      // Mock track — vi.mock would require module restructure. Simpler: spy on window dispatch
      // pattern is NOT applicable here (track is not via window). Instead, assert that
      // the function completes ok — granular track verification is tested in integration.

      const { result } = renderHook(() => useEditWateringInterval());

      let res: { ok: boolean } | undefined;
      await act(async () => {
        res = await result.current.editInterval("plant-001", 5, 7, "user_override");
      });

      expect(res?.ok).toBe(true);
      // CustomEvent dispatched (proxy for "track also fired" since both are in same path)
      // (or refactor to mock @/lib/track if granular assert needed)
    });
    ```

    NOTA: si se prefiere verificación granular de `track`, hacer `vi.mock("@/lib/track", () => ({ track: vi.fn() }))` y assert sobre el mock. El test actualmente solo verifica el dispatch — está bien para v0; el tracking call está en el mismo code path por lo que es difícil que se rompa silenciosamente.

    **Cambio 4 — actualizar `RegarPage.handlePickerSave` para pasar prevDays y source:**

    Estado actual (post-3-04):
    ```tsx
    const handlePickerSave = async (newDays: number) => {
      if (!pickerState) return;
      const { plant, mode } = pickerState;
      setPickerState(null);

      const editResult = await editInterval(plant.id, newDays);
      // ...
    };
    ```

    Estado final:
    ```tsx
    const handlePickerSave = async (newDays: number) => {
      if (!pickerState) return;
      const { plant, mode } = pickerState;
      setPickerState(null);

      // D-17 source classification:
      //  - 'null_filled_in_by_user': prev was null (IA didn't determine; user filled)
      //  - 'user_override': prev was a value, user changed it
      //  - 'ia_initial': prev was a value, user kept the same (no-op edit — rare)
      const prevDays = plant.wateringIntervalDays;
      let source: "ia_initial" | "user_override" | "null_filled_in_by_user";
      if (prevDays === null) source = "null_filled_in_by_user";
      else if (prevDays === newDays) source = "ia_initial";
      else source = "user_override";

      const editResult = await editInterval(plant.id, newDays, prevDays, source);
      if (!editResult.ok) {
        toast.error("No se pudo guardar la frecuencia. Inténtalo de nuevo.");
        return;
      }

      toast(`Frecuencia actualizada · Cada ${newDays} días`, { duration: 3000 });

      if (mode === "frequency-then-water") {
        const logResult = await logWatering(plant);
        if (!logResult.ok) {
          toast.error("La frecuencia se guardó, pero no se pudo registrar el riego. Inténtalo de nuevo.");
        } else {
          toast(`✓ Regada · Siguiente riego en ${newDays} días`, { duration: 4000 });
        }
      }
    };
    ```

    **NOTA**: `logWatering(plant.id)` cambió a `logWatering(plant)` por el cambio de signature en Task 2. Verificar la consistencia.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('src/hooks/use-edit-watering-interval.ts','utf8');const r=fs.readFileSync('src/pages/RegarPage.tsx','utf8');const checks={hookImportsTrack:h.includes('import { track }'),hookHasSourceType:h.includes('FrequencyEditSource')||h.includes('source: \"ia_initial\"'),hookHasTrackCall:h.includes('track(\"watering_frequency_edited\"'),hookSignatureUpdated:h.includes('prevDays:')&&h.includes('source:'),hookHasPrevInterval:h.includes('prev_interval: prevDays'),hookHasNewInterval:h.includes('new_interval: newDays'),hookHasSourceInTrack:h.includes('source,')||h.includes('source: source'),pageHasSourceLogic:r.includes('source = \"null_filled_in_by_user\"')&&r.includes('source = \"user_override\"')&&r.includes('source = \"ia_initial\"'),pageInvocationUpdated:r.includes('editInterval(plant.id, newDays, prevDays, source)'),pageLogWateringUpdatedSignature:r.includes('logWatering(plant)')&&!r.includes('logWatering(plant.id)')};const fails=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);if(fails.length){console.error('FAIL:',fails);process.exit(1);}console.log('OK frequency edited tracking');"</automated>
  </verify>
  <acceptance_criteria>
    - `useEditWateringInterval` import `track`
    - Type `FrequencyEditSource = "ia_initial" | "user_override" | "null_filled_in_by_user"` exportado o inlined
    - Signature de `editInterval` extendida a 4 params: `(plantId, newDays, prevDays, source)`
    - Track call: `track("watering_frequency_edited", { plant_search_id, prev_interval, new_interval, source })`
    - Track antes del dispatch del CustomEvent
    - `RegarPage.handlePickerSave` calcula `prevDays` y `source` (3 branches) y los pasa a `editInterval`
    - `RegarPage.handlePickerSave` invoca `logWatering(plant)` (no `plant.id`) — consistencia con Task 2
    - Tests existentes actualizados con los 4 args; `npm test -- --run use-edit-watering-interval` exit 0
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>watering_frequency_edited cableado en hook; RegarPage deriva source correctamente.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Wire `calendar_card_navigated_to_detail` + tap-to-detail handler en `PlantWateringCard`</name>
  <files>src/components/PlantWateringCard.tsx, src/pages/RegarPage.tsx</files>
  <read_first>
    - src/components/PlantWateringCard.tsx (CURRENT desde 3-04)
    - src/pages/RegarPage.tsx (CURRENT post-Task 3)
    - src/components/PlantMapSheet.tsx líneas 67-74 (analog del track-then-navigate pattern)
    - .planning/phases/03-calendar-v0/03-CONTEXT.md D-17 evento 5
    - .planning/phases/03-calendar-v0/03-CONTEXT.md Success Criterion #6 (tap en card → /planta/:id)
  </read_first>
  <action>
    Implementar tap-to-detail en `PlantWateringCard` con tracking. El área principal de la card (foto + nombre + frecuencia + badge) navega a `/planta/:id`. Los elementos interactivos (botón frecuencia + botón Regar) hacen `stopPropagation` para evitar doble-trigger.

    **Cambio 1 — extender `PlantWateringCardProps`** con `onNavigateToDetail`:

    ```typescript
    export interface PlantWateringCardProps {
      plant: HomePlant;
      /** Position 0-based en la lista ordenada — para tracking. */
      position: number;
      onWater?: (plant: HomePlant) => Promise<{ ok: boolean }>;
      onUndo?: (plant: HomePlant, previousLastWateredAt: string | null) => Promise<{ ok: boolean }>;
      onEditFrequency?: (plant: HomePlant) => void;
      onWaterRequiringFrequency?: (plant: HomePlant) => void;
      /**
       * D-17 evento 5 + Success Criterion #6: tap en el área principal de la card
       * (foto + texto, NO botones internos) navega al detalle.
       * El componente trackea ANTES de invocar.
       */
      onNavigateToDetail?: (plant: HomePlant, position: number) => void;
    }
    ```

    **Cambio 2 — añadir handler `handleCardClick`:**

    ```typescript
    const handleCardClick = () => {
      if (onNavigateToDetail) {
        onNavigateToDetail(plant, position);
      }
    };
    ```

    **Cambio 3 — añadir `onClick={handleCardClick}` al área foto+nombre + `e.stopPropagation()` en los handlers de los botones internos:**

    Estado del JSX del article (post-3-04):

    ```tsx
    return (
      <article
        className={`flex flex-col gap-3 p-4 bg-card border-2 border-foreground rounded-2xl ${flashing ? "animate-flash-success" : ""}`}
        style={{ boxShadow: "var(--shadow-press)" }}
        aria-label={`Planta: ${commonName}`}
        data-watering-status={status}
      >
        <div className="flex items-start gap-3">
          {/* photo + name + freq + badge — area de tap-to-detail */}
        </div>
        <button onClick={handleWater}>...</button>
      </article>
    );
    ```

    Estado final: envolver el bloque `<div className="flex items-start gap-3">` en un `<button>` (o div con role="button") clickable que dispara `handleCardClick`. Los botones internos (`handleEditFrequency` y `handleWater`) ya están en el árbol — el click bubbling natural los activa primero porque son elementos `<button>` hijos. Pero PARA SEGURIDAD, añadir `e.stopPropagation()` en sus handlers:

    Cambiar el `<div className="flex items-start gap-3">` (la fila con foto + textos + badge) por un `<button>` (preserving the div className) que dispare `onClick={handleCardClick}`. Y modificar `handleEditFrequency` y `handleWater` para hacer `e.stopPropagation()`.

    JSX final:

    ```tsx
    return (
      <article
        className={`flex flex-col gap-3 p-4 bg-card border-2 border-foreground rounded-2xl ${flashing ? "animate-flash-success" : ""}`}
        style={{ boxShadow: "var(--shadow-press)" }}
        data-watering-status={status}
      >
        <button
          type="button"
          onClick={handleCardClick}
          className="w-full flex items-start gap-3 text-left focus-visible:outline-none focus-visible:ring-4 focus-visible:ring-ring rounded-xl"
          aria-label={`Ver detalle de ${commonName}`}
        >
          <img
            src={plant.imageUrl}
            alt=""
            loading="lazy"
            className="size-16 shrink-0 object-cover rounded-xl border-2 border-foreground"
          />
          <div className="flex-1 min-w-0 flex flex-col gap-1">
            <h2 className="font-display text-lg font-semibold text-foreground leading-tight">
              {commonName}
            </h2>
            <button
              type="button"
              onClick={(e) => { e.stopPropagation(); handleEditFrequency(); }}
              className="font-body text-sm text-muted-foreground text-left underline-offset-2 hover:underline focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring rounded"
              aria-label={`Frecuencia de riego: ${intervalLabel}. Pulsa para editar.`}
            >
              {intervalLabel}
            </button>
            <p className="font-body text-xs text-muted-foreground italic">
              {statusText}
            </p>
          </div>
          <span
            className={
              badgeIsWarn
                ? "shrink-0 px-2 py-1 rounded-lg bg-soft-warn-bg border-2 border-foreground font-display text-sm font-bold text-foreground"
                : "shrink-0 px-2 py-1 rounded-lg bg-muted border-2 border-foreground/30 font-display text-sm font-bold text-muted-foreground"
            }
            aria-label={`Estado: ${badgeText}`}
          >
            {badgeText}
          </span>
        </button>
        <button
          type="button"
          onClick={(e) => { e.stopPropagation(); handleWater(); }}
          className="w-full min-h-11 px-4 py-2.5 bg-primary text-primary-foreground border-2 border-foreground rounded-xl font-display text-base font-bold active:translate-x-[2px] active:translate-y-[2px] active:shadow-none focus-visible:outline-none focus-visible:ring-4 focus-visible:ring-ring focus-visible:ring-offset-2 transition-shadow"
          style={{ boxShadow: "var(--shadow-press)" }}
          aria-label={`${buttonLabel} ${commonName}`}
        >
          {buttonLabel}
        </button>
      </article>
    );
    ```

    **NOTA importante sobre nested buttons**: HTML no permite `<button>` dentro de `<button>`. El "botón frecuencia" interno (`onClick={handleEditFrequency}`) está dentro del `<button onClick={handleCardClick}>` outer. Esto es HTML inválido y puede causar warnings de React.

    **Solución**: cambiar el outer `<button>` por un `<div role="button" tabIndex={0} onKeyDown=...>` que simule el comportamiento. Esto es el patrón estándar cuando hay nested click areas.

    JSX final corregido:

    ```tsx
    return (
      <article
        className={`flex flex-col gap-3 p-4 bg-card border-2 border-foreground rounded-2xl ${flashing ? "animate-flash-success" : ""}`}
        style={{ boxShadow: "var(--shadow-press)" }}
        data-watering-status={status}
      >
        <div
          role="button"
          tabIndex={0}
          onClick={handleCardClick}
          onKeyDown={(e) => {
            if (e.key === "Enter" || e.key === " ") {
              e.preventDefault();
              handleCardClick();
            }
          }}
          className="w-full flex items-start gap-3 text-left cursor-pointer focus-visible:outline-none focus-visible:ring-4 focus-visible:ring-ring rounded-xl"
          aria-label={`Ver detalle de ${commonName}`}
        >
          <img
            src={plant.imageUrl}
            alt=""
            loading="lazy"
            className="size-16 shrink-0 object-cover rounded-xl border-2 border-foreground"
          />
          <div className="flex-1 min-w-0 flex flex-col gap-1">
            <h2 className="font-display text-lg font-semibold text-foreground leading-tight">
              {commonName}
            </h2>
            <button
              type="button"
              onClick={(e) => { e.stopPropagation(); handleEditFrequency(); }}
              className="font-body text-sm text-muted-foreground text-left underline-offset-2 hover:underline focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring rounded"
              aria-label={`Frecuencia de riego: ${intervalLabel}. Pulsa para editar.`}
            >
              {intervalLabel}
            </button>
            <p className="font-body text-xs text-muted-foreground italic">
              {statusText}
            </p>
          </div>
          <span
            className={
              badgeIsWarn
                ? "shrink-0 px-2 py-1 rounded-lg bg-soft-warn-bg border-2 border-foreground font-display text-sm font-bold text-foreground"
                : "shrink-0 px-2 py-1 rounded-lg bg-muted border-2 border-foreground/30 font-display text-sm font-bold text-muted-foreground"
            }
            aria-label={`Estado: ${badgeText}`}
          >
            {badgeText}
          </span>
        </div>
        <button
          type="button"
          onClick={handleWater}
          className="w-full min-h-11 px-4 py-2.5 bg-primary text-primary-foreground border-2 border-foreground rounded-xl font-display text-base font-bold active:translate-x-[2px] active:translate-y-[2px] active:shadow-none focus-visible:outline-none focus-visible:ring-4 focus-visible:ring-ring focus-visible:ring-offset-2 transition-shadow"
          style={{ boxShadow: "var(--shadow-press)" }}
          aria-label={`${buttonLabel} ${commonName}`}
        >
          {buttonLabel}
        </button>
      </article>
    );
    ```

    El botón Regar está como `<button>` directamente bajo `<article>` (no anidado), por lo que no necesita `stopPropagation`. Solo el botón frecuencia (que está dentro del div role="button") necesita `e.stopPropagation()`.

    **Cambio 4 — RegarPage añade `position` y wirea `onNavigateToDetail`:**

    En `RegarPage.tsx`, el `sortedPlants.map((plant) => ...)` debe usar el index y pasar `position`. Añadir también el handler `handleNavigateToDetail`:

    ```tsx
    import { useNavigate } from "react-router-dom";
    // ...

    const navigate = useNavigate();

    // D-17 evento 5: track + navigate al detalle.
    const handleNavigateToDetail = (plant: HomePlant, position: number) => {
      track("calendar_card_navigated_to_detail", {
        plant_search_id: plant.id,
        position,
      });
      navigate(`/planta/${plant.id}`);
    };

    // ... en el JSX:
    <ul className="flex flex-col gap-3" aria-label="Lista de plantas de casa">
      {sortedPlants.map((plant, index) => (
        <li key={plant.id}>
          <PlantWateringCard
            plant={plant}
            position={index}
            onWater={handleWater}
            onUndo={handleUndo}
            onEditFrequency={handleEditFrequency}
            onWaterRequiringFrequency={handleWaterRequiringFrequency}
            onNavigateToDetail={handleNavigateToDetail}
          />
        </li>
      ))}
    </ul>
    ```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('src/components/PlantWateringCard.tsx','utf8');const r=fs.readFileSync('src/pages/RegarPage.tsx','utf8');const checks={cardHasPositionProp:c.includes('position: number'),cardHasNavigateProp:c.includes('onNavigateToDetail?:'),cardHasHandleCardClick:c.includes('const handleCardClick'),cardHasRoleButton:c.includes('role=\"button\"'),cardHasKeyDown:c.includes('onKeyDown'),cardHasStopPropagation:c.includes('e.stopPropagation()'),pageImportsNavigate:r.includes('import { useNavigate }')||r.includes('useNavigate'),pageHasHandleNavigate:r.includes('const handleNavigateToDetail'),pageHasNavCall:r.includes('navigate(`/planta/${plant.id}`)'),pageHasTrackCall:r.includes('track(\"calendar_card_navigated_to_detail\"'),pageHasPositionProp:r.includes('position={index}'),pageHasOnNavigateProp:r.includes('onNavigateToDetail={handleNavigateToDetail}'),pageHasMapIndex:r.includes('sortedPlants.map((plant, index)')};const fails=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);if(fails.length){console.error('FAIL:',fails);process.exit(1);}console.log('OK calendar_card_navigated_to_detail');"</automated>
  </verify>
  <acceptance_criteria>
    - `PlantWateringCardProps` añade `position: number` (required) y `onNavigateToDetail?: (plant, position) => void`
    - Outer wrapper de foto/nombre cambiado a `<div role="button" tabIndex={0} onClick={handleCardClick} onKeyDown={...}>` (NO `<button>` anidado)
    - Botón frecuencia interno tiene `onClick={(e) => { e.stopPropagation(); handleEditFrequency(); }}`
    - Botón Regar exterior (NO está dentro del role=button) — no necesita stopPropagation
    - Keyboard support: Enter/Space dispara click en el div role=button
    - `RegarPage` importa `useNavigate`, define `handleNavigateToDetail`
    - `handleNavigateToDetail` trackea `calendar_card_navigated_to_detail` con `plant_search_id` y `position` ANTES de `navigate(`/planta/${plant.id}`)`
    - `sortedPlants.map((plant, index) => ...)` usa el index como position
    - `<PlantWateringCard>` recibe `position={index}` y `onNavigateToDetail={handleNavigateToDetail}`
    - `npx tsc --noEmit` exit 0
    - `npm run build` exit 0
  </acceptance_criteria>
  <done>Card tap-to-detail con tracking; nested HTML válido (div role=button); RegarPage pasa position y handler.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Actualizar `docs/posthog-events.md` con sección Phase 3 — Calendar v0</name>
  <files>docs/posthog-events.md</files>
  <read_first>
    - docs/posthog-events.md líneas 155-203 (analog: sección Phase 03.1 — copy structure VERBATIM)
    - .planning/phases/03-calendar-v0/03-CONTEXT.md D-17 (specs de los 5 eventos)
  </read_first>
  <action>
    Añadir nueva sección al final de `docs/posthog-events.md` (después de la línea 203 que cierra el funnel cross-fase de Phase 03.1).

    Contenido a añadir (literal):

    ```markdown

    ---

    ## Phase 3 — Calendar v0

    **Status:** Implementado en Phase 3. Los archivos `.tsx` / `.ts` son `src/pages/RegarPage.tsx`, `src/components/PlantWateringCard.tsx`, `src/hooks/use-log-watering.ts` y `src/hooks/use-edit-watering-interval.ts`.

    ### Eventos del calendario de riego

    | Evento | Propiedades | Archivo | Cuándo se dispara |
    |---|---|---|---|
    | `calendar_opened` | `home_count`, `overdue_count`, `pending_first_time_count` | `src/pages/RegarPage.tsx` | Usuario navega a `/regar` y el hook `useHomePlants` resuelve (disparar **una vez por montaje**, gated con flag local `trackedOpen` para evitar inflar la métrica) |
    | `watering_logged` | `plant_search_id`, `days_remaining_before`, `interval_days`, `was_first_time` | `src/hooks/use-log-watering.ts` | Tras el UPDATE exitoso de `last_watered_at`, **antes** del dispatch de `mp:plant-watered`. NO se dispara en el optimistic update — solo cuando la DB confirma |
    | `watering_undone` | `plant_search_id` | `src/hooks/use-log-watering.ts` | Tras el UPDATE de revert (deshacer) exitoso, **antes** del dispatch de `mp:plant-watered`. Mide tasa de mistaps |
    | `watering_frequency_edited` | `plant_search_id`, `prev_interval`, `new_interval`, `source` | `src/hooks/use-edit-watering-interval.ts` | Tras el UPDATE exitoso de `watering_interval_days`, **antes** del dispatch de `mp:plant-frequency-updated` |
    | `calendar_card_navigated_to_detail` | `plant_search_id`, `position` | `src/components/PlantWateringCard.tsx` | Usuario toca el área principal de la card (foto + nombre, NO el botón ni el texto frecuencia). Disparar **antes** del `navigate(\`/planta/${id}\`)` |

    ### Detalle de propiedades

    **`calendar_opened`**
    - `home_count`: número total de plantas casa del usuario (= `plants.length` del hook `useHomePlants`).
    - `overdue_count`: cuántas plantas tienen `daysRemaining < 0` (atrasadas) en el momento del open.
    - `pending_first_time_count`: cuántas plantas tienen `status === 'pending-first'` (sin `last_watered_at` o sin `watering_interval_days`).
    - Disparar **una sola vez por montaje** de `RegarPage` (flag in-component `trackedOpen`).

    **`watering_logged`**
    - `plant_search_id`: UUID de la fila.
    - `days_remaining_before`: signed integer; negativo si atrasada al momento del log, 0 si era "Toca regar hoy", positivo si el usuario regó anticipadamente, **null** si era pending-first (no había countdown).
    - `interval_days`: frecuencia configurada al momento del log (puede ser null si la planta era pending-first sin intervalo IA).
    - `was_first_time`: boolean. True si la planta no tenía `last_watered_at` antes del log (i.e. era pending-first por only-null lastWatered).

    **`watering_undone`**
    - `plant_search_id`: UUID. Permite analizar qué plantas se deshacen más (¿confusión UX? ¿botón demasiado prominente?).

    **`watering_frequency_edited`**
    - `plant_search_id`: UUID.
    - `prev_interval`: valor previo (null si IA no dio frecuencia).
    - `new_interval`: nuevo valor [1, 60].
    - `source`: enum derivado por el caller (RegarPage):
      - `'null_filled_in_by_user'`: `prev_interval === null` (IA no dio frecuencia, usuario rellenó por primera vez). Este es típicamente el flujo D-14 case 2.
      - `'user_override'`: `prev_interval !== null AND new_interval !== prev_interval`. Usuario sobreescribió la frecuencia IA.
      - `'ia_initial'`: `prev_interval !== null AND new_interval === prev_interval`. No-op edit (usuario abrió picker y guardó sin cambiar) — debería ser raro.

    **`calendar_card_navigated_to_detail`**
    - `plant_search_id`: UUID.
    - `position`: índice **0-based** en la lista ordenada (por urgencia, ver D-09). Permite analizar si los usuarios prefieren explorar las plantas urgentes (índice bajo) o navegan a cualquiera.

    ### Funnel previsto (para análisis posterior)

    Funnel principal — riego completado:
    ```
    calendar_opened (con home_count >= 1)
      → watering_logged (con was_first_time = false)        [riego habitual]
    ```

    Funnel onboarding del calendario — primera planta regada:
    ```
    plant_identified → classification_action_clicked (action: 'home') → classification_completed
      → calendar_opened (con pending_first_time_count >= 1)
      → watering_frequency_edited (con source: 'null_filled_in_by_user')  [si IA dio null]
      → watering_logged (con was_first_time: true)
    ```

    Funnel de exploración:
    ```
    calendar_opened
      → calendar_card_navigated_to_detail (con position bajo → planta urgente)
      → result_section_click (en /planta/:id)
    ```

    Funnel de mistaps (signal de UX issues):
    ```
    watering_logged
      → watering_undone (en los 4s siguientes)              [tasa alta = botón Regar demasiado fácil de pulsar]
    ```
    ```

    **NO MODIFICAR:**
    - Las secciones existentes (Phase 2, Phase 02.1, Phase 03.1)
    - El header del documento
    - El TOC implícito por estructura
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('docs/posthog-events.md','utf8');const checks={hasPhase3Section:s.includes('## Phase 3 — Calendar v0'),hasCalendarOpenedEntry:s.includes('| `calendar_opened` |'),hasWateringLoggedEntry:s.includes('| `watering_logged` |'),hasWateringUndoneEntry:s.includes('| `watering_undone` |'),hasFrequencyEditedEntry:s.includes('| `watering_frequency_edited` |'),hasCardNavEntry:s.includes('| `calendar_card_navigated_to_detail` |'),hasSourceEnumDocs:s.includes('null_filled_in_by_user')&&s.includes('user_override')&&s.includes('ia_initial'),hasFunnelMain:s.includes('Funnel principal'),hasFunnelMistaps:s.includes('mistaps'),preservesPhase031:s.includes('## Phase 03.1 — Plant Map v0')};const fails=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);if(fails.length){console.error('FAIL:',fails);process.exit(1);}console.log('OK posthog-events doc');"</automated>
  </verify>
  <acceptance_criteria>
    - Nueva sección `## Phase 3 — Calendar v0` añadida al final del documento
    - Tabla de 5 eventos con columnas (Evento, Propiedades, Archivo, Cuándo se dispara)
    - Detalle de propiedades para cada uno de los 5 eventos
    - 4 funnels documentados (principal, onboarding, exploración, mistaps)
    - Source enum (3 valores) documentado con derivación lógica
    - Secciones previas (Phase 2, 02.1, 03.1) intactas
    - El doc compila como markdown (no broken syntax)
  </acceptance_criteria>
  <done>docs/posthog-events.md actualizado con sección Phase 3 completa + funnels.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Añadir `MOCK_HOME_PLANTS` a `e2e/fixtures.ts` + crear `e2e/regar.spec.ts`</name>
  <files>e2e/fixtures.ts, e2e/regar.spec.ts</files>
  <read_first>
    - e2e/fixtures.ts (CURRENT — entender el patrón MOCK_WILD_WITH_COORDS líneas 76-116)
    - e2e/map.spec.ts (analog del E2E — copy structure)
    - .planning/phases/03-calendar-v0/03-CONTEXT.md Success Criterion #11 (Playwright happy path)
    - .planning/phases/03-calendar-v0/03-RESEARCH.md sub-phase 3-05 (E2E strategy)
  </read_first>
  <action>
    **Sub-task 6.1 — Añadir `MOCK_HOME_PLANTS` a `e2e/fixtures.ts`:**

    Añadir DESPUÉS de `MOCK_WILD_WITH_COORDS` (línea 116) y antes de la sección de Supabase URL (línea 119):

    ```typescript
    // ---------------------------------------------------------------------------
    // Phase 3 — Home plants (for /regar tests)
    // ---------------------------------------------------------------------------

    // VERIFICATION ajuste #1: fixed reference time to eliminate midnight flakiness.
    // Using Date.now() at module load means the relative timestamps shift across
    // test runs and can flip status (e.g. X=0 → X=-1) if the test starts before
    // midnight and asserts after. A fixed anchor + `page.clock.install` (in the
    // e2e spec below) keeps the wall clock deterministic.
    const FIXED_TEST_NOW = new Date("2026-05-17T12:00:00Z").getTime();

    // Helper: ISO string for N days before the FIXED anchor (not Date.now()).
    function isoDaysAgo(days: number): string {
      return new Date(FIXED_TEST_NOW - days * 86_400_000).toISOString();
    }

    export const MOCK_HOME_PLANTS = [
      {
        id: "home-001",
        name: "Monstera deliciosa (Monstera deliciosa)",
        description: "Planta tropical de hojas grandes.",
        care: "Riego semanal, luz indirecta.",
        diagnosis: "Saludable.",
        image_url: "data:image/jpeg;base64,/9j/home001",
        context: "home" as const,
        watering_interval_days: 7,
        last_watered_at: isoDaysAgo(3),  // X = 4 days remaining → "normal"
        created_at: "2026-05-10T10:00:00Z",
        user_id: MOCK_USER.id,
      },
      {
        id: "home-002",
        name: "Pothos dorado (Epipremnum aureum)",
        description: "Planta colgante muy resistente.",
        care: "Riego cada 10 días.",
        diagnosis: "Hojas amarillas en la base.",
        image_url: "data:image/jpeg;base64,/9j/home002",
        context: "home" as const,
        watering_interval_days: 10,
        last_watered_at: isoDaysAgo(10),  // X = 0 → "urgent" / "Toca regar hoy"
        created_at: "2026-04-15T10:00:00Z",
        user_id: MOCK_USER.id,
      },
      {
        id: "home-003",
        name: "Ficus elastica (Ficus elastica)",
        description: "Árbol de interior de hojas brillantes.",
        care: "Luz brillante, riego moderado.",
        diagnosis: "Buen estado.",
        image_url: "data:image/jpeg;base64,/9j/home003",
        context: "home" as const,
        watering_interval_days: 7,
        last_watered_at: isoDaysAgo(12),  // X = -5 → "overdue"
        created_at: "2026-03-20T10:00:00Z",
        user_id: MOCK_USER.id,
      },
      {
        id: "home-004",
        name: "Begonia maculata",
        description: "Begonia con motas blancas.",
        care: "Luz filtrada, ambiente húmedo.",
        diagnosis: "Buen estado.",
        image_url: "data:image/jpeg;base64,/9j/home004",
        context: "home" as const,
        watering_interval_days: null,  // IA didn't determine → pending-first
        last_watered_at: null,
        created_at: "2026-05-15T10:00:00Z",
        user_id: MOCK_USER.id,
      },
    ];
    ```

    **Sub-task 6.2 — Crear `e2e/regar.spec.ts`:**

    Copy structure verbatim de `e2e/map.spec.ts`. 2 tests: happy path + defensive empty.

    ```typescript
    import { test, expect, MOCK_HOME_PLANTS } from "./fixtures";

    const SUPABASE_PLANT_SEARCHES = "https://sdxfxkqzgnonxfshbjfc.supabase.co/rest/v1/plant_searches*";

    /**
     * Phase 3 — Calendar v0 E2E tests
     *
     * Cubre Success Criterion #11 (Playwright happy path: clasificar casa →
     * ver tab → tap Regar → contador reset) y el caso defensivo del
     * redirect-on-empty.
     *
     * Las llamadas REST a plant_searches se mockean por test para evitar
     * dependencia del Supabase real.
     */
    test.describe("Calendar (/regar)", () => {
      // ───────────────────────────────────────────────────────────────────
      // Happy path: navigate /regar, see cards, tap Regar, see toast, counter resets
      // ───────────────────────────────────────────────────────────────────
      test("happy path: navigate /regar, see cards, tap Regar, toast con Deshacer, counter reset", async ({
        page,
        asAuthenticated: _asAuth,
      }) => {
        // VERIFICATION ajuste #1: install clock at the FIXED anchor used by
        // MOCK_HOME_PLANTS so day-boundary transitions don't affect status assertions.
        await page.clock.install({ time: new Date("2026-05-17T12:00:00Z") });
        await page.unroute(SUPABASE_PLANT_SEARCHES);
        await page.route("**/rest/v1/plant_searches*", (route) => {
          const req = route.request();
          const method = req.method();
          if (method === "DELETE") {
            return route.fulfill({ status: 200, json: [] });
          }
          // PATCH = UPDATE (Supabase REST uses PATCH for updates). Respond 200 with empty array.
          if (method === "PATCH") {
            return route.fulfill({ status: 200, json: [] });
          }
          // GET = SELECT
          return route.fulfill({
            status: 200,
            headers: {
              "content-range": `0-${MOCK_HOME_PLANTS.length - 1}/${MOCK_HOME_PLANTS.length}`,
            },
            json: MOCK_HOME_PLANTS,
          });
        });

        // Navigate directly to /regar
        await page.goto("/regar");

        // Cards should appear. Use a generous timeout to accommodate webkit auth bootstrapping.
        await expect(page.locator("article[data-watering-status]").first()).toBeVisible({
          timeout: 10000,
        });

        // Header copy verbatim
        await expect(page.getByRole("heading", { name: "¿Toca regar?" })).toBeVisible();
        await expect(page.getByText("Tus plantas casa")).toBeVisible();

        // Sort por urgencia (D-09): overdue (home-003) primero, después urgent (home-002), después normal (home-001), después pending-first (home-004)
        const cards = page.locator("article[data-watering-status]");
        await expect(cards).toHaveCount(MOCK_HOME_PLANTS.length);

        // Tap the FIRST card's "Regar" button (overdue: home-003, "Ficus elastica")
        const firstCard = cards.first();
        await expect(firstCard).toHaveAttribute("data-watering-status", "overdue");

        const regarButton = firstCard.getByRole("button", { name: /^Regar Ficus/ });
        await regarButton.click();

        // Toast with Deshacer should appear
        const toast = page.locator("[data-sonner-toast]").first();
        await expect(toast).toBeVisible({ timeout: 5000 });
        await expect(toast.getByText(/✓ Regada · Siguiente riego en/)).toBeVisible();
        await expect(toast.getByRole("button", { name: "Deshacer" })).toBeVisible();

        // Counter resets visually: the optimistic update changes the card's
        // data-watering-status from "overdue" to "normal" (since lastWateredAt is now).
        await expect(firstCard).toHaveAttribute("data-watering-status", /normal|urgent/, {
          timeout: 5000,
        });
      });

      // ───────────────────────────────────────────────────────────────────
      // Defensive: redirect a / cuando user no tiene plantas casa
      // ───────────────────────────────────────────────────────────────────
      test("redirects to / when user has zero home plants", async ({
        page,
        asAuthenticated: _asAuth,
      }) => {
        // VERIFICATION ajuste #1: same clock anchor as happy path (consistency).
        await page.clock.install({ time: new Date("2026-05-17T12:00:00Z") });
        await page.unroute(SUPABASE_PLANT_SEARCHES);
        await page.route("**/rest/v1/plant_searches*", (route) =>
          route.fulfill({
            status: 200,
            headers: { "content-range": "0-0/0" },
            json: [],
          }),
        );

        await page.goto("/regar");

        // Should silently redirect to /. Verify by checking we're NOT on /regar.
        await expect(page).not.toHaveURL(/\/regar/, { timeout: 5000 });
        // And we should see the home page (Inicio tab active in BottomTabBar)
        await expect(page.getByRole("button", { name: "Inicio" })).toBeVisible({
          timeout: 5000,
        });
      });
    });
    ```

    **NOTAS sobre el E2E**:
    - El optimistic update cambia `data-watering-status` inmediatamente — el test verifica el cambio (overdue → normal).
    - El PATCH del UPDATE se mockea con éxito (devuelve 200 + array vacío como respuesta de Supabase).
    - Si el sort por urgencia no funciona en el render por timing/cache, el test ajusta el selector — pero esperamos que `data-watering-status` del primer card sea "overdue" basado en el MOCK_HOME_PLANTS.
    - El test usa `regex` para el toast copy porque el número de días depende del cálculo en tiempo real (puede ser "Siguiente riego en 7 días" exactamente).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const f=fs.readFileSync('e2e/fixtures.ts','utf8');const s=fs.readFileSync('e2e/regar.spec.ts','utf8');const checks={fixturesHasMock:f.includes('export const MOCK_HOME_PLANTS'),fixturesHasContextHome:f.includes('context: \"home\"'),fixturesHasFourStates:f.includes('home-001')&&f.includes('home-002')&&f.includes('home-003')&&f.includes('home-004'),fixturesHasNullCase:f.includes('watering_interval_days: null'),specHasImports:s.includes('import { test, expect, MOCK_HOME_PLANTS }'),specHasHappyPath:s.includes('happy path'),specHasDefensive:s.includes('redirects to /'),specRoutesPATCH:s.includes('method === \"PATCH\"'),specMocksHomePlants:s.includes('MOCK_HOME_PLANTS'),specChecksDataAttr:s.includes('data-watering-status'),specChecksToast:s.includes('data-sonner-toast'),specChecksDeshacer:s.includes('Deshacer'),specChecksHeader:s.includes('¿Toca regar?')};const fails=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);if(fails.length){console.error('FAIL:',fails);process.exit(1);}console.log('OK e2e regar');"</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/fixtures.ts` exporta `MOCK_HOME_PLANTS` con 4 plantas
    - Las 4 plantas cubren los 4 estados D-08: normal (X=4), urgent (X=0), overdue (X=-5), pending-first (interval=null AND lastWatered=null)
    - `e2e/regar.spec.ts` tiene 2 tests: happy path + defensive empty redirect
    - Happy path mockea GET + PATCH a `plant_searches`
    - Happy path verifica header copy "¿Toca regar?" + "Tus plantas casa"
    - Happy path verifica tap Regar → toast aparece con Deshacer + counter reset (data-watering-status cambia)
    - Defensive test verifica redirect a `/` (NOT toHaveURL /regar) y home page visible
    - `npx playwright test e2e/regar.spec.ts --list` muestra los 2 tests sin error de import
    - `npx tsc --noEmit` exit 0 (incluye e2e/ si tsconfig lo abarca; sino skip)
  </acceptance_criteria>
  <done>MOCK_HOME_PLANTS + e2e/regar.spec.ts creados; 2 tests cubren happy path + defensive.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 7: Microcopy review final (D-15/D-16)</name>
  <files>src/components/PlantWateringCard.tsx, src/components/WateringFrequencyPicker.tsx, src/pages/RegarPage.tsx</files>
  <read_first>
    - src/components/PlantWateringCard.tsx (TODO el archivo)
    - src/components/WateringFrequencyPicker.tsx (TODO el archivo)
    - src/pages/RegarPage.tsx (TODO el archivo)
    - .planning/phases/03-calendar-v0/03-CONTEXT.md D-15, D-16
  </read_first>
  <action>
    Pasada manual de microcopy verbatim contra D-15 y D-16. La revisión se hace por grep + lectura visual. NO se modifica código a menos que se detecte una regresión.

    **Checklist D-15 (tono suave, no alarmista):**

    1. **No usar rojo intenso para urgencia:**
       - Grep `text-destructive` en PlantWateringCard.tsx, RegarPage.tsx, WateringFrequencyPicker.tsx.
       - Resultado esperado: `text-destructive` SOLO en WateringFrequencyPicker.tsx (error message del input out-of-range — uso correcto, es un error de input, no de urgencia).
       - Resultado esperado: NINGUNA aparición en PlantWateringCard.tsx (badge urgent/overdue usa `bg-soft-warn-bg`, NO `bg-destructive`).

    2. **Copy strings empáticos verbatim:**
       - `"Próximo riego en X días"` ✓ (status normal)
       - `"Toca regar hoy"` ✓ (status urgent — NO "URGENTE!")
       - `"Lleva N días esperándote"` ✓ (status overdue — NO "ATRASADA")
       - `"Pendiente primera vez · Toca regar para empezar"` ✓ (status pending-first)
       - `"Tus plantas casa"` ✓ (subheader — NO "Lista de tareas")
       - `"✓ Regada · Siguiente riego en N días"` ✓ (toast log)

    **Checklist D-16 (frecuencia sin atribución):**

    1. **Grep prohibido:**
       - `según IA` — NO debe aparecer en ningún archivo de la phase
       - `tú decidiste` — NO
       - `IA dijo` — NO
       - `inicial IA` — NO

    2. **Copy strings verbatim:**
       - `"Cada N días"` ✓ (cuando intervalDays !== null)
       - `"Sin frecuencia"` ✓ (cuando intervalDays === null)

    **Acción correctiva si se detecta regresión:** corregir el archivo y documentar en SUMMARY de esta sub-phase.

    **Verificación final (grep matrix):**

    Correr estos greps en el codebase y verificar que ningún resultado contradice las reglas:

    ```bash
    # D-15 verifications
    grep -rn 'bg-destructive\|text-destructive' src/components/PlantWateringCard.tsx
    # Expected: 0 matches in PlantWateringCard

    grep -rn 'URGENTE\|ATRASAD\|EXCLAMAT' src/components/PlantWateringCard.tsx src/pages/RegarPage.tsx
    # Expected: 0 matches (no copy alarmista)

    grep -rn 'soft-warn-bg' src/components/PlantWateringCard.tsx
    # Expected: at least 1 match (badge urgent/overdue uses soft-warn)

    # D-16 verifications
    grep -rEn 'según\s+IA|tú\s+decidiste|IA\s+dijo|inicial\s+IA' src/
    # Expected: 0 matches

    grep -rn '"Cada\|Sin frecuencia' src/components/PlantWateringCard.tsx
    # Expected: matches present
    ```

    NO MODIFICAR archivos en esta task — es una revisión read-only. Si la check encuentra un fallo:
    1. Documentar el fallo encontrado
    2. Corregir el copy strings sobre la marcha en el archivo afectado
    3. Re-correr la verify para confirmar

    Es probable que NO se detecte ningún fallo si las tasks anteriores siguieron los copy strings verbatim de los planes.
  </action>
  <verify>
    <automated>bash -c "set -e; failures=''; for term in 'URGENTE' 'ATRASADA DÍAS' '!!!'; do count=$(grep -rEn \"\$term\" src/components/PlantWateringCard.tsx src/pages/RegarPage.tsx 2>/dev/null | wc -l | tr -d ' '); if [ \"\$count\" != \"0\" ]; then failures=\"\$failures \$term\"; fi; done; if [ \"$(grep -rEn 'según\s+IA|tú\s+decidiste' src/ 2>/dev/null | wc -l | tr -d ' ')\" != \"0\" ]; then failures=\"\$failures attribution-found\"; fi; bg_destructive_in_card=$(grep -c 'bg-destructive' src/components/PlantWateringCard.tsx || echo 0); if [ \"\$bg_destructive_in_card\" != \"0\" ]; then failures=\"\$failures bg-destructive-in-card\"; fi; soft_warn_in_card=$(grep -c 'soft-warn-bg' src/components/PlantWateringCard.tsx || echo 0); if [ \"\$soft_warn_in_card\" = \"0\" ]; then failures=\"\$failures missing-soft-warn-in-card\"; fi; if [ -n \"\$failures\" ]; then echo \"MICROCOPY FAILURES:\$failures\"; exit 1; fi; echo 'OK microcopy review'"</automated>
  </verify>
  <acceptance_criteria>
    - `PlantWateringCard.tsx`: 0 usos de `bg-destructive` o `text-destructive`
    - `PlantWateringCard.tsx`: al menos 1 uso de `bg-soft-warn-bg`
    - 0 strings con "URGENTE", "ATRASADA", "!!!" o patrones similares alarmistas
    - 0 usos de "según IA", "tú decidiste", "IA dijo", "inicial IA"
    - `WateringFrequencyPicker.tsx` `text-destructive` está OK (error message de validación)
    - Si se detecta regresión y se corrige, documentar en SUMMARY
  </acceptance_criteria>
  <done>Microcopy review completo; D-15 y D-16 confirmadas sin regresiones (o corregidas).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→PostHog | track() llama via `posthog-js` ya inicializado por consent. Sin riesgo nuevo. |
| client→React Router | navigate(`/planta/${id}`) — id es UUID del propio user (filtered por RLS). |
| test→mock server | Playwright route mocks aislan los E2E del Supabase real. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-23 | Information disclosure | Track events incluyen `plant_search_id` (UUID) | accept | UUIDs no son PII. Mismo patrón que Phase 02.1 y 03.1. Consent gating via `src/lib/track.ts`. |
| T-03-24 | Information disclosure | `position` revela orden de la lista — puede inferirse comportamiento de usuario | accept | El position es información agregada agregada por urgencia, no expone datos sensibles del usuario. |
| T-03-25 | Open redirect | `navigate(/planta/${plant.id})` — el id viene del plant que el componente ya tiene | mitigate | El plant viene de useHomePlants filtrado por user_id. Sin user input. Sin riesgo. |
| T-03-26 | DoS via tracking | Spam de Regar/Deshacer dispararía track 100 veces | accept | Costo trivial; consent gating limita el blast radius. |
| T-03-27 | Test data leak | MOCK_HOME_PLANTS contiene "user_id: MOCK_USER.id" — coherente con fixtures de map y history | accept | Solo en e2e/, no shipping al cliente. |
</threat_model>

<verification>
- 5 eventos PostHog cableados en sus 5 archivos correctos.
- `docs/posthog-events.md` con sección Phase 3 completa.
- `e2e/fixtures.ts` exporta `MOCK_HOME_PLANTS`.
- `e2e/regar.spec.ts` con 2 tests.
- Microcopy review pasa los greps de D-15/D-16.
- `npx tsc --noEmit` exit 0.
- `npm run build` exit 0.
- `npm test -- --run` sin regresiones (los tests actualizados de Tasks 2 y 3 pasan).
- `npx playwright test e2e/regar.spec.ts` corre los 2 tests (puede requerir el dev server con `npm run dev` en otra terminal).
- Smoke manual: abrir DevTools → Network → tab `PostHog`. Navegar a `/regar` → ver `calendar_opened` en el log. Tap Regar → ver `watering_logged`. Tap Deshacer → ver `watering_undone`. Tap "Cada 7 días" → editar → save → ver `watering_frequency_edited`. Tap área foto/nombre de una card → ver `calendar_card_navigated_to_detail`.
- Funnel verification: en PostHog dashboard (en producción) crear un funnel `plant_identified` → `classification_action_clicked` (action: home) → `calendar_opened` → `watering_logged`. Verificar que los eventos llegan en este orden.
</verification>

<success_criteria>
- 5 eventos PostHog disparándose en producción tras el deploy.
- Tests E2E pasan en CI (Playwright).
- Documentación de eventos completa y accesible para el equipo (docs/posthog-events.md).
- Microcopy review confirma tono suave (D-15) y sin atribución (D-16).
- Tap en área principal de card navega a `/planta/:id` con tracking previo.
- TypeScript compila, build pasa, todos los tests unitarios + E2E pasan.
- Phase 3 está production-ready.
</success_criteria>

<output>
After completion, create `.planning/phases/03-calendar-v0/03-05-SUMMARY.md` documenting:
- 5 eventos PostHog cableados en sus 5 sitios correctos siguiendo el pattern "track BEFORE side-effect"
- El track de `watering_frequency_edited` requiere prev_interval y source — el caller (RegarPage) los deriva en `handlePickerSave` y los pasa al hook (decisión: caller-tracks vs hook-tracks resuelta a caller-side para evitar round-trip a DB)
- Card tap-to-detail implementado con `<div role="button" tabIndex={0}>` para evitar HTML inválido de nested buttons
- E2E test cubre happy path completo y defensive empty redirect
- Microcopy review pasa los gates de D-15/D-16
- Phase 3 cierra con todos los gates (lint, types, tests, e2e, docs, tracking) en verde
- Sub-phase final del release — ahora se puede mergear todo a develop y luego a main como un único PR release per CONTEXT.md § sub-phases
</output>
