---
phase: 03-calendar-v0
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/hooks/use-home-plants.ts
  - src/hooks/use-home-plants.test.ts
  - src/components/PlantWateringCard.tsx
  - src/pages/RegarPage.tsx
  - src/index.css
  - tailwind.config.ts
autonomous: true
requirements: [RIEG-01]
requirements_addressed: [RIEG-01]
tags: [phase-03, regar-page, home-plants, watering-card, soft-warn-token]

must_haves:
  truths:
    - "Existe el hook `useHomePlants` que devuelve `{ plants: HomePlant[], isLoading, refetch }` con plantas filtradas por `context = 'home'` del usuario actual."
    - "El hook listens to `mp:plant-context-updated` para refetch reactivo cuando una planta se clasifica como casa o se reclasifica."
    - "Existe el componente `PlantWateringCard` que renderiza: foto 64×64 rounded-xl border-2, nombre común (font-display), frecuencia 'Cada N días' o 'Sin frecuencia', badge 'X d' (estado pending-first-time → '—'), status text empático per D-15, y botón full-width con copy state-dependent 'Regada' / 'Regar' (D-10)."
    - "El botón es renderizado pero NO funcional (onClick es no-op o console.log) — sub-phase 3-03 wirea el botón."
    - "Card status copy verbatim (D-15): 'Próximo riego en X días' (X>0), 'Toca regar hoy' (X=0), 'Lleva N días esperándote' (X<0), 'Pendiente primera vez · Toca regar para empezar' (last_watered_at=null OR interval_days=null)."
    - "Frecuencia copy verbatim (D-16): 'Cada N días' (sin atribución IA/usuario) o 'Sin frecuencia' (interval_days=null)."
    - "En sub-phase 3-02 SIN migración: `last_watered_at` aún no existe en la DB; toda planta home aparece como 'Pendiente primera vez' (estado D-14). Sub-phase 3-03 añade `last_watered_at` y el countdown real."
    - "`RegarPage.tsx` reemplaza el placeholder con la lista: `useHomePlants` + redirect a `/` si `plants.length === 0` (deep-link defensivo) + render N `PlantWateringCard`."
    - "El token `--soft-warn: 38 80% 60%` está definido en `src/index.css` `:root` (D-15 + researcher risk #5)."
    - "`--soft-warn-bg: 48 85% 92%` también está definido (fondo suave para el badge urgente)."
    - "`tailwind.config.ts` expone `softWarn` y `softWarnBg` en `theme.extend.colors` para que `bg-soft-warn-bg` y `text-soft-warn` (o equivalentes) sean utilidades disponibles."
    - "El hook tiene unit test `use-home-plants.test.ts` siguiendo el patrón `makeChain` de `use-context-counts.test.ts` y `use-wild-plants-with-coords.test.ts`."
  artifacts:
    - path: "src/hooks/use-home-plants.ts"
      provides: "Hook con filter context='home' + mountedRef + listener mp:plant-context-updated"
      exports: ["useHomePlants", "HomePlant", "UseHomePlantsReturn"]
      min_lines: 80
    - path: "src/hooks/use-home-plants.test.ts"
      provides: "Unit tests for use-home-plants — anon returns empty, auth returns rows, listener triggers refetch"
      min_lines: 100
    - path: "src/components/PlantWateringCard.tsx"
      provides: "Sticker card con foto + nombre + frecuencia + badge + status + botón Regar/Regada"
      exports: ["PlantWateringCard", "PlantWateringCardProps"]
      min_lines: 100
    - path: "src/pages/RegarPage.tsx"
      provides: "Lista con cards, redirect-on-empty, layout normal"
      contains: "useHomePlants"
    - path: "src/index.css"
      provides: "Tokens --soft-warn y --soft-warn-bg en :root"
      contains: "--soft-warn"
    - path: "tailwind.config.ts"
      provides: "extend.colors.softWarn + softWarnBg"
      contains: "softWarn"
  key_links:
    - from: "src/hooks/use-home-plants.ts"
      to: "supabase.from('plant_searches')"
      via: ".eq('context', 'home') + select fields incluyendo watering_interval_days"
      pattern: "context.*home"
    - from: "src/hooks/use-home-plants.ts"
      to: "window CustomEvent mp:plant-context-updated"
      via: "useEffect addEventListener calling load()"
      pattern: "mp:plant-context-updated"
    - from: "src/pages/RegarPage.tsx"
      to: "src/hooks/use-home-plants.ts"
      via: "import + render N cards"
      pattern: "useHomePlants"
    - from: "src/pages/RegarPage.tsx"
      to: "src/components/PlantWateringCard.tsx"
      via: "plants.map(p => <PlantWateringCard plant={p} />)"
      pattern: "PlantWateringCard"
    - from: "src/components/PlantWateringCard.tsx"
      to: "src/index.css token --soft-warn"
      via: "bg-[hsl(var(--soft-warn-bg))] / text-[hsl(var(--soft-warn))] (NO hardcoded HSL)"
      pattern: "soft-warn"
---

<objective>
Construir la pantalla `/regar` con la lista estática de plantas casa:

1. Hook `useHomePlants` modelado verbatim sobre `useWildPlantsWithCoords` — mismo shape `mountedRef + useAuth + useCallback + early-return-anon`. Filter `context = 'home'`. Select incluye `watering_interval_days` (Phase 2 ya añadió la columna). NO incluye `last_watered_at` todavía — sub-phase 3-03 lo añade.
2. Componente `PlantWateringCard` que renderiza el sticker card (foto + nombre + frecuencia + badge + status + botón). En esta sub-phase TODO el contenido es estático/derived sin lógica de countdown — toda planta aparece como "Pendiente primera vez" porque aún no existe `last_watered_at` en la DB (sub-phase 3-03 añade la columna y el cálculo).
3. `RegarPage.tsx` reemplaza el placeholder de 3-01 con la lista real: usa `useHomePlants`, redirect-on-empty a `/`, renderiza N cards.
4. Tokens `--soft-warn` y `--soft-warn-bg` añadidos a `src/index.css` y expuestos como utilidades Tailwind en `tailwind.config.ts` — son necesarios para los badges urgentes que sub-phase 3-03 activará (en esta phase el badge siempre es gris/pending, pero los tokens deben existir).
5. Unit test del hook siguiendo el patrón `makeChain` de los tests existentes.

Purpose: Wave 2 entrega la "carcasa visual" sin lógica de scheduling. La pantalla es navegable, las cards renderizan, y todo el state-machine de countdown está scaffolded esperando el dato (`last_watered_at`) que sub-phase 3-03 introduce. Esto evita un commit-grande-monolítico: la migración + countdown + UPDATE optimista vive en su propia sub-phase con su propio review.

Output: 2 archivos nuevos en hooks + 1 nuevo en components + 1 sobrescrito en pages + 2 modificados en design system (~270 LOC nuevas).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-calendar-v0/03-CONTEXT.md
@.planning/phases/03-calendar-v0/03-RESEARCH.md
@CLAUDE.md
@src/hooks/use-wild-plants-with-coords.ts
@src/hooks/use-wild-plants-with-coords.test.ts
@src/hooks/use-context-counts.test.ts
@src/hooks/use-auth.ts
@src/pages/MapPage.tsx
@src/pages/RegarPage.tsx
@src/pages/History.tsx
@src/index.css
@tailwind.config.ts

<interfaces>
DEPENDS_ON (Wave 1):
- Sub-phase 3-01: ruta `/regar` registrada en App.tsx con `<AppLayout>` (NO fullBleed); `RegarPage.tsx` placeholder existe.

CONSUME (ya existentes — no se tocan):
```typescript
// src/hooks/use-auth.ts
export function useAuth(): { user: User | null; isLoading: boolean; ... };

// src/integrations/supabase/client.ts
export const supabase: SupabaseClient;

// Schema actual de plant_searches (sub-phase 3-02 NO modifica el schema):
// - id (uuid PK)
// - name (string, "Common (Scientific)" format)
// - image_url (string)
// - context (string, 'home' | 'wild' | 'unclassified' | null)
// - watering_interval_days (int | null, CHECK 1-60)
// - created_at (timestamptz)
// - description (string)
// - lat, lng (number | null)
// NOTA: last_watered_at NO existe todavía — sub-phase 3-03 lo añade.
```

CREATES (esta sub-phase):
```typescript
// src/hooks/use-home-plants.ts
export interface HomePlant {
  id: string;
  name: string;
  imageUrl: string;
  createdAt: string;
  wateringIntervalDays: number | null;
  // last_watered_at NOT here yet — sub-phase 3-03 adds.
}
export interface UseHomePlantsReturn {
  plants: HomePlant[];
  isLoading: boolean;
  refetch: () => Promise<void>;
}
export function useHomePlants(): UseHomePlantsReturn;

// src/components/PlantWateringCard.tsx
export interface PlantWateringCardProps {
  plant: HomePlant;
  // Sub-phase 3-03 añade onWater?: (id: string) => void; en esta sub-phase el botón es estático.
}
export function PlantWateringCard(props: PlantWateringCardProps): JSX.Element;
```

PATTERNS to mirror verbatim:
- `useWildPlantsWithCoords` (src/hooks/use-wild-plants-with-coords.ts:48-109) — copy SHAPE; change filter `wild` → `home`; drop the lat/lng filter; add `watering_interval_days` to select; map to `HomePlant` type.
- Listener pattern: `useContextCounts.ts:122-132` — addEventListener/removeEventListener of `mp:plant-context-updated` with refetch handler.
- Card structure: `History.tsx:163-222` (sticker shadow + photo 80px square + name + meta) — adapt to 64×64 photo per design and add full-width button below.
- Sticker shadow idiom: `boxShadow: "var(--shadow-press)"` + `border-2 border-foreground bg-card rounded-2xl`.
- Hook test pattern: `use-wild-plants-with-coords.test.ts` (full file) — uses `makeChain` helper for thenable Supabase mock + `vi.mock("@/hooks/use-auth")`.

LOCKED CONSTRAINTS:
- D-01 / D-02: layout pantalla normal (no fullBleed); título "¿Toca regar?" + subheader "Tus plantas casa" (heredados del placeholder 3-01).
- D-15 (TONE): status copy verbatim — "Próximo riego en X días", "Toca regar hoy", "Lleva N días esperándote", "Pendiente primera vez · Toca regar para empezar". No alarmismo.
- D-16: frecuencia muestra "Cada N días" o "Sin frecuencia" sin atribución IA/usuario.
- D-10: botón state-dependent — "Regada" si X>0, "Regar" si X≤0. En sub-phase 3-02 todas las plantas son pending-first-time → botón siempre "Regar".
- Researcher risk #5: token `--soft-warn` debe ser HSL `38 80% 60%` y vivir en `:root`. Añadir también `--soft-warn-bg: 48 85% 92%` para el fondo del badge.
- NO TDD en este plan (no es business logic puro — es view + data fetch). El hook tiene su test, pero no se hace red→green→refactor — se escribe directo siguiendo el patrón.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Crear hook `useHomePlants` + test unit</name>
  <files>src/hooks/use-home-plants.ts, src/hooks/use-home-plants.test.ts</files>
  <read_first>
    - src/hooks/use-wild-plants-with-coords.ts (TODO el archivo — el shape se copia verbatim, solo cambian filtros y campos)
    - src/hooks/use-wild-plants-with-coords.test.ts (analog del test — copy structure)
    - src/hooks/use-context-counts.ts líneas 122-132 (listener pattern de `mp:plant-context-updated`)
    - .planning/phases/03-calendar-v0/03-RESEARCH.md sub-phase 3-02 file list (use-home-plants.ts)
    - .planning/phases/03-calendar-v0/03-RESEARCH.md Risk #3 (PlantResult tipo no incluye last_watered_at; mantener tipo local HomePlant en el hook)
  </read_first>
  <action>
    **Sub-task 1.1 — Crear `src/hooks/use-home-plants.ts`:**

    Contenido exacto:

    ```typescript
    import { useCallback, useEffect, useRef, useState } from "react";
    import { supabase } from "@/integrations/supabase/client";
    import { useAuth } from "@/hooks/use-auth";

    /**
     * Planta de casa: una fila de `plant_searches` con `context = 'home'`.
     *
     * Phase 3 sub-phase 3-02: incluye `wateringIntervalDays` (Phase 2 ya
     * añadió la columna). NO incluye `lastWateredAt` — la columna no existe
     * todavía en el schema; sub-phase 3-03 la añade vía migración y este
     * hook se extenderá entonces.
     *
     * `name` mantiene el formato legacy "Common (Scientific)" — el card
     * lo splittea con la misma helper que `PlantMapSheet` (o un re-uso
     * via import en sub-phase 3-04 cuando se haga el detail link).
     */
    export interface HomePlant {
      id: string;
      name: string;
      imageUrl: string;
      createdAt: string;
      wateringIntervalDays: number | null;
    }

    export interface UseHomePlantsReturn {
      plants: HomePlant[];
      isLoading: boolean;
      refetch: () => Promise<void>;
    }

    /**
     * Returns home plants (context='home') of the current user.
     *
     * For anonymous users returns `plants: []` without touching Supabase
     * (los anon no tienen context clasificado — RLS los bloquearía igualmente).
     *
     * Reactivity (D-05): listens to `mp:plant-context-updated` (dispatched
     * by useClassifyPlant tras classify/revert) AND `mp:pending-classification-resolved`
     * (dispatched by AuthContext en el flujo anon→auth claim) — patrón verbatim
     * de useContextCounts líneas 122-132. Sub-phase 3-03 añadirá un evento
     * adicional `mp:plant-watered` para refetch tras log de riego.
     *
     * Ordenación: por defecto `created_at desc` (más reciente primero). En
     * sub-phase 3-03 la PÁGINA aplicará un sort secundario por urgencia
     * (X menor primero) que requiere `lastWateredAt` que aún no existe.
     */
    export function useHomePlants(): UseHomePlantsReturn {
      const { user } = useAuth();
      const [plants, setPlants] = useState<HomePlant[]>([]);
      const [isLoading, setIsLoading] = useState(true);
      const mountedRef = useRef(true);
      useEffect(() => {
        mountedRef.current = true;
        return () => {
          mountedRef.current = false;
        };
      }, []);

      const load = useCallback(async () => {
        if (!user) {
          if (!mountedRef.current) return;
          setPlants([]);
          setIsLoading(false);
          return;
        }
        setIsLoading(true);
        const { data, error } = await supabase
          .from("plant_searches")
          .select("id, name, image_url, watering_interval_days, created_at")
          .eq("user_id", user.id)
          .eq("context", "home")
          .order("created_at", { ascending: false });

        if (!mountedRef.current) return;
        if (error) {
          console.error("Error fetching home plants:", error.message);
          setPlants([]);
          setIsLoading(false);
          return;
        }
        setPlants(
          (data ?? []).map((row) => ({
            id: row.id,
            name: row.name,
            imageUrl: row.image_url,
            createdAt: row.created_at,
            wateringIntervalDays: row.watering_interval_days,
          })),
        );
        setIsLoading(false);
      }, [user]);

      useEffect(() => {
        load();
      }, [load]);

      // D-05 reactividad: refetch on classify/revert (Phase 02.1 + 03.1
      // patterns) o tras anon→auth claim (AuthContext patrón existente).
      useEffect(() => {
        const handler = () => {
          load();
        };
        window.addEventListener("mp:plant-context-updated", handler);
        window.addEventListener("mp:pending-classification-resolved", handler);
        return () => {
          window.removeEventListener("mp:plant-context-updated", handler);
          window.removeEventListener("mp:pending-classification-resolved", handler);
        };
      }, [load]);

      return { plants, isLoading, refetch: load };
    }
    ```

    **Sub-task 1.2 — Crear `src/hooks/use-home-plants.test.ts`:**

    Copiar el patrón `makeChain` verbatim de `src/hooks/use-wild-plants-with-coords.test.ts`. Cubrir:
    - Anon (user=null): retorna `plants: []`, no llama a supabase.
    - Auth: la query usa `.eq("user_id", ...)`, `.eq("context", "home")`, `.order("created_at", { ascending: false })`.
    - El mapping convierte `image_url` → `imageUrl`, `watering_interval_days` → `wateringIntervalDays`, `created_at` → `createdAt`.
    - Error de Supabase: setea `plants: []` y `isLoading=false`, console.error.
    - Listener `mp:plant-context-updated` dispatcheado → causa refetch (verificar increment de calls).

    Contenido del test (basado en `use-wild-plants-with-coords.test.ts`):

    ```typescript
    import { renderHook, waitFor, act } from "@testing-library/react";
    import { describe, it, expect, vi, beforeEach } from "vitest";
    import { useHomePlants } from "./use-home-plants";

    // Mock supabase client
    const makeChain = (result: { data: unknown; error: unknown }) => {
      const chain = {
        select: vi.fn(() => chain),
        eq: vi.fn(() => chain),
        order: vi.fn(() => Promise.resolve(result)),
      };
      return chain;
    };

    let mockSupabaseFrom: ReturnType<typeof vi.fn>;

    vi.mock("@/integrations/supabase/client", () => ({
      supabase: {
        from: (...args: unknown[]) => mockSupabaseFrom(...args),
      },
    }));

    let mockUser: { id: string } | null = { id: "user-test-001" };
    vi.mock("@/hooks/use-auth", () => ({
      useAuth: () => ({ user: mockUser, isLoading: false }),
    }));

    describe("useHomePlants", () => {
      beforeEach(() => {
        mockUser = { id: "user-test-001" };
        mockSupabaseFrom = vi.fn();
      });

      it("returns empty plants and skips supabase when user is null", async () => {
        mockUser = null;
        mockSupabaseFrom.mockReturnValue(makeChain({ data: [], error: null }));

        const { result } = renderHook(() => useHomePlants());

        await waitFor(() => {
          expect(result.current.isLoading).toBe(false);
        });
        expect(result.current.plants).toEqual([]);
        expect(mockSupabaseFrom).not.toHaveBeenCalled();
      });

      it("fetches home plants with the correct filters and maps rows", async () => {
        const chain = makeChain({
          data: [
            {
              id: "plant-1",
              name: "Monstera deliciosa (Monstera deliciosa)",
              image_url: "https://img/1.jpg",
              watering_interval_days: 7,
              created_at: "2026-05-10T10:00:00Z",
            },
            {
              id: "plant-2",
              name: "Pothos",
              image_url: "https://img/2.jpg",
              watering_interval_days: null,
              created_at: "2026-04-01T10:00:00Z",
            },
          ],
          error: null,
        });
        mockSupabaseFrom.mockReturnValue(chain);

        const { result } = renderHook(() => useHomePlants());

        await waitFor(() => {
          expect(result.current.isLoading).toBe(false);
        });
        expect(mockSupabaseFrom).toHaveBeenCalledWith("plant_searches");
        // chain.eq called with user_id and context=home
        expect(chain.eq).toHaveBeenCalledWith("user_id", "user-test-001");
        expect(chain.eq).toHaveBeenCalledWith("context", "home");
        expect(chain.order).toHaveBeenCalledWith("created_at", { ascending: false });
        expect(result.current.plants).toEqual([
          {
            id: "plant-1",
            name: "Monstera deliciosa (Monstera deliciosa)",
            imageUrl: "https://img/1.jpg",
            createdAt: "2026-05-10T10:00:00Z",
            wateringIntervalDays: 7,
          },
          {
            id: "plant-2",
            name: "Pothos",
            imageUrl: "https://img/2.jpg",
            createdAt: "2026-04-01T10:00:00Z",
            wateringIntervalDays: null,
          },
        ]);
      });

      it("handles supabase error: sets plants to empty and logs", async () => {
        const consoleSpy = vi.spyOn(console, "error").mockImplementation(() => {});
        mockSupabaseFrom.mockReturnValue(
          makeChain({ data: null, error: { message: "boom" } }),
        );

        const { result } = renderHook(() => useHomePlants());

        await waitFor(() => {
          expect(result.current.isLoading).toBe(false);
        });
        expect(result.current.plants).toEqual([]);
        expect(consoleSpy).toHaveBeenCalledWith(
          "Error fetching home plants:",
          "boom",
        );
        consoleSpy.mockRestore();
      });

      it("refetches when mp:plant-context-updated event fires", async () => {
        const chain = makeChain({ data: [], error: null });
        mockSupabaseFrom.mockReturnValue(chain);

        const { result } = renderHook(() => useHomePlants());

        await waitFor(() => {
          expect(result.current.isLoading).toBe(false);
        });
        const callsBefore = mockSupabaseFrom.mock.calls.length;

        act(() => {
          window.dispatchEvent(new CustomEvent("mp:plant-context-updated"));
        });

        await waitFor(() => {
          expect(mockSupabaseFrom.mock.calls.length).toBeGreaterThan(callsBefore);
        });
      });

      it("refetches when mp:pending-classification-resolved event fires", async () => {
        const chain = makeChain({ data: [], error: null });
        mockSupabaseFrom.mockReturnValue(chain);

        const { result } = renderHook(() => useHomePlants());

        await waitFor(() => {
          expect(result.current.isLoading).toBe(false);
        });
        const callsBefore = mockSupabaseFrom.mock.calls.length;

        act(() => {
          window.dispatchEvent(new CustomEvent("mp:pending-classification-resolved"));
        });

        await waitFor(() => {
          expect(mockSupabaseFrom.mock.calls.length).toBeGreaterThan(callsBefore);
        });
      });
    });
    ```

    NOTAS:
    - Si el helper `makeChain` real en `use-wild-plants-with-coords.test.ts` tiene un shape diferente (.not, .or, etc), AJUSTAR al patrón existente. La idea es que `chain.order(...)` retorne la Promise — read el test analog primero y copiar exactamente. Para esta query simple, .eq().eq().order() basta (no se usa .not ni .or).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('src/hooks/use-home-plants.ts','utf8');const t=fs.readFileSync('src/hooks/use-home-plants.test.ts','utf8');const checks={hookExportsUseHomePlants:h.includes('export function useHomePlants'),hookExportsHomePlant:h.includes('export interface HomePlant'),hookFilterHome:h.includes('.eq(\"context\", \"home\")'),hookSelectsWateringDays:h.includes('watering_interval_days'),hookListensContextUpdated:h.includes('mp:plant-context-updated'),hookListensPendingResolved:h.includes('mp:pending-classification-resolved'),hookMountedRef:h.includes('mountedRef'),testImportsHook:t.includes('import { useHomePlants }'),testCoversAnon:t.includes('when user is null'),testCoversAuthFetch:t.includes('fetches home plants'),testCoversError:t.includes('supabase error'),testCoversListener:t.includes('mp:plant-context-updated')};const fails=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);if(fails.length){console.error('FAIL:',fails);process.exit(1);}console.log('OK useHomePlants + test');"</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/use-home-plants.ts` exporta `useHomePlants`, `HomePlant`, `UseHomePlantsReturn`
    - El hook usa `mountedRef` (patrón verbatim de useWildPlantsWithCoords)
    - El hook filtra `.eq("context", "home")` (no wild, no lat/lng filter)
    - El SELECT incluye `id, name, image_url, watering_interval_days, created_at`
    - El mapping convierte snake_case → camelCase (`image_url` → `imageUrl`, etc)
    - Listener añadido para `mp:plant-context-updated` AND `mp:pending-classification-resolved`
    - `src/hooks/use-home-plants.test.ts` cubre 5 casos: anon, auth-fetch, error, listener-context-updated, listener-pending-resolved
    - `npm test -- --run use-home-plants` pasa exit 0
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>Hook useHomePlants + tests creados, patrón verbatim del wild plants hook, listener reactivo cableado.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Añadir tokens `--soft-warn` y `--soft-warn-bg` a `src/index.css` + `tailwind.config.ts`</name>
  <files>src/index.css, tailwind.config.ts</files>
  <read_first>
    - src/index.css (CURRENT — entender el bloque `:root {}` lines 8-51, donde están los demás tokens)
    - tailwind.config.ts (CURRENT — entender el bloque `extend.colors` lines 32-75)
    - .planning/phases/03-calendar-v0/03-CONTEXT.md D-15 (HSL ~38 80% 60%)
    - .planning/phases/03-calendar-v0/03-RESEARCH.md Risk #5 (recomendación añadir token nuevo, no reusar `--accent`)
  </read_first>
  <action>
    **Sub-task 2.1 — Modificar `src/index.css`:**

    Añadir dos tokens al bloque `:root { ... }` después de la línea de `--accent-foreground` (linea 28) y antes de `--destructive` (linea 30). El bloque resultante incluye estas dos líneas NUEVAS:

    ```css
        --accent: 16 76% 61%;
        --accent-foreground: 0 0% 100%;

        /* Phase 3 (D-15): amarillo cálido suave para urgencia de riego.
           NO usar para destructive/error — solo para countdown urgente/atrasado.
           Background suave para badges; foreground saturado para texto/borde. */
        --soft-warn: 38 80% 60%;
        --soft-warn-bg: 48 85% 92%;

        --destructive: 16 76% 61%;
    ```

    El orden de los tokens importa para legibilidad pero no funcionalmente — el agrupador clave es que estén dentro de `:root {}` antes del cierre `}` línea 51.

    **Sub-task 2.2 — Modificar `tailwind.config.ts`:**

    Añadir dos entradas a `theme.extend.colors` (bloque líneas 32-75). Mantener formato existente (las claves usan camelCase para multi-word tokens). Insertar después de `accent` (línea 57) y antes de `popover` (línea 58):

    ```typescript
        accent: {
          DEFAULT: "hsl(var(--accent))",
          foreground: "hsl(var(--accent-foreground))",
        },
        // Phase 3 (D-15): soft warm yellow para urgencia de riego.
        // Usado por PlantWateringCard badge "X d" cuando X <= 0.
        softWarn: "hsl(var(--soft-warn))",
        softWarnBg: "hsl(var(--soft-warn-bg))",
        popover: {
    ```

    Esto expone las utilidades `bg-soft-warn`, `bg-soft-warn-bg`, `text-soft-warn`, `border-soft-warn`, `text-soft-warn-bg`, etc. en Tailwind. (Tailwind transforma `softWarn` → `soft-warn` automáticamente para las utility classes per JIT.)

    **NO MODIFICAR:**
    - Cualquier otro token de `:root`
    - El bloque `.plant-map-pin` y `.leaflet-control-attribution` overrides (líneas 73-112 de index.css)
    - Otras entries de `extend.colors`
    - El bloque `keyframes` ni `animation` (sub-phase 3-03 añade `flash-success`)
    - El `safeArea` plugin
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const css=fs.readFileSync('src/index.css','utf8');const tw=fs.readFileSync('tailwind.config.ts','utf8');const checks={cssHasSoftWarn:/--soft-warn:\s*38\s+80%\s+60%/.test(css),cssHasSoftWarnBg:/--soft-warn-bg:\s*48\s+85%\s+92%/.test(css),cssTokensInRoot:css.indexOf('--soft-warn')<css.indexOf('}',css.indexOf(':root')),twHasSoftWarn:tw.includes('softWarn:')||tw.includes('softWarn: \"hsl(var(--soft-warn))\"'),twHasSoftWarnBg:tw.includes('softWarnBg:'),twPreservesAccent:tw.includes('accent: {'),twPreservesSecondary:tw.includes('secondary: {')};const fails=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);if(fails.length){console.error('FAIL:',fails);process.exit(1);}console.log('OK soft-warn tokens');"</automated>
  </verify>
  <acceptance_criteria>
    - `src/index.css` contiene `--soft-warn: 38 80% 60%;` dentro del bloque `:root {}`
    - `src/index.css` contiene `--soft-warn-bg: 48 85% 92%;` dentro del bloque `:root {}`
    - Otros tokens (`--primary`, `--accent`, `--background`, etc) intactos
    - `tailwind.config.ts` contiene `softWarn: "hsl(var(--soft-warn))"` en `theme.extend.colors`
    - `tailwind.config.ts` contiene `softWarnBg: "hsl(var(--soft-warn-bg))"` en `theme.extend.colors`
    - Las demás entries de colors (primary, secondary, accent, popover, card, sidebar) intactas
    - `npm run build` exit 0 (verifica que Tailwind procesa la config sin error)
  </acceptance_criteria>
  <done>Tokens --soft-warn y --soft-warn-bg añadidos a index.css y expuestos como utilidades Tailwind.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Crear `PlantWateringCard.tsx` (sticker card foto + nombre + frecuencia + badge + status + botón estático)</name>
  <files>src/components/PlantWateringCard.tsx</files>
  <read_first>
    - src/pages/History.tsx líneas 163-222 (analog card structure: sticker shadow, border, photo, content column)
    - src/components/ui/button.tsx líneas 14-22 (variants `hero` y `outline` — botón full-width usa `hero`)
    - .planning/phases/03-calendar-v0/03-CONTEXT.md D-10, D-11, D-14, D-15, D-16
    - src/components/PlantMapSheet.tsx líneas 23-39 (splitNameField helper — REUSE patrón pero NO importar para evitar coupling; copiar la función local)
    - tailwind.config.ts (verificar que softWarn y softWarnBg ya están — task 2 los añadió)
  </read_first>
  <action>
    Crear `src/components/PlantWateringCard.tsx` — el componente que renderiza cada planta casa en `/regar`.

    En esta sub-phase TODA planta aparece como "Pendiente primera vez" (D-14) porque `last_watered_at` no existe en la DB. Sub-phase 3-03 introduce el countdown real. El componente ya tiene los slots/conditionals preparados para todos los estados de D-08 (`normal | urgent | overdue | pending-first`) — solo que en esta phase solo el branch `pending-first` activa.

    Contenido exacto:

    ```tsx
    import type { HomePlant } from "@/hooks/use-home-plants";

    /**
     * Splittea "Common (Scientific)" en sus dos partes.
     * Patrón copiado verbatim de PlantMapSheet (no importado para evitar coupling).
     */
    function splitNameField(name: string): {
      commonName: string;
      scientificName: string | null;
    } {
      const match = name.match(/^(.+?)\s*\(([^)]+)\)$/);
      if (match) {
        return {
          commonName: match[1].trim(),
          scientificName: match[2].trim(),
        };
      }
      return { commonName: name, scientificName: null };
    }

    export interface PlantWateringCardProps {
      plant: HomePlant;
      /**
       * Sub-phase 3-02: no-op opcional. Sub-phase 3-03 lo wirea al hook
       * useLogWatering. Si no se pasa, el botón hace console.log informativo.
       */
      onWater?: (plantId: string) => void;
      /**
       * Sub-phase 3-04: tap on frecuencia abrirá un picker bottom-sheet.
       * En 3-02 no se renderiza interactivo todavía — el texto está pero
       * sin onClick.
       */
      onEditFrequency?: (plantId: string) => void;
    }

    /**
     * Card del listado /regar — representa una planta casa con su estado de riego.
     *
     * Phase 3 sub-phase 3-02: estructura visual completa, pero TODAS las
     * plantas se renderizan en estado "Pendiente primera vez" porque el
     * dato `lastWateredAt` no existe todavía. Sub-phase 3-03 introduce
     * `computeDaysRemaining()` y los 4 estados D-08.
     *
     * Sticker card visual (CLAUDE.md design system):
     * - border-2 border-foreground rounded-2xl bg-card
     * - boxShadow var(--shadow-press)
     * - foto 64×64 rounded-xl border-2
     * - nombre common (font-display semibold)
     * - frecuencia "Cada N días" o "Sin frecuencia" (D-16, sin atribución)
     * - badge "X d" right-aligned (en 3-02: "—" gris)
     * - status text empático (D-15)
     * - botón full-width abajo con copy state-dependent (D-10):
     *   "Regada" si X>0, "Regar" si X<=0 o pending-first
     *
     * D-15 tono suave: amarillo cálido (soft-warn) NO rojo.
     */
    export function PlantWateringCard({
      plant,
      onWater,
      onEditFrequency,
    }: PlantWateringCardProps) {
      const { commonName } = splitNameField(plant.name);
      const intervalLabel =
        plant.wateringIntervalDays !== null
          ? `Cada ${plant.wateringIntervalDays} días`
          : "Sin frecuencia";

      // Sub-phase 3-02: sin lastWateredAt, todas las plantas son pending-first-time.
      // Sub-phase 3-03 reemplaza esta lógica con computeDaysRemaining().
      const status: "normal" | "urgent" | "overdue" | "pending-first" = "pending-first";

      // Per D-15 status copy:
      let statusText: string;
      let badgeText: string;
      let badgeIsWarn: boolean;
      let buttonLabel: string;

      switch (status) {
        case "pending-first":
          statusText = "Pendiente primera vez · Toca regar para empezar";
          badgeText = "—";
          badgeIsWarn = false;
          buttonLabel = "Regar";
          break;
        // Sub-phase 3-03 cablea estos otros branches:
        // case "normal": ...
        // case "urgent": ...
        // case "overdue": ...
        default:
          statusText = "";
          badgeText = "—";
          badgeIsWarn = false;
          buttonLabel = "Regar";
      }

      const handleWater = () => {
        if (onWater) {
          onWater(plant.id);
        } else {
          // Sub-phase 3-02: botón estático. Sub-phase 3-03 wirea useLogWatering.
          console.log("[PlantWateringCard] water tap (no-op in 3-02):", plant.id);
        }
      };

      const handleEditFrequency = () => {
        if (onEditFrequency) {
          onEditFrequency(plant.id);
        }
        // Sub-phase 3-04 wirea el picker. En 3-02 el tap no hace nada.
      };

      return (
        <article
          className="flex flex-col gap-3 p-4 bg-card border-2 border-foreground rounded-2xl"
          style={{ boxShadow: "var(--shadow-press)" }}
          aria-label={`Planta: ${commonName}`}
        >
          <div className="flex items-start gap-3">
            <img
              src={plant.imageUrl}
              alt={commonName}
              loading="lazy"
              className="size-16 shrink-0 object-cover rounded-xl border-2 border-foreground"
            />
            <div className="flex-1 min-w-0 flex flex-col gap-1">
              <h2 className="font-display text-lg font-semibold text-foreground leading-tight">
                {commonName}
              </h2>
              <button
                type="button"
                onClick={handleEditFrequency}
                className="font-body text-sm text-muted-foreground text-left underline-offset-2 hover:underline focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring rounded"
                aria-label={`Frecuencia de riego: ${intervalLabel}. Pulsa para editar.`}
              >
                {intervalLabel}
              </button>
              <p className="font-body text-xs text-muted-foreground italic">
                {statusText}
              </p>
            </div>
            <span
              className={
                badgeIsWarn
                  ? "shrink-0 px-2 py-1 rounded-lg bg-soft-warn-bg border-2 border-foreground font-display text-sm font-bold text-foreground"
                  : "shrink-0 px-2 py-1 rounded-lg bg-muted border-2 border-foreground/30 font-display text-sm font-bold text-muted-foreground"
              }
              aria-label={`Estado: ${badgeText}`}
            >
              {badgeText}
            </span>
          </div>
          <button
            type="button"
            onClick={handleWater}
            className="w-full min-h-11 px-4 py-2.5 bg-primary text-primary-foreground border-2 border-foreground rounded-xl font-display text-base font-bold active:translate-x-[2px] active:translate-y-[2px] active:shadow-none focus-visible:outline-none focus-visible:ring-4 focus-visible:ring-ring focus-visible:ring-offset-2 transition-shadow"
            style={{ boxShadow: "var(--shadow-press)" }}
            aria-label={`${buttonLabel} ${commonName}`}
          >
            {buttonLabel}
          </button>
        </article>
      );
    }
    ```

    Decisiones implementadas:
    - **D-10**: copy del botón state-dependent — en 3-02 siempre "Regar" (pending-first); 3-03 activa "Regada" para X>0
    - **D-11**: botón ancho completo en la parte inferior
    - **D-14**: estado "Pendiente primera vez" con copy verbatim "Pendiente primera vez · Toca regar para empezar"
    - **D-15**: tono suave — el badge usa `bg-soft-warn-bg` (no rojo) cuando `badgeIsWarn=true`; en pending-first el badge es gris neutro con "—"
    - **D-16**: frecuencia "Cada N días" o "Sin frecuencia" — sin atribución IA/usuario
    - **A11y**: `<article>` con aria-label; imagen tiene alt; badge tiene aria-label; botón frecuencia tiene aria-label con instrucción
    - **Touch target**: botón principal `min-h-11` (44px); botón frecuencia es texto-link inline (~28px alto, acceptable para non-primary action)
    - **Compositional readiness**: props `onWater` y `onEditFrequency` ya están en la interface — 3-03 wirea `onWater`, 3-04 wirea `onEditFrequency`. Sin breaking changes.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/components/PlantWateringCard.tsx','utf8');const checks={hasExport:s.includes('export function PlantWateringCard'),hasInterface:s.includes('export interface PlantWateringCardProps'),hasSplitName:s.includes('splitNameField'),hasIntervalLabelCadaN:s.includes('`Cada ${plant.wateringIntervalDays} días`'),hasSinFrecuencia:s.includes('Sin frecuencia'),hasPendingFirstCopy:s.includes('Pendiente primera vez · Toca regar para empezar'),hasButtonRegar:s.includes('buttonLabel = \"Regar\"'),hasShadowPress:s.includes('var(--shadow-press)'),hasSoftWarnBg:s.includes('bg-soft-warn-bg'),hasFontDisplay:s.includes('font-display'),hasMinH11:s.includes('min-h-11'),hasAriaLabels:s.includes('aria-label='),hasNoOnWaterCall:!s.includes('onWater(plant.id)')||s.includes('if (onWater)'),hasNoLastWateredAt:!s.includes('lastWateredAt'),hasNoImportLastWatered:!s.includes('last_watered_at')};const fails=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);if(fails.length){console.error('FAIL:',fails);process.exit(1);}console.log('OK PlantWateringCard');"</automated>
  </verify>
  <acceptance_criteria>
    - El archivo existe y exporta `PlantWateringCard` + `PlantWateringCardProps`
    - El componente acepta `plant: HomePlant`, opcional `onWater`, opcional `onEditFrequency`
    - Sticker shadow: `style={{ boxShadow: "var(--shadow-press)" }}` + `border-2 border-foreground rounded-2xl bg-card`
    - Foto `size-16` (= 64×64) con `rounded-xl border-2 border-foreground`
    - Frecuencia text usa `"Cada N días"` o `"Sin frecuencia"` SIN atribución (D-16)
    - Status text usa `"Pendiente primera vez · Toca regar para empezar"` (D-14)
    - Badge muestra `"—"` con styling gris/neutro (`bg-muted`) cuando pending-first
    - Cuando `badgeIsWarn=true`, el badge usa `bg-soft-warn-bg` (preparado para 3-03)
    - Botón full-width con `min-h-11` (44px touch target)
    - Botón label `"Regar"` (D-10 — en pending-first siempre "Regar")
    - El handler `handleWater` invoca `onWater(plant.id)` si está presente, si no console.log
    - Imports: solo `import type { HomePlant } from "@/hooks/use-home-plants"` (no otros)
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>PlantWateringCard renderiza el sticker card con todos los slots; pending-first es el único estado activo en 3-02.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Reemplazar `RegarPage.tsx` placeholder con la lista real (useHomePlants + cards + redirect-on-empty)</name>
  <files>src/pages/RegarPage.tsx</files>
  <read_first>
    - src/pages/RegarPage.tsx (CURRENT — placeholder de 3-01)
    - src/pages/MapPage.tsx líneas 35-76 (analog: useAuth + plantsLoading guard + redirect-on-empty)
    - .planning/phases/03-calendar-v0/03-CONTEXT.md Success Criterion #3, #8
    - .planning/phases/03-calendar-v0/03-RESEARCH.md sub-phase 3-02 file list (RegarPage)
  </read_first>
  <action>
    Reemplazar el contenido COMPLETO de `src/pages/RegarPage.tsx` con la versión que incluye la lista real. Mantiene el header de 3-01 (título "¿Toca regar?" + subheader "Tus plantas casa") y añade:
    - `useHomePlants` para obtener la lista
    - Loading state (Leaf icon spinner como `RequireAuth` line 11-12)
    - Redirect a `/` si `!isLoading && plants.length === 0` (deep-link defensivo — la tab solo aparece con home_count>=1, pero alguien podría escribir la URL directo)
    - Render N `PlantWateringCard`

    Contenido exacto:

    ```tsx
    import { useEffect } from "react";
    import { Navigate } from "react-router-dom";
    import { Leaf } from "lucide-react";
    import { useAuth } from "@/hooks/use-auth";
    import { useHomePlants } from "@/hooks/use-home-plants";
    import { PlantWateringCard } from "@/components/PlantWateringCard";

    /**
     * /regar — ¿Toca regar? — Calendario de riego.
     *
     * Phase 3 sub-phase 3-02: lista estática de plantas casa. Todas las
     * plantas aparecen en estado "Pendiente primera vez" porque la columna
     * `last_watered_at` no existe todavía. Sub-phase 3-03 añade la columna,
     * la lógica de countdown, y el botón Regar/Regada funcional.
     *
     * D-15 tono suave: header empático "Tus plantas casa" (no "Lista de tareas").
     *
     * Redirect-on-empty defensivo: la tab Regar solo aparece con home_count>=1
     * (sub-phase 3-01), pero un deep-link directo a /regar sin plantas debe
     * redirigir silenciosamente a `/` — mismo patrón que MapPage.
     */
    export default function RegarPage() {
      const { user, isLoading: authLoading } = useAuth();
      const { plants, isLoading: plantsLoading } = useHomePlants();
      // Wait for: (a) auth resolved, (b) user known, (c) home-plants query run at
      // least once. Sin (c) hay un render-window post-mount donde plantsLoading=false
      // (stale del no-user gate) y plants=[] (stale) que triggerearía un redirect
      // prematuro. Patrón verbatim de MapPage.tsx:36-43.
      const isLoading = authLoading || (!!user && plantsLoading);

      useEffect(() => {
        document.title = "¿Toca regar? · Mi Plantita Feliz";
      }, []);

      if (isLoading) {
        return (
          <div
            className="flex items-center justify-center py-20"
            aria-busy="true"
            aria-label="Cargando tus plantas"
          >
            <Leaf className="size-12 text-primary animate-pulse-slow" strokeWidth={1.2} />
          </div>
        );
      }

      // Defensive: tab solo aparece con home_count>=1 (sub-phase 3-01), pero
      // un deep-link directo o un usuario que perdió todas sus plantas debería
      // redirigir silenciosamente a home. NO mostrar empty state copy en /regar
      // (la tab no debería estar visible si esto pasa).
      if (plants.length === 0) {
        return <Navigate to="/" replace />;
      }

      return (
        <div className="px-6 py-8 pb-24">
          <header className="mb-6 flex flex-col gap-1">
            <h1 className="font-display text-2xl font-bold text-foreground">
              ¿Toca regar?
            </h1>
            <p className="font-body text-base text-muted-foreground">
              Tus plantas casa
            </p>
          </header>
          <ul className="flex flex-col gap-3" aria-label="Lista de plantas de casa">
            {plants.map((plant) => (
              <li key={plant.id}>
                <PlantWateringCard plant={plant} />
              </li>
            ))}
          </ul>
        </div>
      );
    }
    ```

    Decisiones implementadas:
    - **D-15**: header empático mantenido del placeholder
    - **MapPage parity**: combined loading state (`authLoading || (!!user && plantsLoading)`) idéntico
    - **Defensive redirect-on-empty**: misma estrategia que MapPage
    - **A11y**: `<ul>` con aria-label; cada card en `<li>` para semántica de lista; loading state con `aria-busy`
    - **NO TRACKING todavía**: el evento `calendar_opened` se cablea en sub-phase 3-05 (PostHog)

    NOTA sobre ordenación: En esta sub-phase el orden viene del hook = `created_at desc`. Sub-phase 3-03 introducirá un sort cliente-side por urgencia (atrasadas → hoy → próximas → pending-first). La lista visual será correcta en 3-02 (todas son pending-first → orden por created_at es razonable; los users acaban de clasificar como casa por primera vez recientemente).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/pages/RegarPage.tsx','utf8');const checks={hasUseHomePlantsImport:s.includes('import { useHomePlants }'),hasUseAuthImport:s.includes('import { useAuth }'),hasPlantWateringCardImport:s.includes('import { PlantWateringCard }'),hasNavigate:s.includes('<Navigate to=\"/\"'),hasLoadingState:s.includes('animate-pulse-slow'),hasIsLoadingCombined:s.includes('authLoading || (!!user && plantsLoading)'),hasEmptyRedirect:s.includes('plants.length === 0'),hasDocTitle:s.includes('document.title'),hasHeaderH1:s.includes('¿Toca regar?'),hasSubheader:s.includes('Tus plantas casa'),hasListMap:s.includes('plants.map'),hasListWithLi:s.includes('<li key={plant.id}>')};const fails=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);if(fails.length){console.error('FAIL:',fails);process.exit(1);}console.log('OK RegarPage real list');"</automated>
  </verify>
  <acceptance_criteria>
    - El archivo `src/pages/RegarPage.tsx` reemplazado (no añadido — sobrescribe el placeholder)
    - Importa `useAuth`, `useHomePlants`, `PlantWateringCard`, `Navigate`, `Leaf`, `useEffect`
    - Combined loading state: `authLoading || (!!user && plantsLoading)` (patrón verbatim MapPage)
    - Loading UI: Leaf icon con `animate-pulse-slow` + `aria-busy="true"`
    - Empty branch: `if (plants.length === 0) return <Navigate to="/" replace />`
    - Render: `<ul>` con aria-label + `<li>` per planta + `<PlantWateringCard plant={plant} />`
    - Header mantenido del 3-01: h1 "¿Toca regar?" + subheader "Tus plantas casa"
    - `document.title` mantenido: `"¿Toca regar? · Mi Plantita Feliz"`
    - NO contiene `track(...)` (sub-phase 3-05 lo añade)
    - `npx tsc --noEmit` exit 0
    - `npm run build` exit 0
  </acceptance_criteria>
  <done>RegarPage renderiza la lista real con loading + redirect-on-empty + cards estáticas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Supabase REST | `useHomePlants` hace SELECT a `plant_searches` filtrada por `user_id`. RLS gate ya existente (`auth.uid() = user_id`). |
| client-internal | CustomEvent `mp:plant-context-updated` es same-origin; ya cableado por Phase 02.1 y 03.1. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Information disclosure | `useHomePlants` SELECT — un usuario rogue podría intentar ver plantas de otro user | mitigate | RLS policy existing (`plant_searches` con `user_id = auth.uid()`) gates la query. El filtro adicional `.eq("user_id", user.id)` es belt-and-suspenders cliente. |
| T-03-07 | XSS | `PlantWateringCard` renderiza `plant.name` (data del usuario) | mitigate | React escapa por defecto. El nombre viene de la IA o del usuario (en futuras phases) — sin sanitizado explícito. Si en v1+ se introduce apodos editables, considerar adicional. |
| T-03-08 | DoS | Lista renderizada sin limit ni virtualización | accept | Plantas casa por usuario: <20 típico, <50 caso heavy. Render directo sin perf issues. Si crece en v1+, considerar `react-window` (deferido). |
| T-03-09 | Tampering | `--soft-warn` token podría ser sobrescrito por CSS injection rogue | accept | Same-origin scripts ya tienen acceso arbitrario al DOM. Riesgo no incremental. |
</threat_model>

<verification>
- `src/hooks/use-home-plants.ts` y `use-home-plants.test.ts` existen.
- `src/components/PlantWateringCard.tsx` existe.
- `src/pages/RegarPage.tsx` reemplazado con la lista real.
- `src/index.css` tiene `--soft-warn` y `--soft-warn-bg` en `:root`.
- `tailwind.config.ts` expone `softWarn` y `softWarnBg`.
- `npx tsc --noEmit` exit 0.
- `npm run build` exit 0.
- `npm test -- --run use-home-plants` pasa exit 0.
- Smoke manual: con usuario logado + ≥1 planta casa, navegar a `/regar` muestra la lista con N cards. Cada card muestra "Pendiente primera vez · Toca regar para empezar". Click en botón "Regar" muestra console.log informativo (no-op). Click en "Cada N días" no hace nada (sub-phase 3-04). Sin plantas casa, `/regar` redirige a `/`.
- Cross-platform: layout normal heredado de AppLayout — mobile-first; `px-6 py-8 pb-24` correcto para BottomTabBar.
- A11y: lista usa `<ul>` con aria-label; cards son `<article>` con aria-label per planta; badge tiene aria-label; loading state aria-busy.
</verification>

<success_criteria>
- Hook `useHomePlants` retorna home plants filtradas + reactivo a `mp:plant-context-updated`.
- `PlantWateringCard` renderiza el sticker visual completo (foto + nombre + frecuencia + badge + status + botón).
- `RegarPage` muestra la lista, con loading y redirect-on-empty.
- Tokens `--soft-warn` y `--soft-warn-bg` disponibles para sub-phase 3-03 (estados urgent/overdue).
- TypeScript compila, build pasa, test del hook pasa.
- En esta sub-phase TODAS las plantas son "Pendiente primera vez" — comportamiento esperado dado que `last_watered_at` no existe en la DB todavía.
</success_criteria>

<output>
After completion, create `.planning/phases/03-calendar-v0/03-02-SUMMARY.md` documentando:
- El hook sigue el patrón verbatim de useWildPlantsWithCoords (mountedRef + listener mp:plant-context-updated)
- El componente está scaffolded para los 4 estados D-08 pero solo `pending-first` activa en 3-02
- Los props `onWater` y `onEditFrequency` son opcionales — wired en 3-03 y 3-04 respectivamente sin breaking changes
- Tokens `--soft-warn` se añadieron globalmente porque 3-03 los necesita; en 3-02 no se usan visualmente pero el shape Tailwind ya está expuesto
- La lista NO tiene ordenación por urgencia todavía (necesita `last_watered_at`) — sub-phase 3-03 introduce ese sort
</output>
