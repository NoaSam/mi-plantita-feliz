---
phase: 03-calendar-v0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/BottomTabBar.tsx
  - src/App.tsx
  - src/pages/Settings.tsx
  - src/components/UnclassifiedSection.tsx
  - src/components/HistoryRelocationNotice.tsx
  - src/pages/RegarPage.tsx
autonomous: true
requirements: [RIEG-01]
requirements_addressed: [RIEG-01]
tags: [phase-03, navigation, bottom-tab-bar, route-relocation, toast-notice]

must_haves:
  truths:
    - "El BottomTabBar muestra la tab '💧 Regar' (icono Droplet) cuando `user && home_count >= 1` (D-01, D-05)."
    - "La tab Regar aparece reactivamente tras clasificar la primera planta como casa, sin reload (mismo patrón que tab Mapa: useContextCounts + listener `mp:plant-context-updated`)."
    - "Para usuario anónimo, la tab Regar nunca se renderiza."
    - "La ruta `/mis-plantas` se mueve a `/ajustes/mis-plantas` (Opción A de D-03). La URL antigua `/mis-plantas` redirige con `<Navigate to=\"/ajustes/mis-plantas\" replace />` para mantener compatibilidad con bookmarks y deep-links."
    - "La pantalla `/ajustes` incluye un nuevo botón 'Mis plantas (casa + descubrimientos)' (auth-gated) que navega a `/ajustes/mis-plantas`."
    - "El primer acceso post-deploy a la app dispara un toast 'Hemos movido Mis plantas a Ajustes' durante 8s. Se muestra una sola vez por dispositivo gracias al flag `mp_seen_history_relocation_notice` en localStorage (D-04)."
    - "La ruta `/regar` está registrada en `src/App.tsx` con `<AppLayout>` (layout normal, NO `fullBleed` — researcher recommendation)."
    - "`RegarPage` placeholder existe pero solo renderiza el app bar / título 'Tus plantas casa' (sub-phase 3-02 fills it)."
    - "Los call sites de `/mis-plantas` con query params se actualizan: `UnclassifiedSection.tsx` ahora navega a `/ajustes/mis-plantas?context=unclassified`."
    - "Los call sites de login/signup redirects (App.tsx) y auth.service.ts emailRedirectTo se mantienen apuntando a `/mis-plantas` que ahora redirige internamente — no se cambian (la redirect chain funciona)."
  artifacts:
    - path: "src/components/BottomTabBar.tsx"
      provides: "Tab Regar condicional insertada entre Inicio y Mapa"
      contains: "Droplet"
    - path: "src/App.tsx"
      provides: "Rutas `/ajustes/mis-plantas` registrada + `/mis-plantas` redirige + `/regar` registrada + `<HistoryRelocationNotice />` montado"
      contains: "/ajustes/mis-plantas"
    - path: "src/pages/Settings.tsx"
      provides: "Sección 'Mi colección' con botón navegando a `/ajustes/mis-plantas`"
      contains: "Mis plantas (casa + descubrimientos)"
    - path: "src/components/UnclassifiedSection.tsx"
      provides: "Navega a `/ajustes/mis-plantas?context=unclassified` en handleViewAll"
      contains: "/ajustes/mis-plantas?context=unclassified"
    - path: "src/components/HistoryRelocationNotice.tsx"
      provides: "Componente lifecycle-only que dispara toast una vez por device"
      exports: ["default HistoryRelocationNotice"]
      min_lines: 15
    - path: "src/pages/RegarPage.tsx"
      provides: "Página placeholder con título 'Tus plantas casa' y `document.title`"
      exports: ["default RegarPage"]
      min_lines: 20
  key_links:
    - from: "src/components/BottomTabBar.tsx"
      to: "useContextCounts (existing) — home counter"
      via: "if (user && home >= 1) result.push({ path: '/regar', label: 'Regar', icon: Droplet })"
      pattern: "user && home >= 1"
    - from: "src/App.tsx"
      to: "src/pages/RegarPage.tsx"
      via: "Route path=/regar element AppLayout (NOT fullBleed)"
      pattern: "<Route path=\"/regar\""
    - from: "src/pages/Settings.tsx"
      to: "/ajustes/mis-plantas"
      via: "navigate('/ajustes/mis-plantas')"
      pattern: "/ajustes/mis-plantas"
    - from: "src/components/HistoryRelocationNotice.tsx"
      to: "Sonner toast"
      via: "toast(..., { duration: 8000 }) gated by localStorage flag"
      pattern: "mp_seen_history_relocation_notice"
---

<objective>
Restructurar la navegación inferior para hacer hueco al calendario:

1. Añadir la tab "💧 Regar" en `BottomTabBar.tsx` condicional a `user && home_count >= 1` (D-01, D-05). Mismo patrón que la tab Mapa existente — reusar `useContextCounts()` y el listener `mp:plant-context-updated` que ya está cableado.
2. Mover `/mis-plantas` → `/ajustes/mis-plantas` (D-03 Opción A). Mantener `/mis-plantas` como redirect para backwards compat.
3. Añadir entrada de navegación en `/ajustes` (sección "Mi colección" auth-gated) que abre `/ajustes/mis-plantas` con el componente `History` existente intacto.
4. Crear `HistoryRelocationNotice.tsx` que dispara un toast "📍 Hemos movido Mis plantas a Ajustes" durante 8s, una sola vez por dispositivo (D-04). Flag en localStorage `mp_seen_history_relocation_notice`.
5. Registrar la ruta `/regar` en `App.tsx` con `<AppLayout>` (layout normal — researcher recomendó NO fullBleed porque la lista es vertical scroll constrained).
6. Crear `RegarPage.tsx` placeholder que solo renderiza el título "Tus plantas casa" y `document.title`. La lógica de la lista llega en 3-02.

Purpose: Wave 1 que abre el slot de navegación necesario para que el resto de sub-phases puedan ir cargando contenido. Sin esta phase, la tab Regar no existe y la URL `/regar` no resuelve.

Output: 4 archivos modificados (BottomTabBar, App.tsx, Settings.tsx, UnclassifiedSection.tsx) + 2 archivos nuevos (HistoryRelocationNotice, RegarPage placeholder). ~80 LOC nuevas + ~20 LOC modificadas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-calendar-v0/03-CONTEXT.md
@.planning/phases/03-calendar-v0/03-RESEARCH.md
@CLAUDE.md
@src/components/BottomTabBar.tsx
@src/App.tsx
@src/pages/Settings.tsx
@src/components/UnclassifiedSection.tsx
@src/hooks/use-context-counts.ts
@src/hooks/use-classify-plant.ts
@src/layouts/AppLayout.tsx

<interfaces>
CONSUME (ya existentes — no se tocan):

```typescript
// src/hooks/use-context-counts.ts (lines 5-15)
export interface ContextCounts {
  home: number;
  wild_with_coords: number;
  wild_without_coords: number;
  unclassified: number;
}
export interface UseContextCountsReturn extends ContextCounts {
  isLoading: boolean;
  refetch: () => Promise<void>;
}
```

```typescript
// src/hooks/use-classify-plant.ts (line 14)
export const UNDO_WINDOW_MS = 5000;
// El hook ya dispatcha `mp:plant-context-updated` tras classify/revert (lines 27-33, 47-53).
// useContextCounts ya escucha este evento (lines 122-132). Cero refactor necesario.
```

```typescript
// src/components/ui/sonner.tsx exports
import { toast } from "sonner";
// El Toaster está montado globalmente en App.tsx:49.
```

PATTERNS to mirror:
- BottomTabBar tab insertion: lines 22-24 (wild_with_coords block) — copy the shape for home.
- Settings Section wrapper: lines 30-50 — reuse for the new "Mi colección" section.
- Sonner toast call: ver `src/pages/PlantDetail.tsx:15` (toast.error) — el mismo wrapper (no custom).
- localStorage one-time flag: ver `use-consent.ts` STORAGE_KEY pattern y `UnclassifiedSection.tsx:10` (module-level flag para per-session dedupe, aunque aquí necesitamos per-device persistence).

LOCKED CONSTRAINTS:
- Tab order final (D-01): Inicio · 💧 Regar (cond) · 🗺️ Mapa (cond) · ⚙️ Ajustes (cond auth). NOTA: `Mis plantas` deja de ser tab (D-03 — se mueve a sub-ruta de Ajustes).
- Slug `/regar` lockeado por D-02 (no `/calendario`, no `/mi-jardin`).
- localStorage key lockeado por D-04: `mp_seen_history_relocation_notice`.
- Toast duration: 8000ms (D-04).
- Toast copy verbatim: `"📍 Hemos movido Mis plantas a Ajustes"`.
- Settings section label verbatim (D-03): `"Mis plantas (casa + descubrimientos)"`.
- `/regar` placeholder: layout NORMAL (no fullBleed) — researcher recommendation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Añadir tab "Regar" al BottomTabBar (condicional a home_count >= 1)</name>
  <files>src/components/BottomTabBar.tsx</files>
  <read_first>
    - src/components/BottomTabBar.tsx (CURRENT — entender el bloque useMemo lines 20-30; el `home` ya está disponible desde useContextCounts)
    - src/hooks/use-context-counts.ts (verificar interface ContextCounts; `home` ya forma parte)
    - .planning/phases/03-calendar-v0/03-CONTEXT.md D-01, D-02, D-05
  </read_first>
  <action>
    Modificar `src/components/BottomTabBar.tsx`:

    **Cambio 1 — import:** Añadir `Droplet` al import de `lucide-react`:
    ```tsx
    import { Leaf, BookOpen, Settings, MapPin, Droplet } from "lucide-react";
    ```

    **Cambio 2 — destructuring:** Añadir `home` a la línea 18 (junto a `wild_with_coords`):
    ```tsx
    const { home, wild_with_coords } = useContextCounts();
    ```

    **Cambio 3 — useMemo:** Insertar el push de la tab Regar ENTRE el push de "Inicio" (line 21) y el bloque condicional de Mapa (line 22). Tab order final per D-01: Inicio → Regar (cond) → Mapa (cond) → Ajustes (cond auth). **NOTA CRÍTICA:** D-03 quita `Mis plantas` como tab del menú principal — esa línea (`result.push({ path: "/mis-plantas", ... })` en line 25) debe ELIMINARSE en este cambio.

    Bloque final del useMemo (líneas 20-30, contenido completo):

    ```tsx
    const tabs: Tab[] = useMemo(() => {
      const result: Tab[] = [{ path: "/", label: "Inicio", icon: Leaf }];
      // Phase 3 (D-01): tab Regar condicional a usuario logado con >= 1 planta casa.
      if (user && home >= 1) {
        result.push({ path: "/regar", label: "Regar", icon: Droplet });
      }
      // Phase 03.1: tab Mapa condicional a wild_with_coords >= 1.
      if (user && wild_with_coords >= 1) {
        result.push({ path: "/mapa", label: "Mapa", icon: MapPin });
      }
      // D-03: "Mis plantas" deja de ser tab principal — vive en /ajustes/mis-plantas.
      if (user) {
        result.push({ path: "/ajustes", label: "Ajustes", icon: Settings });
      }
      return result;
    }, [user, home, wild_with_coords]);
    ```

    **Cambio 4 — deps del useMemo:** Incluir `home` en las dependencies (línea 30 → `[user, home, wild_with_coords]`).

    **NO MODIFICAR:**
    - El wrapper `<nav>` ni el render JSX (líneas 32-59 actuales)
    - El import de `useMemo`, `useLocation`, `useNavigate`, `LucideIcon`, `useAuth`
    - La interface `Tab`
    - El default export
    - El `BookOpen` (sigue importado pero ya no se usa para Mis plantas — Settings.tsx lo usa en cambio 3 del Task 3)

    Decisiones implementadas:
    - **D-01**: tab order finalizado
    - **D-02**: slug `/regar` (literal)
    - **D-05**: reactividad heredada — `useContextCounts` ya re-fetcha vía `mp:plant-context-updated` (Phase 03.1)
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/components/BottomTabBar.tsx','utf8');const checks={hasDropletImport:/import\s*\{[^}]*Droplet[^}]*\}\s*from\s*['\"]lucide-react['\"]/.test(s),hasHomeDestructure:/const\s*\{[^}]*home[^}]*\}\s*=\s*useContextCounts/.test(s),hasRegarPath:s.includes('\"/regar\"'),hasRegarLabel:s.includes('\"Regar\"'),hasHomeGate:s.includes('user && home >= 1'),hasUserGate:s.includes('user && home >= 1'),hasNoMisPlantasTab:!/result\.push\(\{[^}]*\"\/mis-plantas\"/.test(s),hasMapaTabStill:s.includes('\"/mapa\"'),hasAjustesTabStill:s.includes('\"/ajustes\"'),hasUseMemoDeps:/\[user,\s*home,\s*wild_with_coords\]/.test(s),preservesNavClass:s.includes('fixed bottom-0 left-0 right-0 z-50 bg-background border-t-2 border-foreground/10 pb-safe')};const fails=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);if(fails.length){console.error('FAIL:',fails);process.exit(1);}console.log('OK BottomTabBar phase-3 tab');"</automated>
  </verify>
  <acceptance_criteria>
    - Import incluye `Droplet` desde `lucide-react`
    - El bloque `useMemo` extrae `home` de `useContextCounts()` (junto a `wild_with_coords`)
    - El array de tabs incluye el push `{ path: "/regar", label: "Regar", icon: Droplet }` gated por `user && home >= 1`
    - El push de `"/mis-plantas"` (línea 25 original) **ya no existe** — D-03 lo quita del menú principal
    - El push de Mapa (línea 22 original) **sigue intacto** — gated por `user && wild_with_coords >= 1`
    - El push de Ajustes sigue intacto, gated por `user`
    - Las deps del useMemo son exactamente `[user, home, wild_with_coords]`
    - El render JSX (líneas 32-59) queda byte-identical
    - `npx tsc --noEmit` exit 0
    - `npm run build` completa exit 0
  </acceptance_criteria>
  <done>BottomTabBar.tsx tiene la tab Regar condicional, la tab Mis plantas eliminada, render JSX intacto, TypeScript compila.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Registrar `/regar`, mover `/mis-plantas` a `/ajustes/mis-plantas` y montar HistoryRelocationNotice en App.tsx</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (CURRENT — entender el bloque de Routes lines 53-67 + PendingClassificationListener lines 31-44)
    - .planning/phases/03-calendar-v0/03-CONTEXT.md D-02, D-03, D-04
    - .planning/phases/03-calendar-v0/03-RESEARCH.md § Risk #10 (redirect compat para `/mis-plantas`)
  </read_first>
  <action>
    Modificar `src/App.tsx`:

    **Cambio 1 — añadir imports de RegarPage y HistoryRelocationNotice** (cerca de los demás page imports, líneas 8-17):

    ```tsx
    import RegarPage from "./pages/RegarPage";
    import HistoryRelocationNotice from "@/components/HistoryRelocationNotice";
    ```

    **Cambio 2 — montar `<HistoryRelocationNotice />`** como sibling de `<PendingClassificationListener />` (linea 52):

    ```tsx
    <PendingClassificationListener />
    <HistoryRelocationNotice />
    ```

    **Cambio 3 — modificar las Routes:**

    Estado actual de las Routes relevantes (líneas 53-67):
    ```tsx
    <Routes>
      <Route path="/" element={<AppLayout><Index /></AppLayout>} />
      <Route path="/mis-plantas" element={<AppLayout><History /></AppLayout>} />
      <Route path="/planta/:id" element={<AppLayout><PlantDetail /></AppLayout>} />
      <Route path="/ajustes" element={<AppLayout><SettingsPage /></AppLayout>} />
      <Route path="/mapa" element={<AppLayout fullBleed><MapPage /></AppLayout>} />
      <Route path="/login" element={<Navigate to="/mis-plantas" replace />} />
      <Route path="/signup" element={<Navigate to="/mis-plantas" replace />} />
      ...
    </Routes>
    ```

    Estado FINAL (cambios marcados):

    ```tsx
    <Routes>
      <Route path="/" element={<AppLayout><Index /></AppLayout>} />
      {/* D-03: /mis-plantas redirige a /ajustes/mis-plantas (backwards compat para bookmarks). */}
      <Route path="/mis-plantas" element={<Navigate to="/ajustes/mis-plantas" replace />} />
      <Route path="/ajustes/mis-plantas" element={<AppLayout><History /></AppLayout>} />
      <Route path="/planta/:id" element={<AppLayout><PlantDetail /></AppLayout>} />
      <Route path="/ajustes" element={<AppLayout><SettingsPage /></AppLayout>} />
      <Route path="/mapa" element={<AppLayout fullBleed><MapPage /></AppLayout>} />
      {/* Phase 3 (D-02): /regar — layout normal (no fullBleed); lista vertical scroll. */}
      <Route path="/regar" element={<AppLayout><RegarPage /></AppLayout>} />
      {/* /login y /signup mantienen el redirect a /mis-plantas que ahora redirige a /ajustes/mis-plantas (chain funciona). */}
      <Route path="/login" element={<Navigate to="/mis-plantas" replace />} />
      <Route path="/signup" element={<Navigate to="/mis-plantas" replace />} />
      {/* Legal pages — accessible without login */}
      <Route path="/privacidad" element={<PrivacyPolicy />} />
      <Route path="/cookies" element={<CookiePolicy />} />
      <Route path="/aviso-legal" element={<LegalNotice />} />
      <Route path="/terminos" element={<TermsOfService />} />
      <Route path="*" element={<NotFound />} />
    </Routes>
    ```

    **NO MODIFICAR:**
    - El BrowserRouter wrapper
    - El `<ScrollToTop />` y `<PendingClassificationListener />` (solo se le añade el sibling)
    - Los providers (AuthProvider, TooltipProvider)
    - El `<Toaster />` (sigue siendo el global de App.tsx:49)
    - El `<CookieConsentBanner />`
    - Los imports existentes de Index, History, PlantDetail, SettingsPage, MapPage, NotFound, legal pages

    Decisiones implementadas:
    - **D-02**: ruta `/regar` registrada con `AppLayout` (NO `fullBleed`)
    - **D-03 Opción A**: `/mis-plantas` redirige a `/ajustes/mis-plantas`; el componente `History` se monta tal cual en la nueva ruta (cero refactor del componente)
    - **D-04**: `<HistoryRelocationNotice />` montado para gestionar el toast aviso

    NOTA sobre login/signup redirects (per researcher risk #10): mantenemos `/mis-plantas` como target porque la cadena de redirect funciona (`/login` → `/mis-plantas` → `/ajustes/mis-plantas`). No tocar `auth.service.ts` ni `LoginPage.tsx`. Los tests E2E `e2e/auth.spec.ts` y `e2e/smoke.spec.ts` se actualizan en sub-phase 3-05 si es necesario (verificar que asumen el redirect chain).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/App.tsx','utf8');const checks={hasRegarPageImport:/import\s+RegarPage\s+from\s+['\"]\.\/pages\/RegarPage['\"]/.test(s),hasNoticeImport:/import\s+HistoryRelocationNotice\s+from\s+['\"]@\/components\/HistoryRelocationNotice['\"]/.test(s),hasNoticeMount:s.includes('<HistoryRelocationNotice />'),hasRegarRoute:/Route\s+path=[\"']\/regar[\"']\s+element=\{<AppLayout><RegarPage\s*\/><\/AppLayout>\}/.test(s),hasMisPlantasRedirect:/Route\s+path=[\"']\/mis-plantas[\"']\s+element=\{<Navigate\s+to=[\"']\/ajustes\/mis-plantas[\"']\s+replace/.test(s),hasNewMisPlantasRoute:/Route\s+path=[\"']\/ajustes\/mis-plantas[\"']\s+element=\{<AppLayout><History\s*\/><\/AppLayout>\}/.test(s),preservesIndex:/Route\s+path=[\"']\/[\"']\s+element=\{<AppLayout><Index/.test(s),preservesPlantDetail:s.includes('/planta/:id'),preservesMapa:s.includes('<Route path=\"/mapa\"'),preservesNotFound:s.includes('<Route path=\"*\"'),preservesAjustes:s.includes('<Route path=\"/ajustes\" element=')};const fails=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);if(fails.length){console.error('FAIL:',fails);process.exit(1);}console.log('OK App.tsx Phase-3 routes');"</automated>
  </verify>
  <acceptance_criteria>
    - `import RegarPage from "./pages/RegarPage"` presente
    - `import HistoryRelocationNotice from "@/components/HistoryRelocationNotice"` presente
    - `<HistoryRelocationNotice />` montado tras `<PendingClassificationListener />`
    - Ruta `<Route path="/mis-plantas" element={<Navigate to="/ajustes/mis-plantas" replace />} />` presente (redirect)
    - Ruta `<Route path="/ajustes/mis-plantas" element={<AppLayout><History /></AppLayout>} />` presente
    - Ruta `<Route path="/regar" element={<AppLayout><RegarPage /></AppLayout>} />` presente (NO `fullBleed`)
    - Rutas existentes intactas: `/`, `/planta/:id`, `/ajustes`, `/mapa` (fullBleed), `/login`, `/signup`, legal pages, `*`
    - `npx tsc --noEmit` exit 0
    - `npm run build` exit 0
  </acceptance_criteria>
  <done>App.tsx registra /regar, redirige /mis-plantas → /ajustes/mis-plantas, monta HistoryRelocationNotice; rutas existentes sin regresiones.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Añadir sección "Mi colección" en Settings.tsx con botón a /ajustes/mis-plantas</name>
  <files>src/pages/Settings.tsx</files>
  <read_first>
    - src/pages/Settings.tsx (CURRENT — entender Section wrapper líneas 30-50, el orden de secciones lines 134-227)
    - .planning/phases/03-calendar-v0/03-CONTEXT.md D-03 (label verbatim)
    - .planning/phases/03-calendar-v0/03-RESEARCH.md Open Question #8 (placement: después de "Cuenta" — UX call confirmada)
  </read_first>
  <action>
    Modificar `src/pages/Settings.tsx`:

    **Cambio 1 — import:** Añadir `BookOpen` al import de `lucide-react` (líneas 3-9). Ya hay `Leaf` y otros — añadir `BookOpen`:

    ```tsx
    import {
      BookOpen,
      Download,
      FileText,
      Leaf,
      Trash2,
      UserX,
    } from "lucide-react";
    ```

    **Cambio 2 — insertar la nueva Section ENTRE "Cuenta" (line 135-166) y "Tus datos" (line 169-227)**. Auth-gated (solo se renderiza si `user`). Label exacto per D-03: "Mis plantas (casa + descubrimientos)".

    Insertar exactamente este bloque entre la Section "Cuenta" (que termina en `</Section>` después de `)}`) y el bloque condicional `{user && (` de "Tus datos":

    ```tsx
          {/* D-03: Mis plantas vive ahora bajo Ajustes (movido desde tab principal en Phase 3). */}
          {user && (
            <Section title="Mi colección" icon={BookOpen}>
              <p className="text-base text-muted-foreground">
                Tus plantas guardadas, incluyendo las de casa y los descubrimientos.
              </p>
              <Button
                variant="outline"
                size="lg"
                onClick={() => navigate("/ajustes/mis-plantas")}
                className="w-full justify-start gap-2"
              >
                <BookOpen className="size-5" />
                Mis plantas (casa + descubrimientos)
              </Button>
            </Section>
          )}
    ```

    **NO MODIFICAR:**
    - El Section wrapper helper (líneas 30-50)
    - La Section "Cuenta" (líneas 135-166) — solo se inserta DESPUÉS de su cierre
    - La Section "Tus datos" (líneas 169-227) — solo se inserta ANTES de su apertura
    - El bloque "App info" (líneas 230-233) — sigue al final
    - Los handlers `handleExportData`, `handleDeleteAccount`
    - El header `<h1>Ajustes</h1>` (línea 130-132)

    Decisiones implementadas:
    - **D-03**: label verbatim "Mis plantas (casa + descubrimientos)"; ruta destino `/ajustes/mis-plantas` (Task 2 ya la registra)
    - **Researcher Q#8**: placement después de "Cuenta" (UX rationale: usuarios anónimos ven login CTA primero)
    - **Auth-gating**: la Section solo se renderiza si `user` (consistente con cómo "Tus datos" se gate-a)
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/pages/Settings.tsx','utf8');const checks={hasBookOpenImport:/import\s*\{[^}]*BookOpen[^}]*\}\s*from\s*['\"]lucide-react['\"]/.test(s),hasMiColeccionSection:s.includes('Mi colección'),hasLabelVerbatim:s.includes('Mis plantas (casa + descubrimientos)'),hasNavigateCall:s.includes('navigate(\"/ajustes/mis-plantas\")'),hasAuthGate:/\{user\s*&&\s*\(\s*<Section\s+title=\"Mi colección\"/.test(s),preservesCuentaSection:s.includes('<Section title=\"Cuenta\" icon={Leaf}>'),preservesTusDatosSection:s.includes('<Section title=\"Tus datos\" icon={FileText}>'),preservesAjustesH1:s.includes('Ajustes')};const fails=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);if(fails.length){console.error('FAIL:',fails);process.exit(1);}console.log('OK Settings.tsx Mi colección section');"</automated>
  </verify>
  <acceptance_criteria>
    - Import de `BookOpen` añadido al bloque `lucide-react`
    - Nueva `<Section title="Mi colección" icon={BookOpen}>` insertada entre "Cuenta" y "Tus datos"
    - La sección es auth-gated (`{user && (...)}`)
    - El botón usa label verbatim `"Mis plantas (casa + descubrimientos)"`
    - El botón llama a `navigate("/ajustes/mis-plantas")` en `onClick`
    - Sección "Cuenta" intacta
    - Sección "Tus datos" intacta
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>Settings.tsx tiene una nueva sección "Mi colección" auth-gated con botón hacia /ajustes/mis-plantas.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Actualizar `UnclassifiedSection.tsx` para apuntar a `/ajustes/mis-plantas?context=unclassified`</name>
  <files>src/components/UnclassifiedSection.tsx</files>
  <read_first>
    - src/components/UnclassifiedSection.tsx (CURRENT — entender el handler handleViewAll línea 42-45)
    - .planning/phases/03-calendar-v0/03-RESEARCH.md § Risk #10 (call sites a actualizar)
  </read_first>
  <action>
    Modificar `src/components/UnclassifiedSection.tsx`:

    En el handler `handleViewAll` (línea 42-45), cambiar el path del `navigate(...)` de `/mis-plantas?context=unclassified` a `/ajustes/mis-plantas?context=unclassified`.

    Estado actual (línea 44):
    ```tsx
    navigate("/mis-plantas?context=unclassified");
    ```

    Estado final:
    ```tsx
    navigate("/ajustes/mis-plantas?context=unclassified");
    ```

    Esto evita el redirect intermedio (`/mis-plantas` → `/ajustes/mis-plantas`) en el flujo del CTA "Ver todas ›" cuando hay plantas sin clasificar. El componente `History` lee el query param `context` con `useSearchParams()` (verificado en `History.tsx:38-39`) — el cambio del path no afecta cómo se aplica el filtro.

    **NO MODIFICAR:**
    - El handler `handleThumbClick` (sigue navegando a `/planta/${id}` — no a `/mis-plantas`)
    - El módulo-level flag `_unclassifiedSectionShownThisSession`
    - El track call `track("unclassified_section_clicked", { target: 'view_all' })`
    - El JSX del componente

    NOTA: hay otros call sites que apuntan a `/mis-plantas` pero NO se modifican en esta task:
    - `src/services/auth.service.ts:77` (emailRedirectTo) — mantener; redirige a `/mis-plantas` que ahora redirige internamente
    - `src/pages/LoginPage.tsx:68` (navigate("/mis-plantas")) — mantener por la misma razón
    - `src/App.tsx:59-60` (login/signup redirects) — mantenido en Task 2 por la misma razón

    Razón para no tocar los anteriores: la redirect chain funciona (`/mis-plantas` → `/ajustes/mis-plantas`) y minimizar el blast radius del refactor. La única navegación que actualizamos es el CTA del usuario en `UnclassifiedSection` porque (a) está en un componente nuevo de Phase 02.1 donde el equipo tiene el contexto fresco, y (b) evita un redirect visible en una navegación frecuente del flujo de clasificación pendiente.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/components/UnclassifiedSection.tsx','utf8');const checks={hasNewPath:s.includes('navigate(\"/ajustes/mis-plantas?context=unclassified\")'),noOldPath:!s.includes('navigate(\"/mis-plantas?context=unclassified\")'),preservesThumbClick:s.includes('navigate(`/planta/'),preservesTrackCall:s.includes('track(\"unclassified_section_clicked\"')};const fails=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);if(fails.length){console.error('FAIL:',fails);process.exit(1);}console.log('OK UnclassifiedSection viewall path');"</automated>
  </verify>
  <acceptance_criteria>
    - `handleViewAll` navega a `/ajustes/mis-plantas?context=unclassified` (no a `/mis-plantas?context=unclassified`)
    - El `handleThumbClick` sigue intacto (apunta a `/planta/${id}`)
    - El track call `track("unclassified_section_clicked", { target: 'view_all' })` se mantiene ANTES del navigate
    - El JSX queda sin cambios
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>UnclassifiedSection.tsx apunta directo a /ajustes/mis-plantas?context=unclassified (sin redirect intermedio).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Crear `HistoryRelocationNotice.tsx` (toast aviso una vez por device)</name>
  <files>src/components/HistoryRelocationNotice.tsx</files>
  <read_first>
    - src/hooks/use-consent.ts (analog: STORAGE_KEY pattern, isAnalyticsAllowed lines 25-34 — para entender el `typeof window !== "undefined"` guard SSR-safe)
    - src/App.tsx líneas 31-44 (`PendingClassificationListener`: pattern lifecycle-only component que retorna null)
    - .planning/phases/03-calendar-v0/03-CONTEXT.md D-04
  </read_first>
  <action>
    Crear `src/components/HistoryRelocationNotice.tsx` con el contenido EXACTO siguiente. Componente lifecycle-only (sin DOM) que dispara un único toast informativo en el primer mount post-deploy y persiste el flag en localStorage.

    ```tsx
    import { useEffect } from "react";
    import { toast } from "sonner";

    /**
     * D-04 (Phase 3 / Calendar v0): aviso one-shot de la migración de la
     * sección "Mis plantas" a Ajustes.
     *
     * Comportamiento:
     * - En el primer mount post-deploy, dispara `toast("📍 Hemos movido Mis
     *   plantas a Ajustes", { duration: 8000 })`.
     * - Persiste flag en localStorage para no repetirse en futuros mounts.
     * - No bloquea la app — el toast vive en el `<Toaster />` global de App.tsx.
     * - No tiene UI propia (return null).
     *
     * Key del flag: `mp_seen_history_relocation_notice` (lockeado por D-04).
     * Cuando el flag esté presente, el componente es no-op.
     *
     * SSR-safe: gated por `typeof window !== "undefined"` (paranoid pero
     * consistente con el resto del código que toca window/localStorage).
     */
    const STORAGE_KEY = "mp_seen_history_relocation_notice";

    export default function HistoryRelocationNotice() {
      useEffect(() => {
        if (typeof window === "undefined") return;
        try {
          if (localStorage.getItem(STORAGE_KEY) === "1") return;
          toast("📍 Hemos movido Mis plantas a Ajustes", { duration: 8000 });
          localStorage.setItem(STORAGE_KEY, "1");
        } catch {
          // localStorage puede fallar en modo incógnito (iOS Safari) o si la
          // cuota está llena. No bloquea — simplemente se reintentará en el
          // siguiente mount (acepta repetir el toast en ese edge case).
        }
      }, []);
      return null;
    }
    ```

    Decisiones implementadas:
    - **D-04**: copy verbatim `"📍 Hemos movido Mis plantas a Ajustes"`, duración 8000ms, key `mp_seen_history_relocation_notice`, valor `"1"` (compact y string-typed para coherencia con localStorage)
    - **SSR-safe**: `typeof window` guard
    - **Try/catch**: en iOS Safari modo privado `localStorage.setItem` puede tirar — el componente acepta el fallback de repetir el toast en lugar de crashear
    - **Lifecycle-only**: retorna `null` (no añade DOM)
    - **No depende de auth**: el toast se muestra a TODOS los usuarios (anon + logged), porque incluso los anon podrían haber bookmarkeado `/mis-plantas` previamente
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/components/HistoryRelocationNotice.tsx','utf8');const checks={hasUseEffect:s.includes('useEffect'),hasToastImport:/import\s*\{\s*toast\s*\}\s*from\s*['\"]sonner['\"]/.test(s),hasStorageKey:s.includes('mp_seen_history_relocation_notice'),hasCopy:s.includes('📍 Hemos movido Mis plantas a Ajustes'),hasDuration8000:s.includes('duration: 8000'),hasSSRGuard:s.includes('typeof window'),hasReturnNull:s.includes('return null'),hasDefaultExport:s.includes('export default function HistoryRelocationNotice'),hasTryCatch:s.includes('try {')&&s.includes('catch')};const fails=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);if(fails.length){console.error('FAIL:',fails);process.exit(1);}console.log('OK HistoryRelocationNotice');"</automated>
  </verify>
  <acceptance_criteria>
    - El archivo existe en `src/components/HistoryRelocationNotice.tsx`
    - Exporta `default function HistoryRelocationNotice()`
    - Importa `toast` desde `"sonner"`
    - Constante `STORAGE_KEY = "mp_seen_history_relocation_notice"`
    - Toast copy literal: `"📍 Hemos movido Mis plantas a Ajustes"`
    - Toast options: `{ duration: 8000 }`
    - Guard SSR: `typeof window === "undefined"` early-return
    - Bloque `try/catch` envuelve la lectura/escritura de localStorage
    - `return null;`
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>HistoryRelocationNotice.tsx creado: lifecycle-only, toast one-shot persistido en localStorage.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Crear `RegarPage.tsx` placeholder (app bar + título; sub-phase 3-02 fills it)</name>
  <files>src/pages/RegarPage.tsx</files>
  <read_first>
    - src/pages/History.tsx líneas 100-120 (header pattern: px-6 py-8 pb-24 + h1 font-display)
    - src/pages/Settings.tsx líneas 128-132 (similar header pattern)
    - .planning/phases/03-calendar-v0/03-CONTEXT.md (header title "Tus plantas casa" per D-15 — empático, no "Lista de tareas")
    - .planning/phases/03-calendar-v0/03-RESEARCH.md sub-phase 3-02 file list (RegarPage)
  </read_first>
  <action>
    Crear `src/pages/RegarPage.tsx` placeholder. La sub-phase 3-02 lo extenderá con `useHomePlants`, la lista, redirect-on-empty, y `PlantWateringCard`. En esta sub-phase solo se crea el shell mínimo: header con título y `document.title`.

    Contenido exacto:

    ```tsx
    import { useEffect } from "react";

    /**
     * /regar — ¿Toca regar? — Calendario de riego.
     *
     * Phase 3 sub-phase 3-01: placeholder shell. Solo renderiza el header.
     * Sub-phase 3-02 añade: useHomePlants + PlantWateringCard list +
     * redirect-on-empty + sort by urgency.
     * Sub-phase 3-03 añade: countdown real + botón Regar/Regada.
     * Sub-phase 3-04 añade: frequency picker + pendiente primera vez.
     * Sub-phase 3-05 añade: PostHog tracking + tests + E2E.
     *
     * Tono suave (D-15): header empático "Tus plantas casa" (no "Lista de tareas").
     * El título visible "¿Toca regar?" se locked-in en D-02 — el header de la pantalla
     * debe transmitir cuidado, no obligación.
     */
    export default function RegarPage() {
      useEffect(() => {
        document.title = "¿Toca regar? · Mi Plantita Feliz";
      }, []);

      return (
        <div className="px-6 py-8 pb-24">
          <header className="mb-6 flex flex-col gap-1">
            <h1 className="font-display text-2xl font-bold text-foreground">
              ¿Toca regar?
            </h1>
            <p className="font-body text-base text-muted-foreground">
              Tus plantas casa
            </p>
          </header>
          {/* Sub-phase 3-02 reemplaza este placeholder con la lista de plantas. */}
        </div>
      );
    }
    ```

    Decisiones implementadas:
    - **D-02**: título visible "¿Toca regar?" (header h1)
    - **D-15**: subheader "Tus plantas casa" (tono suave, empático)
    - `document.title`: "¿Toca regar? · Mi Plantita Feliz" (consistente con `MapPage.tsx:48`)
    - Layout: `px-6 py-8 pb-24` (mismo patrón que `History.tsx:102` y `Settings.tsx:129`)
    - `pb-24` deja espacio para el BottomTabBar (h-16 + safe-area)
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/pages/RegarPage.tsx','utf8');const checks={hasUseEffect:s.includes('useEffect'),hasDefaultExport:s.includes('export default function RegarPage'),hasDocTitle:s.includes('document.title = \"¿Toca regar? · Mi Plantita Feliz\"'),hasH1:s.includes('¿Toca regar?'),hasSubheader:s.includes('Tus plantas casa'),hasPadding:s.includes('px-6 py-8 pb-24'),hasFontDisplay:s.includes('font-display')};const fails=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);if(fails.length){console.error('FAIL:',fails);process.exit(1);}console.log('OK RegarPage placeholder');"</automated>
  </verify>
  <acceptance_criteria>
    - El archivo existe en `src/pages/RegarPage.tsx`
    - Exporta `default function RegarPage()`
    - `document.title` se setea en `useEffect` mount: `"¿Toca regar? · Mi Plantita Feliz"`
    - Renderiza `<h1>¿Toca regar?</h1>` con `font-display text-2xl font-bold`
    - Renderiza un subheader `<p>Tus plantas casa</p>` con `text-muted-foreground`
    - Container usa `className="px-6 py-8 pb-24"`
    - Sin imports de hooks de data fetching (sub-phase 3-02 los añade)
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>RegarPage.tsx placeholder creado con título y subheader; listo para sub-phase 3-02 que añade la lista.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→localStorage | El flag `mp_seen_history_relocation_notice` vive en localStorage del navegador (same-origin); no cruza network |
| client→React Router | Las rutas `/regar` y `/ajustes/mis-plantas` mapean a páginas en el bundle; navegación via `<Navigate>` y `useNavigate` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | localStorage flag `mp_seen_history_relocation_notice` | accept | El usuario podría borrarlo y volver a ver el toast — efecto inocuo. No es un control de seguridad, solo UX dedupe. |
| T-03-02 | Open redirect | `<Navigate to="/ajustes/mis-plantas">` desde `/mis-plantas` | mitigate | Path literal hardcoded en App.tsx, no derivado de query string. Sin riesgo de injection. |
| T-03-03 | DoS | Toast en cada mount si localStorage está bloqueado (iOS Safari modo privado) | accept | El try/catch evita crash; el usuario verá el toast en cada apertura de la app — molestia menor, no es vector de ataque. |
| T-03-04 | Information disclosure | El componente `HistoryRelocationNotice` lee `localStorage` (lectura own-origin) | accept | Lectura same-origin; no expone datos sensibles. La key es genérica (no contiene UUIDs ni emails). |
| T-03-05 | XSS | El toast usa `toast("string literal", ...)` sin interpolación de datos del usuario | mitigate | El string es hardcoded en el código fuente. Sonner escapa el content por defecto. Sin riesgo. |
</threat_model>

<verification>
- `src/components/BottomTabBar.tsx` tiene la tab Regar condicional, ha eliminado el push de Mis plantas, mantiene Mapa y Ajustes.
- `src/App.tsx` registra `/regar`, redirige `/mis-plantas` → `/ajustes/mis-plantas`, monta `<HistoryRelocationNotice />`.
- `src/pages/Settings.tsx` tiene nueva Section "Mi colección" con botón a `/ajustes/mis-plantas`.
- `src/components/UnclassifiedSection.tsx` actualiza el path del "Ver todas".
- `src/components/HistoryRelocationNotice.tsx` existe (lifecycle-only).
- `src/pages/RegarPage.tsx` placeholder existe.
- `npx tsc --noEmit` exit 0.
- `npm run build` exit 0.
- `npm test -- --run` sin regresiones (los tests de auth.service ya esperan `/mis-plantas?email_confirmed=true` — no se toca).
- Smoke manual: en dev (`npm run dev`), `/` carga, BottomTabBar muestra solo Inicio sin login. Tras login + crear planta casa (mock data o flujo real), la tab Regar aparece sin reload. Navegar a `/regar` muestra el placeholder "¿Toca regar?". Navegar a `/mis-plantas` redirige a `/ajustes/mis-plantas`. Primer mount muestra el toast 📍.
- Cross-platform: localStorage es estándar Web API soportado en Capacitor 8 WebView (Android) y Safari iOS 16+ PWA standalone. Mode privado iOS Safari puede tirar — try/catch lo absorbe.
- A11y: el toast hereda accesibilidad de Sonner (`role="status"` por defecto en richColors mode). La tab Regar mantiene `aria-label="Regar"` y `aria-current="page"` cuando activa (heredado del render JSX intacto).
</verification>

<success_criteria>
- Tab "💧 Regar" visible solo para `user && home >= 1` (D-01, D-05).
- Tab Mis plantas eliminada del menú principal (D-03).
- `/mis-plantas` redirige a `/ajustes/mis-plantas` (backwards compat).
- Settings tiene nueva entrada "Mis plantas (casa + descubrimientos)".
- Toast aviso D-04 se muestra una vez por device.
- `/regar` resuelve a `RegarPage` placeholder.
- TypeScript compila, build pasa, sin regresiones en tests existentes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-calendar-v0/03-01-SUMMARY.md` documenting: tab reactivamente añadida via `useContextCounts`, ruta `/regar` registrada con AppLayout normal (no fullBleed), redirect chain de `/mis-plantas` para compat, toast D-04 lifecycle-only. Notar que el componente `History` se mueve sin tocar (cero refactor — `useSearchParams` ya lee el query param `context` independiente del path).
</output>
