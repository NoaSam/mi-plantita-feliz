---
phase: 03-calendar-v0
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - src/components/WateringFrequencyPicker.tsx
  - src/hooks/use-edit-watering-interval.ts
  - src/hooks/use-edit-watering-interval.test.ts
  - src/hooks/use-home-plants.ts
  - src/components/PlantWateringCard.tsx
  - src/pages/RegarPage.tsx
autonomous: true
requirements: [RIEG-02]
requirements_addressed: [RIEG-02]
tags: [phase-03, watering-interval, frequency-picker, bottom-sheet, pending-first-flow]

must_haves:
  truths:
    - "Existe el componente `WateringFrequencyPicker.tsx` — bottom sheet shadcn (`<Sheet side=\"bottom\">`) con input numérico clamped 1-60, botones Cancelar/Guardar (D-13)."
    - "El input es numeric con validación cliente: rechaza valores fuera de [1, 60], default vacío si `intervalDays === null`."
    - "Existe el hook `useEditWateringInterval` con función `editInterval(plantId, newDays)` modelado verbatim sobre `useLogWatering`: UPDATE de `watering_interval_days`, dispatch `mp:plant-frequency-updated`, retorna `{ok: boolean}`."
    - "Existe el unit test `use-edit-watering-interval.test.ts` cubriendo success, error, dispatch event."
    - "`useHomePlants` escucha también `mp:plant-frequency-updated` (añadido a los listeners de 3-02/3-03)."
    - "Tap en el texto 'Cada N días' / 'Sin frecuencia' de la card abre `WateringFrequencyPicker` con el valor actual pre-filled. Save → editInterval → toast 'Frecuencia actualizada · Cada N días' (D-13). Cancel cierra sin escribir."
    - "Flujo D-14 'Pendiente primera vez' implementado:"
    - "  - Si `wateringIntervalDays IS NOT NULL` y el usuario tapea 'Regar', el flujo va directo a logWatering (sin picker)."
    - "  - Si `wateringIntervalDays IS NULL` y el usuario tapea 'Regar', PRIMERO se abre el `WateringFrequencyPicker`. Al Guardar la frecuencia, el `editInterval` ejecuta y AUTOMÁTICAMENTE encadena el `logWatering` (single-flow two-step). Cancelar el picker en este flujo no marca la planta como regada."
    - "Toast picker save copy verbatim (D-13): 'Frecuencia actualizada · Cada N días' (N = newDays guardado)."
    - "Toast error copy: 'No se pudo guardar la frecuencia. Inténtalo de nuevo.'"
    - "La card sigue funcionando para planta con `intervalDays != null` y `lastWateredAt = null` (pending-first por only-null lastWateredAt): tap 'Regar' va directo a logWatering (D-14 caso 1)."
    - "El picker es accesible: input con label visible, botones con `min-h-11` (44px), focus management correcto (al abrir, foco al input; Esc/overlay cierra)."
  artifacts:
    - path: "src/components/WateringFrequencyPicker.tsx"
      provides: "Bottom sheet con input numérico 1-60 y botones Guardar/Cancelar"
      exports: ["WateringFrequencyPicker", "WateringFrequencyPickerProps"]
      min_lines: 90
    - path: "src/hooks/use-edit-watering-interval.ts"
      provides: "UPDATE watering_interval_days + dispatch mp:plant-frequency-updated"
      exports: ["useEditWateringInterval", "UseEditWateringIntervalReturn"]
      min_lines: 45
    - path: "src/hooks/use-edit-watering-interval.test.ts"
      provides: "Unit tests success + error + dispatch"
      min_lines: 80
  key_links:
    - from: "src/pages/RegarPage.tsx"
      to: "src/components/WateringFrequencyPicker.tsx"
      via: "<WateringFrequencyPicker open={pickerOpen} ... onSave onCancel />"
      pattern: "WateringFrequencyPicker"
    - from: "src/components/PlantWateringCard.tsx"
      to: "onEditFrequency prop (RegarPage opens picker)"
      via: "tap en texto Cada N días → onEditFrequency(plant.id)"
      pattern: "onEditFrequency"
    - from: "src/components/PlantWateringCard.tsx"
      to: "two-step pending-first flow"
      via: "if intervalDays === null AND user taps Regar → onEditFrequencyFirst then onWater"
      pattern: "intervalDays === null"
    - from: "src/hooks/use-edit-watering-interval.ts"
      to: "window CustomEvent mp:plant-frequency-updated"
      via: "window.dispatchEvent en editInterval"
      pattern: "mp:plant-frequency-updated"
    - from: "src/hooks/use-home-plants.ts"
      to: "listener mp:plant-frequency-updated"
      via: "addEventListener triggers refetch"
      pattern: "mp:plant-frequency-updated"
---

<objective>
Cerrar dos flujos UX que faltaban tras sub-phase 3-03:

1. **Edición inline de frecuencia (D-13)**: tap en el texto "Cada N días" abre un picker bottom-sheet (shadcn `<Sheet side="bottom">`). El usuario puede cambiar la frecuencia a cualquier valor entre 1-60. Guardar persiste vía nuevo hook `useEditWateringInterval` + toast confirmación.

2. **Pendiente primera vez con `intervalDays = null` (D-14 case 2)**: si la planta no tiene intervalo configurado (IA no pudo determinarlo) y el usuario tapea "Regar", PRIMERO se abre el picker para que el usuario establezca la frecuencia, y AUTOMÁTICAMENTE tras guardar se ejecuta `logWatering`. Two-step flujo en single tap-stream del usuario.

3. **Refactor de `PlantWateringCard.tsx`**: introducir una nueva prop `onWaterRequiringFrequency` que `RegarPage` usa para detectar el caso D-14 case 2 y orquestar el picker → log encadenado. Mantiene `onWater` y `onUndo` cableados sin breaking changes.

Sub-phases 3-02 y 3-03 dejaron `onEditFrequency` como prop opcional sin handler — esta sub-phase lo wirea. El handler vive en `RegarPage` porque el picker debe ser un singleton compartido por todas las cards (no uno por card).

Purpose: Wave 4 cierra el contrato funcional de RIEG-02 (frecuencia editable) y el caso edge "IA no dio frecuencia" de D-14. Sin esta sub-phase, las plantas con `interval_days = null` quedan atrapadas en "Pendiente primera vez · Toca regar para empezar" para siempre (el botón loggea con fallback copy pero nunca arranca el countdown real).

Output: 1 nuevo componente (picker) + 1 nuevo hook + test + 3 modificados (useHomePlants extiende listener, PlantWateringCard refactoriza, RegarPage orquesta). ~280 LOC nuevas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-calendar-v0/03-CONTEXT.md
@.planning/phases/03-calendar-v0/03-RESEARCH.md
@CLAUDE.md
@src/components/PlantMapSheet.tsx
@src/components/ui/sheet.tsx
@src/hooks/use-log-watering.ts
@src/hooks/use-home-plants.ts
@src/components/PlantWateringCard.tsx
@src/pages/RegarPage.tsx
@src/pages/History.tsx

<interfaces>
DEPENDS_ON (Wave 3 — sub-phase 3-03):
- Columna `last_watered_at` aplicada en DB; types regenerados.
- `useHomePlants` extendido con `lastWateredAt`.
- `useLogWatering` (logWatering + revertWatering).
- `PlantWateringCard` consume `computeStatus`, renderiza 4 states, optimistic + toast undo cableado.
- `RegarPage` aplica sort por urgencia + wirea onWater/onUndo.

CONSUME (ya existentes):
```typescript
// shadcn Sheet — verbatim usage en PlantMapSheet.tsx:82-134
import { Sheet, SheetContent, SheetHeader, SheetTitle, SheetFooter } from "@/components/ui/sheet";
// shadcn Button
import { Button } from "@/components/ui/button";
// Sonner toast
import { toast } from "sonner";
// computeStatus para deriving status de la planta updated
import { computeStatus } from "@/lib/watering-countdown";
```

CREATES:
```typescript
// src/components/WateringFrequencyPicker.tsx
export interface WateringFrequencyPickerProps {
  open: boolean;
  /** Pre-fill value (current intervalDays). Null → empty input. */
  currentIntervalDays: number | null;
  /** Plant name for the dialog title (e.g., "Monstera deliciosa"). */
  plantName: string;
  onSave: (newDays: number) => void;
  onCancel: () => void;
}
export function WateringFrequencyPicker(props: WateringFrequencyPickerProps): JSX.Element;

// src/hooks/use-edit-watering-interval.ts
export interface UseEditWateringIntervalReturn {
  editInterval: (plantId: string, newDays: number) => Promise<{ ok: boolean }>;
}
export function useEditWateringInterval(): UseEditWateringIntervalReturn;
```

REFACTORS:
```typescript
// src/components/PlantWateringCard.tsx — NEW prop:
export interface PlantWateringCardProps {
  plant: HomePlant;
  onWater?: (plant: HomePlant) => Promise<{ ok: boolean }>;
  onUndo?: (plant: HomePlant, previousLastWateredAt: string | null) => Promise<{ ok: boolean }>;
  onEditFrequency?: (plant: HomePlant) => void;
  /**
   * NEW: cuando el usuario tapea "Regar" pero la planta no tiene intervalDays,
   * el card invoca esta callback en lugar de onWater. El padre (RegarPage)
   * abre el picker y encadena editInterval → logWatering.
   */
  onWaterRequiringFrequency?: (plant: HomePlant) => void;
}
```

CONSTRAINT D-13 (locked):
- Input numérico clamped a [1, 60] (mismo rango que CHECK constraint del schema).
- Botones Cancelar (left) + Guardar (primary, right). Mobile: stacked vertical full-width (consistente con `PlantMapSheet.tsx:114-131`).

CONSTRAINT D-14 (locked):
- Plant con `intervalDays !== null` + `lastWateredAt = null`: tap "Regar" va directo a logWatering. La planta pasa a estado normal.
- Plant con `intervalDays === null` (independiente de lastWateredAt — aunque el caso típico es lastWateredAt también null): tap "Regar" abre picker; Save → editInterval → automatically logWatering encadenado.

EVENT CHANNEL:
- Nuevo CustomEvent: `mp:plant-frequency-updated` (per researcher Q#7, similar a `mp:plant-watered`).
- `useHomePlants` listens to este evento + los 3 existentes (`mp:plant-context-updated`, `mp:pending-classification-resolved`, `mp:plant-watered`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Crear hook `useEditWateringInterval` + test unit</name>
  <files>src/hooks/use-edit-watering-interval.ts, src/hooks/use-edit-watering-interval.test.ts</files>
  <read_first>
    - src/hooks/use-log-watering.ts (analog VERBATIM — copy shape, cambiar update field y event name)
    - src/hooks/use-log-watering.test.ts (analog del test)
    - .planning/phases/03-calendar-v0/03-CONTEXT.md D-07, D-13
  </read_first>
  <action>
    **Sub-task 1.1 — Crear `src/hooks/use-edit-watering-interval.ts`:**

    ```typescript
    import { useCallback } from "react";
    import { supabase } from "@/integrations/supabase/client";

    /**
     * Hook que actualiza `watering_interval_days` en una fila de plant_searches.
     *
     * Patrón verbatim de `useLogWatering`: SYNCHRONOUS UPDATE, retorna `{ ok }`.
     * Dispatch CustomEvent `mp:plant-frequency-updated` para que `useHomePlants`
     * refetche y la card recalcule el countdown.
     *
     * D-07: el valor se sobrescribe directamente — sin columna separada para
     * "valor original IA". Trade-off aceptado per CONTEXT.md.
     *
     * D-13: el input cliente valida [1, 60]; este hook NO re-valida porque
     * el CHECK constraint del DB rechaza valores fuera de rango. Si el caller
     * envía un valor inválido, supabase devuelve error y este hook retorna
     * `{ ok: false }` — el caller toste el error.
     */
    export interface UseEditWateringIntervalReturn {
      editInterval: (plantId: string, newDays: number) => Promise<{ ok: boolean }>;
    }

    export function useEditWateringInterval(): UseEditWateringIntervalReturn {
      const editInterval = useCallback(async (plantId: string, newDays: number) => {
        const { error } = await supabase
          .from("plant_searches")
          .update({ watering_interval_days: newDays })
          .eq("id", plantId);

        if (error) {
          console.error("[useEditWateringInterval] update failed:", error.message);
          return { ok: false as const };
        }
        if (typeof window !== "undefined") {
          window.dispatchEvent(
            new CustomEvent("mp:plant-frequency-updated", {
              detail: { plant_search_id: plantId, new_interval_days: newDays },
            }),
          );
        }
        return { ok: true as const };
      }, []);

      return { editInterval };
    }
    ```

    **Sub-task 1.2 — Crear `src/hooks/use-edit-watering-interval.test.ts`:**

    Copia adaptada del patrón `use-log-watering.test.ts`:

    ```typescript
    import { renderHook, act } from "@testing-library/react";
    import { describe, it, expect, vi, beforeEach } from "vitest";
    import { useEditWateringInterval } from "./use-edit-watering-interval";

    const makeChain = (result: { error: unknown }) => {
      const chain = {
        update: vi.fn(() => chain),
        eq: vi.fn(() => Promise.resolve(result)),
      };
      return chain;
    };

    let mockSupabaseFrom: ReturnType<typeof vi.fn>;
    vi.mock("@/integrations/supabase/client", () => ({
      supabase: {
        from: (...args: unknown[]) => mockSupabaseFrom(...args),
      },
    }));

    describe("useEditWateringInterval", () => {
      beforeEach(() => {
        mockSupabaseFrom = vi.fn();
      });

      it("editInterval: success path returns ok=true, dispatches mp:plant-frequency-updated", async () => {
        const chain = makeChain({ error: null });
        mockSupabaseFrom.mockReturnValue(chain);
        const dispatchSpy = vi.spyOn(window, "dispatchEvent");
        dispatchSpy.mockClear();

        const { result } = renderHook(() => useEditWateringInterval());

        let res: { ok: boolean } | undefined;
        await act(async () => {
          res = await result.current.editInterval("plant-001", 5);
        });

        expect(res?.ok).toBe(true);
        expect(mockSupabaseFrom).toHaveBeenCalledWith("plant_searches");
        expect(chain.update).toHaveBeenCalledWith({ watering_interval_days: 5 });
        expect(chain.eq).toHaveBeenCalledWith("id", "plant-001");
        const dispatched = dispatchSpy.mock.calls.find(
          ([e]) => (e as CustomEvent).type === "mp:plant-frequency-updated",
        );
        expect(dispatched).toBeDefined();
        const detail = (dispatched![0] as CustomEvent).detail;
        expect(detail.plant_search_id).toBe("plant-001");
        expect(detail.new_interval_days).toBe(5);
      });

      it("editInterval: error path returns ok=false, does NOT dispatch", async () => {
        const consoleSpy = vi.spyOn(console, "error").mockImplementation(() => {});
        const chain = makeChain({ error: { message: "CHECK constraint violation" } });
        mockSupabaseFrom.mockReturnValue(chain);
        const dispatchSpy = vi.spyOn(window, "dispatchEvent");
        dispatchSpy.mockClear();

        const { result } = renderHook(() => useEditWateringInterval());

        let res: { ok: boolean } | undefined;
        await act(async () => {
          res = await result.current.editInterval("plant-001", 99);
        });

        expect(res?.ok).toBe(false);
        const dispatched = dispatchSpy.mock.calls.find(
          ([e]) => (e as CustomEvent).type === "mp:plant-frequency-updated",
        );
        expect(dispatched).toBeUndefined();
        expect(consoleSpy).toHaveBeenCalled();
        consoleSpy.mockRestore();
      });

      it("editInterval: persists the exact value sent (1, 7, 60 boundaries)", async () => {
        const chain = makeChain({ error: null });
        mockSupabaseFrom.mockReturnValue(chain);

        const { result } = renderHook(() => useEditWateringInterval());

        for (const value of [1, 7, 60]) {
          await act(async () => {
            await result.current.editInterval("plant-001", value);
          });
          expect(chain.update).toHaveBeenCalledWith({ watering_interval_days: value });
        }
      });
    });
    ```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('src/hooks/use-edit-watering-interval.ts','utf8');const t=fs.readFileSync('src/hooks/use-edit-watering-interval.test.ts','utf8');const checks={hookExports:h.includes('export function useEditWateringInterval'),hookExportsReturn:h.includes('export interface UseEditWateringIntervalReturn'),hookHasEdit:h.includes('editInterval'),hookUpdatesInterval:h.includes('update({ watering_interval_days: newDays })'),hookDispatchesEvent:h.includes('new CustomEvent(\"mp:plant-frequency-updated\"'),hookSSRGuard:h.includes('typeof window !== \"undefined\"'),testSuccess:t.includes('success path returns ok=true'),testError:t.includes('error path returns ok=false'),testBoundaries:t.includes('persists the exact value')};const fails=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);if(fails.length){console.error('FAIL:',fails);process.exit(1);}console.log('OK useEditWateringInterval + tests');"</automated>
  </verify>
  <acceptance_criteria>
    - Hook exporta `useEditWateringInterval` y `UseEditWateringIntervalReturn`
    - `editInterval(plantId, newDays)`: UPDATE `watering_interval_days` + dispatch `mp:plant-frequency-updated` + return `{ok}`
    - Detail del event incluye `plant_search_id` y `new_interval_days`
    - Console.error en error paths
    - SSR guard antes de dispatch
    - 3 unit tests cubren: success-with-dispatch, error-no-dispatch, boundary values (1, 7, 60)
    - `npm test -- --run use-edit-watering-interval` exit 0
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>useEditWateringInterval creado + 3 tests passing.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extender `useHomePlants` con listener `mp:plant-frequency-updated`</name>
  <files>src/hooks/use-home-plants.ts</files>
  <read_first>
    - src/hooks/use-home-plants.ts (CURRENT desde 3-03 — listener block tras Task 3 de 3-03)
  </read_first>
  <action>
    Modificar el listener block en `src/hooks/use-home-plants.ts` para añadir `mp:plant-frequency-updated` al conjunto de eventos que disparan refetch.

    Estado actual del listener (post-3-03):
    ```typescript
    useEffect(() => {
      const handler = () => {
        load();
      };
      window.addEventListener("mp:plant-context-updated", handler);
      window.addEventListener("mp:pending-classification-resolved", handler);
      window.addEventListener("mp:plant-watered", handler);
      return () => {
        window.removeEventListener("mp:plant-context-updated", handler);
        window.removeEventListener("mp:pending-classification-resolved", handler);
        window.removeEventListener("mp:plant-watered", handler);
      };
    }, [load]);
    ```

    Estado final:
    ```typescript
    useEffect(() => {
      const handler = () => {
        load();
      };
      window.addEventListener("mp:plant-context-updated", handler);
      window.addEventListener("mp:pending-classification-resolved", handler);
      window.addEventListener("mp:plant-watered", handler);
      // Phase 3 sub-phase 3-04: dispatched by useEditWateringInterval tras edit.
      window.addEventListener("mp:plant-frequency-updated", handler);
      return () => {
        window.removeEventListener("mp:plant-context-updated", handler);
        window.removeEventListener("mp:pending-classification-resolved", handler);
        window.removeEventListener("mp:plant-watered", handler);
        window.removeEventListener("mp:plant-frequency-updated", handler);
      };
    }, [load]);
    ```

    **NO MODIFICAR:**
    - El type `HomePlant` (sin cambios — `wateringIntervalDays` ya estaba)
    - El SELECT ni el mapping
    - El mountedRef / early-return-anon
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/hooks/use-home-plants.ts','utf8');const checks={hasFrequencyAdd:s.includes('window.addEventListener(\"mp:plant-frequency-updated\"'),hasFrequencyRemove:s.includes('window.removeEventListener(\"mp:plant-frequency-updated\"'),hasWateredStill:s.includes('mp:plant-watered'),hasContextUpdatedStill:s.includes('mp:plant-context-updated')};const fails=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);if(fails.length){console.error('FAIL:',fails);process.exit(1);}console.log('OK useHomePlants frequency listener');"</automated>
  </verify>
  <acceptance_criteria>
    - Listener añade y remueve `mp:plant-frequency-updated` (simétrico)
    - Los 3 listeners previos (`mp:plant-context-updated`, `mp:pending-classification-resolved`, `mp:plant-watered`) intactos
    - `npx tsc --noEmit` exit 0
    - `npm test -- --run use-home-plants` sigue pasando
  </acceptance_criteria>
  <done>useHomePlants escucha 4 eventos: context-updated, pending-resolved, plant-watered, frequency-updated.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Crear `WateringFrequencyPicker.tsx` (bottom sheet con input 1-60)</name>
  <files>src/components/WateringFrequencyPicker.tsx</files>
  <read_first>
    - src/components/PlantMapSheet.tsx (analog VERBATIM del bottom sheet — Sheet, SheetContent side="bottom", drag handle, footer stacked)
    - src/components/ui/sheet.tsx (verificar las exports del primitive)
    - src/pages/History.tsx líneas 122-130 aprox (analog input styling)
    - src/components/ui/button.tsx (variants `hero` y `outline`)
    - .planning/phases/03-calendar-v0/03-CONTEXT.md D-13
  </read_first>
  <action>
    Crear `src/components/WateringFrequencyPicker.tsx`. Bottom sheet con input numérico clamped 1-60. Modelado verbatim sobre `PlantMapSheet`.

    Contenido exacto:

    ```tsx
    import { useEffect, useId, useRef, useState } from "react";
    import { Sheet, SheetContent, SheetFooter, SheetHeader, SheetTitle } from "@/components/ui/sheet";
    import { Button } from "@/components/ui/button";

    export interface WateringFrequencyPickerProps {
      open: boolean;
      /** Pre-fill value. Null → empty input. */
      currentIntervalDays: number | null;
      /** Plant common name for the dialog title. */
      plantName: string;
      /** Called with the validated new value (already clamped 1-60). */
      onSave: (newDays: number) => void;
      /** Called when user cancels (button or overlay/Esc). */
      onCancel: () => void;
    }

    const MIN_DAYS = 1;
    const MAX_DAYS = 60;

    /**
     * D-13: bottom sheet con input numérico para editar la frecuencia de riego.
     *
     * Visual idiom verbatim de `PlantMapSheet` (bottom sheet shadcn):
     * - side="bottom", rounded-t-2xl
     * - drag handle visual
     * - pb-[max(1.5rem,env(safe-area-inset-bottom))] para iOS home indicator
     * - max-h-[75dvh]
     *
     * Input: type="number" min={1} max={60}, clamped via onChange + onBlur.
     * Mobile: el `inputMode="numeric"` levanta el numpad sin teclado completo.
     *
     * A11y:
     * - Input con label visible "Días entre riegos" + aria-describedby helper.
     * - Buttons: Cancelar (outline) + Guardar (hero/primary), stacked vertical.
     * - Focus management: al abrir, foco al input automaticamente.
     * - Esc o overlay tap cierra (heredado del shadcn Sheet behavior).
     */
    export function WateringFrequencyPicker({
      open,
      currentIntervalDays,
      plantName,
      onSave,
      onCancel,
    }: WateringFrequencyPickerProps) {
      const [value, setValue] = useState<string>(
        currentIntervalDays !== null ? String(currentIntervalDays) : "",
      );
      const inputRef = useRef<HTMLInputElement | null>(null);
      const helperId = useId();
      const errorId = useId();
      const [showError, setShowError] = useState(false);

      // Reset/refill el value cuando el sheet se abre con nuevo plant.
      useEffect(() => {
        if (open) {
          setValue(currentIntervalDays !== null ? String(currentIntervalDays) : "");
          setShowError(false);
          // Auto-focus el input al abrir (small timeout para que el sheet se monte).
          window.setTimeout(() => {
            inputRef.current?.focus();
            inputRef.current?.select();
          }, 100);
        }
      }, [open, currentIntervalDays]);

      const handleSave = () => {
        const parsed = parseInt(value, 10);
        if (!Number.isFinite(parsed) || parsed < MIN_DAYS || parsed > MAX_DAYS) {
          setShowError(true);
          return;
        }
        onSave(parsed);
      };

      const handleChange = (e: React.ChangeEvent<HTMLInputElement>) => {
        // Permitir cualquier input numérico mientras tipea; validar al save/blur.
        setValue(e.target.value);
        if (showError) setShowError(false);
      };

      const handleBlur = () => {
        // Soft clamp visual on blur (no auto-fix — solo si el value es claramente fuera de rango).
        const parsed = parseInt(value, 10);
        if (Number.isFinite(parsed) && parsed > MAX_DAYS) {
          setValue(String(MAX_DAYS));
        } else if (Number.isFinite(parsed) && parsed < MIN_DAYS && parsed !== 0) {
          // Permitir vacío (0 = blank case); solo clamp si claramente < 1.
          setValue(String(MIN_DAYS));
        }
      };

      return (
        <Sheet open={open} onOpenChange={(o) => { if (!o) onCancel(); }}>
          <SheetContent
            side="bottom"
            className="bg-background border-t-2 border-foreground rounded-t-2xl p-6 max-h-[75dvh] overflow-y-auto pb-[max(1.5rem,env(safe-area-inset-bottom))] sm:max-w-md sm:left-1/2 sm:right-auto sm:-translate-x-1/2"
          >
            <div
              className="w-9 h-1 bg-foreground/25 rounded-full mx-auto mb-5"
              aria-hidden
            />

            <SheetHeader className="items-start text-left gap-1 p-0 mb-4">
              <SheetTitle className="font-display text-xl font-semibold leading-tight text-foreground">
                Frecuencia de riego
              </SheetTitle>
              <p className="font-body text-sm text-muted-foreground">
                {plantName}
              </p>
            </SheetHeader>

            <div className="flex flex-col gap-2 mb-6">
              <label
                htmlFor="watering-frequency-input"
                className="font-body text-sm font-semibold text-foreground"
              >
                Días entre riegos
              </label>
              <input
                ref={inputRef}
                id="watering-frequency-input"
                type="number"
                inputMode="numeric"
                min={MIN_DAYS}
                max={MAX_DAYS}
                value={value}
                onChange={handleChange}
                onBlur={handleBlur}
                aria-describedby={`${helperId} ${showError ? errorId : ""}`.trim()}
                aria-invalid={showError}
                className="w-full px-4 py-3 bg-secondary border-2 border-foreground rounded-2xl font-display text-2xl font-bold text-foreground text-center focus-visible:outline-none focus-visible:ring-4 focus-visible:ring-ring"
                placeholder="7"
              />
              <p id={helperId} className="font-body text-xs text-muted-foreground">
                Entre {MIN_DAYS} y {MAX_DAYS} días.
              </p>
              {showError && (
                <p
                  id={errorId}
                  className="font-body text-xs text-destructive"
                  role="alert"
                >
                  Introduce un número entre {MIN_DAYS} y {MAX_DAYS}.
                </p>
              )}
            </div>

            <SheetFooter className="flex flex-col gap-2 sm:flex-col p-0">
              <Button
                variant="hero"
                size="default"
                onClick={handleSave}
                className="w-full font-display text-base font-bold min-h-11"
              >
                Guardar
              </Button>
              <Button
                variant="outline"
                size="default"
                onClick={onCancel}
                className="w-full font-body text-base font-semibold border-2 border-foreground min-h-11"
              >
                Cancelar
              </Button>
            </SheetFooter>
          </SheetContent>
        </Sheet>
      );
    }
    ```

    Decisiones implementadas:
    - **D-13**: input numérico clamped 1-60, botones Cancelar/Guardar stacked
    - **shadcn Sheet**: mismo visual idiom que `PlantMapSheet` (rounded-t-2xl, drag handle, pb safe area)
    - **A11y**: label + aria-describedby + aria-invalid + role="alert" en error; auto-focus al abrir; select() pre-selecciona el value para fácil overwrite
    - **Touch targets**: ambos botones `min-h-11`
    - **Mobile**: `inputMode="numeric"` para numpad iOS/Android
    - **Validación**: clamp visual on blur (suave), error toast on save attempt out of range
    - **No persistencia**: el componente es controlled — el padre maneja la persistencia vía `onSave`
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/components/WateringFrequencyPicker.tsx','utf8');const checks={hasExport:s.includes('export function WateringFrequencyPicker'),hasInterface:s.includes('export interface WateringFrequencyPickerProps'),hasSheetImport:s.includes('import { Sheet'),hasButtonImport:s.includes('import { Button }'),hasMinMax:s.includes('MIN_DAYS = 1')&&s.includes('MAX_DAYS = 60'),hasInputNumber:s.includes('type=\"number\"'),hasInputMode:s.includes('inputMode=\"numeric\"'),hasMinAttr:s.includes('min={MIN_DAYS}'),hasMaxAttr:s.includes('max={MAX_DAYS}'),hasLabelDias:s.includes('Días entre riegos'),hasSaveButton:s.includes('Guardar'),hasCancelButton:s.includes('Cancelar'),hasMinH11:s.includes('min-h-11'),hasAutoFocus:s.includes('inputRef.current?.focus()'),hasErrorMessage:s.includes('role=\"alert\"'),hasSafeAreaInset:s.includes('env(safe-area-inset-bottom)')};const fails=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);if(fails.length){console.error('FAIL:',fails);process.exit(1);}console.log('OK WateringFrequencyPicker');"</automated>
  </verify>
  <acceptance_criteria>
    - Componente exporta `WateringFrequencyPicker` + `WateringFrequencyPickerProps`
    - Acepta props: `open`, `currentIntervalDays`, `plantName`, `onSave`, `onCancel`
    - Usa `<Sheet open={open} onOpenChange={...}>` + `<SheetContent side="bottom" ...>`
    - Input `<input type="number" inputMode="numeric" min={1} max={60} />`
    - Label `"Días entre riegos"` visible
    - Helper text `"Entre 1 y 60 días."`
    - Error message `"Introduce un número entre 1 y 60."` con `role="alert"` cuando out of range
    - Buttons: Guardar (variant="hero") + Cancelar (variant="outline"), ambos `min-h-11`, stacked vertical
    - Drag handle decorativo `w-9 h-1 bg-foreground/25 rounded-full`
    - Safe-area: `pb-[max(1.5rem,env(safe-area-inset-bottom))]`
    - Auto-focus al input cuando `open` cambia a true
    - Reset del `value` cuando `open` o `currentIntervalDays` cambian
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>WateringFrequencyPicker creado: bottom sheet shadcn con input numérico clamped y validación.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Refactorizar `PlantWateringCard.tsx` para detectar caso D-14 con `onWaterRequiringFrequency`</name>
  <files>src/components/PlantWateringCard.tsx</files>
  <read_first>
    - src/components/PlantWateringCard.tsx (CURRENT desde 3-03 — handleWater wireado a onWater)
    - .planning/phases/03-calendar-v0/03-CONTEXT.md D-14
  </read_first>
  <action>
    Modificar `src/components/PlantWateringCard.tsx` para añadir la prop `onWaterRequiringFrequency` y branching D-14:

    **Cambio 1 — extender la interface `PlantWateringCardProps`:**

    Estado actual (post-3-03):
    ```typescript
    export interface PlantWateringCardProps {
      plant: HomePlant;
      onWater?: (plant: HomePlant) => Promise<{ ok: boolean }>;
      onUndo?: (plant: HomePlant, previousLastWateredAt: string | null) => Promise<{ ok: boolean }>;
      onEditFrequency?: (plantId: string) => void;
    }
    ```

    Estado final (cambios: `onEditFrequency` ahora pasa `HomePlant` en vez de solo id; nueva prop `onWaterRequiringFrequency`):

    ```typescript
    export interface PlantWateringCardProps {
      plant: HomePlant;
      onWater?: (plant: HomePlant) => Promise<{ ok: boolean }>;
      onUndo?: (plant: HomePlant, previousLastWateredAt: string | null) => Promise<{ ok: boolean }>;
      /** Tap on "Cada N días" / "Sin frecuencia" text. Caller opens the picker. */
      onEditFrequency?: (plant: HomePlant) => void;
      /**
       * D-14 case 2: invoked when user taps "Regar" but the plant has no
       * watering interval configured. Caller (RegarPage) opens the picker
       * first; on save, the picker's `onSave` invokes editInterval and then
       * automatically chains logWatering. Card does NOT proceed with the
       * normal optimistic flow in this case.
       */
      onWaterRequiringFrequency?: (plant: HomePlant) => void;
    }
    ```

    **Cambio 2 — modificar `handleWater` para branching D-14:**

    Estado actual (post-3-03):
    ```typescript
    const handleWater = async () => {
      if (!onWater) {
        console.log("[PlantWateringCard] water tap (no onWater wired):", plant.id);
        return;
      }
      // ... rest of optimistic update logic
    };
    ```

    Estado final:
    ```typescript
    const handleWater = async () => {
      // D-14 case 2: si la planta no tiene intervalo configurado, delegar al
      // padre para que abra el picker primero antes de logear el riego.
      if (plant.wateringIntervalDays === null) {
        if (onWaterRequiringFrequency) {
          onWaterRequiringFrequency(plant);
          return;
        }
        // Fallback si el padre no cablea — log la situación pero permite el flujo normal.
        console.warn(
          "[PlantWateringCard] plant has no intervalDays but no onWaterRequiringFrequency wired:",
          plant.id,
        );
      }

      if (!onWater) {
        console.log("[PlantWateringCard] water tap (no onWater wired):", plant.id);
        return;
      }

      // Existing optimistic flow from 3-03 sigue aquí (sin cambios) ...
      // Capture previous BEFORE optimistic mutation (for undo target).
      const previousLastWateredAt = plant.lastWateredAt;

      // 1. Optimistic update visible
      const newTimestamp = new Date().toISOString();
      setOptimisticLastWatered(newTimestamp);
      // 2. Flash animation 1s
      setFlashing(true);
      window.setTimeout(() => setFlashing(false), 1000);

      // 3. Persist immediately
      const result = await onWater(plant);

      if (!result.ok) {
        setOptimisticLastWatered(undefined);
        setFlashing(false);
        toast.error("No se pudo guardar el riego. Inténtalo de nuevo.");
        return;
      }

      const newStatus = computeStatus({
        lastWateredAt: newTimestamp,
        intervalDays: plant.wateringIntervalDays,
      });
      const toastMessage =
        newStatus.daysRemaining !== null
          ? `✓ Regada · Siguiente riego en ${newStatus.daysRemaining} días`
          : "✓ Regada · Configura la frecuencia para ver el próximo riego";

      toast(toastMessage, {
        duration: 4000,
        ...(onUndo
          ? {
              action: {
                label: "Deshacer",
                onClick: async () => {
                  setOptimisticLastWatered(previousLastWateredAt);
                  setFlashing(false);
                  const undoResult = await onUndo(plant, previousLastWateredAt);
                  if (!undoResult.ok) {
                    toast.error("No se pudo deshacer el riego. Refresca para sincronizar.");
                  } else {
                    setOptimisticLastWatered(undefined);
                  }
                },
              },
            }
          : {}),
      });
    };
    ```

    **Cambio 3 — modificar `handleEditFrequency` para pasar `plant` (no solo `plant.id`):**

    Estado actual:
    ```typescript
    const handleEditFrequency = () => {
      if (onEditFrequency) {
        onEditFrequency(plant.id);
      }
    };
    ```

    Estado final:
    ```typescript
    const handleEditFrequency = () => {
      if (onEditFrequency) {
        onEditFrequency(plant);
      }
    };
    ```

    **NO MODIFICAR:**
    - El bloque switch de status (los 4 estados D-08)
    - El render JSX (article, foto, badge, botones)
    - Los hooks `useState`
    - Los imports de `toast`, `computeStatus`, etc.
    - El data-watering-status attribute

    Razón del cambio en `onEditFrequency`: el padre necesita acceso al `wateringIntervalDays` actual para pre-fill el picker. Pasar el `plant` completo evita un lookup adicional. Es un breaking change para los call sites — pero PlantWateringCard solo se usa en RegarPage, que se actualiza en Task 5 abajo.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/components/PlantWateringCard.tsx','utf8');const checks={hasNewProp:s.includes('onWaterRequiringFrequency?:'),hasD14Branch:s.includes('plant.wateringIntervalDays === null'),hasD14Callback:s.includes('onWaterRequiringFrequency(plant)'),hasEditFreqUpdatedSignature:s.includes('onEditFrequency?: (plant: HomePlant)'),hasEditFreqCallWithPlant:s.includes('onEditFrequency(plant)')&&!s.includes('onEditFrequency(plant.id)'),preservesToast:s.includes('toast(toastMessage'),preservesOptimistic:s.includes('setOptimisticLastWatered'),preservesFlash:s.includes('animate-flash-success'),preservesStatusSwitch:s.includes('case \"urgent\":')};const fails=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);if(fails.length){console.error('FAIL:',fails);process.exit(1);}console.log('OK PlantWateringCard refactored');"</automated>
  </verify>
  <acceptance_criteria>
    - Interface `PlantWateringCardProps` añade `onWaterRequiringFrequency?: (plant: HomePlant) => void`
    - Interface `onEditFrequency` cambia signature: ahora `(plant: HomePlant) => void` (no `(plantId: string) => void`)
    - `handleWater` chequea `plant.wateringIntervalDays === null` PRIMERO; si null y `onWaterRequiringFrequency` está cableado, invoca y retorna; sino warn + sigue con el flujo normal
    - El flujo normal de optimistic + toast (de 3-03) sigue intacto cuando `intervalDays !== null`
    - `handleEditFrequency` invoca `onEditFrequency(plant)` (no `.id`)
    - El render JSX y el switch de status siguen intactos
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>PlantWateringCard branch D-14 case 2 + onEditFrequency pasa plant completo.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Orquestar picker + flujo D-14 en `RegarPage.tsx`</name>
  <files>src/pages/RegarPage.tsx</files>
  <read_first>
    - src/pages/RegarPage.tsx (CURRENT desde 3-03)
    - src/components/WateringFrequencyPicker.tsx (recién creado)
    - src/hooks/use-edit-watering-interval.ts (recién creado)
    - .planning/phases/03-calendar-v0/03-CONTEXT.md D-13, D-14
  </read_first>
  <action>
    Modificar `src/pages/RegarPage.tsx` para:

    1. Importar `useEditWateringInterval` y `WateringFrequencyPicker`.
    2. Añadir state local para el picker: `{ pickerState: { plant: HomePlant; mode: "edit" | "frequency-then-water" } | null }`.
    3. Definir handlers: `handleOpenEditPicker`, `handleOpenPickerForWater`, `handlePickerSave`, `handlePickerCancel`.
    4. Pasar las nuevas props al `PlantWateringCard`.
    5. Renderizar `<WateringFrequencyPicker />` al final del JSX (singleton fuera de la lista).

    Contenido final exacto:

    ```tsx
    import { useEffect, useMemo, useState } from "react";
    import { Navigate } from "react-router-dom";
    import { Leaf } from "lucide-react";
    import { toast } from "sonner";
    import { useAuth } from "@/hooks/use-auth";
    import { useHomePlants, type HomePlant } from "@/hooks/use-home-plants";
    import { useLogWatering } from "@/hooks/use-log-watering";
    import { useEditWateringInterval } from "@/hooks/use-edit-watering-interval";
    import { PlantWateringCard } from "@/components/PlantWateringCard";
    import { WateringFrequencyPicker } from "@/components/WateringFrequencyPicker";
    import { computeStatus } from "@/lib/watering-countdown";

    /**
     * /regar — ¿Toca regar? — Calendario de riego.
     *
     * Phase 3 sub-phase 3-04: añade flujo de edición de frecuencia (D-13) y
     * flujo "pending-first sin intervalo" (D-14 case 2).
     *
     * El picker es un singleton montado al final del JSX. Tres flujos lo abren:
     *  - Tap en "Cada N días": modo "edit" — save persiste el nuevo intervalo.
     *  - Tap en "Regar" cuando la planta no tiene intervalo: modo
     *    "frequency-then-water" — save persiste intervalo Y encadena un log
     *    de riego automáticamente (single-step UX, two-step persistencia).
     *  - Cancel: cierra el picker sin escribir.
     */

    function splitCommonName(name: string): string {
      const match = name.match(/^(.+?)\s*\(([^)]+)\)$/);
      return match ? match[1].trim() : name;
    }

    function urgencyKey(plant: HomePlant): number {
      const { status, daysRemaining } = computeStatus({
        lastWateredAt: plant.lastWateredAt,
        intervalDays: plant.wateringIntervalDays,
      });
      if (status === "pending-first") return Number.POSITIVE_INFINITY;
      return daysRemaining ?? 0;
    }

    type PickerState =
      | { plant: HomePlant; mode: "edit" }
      | { plant: HomePlant; mode: "frequency-then-water" }
      | null;

    export default function RegarPage() {
      const { user, isLoading: authLoading } = useAuth();
      const { plants, isLoading: plantsLoading } = useHomePlants();
      const { logWatering, revertWatering } = useLogWatering();
      const { editInterval } = useEditWateringInterval();

      const isLoading = authLoading || (!!user && plantsLoading);

      const [pickerState, setPickerState] = useState<PickerState>(null);

      useEffect(() => {
        document.title = "¿Toca regar? · Mi Plantita Feliz";
      }, []);

      const sortedPlants = useMemo(() => {
        return [...plants].sort((a, b) => {
          const ka = urgencyKey(a);
          const kb = urgencyKey(b);
          if (ka !== kb) return ka - kb;
          return splitCommonName(a.name).localeCompare(splitCommonName(b.name), "es");
        });
      }, [plants]);

      if (isLoading) {
        return (
          <div
            className="flex items-center justify-center py-20"
            aria-busy="true"
            aria-label="Cargando tus plantas"
          >
            <Leaf className="size-12 text-primary animate-pulse-slow" strokeWidth={1.2} />
          </div>
        );
      }

      if (plants.length === 0) {
        return <Navigate to="/" replace />;
      }

      const handleWater = async (plant: HomePlant) => {
        return logWatering(plant.id);
      };

      const handleUndo = async (plant: HomePlant, previousLastWateredAt: string | null) => {
        return revertWatering(plant.id, previousLastWateredAt);
      };

      // D-13: tap on "Cada N días" / "Sin frecuencia" text.
      const handleEditFrequency = (plant: HomePlant) => {
        setPickerState({ plant, mode: "edit" });
      };

      // D-14 case 2: tap on "Regar" when intervalDays is null.
      const handleWaterRequiringFrequency = (plant: HomePlant) => {
        setPickerState({ plant, mode: "frequency-then-water" });
      };

      const handlePickerSave = async (newDays: number) => {
        if (!pickerState) return;
        const { plant, mode } = pickerState;
        // Close optimistically — toast on outcome.
        setPickerState(null);

        const editResult = await editInterval(plant.id, newDays);
        if (!editResult.ok) {
          toast.error("No se pudo guardar la frecuencia. Inténtalo de nuevo.");
          return;
        }

        toast(`Frecuencia actualizada · Cada ${newDays} días`, { duration: 3000 });

        // D-14 case 2: tras editar, encadenar log de riego.
        if (mode === "frequency-then-water") {
          const logResult = await logWatering(plant.id);
          if (!logResult.ok) {
            toast.error("La frecuencia se guardó, pero no se pudo registrar el riego. Inténtalo de nuevo.");
          } else {
            toast(`✓ Regada · Siguiente riego en ${newDays} días`, { duration: 4000 });
          }
        }
      };

      const handlePickerCancel = () => {
        setPickerState(null);
      };

      return (
        <div className="px-6 py-8 pb-24">
          <header className="mb-6 flex flex-col gap-1">
            <h1 className="font-display text-2xl font-bold text-foreground">
              ¿Toca regar?
            </h1>
            <p className="font-body text-base text-muted-foreground">
              Tus plantas casa
            </p>
          </header>
          <ul className="flex flex-col gap-3" aria-label="Lista de plantas de casa">
            {sortedPlants.map((plant) => (
              <li key={plant.id}>
                <PlantWateringCard
                  plant={plant}
                  onWater={handleWater}
                  onUndo={handleUndo}
                  onEditFrequency={handleEditFrequency}
                  onWaterRequiringFrequency={handleWaterRequiringFrequency}
                />
              </li>
            ))}
          </ul>

          {pickerState && (
            <WateringFrequencyPicker
              open={true}
              currentIntervalDays={pickerState.plant.wateringIntervalDays}
              plantName={splitCommonName(pickerState.plant.name)}
              onSave={handlePickerSave}
              onCancel={handlePickerCancel}
            />
          )}
        </div>
      );
    }
    ```

    Decisiones implementadas:
    - **D-13**: tap en frecuencia abre picker en modo "edit"; Guardar persiste + toast
    - **D-14 case 2**: tap en Regar con intervalDays=null abre picker en modo "frequency-then-water"; Save → editInterval + logWatering encadenados
    - **Picker singleton**: una sola instancia montada en RegarPage, controlada por `pickerState`
    - **Toast picker save**: copy `"Frecuencia actualizada · Cada N días"` (duration 3s — más corto que el de log de 4s porque no tiene undo aquí)
    - **Toast frequency-then-water log**: encadena con "✓ Regada · Siguiente riego en N días" tras el log exitoso
    - **Error handling**: ambos failures (editInterval o logWatering) muestran toast.error específico
    - **D-14 case 1** (intervalDays != null y lastWateredAt = null): el card va directo a `onWater` (sin picker) — handled por el `if (plant.wateringIntervalDays === null)` branch en `PlantWateringCard.handleWater`

    NOTA: el picker NO incluye action undo porque el caso es "configurar por primera vez" — el undo natural es volver al picker y editar de nuevo. Si en v1+ se quiere undo para edit, se añade entonces.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/pages/RegarPage.tsx','utf8');const checks={hasUseEditImport:s.includes('import { useEditWateringInterval }'),hasPickerImport:s.includes('import { WateringFrequencyPicker }'),hasPickerStateType:s.includes('type PickerState'),hasPickerStateInit:s.includes('useState<PickerState>(null)'),hasHandleEdit:s.includes('const handleEditFrequency'),hasHandleWaterRequiringFreq:s.includes('const handleWaterRequiringFrequency'),hasHandleSave:s.includes('const handlePickerSave'),hasHandleCancel:s.includes('const handlePickerCancel'),hasPickerJSX:s.includes('<WateringFrequencyPicker'),hasFrequencyThenWater:s.includes('mode === \"frequency-then-water\"'),hasToastFreqUpdated:s.includes('Frecuencia actualizada · Cada'),hasToastError:s.includes('No se pudo guardar la frecuencia'),hasOnEditFrequencyProp:s.includes('onEditFrequency={handleEditFrequency}'),hasOnWaterReqProp:s.includes('onWaterRequiringFrequency={handleWaterRequiringFrequency}')};const fails=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);if(fails.length){console.error('FAIL:',fails);process.exit(1);}console.log('OK RegarPage picker orchestration');"</automated>
  </verify>
  <acceptance_criteria>
    - Imports añadidos: `useEditWateringInterval`, `WateringFrequencyPicker`
    - Type `PickerState` con discriminated union `mode: "edit" | "frequency-then-water"`
    - State `pickerState` inicializa `null`
    - `handleEditFrequency(plant)`: setea pickerState modo "edit"
    - `handleWaterRequiringFrequency(plant)`: setea pickerState modo "frequency-then-water"
    - `handlePickerSave(newDays)`: cierra picker, llama `editInterval`, en éxito toste "Frecuencia actualizada · Cada N días"
    - Si mode === "frequency-then-water", tras editInterval éxito llama `logWatering` encadenado y toste "✓ Regada · Siguiente riego en N días"
    - `handlePickerCancel()`: cierra picker
    - `<PlantWateringCard>` recibe las 4 props: `onWater`, `onUndo`, `onEditFrequency`, `onWaterRequiringFrequency`
    - `<WateringFrequencyPicker>` renderizado condicional `{pickerState && (...)}` al final del JSX
    - Error handling: toast.error si editInterval falla; toast.error específico si log encadenado falla
    - `npx tsc --noEmit` exit 0
    - `npm run build` exit 0
  </acceptance_criteria>
  <done>RegarPage orquesta picker singleton, encadena edit + log para D-14 case 2, toast confirmaciones.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Supabase REST (UPDATE) | `useEditWateringInterval` UPDATE `watering_interval_days`. RLS UPDATE policy ya gate-a por user_id. |
| client→DB CHECK constraint | Si el cliente bypassa la validación 1-60, el CHECK constraint del schema rechaza — el hook retorna `{ok: false}` y el caller toste. |
| client-internal | CustomEvent `mp:plant-frequency-updated` same-origin. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-17 | Tampering | Un usuario rogue intenta UPDATE de planta de otro user | mitigate | RLS policy `auth.uid() = user_id` (Phase 02.1) cubre el UPDATE de cualquier columna. |
| T-03-18 | Input validation | Un usuario podría tipear 0, -1, 100, "abc" en el picker | mitigate | Cliente: `min`/`max` HTML5 + parseInt + range check antes de onSave. DB: CHECK constraint `1-60` belt-and-suspenders. |
| T-03-19 | XSS | `plantName` se renderiza en el SheetTitle del picker | mitigate | React escapa por defecto. Plant name proviene de la IA o del usuario (en futuras phases) — sin innerHTML. |
| T-03-20 | Race condition | Usuario tapea "Regar" rápido cuando intervalDays != null Y tapea de nuevo durante el flash | accept | El segundo tap hace UPDATE redundante. Edge case raro; no es vector de attaque. |
| T-03-21 | Race condition | Usuario abre picker, modifica, luego dispatch externo cambia plant.wateringIntervalDays | accept | El picker tiene su propio state local; se reset al cerrar/abrir. Si el dispatch ocurre con el picker abierto, el next save sobrescribe el valor recibido — comportamiento esperado del último-en-escribir. |
| T-03-22 | Information disclosure | Picker muestra el plant name en el title | accept | El plant name es del propio user (filtrado por RLS al fetch). |
</threat_model>

<verification>
- `src/hooks/use-edit-watering-interval.ts` + test creados, tests pasan.
- `src/hooks/use-home-plants.ts` listens también a `mp:plant-frequency-updated`.
- `src/components/WateringFrequencyPicker.tsx` creado, sheet con input + botones.
- `src/components/PlantWateringCard.tsx` refactorizado: nueva prop `onWaterRequiringFrequency`, branching D-14, `onEditFrequency` signature pasa `HomePlant`.
- `src/pages/RegarPage.tsx` orquesta picker singleton con 4 flujos.
- `npx tsc --noEmit` exit 0.
- `npm run build` exit 0.
- `npm test -- --run` no regresiones (3 nuevos tests + existentes pasan).
- Smoke manual D-13: tap en "Cada 7 días" → picker abre con 7 pre-fill → editar a 10 → Save → toast "Frecuencia actualizada · Cada 10 días" → badge muestra el nuevo countdown.
- Smoke manual D-14 case 2: planta con `interval_days=null`. Tap "Regar" → picker abre vacío → escribir 5 → Save → toast picker "Frecuencia actualizada" + toast log "✓ Regada · Siguiente riego en 5 días". Refresh: la planta está en estado normal con countdown 5d.
- Smoke manual D-14 case 1: planta con `interval_days != null` y `last_watered_at = null`. Tap "Regar" → flujo directo (sin picker) → log con optimistic + toast con Deshacer.
- Smoke manual cancel: abrir picker, tap "Cancelar" o overlay → cierra sin escribir; la planta queda igual.
- Smoke manual error: tipear "0" en picker → Save → error message visible "Introduce un número entre 1 y 60".
</verification>

<success_criteria>
- `useEditWateringInterval` UPDATE + dispatch + tests.
- `WateringFrequencyPicker` accesible, validado, mobile-friendly.
- `PlantWateringCard` correctamente brancha entre flujo normal y flujo D-14 case 2.
- `RegarPage` orquesta el singleton picker para los 3 flujos.
- D-13 funciona: tap on frequency → picker → save → countdown actualiza.
- D-14 case 2 funciona: tap on Regar sin intervalo → picker → save → editInterval + log encadenados.
- TypeScript compila, build pasa, tests pasan.
</success_criteria>

<output>
After completion, create `.planning/phases/03-calendar-v0/03-04-SUMMARY.md` documenting:
- Nuevo hook + componente picker creados con TDD para hook + estructura analog del PlantMapSheet para picker
- `PlantWateringCard` ahora acepta 4 callbacks (onWater, onUndo, onEditFrequency, onWaterRequiringFrequency); las 4 cableadas desde RegarPage
- D-14 caso 2 implementado como single-flow two-step: el usuario percibe un solo flow (Regar → picker → done) pero internamente son 2 UPDATEs
- Nuevo event channel `mp:plant-frequency-updated` para refetch de useHomePlants tras edit
- Sub-phase 3-05 cierra el ciclo con tracking PostHog + tests E2E + microcopy review final
</output>
