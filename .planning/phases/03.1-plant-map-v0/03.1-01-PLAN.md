---
phase: 03.1-plant-map-v0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/main.tsx
  - src/index.css
autonomous: true
requirements: [SPEC-R3, SPEC-R4]
requirements_addressed: [SPEC-R3, SPEC-R4]
tags: [phase-03.1, leaflet, dependencies, stack]

must_haves:
  truths:
    - "Las dependencias leaflet, react-leaflet y @types/leaflet aparecen instaladas en package.json y se resuelven correctamente en npm install"
    - "El CSS de Leaflet se carga en el bundle antes de los estilos del proyecto, permitiendo overrides de .leaflet-* en src/index.css"
    - "Los overrides CSS de .plant-map-pin y .leaflet-control-* están presentes en src/index.css y no rompen estilos existentes"
  artifacts:
    - path: "package.json"
      provides: "Declaración de leaflet@^1.9.4, react-leaflet@^4.2.1 (deps) y @types/leaflet@^1.9.21 (devDeps)"
      contains: '"leaflet": "^1.9.4"'
    - path: "src/main.tsx"
      provides: "Import order: leaflet CSS antes que ./index.css"
      contains: 'import "leaflet/dist/leaflet.css"'
    - path: "src/index.css"
      provides: "Overrides de .plant-map-pin (transparent bg) y .leaflet-control-zoom (display:none) + soporte safe-area-inset-right en attribution"
      contains: ".plant-map-pin"
  key_links:
    - from: "src/main.tsx"
      to: "leaflet/dist/leaflet.css"
      via: "import side-effect"
      pattern: 'import\\s+"leaflet/dist/leaflet\\.css"'
    - from: "src/index.css"
      to: ".plant-map-pin selector"
      via: "global CSS class consumed por buildPlantPinIcon (Plan 05)"
      pattern: "\\.plant-map-pin"
---

<objective>
Instalar el stack de mapas (leaflet, react-leaflet, @types/leaflet) lockeado en CONTEXT.md D-01/D-02/D-03 + RESEARCH.md § Standard Stack, importar `leaflet/dist/leaflet.css` en `src/main.tsx` **antes** de `./index.css` (orden crítico — Pitfall 2 de RESEARCH.md), y añadir overrides de CSS en `src/index.css` para que (a) el `divIcon` del pin no quede envuelto en el cuadrado blanco default de Leaflet, (b) los zoom controls se oculten (touch pinch es suficiente en mobile), y (c) el attribution control respete `safe-area-inset-right` en iOS PWA.

Purpose: bloque foundation del que dependen los Plans 02, 03, 04 y especialmente 05/06/07 (los que efectivamente renderizan el mapa). Sin esto, todo lo demás falla en runtime (mapa en blanco, tiles rotas, pin como caja blanca).
Output: stack instalado + import side-effect + 3 reglas CSS globales nuevas. Cero código React nuevo en este plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-plant-map-v0/03.1-CONTEXT.md
@.planning/phases/03.1-plant-map-v0/03.1-SPEC.md
@.planning/phases/03.1-plant-map-v0/03.1-RESEARCH.md
@.planning/phases/03.1-plant-map-v0/03.1-UI-SPEC.md
@.planning/phases/03.1-plant-map-v0/03.1-PATTERNS.md
@.planning/sketches/004-plant-map-v0/README.md
@CLAUDE.md

<interfaces>
<!-- Tras `npm install`, los siguientes módulos quedan disponibles para Plans 03/05/06: -->
<!-- import L from "leaflet"; — namespace + L.divIcon, L.latLngBounds, L.Map, L.Marker -->
<!-- import { MapContainer, TileLayer, Marker, useMap } from "react-leaflet"; -->
<!-- import type { DivIcon, LatLngBoundsExpression } from "leaflet"; -->

<!-- Versiones lockeadas por RESEARCH.md (verificadas con npm view en research): -->
<!--   leaflet@^1.9.4              — última estable, sin releases nuevos desde 2023 -->
<!--   react-leaflet@^4.2.1        — RAMA 4.x (compatible React 18). NO usar 5.x (requiere React 19) -->
<!--   @types/leaflet@^1.9.21      — DefinitelyTyped, sync con leaflet 1.9.x -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Instalar leaflet + react-leaflet + @types/leaflet</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (verificar bloque `dependencies` y `devDependencies` existentes — NO bumpear versiones de paquetes ya instalados)
    - .planning/phases/03.1-plant-map-v0/03.1-RESEARCH.md líneas 120-171 (§ Standard Stack — versiones lockeadas y peer deps verificadas con `npm view`)
    - .planning/phases/03.1-plant-map-v0/03.1-PATTERNS.md líneas 91-105 (§ 3 `package.json` — delta exacto)
  </read_first>
  <action>
    Ejecutar el comando exacto (lockeado por RESEARCH.md línea 130 y PATTERNS.md línea 102):

    ```bash
    npm install leaflet@^1.9.4 react-leaflet@^4.2.1
    npm install --save-dev @types/leaflet@^1.9.21
    ```

    NO instalar `react-leaflet@^5.x` — requiere React 19 que rompería el proyecto (verificado por RESEARCH.md). NO instalar plugins extra (`leaflet.markercluster`, `Leaflet.GestureHandling`) — están deferred a v1+ en CONTEXT.md `<deferred>`. NO bumpear versiones de paquetes existentes.

    Verificar que `package.json` queda con (orden alfabético entre el resto):
    - `dependencies` añade `"leaflet": "^1.9.4"` y `"react-leaflet": "^4.2.1"`
    - `devDependencies` añade `"@types/leaflet": "^1.9.21"`

    `package-lock.json` se actualiza automáticamente.
  </action>
  <verify>
    <automated>node -e "const pkg = require('./package.json'); const ok = pkg.dependencies['leaflet']==='^1.9.4' && pkg.dependencies['react-leaflet']==='^4.2.1' && pkg.devDependencies['@types/leaflet']==='^1.9.21'; if(!ok){console.error('Missing or wrong deps', { L: pkg.dependencies['leaflet'], RL: pkg.dependencies['react-leaflet'], T: pkg.devDependencies['@types/leaflet'] }); process.exit(1);} console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contiene exactamente `"leaflet": "^1.9.4"` en `dependencies`
    - `package.json` contiene exactamente `"react-leaflet": "^4.2.1"` en `dependencies`
    - `package.json` contiene exactamente `"@types/leaflet": "^1.9.21"` en `devDependencies`
    - `node_modules/leaflet/dist/leaflet.css` existe en disco después de `npm install` (`ls node_modules/leaflet/dist/leaflet.css` exits 0)
    - `node_modules/react-leaflet/package.json` reporta `"version": "4.2.1"` (no 5.x): `node -e "console.log(require('react-leaflet/package.json').version)"` imprime `4.2.1`
    - `npm install` completa exit 0 sin warnings de peer dep rotos para react/react-dom
  </acceptance_criteria>
  <done>Las 3 deps están en package.json con las versiones exactas, package-lock.json actualizado, node_modules/leaflet y node_modules/react-leaflet existen.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Importar leaflet.css ANTES de index.css en src/main.tsx</name>
  <files>src/main.tsx</files>
  <read_first>
    - src/main.tsx completo (verificar el bloque de imports actual — líneas 1-4)
    - .planning/phases/03.1-plant-map-v0/03.1-PATTERNS.md líneas 67-89 (§ 2 `src/main.tsx` — delta exacto)
    - .planning/phases/03.1-plant-map-v0/03.1-RESEARCH.md líneas 1063-1068 (Pitfall 2: "import order CSS / Leaflet → tile dimensions rotas")
  </read_first>
  <action>
    Modificar `src/main.tsx` para añadir el import del CSS de Leaflet **inmediatamente antes** de `import "./index.css";`. Estado final del bloque de imports (líneas 1-5 finales):

    ```tsx
    import { createRoot } from "react-dom/client";
    import { initPostHog } from "./lib/track";
    import App from "./App.tsx";
    import "leaflet/dist/leaflet.css";
    import "./index.css";
    ```

    Orden crítico (Pitfall 2 de RESEARCH.md): si `./index.css` se importa antes de `leaflet/dist/leaflet.css`, los overrides de `.leaflet-control-*` y `.plant-map-pin` del Plan 01 Task 3 quedan PISADOS por las reglas default de Leaflet en lugar de pisarlas. Por eso este orden es load-bearing.

    NO tocar el resto del archivo (initPostHog, SW registration, isCapacitor guard, controllerchange listener). NO modificar el orden de los otros imports.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/main.tsx','utf8');const leafletIdx=s.indexOf('leaflet/dist/leaflet.css');const indexIdx=s.indexOf('\"./index.css\"');if(leafletIdx<0){console.error('Missing leaflet css import');process.exit(1);}if(indexIdx<0){console.error('Missing index.css import');process.exit(1);}if(leafletIdx>=indexIdx){console.error('leaflet.css must come BEFORE index.css. leafletIdx='+leafletIdx+' indexIdx='+indexIdx);process.exit(1);}console.log('OK order');"</automated>
  </verify>
  <acceptance_criteria>
    - `src/main.tsx` contiene la línea exacta `import "leaflet/dist/leaflet.css";` (grep verificable)
    - La posición del string `"leaflet/dist/leaflet.css"` aparece ANTES del string `"./index.css"` en el archivo
    - `initPostHog()` sigue invocándose tras los imports
    - El bloque `if ("serviceWorker" in navigator && !isCapacitor)` sigue intacto y referenciando `isCapacitor`
    - `npm run build` (en sub-terminal de smoke) completa exit 0 sin errores de resolución del módulo
  </acceptance_criteria>
  <done>src/main.tsx tiene el import de leaflet.css en la línea inmediatamente anterior a ./index.css, sin otros cambios.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Añadir overrides CSS en src/index.css (.plant-map-pin + .leaflet-control-*)</name>
  <files>src/index.css</files>
  <read_first>
    - src/index.css completo (entender estructura — @tailwind directives + :root vars + @layer blocks)
    - .planning/phases/03.1-plant-map-v0/03.1-UI-SPEC.md líneas 815-859 (§ src/index.css MODIFY — bloque exacto)
    - .planning/phases/03.1-plant-map-v0/03.1-RESEARCH.md líneas 1069-1075 (Pitfall 3: "divIcon HTML sin override de bg → cuadrado blanco")
    - .planning/phases/03.1-plant-map-v0/03.1-PATTERNS.md línea 309 (className: "plant-map-pin" del Plan 05)
  </read_first>
  <action>
    Append al final de `src/index.css` (después del último @layer o de la última regla existente) el siguiente bloque exacto. NO modificar ni eliminar nada existente — solo añadir:

    ```css
    /* ============================================================
       Phase 03.1 — Plant Map v0 — Leaflet integration overrides
       ============================================================ */

    /* Pitfall 3 de RESEARCH.md: el divIcon default de Leaflet aplica
       .leaflet-div-icon { background: white; border: 1px solid #666 } al wrapper,
       envolviendo nuestro sticker en una caja blanca. Override obligatorio. */
    .plant-map-pin {
      background: transparent !important;
      border: none !important;
    }

    /* Plant map pin — D-10 active feedback (CONTEXT.md) */
    .plant-map-pin > div {
      cursor: pointer;
    }
    .plant-map-pin:active > div,
    .plant-map-pin:focus-within > div {
      transform: scale(1.1);
    }
    @media (prefers-reduced-motion: reduce) {
      .plant-map-pin > div {
        transition: none;
      }
      .plant-map-pin:active > div,
      .plant-map-pin:focus-within > div {
        transform: none;
      }
    }

    /* UI-SPEC line 822-824: Leaflet zoom controls hidden — touch pinch is
       sufficient on mobile; keyboard +/- still works for a11y. */
    .leaflet-control-zoom {
      display: none !important;
    }

    /* UI-SPEC line 826-829: iOS PWA safe-area-inset-right para attribution */
    .leaflet-control-attribution {
      margin-right: max(2px, env(safe-area-inset-right)) !important;
    }
    ```

    Esto cumple D-09 (sticker style sin caja blanca), D-10 (active scale), UI-SPEC línea 822 (zoom controls hidden), UI-SPEC línea 826 (safe-area-right). Reduced-motion respetado per accessibility contract de UI-SPEC.

    NO añadir el bloque comentado de re-estilar `.leaflet-control-attribution { background ... color ... }` — UI-SPEC línea 832-840 lo deja como opcional-discretion; en este plan lo omitimos (lock per researcher recomendation default).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const css=fs.readFileSync('src/index.css','utf8');const needs=['.plant-map-pin','background: transparent !important','.plant-map-pin:active > div','.leaflet-control-zoom','display: none !important','.leaflet-control-attribution','env(safe-area-inset-right)','prefers-reduced-motion'];const missing=needs.filter(s=>!css.includes(s));if(missing.length){console.error('Missing in index.css:',missing);process.exit(1);}console.log('OK all 8 strings present');"</automated>
  </verify>
  <acceptance_criteria>
    - `src/index.css` contiene la regla `.plant-map-pin { background: transparent !important; border: none !important; }` (verbatim)
    - `src/index.css` contiene la regla `.plant-map-pin:active > div, .plant-map-pin:focus-within > div { transform: scale(1.1); }`
    - `src/index.css` contiene el bloque `@media (prefers-reduced-motion: reduce)` con override de `.plant-map-pin > div` a `transition: none` y `transform: none`
    - `src/index.css` contiene `.leaflet-control-zoom { display: none !important; }`
    - `src/index.css` contiene `.leaflet-control-attribution { margin-right: max(2px, env(safe-area-inset-right)) !important; }`
    - Las reglas existentes anteriores al bloque añadido no han sido modificadas (`git diff` muestra solo additions al final)
    - `npm run build` completa exit 0 (PostCSS/Tailwind no se rompen)
  </acceptance_criteria>
  <done>El bloque CSS de overrides de Leaflet está al final de src/index.css y respeta D-09, D-10 y prefers-reduced-motion. Nada existente borrado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local-package-install | npm registry → node_modules (paquetes verificados en RESEARCH.md, sin telemetría conocida) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-01 | Tampering | npm dependencies (leaflet, react-leaflet) | accept | Versiones lockeadas con `^` minor en RESEARCH.md tras verificación con `npm view`; `package-lock.json` versionado. Cualquier reemplazo malicioso requeriría compromiso del registry + flip de lockfile (improbable, mismo riesgo que el resto del stack). |
| T-03.1-02 | Information disclosure | CSS overrides globales en index.css | accept | Solo reglas estéticas (background, transform, display, margin). No exponen datos. |
| T-03.1-03 | Denial of service | Bundle size de leaflet (+42KB gzipped) | accept | RESEARCH.md verificó <60KB total (constraint SPEC.md). Lazy-load del CSS posible en v1+ si emerge problema. |
</threat_model>

<verification>
- `npm install` exit 0 sin warnings de peer deps
- `node_modules/leaflet`, `node_modules/react-leaflet`, `node_modules/@types/leaflet` existen
- `src/main.tsx` import order verificable por offset string en archivo
- `src/index.css` contiene las 5 reglas (plant-map-pin transparent, active scale, reduced-motion, control-zoom display:none, attribution safe-area-right)
- `npm run build` smoke completa sin errores de resolución
- Cross-platform: el CSS añadido es no-op en plataformas que no monten un mapa (estado actual antes de Plan 05). En Android/iOS los selectores `env(safe-area-inset-right)` resuelven a 0 cuando no aplica.
</verification>

<success_criteria>
- 3 deps instaladas con versiones exactas (^1.9.4, ^4.2.1, ^1.9.21)
- src/main.tsx tiene `import "leaflet/dist/leaflet.css";` antes de `import "./index.css";`
- src/index.css contiene los overrides de `.plant-map-pin` y `.leaflet-control-*`
- `npm run build` completa sin errores
- Zero código React/TSX nuevo en este plan
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-plant-map-v0/03.1-01-SUMMARY.md`
</output>
