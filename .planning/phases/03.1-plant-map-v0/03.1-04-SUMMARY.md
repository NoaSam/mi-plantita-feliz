---
phase: 03.1-plant-map-v0
plan: 04
status: complete
completed: 2026-05-16
requirements_addressed: [SPEC-R1, SPEC-AC1, SPEC-AC2, SPEC-AC3]
---

# Plan 03.1-04 — BottomTabBar refactor + dispatch CustomEvent · SUMMARY

## What was built

**Task 1 — `src/components/BottomTabBar.tsx` (refactor):**
Reemplazo de `publicTabs`/`authTabs` hard-coded por `useMemo` que computa el array reactivamente desde `useAuth()` + `useContextCounts()`. Tab `"Mapa"` (icono `MapPin` de lucide-react, path `/mapa`) insertada SOLO si `user && wild_with_coords >= 1`. Render JSX byte-identical al original (preserva `<nav>` wrapper con clases, aria-label, aria-current, strokeWidth, `pb-safe`).

**Task 2 — `src/hooks/use-classify-plant.ts` (dispatch):**
Añadido `window.dispatchEvent(new CustomEvent("mp:pending-classification-resolved", { detail: { plant_search_id, action } }))` tras UPDATE exitoso en `classify` Y `revert`. Cierra landmine #4 (`useContextCounts` ya escucha este evento, pero antes nadie lo dispatchaba en el flujo de clasificación). SSR guard `typeof window !== "undefined"`.

## Files changed

- `src/components/BottomTabBar.tsx` — refactor completo (21 insertions, 12 deletions)
- `src/hooks/use-classify-plant.ts` — añadidas 14 líneas (2 dispatch blocks)

## Verification

- `npx tsc --noEmit` → exit 0
- `npm test -- --run` → 107/107 tests pasando (incluye los 8 tests de useContextCounts del Plan 02 y los 7 de useWildPlantsWithCoords del Plan 03)
- `node` check verifica: useMemo, useContextCounts import, MapPin import, tab "Mapa", path /mapa, gate user && wild_with_coords >= 1, no publicTabs/authTabs residuales, nav className intacto, aria-label preserved
- `node` check verifica: 2 dispatchEvent, 2 event-name strings, 2 SSR guards, detail payload presente

## Decisiones honradas

- **D-04**: tab order `Inicio · [Mapa cond] · Mis plantas · [Ajustes cond]`
- **D-07**: anon nunca ve la tab Mapa
- **Landmine #2** (PATTERNS.md): NO bloquea render del bar entero por isLoading; las tabs base se muestran siempre, la tab Mapa simplemente no se inserta hasta que el count se resuelve
- **Pattern S4**: mismo channel `mp:pending-classification-resolved` que `AuthContext` usa en flujo anon→auth (consistency)

## Cross-platform

`window.dispatchEvent` + `CustomEvent` son estándar Web API soportados en Capacitor 8 WebView Android y Safari 16+ PWA standalone. Sin polyfills necesarios.

## Commits

- `f93d5b7` feat(03.1-04): refactor BottomTabBar — tabs computadas via useMemo + useContextCounts
- `530adf6` feat(03.1-04): dispatch mp:pending-classification-resolved tras classify/revert
