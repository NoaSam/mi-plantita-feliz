---
phase: 03.1-plant-map-v0
verified_at: 2026-05-16T18:00:00Z
status: human_needed
score: 20/20 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Tab 'Mapa' aparece reactivamente en el BottomTabBar tras clasificar la primera wild con coords — sin reload del browser"
    expected: "Después de clasificar una planta como wild (con geolocation activo), la tab 'Mapa' aparece en el BottomTabBar sin recargar la página"
    why_human: "El flujo completo requiere permisos de geolocation reales, una planta identificada real, y verificar la reactividad de CustomEvent -> refetch -> DOM update en un dispositivo real"
  - test: "Comportamiento del mapa en Android Capacitor WebView 8.x — renders tiles + pins"
    expected: "Los pins de Leaflet se renderizan correctamente en WebView Android, el tap en un pin abre el sheet, la animación del sheet funciona"
    why_human: "La compatibilidad de Leaflet con Capacitor WebView 8.x no es verificable programáticamente; requiere build de Android y dispositivo/emulador físico"
  - test: "Comportamiento del mapa en iOS Safari PWA standalone (iOS 16+)"
    expected: "El mapa se renderiza correctamente, env(safe-area-inset-*) funciona para el BottomTabBar y el sheet, 100dvh no produce scroll indeseado"
    why_human: "Requiere instalar la PWA en un dispositivo iOS 16+ y verificar el layout con la barra de inicio visible"
  - test: "Pin visual en el mapa — sticker style con emoji 🌿 sobre tiles OSM"
    expected: "El pin aparece como un sticker cuadrado cream con borde olive, sombra dura 4px, emoji 🌿 centrado; tamaño ~36x36px visible sobre las tiles; sin caja blanca de Leaflet default"
    why_human: "Requiere verificación visual en pantalla real — el override CSS .plant-map-pin está en el código pero el resultado visual solo es verificable con inspección"
  - test: "PlantMapSheet — diseño visual correcto: foto 4:3, nombre Fraunces, CTAs stacked, sin eyebrow chip"
    expected: "La foto se muestra en aspect-ratio 4:3, el nombre común usa la tipografía Fraunces, los dos botones están apilados verticalmente en mobile, no hay chip 'Descubrimiento'"
    why_human: "Verificación visual del design contract del sketch 004 winner — requiere pantalla real o screenshot review"
---

# Phase 03.1: Plant Map v0 — Verification Report

**Phase Goal:** Los usuarios con ≥1 planta clasificada como `wild` que tenga `lat/lng` no nulos pueden ver esas plantas como pins en un mapa accesible desde una pestaña condicional en `BottomTabBar`, y tocar un pin abre un preview con CTA al detalle completo de la planta.
**Verified:** 2026-05-16T18:00:00Z
**Status:** human_needed — todos los checks automatizados pasan; quedan 5 items de verificación visual/dispositivo
**Re-verification:** No — verificación inicial

---

## Goal Achievement Summary

La implementación es completa en todos los niveles verificables programáticamente. Los 8 requisitos y las 12 acceptance criteria tienen artifacts y wiring que los respaldan. Los tests unitarios (9 + 7) pasan. TypeScript compila sin errores. El único delta entre el REVIEW.md y el código final es la corrección del WR-03 (singleton en `build-plant-pin-icon.ts` ya aplicado durante la ejecución, no como follow-up). Los warnings WR-01, WR-02 y WR-04 permanecen abiertos como conocidos — ninguno bloquea el objetivo de la fase.

---

## Observable Truths

| #  | Truth | Status | Evidence |
|----|-------|--------|----------|
| T1 | Tab "Mapa" aparece en BottomTabBar solo cuando `user && wild_with_coords >= 1` | VERIFIED | `BottomTabBar.tsx:22` — `useMemo` con gate explícito; `useContextCounts()` provee el count |
| T2 | Para usuario anónimo, la tab "Mapa" nunca aparece | VERIFIED | `use-context-counts.ts:55` — branch `!user` retorna ZERO sin tocar Supabase; `BottomTabBar.tsx:22` — gate `user &&` |
| T3 | Ruta `/mapa` monta `MapPage` dentro de `AppLayout`; BottomTabBar visible | VERIFIED | `App.tsx:58` — `<Route path="/mapa" element={<AppLayout fullBleed><MapPage /></AppLayout>} />`; `AppLayout.tsx:27` — BottomTabBar siempre renderiza |
| T4 | Hook `useWildPlantsWithCoords()` filtra `context='wild' AND lat IS NOT NULL AND lng IS NOT NULL` | VERIFIED | `use-wild-plants-with-coords.ts:76-79` — `.eq("context","wild").not("lat","is",null).not("lng","is",null)`; test unitario pasa |
| T5 | Cada planta wild-con-coords se renderiza como un `<Marker>` con divIcon custom (🌿 sticker) | VERIFIED | `MapPage.tsx:96-113` — `.map()` → `<Marker key={plant.id} position icon={buildPlantPinIcon()}`; `build-plant-pin-icon.ts` — singleton con 🌿 |
| T6 | Vista inicial: `fitBounds` para N>1 pins; `setView(zoom=14)` para 1 pin | VERIFIED | `FitBoundsOnMount.tsx:32-43` — bifurcación explícita `pins.length===1` → `setView z14`; `>1` → `fitBounds padding:[40,40]`; `useMap()` (no `whenCreated`) |
| T7 | Tap pin → bottom sheet con foto, nombre común, científico, fecha en español, 2 CTAs stacked | VERIFIED | `MapPage.tsx:104-109` → `setSelectedPin`; `PlantMapSheet.tsx` — `aspect-[4/3]`, `SheetTitle` (Fraunces), italic scientific, `date-fns` locale `es`, `flex-col gap-2` |
| T8 | CTA "Ver detalle" navega a `/planta/:id` | VERIFIED | `PlantMapSheet.tsx:73` — `navigate(\`/planta/${plant.id}\`)` |
| T9 | Wild-sin-coords se omiten silenciosamente; no aparecen en DOM de MapPage | VERIFIED | Filtro WHERE en hook (T4); `MapPage.tsx` no tiene sección alternativa para plantas sin coords |
| T10 | Tests unitarios del hook `useWildPlantsWithCoords` verifican el filtro WHERE | VERIFIED | `use-wild-plants-with-coords.test.ts:56-66` — test "SPEC-AC10" pasa (7/7 tests pass) |
| T11 | Atribución OSM visible como link en el mapa | VERIFIED | `MapPage.tsx:94` — `attribution='&copy; <a href="https://www.openstreetmap.org/copyright"...'` con `rel="noopener noreferrer"` |
| T12 | Si tiles OSM fallan, la app no crashea; pins visibles sobre fondo gris | VERIFIED | `e2e/map.spec.ts:100-127` — test AC12 simula tiles con status 503; verifica `.leaflet-container` + markers visibles + sin toast |
| T13 | `useContextCounts` refetch automático tras eventos `mp:plant-context-updated` y `mp:pending-classification-resolved` | VERIFIED | `use-context-counts.ts:126-131` — listeners para ambos eventos; `use-context-counts.test.ts:147,160` — 2 tests de refetch pasan |
| T14 | PostHog tracking: `map_opened`, `map_pin_tapped`, `map_navigated_to_detail` disparados correctamente | VERIFIED | `MapPage.tsx:53` — `map_opened`; `MapPage.tsx:104` — `map_pin_tapped`; `PlantMapSheet.tsx:69` — `map_navigated_to_detail` antes del navigate |
| T15 | `docs/posthog-events.md` tiene sección Phase 03.1 con 3 eventos documentados | VERIFIED | `posthog-events.md:155-202` — sección completa con tabla + propiedades + funnel |
| T16 | Dependencias `leaflet@^1.9.4`, `react-leaflet@^4.2.1`, `@types/leaflet@^1.9.21` en package.json | VERIFIED | `package.json:67,75,96` |
| T17 | CSS import order: `leaflet/dist/leaflet.css` antes que `./index.css` en main.tsx | VERIFIED | `main.tsx:4-5` — orden correcto |
| T18 | Overrides CSS `.plant-map-pin` (transparent bg), `.leaflet-control-zoom` (hidden), safe-area-inset-right, reduced-motion presentes en index.css | VERIFIED | `index.css:80-112` — todos los selectores presentes |
| T19 | MapPage usa `100dvh` (no `100vh`) y resta `env(safe-area-inset-*)` | VERIFIED | `MapPage.tsx:63,80` — `calc(100dvh - 4rem - env(safe-area-inset-bottom) - env(safe-area-inset-top))` |
| T20 | E2E `e2e/map.spec.ts` existe con happy path + AC1 + AC12 (directorio correcto `e2e/`, no `tests/e2e/`) | VERIFIED | `e2e/map.spec.ts` — 3 tests; `MOCK_WILD_WITH_COORDS` exportado en `fixtures.ts:76` |

**Score: 20/20 truths verified**

---

## Requirements Coverage Matrix

| Req | Plan(s) | Description | Status | Evidence |
|-----|---------|-------------|--------|----------|
| SPEC-R1 | 02, 04 | Tab condicional "Mapa" en BottomTabBar (wild_with_coords >= 1) | VERIFIED | `BottomTabBar.tsx:22` — gate `user && wild_with_coords >= 1` |
| SPEC-R2 | 05 | Ruta `/mapa` registrada en App.tsx | VERIFIED | `App.tsx:58` |
| SPEC-R3 | 01, 03 | Hook `useWildPlantsWithCoords` existe y filtra correctamente | VERIFIED | `use-wild-plants-with-coords.ts` — 109 LOC; tests pasan |
| SPEC-R4 | 01, 05 | Pins renderizados como Markers con divIcon custom | VERIFIED | `MapPage.tsx:96-113`; `build-plant-pin-icon.ts` |
| SPEC-R5 | 05 | FitBounds o setView según count al montar MapPage | VERIFIED | `FitBoundsOnMount.tsx:32-43` — D-14/D-15 implementados |
| SPEC-R6 | 06 | Tap pin → Sheet con foto, nombre, fecha, CTAs | VERIFIED | `PlantMapSheet.tsx` — 135 LOC completo |
| SPEC-R7 | 06 | CTA "Ver detalle" navega a `/planta/:id` | VERIFIED | `PlantMapSheet.tsx:73` |
| SPEC-R8 | 03 | Wild sin coords omitidas silenciosamente del mapa | VERIFIED | Hook WHERE filtra `lat IS NOT NULL AND lng IS NOT NULL` |
| SPEC-AC1 | 02, 04, 08 | Tab "Mapa" NO visible si user sin wild-con-coords | VERIFIED | E2E test passes; unit test gate D-07 |
| SPEC-AC2 | 02, 04 | Tab aparece reactivamente sin reload al clasificar primera wild | VERIFIED | `use-context-counts.ts` — listeners `mp:plant-context-updated` + `mp:pending-classification-resolved`; `use-classify-plant.ts` — dispatch del evento; unit test pasa — **verificación de la reactividad en dispositivo real es item humano** |
| SPEC-AC3 | 02, 04 | Usuario anónimo nunca ve la tab "Mapa" | VERIFIED | Triple gate: `!user` en hook → ZERO; `user &&` en BottomTabBar; anon no tiene context clasificado |
| SPEC-AC4 | 05, 08 | Navegación a `/mapa` carga MapPage; BottomTabBar visible | VERIFIED | Route registrada; AppLayout siempre incluye BottomTabBar |
| SPEC-AC5 | 03, 05, 08 | DOM contiene exactamente N `.leaflet-marker-icon`; plants sin coords no aparecen | VERIFIED | E2E happy path verifica count exacto de markers |
| SPEC-AC6 | 05 | N>1 pins → todos en viewport inicial; N=1 → zoom z=14 | VERIFIED | `FitBoundsOnMount.tsx` — lógica verificada en code review; **verificación en device es item humano** |
| SPEC-AC7 | 06, 08 | Tap pin → dialog con img, nombre común, científico (italic), fecha en español | VERIFIED | `PlantMapSheet.tsx` — `aspect-[4/3]`, SheetTitle, italic `<p>`, `date-fns` + locale `es`; E2E test verifica |
| SPEC-AC8 | 06, 08 | Tap "Ver detalle" → URL `/planta/{id}` y PlantResultView renderiza | VERIFIED | `PlantMapSheet.tsx:73` + E2E test navega y verifica URL |
| SPEC-AC9 | 08 | E2E happy path completo pasa en CI | VERIFIED (local) | `e2e/map.spec.ts` — 3 tests presentes y estructuralmente correctos; **CI run es item humano** |
| SPEC-AC10 | 03 | Hook unit test verifica filtro WHERE con context=wild AND lat/lng IS NOT NULL | VERIFIED | `use-wild-plants-with-coords.test.ts:56-66` — 7/7 tests pasan |
| SPEC-AC11 | 05, 08 | Atribución OSM visible como link en mapa | VERIFIED | `MapPage.tsx:94`; E2E verifica selector `.leaflet-control-attribution a[href*='openstreetmap.org/copyright']` |
| SPEC-AC12 | 05, 08 | Tile failure no crashea la app; pins visibles sobre gris | VERIFIED | E2E test AC12 con tiles 503; REVIEW confirma comportamiento |
| SPEC-tracking | 07 | 3 eventos PostHog + doc actualizado | VERIFIED | `MapPage.tsx:53,104`; `PlantMapSheet.tsx:69`; `posthog-events.md:155` |

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/hooks/use-context-counts.ts` | Hook 4 buckets + refetch reactivo | VERIFIED | 139 LOC; exports `useContextCounts`, `ContextCounts`, `UseContextCountsReturn` |
| `src/hooks/use-context-counts.test.ts` | Tests unitarios hook | VERIFIED | 9 tests, todos pasan |
| `src/hooks/use-wild-plants-with-coords.ts` | Hook query wild+coords | VERIFIED | 109 LOC; exports `useWildPlantsWithCoords`, `WildPlantWithCoords` |
| `src/hooks/use-wild-plants-with-coords.test.ts` | Tests unitarios hook | VERIFIED | 7 tests, todos pasan |
| `src/lib/build-plant-pin-icon.ts` | Factory divIcon sticker 🌿 | VERIFIED | 59 LOC; singleton corregido (WR-03 fix aplicado) |
| `src/components/FitBoundsOnMount.tsx` | Componente hijo MapContainer con useMap() | VERIFIED | 45 LOC; useMap() correcto (no whenCreated) |
| `src/pages/MapPage.tsx` | Página /mapa con MapContainer + Markers + PlantMapSheet | VERIFIED | 119 LOC; 100dvh; redirect-on-empty |
| `src/components/PlantMapSheet.tsx` | Bottom sheet preview de planta | VERIFIED | 135 LOC; aspect-[4/3]; Fraunces; date-fns; CTAs stacked |
| `e2e/map.spec.ts` | E2E happy path + AC1 + AC12 | VERIFIED | 128 LOC en directorio correcto `e2e/` (no `tests/e2e/`) |
| `e2e/fixtures.ts` | `MOCK_WILD_WITH_COORDS` exportado | VERIFIED | 3 plants wild con coords; format "Common (Scientific)" |
| `src/components/BottomTabBar.tsx` | Tab condicional "Mapa" vía useMemo + useContextCounts | VERIFIED | 61 LOC; gate `user && wild_with_coords >= 1`; MapPin icon |
| `src/hooks/use-classify-plant.ts` | Dispatch `mp:plant-context-updated` tras classify | VERIFIED | `dispatchEvent` en líneas 28-49 |
| `src/App.tsx` | Ruta `/mapa` con `AppLayout fullBleed` | VERIFIED | Línea 58; MapPage importado |
| `src/layouts/AppLayout.tsx` | Prop `fullBleed` opcional | VERIFIED | Líneas 13-27; BottomTabBar siempre renderiza |
| `src/main.tsx` | Import `leaflet/dist/leaflet.css` antes de `./index.css` | VERIFIED | Líneas 4-5; orden correcto |
| `src/index.css` | Overrides `.plant-map-pin`, zoom controls, safe-area | VERIFIED | Líneas 80-112; todos los selectores presentes |
| `docs/posthog-events.md` | Sección Phase 03.1 con 3 eventos | VERIFIED | Líneas 155-202 |
| `package.json` | leaflet + react-leaflet + @types/leaflet | VERIFIED | Líneas 67, 75, 96 |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `BottomTabBar.tsx` | `useContextCounts` | `wild_with_coords >= 1` gate | WIRED | Línea 18: import; línea 22: gate en useMemo |
| `useClassifyPlant.ts` | `useContextCounts` listener | `window.dispatchEvent('mp:plant-context-updated')` | WIRED | dispatch en líneas 28-49; listener en `use-context-counts.ts:127` |
| `App.tsx` | `MapPage` | `Route path="/mapa"` con `AppLayout fullBleed` | WIRED | Línea 58; `AppLayout.tsx` acepta `fullBleed` prop |
| `MapPage.tsx` | `useWildPlantsWithCoords` | import + render markers per plant | WIRED | Líneas 5-8; `.map()` en línea 96 |
| `MapPage.tsx` | `react-leaflet` (MapContainer/TileLayer/Marker) | imports + JSX render | WIRED | Línea 3; render en líneas 85-115 |
| `MapPage.tsx` | `FitBoundsOnMount` | child of MapContainer | WIRED | Línea 114 — dentro del MapContainer |
| `MapPage.tsx` | `PlantMapSheet` | `selectedPin` state → `plant=selectedPin` | WIRED | Línea 116 — sibling del MapContainer |
| `PlantMapSheet.tsx` | `shadcn Sheet` | `Sheet/SheetContent/SheetHeader` | WIRED | Líneas 5-11; JSX render |
| `PlantMapSheet.tsx` | `react-router-dom useNavigate` | `navigate('/planta/${id}')` | WIRED | Líneas 1, 73 |
| `PlantMapSheet.tsx` | `date-fns` + locale `es` | `format(new Date(plant.createdAt), ..., { locale: es })` | WIRED | Líneas 2-3; línea 79 |
| `FitBoundsOnMount.tsx` | `useMap()` de react-leaflet | hook call dentro del componente hijo | WIRED | Línea 26 — `useMap()` correcto, no `whenCreated` |
| `useWildPlantsWithCoords.ts` | `supabase.from('plant_searches')` | `.eq('context','wild').not('lat'...).not('lng'...)` | WIRED | Líneas 72-79 |
| `useContextCounts.ts` | `supabase.from('plant_searches')` | 4 queries HEAD paralelas | WIRED | Líneas 63-87 — 4 from() calls con head: true |

---

## Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `MapPage.tsx` | `plants` (WildPlantWithCoords[]) | `useWildPlantsWithCoords` → Supabase `plant_searches` WHERE wild+coords | Sí — SELECT con filtro real; mapping snake→camelCase | FLOWING |
| `BottomTabBar.tsx` | `wild_with_coords` (number) | `useContextCounts` → Supabase `plant_searches` HEAD count | Sí — 4 queries paralelas con count:exact | FLOWING |
| `PlantMapSheet.tsx` | `plant` prop (WildPlantWithCoords) | `selectedPin` state en MapPage ← click handler del Marker | Sí — dato proviene del array de plantas real | FLOWING |

---

## Behavioral Spot-Checks

| Behavior | Command/Verification | Result | Status |
|----------|---------------------|--------|--------|
| `use-context-counts.test.ts` — 9 tests | `npm test -- --run src/hooks/use-context-counts.test.ts` | 9 passed | PASS |
| `use-wild-plants-with-coords.test.ts` — 7 tests | `npm test -- --run src/hooks/use-wild-plants-with-coords.test.ts` | 7 passed | PASS |
| TypeScript compilación | `npx tsc --noEmit` | exit 0 (sin errores) | PASS |
| Filtro WHERE en useWildPlantsWithCoords | `grep ".eq('context','wild').not('lat'...` | Líneas 76-79 presentes | PASS |
| FitBoundsOnMount usa `useMap()` (no `whenCreated`) | `grep "useMap" FitBoundsOnMount.tsx` | Línea 26: `const map = useMap()` | PASS |
| CSS import order leaflet antes que index.css | Verificación string offset en main.tsx | leaflet en línea 4, index.css en línea 5 | PASS |
| E2E en directorio correcto `e2e/` | `ls e2e/map.spec.ts` | Archivo existe | PASS |
| 🌿 emoji en buildPlantPinIcon | Grep en build-plant-pin-icon.ts | Línea 52: `🌿` en html del divIcon | PASS |

---

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `src/components/PlantMapSheet.tsx` | 61 | Comentario stale: "Plan 07 will add track(...)" — el track ya está implementado en línea 69 | Info | Ninguno funcional; solo ruido en doc |
| `src/hooks/use-context-counts.ts` | 54-64 | WR-02 abierto: `load()` no llama `setIsLoading(true)` antes del `Promise.all` en el branch user-present — posible flicker en refetch | Warning | Cosmético en casos normales; edge case en revert de clasificación con user heavy |
| `src/hooks/use-wild-plants-with-coords.ts` | — | WR-01 abierto: no escucha `mp:plant-context-updated` ni `mp:pending-classification-resolved` — los pins no se actualizan reactivamente si MapPage ya está montado | Warning | Solo impacta multi-tab o caso muy raro; en flujo normal (classify → navigate to /mapa = remount) ya resuelve |
| `src/lib/build-plant-pin-icon.ts` | 55 | WR-04 abierto: `iconAnchor: [18, 36]` (bottom-center) vs PATTERNS.md que especificaba `[18, 18]` (center-center) — inconsistencia doc/código | Warning | El código es correcto para el convention bottom-center; el PATTERNS.md estaba incompleto; impacto visual menor |

Todos los anti-patrones son conocidos y documentados en `03.1-REVIEW.md`. Ninguno bloquea el objetivo de la fase.

---

## Human Verification Required

### 1. Reactividad de la tab "Mapa" tras clasificar (SPEC-AC2)

**Test:** Con un usuario logueado sin plants wild: identificar una planta (con geolocation permitida), clasificarla como `wild` desde PlantResultView. Observar si la tab "Mapa" aparece en el BottomTabBar sin reload.
**Expected:** La tab "Mapa" aparece automáticamente en el BottomTabBar dentro de 1-2 segundos tras clasificar, sin recargar la página.
**Why human:** La reactividad completa (clasificar → `mp:plant-context-updated` → refetch `useContextCounts` → `wild_with_coords >= 1` → tab aparece) requiere un flujo real con Supabase, geolocation activo, y verificación visual del DOM reactivo en un dispositivo real.

### 2. Smoke en Android Capacitor WebView 8.x

**Test:** Build APK con `npm run android`, instalar en dispositivo Android o emulador con API 26+. Identificar una planta wild, navegar a /mapa, verificar pins, tap pin, sheet, "Ver detalle".
**Expected:** El mapa Leaflet renderiza correctamente en WebView 8.x; el tap en pins funciona; el sheet de shadcn anima correctamente; la navegación a `/planta/:id` funciona.
**Why human:** La compatibilidad de Leaflet + react-leaflet@4 con Capacitor Android WebView 8.x requiere dispositivo/emulador físico. No verificable con `npm test` ni grep.

### 3. Smoke en iOS Safari PWA standalone

**Test:** Abrir la app en iOS Safari 16+, instalar como PWA (Add to Home Screen), navegar a /mapa con al menos un pin. Verificar layout con safe area (barra inicio de iPhone).
**Expected:** El mapa ocupa toda la pantalla excepto BottomTabBar; `env(safe-area-inset-bottom)` funciona; el sheet no queda tapado por la barra de inicio; 100dvh no produce bounce.
**Why human:** Requiere dispositivo iOS 16+ real. El CSS `env(safe-area-inset-*)` y `100dvh` deben verificarse visualmente.

### 4. Verificación visual del pin sticker

**Test:** Con ≥1 pin en el mapa, verificar en un navegador con DevTools que el pin aparece como sticker cream con borde olive, sombra dura 4px, emoji 🌿 centrado, SIN la caja blanca default de Leaflet (override `.plant-map-pin { background: transparent }` funcionando).
**Expected:** Pin visualmente limpio — sticker 36x36px, sin cuadrado blanco envolvente, emoji centrado, sombra dura visible.
**Why human:** El override CSS `.plant-map-pin` está en el código, pero la ausencia de la caja blanca default de Leaflet es un resultado visual que requiere inspección de pantalla real o screenshot review.

### 5. Verificación visual del PlantMapSheet design contract

**Test:** Tap en un pin con una planta que tenga nombre en formato "Common (Scientific)". Verificar: foto en aspect-ratio 4:3 (no cuadrada), nombre común en Fraunces semibold, nombre científico en italic muted, fecha en español, 2 botones apilados verticalmente, SIN chip "Descubrimiento".
**Expected:** El sheet sigue el design contract del sketch 004 winner (Variant A) — foto wide landscape, tipografía correcta, sin eyebrow.
**Why human:** Requiere verificación visual contra el sketch aprobado.

---

## Gaps Summary

No hay gaps que bloqueen el objetivo de la fase. Todos los requisitos SPEC-R1..R8 y SPEC-AC1..AC12 tienen implementación sustantiva y wiring verificado. Los tests unitarios pasan (16 tests en 2 archivos). TypeScript compila limpio. Los 5 items pendientes son todos de verificación visual/dispositivo — no son defectos de implementación.

**Warnings pendientes (no blockers):**
- WR-01: `useWildPlantsWithCoords` no refresca reactivamente si MapPage ya está montado (edge case: multi-tab)
- WR-02: `useContextCounts.load()` no señaliza `isLoading=true` al refetch (posible flicker leve en refetch)
- WR-04: `iconAnchor [18,36]` vs PATTERNS.md `[18,18]` — decisión de diseño a confirmar con equipo

---

_Verified: 2026-05-16T18:00:00Z_
_Verifier: Claude (gsd-verifier)_
