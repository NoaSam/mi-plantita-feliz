---
phase: 03.1-plant-map-v0
plan: 07
status: complete
completed: 2026-05-16
requirements_addressed: [tracking-contract]
---

# Plan 03.1-07 — PostHog tracking (3 events) + docs · SUMMARY

## What was built

3 commits atómicos:

**Task 1 — `src/pages/MapPage.tsx` (MODIFIED · +6/-1)**
Marker `eventHandlers.click` ahora invoca `track("map_pin_tapped", { plant_search_id, pin_index_among_total, total_pins })` ANTES de `setSelectedPin(plant)`. `plants.map((plant, index) =>` capta el índice 0-based.

**Task 2 — `src/components/PlantMapSheet.tsx` (MODIFIED · +5)**
`handleViewDetail` ahora invoca `track("map_navigated_to_detail", { plant_search_id, from: "pin_sheet" })` ANTES de `navigate(\`/planta/${plant.id}\`)`. Import de `track` añadido.

**Task 3 — `docs/posthog-events.md` (MODIFIED · +52)**
Nueva sección `## Phase 03.1 — Plant Map v0` después de Phase 02.1: tabla de los 3 eventos, detalle de props con explicación de cada campo, 2 funnels previstos (intra-fase + cross-fase 02.1→03.1).

## Files changed

- `src/pages/MapPage.tsx`
- `src/components/PlantMapSheet.tsx`
- `docs/posthog-events.md`

## Verification

- `npx tsc --noEmit` → exit 0
- `npm test -- --run` → 108/108 pasando

## Eventos finales (resumen)

| Evento | Disparo | Props |
|---|---|---|
| `map_opened` | Mount de MapPage (gated por flag, una vez por mount) | `pin_count` |
| `map_pin_tapped` | Marker click, antes de setSelectedPin | `plant_search_id`, `pin_index_among_total` (0-based), `total_pins` |
| `map_navigated_to_detail` | "Ver detalle" en sheet, antes de navigate | `plant_search_id`, `from: 'pin_sheet'` |

## Commits

- `1742d4b` feat(03.1-07): track map_pin_tapped on Marker click
- `35c7e6f` feat(03.1-07): track map_navigated_to_detail before navigate from sheet CTA
- `8bc9089` docs(03.1-07): add Phase 03.1 — Plant Map v0 section to posthog-events.md
