---
phase: 03.1-plant-map-v0
plan: 07
type: execute
wave: 4
depends_on: [05, 06]
files_modified:
  - src/pages/MapPage.tsx
  - src/components/PlantMapSheet.tsx
  - docs/posthog-events.md
autonomous: true
requirements: [SPEC-tracking]
requirements_addressed: [SPEC-tracking]
tags: [phase-03.1, posthog, tracking, analytics]

must_haves:
  truths:
    - "MapPage dispara `track('map_opened', { pin_count })` UNA sola vez por mount tras resolverse el hook (sin double-fire en re-renders)"
    - "Cada Marker click en MapPage dispara `track('map_pin_tapped', { plant_search_id, pin_index_among_total, total_pins })` antes de setSelectedPin"
    - "PlantMapSheet dispara `track('map_navigated_to_detail', { plant_search_id, from: 'pin_sheet' })` ANTES del navigate() en handleViewDetail"
    - "El doc `docs/posthog-events.md` contiene una nueva sección `## Phase 03.1 — Plant Map v0` con la tabla de eventos + detalle de propiedades + funnel previsto"
    - "Los eventos siguen el patrón snake_case + properties flat establecido en Phase 02.1"
  artifacts:
    - path: "src/pages/MapPage.tsx"
      provides: "Track call refinado de map_opened (ya estaba) + track call de map_pin_tapped en Marker click handler"
      contains: "map_pin_tapped"
    - path: "src/components/PlantMapSheet.tsx"
      provides: "Track call de map_navigated_to_detail antes de navigate"
      contains: "map_navigated_to_detail"
    - path: "docs/posthog-events.md"
      provides: "Nueva sección Phase 03.1 con 3 eventos documentados + funnel"
      contains: "## Phase 03.1 — Plant Map v0"
  key_links:
    - from: "src/pages/MapPage.tsx Marker eventHandlers.click"
      to: "@/lib/track"
      via: "track('map_pin_tapped', { plant_search_id, pin_index_among_total, total_pins })"
      pattern: "map_pin_tapped"
    - from: "src/components/PlantMapSheet.tsx handleViewDetail"
      to: "@/lib/track"
      via: "track('map_navigated_to_detail', { plant_search_id, from: 'pin_sheet' })"
      pattern: "map_navigated_to_detail"
---

<objective>
Completar el contrato de tracking de PostHog para Phase 03.1: añadir los 2 eventos pendientes (`map_pin_tapped` en el Marker click handler del Plan 05, `map_navigated_to_detail` en el handleViewDetail del Plan 06) y actualizar `docs/posthog-events.md` con una nueva sección `## Phase 03.1 — Plant Map v0` documentando los 3 eventos + funnel previsto.

El track de `map_opened` ya fue añadido en Plan 05 — este plan solo asegura que las propiedades coinciden con el contrato lockeado en RESEARCH.md § PostHog event contract (líneas 970-1014). Si Plan 05 setea `pin_count` correctamente, no requiere cambios; verificar.

Purpose: el tracking es lo que permite a la dueña del producto medir si el mapa "funciona" (funnel `map_opened → map_pin_tapped → map_navigated_to_detail`). Sin docs/posthog-events.md actualizado, futuros mantenedores no sabrán qué eventos existen. Este plan es lightweight (1 línea en MapPage, 1 línea en PlantMapSheet, ~40 líneas de docs) pero load-bearing.
Output: 2 archivos modificados (~5 LOC nuevas) + 1 doc actualizado (~50 LOC nuevas).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03.1-plant-map-v0/03.1-CONTEXT.md
@.planning/phases/03.1-plant-map-v0/03.1-RESEARCH.md
@.planning/phases/03.1-plant-map-v0/03.1-UI-SPEC.md
@.planning/phases/03.1-plant-map-v0/03.1-PATTERNS.md
@.planning/phases/03.1-plant-map-v0/03.1-05-PLAN.md
@.planning/phases/03.1-plant-map-v0/03.1-06-PLAN.md
@src/pages/MapPage.tsx
@src/components/PlantMapSheet.tsx
@src/lib/track.ts
@docs/posthog-events.md
@CLAUDE.md

<interfaces>
CONSUME:
- Plan 05: MapPage already tracks `map_opened` con `{ pin_count: plants.length }` y tiene comentarios inline marcando dónde añadir `map_pin_tapped`
- Plan 06: PlantMapSheet tiene `handleViewDetail` con comentario inline marcando dónde añadir `map_navigated_to_detail`
- `@/lib/track` exporta `track(event: string, properties?: Record<string, unknown>): void` (gated por consent; no-op si PostHog no inicializado)

CONTRATO LOCKEADO (RESEARCH.md § PostHog event contract + UI-SPEC líneas 362-369):

| Evento | Propiedades | Cuándo dispara |
|--------|-------------|----------------|
| `map_opened` | `pin_count: number` | Una vez por mount de MapPage, post-load del hook |
| `map_pin_tapped` | `plant_search_id: string`, `pin_index_among_total: number` (1-indexed o 0-indexed — locked al final), `total_pins: number` | En el Marker `eventHandlers.click` |
| `map_navigated_to_detail` | `plant_search_id: string`, `from: 'pin_sheet'` | En handleViewDetail ANTES de navigate() |

DECISIÓN INDEXING: usar `pin_index_among_total: 0` (cero-indexed) per RESEARCH.md línea 995 ("índice 0-based del pin entre los plants.length totales"). El doc lo documenta explícitamente.

ANALOG primario (docs/posthog-events.md): sección `## Phase 02.1 — Foundations (Classification + Reveal)` (verificar ubicación con grep; añadir después del último bloque § Phase 02.1 y antes del bloque "Funnel previsto" global si existe).

ANALOG código (Marker click + track): `src/components/UnclassifiedSection.tsx:26` — `track("unclassified_section_shown", { count })` después de check isLoading.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Añadir track call de map_pin_tapped en src/pages/MapPage.tsx</name>
  <files>src/pages/MapPage.tsx</files>
  <read_first>
    - src/pages/MapPage.tsx (creado en Plan 05 — buscar el comentario inline `// Plan 07 will add: track("map_pin_tapped"...`)
    - src/lib/track.ts (verificar firma exacta de track())
    - .planning/phases/03.1-plant-map-v0/03.1-RESEARCH.md líneas 982-996 (props lockeadas)
    - .planning/phases/03.1-plant-map-v0/03.1-UI-SPEC.md líneas 296-312 (Marker click handler skeleton)
  </read_first>
  <action>
    Modificar `src/pages/MapPage.tsx`:

    **Cambio 1** — verificar que `import { track } from "@/lib/track";` ya está (Plan 05 lo añadió). Si no, añadirlo.

    **Cambio 2** — modificar el `eventHandlers.click` del `<Marker>` para añadir el track call ANTES de `setSelectedPin(plant)`. Reemplazar el bloque:

    ```tsx
    {plants.map((plant) => (
      <Marker
        key={plant.id}
        position={[plant.lat, plant.lng]}
        icon={buildPlantPinIcon()}
        title={plant.name}
        eventHandlers={{
          click: () => {
            // Plan 07 will add: track("map_pin_tapped", { plant_search_id, pin_index_among_total, total_pins })
            setSelectedPin(plant);
          },
        }}
      />
    ))}
    ```

    Por la versión final con el track call + index (necesitamos el index, así que reemplazar `plants.map((plant) =>` por `plants.map((plant, index) =>`):

    ```tsx
    {plants.map((plant, index) => (
      <Marker
        key={plant.id}
        position={[plant.lat, plant.lng]}
        icon={buildPlantPinIcon()}
        title={plant.name}
        eventHandlers={{
          click: () => {
            track("map_pin_tapped", {
              plant_search_id: plant.id,
              pin_index_among_total: index,
              total_pins: plants.length,
            });
            setSelectedPin(plant);
          },
        }}
      />
    ))}
    ```

    Decisiones:
    - `pin_index_among_total: index` — 0-based (per RESEARCH.md línea 995); el doc lo documenta
    - `total_pins: plants.length` — explícito (no se infiere del max index, por si el sample size es 0)
    - Track fires ANTES del setSelectedPin (UI-SPEC línea 305 explicitly orders it antes)

    NO modificar:
    - El useEffect de map_opened (ya correcto desde Plan 05)
    - El document.title
    - El branch loading / empty / Navigate
    - El `<PlantMapSheet>` mount (añadido en Plan 06)
    - Cualquier otro track call
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/pages/MapPage.tsx','utf8');const checks={hasTrackImport:s.includes('import { track } from \"@/lib/track\"'),hasMapPinTappedCall:s.includes('track(\"map_pin_tapped\"'),hasMapOpenedCall:s.includes('track(\"map_opened\"'),hasPlantSearchId:/plant_search_id:\\s*plant\\.id/.test(s),hasPinIndex:s.includes('pin_index_among_total: index'),hasTotalPins:s.includes('total_pins: plants.length'),hasIndexInMap:/plants\\.map\\(\\(plant,\\s*index\\)\\s*=>/.test(s),hasNoPlan07Comment:!s.includes('Plan 07 will add'),trackBeforeSetSelected:(()=>{const trackIdx=s.indexOf('track(\"map_pin_tapped\"');const setIdx=s.indexOf('setSelectedPin(plant)');return trackIdx>0&&setIdx>trackIdx;})()};const fails=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);if(fails.length){console.error('FAIL:',fails);process.exit(1);}console.log('OK MapPage tracking');"</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/MapPage.tsx` contiene `import { track } from "@/lib/track"` (ya estaba o se añade)
    - `src/pages/MapPage.tsx` contiene `track("map_pin_tapped", {` con props exactas: `plant_search_id: plant.id`, `pin_index_among_total: index`, `total_pins: plants.length`
    - El `track("map_pin_tapped"...)` se invoca ANTES de `setSelectedPin(plant)` (orden importante)
    - El `plants.map(...)` ahora recibe `(plant, index) =>` (2 args) en lugar de solo `(plant) =>`
    - El `track("map_opened", { pin_count: plants.length })` sigue presente sin cambios
    - El comentario placeholder `// Plan 07 will add: ...` ha sido removido
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>map_pin_tapped trackeado con 3 props correctas, antes de setSelectedPin. map_opened intacto.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Añadir track call de map_navigated_to_detail en src/components/PlantMapSheet.tsx</name>
  <files>src/components/PlantMapSheet.tsx</files>
  <read_first>
    - src/components/PlantMapSheet.tsx (creado en Plan 06 — buscar el comentario inline `// Plan 07 will add: track("map_navigated_to_detail"...`)
    - src/lib/track.ts (firma de track())
    - .planning/phases/03.1-plant-map-v0/03.1-RESEARCH.md líneas 997-999 (props lockeadas)
    - .planning/phases/03.1-plant-map-v0/03.1-UI-SPEC.md línea 339 (track antes de navigate)
  </read_first>
  <action>
    Modificar `src/components/PlantMapSheet.tsx`:

    **Cambio 1** — añadir el import: `import { track } from "@/lib/track";` cerca de los otros imports `@/...`.

    **Cambio 2** — modificar `handleViewDetail` para añadir el track call ANTES del `navigate()`. Reemplazar:

    ```tsx
    const handleViewDetail = () => {
      if (!plant) return;
      // Plan 07 will add: track("map_navigated_to_detail", { plant_search_id: plant.id, from: "pin_sheet" })
      navigate(`/planta/${plant.id}`);
    };
    ```

    Por la versión final:

    ```tsx
    const handleViewDetail = () => {
      if (!plant) return;
      track("map_navigated_to_detail", {
        plant_search_id: plant.id,
        from: "pin_sheet",
      });
      navigate(`/planta/${plant.id}`);
    };
    ```

    Decisiones:
    - `from: "pin_sheet"` literal — RESEARCH.md línea 999 lockea este valor en v0 ("Reservado el campo por si en v1+ se añade otro punto de navegación al detalle desde el mapa")
    - Track ANTES de navigate — patrón establecido (no esperar a la nueva ruta; si user cancela rapidísimo, el evento ya está fired)

    NO modificar:
    - splitNameField helper
    - El JSX del SheetContent
    - El handleViewDetail signature (sigue sin args)
    - El onClose handler
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/components/PlantMapSheet.tsx','utf8');const checks={hasTrackImport:s.includes('import { track } from \"@/lib/track\"'),hasMapNavigatedCall:s.includes('track(\"map_navigated_to_detail\"'),hasPlantSearchIdProp:/plant_search_id:\\s*plant\\.id/.test(s),hasFromPinSheet:s.includes('from: \"pin_sheet\"'),hasNavigateCall:s.includes('navigate(`/planta/'),trackBeforeNavigate:(()=>{const trackIdx=s.indexOf('track(\"map_navigated_to_detail\"');const navIdx=s.indexOf('navigate(`/planta/');return trackIdx>0&&navIdx>trackIdx;})(),hasNoPlan07Comment:!s.includes('Plan 07 will add')};const fails=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);if(fails.length){console.error('FAIL:',fails);process.exit(1);}console.log('OK PlantMapSheet tracking');"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/PlantMapSheet.tsx` contiene `import { track } from "@/lib/track"`
    - Contiene `track("map_navigated_to_detail", { plant_search_id: plant.id, from: "pin_sheet" })`
    - El track fires ANTES de `navigate(\`/planta/${plant.id}\`)` (orden verificable por offset)
    - El comentario placeholder `// Plan 07 will add: ...` ha sido removido
    - El resto del componente (splitNameField, JSX, props) sin cambios
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>map_navigated_to_detail trackeado con props correctas, antes de navigate.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Añadir sección Phase 03.1 a docs/posthog-events.md</name>
  <files>docs/posthog-events.md</files>
  <read_first>
    - docs/posthog-events.md COMPLETO (entender estructura: H1 + intro + Phase 02.1 con tablas + Detalle de propiedades + Funnel previsto al final)
    - .planning/phases/03.1-plant-map-v0/03.1-RESEARCH.md líneas 970-1014 (snippet markdown listo para copiar — Phase 03.1 section)
    - .planning/phases/03.1-plant-map-v0/03.1-PATTERNS.md líneas 202-233 (§ 6 docs delta)
  </read_first>
  <action>
    Añadir al final de `docs/posthog-events.md`, DESPUÉS de la sección "## Phase 02.1 — Foundations (Classification + Reveal)" (incluyendo su bloque "Detalle de propiedades" y su "Funnel previsto"), un nuevo bloque que documenta los 3 eventos de Phase 03.1.

    Contenido exacto a appendear (basado en RESEARCH.md líneas 972-1014):

    ```markdown

    ---

    ## Phase 03.1 — Plant Map v0

    **Status:** Implementado en Phase 03.1. Los archivos `.tsx` son `src/pages/MapPage.tsx` y `src/components/PlantMapSheet.tsx`.

    ### Eventos del mapa de descubrimientos

    | Evento | Propiedades | Archivo | Cuándo se dispara |
    |---|---|---|---|
    | `map_opened` | `pin_count` | `src/pages/MapPage.tsx` | Usuario navega a `/mapa` y el hook `useWildPlantsWithCoords` resuelve (disparar **una vez por montaje**, gated con flag local `trackedOpen` para evitar inflar la métrica con re-renders) |
    | `map_pin_tapped` | `plant_search_id`, `pin_index_among_total`, `total_pins` | `src/pages/MapPage.tsx` | Usuario toca un pin del mapa. Disparar **antes** de `setSelectedPin` |
    | `map_navigated_to_detail` | `plant_search_id`, `from` | `src/components/PlantMapSheet.tsx` | Usuario toca "Ver detalle" en el bottom sheet del pin. Disparar **antes** del `navigate(\`/planta/${id}\`)` |

    ### Detalle de propiedades

    **`map_opened`**
    - `pin_count`: número de pins visibles en el mapa (= `wild_with_coords_count` del usuario)
    - Disparar **una sola vez por montaje** de `MapPage` (flag in-component `trackedOpen`) para evitar inflar la métrica con re-renders por state changes (selectedPin, etc.)

    **`map_pin_tapped`**
    - `plant_search_id`: UUID de la fila en `plant_searches` correspondiente al pin tocado
    - `pin_index_among_total`: índice **0-based** del pin entre los `plants.length` totales. El hook ordena por `created_at desc`, por lo que `index === 0` siempre es el descubrimiento más reciente. Permite analizar si los usuarios prefieren los pins recientes (índice bajo) o los antiguos.
    - `total_pins`: cardinalidad total de pins en el mapa cuando se hizo el tap. Permite normalizar `pin_index_among_total` y separar comportamiento de usuarios con pocos vs muchos descubrimientos.

    **`map_navigated_to_detail`**
    - `plant_search_id`: UUID de la fila
    - `from`: literal `'pin_sheet'` en v0. Campo reservado por si en v1+ se añade otro punto de entrada al detalle desde el mapa (ej. swipe-to-detail, long-press, mini-card en el header).

    ### Funnel previsto (para análisis posterior)

    Funnel de descubrimiento → exploración:
    ```
    plant_identified (con has_location: true)
      → classification_action_clicked (action: 'wild')
      → classification_completed
      → (visita futura) map_opened
      → map_pin_tapped
      → map_navigated_to_detail        [conversión clave: el mapa lleva al detalle]
    ```

    Funnel cross-fase (Phase 02.1 + 03.1):
    ```
    plant_identified
      → classification_action_clicked (action: 'wild')
      → classification_completed
      → (tab "Mapa" aparece reactivamente — sin event explícito, pero útil para diagnostico)
      → map_opened (con pin_count >= 1)
      → map_pin_tapped (con pin_index_among_total: 0 — el descubrimiento que acaban de hacer)
      → map_navigated_to_detail (from: 'pin_sheet')
    ```
    ```

    NOTA: el snippet anterior usa triple-backtick literal de markdown. Asegurar que el archivo final renderiza correctamente como markdown (sin escape de backticks).

    NO modificar:
    - El bloque H1 inicial
    - La sección Phase 02.1 (incluyendo sus subseccciones "Eventos de clasificación...", "Detalle de propiedades", "Funnel previsto")
    - Cualquier otro contenido pre-existente

    Posición: el separador `---` antes del nuevo H2 es importante — replica el separador entre H2s de la sección Phase 02.1.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('docs/posthog-events.md','utf8');const checks={hasPhase02_1:s.includes('## Phase 02.1 — Foundations'),hasPhase03_1Heading:s.includes('## Phase 03.1 — Plant Map v0'),hasMapOpenedRow:s.includes('| \`map_opened\`'),hasMapPinTappedRow:s.includes('| \`map_pin_tapped\`'),hasMapNavToDetailRow:s.includes('| \`map_navigated_to_detail\`'),hasPinCountProp:s.includes('\`pin_count\`'),hasPlantSearchIdProp:s.includes('\`plant_search_id\`'),hasPinIndexAmongTotal:s.includes('\`pin_index_among_total\`'),hasTotalPins:s.includes('\`total_pins\`'),hasFromPinSheet:s.includes(\"'pin_sheet'\"),hasFunnelSection:s.includes('Funnel previsto'),phase03AfterPhase02:(()=>{const p2=s.indexOf('## Phase 02.1');const p3=s.indexOf('## Phase 03.1');return p2>=0&&p3>p2;})(),preservesPhase02Body:s.includes('classification_action_clicked')};const fails=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);if(fails.length){console.error('FAIL:',fails);process.exit(1);}console.log('OK posthog-events doc');"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/posthog-events.md` contiene la sección `## Phase 03.1 — Plant Map v0` (heading exacto)
    - La sección Phase 03.1 aparece DESPUÉS de la sección `## Phase 02.1 — Foundations`
    - Contiene una tabla con los 3 eventos: `map_opened`, `map_pin_tapped`, `map_navigated_to_detail`
    - Contiene subsección `### Detalle de propiedades` con detalles de los 3 eventos
    - Detalla las props: `pin_count`, `plant_search_id`, `pin_index_among_total` (con nota "0-based"), `total_pins`, `from` (con nota literal `'pin_sheet'`)
    - Contiene la subsección `### Funnel previsto` con al menos 1 bloque de funnel
    - La sección Phase 02.1 NO ha sido modificada (verificable por grep de strings característicos como `classification_action_clicked`)
    - El doc completo sigue siendo markdown válido (no errores de sintaxis)
  </acceptance_criteria>
  <done>docs/posthog-events.md actualizado con sección Phase 03.1 completa: tabla de eventos + detalle de props + funnel previsto. Sin tocar Phase 02.1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→PostHog (EU) | Eventos con props se envían al endpoint configurado de PostHog, gated por consent del user (cookie banner) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-23 | Information disclosure | plant_search_id (UUID) enviado a PostHog | accept | UUID no es PII per se. Combinado con `user.id` del PostHog identity, permitiría linkear comportamiento a usuario — pero eso es exactamente el propósito del tracking (analytics gated por consent). Verificable en docs/posthog-events.md sección que ya describe pattern Phase 02.1 análogo. |
| T-03.1-24 | DoS | Spam de map_pin_tapped si user fast-taps muchos pins | accept | `track()` es async fire-and-forget no-op si !consent; PostHog SDK ya tiene rate-limiting interno. Sin riesgo. |
| T-03.1-25 | Tampering | Cliente puede enviar props arbitrarias modificando local code (DevTools) | accept | PostHog analytics, no security boundary. Worst case: data poisoning de propias métricas. Sin impacto en seguridad de otros users. |
</threat_model>

<verification>
- MapPage.tsx contiene `track("map_pin_tapped", ...)` con 3 props correctas, antes de setSelectedPin
- PlantMapSheet.tsx contiene `track("map_navigated_to_detail", ...)` con 2 props, antes de navigate
- docs/posthog-events.md contiene sección `## Phase 03.1 — Plant Map v0` con tabla + detalle + funnel
- `npx tsc --noEmit` exit 0
- `npm run build` exit 0
- No regresión en `npm test -- --run`
- Cross-platform: `track()` es browser-agnostic; PostHog SDK soportado en Capacitor WebView y iOS PWA
</verification>

<success_criteria>
- 3 eventos del contrato (map_opened, map_pin_tapped, map_navigated_to_detail) implementados
- Props snake_case correctas con tipos esperados
- Eventos disparan en el momento correcto (antes de side effects)
- Documentación legible en docs/posthog-events.md
- Sin regresión de código existente
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-plant-map-v0/03.1-07-SUMMARY.md`
</output>
