---
phase: 03.1-plant-map-v0
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/use-context-counts.ts
  - src/hooks/use-context-counts.test.ts
autonomous: true
requirements: [SPEC-R1, SPEC-AC1, SPEC-AC2, SPEC-AC3]
requirements_addressed: [SPEC-R1, SPEC-AC1, SPEC-AC2, SPEC-AC3]
tags: [phase-03.1, hooks, context-counts, bottom-tab-bar]

must_haves:
  truths:
    - "Existe un hook `useContextCounts()` que devuelve los 4 buckets {home, wild_with_coords, wild_without_coords, unclassified} más isLoading y refetch"
    - "Para usuario no logueado, el hook devuelve todos los counts en 0 sin llamar a Supabase"
    - "Para usuario logueado, el hook ejecuta queries filtradas por user_id y devuelve los counts correctos del bucket correspondiente"
    - "El hook escucha el evento `mp:pending-classification-resolved` y refetcha automáticamente cuando se dispara (cubre SPEC-AC2)"
    - "Existe un test unitario que cubre: filtro user_id, 4 buckets, edge case sin user (zeros), edge case mixto wild con/sin coords"
  artifacts:
    - path: "src/hooks/use-context-counts.ts"
      provides: "Hook agregador de los 4 buckets de plant_searches por context + lat/lng presence"
      exports: ["useContextCounts", "ContextCounts", "UseContextCountsReturn"]
      min_lines: 80
    - path: "src/hooks/use-context-counts.test.ts"
      provides: "Tests unitarios del hook con Supabase mock"
      contains: "describe(\"useContextCounts\""
      min_lines: 60
  key_links:
    - from: "src/hooks/use-context-counts.ts"
      to: "supabase.from('plant_searches')"
      via: "4 queries paralelas con count: exact, head: true"
      pattern: "from\\(\"plant_searches\"\\)"
    - from: "src/hooks/use-context-counts.ts"
      to: "window event mp:pending-classification-resolved"
      via: "addEventListener en useEffect"
      pattern: "mp:pending-classification-resolved"
    - from: "src/hooks/use-context-counts.ts"
      to: "src/hooks/use-auth.ts"
      via: "useAuth() para obtener user.id"
      pattern: "useAuth"
---

<objective>
Crear un hook React `useContextCounts()` NUEVO en `src/hooks/use-context-counts.ts` que devuelve los 4 buckets del usuario logueado: `{ home, wild_with_coords, wild_without_coords, unclassified }` + `isLoading` + `refetch`. Replicar verbatim el patrón establecido en `src/hooks/use-unclassified-count.ts` (mountedRef guard, useAuth, useCallback, useEffect, manejo de error con clear-to-zero) per Pattern S3 de PATTERNS.md. Hacer que el hook escuche `mp:pending-classification-resolved` y refetche automáticamente para cubrir SPEC-AC2 (la tab "Mapa" aparece reactivamente al clasificar wild sin reload).

Tests unitarios siblings (`use-context-counts.test.ts`) que validen los counts, el filtro por `user_id`, y el branching wild-with vs wild-without coords.

Purpose: este hook es la fuente de verdad para la visibilidad condicional de la tab "Mapa" (Plan 04) y para futuras superficies (HistorySummary, Phase 3 Mi jardín). El planner decisión per RESEARCH.md § Open Question 2: hook NUEVO en lugar de extender `useUnclassifiedCount` (más DRY a futuro, separa concerns).
Output: 1 hook nuevo (~90 LOC) + 1 test file (~110 LOC).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-plant-map-v0/03.1-CONTEXT.md
@.planning/phases/03.1-plant-map-v0/03.1-SPEC.md
@.planning/phases/03.1-plant-map-v0/03.1-RESEARCH.md
@.planning/phases/03.1-plant-map-v0/03.1-PATTERNS.md
@src/hooks/use-unclassified-count.ts
@src/hooks/use-auth.ts
@src/hooks/use-plant-identifier.test.ts
@src/integrations/supabase/client.ts
@CLAUDE.md

<interfaces>
<!-- API CONTRACT que este plan crea para que el Plan 04 (BottomTabBar) lo consuma: -->

```typescript
// src/hooks/use-context-counts.ts (NEW — created by this plan)

export interface ContextCounts {
  home: number;
  wild_with_coords: number;
  wild_without_coords: number;
  unclassified: number;
}

export interface UseContextCountsReturn extends ContextCounts {
  isLoading: boolean;
  refetch: () => Promise<void>;
}

export function useContextCounts(): UseContextCountsReturn;
```

<!-- ANALOG verbatim a replicar (estructura idéntica salvo por las 4 queries): -->
<!--   src/hooks/use-unclassified-count.ts líneas 1-79 -->
<!--   Pattern S3 mountedRef guard (PATTERNS.md líneas 794-809) -->
<!--   Pattern S4 cross-component invalidation via window CustomEvent (PATTERNS.md líneas 813-833) -->

<!-- Test pattern reference (vi.mock chainable): -->
<!--   PATTERNS.md líneas 668-682 — mockSupabaseSelect helper -->
<!--   src/hooks/use-plant-identifier.test.ts líneas 1-9 — vi.mock + renderHook + act -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear hook src/hooks/use-context-counts.ts con 4 queries paralelas + listener de eventos</name>
  <files>src/hooks/use-context-counts.ts</files>
  <read_first>
    - src/hooks/use-unclassified-count.ts (analog PRIMARIO — replicar shell verbatim: mountedRef pattern WR-02, useAuth, useCallback load, useEffect)
    - src/hooks/use-auth.ts (para entender el shape de `useAuth()` — devuelve `{ user, ... }` donde `user` es `User | null`)
    - src/integrations/supabase/client.ts (para entender el cliente exportado como `supabase`)
    - .planning/phases/03.1-plant-map-v0/03.1-PATTERNS.md líneas 570-619 (§ 12 `use-context-counts.ts` — delta exacto + recomendación de 4 queries paralelas con `head: true`)
    - .planning/phases/03.1-plant-map-v0/03.1-RESEARCH.md líneas 600-676 (§ Implementation guidance → R1 skeleton, AC2 refetch options)
    - .planning/phases/03.1-plant-map-v0/03.1-CONTEXT.md D-06, D-07 (shape lockeado, auth gating)
  </read_first>
  <behavior>
    - Test 1: Cuando `useAuth()` devuelve `user: null`, el hook devuelve `{ home: 0, wild_with_coords: 0, wild_without_coords: 0, unclassified: 0, isLoading: false }` sin llamar a supabase
    - Test 2: Cuando hay user logueado, el hook ejecuta 4 queries paralelas y devuelve los counts correctos
    - Test 3: Cada query filtra por `user_id = user.id` (verificar con spy en `.eq`)
    - Test 4: La query de `wild_with_coords` filtra adicionalmente con `.eq("context","wild").not("lat","is",null).not("lng","is",null)`
    - Test 5: La query de `wild_without_coords` filtra con `.eq("context","wild").or("lat.is.null,lng.is.null")`
    - Test 6: Si una query falla, todos los counts se setean a 0 y `isLoading` queda en false (graceful degradation)
    - Test 7: El hook re-ejecuta `load` cuando se dispara `window.dispatchEvent(new CustomEvent("mp:pending-classification-resolved"))`
  </behavior>
  <action>
    Crear `src/hooks/use-context-counts.ts` con el contenido exacto siguiente (replicando verbatim el shell de `use-unclassified-count.ts` líneas 1-79 + adiciones para 4 buckets y event listener):

    ```typescript
    import { useCallback, useEffect, useRef, useState } from "react";
    import { supabase } from "@/integrations/supabase/client";
    import { useAuth } from "@/hooks/use-auth";

    export interface ContextCounts {
      home: number;
      wild_with_coords: number;
      wild_without_coords: number;
      unclassified: number;
    }

    export interface UseContextCountsReturn extends ContextCounts {
      isLoading: boolean;
      refetch: () => Promise<void>;
    }

    const ZERO: ContextCounts = {
      home: 0,
      wild_with_coords: 0,
      wild_without_coords: 0,
      unclassified: 0,
    };

    /**
     * Hook agregador de los 4 buckets de plant_searches del usuario actual.
     *
     * Para anónimos (user === null) devuelve siempre ZERO sin tocar Supabase
     * (per D-07: tabs nuevas nunca aparecen para anonymous; sus searches no
     * tienen `context` clasificado).
     *
     * Usa 4 queries paralelas con `count: "exact", head: true` (sin transfer
     * de filas; solo el header `Content-Range`) — más eficiente que select
     * completo + agregación en cliente.
     *
     * Refetch automático cuando se dispara `mp:pending-classification-resolved`
     * (Pattern S4 de PATTERNS.md) — cubre SPEC-AC2: la tab "Mapa" aparece
     * reactivamente al clasificar la primera wild con coords. (El dispatch del
     * evento se añade en Plan 04 a useClassifyPlant.ts.)
     */
    export function useContextCounts(): UseContextCountsReturn {
      const { user } = useAuth();
      const [counts, setCounts] = useState<ContextCounts>(ZERO);
      const [isLoading, setIsLoading] = useState(true);
      // WR-02 fix: ref-tracked mounted boolean — patrón verbatim de
      // use-unclassified-count.ts líneas 24-33.
      const mountedRef = useRef(true);
      useEffect(() => {
        mountedRef.current = true;
        return () => {
          mountedRef.current = false;
        };
      }, []);

      const load = useCallback(async () => {
        if (!user) {
          if (!mountedRef.current) return;
          setCounts(ZERO);
          setIsLoading(false);
          return;
        }

        // 4 head-only count queries en paralelo (no transfer de rows).
        const [home, wildWith, wildWithout, unclassified] = await Promise.all([
          supabase
            .from("plant_searches")
            .select("id", { count: "exact", head: true })
            .eq("user_id", user.id)
            .eq("context", "home"),
          supabase
            .from("plant_searches")
            .select("id", { count: "exact", head: true })
            .eq("user_id", user.id)
            .eq("context", "wild")
            .not("lat", "is", null)
            .not("lng", "is", null),
          supabase
            .from("plant_searches")
            .select("id", { count: "exact", head: true })
            .eq("user_id", user.id)
            .eq("context", "wild")
            .or("lat.is.null,lng.is.null"),
          supabase
            .from("plant_searches")
            .select("id", { count: "exact", head: true })
            .eq("user_id", user.id)
            .eq("context", "unclassified"),
        ]);

        if (!mountedRef.current) return;

        const anyError = home.error || wildWith.error || wildWithout.error || unclassified.error;
        if (anyError) {
          console.error("Error fetching context counts:", anyError.message);
          setCounts(ZERO);
          setIsLoading(false);
          return;
        }

        setCounts({
          home: home.count ?? 0,
          wild_with_coords: wildWith.count ?? 0,
          wild_without_coords: wildWithout.count ?? 0,
          unclassified: unclassified.count ?? 0,
        });
        setIsLoading(false);
      }, [user]);

      useEffect(() => {
        load();
      }, [load]);

      // Pattern S4: cross-component invalidation via CustomEvent (PATTERNS.md S4).
      // Cubre SPEC-AC2: tras clasificar una wild con coords, la tab Mapa
      // aparece sin reload del browser.
      useEffect(() => {
        const handler = () => {
          load();
        };
        window.addEventListener("mp:pending-classification-resolved", handler);
        return () => {
          window.removeEventListener("mp:pending-classification-resolved", handler);
        };
      }, [load]);

      return {
        ...counts,
        isLoading,
        refetch: load,
      };
    }
    ```

    Decisiones específicas implementadas (citar IDs en comentarios del archivo):
    - **D-06**: shape `{ home, wild_with_coords, wild_without_coords, unclassified }` lockeado
    - **D-07**: gate `if (!user)` antes de tocar Supabase
    - **D-11**: bucket separado `wild_without_coords` permite que las plantas wild-sin-coords se cuenten pero la tab use solo `wild_with_coords` (per D-13)
    - **Pattern S3** (PATTERNS.md): mountedRef + WR-02 comment fix
    - **Pattern S4** (PATTERNS.md): listener `mp:pending-classification-resolved` para AC2
    - **RESEARCH.md Open Question 2**: hook NUEVO (no extensión de useUnclassifiedCount) por DRY

    Path alias `@/` para todos los imports (Pattern S2). Comentarios en inglés (CLAUDE.md), exports inglés.
  </action>
  <verify>
    <automated>npm test -- --run src/hooks/use-context-counts.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/use-context-counts.ts` existe y exporta `useContextCounts`, `ContextCounts`, `UseContextCountsReturn`
    - `grep -c "from(\"plant_searches\")" src/hooks/use-context-counts.ts` devuelve `4` (las 4 queries paralelas)
    - `grep -c "Promise.all" src/hooks/use-context-counts.ts` devuelve `1`
    - `grep -c "head: true" src/hooks/use-context-counts.ts` devuelve `4`
    - `grep -c "mountedRef" src/hooks/use-context-counts.ts` devuelve >= 4 (declaración + 2 setters + cleanup)
    - `grep "mp:pending-classification-resolved" src/hooks/use-context-counts.ts` matchea 2 veces (addEventListener + removeEventListener)
    - `grep "if (!user)" src/hooks/use-context-counts.ts` matchea (gate anónimo per D-07)
    - `grep "WR-02" src/hooks/use-context-counts.ts` matchea (comment de mountedRef pattern)
    - `npx tsc --noEmit` exit 0 (compilación TypeScript pasa)
  </acceptance_criteria>
  <done>Hook creado con shell idéntico a use-unclassified-count.ts, 4 queries paralelas head:true, listener del CustomEvent, gate de anónimo. TypeScript compila.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Crear test unitario sibling src/hooks/use-context-counts.test.ts</name>
  <files>src/hooks/use-context-counts.test.ts</files>
  <read_first>
    - src/hooks/use-plant-identifier.test.ts (analog del proyecto para vi.mock + renderHook + act + waitFor)
    - .planning/phases/03.1-plant-map-v0/03.1-PATTERNS.md líneas 624-695 (§ 13-14 tests delta + mock chain helper en líneas 667-682)
    - src/hooks/use-context-counts.ts (recién creado en Task 1)
    - vitest.config.* o vite.config.ts (verificar setup de jsdom + globals)
  </read_first>
  <behavior>
    Tests obligatorios (en orden):
    1. `returns ZERO counts and isLoading=false when no user` — anonymous gate (D-07)
    2. `runs 4 parallel queries to plant_searches when user is present` — verificar 4 invocaciones de `.from('plant_searches')`
    3. `filters each query by user_id` — verificar `.eq("user_id", "test-user-id")` invocado 4 veces
    4. `filters wild_with_coords by context=wild AND lat,lng NOT NULL` — verificar `.eq("context","wild")` + `.not("lat","is",null)` + `.not("lng","is",null)`
    5. `filters wild_without_coords by context=wild AND (lat IS NULL OR lng IS NULL)` — verificar `.or("lat.is.null,lng.is.null")`
    6. `returns ZERO counts on Supabase error (graceful degradation)`
    7. `refetches counts when window dispatches mp:pending-classification-resolved`
  </behavior>
  <action>
    Crear `src/hooks/use-context-counts.test.ts` como sibling (PATTERNS.md Pattern S6: tests sibling `*.test.ts`, NO `__tests__/`). Contenido completo:

    ```typescript
    import { describe, it, expect, vi, beforeEach } from "vitest";
    import { renderHook, waitFor, act } from "@testing-library/react";

    // Mock track to avoid PostHog calls (PATTERNS.md test convention)
    vi.mock("@/lib/track", () => ({ track: vi.fn() }));

    // Chainable Supabase mock builder. Each .from() call returns a new chain
    // whose terminal Promise resolves to { data, error, count }.
    // Patterns: PATTERNS.md lines 667-682 (canonical Supabase test idiom).
    type QueryResult = { data: unknown; error: unknown; count: number | null };

    let homeResult: QueryResult;
    let wildWithResult: QueryResult;
    let wildWithoutResult: QueryResult;
    let unclassifiedResult: QueryResult;
    let queryCounter: number;
    let chainCalls: { method: string; args: unknown[] }[];

    function makeChain(result: QueryResult) {
      const chain: Record<string, unknown> = {};
      const methods = ["select", "eq", "not", "or", "order", "limit"] as const;
      for (const m of methods) {
        chain[m] = vi.fn((...args: unknown[]) => {
          chainCalls.push({ method: m, args });
          return chain;
        });
      }
      // Thenable so `await chain` resolves to result.
      (chain as { then: (resolve: (r: QueryResult) => void) => void }).then = (
        resolve,
      ) => resolve(result);
      return chain;
    }

    vi.mock("@/integrations/supabase/client", () => ({
      supabase: {
        from: vi.fn(() => {
          queryCounter += 1;
          // Order: home → wild_with → wild_without → unclassified
          // (matches Promise.all order in the hook).
          if (queryCounter === 1) return makeChain(homeResult);
          if (queryCounter === 2) return makeChain(wildWithResult);
          if (queryCounter === 3) return makeChain(wildWithoutResult);
          return makeChain(unclassifiedResult);
        }),
      },
    }));

    // Default to a logged-in user; individual tests can override.
    let mockUser: { id: string } | null = { id: "test-user-id" };
    vi.mock("@/hooks/use-auth", () => ({
      useAuth: () => ({ user: mockUser }),
    }));

    import { useContextCounts } from "./use-context-counts";

    describe("useContextCounts", () => {
      beforeEach(() => {
        vi.clearAllMocks();
        queryCounter = 0;
        chainCalls = [];
        mockUser = { id: "test-user-id" };
        homeResult = { data: null, error: null, count: 0 };
        wildWithResult = { data: null, error: null, count: 0 };
        wildWithoutResult = { data: null, error: null, count: 0 };
        unclassifiedResult = { data: null, error: null, count: 0 };
      });

      it("returns ZERO counts and isLoading=false when no user (D-07)", async () => {
        mockUser = null;
        const { result } = renderHook(() => useContextCounts());
        await waitFor(() => expect(result.current.isLoading).toBe(false));
        expect(result.current.home).toBe(0);
        expect(result.current.wild_with_coords).toBe(0);
        expect(result.current.wild_without_coords).toBe(0);
        expect(result.current.unclassified).toBe(0);
        expect(queryCounter).toBe(0); // Supabase NOT called
      });

      it("runs 4 parallel queries to plant_searches when user is present", async () => {
        const { result } = renderHook(() => useContextCounts());
        await waitFor(() => expect(result.current.isLoading).toBe(false));
        expect(queryCounter).toBe(4);
      });

      it("filters each query by user_id", async () => {
        const { result } = renderHook(() => useContextCounts());
        await waitFor(() => expect(result.current.isLoading).toBe(false));
        const userIdCalls = chainCalls.filter(
          (c) => c.method === "eq" && c.args[0] === "user_id" && c.args[1] === "test-user-id",
        );
        expect(userIdCalls.length).toBe(4);
      });

      it("filters wild_with_coords by context=wild AND lat,lng NOT NULL (SPEC-AC10)", async () => {
        const { result } = renderHook(() => useContextCounts());
        await waitFor(() => expect(result.current.isLoading).toBe(false));
        const ctxWildCalls = chainCalls.filter(
          (c) => c.method === "eq" && c.args[0] === "context" && c.args[1] === "wild",
        );
        // wild_with AND wild_without both filter context=wild → 2 calls
        expect(ctxWildCalls.length).toBe(2);
        const notLatCalls = chainCalls.filter(
          (c) => c.method === "not" && c.args[0] === "lat" && c.args[1] === "is" && c.args[2] === null,
        );
        const notLngCalls = chainCalls.filter(
          (c) => c.method === "not" && c.args[0] === "lng" && c.args[1] === "is" && c.args[2] === null,
        );
        expect(notLatCalls.length).toBe(1);
        expect(notLngCalls.length).toBe(1);
      });

      it("filters wild_without_coords with .or('lat.is.null,lng.is.null')", async () => {
        const { result } = renderHook(() => useContextCounts());
        await waitFor(() => expect(result.current.isLoading).toBe(false));
        const orCalls = chainCalls.filter(
          (c) => c.method === "or" && c.args[0] === "lat.is.null,lng.is.null",
        );
        expect(orCalls.length).toBe(1);
      });

      it("returns the exact counts from each bucket", async () => {
        homeResult = { data: null, error: null, count: 3 };
        wildWithResult = { data: null, error: null, count: 5 };
        wildWithoutResult = { data: null, error: null, count: 1 };
        unclassifiedResult = { data: null, error: null, count: 7 };
        const { result } = renderHook(() => useContextCounts());
        await waitFor(() => expect(result.current.isLoading).toBe(false));
        expect(result.current.home).toBe(3);
        expect(result.current.wild_with_coords).toBe(5);
        expect(result.current.wild_without_coords).toBe(1);
        expect(result.current.unclassified).toBe(7);
      });

      it("returns ZERO counts on Supabase error (graceful degradation)", async () => {
        wildWithResult = { data: null, error: { message: "boom" }, count: null };
        const { result } = renderHook(() => useContextCounts());
        await waitFor(() => expect(result.current.isLoading).toBe(false));
        expect(result.current.home).toBe(0);
        expect(result.current.wild_with_coords).toBe(0);
        expect(result.current.wild_without_coords).toBe(0);
        expect(result.current.unclassified).toBe(0);
      });

      it("refetches counts when window dispatches mp:pending-classification-resolved (SPEC-AC2)", async () => {
        const { result } = renderHook(() => useContextCounts());
        await waitFor(() => expect(result.current.isLoading).toBe(false));
        expect(queryCounter).toBe(4);
        // Mutate the would-be next result so refetch picks it up.
        wildWithResult = { data: null, error: null, count: 1 };
        await act(async () => {
          window.dispatchEvent(new CustomEvent("mp:pending-classification-resolved"));
        });
        await waitFor(() => expect(result.current.wild_with_coords).toBe(1));
        expect(queryCounter).toBe(8); // 4 initial + 4 refetch
      });
    });
    ```

    Notas:
    - Usar `@testing-library/react` (ya en `package.json`) — patrón verbatim de `use-plant-identifier.test.ts`
    - Mock chain con `then` thenable es el patrón canónico (PATTERNS.md líneas 667-682)
    - `vi.clearAllMocks()` + reset de counters en `beforeEach`
    - Comentar cada test con el ID de SPEC/D que cubre (D-07, AC2, AC10)
  </action>
  <verify>
    <automated>npm test -- --run src/hooks/use-context-counts.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/use-context-counts.test.ts` existe y compila TypeScript (`npx tsc --noEmit` exit 0)
    - `npm test -- --run src/hooks/use-context-counts.test.ts` exit 0
    - `grep -c "^  it(" src/hooks/use-context-counts.test.ts` devuelve >= 7 (tests obligatorios listados en behavior)
    - Output del test runner reporta `7 passed` (o más si añades cases adicionales)
    - El test "filters wild_with_coords by context=wild AND lat,lng NOT NULL" pasa (cubre SPEC-AC10)
    - El test "refetches counts when window dispatches mp:pending-classification-resolved" pasa (cubre SPEC-AC2)
    - El test "returns ZERO counts and isLoading=false when no user" pasa (cubre SPEC-AC3 — usuario anónimo)
  </acceptance_criteria>
  <done>Test file con >= 7 it() todos pasando, cubriendo D-07/AC2/AC10 explícitamente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Supabase | Hook ejecuta queries con `user.id` derivado de la sesión authenticada; RLS de plant_searches debe filtrar a `auth.uid()` en el server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-04 | Information disclosure | useContextCounts query a plant_searches | mitigate | Cada query incluye `.eq("user_id", user.id)` Y RLS de Supabase ya filtra a `auth.uid()` (defense in depth — la phase 02.1 dejó RLS activa per `phases/02.1-*/02.1-*-SUMMARY.md`). Si el client manda `user.id` distinto, RLS lo rechaza. |
| T-03.1-05 | Tampering | window CustomEvent `mp:pending-classification-resolved` puede dispararse por código del proyecto | accept | Listener solo invoca `load()` (refetch). El payload del evento no se consume. Worst case: refetch innecesario (no daño). |
| T-03.1-06 | DoS | Llamadas spam al refetch si el CustomEvent se dispara repetidamente | accept | El refetch es 4 head-only queries (~1KB total response, sin row transfer). Costo trivial. Si emerge en producción, añadir debounce en v1+. |
</threat_model>

<verification>
- `src/hooks/use-context-counts.ts` existe con exports correctos
- `src/hooks/use-context-counts.test.ts` existe con >= 7 tests
- `npm test -- --run src/hooks/use-context-counts.test.ts` exit 0
- `npx tsc --noEmit` exit 0 (compilación TypeScript del proyecto entero pasa)
- Cross-platform: hook es 100% browser-agnostic (Supabase JS client funciona idéntico en Capacitor WebView y iOS PWA). El CustomEvent `mp:pending-classification-resolved` es API estándar de Window, soportada en ambos.
</verification>

<success_criteria>
- Hook nuevo con shape lockeado `{ home, wild_with_coords, wild_without_coords, unclassified, isLoading, refetch }`
- 4 queries paralelas con `count: "exact", head: true` (sin row transfer)
- Gate anónimo en D-07: si `!user`, retorna ZERO sin tocar Supabase
- Listener `mp:pending-classification-resolved` cubre SPEC-AC2 (refetch sin reload)
- 7+ tests unitarios todos pasando
- TypeScript compila sin errores
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-plant-map-v0/03.1-02-SUMMARY.md`
</output>
