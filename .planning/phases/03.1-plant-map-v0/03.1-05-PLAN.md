---
phase: 03.1-plant-map-v0
plan: 05
type: execute
wave: 2
depends_on: [01, 03]
files_modified:
  - src/lib/build-plant-pin-icon.ts
  - src/components/FitBoundsOnMount.tsx
  - src/pages/MapPage.tsx
  - src/layouts/AppLayout.tsx
  - src/App.tsx
autonomous: true
requirements: [SPEC-R2, SPEC-R4, SPEC-R5, SPEC-AC4, SPEC-AC5, SPEC-AC6, SPEC-AC11, SPEC-AC12]
requirements_addressed: [SPEC-R2, SPEC-R4, SPEC-R5, SPEC-AC4, SPEC-AC5, SPEC-AC6, SPEC-AC11, SPEC-AC12]
tags: [phase-03.1, map-page, leaflet, fit-bounds, pin-icon, full-bleed]

must_haves:
  truths:
    - "Existe la ruta /mapa registrada en App.tsx dentro de AppLayout que monta MapPage"
    - "MapPage renderiza un MapContainer de react-leaflet con TileLayer de OpenStreetMap y atribución visible"
    - "Cada planta wild-con-coords del hook se renderiza como un Marker con divIcon custom (sticker emoji 🌿 + cream + olive border + shadow 4px 4px 0)"
    - "Si pins.length > 1, fitBounds ajusta el viewport inicial; si pins.length === 1, setView zoom=14"
    - "MapPage usa 100dvh (no 100vh) y resta env(safe-area-inset-bottom) + env(safe-area-inset-top) + 4rem (BottomTabBar)"
    - "AppLayout acepta una prop `fullBleed` que cuando es true omite el wrapper max-w-md/pb-20/mb-safe y no renderiza LegalFooter"
    - "Para 0 pins (deep-link directo a /mapa sin tener pins), MapPage redirige a / silenciosamente (sin empty state copy per sketch winner)"
    - "Si las tiles OSM fallan al cargar, la app no crashea; los pins quedan visibles sobre el fondo gris default de Leaflet (SPEC-AC12)"
  artifacts:
    - path: "src/lib/build-plant-pin-icon.ts"
      provides: "Factory que retorna L.DivIcon con HTML sticker style del design system + emoji 🌿"
      exports: ["buildPlantPinIcon"]
      min_lines: 20
    - path: "src/components/FitBoundsOnMount.tsx"
      provides: "Componente hijo del MapContainer que usa useMap() para fitBounds o setView según count"
      exports: ["FitBoundsOnMount"]
      min_lines: 15
    - path: "src/pages/MapPage.tsx"
      provides: "Página principal /mapa: orquesta hook, MapContainer, Markers, FitBoundsOnMount, redirect-on-empty"
      min_lines: 60
    - path: "src/layouts/AppLayout.tsx"
      provides: "Layout con prop opcional `fullBleed` que omite el wrapper constrained y LegalFooter"
      contains: "fullBleed"
    - path: "src/App.tsx"
      provides: "Ruta /mapa registrada con AppLayout fullBleed wrapping MapPage"
      contains: "/mapa"
  key_links:
    - from: "src/App.tsx"
      to: "src/pages/MapPage.tsx"
      via: "Route path=/mapa element AppLayout fullBleed"
      pattern: "MapPage"
    - from: "src/pages/MapPage.tsx"
      to: "useWildPlantsWithCoords (Plan 03)"
      via: "import hook + render markers per plant"
      pattern: "useWildPlantsWithCoords"
    - from: "src/pages/MapPage.tsx"
      to: "react-leaflet MapContainer / TileLayer / Marker"
      via: "imports + JSX render"
      pattern: "react-leaflet"
    - from: "src/pages/MapPage.tsx"
      to: "src/components/FitBoundsOnMount.tsx"
      via: "child of MapContainer using useMap()"
      pattern: "FitBoundsOnMount"
    - from: "src/pages/MapPage.tsx"
      to: "src/lib/build-plant-pin-icon.ts"
      via: "icon prop of each Marker"
      pattern: "buildPlantPinIcon"
---

<objective>
Construir el core del mapa: 3 archivos nuevos (`build-plant-pin-icon.ts`, `FitBoundsOnMount.tsx`, `MapPage.tsx`), 1 modificado (`AppLayout.tsx` con prop opcional `fullBleed`), y 1 modificado (`App.tsx` con la nueva ruta `/mapa`). El MapPage renderiza el `MapContainer` de react-leaflet@4 a pantalla completa (100dvh menos BottomTabBar y safe-areas), con tiles OSM, un Marker por planta usando el divIcon sticker, y `FitBoundsOnMount` ajustando viewport según count (D-14/D-15). Sin empty state copy — si llega con 0 pins, redirect silencioso a `/` per sketch winner.

Este plan NO incluye el PlantMapSheet (Plan 06 hace eso) ni los eventos PostHog completos (Plan 07 los añade). MapPage se entrega con state `selectedPin` no consumido todavía — Plan 06 lo conecta. Acceptance criteria de este plan: la página carga, los pins están en el DOM, fit-bounds funciona, atribución visible, redirect-on-empty funciona.

Purpose: Wave 2 entrega la "carcasa" del mapa que Plan 06 y Plan 07 luego decoran. Es el plan más grande de la phase (5 archivos), justificado porque crear MapPage sin pin/FitBounds/route es una mitad-cosa no-deployable; mejor entregar la unidad coherente en una pasada.
Output: 3 archivos nuevos + 2 archivos modificados (~150 LOC nuevas + ~15 LOC modificadas).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03.1-plant-map-v0/03.1-CONTEXT.md
@.planning/phases/03.1-plant-map-v0/03.1-SPEC.md
@.planning/phases/03.1-plant-map-v0/03.1-RESEARCH.md
@.planning/phases/03.1-plant-map-v0/03.1-UI-SPEC.md
@.planning/phases/03.1-plant-map-v0/03.1-PATTERNS.md
@.planning/phases/03.1-plant-map-v0/03.1-01-PLAN.md
@.planning/phases/03.1-plant-map-v0/03.1-03-PLAN.md
@.planning/sketches/004-plant-map-v0/README.md
@src/App.tsx
@src/layouts/AppLayout.tsx
@src/pages/Index.tsx
@CLAUDE.md

<interfaces>
DEPENDS_ON:
- Plan 01: `leaflet/dist/leaflet.css` ya importado en main.tsx + reglas CSS `.plant-map-pin` en index.css
- Plan 03: hook `useWildPlantsWithCoords()` exportando `{ plants: WildPlantWithCoords[], isLoading, refetch }`

CREATES:
```typescript
// src/lib/build-plant-pin-icon.ts (NEW)
export function buildPlantPinIcon(): import("leaflet").DivIcon;

// src/components/FitBoundsOnMount.tsx (NEW)
export interface FitBoundsOnMountProps { pins: Array<{ lat: number; lng: number }>; }
export function FitBoundsOnMount(props: FitBoundsOnMountProps): null;

// src/pages/MapPage.tsx (NEW) — default export
export default function MapPage(): JSX.Element;

// src/layouts/AppLayout.tsx (MODIFIED)
export default function AppLayout(props: { children: ReactNode; fullBleed?: boolean }): JSX.Element;
```

CONSUMERS (Plan 06): MapPage tendrá un `useState<WildPlantWithCoords | null>(selectedPin)` que Plan 06 conectará al PlantMapSheet. Plan 06 modificará MapPage para añadir `<PlantMapSheet plant={selectedPin} onClose={...} />` y el track call en Marker click.

CRITICAL CONSTRAINTS:
- react-leaflet@4 `MapContainer` `center`/`zoom` son INMUTABLES después de mount. Pasar valores placeholder + usar FitBoundsOnMount con useMap() (PATTERNS.md landmine #5).
- 100dvh NO 100vh (PATTERNS.md landmine #9).
- AppLayout actualmente wrappea TODO en `max-w-md mx-auto pb-20 mb-safe` (verified `src/layouts/AppLayout.tsx:8`) + renderiza LegalFooter. Map necesita full-bleed (PATTERNS.md landmine #3) — prop `fullBleed`.
- Empty state copy NO se implementa — sketch winner D-04/D-07/D-13 explícitamente dice: "Sin empty state. Si por bug navega directo a /mapa sin pins, redirect a /." (.planning/sketches/004-plant-map-v0/README.md líneas 14-15).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Crear src/lib/build-plant-pin-icon.ts (factory L.DivIcon sticker emoji 🌿)</name>
  <files>src/lib/build-plant-pin-icon.ts</files>
  <read_first>
    - .planning/phases/03.1-plant-map-v0/03.1-PATTERNS.md líneas 299-337 (§ 8 — pattern del RESEARCH + delta lock emoji)
    - .planning/phases/03.1-plant-map-v0/03.1-UI-SPEC.md líneas 670-760 (§ 5 Pin marker — locked decisions)
    - .planning/phases/03.1-plant-map-v0/03.1-CONTEXT.md D-09, D-10 (sticker style + active transform)
    - .planning/phases/03.1-plant-map-v0/03.1-RESEARCH.md líneas 386-447 (§ Pattern 3 + trade-off emoji vs SVG)
    - .planning/sketches/004-plant-map-v0/README.md (winner Variant A LOCKS emoji 🌿, no lucide Leaf)
    - src/index.css (verificar que el bloque `.plant-map-pin { background: transparent !important }` del Plan 01 ya está — el factory depende de eso)
  </read_first>
  <action>
    Crear `src/lib/build-plant-pin-icon.ts` con el contenido exacto siguiente (lock: emoji 🌿, NO lucide Leaf SVG — per sketch winner explícito):

    ```typescript
    import L from "leaflet";

    /**
     * Build a custom Leaflet divIcon for plant discovery pins.
     *
     * Sticker style (CONTEXT.md D-09 + UI-SPEC §5):
     * - 36×36px square
     * - cream fill (`hsl(var(--card))`)
     * - 2px solid foreground border (verde oliva profundo)
     * - 1rem border-radius
     * - 4×4px hard shadow with foreground color
     * - leaf emoji 🌿 centered (font-size: 20px)
     *
     * Active feedback (CONTEXT.md D-10): `transform: scale(1.1)` on `:active`
     * and `:focus-within`. Implemented in src/index.css `.plant-map-pin > div`
     * rule (Plan 01 Task 3). Reduced-motion respected via prefers-reduced-motion
     * media query in the same CSS block.
     *
     * Glyph decision lock (sketch 004 winner + RESEARCH.md recommendation):
     * EMOJI 🌿, not lucide-react `Leaf` SVG. Reasons:
     *   - No `renderToStaticMarkup` overhead per pin
     *   - Renders identically in Capacitor WebView 8 (Chrome 121+) and Safari iOS 16+
     *   - No React reconciliation overhead (divIcon is rendered outside React tree)
     *   - Slight cross-platform variance (Android Noto vs Apple Color Emoji)
     *     is acceptable: both clearly read as "leaf"
     *
     * iconAnchor [18, 36] (bottom-center): the geographic coordinate maps
     * to the bottom of the sticker, so the visual "tip" of the pin sits
     * on the lat/lng. Standard mapping convention.
     *
     * className "plant-map-pin": hooks into the override rules in
     * src/index.css that remove Leaflet's default white background +
     * grey border (Pitfall 3 of RESEARCH.md).
     */
    export function buildPlantPinIcon(): L.DivIcon {
      return L.divIcon({
        className: "plant-map-pin",
        html: `
          <div style="
            width: 36px;
            height: 36px;
            background: hsl(var(--card));
            border: 2px solid hsl(var(--foreground));
            border-radius: 1rem;
            box-shadow: 4px 4px 0 0 hsl(var(--foreground));
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 20px;
            line-height: 1;
            transition: transform 100ms ease;
          ">🌿</div>
        `,
        iconSize: [36, 36],
        iconAnchor: [18, 36],
        popupAnchor: [0, -36],
      });
    }
    ```

    Notas:
    - `import L from "leaflet"` (no `import { divIcon }`) — el ESM de leaflet exporta L como default (verificado en types)
    - El HTML inline NO hereda CSS del padre porque Leaflet inserta el divIcon en `.leaflet-marker-pane` (RESEARCH.md Pitfall 3) — por eso estilos inline
    - `hsl(var(--card))` y `hsl(var(--foreground))` resuelven a los tokens del design system (UI-SPEC líneas 46-58)
    - NO usar `--olive-dark` directamente — UI-SPEC línea 58 aclara que el token canónico es `--foreground`
    - `iconAnchor: [18, 36]` per UI-SPEC línea 752 (bottom-center; el pin "apunta" al lat/lng)
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/lib/build-plant-pin-icon.ts','utf8');const checks={hasLImport:s.includes('import L from \"leaflet\"'),hasExport:s.includes('export function buildPlantPinIcon'),returnsL_DivIcon:s.includes(': L.DivIcon'),usesDivIcon:s.includes('L.divIcon('),hasPlantMapPinClass:s.includes(\"className: \\\"plant-map-pin\\\"\"),hasEmoji:s.includes('🌿'),hasNoLucideLeaf:!s.includes('import { Leaf }')&&!s.includes('renderToStaticMarkup'),hasIconSize36:s.includes('iconSize: [36, 36]'),hasIconAnchor18_36:s.includes('iconAnchor: [18, 36]'),hasCardBg:s.includes('hsl(var(--card))'),hasForegroundBorder:s.includes('hsl(var(--foreground))'),hasShadowPress:s.includes('4px 4px 0 0 hsl(var(--foreground))')};const fails=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);if(fails.length){console.error('FAIL:',fails);process.exit(1);}console.log('OK pin factory');"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/build-plant-pin-icon.ts` existe y exporta `buildPlantPinIcon`
    - Importa `L from "leaflet"` (no `import { divIcon }`)
    - Retorna `L.DivIcon` (verificable en signature)
    - Contiene `className: "plant-map-pin"` (string exacto — hooks into index.css overrides)
    - Contiene el emoji `🌿` (no lucide-react Leaf SVG)
    - NO contiene `renderToStaticMarkup` ni `import { Leaf }` de lucide-react
    - `iconSize: [36, 36]` y `iconAnchor: [18, 36]` exactos
    - Usa `hsl(var(--card))` y `hsl(var(--foreground))` (no hex hardcoded ni `--olive-dark`)
    - `npx tsc --noEmit` exit 0 (los tipos de leaflet@1.9 deben resolver correctamente — Plan 01 los instaló)
  </acceptance_criteria>
  <done>Factory creado con sticker emoji 🌿, sticker 36×36 cream+olive, hooks into .plant-map-pin override CSS, TypeScript compila.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Crear src/components/FitBoundsOnMount.tsx (useMap() child for viewport fit)</name>
  <files>src/components/FitBoundsOnMount.tsx</files>
  <read_first>
    - .planning/phases/03.1-plant-map-v0/03.1-PATTERNS.md líneas 440-474 (§ 10 — pattern verbatim)
    - .planning/phases/03.1-plant-map-v0/03.1-RESEARCH.md líneas 332-385 (§ Pattern 2 + warning sobre MapContainer props inmutables)
    - .planning/phases/03.1-plant-map-v0/03.1-CONTEXT.md D-14, D-15 (fitBounds + single-pin setView zoom=14)
    - .planning/phases/03.1-plant-map-v0/03.1-UI-SPEC.md líneas 870-873 (motion: animate:false si prefers-reduced-motion)
  </read_first>
  <action>
    Crear `src/components/FitBoundsOnMount.tsx` con el contenido exacto:

    ```typescript
    import { useEffect } from "react";
    import { useMap } from "react-leaflet";
    import L from "leaflet";

    export interface FitBoundsOnMountProps {
      pins: Array<{ lat: number; lng: number }>;
    }

    /**
     * Imperative bridge: adjusts the map viewport when pins change.
     *
     * Pattern: react-leaflet@4 official docs (example-view-bounds). The
     * MapContainer `center`/`zoom` props are immutable after mount — the
     * canonical way to update the viewport reactively is a child component
     * that calls `useMap()` and dispatches imperative methods.
     *
     * Decisions:
     * - D-14: pins.length > 1 → map.fitBounds with [40, 40] padding
     * - D-15: pins.length === 1 → map.setView at zoom 14 (city-level)
     * - pins.length === 0 → no-op (MapPage will not even render the map
     *   in this branch; it redirects per sketch winner)
     *
     * Reduced-motion: respects prefers-reduced-motion via `animate: false`
     * option on fitBounds/setView (UI-SPEC line 873).
     *
     * Must be a CHILD of <MapContainer> — useMap() reads from context.
     */
    export function FitBoundsOnMount({ pins }: FitBoundsOnMountProps) {
      const map = useMap();
      useEffect(() => {
        if (pins.length === 0) return;
        const reduceMotion =
          typeof window !== "undefined" &&
          window.matchMedia("(prefers-reduced-motion: reduce)").matches;
        if (pins.length === 1) {
          // D-15: single pin → zoom city-level (~3km wide).
          map.setView([pins[0].lat, pins[0].lng], 14, {
            animate: !reduceMotion,
          });
          return;
        }
        // D-14: multiple pins → fit bounds with visual padding.
        const bounds = L.latLngBounds(pins.map((p) => [p.lat, p.lng]));
        map.fitBounds(bounds, {
          padding: [40, 40],
          animate: !reduceMotion,
        });
      }, [map, pins]);
      return null;
    }
    ```

    Notas:
    - Return `null` (componente lifecycle-only, sin DOM)
    - useMap() depende de estar dentro de MapContainer context (Plan 05 Task 3 lo asegura)
    - Deps `[map, pins]`: re-ejecuta si el array de pins cambia (cubre futuras navegaciones back-to-map)
    - `animate: !reduceMotion` per UI-SPEC línea 873 accessibility contract
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/components/FitBoundsOnMount.tsx','utf8');const checks={hasUseMap:s.includes('useMap'),hasLImport:s.includes('import L from \"leaflet\"'),hasFitBounds:s.includes('map.fitBounds'),hasSetView:s.includes('map.setView'),hasZoom14:s.includes(', 14,'),hasPadding40:s.includes('padding: [40, 40]'),hasSinglePinBranch:s.includes('pins.length === 1'),hasMultiPinBranch:s.includes('L.latLngBounds'),hasReturnNull:s.includes('return null'),hasReducedMotion:s.includes('prefers-reduced-motion'),hasExportInterface:s.includes('export interface FitBoundsOnMountProps')};const fails=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);if(fails.length){console.error('FAIL:',fails);process.exit(1);}console.log('OK FitBoundsOnMount');"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/FitBoundsOnMount.tsx` existe y exporta named `FitBoundsOnMount` + `FitBoundsOnMountProps`
    - Importa `useMap` desde `react-leaflet`
    - Branch 1: `pins.length === 1` → `map.setView([lat, lng], 14, { animate })`
    - Branch 2: `pins.length > 1` → `map.fitBounds(L.latLngBounds(...), { padding: [40, 40], animate })`
    - Branch 0: early return (no-op)
    - Respeta `prefers-reduced-motion`: `animate: !reduceMotion`
    - Retorna `null` (lifecycle-only)
    - `useEffect` deps `[map, pins]`
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>FitBoundsOnMount creado con 3 branches (0/1/multi), useMap() child, prefers-reduced-motion respected.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Modificar src/layouts/AppLayout.tsx añadiendo prop opcional `fullBleed`</name>
  <files>src/layouts/AppLayout.tsx</files>
  <read_first>
    - src/layouts/AppLayout.tsx (CURRENT — 15 líneas exactas, todo el archivo)
    - .planning/phases/03.1-plant-map-v0/03.1-UI-SPEC.md líneas 784-806 (§ AppLayout MODIFIED — pattern verbatim)
    - .planning/phases/03.1-plant-map-v0/03.1-PATTERNS.md landmine #3 línea 871 (justificación)
  </read_first>
  <action>
    Reemplazar el contenido COMPLETO de `src/layouts/AppLayout.tsx` con la versión que acepta una prop opcional `fullBleed?: boolean`. Cuando `fullBleed=true`: omitir el wrapper `<div className="max-w-md mx-auto pb-20 mb-safe">` Y NO renderizar `<LegalFooter />` (per UI-SPEC línea 806: el OSM attribution es el footer legal del mapa; los enlaces del proyecto siguen siendo accesibles vía /ajustes).

    Contenido final exacto:

    ```tsx
    import type { ReactNode } from "react";
    import BottomTabBar from "@/components/BottomTabBar";
    import LegalFooter from "@/components/LegalFooter";

    interface AppLayoutProps {
      children: ReactNode;
      /**
       * Phase 03.1: when true, omit the `max-w-md mx-auto pb-20 mb-safe` wrapper
       * and the LegalFooter so the child can render full-bleed (e.g. /mapa,
       * where the OSM attribution is the legal footer for that surface).
       * Defaults to false — existing pages render unchanged.
       */
      fullBleed?: boolean;
    }

    export default function AppLayout({ children, fullBleed = false }: AppLayoutProps) {
      return (
        <div className="min-h-screen bg-background">
          {fullBleed ? (
            children
          ) : (
            <div className="max-w-md mx-auto pb-20 mb-safe">
              {children}
              <LegalFooter />
            </div>
          )}
          <BottomTabBar />
        </div>
      );
    }
    ```

    NOTA CRÍTICA: cuando `fullBleed=true`, NO se renderiza `LegalFooter`. Esto NO desaparece los enlaces legales del proyecto — siguen accesibles desde otras páginas (Index, History, Settings). El mapa tiene su propio "footer" obligatorio (OSM attribution renderizada por Leaflet automáticamente).

    El default `fullBleed = false` asegura que NINGUNA de las páginas existentes (Index, History, PlantDetail, SettingsPage) se vea afectada — siguen recibiendo el wrapper constrained y el LegalFooter como antes.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/layouts/AppLayout.tsx','utf8');const checks={hasFullBleedProp:s.includes('fullBleed?: boolean')||s.includes('fullBleed: boolean'),hasFullBleedDefault:s.includes('fullBleed = false'),hasConditionalRender:s.includes('fullBleed ?'),preservesMaxWMd:s.includes('max-w-md mx-auto pb-20 mb-safe'),preservesBottomTabBar:s.includes('<BottomTabBar />'),preservesLegalFooter:s.includes('<LegalFooter />'),preservesMinH:s.includes('min-h-screen bg-background')};const fails=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);if(fails.length){console.error('FAIL:',fails);process.exit(1);}console.log('OK AppLayout fullBleed');"</automated>
  </verify>
  <acceptance_criteria>
    - `src/layouts/AppLayout.tsx` define interface o props con `fullBleed?: boolean`
    - Default value `fullBleed = false` (no-op para páginas existentes)
    - Cuando `fullBleed=false` (default), el wrapper `max-w-md mx-auto pb-20 mb-safe` y `<LegalFooter />` siguen renderizándose (sin regresión)
    - Cuando `fullBleed=true`, children se renderizan directamente sin wrapper ni LegalFooter
    - `<BottomTabBar />` SIEMPRE se renderiza (en ambos branches)
    - `min-h-screen bg-background` del wrapper outer no cambia
    - `npx tsc --noEmit` exit 0
    - Las páginas existentes (verificar visualmente en `npm run dev` o por TS check) siguen recibiendo la firma sin cambios (porque AppLayout aceptaba antes solo `children`; ahora acepta `children + fullBleed?` opcional)
  </acceptance_criteria>
  <done>AppLayout acepta fullBleed?:boolean opcional con default false. Páginas existentes no se ven afectadas.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Crear src/pages/MapPage.tsx (orquestación map + redirect-on-empty)</name>
  <files>src/pages/MapPage.tsx</files>
  <read_first>
    - src/pages/Index.tsx (analog page shell — useState, useEffect, conditional render, imports)
    - src/pages/History.tsx (analog — refetch on event listener pattern; aunque Map no lo necesita en este plan)
    - .planning/phases/03.1-plant-map-v0/03.1-PATTERNS.md líneas 236-296 (§ 7 MapPage delta)
    - .planning/phases/03.1-plant-map-v0/03.1-UI-SPEC.md líneas 252-378 (§ 1 MapPage verbatim pseudo-structure)
    - .planning/phases/03.1-plant-map-v0/03.1-RESEARCH.md líneas 753-848 (§ R4 layout + skeleton)
    - .planning/phases/03.1-plant-map-v0/03.1-CONTEXT.md D-03, D-08, D-14, D-15, D-17
    - .planning/sketches/004-plant-map-v0/README.md (winner Variant A — no empty state, redirect a /)
    - src/hooks/use-wild-plants-with-coords.ts (creado en Plan 03 — interface y shape consumido)
  </read_first>
  <action>
    Crear `src/pages/MapPage.tsx` con el contenido exacto siguiente. Este plan NO incluye el PlantMapSheet (Plan 06 lo conecta) ni los tracking events `map_pin_tapped`/`map_navigated_to_detail` (Plan 07 los añade). Solo `map_opened` se trackea aquí porque es trivial (1 useEffect on mount). El `selectedPin` state se declara pero el sheet aún no se monta — Plan 06 añade `<PlantMapSheet plant={selectedPin} onClose={...} />` como sibling del MapContainer.

    ```tsx
    import { useEffect, useState } from "react";
    import { Navigate } from "react-router-dom";
    import { MapContainer, TileLayer, Marker } from "react-leaflet";
    import {
      useWildPlantsWithCoords,
      type WildPlantWithCoords,
    } from "@/hooks/use-wild-plants-with-coords";
    import { FitBoundsOnMount } from "@/components/FitBoundsOnMount";
    import { buildPlantPinIcon } from "@/lib/build-plant-pin-icon";
    import { track } from "@/lib/track";

    /**
     * /mapa — Plant discovery map.
     *
     * Layout: full-bleed within AppLayout (mounted with `fullBleed=true`
     * in App.tsx). Map fills `100dvh - 4rem (BottomTabBar) - safe-areas`.
     * `100dvh` (NOT 100vh) per UI-SPEC line 78 — iOS Safari address-bar
     * collapse cropping (landmine #9 PATTERNS.md).
     *
     * No empty state (sketch 004 winner): if the user lands here without
     * any wild-with-coords pins (deep link), redirect silently to `/`.
     * The conditional tab in BottomTabBar (Plan 04) prevents normal users
     * from ever seeing this branch.
     *
     * Pin tap → sets `selectedPin` state. Plan 06 (PlantMapSheet) will
     * mount the bottom sheet keyed off this state, and Plan 07 will add
     * the `map_pin_tapped` track call inside the click handler.
     *
     * react-leaflet@4 caveat (PATTERNS.md landmine #5): MapContainer
     * center/zoom are immutable after mount. We pass placeholder values
     * and let <FitBoundsOnMount /> set the true viewport via useMap().
     */
    export default function MapPage() {
      const { plants, isLoading } = useWildPlantsWithCoords();
      // selectedPin: state placeholder. Plan 06 will consume to mount the sheet.
      // eslint-disable-next-line @typescript-eslint/no-unused-vars
      const [selectedPin, setSelectedPin] = useState<WildPlantWithCoords | null>(null);
      const [trackedOpen, setTrackedOpen] = useState(false);

      // Document title on mount (UI-SPEC line 266 — page-level seo/a11y).
      useEffect(() => {
        document.title = "Mapa de descubrimientos · Mi Plantita Feliz";
      }, []);

      // Track map_opened once, after the query resolves. Avoid double-fire
      // on re-renders by gating via local boolean (PostHog contract Phase 03.1).
      useEffect(() => {
        if (isLoading || trackedOpen) return;
        track("map_opened", { pin_count: plants.length });
        setTrackedOpen(true);
      }, [isLoading, trackedOpen, plants.length]);

      // Still loading: render the empty container so layout doesn't jump,
      // but Leaflet tiles wait for fitBounds. The map will visually settle
      // when plants resolve. (Spec line 226: no spinner overlay.)
      if (isLoading) {
        return (
          <div
            className="relative w-full bg-secondary/30"
            style={{
              height:
                "calc(100dvh - 4rem - env(safe-area-inset-bottom) - env(safe-area-inset-top))",
            }}
            aria-busy="true"
            aria-label="Cargando mapa de descubrimientos"
          />
        );
      }

      // Sketch 004 winner D-04/D-13: if 0 pins (edge-case deep link),
      // redirect silently to /. NO empty state copy.
      if (plants.length === 0) {
        return <Navigate to="/" replace />;
      }

      return (
        <div
          className="relative w-full"
          style={{
            height:
              "calc(100dvh - 4rem - env(safe-area-inset-bottom) - env(safe-area-inset-top))",
          }}
          role="application"
          aria-label="Mapa de descubrimientos de plantas"
        >
          <MapContainer
            // Placeholder coords — FitBoundsOnMount overrides via useMap().
            center={[plants[0].lat, plants[0].lng]}
            zoom={13}
            scrollWheelZoom={true}
            style={{ height: "100%", width: "100%" }}
            attributionControl={true}
          >
            <TileLayer
              url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png"
              attribution='&copy; <a href="https://www.openstreetmap.org/copyright" target="_blank" rel="noopener noreferrer">OpenStreetMap</a> contributors'
            />
            {plants.map((plant) => (
              <Marker
                key={plant.id}
                position={[plant.lat, plant.lng]}
                icon={buildPlantPinIcon()}
                title={plant.name}
                eventHandlers={{
                  click: () => {
                    // Plan 07 will add: track("map_pin_tapped", { plant_search_id, pin_index_among_total, total_pins })
                    setSelectedPin(plant);
                  },
                }}
              />
            ))}
            <FitBoundsOnMount pins={plants.map((p) => ({ lat: p.lat, lng: p.lng }))} />
          </MapContainer>
          {/* Plan 06 will mount: <PlantMapSheet plant={selectedPin} onClose={() => setSelectedPin(null)} /> */}
        </div>
      );
    }
    ```

    Decisiones implementadas:
    - **SPEC-R2 + AC4**: page registrada (Plan 05 Task 5 hace el route registration)
    - **SPEC-R4 + AC5**: cada planta → un `<Marker>` con icon de `buildPlantPinIcon`
    - **SPEC-R5 + AC6**: `<FitBoundsOnMount pins={...}>` como child del MapContainer
    - **SPEC-AC11**: atribución OSM renderizada por TileLayer attribution prop (Leaflet la pinta en `.leaflet-control-attribution`)
    - **SPEC-AC12**: si tiles fallan, Leaflet muestra gray cells; el JSX no maneja "tile error" — comportamiento correcto
    - **CONTEXT.md D-03**: tile URL exacta + attribution exacto
    - **CONTEXT.md D-04 / D-07 / D-13 / sketch winner**: 0 pins → `<Navigate to="/" replace />` (no empty state copy)
    - **CONTEXT.md D-17**: defaults de Leaflet (minZoom/maxZoom no especificados = defaults 0-18) — NO setear
    - **UI-SPEC línea 282**: `height: calc(100dvh - 4rem - env(safe-area-inset-*))`
    - **UI-SPEC línea 372**: `role="application" aria-label="Mapa de descubrimientos de plantas"`
    - **PATTERNS.md landmine #5**: center/zoom placeholder, FitBoundsOnMount maneja viewport real
    - **PATTERNS.md landmine #9**: `100dvh`, no `100vh`
    - **Plan 07 hook anchor**: comentario inline marca dónde Plan 07 añadirá track call para `map_pin_tapped`
    - **Plan 06 hook anchor**: comentario inline marca dónde Plan 06 añadirá el `<PlantMapSheet />`
    - **selectedPin state**: ya declarado para que Plan 06 solo necesite añadir la importación del sheet y el JSX; cero refactor de MapPage en Plan 06

    `eslint-disable-next-line @typescript-eslint/no-unused-vars` — el state se declara aquí pero `selectedPin` no se lee hasta Plan 06. Es intencional: minimiza diff de Plan 06 a 2 líneas (1 import + 1 JSX).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/pages/MapPage.tsx','utf8');const checks={hasUseWildPlantsHook:s.includes('useWildPlantsWithCoords'),hasMapContainer:s.includes('MapContainer'),hasTileLayer:s.includes('TileLayer'),hasMarker:s.includes('<Marker'),hasFitBounds:s.includes('FitBoundsOnMount'),hasBuildPin:s.includes('buildPlantPinIcon'),hasMapOpenedTrack:s.includes('track(\"map_opened\"'),hasOSMTileURL:s.includes('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png'),hasAttribution:s.includes('openstreetmap.org/copyright'),hasNavigateRedirect:s.includes('<Navigate to=\"/\"')||s.includes('Navigate to=\"/\" replace'),has100dvh:s.includes('100dvh'),hasNo100vh:!/calc\\(100vh/.test(s),hasSafeAreaBottom:s.includes('env(safe-area-inset-bottom)'),hasSafeAreaTop:s.includes('env(safe-area-inset-top)'),hasBottomTabBarOffset:s.includes('4rem'),hasRoleApplication:s.includes('role=\"application\"'),hasSelectedPinState:s.includes('selectedPin'),hasDocTitle:s.includes('Mapa de descubrimientos'),hasIsLoadingBranch:s.includes('if (isLoading)'),hasEmptyBranch:s.includes('plants.length === 0')};const fails=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);if(fails.length){console.error('FAIL:',fails);process.exit(1);}console.log('OK MapPage');"</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/MapPage.tsx` existe con `export default function MapPage()`
    - Importa: useEffect/useState/Navigate/MapContainer/TileLayer/Marker/useWildPlantsWithCoords/FitBoundsOnMount/buildPlantPinIcon/track
    - Hook `useWildPlantsWithCoords()` invocado, `plants` y `isLoading` extraídos
    - `useState<WildPlantWithCoords | null>(null)` declarado para selectedPin (Plan 06 hook)
    - Branch loading: render container vacío con aria-busy="true" + altura calc 100dvh
    - Branch empty (`plants.length === 0`): retorna `<Navigate to="/" replace />` (NO empty state copy)
    - Branch normal: render `<MapContainer>` con `<TileLayer>` + N `<Marker>` + `<FitBoundsOnMount>`
    - TileLayer URL exacta `https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png` (per D-03)
    - Attribution string contiene `openstreetmap.org/copyright` con `target="_blank"` y `rel="noopener"`
    - Wrapper height: `calc(100dvh - 4rem - env(safe-area-inset-bottom) - env(safe-area-inset-top))` (exact)
    - Contiene `role="application"` y `aria-label="Mapa de descubrimientos de plantas"` (UI-SPEC línea 372)
    - `document.title = "Mapa de descubrimientos · Mi Plantita Feliz"` seteado en useEffect mount
    - `track("map_opened", { pin_count: plants.length })` invocado una sola vez (gated por `trackedOpen` flag)
    - NO contiene `100vh` (solo `100dvh`)
    - NO contiene PlantMapSheet (Plan 06 lo añade)
    - NO contiene `track("map_pin_tapped"...` ni `track("map_navigated_to_detail"...` (Plan 07 los añade)
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>MapPage creado con hook + MapContainer + N Markers + FitBoundsOnMount + redirect-on-empty + atribución OSM + 100dvh + role=application + tracking de map_opened.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Registrar ruta /mapa en src/App.tsx con AppLayout fullBleed</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (CURRENT — entender bloque de imports + bloque `<Routes>` líneas 52-65)
    - .planning/phases/03.1-plant-map-v0/03.1-PATTERNS.md líneas 38-64 (§ 1 App.tsx delta)
    - .planning/phases/03.1-plant-map-v0/03.1-UI-SPEC.md línea 770 (Route element con fullBleed)
    - src/layouts/AppLayout.tsx (modificado en Task 3 — verificar que acepta fullBleed prop)
  </read_first>
  <action>
    Modificar `src/App.tsx`:

    **Cambio 1** — añadir import de MapPage cerca de los otros page imports (verificar bloque alrededor de línea 8-11 donde están `Index`, `History`, etc.):

    ```tsx
    import MapPage from "./pages/MapPage";
    ```

    Insertar este import en el bloque de imports de páginas, en orden alfabético si los existentes lo siguen, o cerca del import de PlantDetail si no. NO duplicar imports existentes.

    **Cambio 2** — añadir la ruta `/mapa` dentro del `<Routes>` block, INMEDIATAMENTE DESPUÉS de la línea `<Route path="/ajustes" ... />` (línea 56 actual) y ANTES de las redirects de `/login`/`/signup`. Esto agrupa la ruta junto a las demás "auth-gated functional routes" antes del bloque legal (PATTERNS.md línea 62):

    ```tsx
    <Route path="/mapa" element={<AppLayout fullBleed><MapPage /></AppLayout>} />
    ```

    Nota: la prop `fullBleed` se pasa SIN valor explícito (`<AppLayout fullBleed>`) — TypeScript / JSX lo evalúa como `fullBleed={true}`. Esto es equivalente a `fullBleed={true}` pero más conciso. Si el proyecto tiene linter strict que prefiere explícito, usar `fullBleed={true}`.

    Estado final esperado del bloque `<Routes>` (líneas 52-66 aprox tras el cambio):

    ```tsx
    <Routes>
      <Route path="/" element={<AppLayout><Index /></AppLayout>} />
      <Route path="/mis-plantas" element={<AppLayout><History /></AppLayout>} />
      <Route path="/planta/:id" element={<AppLayout><PlantDetail /></AppLayout>} />
      <Route path="/ajustes" element={<AppLayout><SettingsPage /></AppLayout>} />
      <Route path="/mapa" element={<AppLayout fullBleed><MapPage /></AppLayout>} />
      <Route path="/login" element={<Navigate to="/mis-plantas" replace />} />
      <Route path="/signup" element={<Navigate to="/mis-plantas" replace />} />
      {/* Legal pages — accessible without login */}
      <Route path="/privacidad" element={<PrivacyPolicy />} />
      <Route path="/cookies" element={<CookiePolicy />} />
      <Route path="/aviso-legal" element={<LegalNotice />} />
      <Route path="/terminos" element={<TermsOfService />} />
      <Route path="*" element={<NotFound />} />
    </Routes>
    ```

    NO modificar:
    - Otros imports
    - BrowserRouter wrapper
    - El listener `mp:pending-classification-resolved` global si existe
    - Cualquier provider o wrapper
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/App.tsx','utf8');const checks={hasMapPageImport:/import\\s+MapPage\\s+from\\s+[\"']\\.\\/pages\\/MapPage[\"']/.test(s),hasMapaRoute:/Route\\s+path=[\"']\\/mapa[\"']/.test(s),hasFullBleedProp:/<AppLayout\\s+fullBleed(\\s|>)/.test(s),hasMapPageElement:s.includes('<MapPage />'),preservesIndexRoute:s.includes('<Route path=\"/\" element=')&&s.includes('<Index />'),preservesHistoryRoute:s.includes('<Route path=\"/mis-plantas\"'),preservesPlantDetailRoute:s.includes('<Route path=\"/planta/:id\"'),preservesNotFound:s.includes('<Route path=\"*\" element={<NotFound />} />')};const fails=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);if(fails.length){console.error('FAIL:',fails);process.exit(1);}console.log('OK App.tsx route registration');"</automated>
  </verify>
  <acceptance_criteria>
    - `src/App.tsx` contiene `import MapPage from "./pages/MapPage"` (o equivalente exacto)
    - `src/App.tsx` contiene la ruta `<Route path="/mapa" element={<AppLayout fullBleed><MapPage /></AppLayout>} />`
    - La ruta `/mapa` aparece en el bloque `<Routes>` ANTES de la primera ruta legal (`/privacidad`)
    - Las rutas existentes (`/`, `/mis-plantas`, `/planta/:id`, `/ajustes`, `/login`, `/signup`, `/privacidad`, `/cookies`, `/aviso-legal`, `/terminos`, `*`) siguen presentes sin cambios
    - `npx tsc --noEmit` exit 0
    - `npm run build` completa exit 0 (verifica que el bundle entero resuelve sin errores)
  </acceptance_criteria>
  <done>App.tsx registra MapPage en /mapa dentro de AppLayout con fullBleed. Sin regresión de rutas existentes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→OSM CDN | TileLayer hace requests a tile.openstreetmap.org (CDN público, sin auth, raster PNG) |
| client→React Router | URL `/mapa` mapea a página; navegación con `<Navigate to="/">` consume react-router state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-13 | Information disclosure | Tiles de OSM podrían ser monitoreadas por el CDN (heatmap de zonas que el user visita en su mapa) | accept | OSM tile policy + Vercel routing: requests son anónimos (cookies third-party bloqueadas en Safari ITP por defecto). Coordenadas vienen de las propias filas del user. Riesgo de privacy bajo. Si emerge, evaluar self-hosted tile server en v1+. |
| T-03.1-14 | Spoofing | Atribución OSM como `<a>` link a externo | mitigate | `target="_blank" rel="noopener noreferrer"` previene window.opener access. Link es a `openstreetmap.org` constante (no derivado de input). |
| T-03.1-15 | XSS | divIcon HTML inline en buildPlantPinIcon | mitigate | El HTML es string literal hardcoded — no inserta data dinámica del user. Plant data (name, scientific) NO se renderiza en el pin (va al sheet en Plan 06 vía React, donde escape default protege). |
| T-03.1-16 | Open redirect | `<Navigate to="/">` cuando 0 pins | mitigate | Path "/" es literal, no derivado de query string. Sin riesgo. |
| T-03.1-17 | DoS | MapContainer con muchos Markers (>200) podría laggear | accept | RESEARCH.md verificó <20 pins typical, ~200 max. Direct render aceptable per SPEC.md Constraints. Clustering deferred a v1+. |
| T-03.1-18 | Tampering | center/zoom placeholder en MapContainer | accept | Inmutable después de mount (per react-leaflet@4); FitBoundsOnMount sobreescribe. Sin riesgo de XSS — son números primitivos. |
</threat_model>

<verification>
- 3 archivos nuevos existen: build-plant-pin-icon.ts, FitBoundsOnMount.tsx, MapPage.tsx
- 2 archivos modificados: AppLayout.tsx (fullBleed prop), App.tsx (ruta /mapa)
- `npx tsc --noEmit` exit 0
- `npm run build` exit 0 (importante: verifica que leaflet/react-leaflet resuelve correctamente)
- `npm test -- --run` no muestra regresiones en tests existentes (use-context-counts.test.ts, use-wild-plants-with-coords.test.ts, use-plant-identifier.test.ts, auth.service.test.ts, etc.)
- Cross-platform:
  - **Android Capacitor 8.3.1 WebView**: `npm run build:android` debe completar sin error de bundling. `base: './'` resuelve paths relativos del CSS de leaflet correctamente (verificado en RESEARCH.md Pitfall Android #1).
  - **iOS PWA Safari 16+**: `100dvh` + `env(safe-area-inset-*)` ya soportados. `viewport-fit=cover` + `user-scalable=no` ya en `index.html` línea 5 (verificado por grep).
- A11y:
  - MapPage tiene `role="application"` y `aria-label`
  - Loading state tiene `aria-busy="true"`
  - Pins tienen `title={plant.name}` (Leaflet lo renderiza como atributo accesible)
  - prefers-reduced-motion respetado en FitBoundsOnMount
</verification>

<success_criteria>
- Ruta `/mapa` carga MapPage sin NotFound (SPEC-AC4)
- N pins en DOM como `.leaflet-marker-icon` (SPEC-AC5)
- 1 pin → setView zoom=14; N>1 pins → fitBounds con padding 40 (SPEC-AC6, D-14/D-15)
- Atribución OSM visible vía Leaflet's `.leaflet-control-attribution` (SPEC-AC11)
- 0 pins → redirect a "/" silencioso (sketch winner, NO empty state copy)
- Tiles fallan → pins visibles sobre fondo gris, sin crash (SPEC-AC12)
- TypeScript compila, build pasa
- AppLayout backward-compatible (fullBleed default false)
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-plant-map-v0/03.1-05-SUMMARY.md`
</output>
