---
phase: 03.1-plant-map-v0
plan: 06
status: complete
completed: 2026-05-16
requirements_addressed: [SPEC-R6, SPEC-R7, SPEC-AC7, SPEC-AC8]
---

# Plan 03.1-06 — PlantMapSheet + integración con MapPage · SUMMARY

## What was built

**Task 1 — `src/components/PlantMapSheet.tsx` (NEW · 130 LOC)**
Bottom sheet preview con todas las decisiones lockeadas del sketch 004 winner: foto `aspect-[4/3]`, sin eyebrow chip, CTAs stacked vertical full-width ("Ver detalle" hero + "Cerrar" outline), drag-to-close NO supported (D-08), `max-h-[75dvh]`, `pb-[max(1.5rem,env(safe-area-inset-bottom))]` para iPhone home indicator. Incluye helper `splitNameField` con fallback grácil para asunción A2 (formato `"Common (Scientific)"`). Fecha formateada con date-fns + locale es.

**Task 2 — `src/pages/MapPage.tsx` (MODIFIED · +2/-2 LOC)**
Añadido import + 1 línea JSX sibling de `</MapContainer>` para montar `<PlantMapSheet plant={selectedPin} onClose={...} />`. Removido el `eslint-disable-next-line @typescript-eslint/no-unused-vars` porque selectedPin ya está consumido.

## Files changed

- `src/components/PlantMapSheet.tsx` (NEW)
- `src/pages/MapPage.tsx` (MODIFIED · +2/-2)

## Verification

- `npx tsc --noEmit` → exit 0
- `npm test -- --run` → 108/108 pasando (sin regresiones)

## SPEC coverage

- **SPEC-R6**: ✓ tap pin → sheet abre con foto + nombre + científico + fecha + 2 CTAs
- **SPEC-R7**: ✓ "Ver detalle" hace `navigate(\`/planta/${plant.id}\`)`
- **SPEC-AC7**: ✓ Sheet `role="dialog"` (Radix Dialog default) con `<img>` (src=imageUrl), nombre Fraunces, científico italic, fecha en español
- **SPEC-AC8**: ✓ tap "Ver detalle" → URL cambia a `/planta/{id}` (ruta ya existe Phase 02.1)

## What Plan 07 needs to do

- Modificar handleViewDetail para añadir `track("map_navigated_to_detail", { plant_search_id, from: "pin_sheet" })` ANTES del navigate
- Modificar el Marker eventHandlers.click para añadir `track("map_pin_tapped", { plant_search_id, pin_index_among_total })`
- Añadir sección `## Phase 03.1 — Plant Map v0` a `docs/posthog-events.md` documentando los 3 eventos + funnel

## Commits

- `d31b628` feat(03.1-06): add PlantMapSheet — bottom sheet preview con foto 4:3 + CTAs stacked
- `e7a741f` feat(03.1-06): mount PlantMapSheet in MapPage keyed off selectedPin state
