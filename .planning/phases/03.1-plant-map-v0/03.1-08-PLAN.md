---
phase: 03.1-plant-map-v0
plan: 08
type: execute
wave: 5
depends_on: [04, 05, 06, 07]
files_modified:
  - e2e/map.spec.ts
  - e2e/fixtures.ts
autonomous: true
requirements: [SPEC-AC1, SPEC-AC4, SPEC-AC5, SPEC-AC7, SPEC-AC8, SPEC-AC9, SPEC-AC11, SPEC-AC12]
requirements_addressed: [SPEC-AC1, SPEC-AC4, SPEC-AC5, SPEC-AC7, SPEC-AC8, SPEC-AC9, SPEC-AC11, SPEC-AC12]
tags: [phase-03.1, e2e, playwright, smoke, happy-path]

must_haves:
  truths:
    - "Existe e2e/map.spec.ts (NO tests/e2e/map.spec.ts — landmine #1 PATTERNS.md) con un happy path Playwright que cubre SPEC-AC9"
    - "El test mockea OSM tiles para que sean deterministas/offline-safe (no requiere red al CDN OSM)"
    - "El test cubre: navegación a /mapa, render de pins, tap pin, sheet visible, tap 'Ver detalle', URL cambia a /planta/:id"
    - "El test secundario cubre AC1: tab Mapa NO visible cuando no hay wild-con-coords"
    - "El test secundario cubre AC12: si OSM tile request es bloqueado, el mapa no crashea y los pins siguen siendo visibles"
    - "fixtures.ts añade un MOCK_WILD_WITH_COORDS array reutilizable por map.spec.ts"
  artifacts:
    - path: "e2e/map.spec.ts"
      provides: "Playwright happy path + secondary cases (tab visibility, tile failure)"
      contains: "test.describe(\"Plant map"
      min_lines: 80
    - path: "e2e/fixtures.ts"
      provides: "Exporta MOCK_WILD_WITH_COORDS + helper para mockear plant_searches con wild data"
      contains: "MOCK_WILD_WITH_COORDS"
  key_links:
    - from: "e2e/map.spec.ts"
      to: "e2e/fixtures.ts (asAuthenticated + MOCK_WILD_WITH_COORDS)"
      via: "import { test, expect, MOCK_WILD_WITH_COORDS } from \"./fixtures\""
      pattern: "from \"./fixtures\""
    - from: "e2e/map.spec.ts"
      to: "page.route(**/tile.openstreetmap.org/**)"
      via: "route.abort para offline-safe testing"
      pattern: "tile.openstreetmap.org"
---

<objective>
Crear el test E2E Playwright (`e2e/map.spec.ts`) que ejecuta el happy path lockeado en SPEC-AC9: cargar `/mapa` directamente como usuario authenticated con wild-con-coords mockeadas → verificar render de pins → tap pin → sheet visible con datos correctos → tap "Ver detalle" → URL cambia a `/planta/:id`. Adicionalmente añadir 2-3 secondary cases (AC1 tab visibility, AC12 tile failure resilience).

LANDMINE #1 CRÍTICO: el directorio E2E del repo es `e2e/` (verificado: `playwright.config.ts:4` → `testDir: "./e2e"`). NO `tests/e2e/` como decía SPEC.md por error. Cualquier test escrito en `tests/e2e/` no sería descubierto por Playwright y el plan fallaría silenciosamente.

Extender `e2e/fixtures.ts` con `MOCK_WILD_WITH_COORDS` para que los tests sean self-contained. Bloquear las requests a `tile.openstreetmap.org` con `page.route(..., abort)` para que los tests no dependan de la red ni de la disponibilidad del CDN OSM.

Purpose: Wave 4 entrega el quality gate. Sin este test, no hay garantía automatizada de que el feature funciona end-to-end. Es la verificación que cierra SPEC-AC9 explícitamente.
Output: 1 archivo nuevo (~120 LOC test) + 1 archivo modificado (~25 LOC mock data añadidas).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03.1-plant-map-v0/03.1-CONTEXT.md
@.planning/phases/03.1-plant-map-v0/03.1-SPEC.md
@.planning/phases/03.1-plant-map-v0/03.1-RESEARCH.md
@.planning/phases/03.1-plant-map-v0/03.1-PATTERNS.md
@.planning/phases/03.1-plant-map-v0/03.1-04-PLAN.md
@.planning/phases/03.1-plant-map-v0/03.1-05-PLAN.md
@.planning/phases/03.1-plant-map-v0/03.1-06-PLAN.md
@.planning/phases/03.1-plant-map-v0/03.1-07-PLAN.md
@e2e/fixtures.ts
@e2e/history.spec.ts
@e2e/identify.spec.ts
@playwright.config.ts
@CLAUDE.md

<interfaces>
CONSUME:
- Plan 04: BottomTabBar tab "Mapa" condicional
- Plan 05: MapPage en /mapa, pins como `.leaflet-marker-icon`
- Plan 06: PlantMapSheet con role="dialog" + button "Ver detalle"
- Plan 07: track calls (no se assertan en E2E, pero PostHog está blocked en fixtures por defecto)
- e2e/fixtures.ts: ya tiene `asAuthenticated`, `MOCK_USER`, `MOCK_SESSION`, helpers de mock para Supabase

CREATES:
```typescript
// e2e/fixtures.ts (MODIFIED) — añadir nuevo export
export const MOCK_WILD_WITH_COORDS: Array<{
  id: string;
  name: string;            // formato "Common (Scientific)"
  description: string | null;
  care: string;
  diagnosis: string;
  image_url: string;
  context: "wild";
  lat: number;
  lng: number;
  created_at: string;
  user_id: string;
}>;
```

CONFIGURACIÓN PLAYWRIGHT (verificado en playwright.config.ts):
- testDir: "./e2e" (NOT "./tests/e2e")
- baseURL probably http://localhost:5173 o similar (verificar)
- timeout: 30_000ms global

PATTERN E2E (verbatim de e2e/history.spec.ts líneas 1-15, e2e/identify.spec.ts líneas 23-58):
- `import { test, expect, MOCK_... } from "./fixtures"`
- `test.describe("...", () => { test("...", async ({ page, asAuthenticated }) => { ... }) })`
- `await page.route("**/rest/v1/plant_searches*", (route) => route.fulfill({ status: 200, json: items }))`
- `await page.goto("/mapa")`
- `await expect(page.locator(".leaflet-marker-icon")).toHaveCount(N)`
- `await expect(page.getByRole("dialog")).toBeVisible()`
- `await page.getByRole("button", { name: "Ver detalle" }).click()`
- `await expect(page).toHaveURL(/\/planta\/[a-z0-9-]+/)`

TILE BLOCK (RESEARCH.md menciona en e2e §):
```typescript
await page.route("**/tile.openstreetmap.org/**", (route) => route.abort());
```

NOTE on `mockPlantHistory` (fixtures.ts líneas ~140-150): mockea TODOS los requests a `plant_searches*`. El test de map.spec.ts probablemente NECESITARÁ sobreescribir esto a nivel test (porque el mock default devuelve `MOCK_HISTORY_ITEMS` que NO tienen `context: 'wild'` ni lat/lng poblados). Solución: `await page.route` ANTES de `page.goto` para que tome precedencia.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Añadir MOCK_WILD_WITH_COORDS a e2e/fixtures.ts</name>
  <files>e2e/fixtures.ts</files>
  <read_first>
    - e2e/fixtures.ts COMPLETO (líneas 1-225 — entender mocks existentes: MOCK_USER, MOCK_SESSION, MOCK_PLANT, MOCK_HISTORY_ITEMS, mockSupabaseAuthenticated, mockPlantHistory, asAuthenticated fixture)
    - .planning/phases/03.1-plant-map-v0/03.1-PATTERNS.md líneas 745-755 (menciona MOCK_WILD_WITH_COORDS)
  </read_first>
  <action>
    Añadir a `e2e/fixtures.ts` un nuevo export `MOCK_WILD_WITH_COORDS` después del export `MOCK_HISTORY_ITEMS` (alrededor de la línea 75-80). Reutilizar `MOCK_USER.id` para `user_id`. Coordenadas de ejemplo: Madrid centro (40.4168, -3.7038), Lisboa (38.7223, -9.1393), Barcelona (41.3851, 2.1734) — 3 ciudades distintas para que fitBounds tenga trabajo real.

    Contenido exacto a appendear después del export `MOCK_HISTORY_ITEMS`:

    ```typescript

    // ---------------------------------------------------------------------------
    // Phase 03.1 — Wild plants with coords (for /mapa tests)
    // ---------------------------------------------------------------------------

    export const MOCK_WILD_WITH_COORDS = [
      {
        id: "wild-001",
        name: "Salvia rosmarinus (Rosmarinus officinalis)",
        description: "Romero silvestre encontrado en zona forestal.",
        care: "Riego escaso, luz directa, suelo seco.",
        diagnosis: "Buen estado.",
        image_url: "data:image/jpeg;base64,/9j/wild001",
        context: "wild" as const,
        lat: 40.4168, // Madrid centro
        lng: -3.7038,
        created_at: "2026-05-10T10:00:00Z",
        user_id: MOCK_USER.id,
      },
      {
        id: "wild-002",
        name: "Lavandula angustifolia (Lavandula angustifolia)",
        description: "Lavanda descubierta en Lisboa.",
        care: "Sol pleno, riego moderado.",
        diagnosis: "Buen estado.",
        image_url: "data:image/jpeg;base64,/9j/wild002",
        context: "wild" as const,
        lat: 38.7223, // Lisboa
        lng: -9.1393,
        created_at: "2026-04-20T14:30:00Z",
        user_id: MOCK_USER.id,
      },
      {
        id: "wild-003",
        name: "Olea europaea (Olea europaea)",
        description: "Olivo joven en parque urbano.",
        care: "Sol directo, poco riego.",
        diagnosis: "Buen estado.",
        image_url: "data:image/jpeg;base64,/9j/wild003",
        context: "wild" as const,
        lat: 41.3851, // Barcelona
        lng: 2.1734,
        created_at: "2026-03-15T09:00:00Z",
        user_id: MOCK_USER.id,
      },
    ];
    ```

    NO modificar:
    - MOCK_USER, MOCK_SESSION, MOCK_PLANT, MOCK_HISTORY_ITEMS existentes
    - mockSupabaseAnonymous, mockSupabaseAuthenticated, mockIdentifyPlant, mockPlantHistory, mockClaimSearches helpers
    - El export `test` con fixtures `asAnonymous`, `asAuthenticated`
    - El re-export `expect from @playwright/test`
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('e2e/fixtures.ts','utf8');const checks={hasExport:s.includes('export const MOCK_WILD_WITH_COORDS'),hasWild001:s.includes('id: \"wild-001\"'),hasWild002:s.includes('id: \"wild-002\"'),hasWild003:s.includes('id: \"wild-003\"'),hasContextWild:(s.match(/context: \"wild\" as const/g)||[]).length>=3,hasLatLng:/lat:\\s*40\\.4168/.test(s)&&/lng:\\s*-3\\.7038/.test(s),hasUserIdRef:s.includes('user_id: MOCK_USER.id'),hasParenSciFormat:s.includes('(Rosmarinus officinalis)'),preservesMockUser:s.includes('export const MOCK_USER'),preservesAsAuthenticated:s.includes('asAuthenticated:')};const fails=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);if(fails.length){console.error('FAIL:',fails);process.exit(1);}console.log('OK fixtures MOCK_WILD_WITH_COORDS');"</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/fixtures.ts` contiene `export const MOCK_WILD_WITH_COORDS = [`
    - El array tiene exactamente 3 elementos con ids `wild-001`, `wild-002`, `wild-003`
    - Cada elemento tiene `context: "wild" as const`, `lat: number`, `lng: number` (NO null)
    - Las coords son 3 ciudades distintas (Madrid 40.4168/-3.7038, Lisboa 38.7223/-9.1393, Barcelona 41.3851/2.1734)
    - Cada elemento `name` tiene formato `"Common (Scientific)"` (con paréntesis)
    - `user_id` referencia `MOCK_USER.id` (no string hardcoded)
    - El export `MOCK_HISTORY_ITEMS` sigue presente
    - El export `test` (extend de @playwright/test) sigue presente
    - `e2e/fixtures.ts` sigue siendo TypeScript válido (`npx tsc --noEmit e2e/fixtures.ts` o el comando que use el proyecto para chequear types de e2e — verificar en `tsconfig.json` si hay separate config)
  </acceptance_criteria>
  <done>fixtures.ts exporta MOCK_WILD_WITH_COORDS con 3 elementos válidos, sin modificar exports existentes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Crear e2e/map.spec.ts con happy path + secondary cases</name>
  <files>e2e/map.spec.ts</files>
  <read_first>
    - e2e/history.spec.ts COMPLETO (analog primario — test.describe + asAuthenticated + page.route patterns)
    - e2e/identify.spec.ts (analog secundario — multi-step flow + setInputFiles + intermediate assertions)
    - e2e/fixtures.ts (después del Task 1 — verificar que MOCK_WILD_WITH_COORDS está exportado)
    - .planning/phases/03.1-plant-map-v0/03.1-PATTERNS.md líneas 700-758 (§ 15 e2e delta)
    - .planning/phases/03.1-plant-map-v0/03.1-SPEC.md AC1, AC4, AC5, AC7, AC8, AC9, AC11, AC12
    - playwright.config.ts (verificar testDir y baseURL)
  </read_first>
  <action>
    Crear `e2e/map.spec.ts` (LANDMINE #1: ¡NO `tests/e2e/map.spec.ts`!). Contenido exacto:

    ```typescript
    import { test, expect, MOCK_WILD_WITH_COORDS, MOCK_USER } from "./fixtures";

    /**
     * Phase 03.1 — Plant Map v0 E2E tests
     *
     * Cubre SPEC-AC9 happy path: identify wild → tab appears → /mapa →
     * pin → sheet → "Ver detalle" → /planta/:id. Adicionalmente: AC1
     * (tab not visible), AC12 (tile failure resilience).
     *
     * Las requests a tile.openstreetmap.org se bloquean en todos los tests
     * (page.route abort) para que sean offline-safe y deterministas.
     *
     * El test no asserts tracking events porque fixtures.blockAnalytics
     * bloquea PostHog endpoints — los eventos se loggean pero no se envían.
     */

    test.describe("Plant map (/mapa)", () => {
      // ─────────────────────────────────────────────────────────────────────
      // SPEC-AC9 happy path: /mapa → pin → sheet → ver detalle → /planta/:id
      // ─────────────────────────────────────────────────────────────────────
      test("happy path: navigate /mapa, see pins, tap pin, open sheet, navigate to detail", async ({
        page,
        asAuthenticated,
      }) => {
        // Mock plant_searches to return only wild-with-coords (override fixtures default).
        // page.route precedence: the LAST matching route handler wins, so this
        // overrides the asAuthenticated default mock.
        await page.route("**/rest/v1/plant_searches*", (route) => {
          if (route.request().method() === "DELETE") {
            return route.fulfill({ status: 200, json: [] });
          }
          return route.fulfill({
            status: 200,
            headers: {
              "content-range": `0-${MOCK_WILD_WITH_COORDS.length - 1}/${MOCK_WILD_WITH_COORDS.length}`,
            },
            json: MOCK_WILD_WITH_COORDS,
          });
        });

        // Block OSM tile requests so the test is offline-safe + deterministic.
        await page.route("**/tile.openstreetmap.org/**", (route) => route.abort());

        // Step 1 — AC4: navigate directly to /mapa loads MapPage (not NotFound).
        await page.goto("/mapa");

        // Step 2 — AC5: exactly N markers rendered in the DOM.
        // Wait for first marker to appear (FitBoundsOnMount has fired).
        await expect(page.locator(".leaflet-marker-icon").first()).toBeVisible({
          timeout: 5000,
        });
        await expect(page.locator(".leaflet-marker-icon")).toHaveCount(
          MOCK_WILD_WITH_COORDS.length,
        );

        // Step 3 — AC11: OSM attribution link is visible.
        await expect(page.locator(".leaflet-control-attribution")).toBeVisible();
        await expect(
          page.locator(".leaflet-control-attribution a[href*='openstreetmap.org/copyright']"),
        ).toBeVisible();

        // Step 4 — tap the first pin → sheet opens (AC7).
        await page.locator(".leaflet-marker-icon").first().click();

        // Step 5 — AC7 details: sheet visible with role="dialog", common name + scientific.
        const dialog = page.getByRole("dialog");
        await expect(dialog).toBeVisible({ timeout: 5000 });
        // The first pin (by creation order, desc) is wild-001 → "Salvia rosmarinus".
        await expect(dialog.getByText("Salvia rosmarinus")).toBeVisible();
        // Scientific (italic) should also be present.
        await expect(dialog.getByText("Rosmarinus officinalis")).toBeVisible();
        // Date should mention "Identificada el ... de ... de 2026".
        await expect(dialog.getByText(/Identificada el .* de .* de 2026/)).toBeVisible();

        // Step 6 — AC8: tap "Ver detalle" → URL changes to /planta/wild-001.
        await dialog.getByRole("button", { name: "Ver detalle" }).click();
        await expect(page).toHaveURL(/\/planta\/wild-001/, { timeout: 5000 });
      });

      // ─────────────────────────────────────────────────────────────────────
      // SPEC-AC1: tab "Mapa" NOT visible when user has zero wild-with-coords.
      // ─────────────────────────────────────────────────────────────────────
      test("tab Mapa NOT visible when user has no wild-with-coords", async ({
        page,
        asAuthenticated,
      }) => {
        // Mock plant_searches to return ZERO rows (empty array).
        await page.route("**/rest/v1/plant_searches*", (route) =>
          route.fulfill({
            status: 200,
            headers: { "content-range": "0-0/0" },
            json: [],
          }),
        );

        await page.goto("/");
        // Wait for the bar to render any tab (Inicio always exists).
        await expect(page.getByRole("button", { name: "Inicio" })).toBeVisible({
          timeout: 5000,
        });
        // The "Mapa" tab should NOT be present.
        await expect(page.getByRole("button", { name: "Mapa" })).toHaveCount(0);
      });

      // ─────────────────────────────────────────────────────────────────────
      // SPEC-AC12: if OSM tiles fail to load, MapPage does not crash.
      // ─────────────────────────────────────────────────────────────────────
      test("MapPage survives OSM tile failures (pins still visible on gray)", async ({
        page,
        asAuthenticated,
      }) => {
        await page.route("**/rest/v1/plant_searches*", (route) =>
          route.fulfill({
            status: 200,
            headers: {
              "content-range": `0-${MOCK_WILD_WITH_COORDS.length - 1}/${MOCK_WILD_WITH_COORDS.length}`,
            },
            json: MOCK_WILD_WITH_COORDS,
          }),
        );
        // Block ALL tile requests with 503 (simulates CDN down).
        await page.route("**/tile.openstreetmap.org/**", (route) =>
          route.fulfill({ status: 503, body: "" }),
        );

        await page.goto("/mapa");

        // Container renders (not NotFound).
        await expect(page.locator(".leaflet-container")).toBeVisible({ timeout: 5000 });

        // Pins still visible (Leaflet renders markers regardless of tile load state).
        await expect(page.locator(".leaflet-marker-icon")).toHaveCount(
          MOCK_WILD_WITH_COORDS.length,
        );

        // No toast / error banner should have appeared. The MapPage does not
        // dispatch any toast on tile failure (SPEC.md Constraints).
        await expect(page.getByRole("status")).toHaveCount(0); // no aria-live toasts
      });
    });
    ```

    Decisiones implementadas:
    - **LANDMINE #1**: archivo en `e2e/`, no `tests/e2e/`
    - **AC9 happy path**: 6 steps cubriendo navegación, render pins, atribución, tap pin, sheet, "Ver detalle", URL change
    - **AC1 secondary**: nuevo test que verifica tab Mapa NO renderiza con 0 wild-with-coords
    - **AC12 secondary**: nuevo test que verifica resiliencia a tile failures (503)
    - **AC5**: `toHaveCount(MOCK_WILD_WITH_COORDS.length)` con datos mockados (3)
    - **AC11**: assert sobre `.leaflet-control-attribution` link
    - **AC4**: assert que `/mapa` no es NotFound (verificable porque renderiza `.leaflet-container`)
    - **AC7**: `getByRole("dialog")` visible + plant data presente
    - **AC8**: `toHaveURL(/\/planta\/wild-001/)`
    - **route precedence**: `page.route` se llama DESPUÉS del fixture `asAuthenticated` por lo que toma precedencia per Playwright docs
    - **OSM tile abort** en happy path para offline-safe + 503 en AC12 test para simular CDN down
    - **Tracking NO assertado**: blockAnalytics del fixture ya filtra PostHog requests
    - **`content-range` header**: incluido porque `useContextCounts` (Plan 02) usa `count: "exact"` que requiere este header de PostgREST

    Nota sobre la query del hook `useContextCounts`: el BottomTabBar (cuando user navega a /, Index page se renderiza CON el BottomTabBar visible) llamará a `useContextCounts` que hace 4 queries paralelas. Esas 4 queries pegan al mock de `**/rest/v1/plant_searches*` — todas devuelven el mismo data array, pero como usan `head: true`, el data array es ignorado y solo se lee el header `content-range`. Por eso es importante setear el header correctamente.

    Para el test happy path: el mock devuelve `MOCK_WILD_WITH_COORDS` (3 rows). El hook `useWildPlantsWithCoords` recibe esos 3 elementos. El hook `useContextCounts` recibe también el array y lee el `content-range` header como `3` → la tab Mapa se renderiza. Coherente.
  </action>
  <verify>
    <automated>npx playwright test e2e/map.spec.ts --reporter=list 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/map.spec.ts` existe (NO `tests/e2e/map.spec.ts`)
    - El archivo importa de `./fixtures` (NO `@playwright/test` raw)
    - Contiene `test.describe("Plant map (/mapa)"`
    - Contiene >= 3 test cases: happy path AC9, AC1 (tab not visible), AC12 (tile failure)
    - Happy path assertea: `.leaflet-marker-icon` count = 3, `getByRole("dialog")` visible, common name "Salvia rosmarinus" visible, scientific "Rosmarinus officinalis" visible, `toHaveURL(/\/planta\/wild-001/)`
    - Cada test bloquea `**/tile.openstreetmap.org/**` con `route.abort()` o `route.fulfill 503`
    - Test AC1 NO espera markers, sino que verifica `getByRole("button", { name: "Mapa" })` count 0
    - `npx playwright test e2e/map.spec.ts` exit 0 (todos los tests pasan)
    - El test output reporta `3 passed` (o más si añades casos adicionales)
  </acceptance_criteria>
  <done>e2e/map.spec.ts pasa todos sus tests (happy path AC9 + AC1 + AC12), corre offline-safe con tiles bloqueados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-isolated | Tests Playwright corren contra build local (dev server o preview), con Supabase + PostHog + OSM CDN totalmente mockeados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-26 | Information disclosure | Mock data contiene coords reales de ciudades públicas | accept | Madrid, Lisboa, Barcelona son coordenadas no sensibles (no PII de users reales). Datos de prueba. |
| T-03.1-27 | Tampering | El test depende de strings literales del UI ("Ver detalle", "Inicio", "Mapa", "Salvia rosmarinus") | accept | Si el copy cambia, el test fallará explícitamente — es el comportamiento deseado (catches regressions). Cubierto por revisión humana del PR. |
| T-03.1-28 | DoS | E2E tests podrían colgar si Playwright timeout default (30s) no es suficiente para el render del map + fitBounds | mitigate | Cada `await expect` tiene `{ timeout: 5000 }` explícito. Si emerge flakiness, ajustar; current 5s es generoso para mock data. |
</threat_model>

<verification>
- `e2e/map.spec.ts` existe en directorio correcto (e2e/, NO tests/e2e/)
- `e2e/fixtures.ts` exporta `MOCK_WILD_WITH_COORDS` (3 elementos)
- `npx playwright test e2e/map.spec.ts` exit 0
- Output reporta `3 passed` (happy path + AC1 + AC12)
- No regresión en otros e2e: `npx playwright test` (run completo) exit 0
- `npx tsc --noEmit` exit 0 (TypeScript de tests compila)
- Cross-platform: Playwright config probablemente incluye `mobile-chrome` (Capacitor approx) y `mobile-safari` (iOS PWA approx) per project default; verificable en playwright.config.ts. Si el config solo tiene desktop, el test cubre la lógica/markup pero NO los pinch touch quirks (RESEARCH.md A7).
</verification>

<success_criteria>
- 3+ tests pasando (happy path AC9 + AC1 + AC12)
- Tests deterministas (no dependen de red OSM)
- TypeScript de e2e compila
- No regresión de otros e2e tests
- Coverage explícito de AC9 (happy path), AC1 (no tab without wild), AC12 (tile failure)
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-plant-map-v0/03.1-08-SUMMARY.md`
</output>
