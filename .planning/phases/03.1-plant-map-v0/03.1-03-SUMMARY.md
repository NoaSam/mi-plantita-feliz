# Plan 03.1-03 Summary — useWildPlantsWithCoords hook

## Status: COMPLETE

## Tasks executed

### Task 1: src/hooks/use-wild-plants-with-coords.ts (NEW)
Hook que retorna `WildPlantWithCoords[]` — todas las plantas `context='wild'`
del usuario actual con `lat/lng` no nulos, ordenadas por `created_at DESC`.

- Pattern S3 verbatim de `use-unclassified-count.ts` (mountedRef + useAuth + useCallback load + useEffect)
- Filtro WHERE exacto SPEC-AC10: `.eq("user_id", user.id).eq("context", "wild").not("lat","is",null).not("lng","is",null)`
- `lat`/`lng` narrowed a `number` (cast seguro garantizado por WHERE)
- Sin `limit()` — lista completa (SPEC.md permite hasta ~200 pins)
- Gate anónimo D-07: devuelve `plants: []` sin tocar Supabase
- `description ?? ""` para defensa contra null

Exporta: `useWildPlantsWithCoords`, `WildPlantWithCoords`, `UseWildPlantsWithCoordsReturn`

### Task 2: src/hooks/use-wild-plants-with-coords.test.ts (NEW)
7 tests pasando (>= 6 requeridos):

1. `returns plants:[] and isLoading=false when no user (D-07)` — gate anónimo, 0 llamadas a Supabase
2. `applies WHERE filter context=wild + lat,lng NOT NULL + user_id (SPEC-AC10)` — verificación del filtro exacto
3. `orders by created_at descending` — ORDER BY correcto
4. `does NOT call limit() — returns full list` — sin limit aplicado
5. `maps snake_case columns to camelCase (image_url → imageUrl, created_at → createdAt)` — mapping shape exacto
6. `returns empty plants[] on Supabase error (graceful degradation)` — manejo de errores
7. `handles null description column gracefully (defaults to empty string)` — defensa contra null

## Commits
- `a28adf4` feat(03.1): add useWildPlantsWithCoords hook (SPEC-R3, SPEC-R8, SPEC-AC10)
- `26cbb37` test(03.1): add useWildPlantsWithCoords unit tests — 7 passing (SPEC-AC10)

## Success criteria verification
- [x] useWildPlantsWithCoords hook existe en src/hooks/use-wild-plants-with-coords.ts
- [x] Filtro WHERE: context='wild' AND lat IS NOT NULL AND lng IS NOT NULL ORDER BY created_at DESC
- [x] Retorna WildPlantWithCoords[] con lat/lng narrowed a number (no null)
- [x] Gate anónimo D-07: plants: [] sin llamar Supabase
- [x] use-wild-plants-with-coords.test.ts existe con 7 tests pasando
- [x] SPEC-AC10: test verifica el filtro WHERE exacto
- [x] TypeScript compila sin errores (npx tsc --noEmit exit 0)
- [x] Sin modificaciones a STATE.md o ROADMAP.md

## Requirements addressed
- SPEC-R3: hook useWildPlantsWithCoords existe y es consumible por MapPage (Plan 05)
- SPEC-R8: plantas wild sin coords excluidas silenciosamente (filtro WHERE, no UI error)
- SPEC-AC5: preparado — hook proporciona el array que MapPage usará para contar markers en DOM
- SPEC-AC10: test unitario verifica el filtro WHERE exacto

## Artifacts
- `/src/hooks/use-wild-plants-with-coords.ts` — 105 líneas (min_lines: 70 ✓)
- `/src/hooks/use-wild-plants-with-coords.test.ts` — 144 líneas (min_lines: 60 ✓)
