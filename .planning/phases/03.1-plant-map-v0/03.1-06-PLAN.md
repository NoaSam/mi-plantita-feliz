---
phase: 03.1-plant-map-v0
plan: 06
type: execute
wave: 3
depends_on: [05]
files_modified:
  - src/components/PlantMapSheet.tsx
  - src/pages/MapPage.tsx
autonomous: true
requirements: [SPEC-R6, SPEC-R7, SPEC-AC7, SPEC-AC8]
requirements_addressed: [SPEC-R6, SPEC-R7, SPEC-AC7, SPEC-AC8]
tags: [phase-03.1, plant-map-sheet, bottom-sheet, integration]

must_haves:
  truths:
    - "Existe el componente PlantMapSheet en src/components/PlantMapSheet.tsx que renderiza un shadcn Sheet side=bottom con preview de planta"
    - "El sheet muestra: foto aspect-[4/3] (no square per sketch winner), nombre común grande Fraunces, nombre científico italic muted, fecha formateada en español, 2 CTAs stacked vertical"
    - "Al tap pin → setSelectedPin(plant) en MapPage → sheet visible con role=dialog"
    - "Drag-down NO cierra el sheet (limitación shadcn Sheet locked per D-08); cierre vía botón 'Cerrar', overlay tap, Esc"
    - "Tap 'Ver detalle' → navigate(`/planta/${plant.id}`) — la ruta /planta/:id ya existe (Phase 02.1)"
    - "Sheet padding-bottom respeta safe-area-inset-bottom (iOS home indicator)"
    - "El sheet NO incluye eyebrow chip 'Descubrimiento' (sketch winner — redundante visualmente)"
    - "Si el formato de plant.name es 'Common (Scientific)', splitNameField extrae ambas partes; si no, muestra el name raw como common y omite scientific (fallback grácil per RESEARCH.md A2)"
  artifacts:
    - path: "src/components/PlantMapSheet.tsx"
      provides: "Bottom sheet con preview de planta; props {plant, onClose}"
      exports: ["PlantMapSheet"]
      min_lines: 80
    - path: "src/pages/MapPage.tsx"
      provides: "MapPage actualizado para montar PlantMapSheet controlado por selectedPin state"
      contains: "PlantMapSheet"
  key_links:
    - from: "src/components/PlantMapSheet.tsx"
      to: "@/components/ui/sheet (shadcn)"
      via: "Sheet/SheetContent/SheetHeader/SheetTitle/SheetDescription/SheetFooter"
      pattern: "@/components/ui/sheet"
    - from: "src/components/PlantMapSheet.tsx"
      to: "react-router-dom useNavigate"
      via: "navigate(`/planta/${plant.id}`) en CTA Ver detalle"
      pattern: "navigate\\(`/planta/"
    - from: "src/components/PlantMapSheet.tsx"
      to: "date-fns + locale es"
      via: "format(createdAt, ..., { locale: es })"
      pattern: "from \"date-fns/locale/es\""
    - from: "src/pages/MapPage.tsx"
      to: "src/components/PlantMapSheet.tsx"
      via: "mount como sibling de MapContainer, plant=selectedPin onClose=setSelectedPin(null)"
      pattern: "<PlantMapSheet"
---

<objective>
Crear el componente `PlantMapSheet` (`src/components/PlantMapSheet.tsx`) — el bottom sheet con preview de planta — y conectarlo al `MapPage.tsx` (Plan 05) para que se monte cuando `selectedPin !== null`. Implementar SPEC-R6 (sheet con foto, nombre común, científico, fecha, 2 CTAs) y SPEC-R7 (CTA "Ver detalle" navega a `/planta/:id`).

Replicar verbatim el patrón visual de `AnonClassificationWall.tsx` (Phase 02.1): shadcn `Sheet side="bottom"`, cream background + border-foreground, rounded-t-2xl, p-6, gap-2 footer stacked. Diferencias clave per sketch 004 winner: foto `aspect-[4/3]` (NO square), CTAs apilados verticales (NO lado-a-lado), SIN eyebrow chip "Descubrimiento" (sketch dice: redundante porque ya estás en /mapa). Fecha formateada con `date-fns` + locale `es` (UI-SPEC línea 215).

Incluir helper `splitNameField` (interno al sheet, no exportado) para extraer "Common (Scientific)" del campo `name` con fallback grácil per RESEARCH.md A2 (MEDIUM risk).

NO añadir el track call `map_navigated_to_detail` aquí — Plan 07 lo hace junto con los demás eventos PostHog (`map_pin_tapped`, `map_opened` refinements, docs/posthog-events.md update).

Purpose: Wave 3 cierra la experiencia interactiva del mapa. Sin este plan, MapPage solo muestra pins estáticos sin feedback al tap. Es la "razón de existir" de los pins.
Output: 1 archivo nuevo (~110 LOC) + edición mínima a MapPage (~3 LOC: 1 import + 1 JSX).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03.1-plant-map-v0/03.1-CONTEXT.md
@.planning/phases/03.1-plant-map-v0/03.1-SPEC.md
@.planning/phases/03.1-plant-map-v0/03.1-RESEARCH.md
@.planning/phases/03.1-plant-map-v0/03.1-UI-SPEC.md
@.planning/phases/03.1-plant-map-v0/03.1-PATTERNS.md
@.planning/phases/03.1-plant-map-v0/03.1-05-PLAN.md
@.planning/sketches/004-plant-map-v0/README.md
@src/components/AnonClassificationWall.tsx
@src/components/ui/sheet.tsx
@src/components/ui/button.tsx
@src/pages/MapPage.tsx
@src/hooks/use-wild-plants-with-coords.ts
@CLAUDE.md

<interfaces>
CONSUME:
- Plan 03: `WildPlantWithCoords` interface { id, name, imageUrl, lat, lng, createdAt, description, context: 'wild' }
- Plan 05: MapPage already has `useState<WildPlantWithCoords | null>(selectedPin, setSelectedPin)` with click handlers wired

CREATES:
```typescript
// src/components/PlantMapSheet.tsx (NEW)
export interface PlantMapSheetProps {
  plant: WildPlantWithCoords | null;
  onClose: () => void;
}
export function PlantMapSheet(props: PlantMapSheetProps): JSX.Element;
```

ANALOG primario: `src/components/AnonClassificationWall.tsx` (exact match — bottom sheet shadcn `Sheet side="bottom"` controlled by parent state, p-6 rounded-t-2xl, header + footer + 2 CTAs stacked).

Imports lockeados:
- `import { Sheet, SheetContent, SheetHeader, SheetTitle, SheetDescription, SheetFooter } from "@/components/ui/sheet"`
- `import { Button } from "@/components/ui/button"`
- `import { useNavigate } from "react-router-dom"`
- `import { format } from "date-fns"`
- `import { es } from "date-fns/locale/es"`

SHEET LAYOUT lockeado por sketch 004 winner + UI-SPEC líneas 380-491:
- Drag handle decorativo `w-9 h-1 bg-foreground/25 rounded-full mx-auto mb-5`
- Foto `aspect-[4/3] w-full object-cover rounded-xl border-2 border-foreground` (NOT square)
- SIN eyebrow chip "Descubrimiento" (sketch winner)
- Title: `font-display text-xl font-semibold leading-tight` (common name)
- Scientific: `font-body text-sm italic text-muted-foreground leading-snug`
- Date: `font-body text-sm text-muted-foreground`
- CTAs stacked vertical full-width `gap-2`
- Primary: `Button variant="hero"` "Ver detalle"
- Secondary: `Button variant="outline" border-2 border-foreground` "Cerrar"
- Padding: `p-6` + `pb-[max(1.5rem,env(safe-area-inset-bottom))]` para iOS home indicator
- max-h-[75dvh] + overflow-y-auto

FECHA FORMAT lockeado: `format(new Date(plant.createdAt), "d 'de' MMMM 'de' yyyy", { locale: es })` → e.g. "5 de marzo de 2026", prefijo "Identificada el ".

splitNameField helper (RESEARCH.md A2 fallback grácil):
- Input: `"Monstera deliciosa (Monstera deliciosa)"` → `{ commonName: "Monstera deliciosa", scientificName: "Monstera deliciosa" }`
- Input: `"Aloe vera"` → `{ commonName: "Aloe vera", scientificName: null }`
- Regex: `/^(.+?)\s*\(([^)]+)\)$/`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Crear src/components/PlantMapSheet.tsx (bottom sheet con preview)</name>
  <files>src/components/PlantMapSheet.tsx</files>
  <read_first>
    - src/components/AnonClassificationWall.tsx COMPLETO (analog primario — replicar shell verbatim: Sheet wrapper, SheetContent className, SheetHeader/Footer structure, button variants)
    - src/components/ui/sheet.tsx (verificar API de Sheet, SheetContent, SheetHeader, SheetTitle, SheetDescription, SheetFooter)
    - src/components/ui/button.tsx (verificar variantes `hero` y `outline` disponibles)
    - .planning/phases/03.1-plant-map-v0/03.1-PATTERNS.md líneas 340-437 (§ 9 PlantMapSheet delta)
    - .planning/phases/03.1-plant-map-v0/03.1-UI-SPEC.md líneas 380-491 (§ 2 PlantMapSheet — visual structure verbatim)
    - .planning/phases/03.1-plant-map-v0/03.1-RESEARCH.md líneas 860-958 (§ R6 skeleton + splitNameField + fallback)
    - .planning/phases/03.1-plant-map-v0/03.1-CONTEXT.md D-08
    - .planning/sketches/004-plant-map-v0/README.md (winner LOCKS: foto 4:3, sin eyebrow, CTAs stacked vertical)
    - src/hooks/use-wild-plants-with-coords.ts (interface WildPlantWithCoords)
  </read_first>
  <action>
    Crear `src/components/PlantMapSheet.tsx` con el contenido exacto siguiente. Replicar el shell de `AnonClassificationWall.tsx` con las modificaciones de UI-SPEC líneas 380-491:

    ```tsx
    import { useNavigate } from "react-router-dom";
    import { format } from "date-fns";
    import { es } from "date-fns/locale/es";
    import {
      Sheet,
      SheetContent,
      SheetDescription,
      SheetFooter,
      SheetHeader,
      SheetTitle,
    } from "@/components/ui/sheet";
    import { Button } from "@/components/ui/button";
    import type { WildPlantWithCoords } from "@/hooks/use-wild-plants-with-coords";

    export interface PlantMapSheetProps {
      plant: WildPlantWithCoords | null;
      onClose: () => void;
    }

    /**
     * Split a `name` field that follows the legacy `"Common (Scientific)"` format
     * into its two parts. Fallback grácil per RESEARCH.md A2:
     *   - If the regex matches → both common and scientific are returned
     *   - If not → the raw string is treated as common and scientific is null
     *
     * The hook (use-wild-plants-with-coords) does NOT split because some rows
     * may have the legacy format and some may have a plain common name.
     */
    function splitNameField(name: string): {
      commonName: string;
      scientificName: string | null;
    } {
      const match = name.match(/^(.+?)\s*\(([^)]+)\)$/);
      if (match) {
        return {
          commonName: match[1].trim(),
          scientificName: match[2].trim(),
        };
      }
      return { commonName: name, scientificName: null };
    }

    /**
     * Bottom sheet shown when a pin is tapped on the map.
     *
     * Visual lock per sketch 004 winner (Variant A) + UI-SPEC §2:
     *   - Drag handle decorative bar (top)
     *   - Photo aspect-[4/3] (NOT square; phone photos are typically portrait)
     *   - Common name (Fraunces, semibold, text-xl)
     *   - Scientific name (italic, muted-foreground, text-sm)
     *   - Date "Identificada el {d} de {MMMM} de {yyyy}" via date-fns locale es
     *   - 2 CTAs stacked vertical (NOT side-by-side):
     *       Primary "Ver detalle" → navigate(`/planta/${plant.id}`)
     *       Secondary "Cerrar"  → onClose()
     *   - NO eyebrow chip "Descubrimiento" (redundant on /mapa surface)
     *
     * Sheet behavior:
     *   - Drag-to-close NOT supported (shadcn Sheet limitation, D-08 locks
     *     this trade-off). Close via "Cerrar" button, tap-on-overlay, or Esc.
     *   - max-h-[75dvh] preserves spatial context (≥25% of map remains visible).
     *   - pb-[max(1.5rem,env(safe-area-inset-bottom))] for iOS home indicator
     *     (Pitfall 6 of RESEARCH.md).
     *
     * Tracking: Plan 07 will add `track("map_navigated_to_detail", ...)` in
     * the handleViewDetail handler before the navigate() call.
     */
    export function PlantMapSheet({ plant, onClose }: PlantMapSheetProps) {
      const navigate = useNavigate();
      const open = plant !== null;

      const handleViewDetail = () => {
        if (!plant) return;
        // Plan 07 will add: track("map_navigated_to_detail", { plant_search_id: plant.id, from: "pin_sheet" })
        navigate(`/planta/${plant.id}`);
      };

      // Don't even render the Sheet shell when there's no selection. Saves
      // a render cycle and keeps the DOM clean.
      if (!plant) return null;

      const { commonName, scientificName } = splitNameField(plant.name);
      const dateLabel = `Identificada el ${format(new Date(plant.createdAt), "d 'de' MMMM 'de' yyyy", { locale: es })}`;

      return (
        <Sheet open={open} onOpenChange={(o) => { if (!o) onClose(); }}>
          <SheetContent
            side="bottom"
            className="bg-background border-t-2 border-foreground rounded-t-2xl p-6 max-h-[75dvh] overflow-y-auto pb-[max(1.5rem,env(safe-area-inset-bottom))]"
          >
            {/* Drag handle decorative bar (matches AnonClassificationWall pattern) */}
            <div
              className="w-9 h-1 bg-foreground/25 rounded-full mx-auto mb-5"
              aria-hidden
            />

            {/* Plant photo — aspect-[4/3] per sketch winner, NOT square */}
            <img
              src={plant.imageUrl}
              alt={commonName}
              loading="lazy"
              className="aspect-[4/3] w-full object-cover rounded-xl border-2 border-foreground mb-4"
            />

            <SheetHeader className="items-start text-left gap-1 p-0">
              <SheetTitle className="font-display text-xl font-semibold leading-tight text-foreground">
                {commonName}
              </SheetTitle>
              {scientificName && (
                <p className="font-body text-sm italic text-muted-foreground leading-snug">
                  {scientificName}
                </p>
              )}
            </SheetHeader>

            <SheetDescription className="font-body text-sm text-muted-foreground mt-3 mb-6">
              {dateLabel}
            </SheetDescription>

            <SheetFooter className="flex flex-col gap-2 sm:flex-col p-0">
              <Button
                variant="hero"
                size="default"
                onClick={handleViewDetail}
                className="w-full font-display text-base font-bold"
              >
                Ver detalle
              </Button>
              <Button
                variant="outline"
                size="default"
                onClick={onClose}
                className="w-full font-body text-base font-semibold border-2 border-foreground"
              >
                Cerrar
              </Button>
            </SheetFooter>
          </SheetContent>
        </Sheet>
      );
    }
    ```

    Decisiones implementadas:
    - **D-08** (CONTEXT.md): shadcn `Sheet side="bottom"`, drag-to-close NO supported (close via button/overlay/Esc only)
    - **Sketch winner Variant A**: foto `aspect-[4/3]`, SIN eyebrow chip, CTAs stacked vertical full-width
    - **UI-SPEC línea 391**: `max-h-[75dvh] overflow-y-auto pb-[max(1.5rem,env(safe-area-inset-bottom))]`
    - **UI-SPEC línea 394**: drag handle `w-9 h-1 bg-foreground/25 rounded-full mx-auto mb-5`
    - **UI-SPEC líneas 401-417**: photo class + SheetHeader + SheetTitle classes
    - **UI-SPEC líneas 425-441**: SheetFooter classes + Button variants
    - **RESEARCH.md A2 fallback**: splitNameField con regex `/^(.+?)\s*\(([^)]+)\)$/` y fallback {commonName: raw, scientificName: null}
    - **UI-SPEC línea 215**: dateLabel format string exacto
    - **A11y**: `aria-hidden` en drag handle (no semantic content), `loading="lazy"` en img, alt={commonName} (significativo en español)
    - **Plan 07 anchor**: comentario inline marca dónde Plan 07 añadirá `track("map_navigated_to_detail", ...)` antes del navigate

    `sm:flex-col` en SheetFooter — esto FUERZA el stacked vertical incluso en breakpoints sm+. Las `SheetFooter` de shadcn por defecto cambian a `sm:flex-row` desde un cierto breakpoint, lo cual rompería el sketch winner.

    `onOpenChange={(o) => { if (!o) onClose(); }}` — patrón estándar para sheets controlados externamente. Cualquier intent de cerrar (overlay tap, Esc, X close button de SheetContent) llama onClose.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/components/PlantMapSheet.tsx','utf8');const checks={hasSheetImport:s.includes('@/components/ui/sheet'),hasButtonImport:s.includes('@/components/ui/button'),hasNavigateImport:s.includes('useNavigate'),hasDateFnsFormat:s.includes('import { format } from \"date-fns\"'),hasDateFnsLocaleEs:s.includes('from \"date-fns/locale/es\"'),hasWildPlantType:s.includes('WildPlantWithCoords'),hasSplitNameField:s.includes('function splitNameField'),hasSplitRegex:s.includes('match(/^(.+?)'),hasFallback:s.includes('scientificName: null'),hasAspect43:s.includes('aspect-[4/3]'),hasNoAspectSquare:!s.includes('aspect-square'),hasStackedFooter:s.includes('flex flex-col gap-2 sm:flex-col'),hasNoEyebrowChip:!s.includes('Descubrimiento'),hasVerDetalle:s.includes('Ver detalle'),hasCerrar:s.includes('Cerrar'),hasIdentificadaEl:s.includes('Identificada el'),hasNavigatePlantaId:s.includes('navigate(`/planta/'),hasMaxH75dvh:s.includes('max-h-[75dvh]'),hasSafeAreaPb:s.includes('pb-[max(1.5rem,env(safe-area-inset-bottom))]'),hasDragHandle:s.includes('w-9 h-1 bg-foreground/25 rounded-full mx-auto mb-5'),hasOpenChangeHandler:s.includes('onOpenChange={(o) =>'),hasHeroVariant:s.includes('variant=\"hero\"'),hasOutlineVariant:s.includes('variant=\"outline\"'),hasBorder2Foreground:s.includes('border-2 border-foreground'),hasReturnNullEmpty:s.includes('if (!plant) return null'),hasExportPlantMapSheet:s.includes('export function PlantMapSheet'),hasExportProps:s.includes('export interface PlantMapSheetProps')};const fails=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);if(fails.length){console.error('FAIL:',fails);process.exit(1);}console.log('OK PlantMapSheet (' + Object.keys(checks).length + ' checks)');"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/PlantMapSheet.tsx` existe y exporta named `PlantMapSheet` + `PlantMapSheetProps`
    - Importa Sheet/SheetContent/SheetHeader/SheetTitle/SheetDescription/SheetFooter desde `@/components/ui/sheet`
    - Importa Button desde `@/components/ui/button`
    - Importa `useNavigate` de react-router-dom
    - Importa `format` de date-fns y `es` de date-fns/locale/es
    - Importa tipo `WildPlantWithCoords` de `@/hooks/use-wild-plants-with-coords`
    - Define `splitNameField` (función interna, no exportada) con regex de extracción
    - Contiene `aspect-[4/3]` (NO `aspect-square`)
    - Contiene `flex flex-col gap-2 sm:flex-col` (forzar stacked vertical en footer)
    - NO contiene la palabra `Descubrimiento` (sin eyebrow chip per sketch winner)
    - Contiene el texto `Ver detalle` (CTA primario)
    - Contiene el texto `Cerrar` (CTA secundario)
    - Contiene el prefijo `Identificada el` y format string `"d 'de' MMMM 'de' yyyy"`
    - Contiene `navigate(\`/planta/${plant.id}\`)` en handleViewDetail
    - Contiene `max-h-[75dvh] overflow-y-auto`
    - Contiene `pb-[max(1.5rem,env(safe-area-inset-bottom))]`
    - Contiene drag handle `w-9 h-1 bg-foreground/25 rounded-full mx-auto mb-5` con `aria-hidden`
    - Contiene `if (!plant) return null` (early return optimization)
    - Contiene `Button variant="hero"` (primary) y `Button variant="outline"` con `border-2 border-foreground` (secondary)
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>PlantMapSheet creado, sticker style, foto 4:3, stacked CTAs, sin eyebrow, splitNameField con fallback, safe-area-inset-bottom, max-h 75dvh.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Conectar PlantMapSheet en MapPage.tsx (1 import + 1 JSX line)</name>
  <files>src/pages/MapPage.tsx</files>
  <read_first>
    - src/pages/MapPage.tsx (creado en Plan 05 — verificar que selectedPin/setSelectedPin state ya existe y los onClick handlers ya setean selectedPin)
    - src/components/PlantMapSheet.tsx (recién creado en Task 1)
    - .planning/phases/03.1-plant-map-v0/03.1-05-PLAN.md (verificar que el placeholder `{/* Plan 06 will mount: ... */}` está dentro del wrapper relative)
  </read_first>
  <action>
    Modificar `src/pages/MapPage.tsx` con 2 cambios mínimos:

    **Cambio 1** — añadir import después de los otros imports `@/components/...` o `@/lib/...`:

    ```tsx
    import { PlantMapSheet } from "@/components/PlantMapSheet";
    ```

    **Cambio 2** — reemplazar el comentario placeholder `{/* Plan 06 will mount: <PlantMapSheet ... /> */}` (creado en Plan 05) con el JSX real:

    ```tsx
    <PlantMapSheet plant={selectedPin} onClose={() => setSelectedPin(null)} />
    ```

    Posición: debe ser SIBLING del `<MapContainer>`, no child (el Sheet renderiza un overlay que cubre toda la pantalla, no debe estar dentro del map container).

    Adicionalmente: REMOVER el comentario `// eslint-disable-next-line @typescript-eslint/no-unused-vars` arriba del `useState<WildPlantWithCoords | null>(null)` — ahora `selectedPin` SÍ se usa (en el JSX del sheet) y `setSelectedPin` también. El lint disable ya no es necesario.

    NO modificar:
    - El hook `useWildPlantsWithCoords()`
    - Los useEffect existentes (document.title, map_opened tracking)
    - El branch loading
    - El branch empty (`<Navigate to="/" />`)
    - La estructura del MapContainer + TileLayer + Marker + FitBoundsOnMount
    - El track call de `map_opened` (Plan 07 actualizará props si necesario)

    Estado final esperado del bloque main return:

    ```tsx
    return (
      <div
        className="relative w-full"
        style={{
          height: "calc(100dvh - 4rem - env(safe-area-inset-bottom) - env(safe-area-inset-top))",
        }}
        role="application"
        aria-label="Mapa de descubrimientos de plantas"
      >
        <MapContainer ...>
          <TileLayer .../>
          {plants.map((plant) => (
            <Marker .../>
          ))}
          <FitBoundsOnMount pins={...} />
        </MapContainer>
        <PlantMapSheet plant={selectedPin} onClose={() => setSelectedPin(null)} />
      </div>
    );
    ```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/pages/MapPage.tsx','utf8');const checks={hasImport:s.includes('import { PlantMapSheet } from \"@/components/PlantMapSheet\"'),hasJSX:s.includes('<PlantMapSheet plant={selectedPin} onClose={'),hasOnCloseHandler:s.includes('setSelectedPin(null)'),preservesMapContainer:s.includes('<MapContainer'),preservesFitBounds:s.includes('<FitBoundsOnMount'),removedPlaceholderComment:!s.includes('Plan 06 will mount'),removedEslintDisable:!s.includes('@typescript-eslint/no-unused-vars'),sheetSiblingNotChild:(()=>{const idx=s.indexOf('<PlantMapSheet');const containerCloseIdx=s.indexOf('</MapContainer>');return idx>containerCloseIdx;})()};const fails=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);if(fails.length){console.error('FAIL:',fails);process.exit(1);}console.log('OK MapPage integration');"</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/MapPage.tsx` contiene `import { PlantMapSheet } from "@/components/PlantMapSheet"`
    - `src/pages/MapPage.tsx` contiene `<PlantMapSheet plant={selectedPin} onClose={() => setSelectedPin(null)} />`
    - El JSX `<PlantMapSheet ...>` aparece DESPUÉS de `</MapContainer>` (sibling, no child)
    - El comentario placeholder `{/* Plan 06 will mount: ... */}` ha sido removido
    - El `// eslint-disable-next-line @typescript-eslint/no-unused-vars` ha sido removido
    - El track call `track("map_opened", ...)` sigue presente
    - El branch `<Navigate to="/" replace />` sigue presente
    - `npx tsc --noEmit` exit 0
    - `npm run build` exit 0
  </acceptance_criteria>
  <done>MapPage integra PlantMapSheet como sibling del MapContainer; sheet reactivo a selectedPin state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client-internal | Sheet renderiza `plant.name`, `plant.scientificName`, `plant.imageUrl` — datos vienen de Supabase del propio user (filtrados por RLS + filter en hook) |
| client→React Router | navigate(`/planta/${plant.id}`) consume URL — plant.id es UUID de DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-19 | XSS | plant.name/scientificName renderizado en el Sheet via React | mitigate | React escape por defecto. NO usar `dangerouslySetInnerHTML`. Verificable: grep "dangerouslySetInnerHTML" en PlantMapSheet.tsx debe estar vacío. |
| T-03.1-20 | XSS | plant.imageUrl en `<img src={...}>` | mitigate | `src` attribute con URL externa NO ejecuta JS (browsers no eval src). Si imageUrl fuera `javascript:...`, el browser bloquea. Worst case: 404 → placeholder. |
| T-03.1-21 | Open redirect | navigate(`/planta/${plant.id}`) | mitigate | `plant.id` viene de Supabase con UUID schema. Si fuera comprometido a `../../escape`, react-router lo resolvería contra la base — no escape de la app. La ruta `/planta/:id` existe en App.tsx y renderiza PlantResultView (Phase 02.1). |
| T-03.1-22 | Pitfall 4 RESEARCH.md | Marker click propagation cierra el Sheet inmediatamente | mitigate | Plan 06 NO añade `e.originalEvent.stopPropagation()` explícitamente (per RESEARCH.md A4: el `eventHandlers.click` de Leaflet maneja propagation correctamente por defecto). Si emerge en QA, añadir el stop como hotfix; cubierto por Plan 08 E2E test. |
</threat_model>

<verification>
- `src/components/PlantMapSheet.tsx` existe con >= 80 LOC, exporta PlantMapSheet
- `src/pages/MapPage.tsx` monta PlantMapSheet como sibling de MapContainer
- `npx tsc --noEmit` exit 0
- `npm run build` exit 0
- `npm test -- --run` sin regresiones
- Cross-platform:
  - **iOS PWA standalone**: `pb-[max(1.5rem,env(safe-area-inset-bottom))]` cubre home indicator (Pitfall 6 RESEARCH.md)
  - **Android Capacitor WebView 8**: `aspect-[4/3]` y `max-h-[75dvh]` soportados (Chromium 121+)
  - **Reduced motion**: Radix Sheet respeta `prefers-reduced-motion` nativamente (UI-SPEC línea 482)
- A11y:
  - `aria-hidden` en drag handle (no semantic value)
  - `alt={commonName}` en img (significativo, no decorativo)
  - Sheet trap focus per Radix (default)
  - SheetTitle y SheetDescription wired a aria-labelledby/aria-describedby por Radix
</verification>

<success_criteria>
- Sheet visible cuando user tapea pin (SPEC-AC7)
- Sheet muestra foto/common name/scientific name/fecha en español
- Tap "Ver detalle" → URL cambia a /planta/{id} (SPEC-AC8)
- Tap "Cerrar" o overlay/Esc → sheet se cierra
- splitNameField con fallback grácil para `name` raw sin formato (RESEARCH.md A2)
- Sheet respeta safe-area-inset-bottom (iOS home indicator)
- No regresión de pin rendering (sigue mostrando N markers)
- TypeScript compila, build pasa
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-plant-map-v0/03.1-06-SUMMARY.md`
</output>
