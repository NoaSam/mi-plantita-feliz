---
phase: 03.1-plant-map-v0
plan: 04
type: execute
wave: 2
depends_on: [02]
files_modified:
  - src/components/BottomTabBar.tsx
  - src/hooks/use-classify-plant.ts
autonomous: true
requirements: [SPEC-R1, SPEC-AC1, SPEC-AC2, SPEC-AC3]
requirements_addressed: [SPEC-R1, SPEC-AC1, SPEC-AC2, SPEC-AC3]
tags: [phase-03.1, bottom-tab-bar, conditional-tabs, classify-event]

must_haves:
  truths:
    - "El BottomTabBar muestra la tab 'Mapa' (icono MapPin) cuando el usuario logueado tiene wild_with_coords >= 1"
    - "Para usuario anónimo (sin sesión), la tab 'Mapa' nunca se renderiza (D-07, SPEC-AC3)"
    - "Para usuario logueado SIN wild-con-coords, la tab 'Mapa' NO se renderiza (SPEC-AC1)"
    - "Tras clasificar la primera wild con coords como wild, la tab 'Mapa' aparece sin reload del browser (SPEC-AC2)"
    - "El BottomTabBar sigue siendo visible y funcional mientras useContextCounts está loading (landmine #2)"
    - "Las 2-3 tabs existentes (Inicio, Mis plantas, Ajustes condicional auth) siguen funcionando idénticas — solo la lógica de cómputo cambia"
  artifacts:
    - path: "src/components/BottomTabBar.tsx"
      provides: "Tab bar con array computado vía useMemo desde useContextCounts; tab 'Mapa' condicional"
      contains: "useContextCounts"
    - path: "src/hooks/use-classify-plant.ts"
      provides: "Dispatch de window CustomEvent 'mp:pending-classification-resolved' tras UPDATE exitoso"
      contains: "mp:pending-classification-resolved"
  key_links:
    - from: "src/components/BottomTabBar.tsx"
      to: "useContextCounts (Plan 02)"
      via: "wild_with_coords >= 1 gate"
      pattern: "useContextCounts"
    - from: "src/hooks/use-classify-plant.ts"
      to: "window CustomEvent → useContextCounts listener (Plan 02)"
      via: "window.dispatchEvent tras supabase update"
      pattern: "window.dispatchEvent"
---

<objective>
Modificar `src/components/BottomTabBar.tsx` para que las tabs se computen reactivamente vía `useMemo` desde `useAuth()` + `useContextCounts()` (Plan 02), reemplazando los arrays hard-coded `publicTabs`/`authTabs`. La tab "Mapa" (label exacto, icon `MapPin` de lucide-react, path `/mapa`) se inserta SOLO si `user && wild_with_coords >= 1`. La render JSX (`<nav>` wrapper + per-tab `<button>` markup) queda byte-identical (PATTERNS.md líneas 146-165).

Adicionalmente, modificar `src/hooks/use-classify-plant.ts` para dispatchar `window.dispatchEvent(new CustomEvent("mp:pending-classification-resolved", { detail: { plant_search_id, action } }))` tras un UPDATE exitoso. Esto cierra el landmine #4 de PATTERNS.md y permite que `useContextCounts` (Plan 02) refetche reactivamente, cumpliendo SPEC-AC2.

Purpose: Wave 2 trabajo de integración. Sin este plan, la tab "Mapa" no aparece reactivamente y los Plans 05-07 no son alcanzables por navegación natural (solo deep-link). Conecta el hook de Plan 02 con la barra navegación.
Output: 1 archivo refactorizado + 1 archivo con ~5 LOC de dispatch añadidas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03.1-plant-map-v0/03.1-CONTEXT.md
@.planning/phases/03.1-plant-map-v0/03.1-SPEC.md
@.planning/phases/03.1-plant-map-v0/03.1-RESEARCH.md
@.planning/phases/03.1-plant-map-v0/03.1-UI-SPEC.md
@.planning/phases/03.1-plant-map-v0/03.1-PATTERNS.md
@.planning/phases/03.1-plant-map-v0/03.1-02-PLAN.md
@src/components/BottomTabBar.tsx
@src/hooks/use-classify-plant.ts
@src/contexts/AuthContext.tsx
@src/components/UnclassifiedSection.tsx
@CLAUDE.md

<interfaces>
CONSUME del Plan 02 (use-context-counts.ts):

```typescript
import { useContextCounts } from "@/hooks/use-context-counts";
const { wild_with_coords, isLoading } = useContextCounts();
// wild_with_coords: number (count exacto)
// isLoading: boolean (NO bloquear render del BottomTabBar; landmine #2)
```

INTERFACE existente: src/components/BottomTabBar.tsx actual (lines 5-21 hard-coded arrays).
ANALOG primario: self (líneas 1-22 son el current pattern a refactorizar).
ANALOG secundario: src/components/UnclassifiedSection.tsx líneas 18-31 (consume counter hook).

DISPATCH pattern (replicar AuthContext.tsx líneas 57-61):

```typescript
window.dispatchEvent(
  new CustomEvent("mp:pending-classification-resolved", {
    detail: { plant_search_id: plantSearchId, action },
  }),
);
```

TAB ORDER lockeado por CONTEXT.md D-04 y UI-SPEC.md líneas 586-602:
- No-auth:  Inicio · Mis plantas
- Auth:     Inicio · [Mapa si wild_with_coords>=1] · Mis plantas · Ajustes
- (Phase 3 insertará "Mi jardín" entre Inicio y Mapa — fuera de scope)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Refactor src/components/BottomTabBar.tsx — tabs computadas con useMemo desde useContextCounts</name>
  <files>src/components/BottomTabBar.tsx</files>
  <read_first>
    - src/components/BottomTabBar.tsx (CURRENT — entender líneas 1-22 hardcoded + líneas 23-50 render JSX)
    - src/components/UnclassifiedSection.tsx (analog secundario — consume `useUnclassifiedCount()` líneas 18-31)
    - src/hooks/use-context-counts.ts (creado en Plan 02 — interface UseContextCountsReturn)
    - .planning/phases/03.1-plant-map-v0/03.1-PATTERNS.md líneas 107-187 (§ 4 — delta exacto)
    - .planning/phases/03.1-plant-map-v0/03.1-UI-SPEC.md líneas 555-668 (§ 4 BottomTabBar MODIFIED — target state verbatim)
    - .planning/phases/03.1-plant-map-v0/03.1-CONTEXT.md D-04, D-07
  </read_first>
  <action>
    Reemplazar el contenido COMPLETO de `src/components/BottomTabBar.tsx` con la versión refactorizada. Reglas:
    - Mantener el wrapper `<nav>` byte-identical (PATTERNS.md líneas 146-165, líneas 24-50 actuales)
    - Mantener el per-tab `<button>` markup byte-identical (active styling, strokeWidth, aria-label, aria-current)
    - Solo cambia: los 2 arrays hardcoded → `useMemo` que computa el array de tabs

    Contenido final exacto (~70 LOC):

    ```tsx
    import { useMemo } from "react";
    import { useLocation, useNavigate } from "react-router-dom";
    import { Leaf, BookOpen, Settings, MapPin } from "lucide-react";
    import type { LucideIcon } from "lucide-react";
    import { useAuth } from "@/hooks/use-auth";
    import { useContextCounts } from "@/hooks/use-context-counts";

    interface Tab {
      path: string;
      label: string;
      icon: LucideIcon;
    }

    /**
     * BottomTabBar — fija al borde inferior, max-w-md mobile-first.
     *
     * Phase 03.1: tabs computadas reactivamente desde useAuth() +
     * useContextCounts(). La tab "Mapa" aparece para usuarios logueados con
     * >= 1 planta wild con coordenadas (D-04, D-07, SPEC-AC1/AC2/AC3).
     *
     * Landmine #2 (PATTERNS.md): NO bloquear el render del bar entero por
     * isLoading de useContextCounts. Las tabs base se muestran siempre;
     * la tab "Mapa" simplemente no se incluye en el array hasta que el
     * count se resuelve (UI-SPEC línea 233 explicitly approves no-animation pop-in).
     *
     * Tab order (D-04): Inicio · [Mi jardín en Phase 3] · [Mapa si aplica]
     *                   · Mis plantas · [Ajustes si auth]
     */
    export default function BottomTabBar() {
      const location = useLocation();
      const navigate = useNavigate();
      const { user } = useAuth();
      const { wild_with_coords } = useContextCounts();

      const tabs: Tab[] = useMemo(() => {
        const result: Tab[] = [{ path: "/", label: "Inicio", icon: Leaf }];
        // Phase 3 insertará "Mi jardín" aquí cuando home_count >= 1 — fuera de scope.
        if (user && wild_with_coords >= 1) {
          result.push({ path: "/mapa", label: "Mapa", icon: MapPin });
        }
        result.push({ path: "/mis-plantas", label: "Mis plantas", icon: BookOpen });
        if (user) {
          result.push({ path: "/ajustes", label: "Ajustes", icon: Settings });
        }
        return result;
      }, [user, wild_with_coords]);

      // Render block — BYTE-IDENTICAL al original (lines 23-50). NO modificar.
      return (
        <nav className="fixed bottom-0 left-0 right-0 z-50 bg-background border-t-2 border-foreground/10 pb-safe">
          <div className="max-w-md mx-auto h-16 flex items-center justify-around">
            {tabs.map(({ path, label, icon: Icon }) => {
              const isActive = location.pathname === path;
              return (
                <button
                  key={path}
                  type="button"
                  onClick={() => navigate(path)}
                  className="flex flex-col items-center gap-0.5 py-2 px-4"
                  aria-label={label}
                  aria-current={isActive ? "page" : undefined}
                >
                  <Icon
                    className={`size-6 ${isActive ? "text-primary" : "text-muted-foreground"}`}
                    strokeWidth={isActive ? 2 : 1.5}
                  />
                  <span
                    className={`text-xs font-body ${isActive ? "text-primary font-semibold" : "text-muted-foreground"}`}
                  >
                    {label}
                  </span>
                </button>
              );
            })}
          </div>
        </nav>
      );
    }
    ```

    Decisiones implementadas:
    - **D-04**: tab order Inicio · [Mapa cond] · Mis plantas · [Ajustes cond]
    - **D-07**: gate `user && ...` para tab Mapa (anon nunca la ve)
    - **Landmine #2**: NO gate del bar entero por `isLoading` — solo la tab Mapa
    - **PATTERNS.md líneas 146-165**: render JSX byte-identical
    - **UI-SPEC línea 117**: aria-label="Mapa" + aria-current cuando active
    - **A11y**: `pb-safe` para iOS home indicator (ya estaba — no tocar)
    - Tipo `LucideIcon` (importado explícito) para tipar `icon` campo de Tab

    Verificar que las deps de useMemo son `[user, wild_with_coords]` — sin estos deps el array no se recompila cuando cambian.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/components/BottomTabBar.tsx','utf8');const checks={hasUseMemo:s.includes('useMemo'),hasUseContextCounts:s.includes('useContextCounts'),hasMapPinImport:/import.*MapPin.*from\\s+\"lucide-react\"/.test(s),hasMapaTab:s.includes('\"Mapa\"'),hasMapaPath:s.includes('\"/mapa\"'),hasWildGate:s.includes('wild_with_coords >= 1'),hasUserGate:s.includes('user && wild_with_coords'),hasNoOldPublicTabs:!s.includes('publicTabs'),hasNoOldAuthTabs:!s.includes('authTabs'),preservesNavClass:s.includes('fixed bottom-0 left-0 right-0 z-50 bg-background border-t-2 border-foreground/10 pb-safe'),preservesAriaLabel:s.includes('aria-label={label}')};const fails=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);if(fails.length){console.error('FAIL:',fails);process.exit(1);}console.log('OK BottomTabBar refactor');"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/BottomTabBar.tsx` importa `useMemo` desde "react"
    - `src/components/BottomTabBar.tsx` importa `MapPin` desde "lucide-react" (junto a Leaf, BookOpen, Settings)
    - `src/components/BottomTabBar.tsx` importa `useContextCounts` desde "@/hooks/use-context-counts"
    - El archivo NO contiene las constantes `publicTabs` ni `authTabs` (refactor completo)
    - El archivo contiene el string `"Mapa"` (label de tab)
    - El archivo contiene `"/mapa"` (path de tab)
    - El archivo contiene la expresión `user && wild_with_coords >= 1`
    - El wrapper `<nav className="fixed bottom-0 left-0 right-0 z-50 bg-background border-t-2 border-foreground/10 pb-safe">` está intacto
    - Las dependencias del useMemo son exactamente `[user, wild_with_coords]`
    - `npx tsc --noEmit` exit 0
    - `npm run build` completa exit 0
  </acceptance_criteria>
  <done>BottomTabBar.tsx refactorizado: tabs vía useMemo, tab "Mapa" condicional, render JSX byte-identical, TypeScript compila.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Añadir dispatch de CustomEvent en src/hooks/use-classify-plant.ts tras UPDATE exitoso</name>
  <files>src/hooks/use-classify-plant.ts</files>
  <read_first>
    - src/hooks/use-classify-plant.ts (CURRENT — entender funciones `classify` y `revert`, ambas hacen supabase update)
    - src/contexts/AuthContext.tsx líneas 50-62 (analog del dispatch pattern verbatim)
    - .planning/phases/03.1-plant-map-v0/03.1-PATTERNS.md líneas 813-833 (Pattern S4 dispatcher + listener)
    - .planning/phases/03.1-plant-map-v0/03.1-PATTERNS.md líneas 870-872 (landmine #4 explica el gap actual)
    - .planning/phases/03.1-plant-map-v0/03.1-RESEARCH.md líneas 672-676 + 1083-1089 (Pitfall 5 — sin este dispatch, AC2 falla)
    - .planning/phases/03.1-plant-map-v0/03.1-02-PLAN.md (verificar que useContextCounts ya tiene el listener)
  </read_first>
  <action>
    Modificar `src/hooks/use-classify-plant.ts` para que, tras un UPDATE exitoso en `classify(...)`, dispatche un CustomEvent que cause re-fetch en `useContextCounts`. NO modificar la firma ni el return de la función — solo añadir el dispatch dentro del bloque `if (!error)` (equivalentemente: justo antes del `return { ok: true as const }`).

    Cambio exacto: en la función `classify` (líneas 17-28 actuales del archivo), tras el `if (error)` que retorna fail, añadir el dispatch ANTES del return success. También hacerlo en `revert` (líneas 30-41) por simetría — revertir a 'unclassified' también cambia counts.

    Contenido final exacto de `classify` y `revert` (líneas 17-41 modificadas):

    ```typescript
    export function useClassifyPlant(): UseClassifyPlantReturn {
      const classify = useCallback(async (plantSearchId: string, action: ClassifyAction) => {
        const { error } = await supabase
          .from("plant_searches")
          .update({ context: action })
          .eq("id", plantSearchId);

        if (error) {
          console.error("[classify] update failed:", error.message);
          return { ok: false as const };
        }
        // Phase 03.1 (landmine #4 / Pitfall 5): dispatch para que
        // useContextCounts (BottomTabBar) refetche y la tab "Mapa" aparezca
        // reactivamente sin reload. Pattern S4 de PATTERNS.md, mismo channel
        // que AuthContext.tsx usa en el flujo anon→auth.
        if (typeof window !== "undefined") {
          window.dispatchEvent(
            new CustomEvent("mp:pending-classification-resolved", {
              detail: { plant_search_id: plantSearchId, action },
            }),
          );
        }
        return { ok: true as const };
      }, []);

      const revert = useCallback(async (plantSearchId: string) => {
        const { error } = await supabase
          .from("plant_searches")
          .update({ context: 'unclassified' })
          .eq("id", plantSearchId);

        if (error) {
          console.error("[classify] revert failed:", error.message);
          return { ok: false as const };
        }
        // Mismo dispatch: revertir también muta counts (un wild→unclassified
        // decrementa wild_with_coords, potencialmente ocultando la tab Mapa).
        if (typeof window !== "undefined") {
          window.dispatchEvent(
            new CustomEvent("mp:pending-classification-resolved", {
              detail: { plant_search_id: plantSearchId, action: 'unclassified' },
            }),
          );
        }
        return { ok: true as const };
      }, []);

      return { classify, revert };
    }
    ```

    Decisiones implementadas:
    - **Pattern S4** (PATTERNS.md): mismo channel `mp:pending-classification-resolved` usado por AuthContext (no inventar uno nuevo — consistency)
    - **Landmine #4 cerrado**: classify ahora dispatcha (antes NO lo hacía — solo AuthContext lo dispatchaba en flujo anon→auth)
    - **Defensa SSR**: `typeof window !== "undefined"` guard (consistent con código defensivo del proyecto en lugares que tocan window)
    - **Detail payload**: incluye `plant_search_id` y `action` por simetría con AuthContext (línea 59), aunque el listener actual de useContextCounts no consume el detail (lo descarta). Útil para futuros listeners (HistorySummary, etc.).
    - **`revert` también dispatcha**: simétrico — un revert también puede ocultar la tab Mapa si era la única wild-with-coords.

    NO modificar:
    - La firma `UseClassifyPlantReturn`
    - El export `UNDO_WINDOW_MS`
    - El export `ClassifyAction`
    - Cualquier otro código del archivo
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/hooks/use-classify-plant.ts','utf8');const dispatches=(s.match(/window\\.dispatchEvent/g)||[]).length;const events=(s.match(/mp:pending-classification-resolved/g)||[]).length;const guards=(s.match(/typeof window !== \"undefined\"/g)||[]).length;if(dispatches!==2){console.error('Expected 2 dispatchEvent calls (classify+revert), got',dispatches);process.exit(1);}if(events!==2){console.error('Expected 2 event-name strings, got',events);process.exit(1);}if(guards!==2){console.error('Expected 2 SSR guards, got',guards);process.exit(1);}if(!s.includes('detail:')){console.error('Missing detail payload');process.exit(1);}console.log('OK classify+revert both dispatch');"</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/use-classify-plant.ts` contiene `window.dispatchEvent` exactamente 2 veces (una en classify, una en revert)
    - `src/hooks/use-classify-plant.ts` contiene `mp:pending-classification-resolved` exactamente 2 veces
    - `src/hooks/use-classify-plant.ts` contiene `typeof window !== "undefined"` exactamente 2 veces (SSR guard)
    - El dispatch en `classify` incluye `detail: { plant_search_id: plantSearchId, action }`
    - El dispatch en `revert` incluye `detail: { plant_search_id: plantSearchId, action: 'unclassified' }`
    - La firma del export `useClassifyPlant` NO cambia (`UseClassifyPlantReturn` intacta)
    - El export `UNDO_WINDOW_MS = 5000` sigue presente
    - `npx tsc --noEmit` exit 0
    - Los tests existentes que pudieran tocar este hook siguen pasando: `npm test -- --run 2>&1 | grep -E "(failed|passed)"` no muestra failed
  </acceptance_criteria>
  <done>useClassifyPlant.ts dispatcha el CustomEvent tras classify Y revert; SSR guarded; firma sin cambios.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client-internal | window CustomEvent es un canal interno entre módulos del mismo origen — no cruza boundaries de auth ni network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-10 | Tampering | window CustomEvent `mp:pending-classification-resolved` payload | accept | El listener de useContextCounts ignora el detail (solo invoca load()). Si código rogue del proyecto dispatcha con detail malicioso, no se consume. Sin sufijo de seguridad. |
| T-03.1-11 | DoS | Spam de dispatches si el usuario clasifica/reverte rápido (5s undo window) | accept | Cada dispatch causa 4 head-only count queries. Costo trivial. |
| T-03.1-12 | Open redirect | Tab "Mapa" navega a `/mapa` constante hardcoded | mitigate | Path es literal string, no derivado de input. Sin riesgo de redirect injection. |
</threat_model>

<verification>
- `src/components/BottomTabBar.tsx` ya no tiene `publicTabs`/`authTabs`, tiene useMemo + useContextCounts + import MapPin
- `src/hooks/use-classify-plant.ts` dispatcha CustomEvent 2 veces (classify + revert)
- `npx tsc --noEmit` exit 0
- `npm run build` exit 0
- `npm test -- --run` no muestra failed tests existentes
- Cross-platform: window CustomEvent es estándar Web API soportado en Capacitor WebView 8 y Safari 16+ PWA standalone
- A11y: aria-label="Mapa" + aria-current="page" preservados; pb-safe del wrapper intacto para iOS home indicator
</verification>

<success_criteria>
- Tab "Mapa" condicional aparece para auth + wild_with_coords>=1
- Tab "Mapa" NO aparece para anon (D-07, SPEC-AC3)
- Tab "Mapa" NO aparece para auth sin wild-con-coords (SPEC-AC1)
- Tras classify (wild con coords), tab aparece sin reload (SPEC-AC2 vía dispatch + listener)
- Render JSX del bar byte-identical (no regresiones visuales)
- TypeScript compila, tests existentes pasan
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-plant-map-v0/03.1-04-SUMMARY.md`
</output>
